/*
  File INTERPRET_ACCESS_FILExADVANCE_CP.c generated by XCOM-I, 2024-08-09
  12:38:15.
*/

#include "runtimeC.h"

int32_t
INTERPRET_ACCESS_FILExADVANCE_CP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard
      = guardReentry (reentryGuard, "INTERPRET_ACCESS_FILExADVANCE_CP");
  // CP = CP + 1; (2790)
  {
    int32_t numberRHS
        = (int32_t)(xadd (COREHALFWORD (mINTERPRET_ACCESS_FILExCP), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mINTERPRET_ACCESS_FILExCP, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF CP > TEXT_LIMIT THEN (2791)
  if (1
      & (xGT (COREHALFWORD (mINTERPRET_ACCESS_FILExCP),
              COREHALFWORD (mTEXT_LIMIT))))
    // DO; (2792)
    {
    rs1:;
      // IF EOF_FLAG THEN (2793)
      if (1
          & (bitToFixed (
              getBIT (1, mINTERPRET_ACCESS_FILExADVANCE_CPxEOF_FLAG))))
        // GO TO END_FILE_EXIT; (2794)
        {
          resetAllReentryGuards ();
          longjmp (jbINTERPRET_ACCESS_FILExEND_FILE_EXIT, 1);
        }
    // READIT: (2795)
    READIT:
      // S = INPUT(6); (2796)
      {
        descriptor_t *stringRHS;
        stringRHS = INPUT (6);
        putCHARACTER (mINTERPRET_ACCESS_FILExS, stringRHS);
        stringRHS->inUse = 0;
      }
      // IF LENGTH(S) = 0 THEN (2797)
      if (1 & (xEQ (LENGTH (getCHARACTER (mINTERPRET_ACCESS_FILExS)), 0)))
        // DO; (2798)
        {
        rs1s1:;
          // S = X70 || X70; (2799)
          {
            descriptor_t *stringRHS;
            stringRHS = xsCAT (getCHARACTER (mX70), getCHARACTER (mX70));
            putCHARACTER (mINTERPRET_ACCESS_FILExS, stringRHS);
            stringRHS->inUse = 0;
          }
          // EOF_FLAG = TRUE; (2800)
          {
            int32_t numberRHS = (int32_t)(1);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (1, mINTERPRET_ACCESS_FILExADVANCE_CPxEOF_FLAG, bitRHS);
            bitRHS->inUse = 0;
          }
        es1s1:;
        } // End of DO block
      // IF BYTE(S) = BYTE('C') THEN (2801)
      if (1
          & (xEQ (BYTE1 (getCHARACTER (mINTERPRET_ACCESS_FILExS)),
                  BYTE1 (cToDescriptor (NULL, "C")))))
        // GO TO READIT; (2802)
        goto READIT;
      // CP = 1; (2803)
      {
        int32_t numberRHS = (int32_t)(1);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mINTERPRET_ACCESS_FILExCP, bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
