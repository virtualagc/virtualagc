/*
  File STREAMxGET_GROUP.c generated by XCOM-I, 2024-08-08 04:31:11.
*/

#include "runtimeC.h"

int32_t
STREAMxGET_GROUP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "STREAMxGET_GROUP");
  // E_LINE = BLANKS || BLANKS; (4533)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (getCHARACTER (mSTREAMxBLANKS), getCHARACTER (mSTREAMxBLANKS));
    putCHARACTER (mSTREAMxE_LINE, stringRHS);
    stringRHS->inUse = 0;
  }
  // S_LINE = BLANKS || BLANKS; (4534)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (getCHARACTER (mSTREAMxBLANKS), getCHARACTER (mSTREAMxBLANKS));
    putCHARACTER (mSTREAMxS_LINE, stringRHS);
    stringRHS->inUse = 0;
  }
  // LAST_E_COUNT = E_COUNT; (4535)
  {
    descriptor_t *bitRHS = getBIT (16, mSTREAMxE_COUNT);
    putBIT (16, mSTREAMxLAST_E_COUNT, bitRHS);
    bitRHS->inUse = 0;
  }
  // LAST_S_COUNT = S_COUNT; (4536)
  {
    descriptor_t *bitRHS = getBIT (16, mSTREAMxS_COUNT);
    putBIT (16, mSTREAMxLAST_S_COUNT, bitRHS);
    bitRHS->inUse = 0;
  }
  // E_COUNT, S_COUNT = 0; (4537)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mSTREAMxE_COUNT, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mSTREAMxS_COUNT, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF ~INCREMENT_DOWN_STMT THEN (4538)
  if (1 & (xNOT (BYTE0 (mINCREMENT_DOWN_STMT))))
    // INCREMENT_DOWN_STMT = (LAST_WRITE <= STMT_PTR); (4539)
    {
      int32_t numberRHS = (int32_t)(xLE (COREHALFWORD (mLAST_WRITE),
                                         COREHALFWORD (mSTMT_PTR)));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mINCREMENT_DOWN_STMT, bitRHS);
      bitRHS->inUse = 0;
    }
  // CALL OUTPUT_GROUP; (4540)
  OUTPUT_GROUP (0);
  // IF INCLUDE_END THEN (4541)
  if (1 & (bitToFixed (getBIT (1, mINCLUDE_END))))
    // DO; (4542)
    {
    rs1:;
      // CALL OUTPUT_WRITER(LAST_WRITE, STMT_PTR); (4543)
      {
        putBITp (16, mOUTPUT_WRITERxPTR_START, getBIT (16, mLAST_WRITE));
        putBITp (16, mOUTPUT_WRITERxPTR_END, getBIT (16, mSTMT_PTR));
        OUTPUT_WRITER (0);
      }
      // OUTPUT = X8 || STARS || 'END' || INCLUDE_MSG || STARS; (4544)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            xsCAT (xsCAT (xsCAT (getCHARACTER (mX8), getCHARACTER (mSTARS)),
                          cToDescriptor (NULL, "END")),
                   getCHARACTER (mINCLUDE_MSG)),
            getCHARACTER (mSTARS));
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
      // NEXT_CC = DOUBLE; (4545)
      {
        descriptor_t *stringRHS;
        stringRHS = getCHARACTER (mDOUBLE);
        putCHARACTER (mNEXT_CC, stringRHS);
        stringRHS->inUse = 0;
      }
      // INCLUDE_LIST = TRUE; (4546)
      {
        int32_t numberRHS = (int32_t)(1);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mINCLUDE_LIST, bitRHS);
        bitRHS->inUse = 0;
      }
      // SRN_COUNT=0; (4547)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mSRN_COUNT, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF TPL_REMOTE THEN (4548)
      if (1 & (bitToFixed (getBIT (1, mTPL_REMOTE))))
        // DO; (4549)
        {
        rs1s1:;
          // CALL ERROR(CLASS_XI,5); (4550)
          {
            putBITp (16, mERRORxCLASS, getBIT (16, mCLASS_XI));
            putBITp (8, mERRORxNUM, fixedToBit (32, (int32_t)(5)));
            ERROR (0);
          }
          // TPL_REMOTE = FALSE; (4551)
          {
            int32_t numberRHS = (int32_t)(0);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (1, mTPL_REMOTE, bitRHS);
            bitRHS->inUse = 0;
          }
        es1s1:;
        } // End of DO block
      // INCLUDE_END = FALSE; (4552)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mINCLUDE_END, bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // GO TO LOOP; (4553)
  goto LOOP;
// READ: (4554)
READ:
  // CALL READ_CARD; (4555)
  STREAMxREAD_CARD (0);
// CHECK_ORDER: (4556)
CHECK_ORDER:
  // IF ~ ORDER_OK(PREV_CARD) THEN (4557)
  if (1
      & (xNOT (
          bitToFixed ((putFIXED (mORDER_OKxTYPE, getFIXED (mSTREAMxPREV_CARD)),
                       ORDER_OK (0))))))
    // DO; (4558)
    {
    rs2:;
      // CALL ERROR(CLASS_M,2); (4559)
      {
        putBITp (16, mERRORxCLASS, getBIT (16, mCLASS_M));
        putBITp (8, mERRORxNUM, fixedToBit (32, (int32_t)(2)));
        ERROR (0);
      }
      // GO TO COMMENT_CARD; (4560)
      goto COMMENT_CARD;
    es2:;
    } // End of DO block
// LOOP: (4561)
LOOP:
  // IF END_GROUP THEN (4562)
  if (1 & (bitToFixed (getBIT (1, mEND_GROUP))))
    // GO TO FOUND_GROUP; (4563)
    goto FOUND_GROUP;
  // DO CASE CARD_TYPE(BYTE(CURRENT_CARD)); (4564)
  {
  rs3:
    switch (
        COREHALFWORD (mCARD_TYPE + 2 * BYTE1 (getCHARACTER (mCURRENT_CARD))))
      {
      case 0:
          // ; (4566)
          ;
        break;
      case 1:
        // DO; (4567)
        {
        rs3s1:;
          // CALL COMP(0); (4567)
          {
            putFIXED (mSTREAMxCOMPxTYPE, 0);
            STREAMxCOMP (0);
          }
          // GO TO LOOP; (4568)
          goto LOOP;
        es3s1:;
        } // End of DO block
        break;
      case 2:
        // DO; (4570)
        {
        rs3s2:;
          // M_LINE = CURRENT_CARD; (4570)
          {
            descriptor_t *stringRHS;
            stringRHS = getCHARACTER (mCURRENT_CARD);
            putCHARACTER (mSTREAMxM_LINE, stringRHS);
            stringRHS->inUse = 0;
          }
          // IF SRN_PRESENT THEN (4571)
          if (1 & (bitToFixed (getBIT (1, mSRN_PRESENT))))
            // DO; (4572)
            {
            rs3s2s1:;
              // IF INCLUDING THEN (4573)
              if (1 & (bitToFixed (getBIT (1, mINCLUDING))))
                // DO; (4574)
                {
                rs3s2s1s1:;
                  // INCL_SRN = SUBSTR(CURRENT_CARD,TEXT_LIMIT+1); (4575)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = SUBSTR2 (getCHARACTER (mCURRENT_CARD),
                                         xadd (COREHALFWORD (mTEXT_LIMIT), 1));
                    putCHARACTER (mINCL_SRN, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // SRN_COUNT = SRN_COUNT + 1; (4576)
                  {
                    int32_t numberRHS
                        = (int32_t)(xadd (COREHALFWORD (mSRN_COUNT), 1));
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (16, mSRN_COUNT, bitRHS);
                    bitRHS->inUse = 0;
                  }
                es3s2s1s1:;
                } // End of DO block
              // ELSE (4577)
              else
                // DO; (4578)
                {
                rs3s2s1s2:;
                  // SRN=SUBSTR(CURRENT_CARD,TEXT_LIMIT+1); (4579)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = SUBSTR2 (getCHARACTER (mCURRENT_CARD),
                                         xadd (COREHALFWORD (mTEXT_LIMIT), 1));
                    putCHARACTER (mSRN, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // RVL(1) = SUBSTR(SRN,6,2); (4580)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = SUBSTR (getCHARACTER (mSRN), 6, 2);
                    putCHARACTER (mRVL + 4 * (1), stringRHS);
                    stringRHS->inUse = 0;
                  }
                es3s2s1s2:;
                } // End of DO block
            es3s2s1:;
            } // End of DO block
          // BYTE(SAVE_CARD) = BYTE('M'); (4581)
          {
            int32_t numberRHS = (int32_t)(BYTE1 (cToDescriptor (NULL, "M")));
            lBYTEb (ADDR (NULL, 0, "SAVE_CARD", 0), 0,
                    BYTE1 (cToDescriptor (NULL, "M")));
          }
          // PREV_CARD = BYTE(CURRENT_CARD); (4582)
          {
            int32_t numberRHS
                = (int32_t)(BYTE1 (getCHARACTER (mCURRENT_CARD)));
            putFIXED (mSTREAMxPREV_CARD, numberRHS);
          }
          // GO TO READ; (4583)
          goto READ;
        es3s2:;
        } // End of DO block
        break;
      case 3:
        // DO; (4585)
        {
        rs3s3:;
          // CALL COMP(1); (4585)
          {
            putFIXED (mSTREAMxCOMPxTYPE, 1);
            STREAMxCOMP (0);
          }
          // GO TO LOOP; (4586)
          goto LOOP;
        es3s3:;
        } // End of DO block
        break;
      case 4:
        // DO; (4588)
        {
        rs3s4:;
          // BYTE(CURRENT_CARD) = BYTE('C'); (4588)
          {
            int32_t numberRHS = (int32_t)(BYTE1 (cToDescriptor (NULL, "C")));
            lBYTEb (ADDR (NULL, 0, "CURRENT_CARD", 0), 0,
                    BYTE1 (cToDescriptor (NULL, "C")));
          }
        // COMMENT_CARD: (4589)
        COMMENT_CARD:
          // IF PREV_CARD = BYTE('C') THEN (4590)
          if (1
              & (xEQ (getFIXED (mSTREAMxPREV_CARD),
                      BYTE1 (cToDescriptor (NULL, "C")))))
            // COMMENTING = TRUE; (4591)
            {
              int32_t numberRHS = (int32_t)(1);
              descriptor_t *bitRHS;
              bitRHS = fixedToBit (32, (int32_t)(numberRHS));
              putBIT (1, mCOMMENTING, bitRHS);
              bitRHS->inUse = 0;
            }
          // ELSE (4592)
          else
            // COMMENTING = FALSE; (4593)
            {
              int32_t numberRHS = (int32_t)(0);
              descriptor_t *bitRHS;
              bitRHS = fixedToBit (32, (int32_t)(numberRHS));
              putBIT (1, mCOMMENTING, bitRHS);
              bitRHS->inUse = 0;
            }
          // PREV_CARD = BYTE('C'); (4594)
          {
            int32_t numberRHS = (int32_t)(BYTE1 (cToDescriptor (NULL, "C")));
            putFIXED (mSTREAMxPREV_CARD, numberRHS);
          }
          // IF SRN_PRESENT THEN (4595)
          if (1 & (bitToFixed (getBIT (1, mSRN_PRESENT))))
            // DO; (4596)
            {
            rs3s4s1:;
              // IF ~INCLUDING THEN (4597)
              if (1 & (xNOT (BYTE0 (mINCLUDING))))
                // DO; (4598)
                {
                rs3s4s1s1:;
                  // SRN=SUBSTR(CURRENT_CARD,TEXT_LIMIT+1); (4599)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = SUBSTR2 (getCHARACTER (mCURRENT_CARD),
                                         xadd (COREHALFWORD (mTEXT_LIMIT), 1));
                    putCHARACTER (mSRN, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // RVL = ''; (4600)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = cToDescriptor (NULL, "");
                    putCHARACTER (mRVL, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // RVL(1) = ''; (4601)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = cToDescriptor (NULL, "");
                    putCHARACTER (mRVL + 4 * (1), stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // NEXT_CHAR_RVL = ''; (4602)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = cToDescriptor (NULL, "");
                    putCHARACTER (mNEXT_CHAR_RVL, stringRHS);
                    stringRHS->inUse = 0;
                  }
                es3s4s1s1:;
                } // End of DO block
            es3s4s1:;
            } // End of DO block
          // CALL PROCESS_COMMENT; (4603)
          STREAMxPROCESS_COMMENT (0);
          // CALL READ_CARD; (4604)
          STREAMxREAD_CARD (0);
          // IF INCLUDE_END THEN (4605)
          if (1 & (bitToFixed (getBIT (1, mINCLUDE_END))))
            // DO; (4606)
            {
            rs3s4s2:;
              // OUTPUT = X8 || STARS || 'END' || INCLUDE_MSG || STARS; (4607)
              {
                descriptor_t *stringRHS;
                stringRHS = xsCAT (xsCAT (xsCAT (xsCAT (getCHARACTER (mX8),
                                                        getCHARACTER (mSTARS)),
                                                 cToDescriptor (NULL, "END")),
                                          getCHARACTER (mINCLUDE_MSG)),
                                   getCHARACTER (mSTARS));
                OUTPUT (0, stringRHS);
                stringRHS->inUse = 0;
              }
              // NEXT_CC = DOUBLE; (4608)
              {
                descriptor_t *stringRHS;
                stringRHS = getCHARACTER (mDOUBLE);
                putCHARACTER (mNEXT_CC, stringRHS);
                stringRHS->inUse = 0;
              }
              // INCLUDE_LIST = TRUE; (4609)
              {
                int32_t numberRHS = (int32_t)(1);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mINCLUDE_LIST, bitRHS);
                bitRHS->inUse = 0;
              }
              // SRN_COUNT=0; (4610)
              {
                int32_t numberRHS = (int32_t)(0);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16, mSRN_COUNT, bitRHS);
                bitRHS->inUse = 0;
              }
              // IF TPL_REMOTE THEN (4611)
              if (1 & (bitToFixed (getBIT (1, mTPL_REMOTE))))
                // DO; (4612)
                {
                rs3s4s2s1:;
                  // CALL ERROR(CLASS_XI,5); (4613)
                  {
                    putBITp (16, mERRORxCLASS, getBIT (16, mCLASS_XI));
                    putBITp (8, mERRORxNUM, fixedToBit (32, (int32_t)(5)));
                    ERROR (0);
                  }
                  // TPL_REMOTE = FALSE; (4614)
                  {
                    int32_t numberRHS = (int32_t)(0);
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (1, mTPL_REMOTE, bitRHS);
                    bitRHS->inUse = 0;
                  }
                es3s4s2s1:;
                } // End of DO block
              // INCLUDE_END = FALSE; (4615)
              {
                int32_t numberRHS = (int32_t)(0);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mINCLUDE_END, bitRHS);
                bitRHS->inUse = 0;
              }
            es3s4s2:;
            } // End of DO block
          // GO TO CHECK_ORDER; (4616)
          goto CHECK_ORDER;
        es3s4:;
        } // End of DO block
        break;
      case 5:
        // DO; (4618)
        {
        rs3s5:;
          // BYTE(CURRENT_CARD) = BYTE('D'); (4618)
          {
            int32_t numberRHS = (int32_t)(BYTE1 (cToDescriptor (NULL, "D")));
            lBYTEb (ADDR (NULL, 0, "CURRENT_CARD", 0), 0,
                    BYTE1 (cToDescriptor (NULL, "D")));
          }
          // GO TO COMMENT_CARD; (4619)
          goto COMMENT_CARD;
        es3s5:;
        } // End of DO block
        break;
      }
  } // End of DO CASE block
// FOUND_GROUP: (4620)
FOUND_GROUP:
  // END_GROUP = FALSE; (4621)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mEND_GROUP, bitRHS);
    bitRHS->inUse = 0;
  }
  // E_LINE = SUBSTR(E_LINE, 0, LENGTH(M_LINE)); (4622)
  {
    descriptor_t *stringRHS;
    stringRHS = SUBSTR (getCHARACTER (mSTREAMxE_LINE), 0,
                        LENGTH (getCHARACTER (mSTREAMxM_LINE)));
    putCHARACTER (mSTREAMxE_LINE, stringRHS);
    stringRHS->inUse = 0;
  }
  // IF E_COUNT ~> 0 THEN (4623)
  if (1 & (xLE (COREHALFWORD (mSTREAMxE_COUNT), 0)))
    // DO; (4624)
    {
    rs4:;
      // DO CP = 1 TO TEXT_LIMIT; (4625)
      {
        int32_t from80, to80, by80;
        from80 = 1;
        to80 = bitToFixed (getBIT (16, mTEXT_LIMIT));
        by80 = 1;
        for (putBIT (16, mSTREAMxCP, fixedToBit (16, from80));
             bitToFixed (getBIT (16, mSTREAMxCP)) <= to80; putBIT (
                 16, mSTREAMxCP,
                 fixedToBit (16, bitToFixed (getBIT (16, mSTREAMxCP)) + by80)))
          {
            // E_INDICATOR(CP) = 0; (4626)
            {
              int32_t numberRHS = (int32_t)(0);
              descriptor_t *bitRHS;
              bitRHS = fixedToBit (32, (int32_t)(numberRHS));
              putBIT (16,
                      mSTREAMxE_INDICATOR + 2 * (COREHALFWORD (mSTREAMxCP)),
                      bitRHS);
              bitRHS->inUse = 0;
            }
          }
      } // End of DO for-loop block
      // E_COUNT = LAST_E_COUNT; (4627)
      {
        descriptor_t *bitRHS = getBIT (16, mSTREAMxLAST_E_COUNT);
        putBIT (16, mSTREAMxE_COUNT, bitRHS);
        bitRHS->inUse = 0;
      }
    es4:;
    } // End of DO block
  // S_LINE = SUBSTR(S_LINE, 0, LENGTH(M_LINE)); (4628)
  {
    descriptor_t *stringRHS;
    stringRHS = SUBSTR (getCHARACTER (mSTREAMxS_LINE), 0,
                        LENGTH (getCHARACTER (mSTREAMxM_LINE)));
    putCHARACTER (mSTREAMxS_LINE, stringRHS);
    stringRHS->inUse = 0;
  }
  // IF S_COUNT ~> 0 THEN (4629)
  if (1 & (xLE (COREHALFWORD (mSTREAMxS_COUNT), 0)))
    // DO; (4630)
    {
    rs5:;
      // DO CP = 1 TO TEXT_LIMIT; (4631)
      {
        int32_t from81, to81, by81;
        from81 = 1;
        to81 = bitToFixed (getBIT (16, mTEXT_LIMIT));
        by81 = 1;
        for (putBIT (16, mSTREAMxCP, fixedToBit (16, from81));
             bitToFixed (getBIT (16, mSTREAMxCP)) <= to81; putBIT (
                 16, mSTREAMxCP,
                 fixedToBit (16, bitToFixed (getBIT (16, mSTREAMxCP)) + by81)))
          {
            // S_INDICATOR(CP) = 0; (4632)
            {
              int32_t numberRHS = (int32_t)(0);
              descriptor_t *bitRHS;
              bitRHS = fixedToBit (32, (int32_t)(numberRHS));
              putBIT (16,
                      mSTREAMxS_INDICATOR + 2 * (COREHALFWORD (mSTREAMxCP)),
                      bitRHS);
              bitRHS->inUse = 0;
            }
          }
      } // End of DO for-loop block
      // S_COUNT = LAST_S_COUNT; (4633)
      {
        descriptor_t *bitRHS = getBIT (16, mSTREAMxLAST_S_COUNT);
        putBIT (16, mSTREAMxS_COUNT, bitRHS);
        bitRHS->inUse = 0;
      }
    es5:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
