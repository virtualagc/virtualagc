/*
  File SCANxPUSH_MACRO.c generated by XCOM-I, 2024-08-08 04:31:11.
*/

#include "runtimeC.h"

int32_t
SCANxPUSH_MACRO (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SCANxPUSH_MACRO");
  // SUPPRESS_THIS_TOKEN_ONLY = FALSE; (6819)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mSUPPRESS_THIS_TOKEN_ONLY, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO WHILE NEXT_CHAR=BYTE(X1); (6820)
  while (1 & (xEQ (BYTE0 (mNEXT_CHAR), BYTE1 (getCHARACTER (mX1)))))
    {
      // CALL STREAM; (6821)
      STREAM (0);
    } // End of DO WHILE block
  // IF MACRO_EXPAN_LEVEL+1 > MACRO_EXPAN_LIMIT THEN (6822)
  if (1 & (xGT (xadd (COREHALFWORD (mMACRO_EXPAN_LEVEL), 1), 8)))
    // DO; (6823)
    {
    rs2:;
      // CALL ERROR(CLASS_IR,9,BCD); (6824)
      {
        putBITp (16, mERRORxCLASS, getBIT (16, mCLASS_IR));
        putBITp (8, mERRORxNUM, fixedToBit (32, (int32_t)(9)));
        putCHARACTERp (mERRORxTEXT, getCHARACTER (mBCD));
        ERROR (0);
      }
      // MACRO_EXPAN_LEVEL,PARM_EXPAN_LEVEL,MACRO_FOUND=0; (6825)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mMACRO_EXPAN_LEVEL, bitRHS);
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mPARM_EXPAN_LEVEL, bitRHS);
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mMACRO_FOUND, bitRHS);
        bitRHS->inUse = 0;
      }
      // NEXT_CHAR=SAVE_NEXT_CHAR; (6826)
      {
        descriptor_t *bitRHS = getBIT (8, mSAVE_NEXT_CHAR);
        putBIT (8, mNEXT_CHAR, bitRHS);
        bitRHS->inUse = 0;
      }
      // OVER_PUNCH=SAVE_OVER_PUNCH; (6827)
      {
        descriptor_t *bitRHS = getBIT (8, mSAVE_OVER_PUNCH);
        putBIT (8, mOVER_PUNCH, bitRHS);
        bitRHS->inUse = 0;
      }
      // PRINTING_ENABLED=PRINT_FLAG; (6828)
      {
        descriptor_t *bitRHS = getBIT (16, mPRINT_FLAG);
        putBIT (16, mPRINTING_ENABLED, bitRHS);
        bitRHS->inUse = 0;
      }
      // RETURN; (6829)
      {
        reentryGuard = 0;
        return 0;
      }
    es2:;
    } // End of DO block
  // MACRO_EXPAN_STACK(MACRO_EXPAN_LEVEL+1) = SYT_INDEX; (6830)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mSYT_INDEX));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16,
            mMACRO_EXPAN_STACK
                + 2 * (xadd (COREHALFWORD (mMACRO_EXPAN_LEVEL), 1)),
            bitRHS);
    bitRHS->inUse = 0;
  }
  // IF PRINTING_ENABLED=PRINT_FLAG THEN (6831)
  if (1 & (xEQ (COREHALFWORD (mPRINTING_ENABLED), COREHALFWORD (mPRINT_FLAG))))
    // DO; (6832)
    {
    rs3:;
      // RESTORE=PRINT_FLAG; (6833)
      {
        descriptor_t *bitRHS = getBIT (16, mPRINT_FLAG);
        putBIT (16, mRESTORE, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF FOUND_CENT THEN (6834)
      if (1 & (bitToFixed (getBIT (1, mFOUND_CENT))))
        // DO; (6835)
        {
        rs3s1:;
          // PASS=PRINT_FLAG; (6836)
          {
            descriptor_t *bitRHS = getBIT (16, mPRINT_FLAG);
            putBIT (16, mPASS, bitRHS);
            bitRHS->inUse = 0;
          }
          // PRINTING_ENABLED=0; (6837)
          {
            int32_t numberRHS = (int32_t)(0);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (16, mPRINTING_ENABLED, bitRHS);
            bitRHS->inUse = 0;
          }
        es3s1:;
        } // End of DO block
      // ELSE (6838)
      else
        // PASS=0; (6839)
        {
          int32_t numberRHS = (int32_t)(0);
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (16, mPASS, bitRHS);
          bitRHS->inUse = 0;
        }
    es3:;
    } // End of DO block
  // ELSE (6840)
  else
    // RESTORE,PASS=0; (6841)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mRESTORE, bitRHS);
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mPASS, bitRHS);
      bitRHS->inUse = 0;
    }
  // CALL SAVE_TOKEN(ID_TOKEN,BCD,7); (6842)
  {
    putBITp (16, mSAVE_TOKENxTOKEN,
             fixedToBit (32, (int32_t)(getFIXED (mID_TOKEN))));
    putCHARACTERp (mSAVE_TOKENxCHAR, getCHARACTER (mBCD));
    putBITp (16, mSAVE_TOKENxTYPE, fixedToBit (32, (int32_t)(7)));
    SAVE_TOKEN (0);
  }
  // GRAMMAR_FLAGS(STMT_PTR) = GRAMMAR_FLAGS(STMT_PTR) | MACRO_ARG_FLAG; (6843)
  {
    int32_t numberRHS = (int32_t)(xOR (
        COREHALFWORD (mGRAMMAR_FLAGS + 2 * COREHALFWORD (mSTMT_PTR)),
        COREHALFWORD (mMACRO_ARG_FLAG)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mGRAMMAR_FLAGS + 2 * (COREHALFWORD (mSTMT_PTR)), bitRHS);
    bitRHS->inUse = 0;
  }
  // NUM_OF_PARM(MACRO_EXPAN_LEVEL+1)= VAR_LENGTH(SYT_INDEX); (6844)
  {
    descriptor_t *bitRHS = getBIT (
        16, getFIXED (mSYM_TAB) + 34 * (getFIXED (mSYT_INDEX)) + 18 + 2 * (0));
    putBIT (16,
            mNUM_OF_PARM + 2 * (xadd (COREHALFWORD (mMACRO_EXPAN_LEVEL), 1)),
            bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL PARAMETER_PROCESSING; (6845)
  SCANxPARAMETER_PROCESSING (0);
  // PRINTING_ENABLED=PASS; (6846)
  {
    descriptor_t *bitRHS = getBIT (16, mPASS);
    putBIT (16, mPRINTING_ENABLED, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF TEMP_STRING = '' THEN (6847)
  if (1 & (xsEQ (getCHARACTER (mTEMP_STRING), cToDescriptor (NULL, ""))))
    // DO; (6848)
    {
    rs4:;
      // BASE_PARM_LEVEL(MACRO_EXPAN_LEVEL)=PARM_EXPAN_LEVEL; (6849)
      {
        descriptor_t *bitRHS = getBIT (16, mPARM_EXPAN_LEVEL);
        putBIT (16, mBASE_PARM_LEVEL + 2 * (COREHALFWORD (mMACRO_EXPAN_LEVEL)),
                bitRHS);
        bitRHS->inUse = 0;
      }
      // M_TOKENS(MACRO_EXPAN_LEVEL)=0; (6850)
      {
        int32_t numberRHS = (int32_t)(0);
        putFIXED (mM_TOKENS + 4 * (COREHALFWORD (mMACRO_EXPAN_LEVEL)),
                  numberRHS);
      }
      // M_CENT(MACRO_EXPAN_LEVEL)=FOUND_CENT; (6851)
      {
        descriptor_t *bitRHS = getBIT (1, mFOUND_CENT);
        putBIT (8, mM_CENT + 1 * (COREHALFWORD (mMACRO_EXPAN_LEVEL)), bitRHS);
        bitRHS->inUse = 0;
      }
      // M_PRINT(MACRO_EXPAN_LEVEL)=RESTORE; (6852)
      {
        descriptor_t *bitRHS = getBIT (16, mRESTORE);
        putBIT (16, mM_PRINT + 2 * (COREHALFWORD (mMACRO_EXPAN_LEVEL)),
                bitRHS);
        bitRHS->inUse = 0;
      }
      // FOUND_CENT=FALSE; (6853)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mFOUND_CENT, bitRHS);
        bitRHS->inUse = 0;
      }
      // MACRO_POINT = SYT_ADDR(SYT_INDEX); (6854)
      {
        int32_t numberRHS = (int32_t)(getFIXED (
            getFIXED (mSYM_TAB) + 34 * (getFIXED (mSYT_INDEX)) + 4 + 4 * (0)));
        putFIXED (mMACRO_POINT, numberRHS);
      }
      // IF MACRO_EXPAN_LEVEL = 1 THEN (6855)
      if (1 & (xEQ (COREHALFWORD (mMACRO_EXPAN_LEVEL), 1)))
        // DO; (6856)
        {
        rs4s1:;
          // MACRO_FOUND = TRUE ; (6857)
          {
            int32_t numberRHS = (int32_t)(1);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (1, mMACRO_FOUND, bitRHS);
            bitRHS->inUse = 0;
          }
          // SAVE_NEXT_CHAR=NEXT_CHAR; (6858)
          {
            descriptor_t *bitRHS = getBIT (8, mNEXT_CHAR);
            putBIT (8, mSAVE_NEXT_CHAR, bitRHS);
            bitRHS->inUse = 0;
          }
          // SAVE_OVER_PUNCH = OVER_PUNCH; (6859)
          {
            descriptor_t *bitRHS = getBIT (8, mOVER_PUNCH);
            putBIT (8, mSAVE_OVER_PUNCH, bitRHS);
            bitRHS->inUse = 0;
          }
          // BLANK_COUNT,OVER_PUNCH=0; (6860)
          {
            int32_t numberRHS = (int32_t)(0);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (16, mBLANK_COUNT, bitRHS);
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (8, mOVER_PUNCH, bitRHS);
            bitRHS->inUse = 0;
          }
        es4s1:;
        } // End of DO block
    es4:;
    } // End of DO block
  // ELSE (6861)
  else
    // FOUND_CENT=FALSE; (6862)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mFOUND_CENT, bitRHS);
      bitRHS->inUse = 0;
    }
  // CALL STREAM; (6863)
  STREAM (0);
  // RETURN; (6864)
  {
    reentryGuard = 0;
    return 0;
  }
}
