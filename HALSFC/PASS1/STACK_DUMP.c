/*
  File STACK_DUMP.c generated by XCOM-I, 2024-08-08 04:31:11.
*/

#include "runtimeC.h"

int32_t
STACK_DUMP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "STACK_DUMP");
  // IF ~PARTIAL_PARSE THEN (9835)
  if (1 & (xNOT (BYTE0 (mPARTIAL_PARSE))))
    // RETURN; (9836)
    {
      reentryGuard = 0;
      return 0;
    }
  // STACK_DUMPED = TRUE; (9837)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mSTACK_DUMPED, bitRHS);
    bitRHS->inUse = 0;
  }
  // LINE = '***** PARTIAL PARSE TO THIS POINT IS: '; (9838)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "***** PARTIAL PARSE TO THIS POINT IS: ");
    putCHARACTER (mSTACK_DUMPxLINE, stringRHS);
    stringRHS->inUse = 0;
  }
  // DO I = 1 TO SP; (9839)
  {
    int32_t from147, to147, by147;
    from147 = 1;
    to147 = getFIXED (mSP);
    by147 = 1;
    for (putFIXED (mI, from147); getFIXED (mI) <= to147;
         putFIXED (mI, getFIXED (mI) + by147))
      {
        // IF LENGTH(LINE) > 105 THEN (9840)
        if (1 & (xGT (LENGTH (getCHARACTER (mSTACK_DUMPxLINE)), 105)))
          // DO; (9841)
          {
          rs1s1:;
            // CALL SAVE_DUMP(LINE); (9842)
            {
              putCHARACTERp (mSAVE_DUMPxMSG, getCHARACTER (mSTACK_DUMPxLINE));
              SAVE_DUMP (0);
            }
            // LINE = X4; (9843)
            {
              descriptor_t *stringRHS;
              stringRHS = getCHARACTER (mX4);
              putCHARACTER (mSTACK_DUMPxLINE, stringRHS);
              stringRHS->inUse = 0;
            }
          es1s1:;
          } // End of DO block
        // LINE = LINE || X1 ||
        // STRING(VOCAB_INDEX(STATE_NAME(STATE_STACK(I)))); (9844)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              xsCAT (getCHARACTER (mSTACK_DUMPxLINE), getCHARACTER (mX1)),
              getCHARACTER (
                  mVOCAB_INDEX
                  + 4
                        * COREHALFWORD (
                            mSTATE_NAME
                            + 2
                                  * getFIXED (mSTATE_STACK
                                              + 4 * getFIXED (mI)))));
          putCHARACTER (mSTACK_DUMPxLINE, stringRHS);
          stringRHS->inUse = 0;
        }
      }
  } // End of DO for-loop block
  // CALL SAVE_DUMP(LINE); (9845)
  {
    putCHARACTERp (mSAVE_DUMPxMSG, getCHARACTER (mSTACK_DUMPxLINE));
    SAVE_DUMP (0);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
