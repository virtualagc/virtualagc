/*
  File PREC_SCALE.c generated by XCOM-I, 2024-08-08 04:31:11.
*/

#include "runtimeC.h"

int32_t
PREC_SCALE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PREC_SCALE");
  // P_TEMP=PSEUDO_FORM(PTR(PP)); (7521)
  {
    descriptor_t *bitRHS = getBIT (
        8, mPSEUDO_FORM
               + 1 * COREHALFWORD (mPTR + 2 * COREHALFWORD (mPREC_SCALExPP)));
    int32_t numberRHS;
    numberRHS = bitToFixed (bitRHS);
    putFIXED (mPREC_SCALExP_TEMP, numberRHS);
    bitRHS->inUse = 0;
  }
  // PTR(PP)=PTR(PP)+1; (7522)
  {
    int32_t numberRHS = (int32_t)(xadd (
        COREHALFWORD (mPTR + 2 * COREHALFWORD (mPREC_SCALExPP)), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPTR + 2 * (COREHALFWORD (mPREC_SCALExPP)), bitRHS);
    bitRHS->inUse = 0;
  }
  // IF (P_TEMP& 15)~=0 THEN (7523)
  if (1 & (xNEQ (xAND (getFIXED (mPREC_SCALExP_TEMP), 15), 0)))
    // DO; (7524)
    {
    rs1:;
      // CALL HALMAT_TUPLE(XMTOM(P_TYPE-MAT_TYPE),0,MP,0, P_TEMP& 15); (7525)
      {
        putBITp (
            16, mHALMAT_TUPLExPOPCODE,
            getBIT (16,
                    mXMTOM + 2 * xsubtract (BYTE0 (mPREC_SCALExP_TYPE), 3)));
        putBITp (8, mHALMAT_TUPLExCOPT, fixedToBit (32, (int32_t)(0)));
        putBITp (16, mHALMAT_TUPLExOP1,
                 fixedToBit (32, (int32_t)(getFIXED (mMP))));
        putBITp (16, mHALMAT_TUPLExOP2, fixedToBit (32, (int32_t)(0)));
        putBITp (8, mHALMAT_TUPLExTAG,
                 fixedToBit (
                     32, (int32_t)(xAND (getFIXED (mPREC_SCALExP_TEMP), 15))));
        HALMAT_TUPLE (0);
      }
      // CALL SETUP_VAC(MP,P_TYPE); (7526)
      {
        putBITp (16, mSETUP_VACxLOC,
                 fixedToBit (32, (int32_t)(getFIXED (mMP))));
        putBITp (16, mSETUP_VACxTYPE, getBIT (8, mPREC_SCALExP_TYPE));
        SETUP_VAC (0);
      }
    es1:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
