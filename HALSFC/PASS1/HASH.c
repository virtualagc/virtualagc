/*
  File HASH.c generated by XCOM-I, 2024-08-08 04:31:11.
*/

#include "runtimeC.h"

int32_t
HASH (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "HASH");
  // HASH = 0; (1240)
  {
    int32_t numberRHS = (int32_t)(0);
    putFIXED (mHASHxHASH, numberRHS);
  }
  // J = LENGTH(BCD) - 1; (1241)
  {
    int32_t numberRHS
        = (int32_t)(xsubtract (LENGTH (getCHARACTER (mHASHxBCD)), 1));
    putFIXED (mHASHxJ, numberRHS);
  }
  // IF J > 22 THEN (1242)
  if (1 & (xGT (getFIXED (mHASHxJ), 22)))
    // J = 22; (1243)
    {
      int32_t numberRHS = (int32_t)(22);
      putFIXED (mHASHxJ, numberRHS);
    }
  // DO I = 0 TO J; (1244)
  {
    int32_t from23, to23, by23;
    from23 = 0;
    to23 = getFIXED (mHASHxJ);
    by23 = 1;
    for (putFIXED (mHASHxI, from23); getFIXED (mHASHxI) <= to23;
         putFIXED (mHASHxI, getFIXED (mHASHxI) + by23))
      {
        // HASH = BYTE(BCD,I)+SHL(HASH,1); (1245)
        {
          int32_t numberRHS = (int32_t)(xadd (
              BYTE (getCHARACTER (mHASHxBCD), getFIXED (mHASHxI)),
              SHL (getFIXED (mHASHxHASH), 1)));
          putFIXED (mHASHxHASH, numberRHS);
        }
      }
  } // End of DO for-loop block
  // RETURN HASH MOD HASHSIZE; (1246)
  {
    reentryGuard = 0;
    return xmod (getFIXED (mHASHxHASH), getFIXED (mHASHxHASHSIZE));
  }
}
