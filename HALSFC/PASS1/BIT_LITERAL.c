/*
  File BIT_LITERAL.c generated by XCOM-I, 2024-08-09 12:38:15.
*/

#include "runtimeC.h"

descriptor_t *
BIT_LITERAL (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "BIT_LITERAL");
  // IF PSEUDO_FORM(PTR(LOC1))~=XLIT THEN (7527)
  if (1
      & (xNEQ (BYTE0 (mPSEUDO_FORM
                      + 1
                            * COREHALFWORD (
                                mPTR + 2 * COREHALFWORD (mBIT_LITERALxLOC1))),
               BYTE0 (mXLIT))))
    // RETURN FALSE; (7528)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
  // FIXV(LOC1)=LIT2(GET_LITERAL(LOC_P(PTR(LOC1)))); (7529)
  {
    int32_t numberRHS = (int32_t)(getFIXED (
        getFIXED (mLIT_PG) + 1560 * (0) + 520
        + 4
              * ((putFIXED (
                      mGET_LITERALxPTR,
                      COREHALFWORD (
                          mLOC_P
                          + 2
                                * COREHALFWORD (
                                    mPTR
                                    + 2 * COREHALFWORD (mBIT_LITERALxLOC1)))),
                  GET_LITERAL (0)))));
    putFIXED (mFIXV + 4 * (COREHALFWORD (mBIT_LITERALxLOC1)), numberRHS);
  }
  // IF LOC2=0 THEN (7530)
  if (1 & (xEQ (COREHALFWORD (mBIT_LITERALxLOC2), 0)))
    // RETURN TRUE; (7531)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(1));
    }
  // IF PSEUDO_FORM(PTR(LOC2))~=XLIT THEN (7532)
  if (1
      & (xNEQ (BYTE0 (mPSEUDO_FORM
                      + 1
                            * COREHALFWORD (
                                mPTR + 2 * COREHALFWORD (mBIT_LITERALxLOC2))),
               BYTE0 (mXLIT))))
    // RETURN FALSE; (7533)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
  // FIXV(LOC2)=LIT2(GET_LITERAL(LOC_P(PTR(LOC2)))); (7534)
  {
    int32_t numberRHS = (int32_t)(getFIXED (
        getFIXED (mLIT_PG) + 1560 * (0) + 520
        + 4
              * ((putFIXED (
                      mGET_LITERALxPTR,
                      COREHALFWORD (
                          mLOC_P
                          + 2
                                * COREHALFWORD (
                                    mPTR
                                    + 2 * COREHALFWORD (mBIT_LITERALxLOC2)))),
                  GET_LITERAL (0)))));
    putFIXED (mFIXV + 4 * (COREHALFWORD (mBIT_LITERALxLOC2)), numberRHS);
  }
  // RETURN TRUE; (7535)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(1));
  }
}
