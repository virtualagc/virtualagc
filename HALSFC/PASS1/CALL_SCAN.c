/*
  File CALL_SCAN.c generated by XCOM-I, 2024-08-08 04:31:11.
*/

#include "runtimeC.h"

int32_t
CALL_SCAN (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "CALL_SCAN");
  // NO_LOOK_AHEAD_DONE = FALSE; (9787)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mNO_LOOK_AHEAD_DONE, bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL SCAN; (9788)
  SCAN (0);
  // IF CONTROL(4) | (PARTIAL_PARSE & RECOVERING) THEN (9789)
  if (1
      & (xOR (BYTE0 (mCONTROL + 1 * 4),
              xAND (BYTE0 (mPARTIAL_PARSE), BYTE0 (mRECOVERING)))))
    // DO; (9790)
    {
    rs1:;
      // S = HEX(DW(7), 0); (9791)
      {
        descriptor_t *stringRHS;
        stringRHS = (putFIXED (mHEXxNUM, getFIXED (getFIXED (mFOR_DW) + 4 * (7)
                                                   + 0 + 4 * (0))),
                     putFIXED (mHEXxWIDTH, 0), HEX (0));
        putCHARACTER (mS, stringRHS);
        stringRHS->inUse = 0;
      }
      // S = HEX(DW(6), 0) || S; (9792)
      {
        descriptor_t *stringRHS;
        stringRHS
            = xsCAT ((putFIXED (mHEXxNUM, getFIXED (getFIXED (mFOR_DW)
                                                    + 4 * (6) + 0 + 4 * (0))),
                      putFIXED (mHEXxWIDTH, 0), HEX (0)),
                     getCHARACTER (mS));
        putCHARACTER (mS, stringRHS);
        stringRHS->inUse = 0;
      }
      // S = 'SCANNER: ' || TOKEN || ', NUMBER_VALUE: ' || S || ', VALUE: ' ||
      // VALUE; (9793)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            xsCAT (xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "SCANNER: "),
                                        fixedToCharacter (getFIXED (mTOKEN))),
                                 cToDescriptor (NULL, ", NUMBER_VALUE: ")),
                          getCHARACTER (mS)),
                   cToDescriptor (NULL, ", VALUE: ")),
            fixedToCharacter (getFIXED (mVALUE)));
        putCHARACTER (mS, stringRHS);
        stringRHS->inUse = 0;
      }
      // IF BCD ~= '' THEN (9794)
      if (1 & (xsNEQ (getCHARACTER (mBCD), cToDescriptor (NULL, ""))))
        // S = S || ', CURRENT BCD: ' || BCD; (9795)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (xsCAT (getCHARACTER (mS),
                                    cToDescriptor (NULL, ", CURRENT BCD: ")),
                             getCHARACTER (mBCD));
          putCHARACTER (mS, stringRHS);
          stringRHS->inUse = 0;
        }
      // IF RECOVERING THEN (9796)
      if (1 & (bitToFixed (getBIT (1, mRECOVERING))))
        // S = S || ', SKIPPED OVER BY RECOVERY'; (9797)
        {
          descriptor_t *stringRHS;
          stringRHS
              = xsCAT (getCHARACTER (mS),
                       cToDescriptor (NULL, ", SKIPPED OVER BY RECOVERY"));
          putCHARACTER (mS, stringRHS);
          stringRHS->inUse = 0;
        }
      // S = S || ', NEXT_CHAR: ' || NEXT_CHAR; (9798)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            xsCAT (getCHARACTER (mS), cToDescriptor (NULL, ", NEXT_CHAR: ")),
            bitToCharacter (getBIT (8, mNEXT_CHAR)));
        putCHARACTER (mS, stringRHS);
        stringRHS->inUse = 0;
      }
      // IF RECOVERING THEN (9799)
      if (1 & (bitToFixed (getBIT (1, mRECOVERING))))
        // CALL SAVE_DUMP(S); (9800)
        {
          putCHARACTERp (mSAVE_DUMPxMSG, getCHARACTER (mS));
          SAVE_DUMP (0);
        }
      // ELSE (9801)
      else
        // OUTPUT = S; (9802)
        {
          descriptor_t *stringRHS;
          stringRHS = getCHARACTER (mS);
          OUTPUT (0, stringRHS);
          stringRHS->inUse = 0;
        }
    es1:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
