/*
  File PTR_LOCATE.c generated by XCOM-I, 2024-08-08 04:31:11.
*/

#include "runtimeC.h"

int32_t
PTR_LOCATE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PTR_LOCATE");
  // PASSED_FLAGS = FLAGS; (889)
  {
    descriptor_t *bitRHS = getBIT (8, mPTR_LOCATExFLAGS);
    putBIT (8, mPTR_LOCATExPASSED_FLAGS, bitRHS);
    bitRHS->inUse = 0;
  }
  // PAGE = SHR(PTR,16) &  65535; (890)
  {
    int32_t numberRHS
        = (int32_t)(xAND (SHR (getFIXED (mPTR_LOCATExPTR), 16), 65535));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPTR_LOCATExPAGE, bitRHS);
    bitRHS->inUse = 0;
  }
  // OFFSET = PTR &  65535; (891)
  {
    int32_t numberRHS = (int32_t)(xAND (getFIXED (mPTR_LOCATExPTR), 65535));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPTR_LOCATExOFFSET, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF OFFSET >= VMEM_PAGE_SIZE THEN (892)
  if (1 & (xGE (COREHALFWORD (mPTR_LOCATExOFFSET), 3360)))
    // CALL BAD_PTR; (893)
    PTR_LOCATExBAD_PTR (0);
  // IF PAGE = VMEM_PRIOR_PAGE THEN (894)
  if (1
      & (xEQ (COREHALFWORD (mPTR_LOCATExPAGE),
              COREHALFWORD (mVMEM_PRIOR_PAGE))))
    // DO; (895)
    {
    rs1:;
      // VMEM_LOC_PTR = PTR; (896)
      {
        int32_t numberRHS = (int32_t)(getFIXED (mPTR_LOCATExPTR));
        putFIXED (mVMEM_LOC_PTR, numberRHS);
      }
      // VMEM_LOC_ADDR = VMEM_PAD_ADDR(VMEM_OLD_NDX) + OFFSET; (897)
      {
        int32_t numberRHS = (int32_t)(xadd (
            getFIXED (mVMEM_PAD_ADDR + 4 * COREHALFWORD (mVMEM_OLD_NDX)),
            COREHALFWORD (mPTR_LOCATExOFFSET)));
        putFIXED (mVMEM_LOC_ADDR, numberRHS);
      }
      // IF FLAGS ~= 0 THEN (898)
      if (1 & (xNEQ (BYTE0 (mPTR_LOCATExFLAGS), 0)))
        // CALL DISP(FLAGS); (899)
        {
          putBITp (8, mDISPxFLAGS, getBIT (8, mPTR_LOCATExFLAGS));
          DISP (0);
        }
      // CALL SAVE_PTR_STATE(VMEM_OLD_NDX); (900)
      {
        putBITp (16, mPTR_LOCATExSAVE_PTR_STATExINDEX,
                 getBIT (16, mVMEM_OLD_NDX));
        PTR_LOCATExSAVE_PTR_STATE (0);
      }
      // RETURN; (901)
      {
        reentryGuard = 0;
        return 0;
      }
    es1:;
    } // End of DO block
  // VMEM_PRIOR_PAGE = PAGE; (902)
  {
    descriptor_t *bitRHS = getBIT (16, mPTR_LOCATExPAGE);
    putBIT (16, mVMEM_PRIOR_PAGE, bitRHS);
    bitRHS->inUse = 0;
  }
  // VMEM_LOC_CNT = VMEM_LOC_CNT + 1; (903)
  {
    int32_t numberRHS = (int32_t)(xadd (getFIXED (mVMEM_LOC_CNT), 1));
    putFIXED (mVMEM_LOC_CNT, numberRHS);
  }
  // IF PAGE > VMEM_LAST_PAGE THEN (904)
  if (1
      & (xGT (COREHALFWORD (mPTR_LOCATExPAGE),
              COREHALFWORD (mVMEM_LAST_PAGE))))
    // DO; (905)
    {
    rs2:;
      // IF (PAGE-1)~=VMEM_LAST_PAGE THEN (906)
      if (1
          & (xNEQ (xsubtract (COREHALFWORD (mPTR_LOCATExPAGE), 1),
                   COREHALFWORD (mVMEM_LAST_PAGE))))
        // CALL BAD_PTR; (907)
        PTR_LOCATExBAD_PTR (0);
      // VMEM_LAST_PAGE = PAGE; (908)
      {
        descriptor_t *bitRHS = getBIT (16, mPTR_LOCATExPAGE);
        putBIT (16, mVMEM_LAST_PAGE, bitRHS);
        bitRHS->inUse = 0;
      }
      // VMEM_PAGE_AVAIL_SPACE(VMEM_LAST_PAGE) = VMEM_PAGE_SIZE; (909)
      {
        int32_t numberRHS = (int32_t)(3360);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16,
                mVMEM_PAGE_AVAIL_SPACE + 2 * (COREHALFWORD (mVMEM_LAST_PAGE)),
                bitRHS);
        bitRHS->inUse = 0;
      }
      // IF VMEM_LAST_PAGE <= VMEM_MAX_PAGE THEN (910)
      if (1
          & (xLE (COREHALFWORD (mVMEM_LAST_PAGE),
                  COREHALFWORD (mVMEM_MAX_PAGE))))
        // DO; (911)
        {
        rs2s1:;
          // CUR_NDX = VMEM_LAST_PAGE; (912)
          {
            descriptor_t *bitRHS = getBIT (16, mVMEM_LAST_PAGE);
            putBIT (16, mPTR_LOCATExCUR_NDX, bitRHS);
            bitRHS->inUse = 0;
          }
        es2s1:;
        } // End of DO block
      // ELSE (913)
      else
        // DO; (914)
        {
        rs2s2:;
          // IF VMEM_LAST_PAGE > VMEM_TOTAL_PAGES THEN (915)
          if (1 & (xGT (COREHALFWORD (mVMEM_LAST_PAGE), 399)))
            // DO; (916)
            {
            rs2s2s1:;
              // CALL ERRORS(CLASS_BI,702); (917)
              {
                putBITp (16, mERRORSxCLASS, getBIT (16, mCLASS_BI));
                putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(702)));
                ERRORS (0);
              }
              // CALL EXIT; (918)
              EXIT ();
            es2s2s1:;
            } // End of DO block
          // CALL PAGING_STRATEGY; (919)
          PTR_LOCATExPAGING_STRATEGY (0);
        es2s2:;
        } // End of DO block
      // FLAGS = FLAGS|MODF; (920)
      {
        int32_t numberRHS
            = (int32_t)(xOR (BYTE0 (mPTR_LOCATExFLAGS), BYTE0 (mMODF)));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (8, mPTR_LOCATExFLAGS, bitRHS);
        bitRHS->inUse = 0;
      }
      // CALL ZERO_CORE(VMEM_PAD_ADDR(CUR_NDX),VMEM_PAGE_SIZE); (921)
      {
        putFIXED (mZERO_CORExCORE_ADDR,
                  getFIXED (mVMEM_PAD_ADDR
                            + 4 * COREHALFWORD (mPTR_LOCATExCUR_NDX)));
        putFIXED (mZERO_CORExCOUNT, 3360);
        ZERO_CORE (0);
      }
      // GO TO LOC_COMMON1; (922)
      goto LOC_COMMON1;
    es2:;
    } // End of DO block
  // ELSE (923)
  else
    // DO; (924)
    {
    rs3:;
      // CUR_NDX = VMEM_PAGE_TO_NDX(PAGE); (925)
      {
        descriptor_t *bitRHS = getBIT (
            16, mVMEM_PAGE_TO_NDX + 2 * COREHALFWORD (mPTR_LOCATExPAGE));
        putBIT (16, mPTR_LOCATExCUR_NDX, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF CUR_NDX = -1 THEN (926)
      if (1 & (xEQ (COREHALFWORD (mPTR_LOCATExCUR_NDX), -1)))
        // DO; (927)
        {
        rs3s1:;
          // CALL PAGING_STRATEGY; (928)
          PTR_LOCATExPAGING_STRATEGY (0);
          // IF VMEM_LOOK_AHEAD_PAGE >= 0 THEN (929)
          if (1 & (xGE (COREHALFWORD (mVMEM_LOOK_AHEAD_PAGE), 0)))
            // DO; (930)
            {
            rs3s1s1:;
              // CALL MONITOR(31,0,-1); (931)
              MONITOR31 (0, -1);
              // VMEM_LOOK_AHEAD_PAGE = -1; (932)
              {
                int32_t numberRHS = (int32_t)(-1);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16, mVMEM_LOOK_AHEAD_PAGE, bitRHS);
                bitRHS->inUse = 0;
              }
            es3s1s1:;
            } // End of DO block
          // COREWORD(ADDR(VMEM_PAGE)) = VMEM_PAD_ADDR(CUR_NDX); (933)
          {
            int32_t numberRHS = (int32_t)(getFIXED (
                mVMEM_PAD_ADDR + 4 * COREHALFWORD (mPTR_LOCATExCUR_NDX)));
            COREWORD2 (ADDR ("PTR_LOCATExVMEM_PAGE", 0x80000000, NULL, 0),
                       numberRHS);
          }
          // VMEM_PAGE = FILE(VMEM_FILE#,PAGE); (934)
          {
            rFILE (ADDR ("PTR_LOCATExVMEM_PAGE", 0, NULL, 0), 6,
                   COREHALFWORD (mPTR_LOCATExPAGE));
          }
          // VMEM_READ_CNT = VMEM_READ_CNT + 1; (935)
          {
            int32_t numberRHS = (int32_t)(xadd (getFIXED (mVMEM_READ_CNT), 1));
            putFIXED (mVMEM_READ_CNT, numberRHS);
          }
        // LOC_COMMON1: (936)
        LOC_COMMON1:
          // VMEM_PAGE_TO_NDX(PAGE) = CUR_NDX; (937)
          {
            descriptor_t *bitRHS = getBIT (16, mPTR_LOCATExCUR_NDX);
            putBIT (16,
                    mVMEM_PAGE_TO_NDX + 2 * (COREHALFWORD (mPTR_LOCATExPAGE)),
                    bitRHS);
            bitRHS->inUse = 0;
          }
          // VMEM_PAD_PAGE(CUR_NDX) = PAGE; (938)
          {
            descriptor_t *bitRHS = getBIT (16, mPTR_LOCATExPAGE);
            putBIT (16,
                    mVMEM_PAD_PAGE + 2 * (COREHALFWORD (mPTR_LOCATExCUR_NDX)),
                    bitRHS);
            bitRHS->inUse = 0;
          }
          // VMEM_PAD_DISP(CUR_NDX) = 0; (939)
          {
            int32_t numberRHS = (int32_t)(0);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (16,
                    mVMEM_PAD_DISP + 2 * (COREHALFWORD (mPTR_LOCATExCUR_NDX)),
                    bitRHS);
            bitRHS->inUse = 0;
          }
        es3s1:;
        } // End of DO block
      // ELSE (940)
      else
        // DO; (941)
        {
        rs3s2:;
          // IF PAGE = VMEM_LOOK_AHEAD_PAGE THEN (942)
          if (1
              & (xEQ (COREHALFWORD (mPTR_LOCATExPAGE),
                      COREHALFWORD (mVMEM_LOOK_AHEAD_PAGE))))
            // DO; (943)
            {
            rs3s2s1:;
              // CALL MONITOR(31,0,-1); (944)
              MONITOR31 (0, -1);
              // VMEM_LOOK_AHEAD_PAGE = -1; (945)
              {
                int32_t numberRHS = (int32_t)(-1);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16, mVMEM_LOOK_AHEAD_PAGE, bitRHS);
                bitRHS->inUse = 0;
              }
            es3s2s1:;
            } // End of DO block
        es3s2:;
        } // End of DO block
      // VMEM_PAD_CNT(CUR_NDX) = VMEM_LOC_CNT; (946)
      {
        int32_t numberRHS = (int32_t)(getFIXED (mVMEM_LOC_CNT));
        putFIXED (mVMEM_PAD_CNT + 4 * (COREHALFWORD (mPTR_LOCATExCUR_NDX)),
                  numberRHS);
      }
    es3:;
    } // End of DO block
  // CALL SAVE_PTR_STATE(CUR_NDX); (947)
  {
    putBITp (16, mPTR_LOCATExSAVE_PTR_STATExINDEX,
             getBIT (16, mPTR_LOCATExCUR_NDX));
    PTR_LOCATExSAVE_PTR_STATE (0);
  }
  // VMEM_OLD_NDX = CUR_NDX; (948)
  {
    descriptor_t *bitRHS = getBIT (16, mPTR_LOCATExCUR_NDX);
    putBIT (16, mVMEM_OLD_NDX, bitRHS);
    bitRHS->inUse = 0;
  }
  // VMEM_LOC_PTR = PTR; (949)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mPTR_LOCATExPTR));
    putFIXED (mVMEM_LOC_PTR, numberRHS);
  }
  // VMEM_LOC_ADDR = VMEM_PAD_ADDR(CUR_NDX) + OFFSET; (950)
  {
    int32_t numberRHS = (int32_t)(xadd (
        getFIXED (mVMEM_PAD_ADDR + 4 * COREHALFWORD (mPTR_LOCATExCUR_NDX)),
        COREHALFWORD (mPTR_LOCATExOFFSET)));
    putFIXED (mVMEM_LOC_ADDR, numberRHS);
  }
  // IF FLAGS ~= 0 THEN (951)
  if (1 & (xNEQ (BYTE0 (mPTR_LOCATExFLAGS), 0)))
    // CALL DISP(FLAGS); (952)
    {
      putBITp (8, mDISPxFLAGS, getBIT (8, mPTR_LOCATExFLAGS));
      DISP (0);
    }
  // IF VMEM_LOOK_AHEAD THEN (953)
  if (1 & (bitToFixed (getBIT (1, mVMEM_LOOK_AHEAD))))
    // DO; (954)
    {
    rs4:;
      // IF PAGE < VMEM_LAST_PAGE THEN (955)
      if (1
          & (xLT (COREHALFWORD (mPTR_LOCATExPAGE),
                  COREHALFWORD (mVMEM_LAST_PAGE))))
        // DO; (956)
        {
        rs4s1:;
          // PAGE = PAGE + 1; (957)
          {
            int32_t numberRHS
                = (int32_t)(xadd (COREHALFWORD (mPTR_LOCATExPAGE), 1));
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (16, mPTR_LOCATExPAGE, bitRHS);
            bitRHS->inUse = 0;
          }
          // CUR_NDX = VMEM_PAGE_TO_NDX(PAGE); (958)
          {
            descriptor_t *bitRHS = getBIT (
                16, mVMEM_PAGE_TO_NDX + 2 * COREHALFWORD (mPTR_LOCATExPAGE));
            putBIT (16, mPTR_LOCATExCUR_NDX, bitRHS);
            bitRHS->inUse = 0;
          }
          // IF CUR_NDX = -1 THEN (959)
          if (1 & (xEQ (COREHALFWORD (mPTR_LOCATExCUR_NDX), -1)))
            // DO; (960)
            {
            rs4s1s1:;
              // VMEM_PAD_DISP(VMEM_OLD_NDX) = VMEM_PAD_DISP(VMEM_OLD_NDX) + 1;
              // (961)
              {
                int32_t numberRHS = (int32_t)(xadd (
                    COREHALFWORD (mVMEM_PAD_DISP
                                  + 2 * COREHALFWORD (mVMEM_OLD_NDX)),
                    1));
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16,
                        mVMEM_PAD_DISP + 2 * (COREHALFWORD (mVMEM_OLD_NDX)),
                        bitRHS);
                bitRHS->inUse = 0;
              }
              // IF VMEM_LAST_PAGE <= VMEM_MAX_PAGE THEN (962)
              if (1
                  & (xLE (COREHALFWORD (mVMEM_LAST_PAGE),
                          COREHALFWORD (mVMEM_MAX_PAGE))))
                // CUR_NDX = VMEM_LAST_PAGE; (963)
                {
                  descriptor_t *bitRHS = getBIT (16, mVMEM_LAST_PAGE);
                  putBIT (16, mPTR_LOCATExCUR_NDX, bitRHS);
                  bitRHS->inUse = 0;
                }
              // ELSE (964)
              else
                // CALL PAGING_STRATEGY; (965)
                PTR_LOCATExPAGING_STRATEGY (0);
              // VMEM_PAD_DISP(VMEM_OLD_NDX) = VMEM_PAD_DISP(VMEM_OLD_NDX) - 1;
              // (966)
              {
                int32_t numberRHS = (int32_t)(xsubtract (
                    COREHALFWORD (mVMEM_PAD_DISP
                                  + 2 * COREHALFWORD (mVMEM_OLD_NDX)),
                    1));
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16,
                        mVMEM_PAD_DISP + 2 * (COREHALFWORD (mVMEM_OLD_NDX)),
                        bitRHS);
                bitRHS->inUse = 0;
              }
              // BUFF_ADDR = VMEM_PAD_ADDR(CUR_NDX); (967)
              {
                int32_t numberRHS = (int32_t)(getFIXED (
                    mVMEM_PAD_ADDR + 4 * COREHALFWORD (mPTR_LOCATExCUR_NDX)));
                putFIXED (mPTR_LOCATExBUFF_ADDR, numberRHS);
              }
              // IF VMEM_LOOK_AHEAD_PAGE >= 0 THEN (968)
              if (1 & (xGE (COREHALFWORD (mVMEM_LOOK_AHEAD_PAGE), 0)))
                // BUFF_ADDR = BUFF_ADDR |  2147483648; (969)
                {
                  int32_t numberRHS = (int32_t)(xOR (
                      getFIXED (mPTR_LOCATExBUFF_ADDR), 2147483648));
                  putFIXED (mPTR_LOCATExBUFF_ADDR, numberRHS);
                }
              // CALL MONITOR(31,BUFF_ADDR,PAGE); (970)
              MONITOR31 (getFIXED (mPTR_LOCATExBUFF_ADDR),
                         COREHALFWORD (mPTR_LOCATExPAGE));
              // VMEM_LOOK_AHEAD_PAGE = PAGE; (971)
              {
                descriptor_t *bitRHS = getBIT (16, mPTR_LOCATExPAGE);
                putBIT (16, mVMEM_LOOK_AHEAD_PAGE, bitRHS);
                bitRHS->inUse = 0;
              }
              // VMEM_READ_CNT = VMEM_READ_CNT + 1; (972)
              {
                int32_t numberRHS
                    = (int32_t)(xadd (getFIXED (mVMEM_READ_CNT), 1));
                putFIXED (mVMEM_READ_CNT, numberRHS);
              }
              // VMEM_PAGE_TO_NDX(PAGE) = CUR_NDX; (973)
              {
                descriptor_t *bitRHS = getBIT (16, mPTR_LOCATExCUR_NDX);
                putBIT (16,
                        mVMEM_PAGE_TO_NDX
                            + 2 * (COREHALFWORD (mPTR_LOCATExPAGE)),
                        bitRHS);
                bitRHS->inUse = 0;
              }
              // VMEM_PAD_PAGE(CUR_NDX) = PAGE; (974)
              {
                descriptor_t *bitRHS = getBIT (16, mPTR_LOCATExPAGE);
                putBIT (16,
                        mVMEM_PAD_PAGE
                            + 2 * (COREHALFWORD (mPTR_LOCATExCUR_NDX)),
                        bitRHS);
                bitRHS->inUse = 0;
              }
              // VMEM_PAD_DISP(CUR_NDX) = 0; (975)
              {
                int32_t numberRHS = (int32_t)(0);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16,
                        mVMEM_PAD_DISP
                            + 2 * (COREHALFWORD (mPTR_LOCATExCUR_NDX)),
                        bitRHS);
                bitRHS->inUse = 0;
              }
              // VMEM_PAD_CNT(CUR_NDX) = VMEM_LOC_CNT - 1; (976)
              {
                int32_t numberRHS
                    = (int32_t)(xsubtract (getFIXED (mVMEM_LOC_CNT), 1));
                putFIXED (mVMEM_PAD_CNT
                              + 4 * (COREHALFWORD (mPTR_LOCATExCUR_NDX)),
                          numberRHS);
              }
            es4s1s1:;
            } // End of DO block
        es4s1:;
        } // End of DO block
    es4:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
