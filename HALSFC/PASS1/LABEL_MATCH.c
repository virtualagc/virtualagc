/*
  File LABEL_MATCH.c generated by XCOM-I, 2024-08-09 12:38:15.
*/

#include "runtimeC.h"

descriptor_t *
LABEL_MATCH (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "LABEL_MATCH");
  // IF FIXL(MPP1)=0 THEN (7286)
  if (1 & (xEQ (getFIXED (mFIXL + 4 * getFIXED (mMPP1)), 0)))
    // RETURN 1; (7287)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(1));
    }
  // LOC=DO_PARSE(TEMP); (7288)
  {
    descriptor_t *bitRHS = getBIT (16, mDO_PARSE + 2 * getFIXED (mTEMP));
    putBIT (16, mLABEL_MATCHxLOC, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO WHILE PARSE_STACK(LOC)=LABEL_DEFINITION; (7289)
  while (1
         & (xEQ (
             COREHALFWORD (mPARSE_STACK + 2 * COREHALFWORD (mLABEL_MATCHxLOC)),
             getFIXED (mLABEL_DEFINITION))))
    {
      // IF FIXL(LOC)=FIXL(MPP1) THEN (7290)
      if (1
          & (xEQ (getFIXED (mFIXL + 4 * COREHALFWORD (mLABEL_MATCHxLOC)),
                  getFIXED (mFIXL + 4 * getFIXED (mMPP1)))))
        // RETURN 1; (7291)
        {
          reentryGuard = 0;
          return fixedToBit (32, (int32_t)(1));
        }
      // LOC=LOC-1; (7292)
      {
        int32_t numberRHS
            = (int32_t)(xsubtract (COREHALFWORD (mLABEL_MATCHxLOC), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mLABEL_MATCHxLOC, bitRHS);
        bitRHS->inUse = 0;
      }
    } // End of DO WHILE block
  // RETURN 0; (7293)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(0));
  }
}
