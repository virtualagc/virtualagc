/*
  File ENTER_LAYOUT.c generated by XCOM-I, 2024-08-08 04:31:11.
*/

#include "runtimeC.h"

int32_t
ENTER_LAYOUT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "ENTER_LAYOUT");
  // PROGRAM_LAYOUT_INDEX = PROGRAM_LAYOUT_INDEX + 1; (1501)
  {
    int32_t numberRHS
        = (int32_t)(xadd (COREHALFWORD (mPROGRAM_LAYOUT_INDEX), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPROGRAM_LAYOUT_INDEX, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF PROGRAM_LAYOUT_INDEX > PROGRAM_LAYOUT_LIM THEN (1502)
  if (1 & (xGT (COREHALFWORD (mPROGRAM_LAYOUT_INDEX), 255)))
    // DO; (1503)
    {
    rs1:;
      // PROGRAM_LAYOUT_INDEX = PROGRAM_LAYOUT_LIM; (1504)
      {
        int32_t numberRHS = (int32_t)(255);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mPROGRAM_LAYOUT_INDEX, bitRHS);
        bitRHS->inUse = 0;
      }
      // CALL ERROR(CLASS_P,6); (1505)
      {
        putBITp (16, mERRORxCLASS, getBIT (16, mCLASS_P));
        putBITp (8, mERRORxNUM, fixedToBit (32, (int32_t)(6)));
        ERROR (0);
      }
    es1:;
    } // End of DO block
  // ELSE (1506)
  else
    // PROGRAM_LAYOUT(PROGRAM_LAYOUT_INDEX) = NDX; (1507)
    {
      descriptor_t *bitRHS = getBIT (16, mENTER_LAYOUTxNDX);
      putBIT (16, mPROGRAM_LAYOUT + 2 * (COREHALFWORD (mPROGRAM_LAYOUT_INDEX)),
              bitRHS);
      bitRHS->inUse = 0;
    }
  // RETURN; (1508)
  {
    reentryGuard = 0;
    return 0;
  }
}
