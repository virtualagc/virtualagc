/*
  File EMIT_SMRK.c generated by XCOM-I, 2024-08-08 04:31:11.
*/

#include "runtimeC.h"

int32_t
EMIT_SMRK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "EMIT_SMRK");
  // IF INLINE_LEVEL=0 THEN (7235)
  if (1 & (xEQ (COREHALFWORD (mINLINE_LEVEL), 0)))
    // DO; (7236)
    {
    rs1:;
      // CALL HALMAT_POP(XSMRK,1,XCO_N,STATEMENT_SEVERITY); (7237)
      {
        putBITp (16, mHALMAT_POPxPOPCODE, getBIT (16, mXSMRK));
        putBITp (8, mHALMAT_POPxPIPp, fixedToBit (32, (int32_t)(1)));
        putBITp (8, mHALMAT_POPxCOPT, getBIT (8, mXCO_N));
        putBITp (8, mHALMAT_POPxTAG,
                 fixedToBit (32, (int32_t)(getFIXED (mSTATEMENT_SEVERITY))));
        HALMAT_POP (0);
      }
      // CALL HALMAT_PIP(STMT_NUM, 0, SMRK_FLAG, T>1); (7238)
      {
        putBITp (16, mHALMAT_PIPxOPERAND,
                 fixedToBit (32, (int32_t)(getFIXED (mCOMM + 4 * 3))));
        putBITp (8, mHALMAT_PIPxQUAL, fixedToBit (32, (int32_t)(0)));
        putBITp (8, mHALMAT_PIPxTAG1, getBIT (8, mSMRK_FLAG));
        putBITp (
            8, mHALMAT_PIPxTAG2,
            fixedToBit (32, (int32_t)(xGT (COREHALFWORD (mEMIT_SMRKxT), 1))));
        HALMAT_PIP (0);
      }
      // IF HALMAT_RELOCATE_FLAG THEN (7239)
      if (1 & (bitToFixed (getBIT (1, mHALMAT_RELOCATE_FLAG))))
        // CALL HALMAT_RELOCATE; (7240)
        HALMAT_RELOCATE (0);
      // ATOM#_FAULT=NEXT_ATOM#; (7241)
      {
        int32_t numberRHS = (int32_t)(getFIXED (mNEXT_ATOMp));
        putFIXED (mATOMp_FAULT, numberRHS);
      }
    es1:;
    } // End of DO block
  // ELSE (7242)
  else
    // IF T<5 THEN (7243)
    if (1 & (xLT (COREHALFWORD (mEMIT_SMRKxT), 5)))
      // DO; (7244)
      {
      rs2:;
        // CALL HALMAT_POP(XIMRK,1,XCO_N,STATEMENT_SEVERITY); (7245)
        {
          putBITp (16, mHALMAT_POPxPOPCODE, getBIT (16, mXIMRK));
          putBITp (8, mHALMAT_POPxPIPp, fixedToBit (32, (int32_t)(1)));
          putBITp (8, mHALMAT_POPxCOPT, getBIT (8, mXCO_N));
          putBITp (8, mHALMAT_POPxTAG,
                   fixedToBit (32, (int32_t)(getFIXED (mSTATEMENT_SEVERITY))));
          HALMAT_POP (0);
        }
        // CALL HALMAT_PIP(STMT_NUM, 0, SMRK_FLAG, T>1); (7246)
        {
          putBITp (16, mHALMAT_PIPxOPERAND,
                   fixedToBit (32, (int32_t)(getFIXED (mCOMM + 4 * 3))));
          putBITp (8, mHALMAT_PIPxQUAL, fixedToBit (32, (int32_t)(0)));
          putBITp (8, mHALMAT_PIPxTAG1, getBIT (8, mSMRK_FLAG));
          putBITp (8, mHALMAT_PIPxTAG2,
                   fixedToBit (
                       32, (int32_t)(xGT (COREHALFWORD (mEMIT_SMRKxT), 1))));
          HALMAT_PIP (0);
        }
      es2:;
      } // End of DO block
  // STATEMENT_SEVERITY=0; (7247)
  {
    int32_t numberRHS = (int32_t)(0);
    putFIXED (mSTATEMENT_SEVERITY, numberRHS);
  }
  // IF SIMULATING THEN (7248)
  if (1 & (bitToFixed (getBIT (8, mSIMULATING))))
    // IF T=3 THEN (7249)
    if (1 & (xEQ (COREHALFWORD (mEMIT_SMRKxT), 3)))
      // CALL STAB_HDR; (7250)
      STAB_HDR (0);
  // IF SRN_PRESENT THEN (7251)
  if (1 & (bitToFixed (getBIT (1, mSRN_PRESENT))))
    // IF T THEN (7252)
    if (1 & (bitToFixed (getBIT (16, mEMIT_SMRKxT))))
      // SRN_FLAG=TRUE; (7253)
      {
        int32_t numberRHS = (int32_t)(1);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mSRN_FLAG, bitRHS);
        bitRHS->inUse = 0;
      }
  // IF INLINE_STMT_RESET>0 THEN (7254)
  if (1 & (xGT (COREHALFWORD (mINLINE_STMT_RESET), 0)))
    // DO; (7255)
    {
    rs3:;
      // STMT_NUM=INLINE_STMT_RESET; (7256)
      {
        descriptor_t *bitRHS = getBIT (16, mINLINE_STMT_RESET);
        int32_t numberRHS;
        numberRHS = bitToFixed (bitRHS);
        putFIXED (mCOMM + 4 * (3), numberRHS);
        bitRHS->inUse = 0;
      }
      // INLINE_STMT_RESET=0; (7257)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mINLINE_STMT_RESET, bitRHS);
        bitRHS->inUse = 0;
      }
    es3:;
    } // End of DO block
  // ELSE (7258)
  else
    // IF T THEN (7259)
    if (1 & (bitToFixed (getBIT (16, mEMIT_SMRKxT))))
      // STMT_NUM=STMT_NUM+1; (7260)
      {
        int32_t numberRHS = (int32_t)(xadd (getFIXED (mCOMM + 4 * 3), 1));
        putFIXED (mCOMM + 4 * (3), numberRHS);
      }
  // T=3; (7261)
  {
    int32_t numberRHS = (int32_t)(3);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mEMIT_SMRKxT, bitRHS);
    bitRHS->inUse = 0;
  }
  // SMRK_FLAG = 0; (7262)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mSMRK_FLAG, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
