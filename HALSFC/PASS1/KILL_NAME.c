/*
  File KILL_NAME.c generated by XCOM-I, 2024-08-08 04:31:11.
*/

#include "runtimeC.h"

descriptor_t *
KILL_NAME (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "KILL_NAME");
  // IF NAME_PSEUDOS THEN (7784)
  if (1 & (bitToFixed (getBIT (1, mNAME_PSEUDOS))))
    // DO; (7785)
    {
    rs1:;
      // IF (VAL_P(PTR(LOC))& 1024)=0 THEN (7786)
      if (1
          & (xEQ (
              xAND (COREHALFWORD (
                        mVAL_P
                        + 2
                              * COREHALFWORD (
                                  mPTR + 2 * COREHALFWORD (mKILL_NAMExLOC))),
                    1024),
              0)))
        // CALL RESET_ARRAYNESS; (7787)
        RESET_ARRAYNESS (0);
      // CALL CHECK_ARRAYNESS; (7788)
      CHECK_ARRAYNESS (0);
      // NAME_PSEUDOS=FALSE; (7789)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mNAME_PSEUDOS, bitRHS);
        bitRHS->inUse = 0;
      }
      // RETURN 1; (7790)
      {
        reentryGuard = 0;
        return fixedToBit (32, (int32_t)(1));
      }
    es1:;
    } // End of DO block
  // RETURN 0; (7791)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(0));
  }
}
