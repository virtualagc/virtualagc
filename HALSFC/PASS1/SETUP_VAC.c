/*
  File SETUP_VAC.c generated by XCOM-I, 2024-08-08 04:31:11.
*/

#include "runtimeC.h"

int32_t
SETUP_VAC (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SETUP_VAC");
  // LOC_P(PTR(LOC))=LAST_POP#; (7303)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mLAST_POPp));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (
        16,
        mLOC_P + 2 * (COREHALFWORD (mPTR + 2 * COREHALFWORD (mSETUP_VACxLOC))),
        bitRHS);
    bitRHS->inUse = 0;
  }
  // PSEUDO_FORM(PTR(LOC))=XVAC; (7304)
  {
    descriptor_t *bitRHS = getBIT (8, mXVAC);
    putBIT (
        8,
        mPSEUDO_FORM
            + 1 * (COREHALFWORD (mPTR + 2 * COREHALFWORD (mSETUP_VACxLOC))),
        bitRHS);
    bitRHS->inUse = 0;
  }
  // PSEUDO_TYPE(PTR(LOC))=TYPE; (7305)
  {
    descriptor_t *bitRHS = getBIT (16, mSETUP_VACxTYPE);
    putBIT (
        8,
        mPSEUDO_TYPE
            + 1 * (COREHALFWORD (mPTR + 2 * COREHALFWORD (mSETUP_VACxLOC))),
        bitRHS);
    bitRHS->inUse = 0;
  }
  // IF SIZE > 0 THEN (7306)
  if (1 & (xGT (COREHALFWORD (mSETUP_VACxSIZE), 0)))
    // PSEUDO_LENGTH(PTR(LOC)) = SIZE; (7307)
    {
      descriptor_t *bitRHS = getBIT (16, mSETUP_VACxSIZE);
      putBIT (
          16,
          mPSEUDO_LENGTH
              + 2 * (COREHALFWORD (mPTR + 2 * COREHALFWORD (mSETUP_VACxLOC))),
          bitRHS);
      bitRHS->inUse = 0;
    }
  // SIZE = -1; (7308)
  {
    int32_t numberRHS = (int32_t)(-1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mSETUP_VACxSIZE, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
