/*
  File SYT_DUMPxPRINT_SYMBOL_HEADER.c generated by XCOM-I, 2024-08-09 12:38:15.
*/

#include "runtimeC.h"

int32_t
SYT_DUMPxPRINT_SYMBOL_HEADER (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SYT_DUMPxPRINT_SYMBOL_HEADER");
  // IF STRUC THEN (5864)
  if (1 & (bitToFixed (getBIT (1, mSYT_DUMPxPRINT_SYMBOL_HEADERxSTRUC))))
    // OUTPUT(1)='0S T R U C T U R E   T E M P L A T E   S Y M B O L  &  C R O
    // S S   R E F E R E N C E   T A B L E   L I S T I N G :'; (5865)
    {
      descriptor_t *stringRHS;
      stringRHS = cToDescriptor (
          NULL, "0S T R U C T U R E   T E M P L A T E   S Y M B O L  &  C R O "
                "S S   R E F E R E N C E   T A B L E   L I S T I N G :");
      OUTPUT (1, stringRHS);
      stringRHS->inUse = 0;
    }
  // ELSE (5866)
  else
    // DO; (5867)
    {
    rs1:;
      // HEADER_PRINTED=TRUE; (5868)
      {
        int32_t numberRHS = (int32_t)(1);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mSYT_DUMPxHEADER_PRINTED, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF NEW_PAGE THEN (5869)
      if (1
          & (bitToFixed (getBIT (1, mSYT_DUMPxPRINT_SYMBOL_HEADERxNEW_PAGE))))
        // DO; (5870)
        {
        rs1s1:;
          // OUTPUT(1)=SUBHEADING; (5871)
          {
            descriptor_t *stringRHS;
            stringRHS = getCHARACTER (mSUBHEADING);
            OUTPUT (1, stringRHS);
            stringRHS->inUse = 0;
          }
          // EJECT_PAGE; (5872)
          {
            descriptor_t *stringRHS;
            stringRHS = getCHARACTER (mPAGE);
            OUTPUT (1, stringRHS);
            stringRHS->inUse = 0;
          }
        es1s1:;
        } // End of DO block
      // OUTPUT(1) = '0S Y M B O L  &  C R O S S   R E F E R E N C E   T A B L
      // E   L I S T I N G :'; (5873)
      {
        descriptor_t *stringRHS;
        stringRHS
            = cToDescriptor (NULL, "0S Y M B O L  &  C R O S S   R E F E R E "
                                   "N C E   T A B L E   L I S T I N G :");
        OUTPUT (1, stringRHS);
        stringRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // IF MAX_LENGTH < 4 THEN (5874)
  if (1 & (xLT (COREHALFWORD (mSYT_DUMPxMAX_LENGTH), 4)))
    // J = 4; (5875)
    {
      int32_t numberRHS = (int32_t)(4);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mSYT_DUMPxPRINT_SYMBOL_HEADERxJ, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (5876)
  else
    // J = MAX_LENGTH; (5877)
    {
      descriptor_t *bitRHS = getBIT (16, mSYT_DUMPxMAX_LENGTH);
      putBIT (16, mSYT_DUMPxPRINT_SYMBOL_HEADERxJ, bitRHS);
      bitRHS->inUse = 0;
    }
  // OUTPUT(1) = '0         (CROSS REFERENCE FLAG KEY:  4 = ASSIGNMENT, 2 =
  // REFERENCE, 1 = SUBSCRIPT USE, 0 = DEFINITION)'; (5878)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (
        NULL, "0         (CROSS REFERENCE FLAG KEY:  4 = ASSIGNMENT, 2 = "
              "REFERENCE, 1 = SUBSCRIPT USE, 0 = DEFINITION)");
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S = ' DCL  NAME'||SUBSTR(X70,0,J-3)||'        TYPE ATTRIBUTES & CROSS
  // REFERENCE'; (5879)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (
        xsCAT (
            cToDescriptor (NULL, " DCL  NAME"),
            SUBSTR (getCHARACTER (mX70), 0,
                    xsubtract (COREHALFWORD (mSYT_DUMPxPRINT_SYMBOL_HEADERxJ),
                               3))),
        cToDescriptor (NULL, "        TYPE                          "
                             "ATTRIBUTES & CROSS REFERENCE"));
    putCHARACTER (mSYT_DUMPxS, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = DOUBLE || S; (5880)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (getCHARACTER (mDOUBLE), getCHARACTER (mSYT_DUMPxS));
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = SUBHEADING || S; (5881)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (getCHARACTER (mSUBHEADING), getCHARACTER (mSYT_DUMPxS));
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = X1; (5882)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mX1);
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
