/*
  File VECTOR_COMPARE.c generated by XCOM-I, 2024-08-09 12:38:15.
*/

#include "runtimeC.h"

int32_t
VECTOR_COMPARE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "VECTOR_COMPARE");
  // IF PSEUDO_LENGTH(PTR(I))~=PSEUDO_LENGTH(PTR(J)) THEN (7309)
  if (1
      & (xNEQ (COREHALFWORD (
                   mPSEUDO_LENGTH
                   + 2
                         * COREHALFWORD (
                             mPTR + 2 * COREHALFWORD (mVECTOR_COMPARExI))),
               COREHALFWORD (
                   mPSEUDO_LENGTH
                   + 2
                         * COREHALFWORD (
                             mPTR + 2 * COREHALFWORD (mVECTOR_COMPARExJ))))))
    // CALL ERROR(CLASS,NUM); (7310)
    {
      putBITp (16, mERRORxCLASS, getBIT (16, mVECTOR_COMPARExCLASS));
      putBITp (8, mERRORxNUM, getBIT (16, mVECTOR_COMPARExNUM));
      ERROR (0);
    }
  {
    reentryGuard = 0;
    return 0;
  }
}
