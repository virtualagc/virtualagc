/*
  File HALMAT_PIP.c generated by XCOM-I, 2024-08-08 04:33:34.
*/

#include "runtimeC.h"

int32_t
HALMAT_PIP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "HALMAT_PIP");
  // CURRENT_ATOM=SHL(OPERAND,16)|SHL(TAG1,8)|SHL(QUAL& 15,4) |SHL(TAG2& 7,1)|
  // 1; (7234)
  {
    int32_t numberRHS = (int32_t)(xOR (
        xOR (xOR (xOR (SHL (COREHALFWORD (mHALMAT_PIPxOPERAND), 16),
                       SHL (BYTE0 (mHALMAT_PIPxTAG1), 8)),
                  SHL (xAND (BYTE0 (mHALMAT_PIPxQUAL), 15), 4)),
             SHL (xAND (BYTE0 (mHALMAT_PIPxTAG2), 7), 1)),
        1));
    putFIXED (mCURRENT_ATOM, numberRHS);
  }
  // CALL HALMAT; (7235)
  HALMAT (0);
  {
    reentryGuard = 0;
    return 0;
  }
}
