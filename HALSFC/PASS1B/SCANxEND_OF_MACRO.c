/*
  File SCANxEND_OF_MACRO.c generated by XCOM-I, 2024-08-08 04:33:34.
*/

#include "runtimeC.h"

descriptor_t *
SCANxEND_OF_MACRO (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SCANxEND_OF_MACRO");
  // MP=MACRO_POINT; (6753)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mMACRO_POINT));
    putFIXED (mSCANxEND_OF_MACROxMP, numberRHS);
  }
  // DO WHILE (MACRO_TEXT(MP)= 238 | MACRO_TEXT(MP)=BYTE(X1)); (6754)
  while (
      1
      & (xOR (
          xEQ (BYTE0 (getFIXED (mMACRO_TEXTS)
                      + 1 * (getFIXED (mSCANxEND_OF_MACROxMP)) + 0 + 1 * (0)),
               238),
          xEQ (BYTE0 (getFIXED (mMACRO_TEXTS)
                      + 1 * (getFIXED (mSCANxEND_OF_MACROxMP)) + 0 + 1 * (0)),
               BYTE1 (getCHARACTER (mX1))))))
    {
      // IF MACRO_TEXT(MP)= 238 THEN (6755)
      if (1
          & (xEQ (BYTE0 (getFIXED (mMACRO_TEXTS)
                         + 1 * (getFIXED (mSCANxEND_OF_MACROxMP)) + 0
                         + 1 * (0)),
                  238)))
        // MP=MP+1; (6756)
        {
          int32_t numberRHS
              = (int32_t)(xadd (getFIXED (mSCANxEND_OF_MACROxMP), 1));
          putFIXED (mSCANxEND_OF_MACROxMP, numberRHS);
        }
      // MP=MP+1; (6757)
      {
        int32_t numberRHS
            = (int32_t)(xadd (getFIXED (mSCANxEND_OF_MACROxMP), 1));
        putFIXED (mSCANxEND_OF_MACROxMP, numberRHS);
      }
    } // End of DO WHILE block
  // IF MACRO_TEXT(MP)= 239 THEN (6758)
  if (1
      & (xEQ (BYTE0 (getFIXED (mMACRO_TEXTS)
                     + 1 * (getFIXED (mSCANxEND_OF_MACROxMP)) + 0 + 1 * (0)),
              239)))
    // IF NEXT_CHAR=BYTE(X1) THEN (6759)
    if (1 & (xEQ (BYTE0 (mNEXT_CHAR), BYTE1 (getCHARACTER (mX1)))))
      // RETURN TRUE; (6760)
      {
        reentryGuard = 0;
        return fixedToBit (32, (int32_t)(1));
      }
  // RETURN FALSE; (6761)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(0));
  }
}
