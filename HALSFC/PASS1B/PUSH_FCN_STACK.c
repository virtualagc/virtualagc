/*
  File PUSH_FCN_STACK.c generated by XCOM-I, 2024-08-09 12:40:41.
*/

#include "runtimeC.h"

int32_t
PUSH_FCN_STACK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PUSH_FCN_STACK");
  // FCN_LV=FCN_LV+1; (7504)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mFCN_LV), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mFCN_LV, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF FCN_LV>FCN_LV_MAX THEN (7505)
  if (1 & (xGT (COREHALFWORD (mFCN_LV), 10)))
    // DO; (7506)
    {
    rs1:;
      // CALL ERROR(CLASS_BS,5); (7507)
      {
        putBITp (16, mERRORxCLASS, getBIT (16, mCLASS_BS));
        putBITp (8, mERRORxNUM, fixedToBit (32, (int32_t)(5)));
        ERROR (0);
      }
      // RETURN FALSE; (7508)
      {
        reentryGuard = 0;
        return 0;
      }
    es1:;
    } // End of DO block
  // FCN_ARG(FCN_LV)=0; (7509)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mFCN_ARG + 2 * (COREHALFWORD (mFCN_LV)), bitRHS);
    bitRHS->inUse = 0;
  }
  // FCN_MODE(FCN_LV)=MODE; (7510)
  {
    descriptor_t *bitRHS = getBIT (16, mPUSH_FCN_STACKxMODE);
    putBIT (8, mFCN_MODE + 1 * (COREHALFWORD (mFCN_LV)), bitRHS);
    bitRHS->inUse = 0;
  }
  // RETURN TRUE; (7511)
  {
    reentryGuard = 0;
    return 1;
  }
}
