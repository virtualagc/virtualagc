/*
  File GET_FCN_PARM.c generated by XCOM-I, 2024-08-08 04:33:34.
*/

#include "runtimeC.h"

int32_t
GET_FCN_PARM (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GET_FCN_PARM");
  // L=SYT_PTR(FCN_LOC(FCN_LV)); (8031)
  {
    descriptor_t *bitRHS = getBIT (
        16, getFIXED (mSYM_TAB)
                + 34 * (COREHALFWORD (mFCN_LOC + 2 * COREHALFWORD (mFCN_LV)))
                + 22 + 2 * (0));
    putBIT (16, mGET_FCN_PARMxL, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF FCN_ARG(FCN_LV)>0 THEN (8032)
  if (1 & (xGT (COREHALFWORD (mFCN_ARG + 2 * COREHALFWORD (mFCN_LV)), 0)))
    // DO; (8033)
    {
    rs1:;
      // L=L+FCN_ARG(FCN_LV); (8034)
      {
        int32_t numberRHS = (int32_t)(xadd (
            COREHALFWORD (mGET_FCN_PARMxL),
            COREHALFWORD (mFCN_ARG + 2 * COREHALFWORD (mFCN_LV))));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mGET_FCN_PARMxL, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF (SYT_FLAGS(L-1)&ENDSCOPE_FLAG)~=0|(SYT_FLAGS(L)&INPUT_PARM)=0 THEN
      // (8035)
      if (1
          & (xOR (xNEQ (xAND (getFIXED (
                                  getFIXED (mSYM_TAB)
                                  + 34
                                        * (xsubtract (
                                            COREHALFWORD (mGET_FCN_PARMxL), 1))
                                  + 8 + 4 * (0)),
                              16384),
                        0),
                  xEQ (xAND (getFIXED (getFIXED (mSYM_TAB)
                                       + 34 * (COREHALFWORD (mGET_FCN_PARMxL))
                                       + 8 + 4 * (0)),
                             1024),
                       0))))
        // L=0; (8036)
        {
          int32_t numberRHS = (int32_t)(0);
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (16, mGET_FCN_PARMxL, bitRHS);
          bitRHS->inUse = 0;
        }
    es1:;
    } // End of DO block
  // IF L>0 THEN (8037)
  if (1 & (xGT (COREHALFWORD (mGET_FCN_PARMxL), 0)))
    // DO; (8038)
    {
    rs2:;
      // IF (SYT_FLAGS(L)&NAME_FLAG)~=0 THEN (8039)
      if (1
          & (xNEQ (xAND (getFIXED (getFIXED (mSYM_TAB)
                                   + 34 * (COREHALFWORD (mGET_FCN_PARMxL)) + 8
                                   + 4 * (0)),
                         268435456),
                   0)))
        // DO; (8040)
        {
        rs2s1:;
          // VAL_P= 768; (8041)
          {
            int32_t numberRHS = (int32_t)(768);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (16, mVAL_P, bitRHS);
            bitRHS->inUse = 0;
          }
          // NAME_PSEUDOS=TRUE; (8042)
          {
            int32_t numberRHS = (int32_t)(1);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (1, mNAME_PSEUDOS, bitRHS);
            bitRHS->inUse = 0;
          }
        es2s1:;
        } // End of DO block
      // ELSE (8043)
      else
        // VAL_P=0; (8044)
        {
          int32_t numberRHS = (int32_t)(0);
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (16, mVAL_P, bitRHS);
          bitRHS->inUse = 0;
        }
      // FCN_ARG(FCN_LV)=FCN_ARG(FCN_LV)+1; (8045)
      {
        int32_t numberRHS = (int32_t)(xadd (
            COREHALFWORD (mFCN_ARG + 2 * COREHALFWORD (mFCN_LV)), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mFCN_ARG + 2 * (COREHALFWORD (mFCN_LV)), bitRHS);
        bitRHS->inUse = 0;
      }
    es2:;
    } // End of DO block
  // ELSE (8046)
  else
    // FCN_ARG(FCN_LV)=-1; (8047)
    {
      int32_t numberRHS = (int32_t)(-1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mFCN_ARG + 2 * (COREHALFWORD (mFCN_LV)), bitRHS);
      bitRHS->inUse = 0;
    }
  // RETURN L; (8048)
  {
    reentryGuard = 0;
    return COREHALFWORD (mGET_FCN_PARMxL);
  }
}
