/*
  File HALMAT_FIX_PIPTAGS.c generated by XCOM-I, 2024-08-08 04:33:34.
*/

#include "runtimeC.h"

int32_t
HALMAT_FIX_PIPTAGS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "HALMAT_FIX_PIPTAGS");
  // IF HALMAT_OK THEN (7246)
  if (1 & (bitToFixed (getBIT (1, mHALMAT_OK))))
    // ATOMS(PIP_LOC)=(ATOMS(PIP_LOC)& 4294902001)| SHL(TAG1,8)|SHL(TAG2 &
    // 7,1); (7247)
    {
      int32_t numberRHS = (int32_t)(xOR (
          xOR (xAND (getFIXED (getFIXED (mFOR_ATOMS)
                               + 4 * (getFIXED (mHALMAT_FIX_PIPTAGSxPIP_LOC))
                               + 0 + 4 * (0)),
                     4294902001),
               SHL (BYTE0 (mHALMAT_FIX_PIPTAGSxTAG1), 8)),
          SHL (xAND (BYTE0 (mHALMAT_FIX_PIPTAGSxTAG2), 7), 1)));
      putFIXED (getFIXED (mFOR_ATOMS)
                    + 4 * (getFIXED (mHALMAT_FIX_PIPTAGSxPIP_LOC)) + 0
                    + 4 * (0),
                numberRHS);
    }
  {
    reentryGuard = 0;
    return 0;
  }
}
