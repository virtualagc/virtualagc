/*
  File PAD.c generated by XCOM-I, 2024-08-08 04:33:34.
*/

#include "runtimeC.h"

descriptor_t *
PAD (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PAD");
  // L = LENGTH(STRING); (717)
  {
    int32_t numberRHS = (int32_t)(LENGTH (getCHARACTER (mPADxSTRING)));
    putFIXED (mPADxL, numberRHS);
  }
  // IF L >= WIDTH THEN (718)
  if (1 & (xGE (getFIXED (mPADxL), getFIXED (mPADxWIDTH))))
    // RETURN STRING; (719)
    {
      reentryGuard = 0;
      return getCHARACTER (mPADxSTRING);
    }
  // ELSE (720)
  else
    // RETURN STRING || SUBSTR(X256, 0, WIDTH - L); (721)
    {
      reentryGuard = 0;
      return xsCAT (
          getCHARACTER (mPADxSTRING),
          SUBSTR (getCHARACTER (mX256), 0,
                  xsubtract (getFIXED (mPADxWIDTH), getFIXED (mPADxL))));
    }
}
