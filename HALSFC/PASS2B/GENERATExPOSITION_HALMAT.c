/*
  File GENERATExPOSITION_HALMAT.c generated by XCOM-I, 2024-08-09 12:41:32.
*/

#include "runtimeC.h"

int32_t
GENERATExPOSITION_HALMAT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExPOSITION_HALMAT");
  // IF BLK ~= CURCBLK THEN (2724)
  if (1
      & (xNEQ (COREHALFWORD (mGENERATExPOSITION_HALMATxBLK),
               BYTE0 (mCURCBLK))))
    // DO; (2725)
    {
    rs1:;
      // CURCBLK = BLK - 1; (2726)
      {
        int32_t numberRHS = (int32_t)(xsubtract (
            COREHALFWORD (mGENERATExPOSITION_HALMATxBLK), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (8, mCURCBLK, bitRHS);
        bitRHS->inUse = 0;
      }
      // CALL NEW_HALMAT_BLOCK; (2727)
      NEW_HALMAT_BLOCK (0);
    es1:;
    } // End of DO block
  // CTR = POS; (2728)
  {
    descriptor_t *bitRHS = getBIT (16, mGENERATExPOSITION_HALMATxPOS);
    putBIT (16, mCTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // NUMOP = POPNUM(CTR); (2729)
  {
    int32_t numberRHS = (int32_t)((
        putBITp (16, mPOPNUMxCTR, getBIT (16, mCTR)), POPNUM (0)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mNUMOP, bitRHS);
    bitRHS->inUse = 0;
  }
  // AUX_CTR = AUX; (2730)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mGENERATExPOSITION_HALMATxAUX));
    putFIXED (mAUX_CTR, numberRHS);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
