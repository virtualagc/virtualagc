/*
  File OBJECT_GENERATORxEMIT_ZCON_MEMBER.c generated by XCOM-I, 2024-08-08
  04:34:25.
*/

#include "runtimeC.h"

int32_t
OBJECT_GENERATORxEMIT_ZCON_MEMBER (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard
      = guardReentry (reentryGuard, "OBJECT_GENERATORxEMIT_ZCON_MEMBER");
  // CARD_COUNTER = 0; (15936)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_COUNTER, bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL EMIT_START_CARD; (15937)
  OBJECT_GENERATORxEMIT_START_CARD (0);
  // CALL MOVE_CHARS(OBJECT_MODULE_NAME, ADDR(CARD_IMAGE(1)), 8); (15938)
  {
    putFIXED (mOBJECT_GENERATORxMOVE_CHARSxFROM_ADDR,
              getFIXED (mOBJECT_MODULE_NAME));
    putFIXED (mOBJECT_GENERATORxMOVE_CHARSxTO_ADDR,
              ADDR (NULL, 0, "OBJECT_GENERATORxCARD_IMAGE", 1));
    putBITp (16, mOBJECT_GENERATORxMOVE_CHARSxBYTE_COUNT,
             fixedToBit (32, (int32_t)(8)));
    OBJECT_GENERATORxMOVE_CHARS (0);
  }
  // TEMP_SYT = PROC_LEVEL(PROGPOINT); (15939)
  {
    descriptor_t *bitRHS
        = getBIT (16, mPROC_LEVEL + 2 * COREHALFWORD (mPROGPOINT));
    putBIT (16, mOBJECT_GENERATORxEMIT_ZCON_MEMBERxTEMP_SYT, bitRHS);
    bitRHS->inUse = 0;
  }
  // CARD_IMAGE(5) = SYT_LOCK#(TEMP_SYT); (15940)
  {
    descriptor_t *bitRHS = getBIT (
        8,
        getFIXED (mSYM_TAB)
            + 34 * (COREHALFWORD (mOBJECT_GENERATORxEMIT_ZCON_MEMBERxTEMP_SYT))
            + 31 + 1 * (0));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (5), bitRHS);
    bitRHS->inUse = 0;
  }
  // HAL_NAME_CHARS = SYT_NAME(TEMP_SYT); (15941)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (
        getFIXED (mSYM_TAB)
        + 34 * (COREHALFWORD (mOBJECT_GENERATORxEMIT_ZCON_MEMBERxTEMP_SYT)) + 0
        + 4 * (0));
    putCHARACTER (mOBJECT_GENERATORxHAL_NAME_CHARS, stringRHS);
    stringRHS->inUse = 0;
  }
  // HAL_NAME_LENGTH = LENGTH(HAL_NAME_CHARS); (15942)
  {
    int32_t numberRHS
        = (int32_t)(LENGTH (getCHARACTER (mOBJECT_GENERATORxHAL_NAME_CHARS)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxEMIT_ZCON_MEMBERxHAL_NAME_LENGTH, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF (HAL_NAME_LENGTH & 1) ~= 0 THEN (15943)
  if (1
      & (xNEQ (xAND (COREHALFWORD (
                         mOBJECT_GENERATORxEMIT_ZCON_MEMBERxHAL_NAME_LENGTH),
                     1),
               0)))
    // DO; (15944)
    {
    rs1:;
      // HAL_NAME_CHARS = PAD(HAL_NAME_CHARS, HAL_NAME_LENGTH + 1); (15945)
      {
        descriptor_t *stringRHS;
        stringRHS
            = (putCHARACTERp (mPADxSTRING,
                              getCHARACTER (mOBJECT_GENERATORxHAL_NAME_CHARS)),
               putFIXED (
                   mPADxWIDTH,
                   xadd (
                       COREHALFWORD (
                           mOBJECT_GENERATORxEMIT_ZCON_MEMBERxHAL_NAME_LENGTH),
                       1)),
               PAD (0));
        putCHARACTER (mOBJECT_GENERATORxHAL_NAME_CHARS, stringRHS);
        stringRHS->inUse = 0;
      }
      // X_CHAR = 1; (15946)
      {
        int32_t numberRHS = (int32_t)(1);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mOBJECT_GENERATORxEMIT_ZCON_MEMBERxX_CHAR, bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // ELSE (15947)
  else
    // X_CHAR = 0; (15948)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mOBJECT_GENERATORxEMIT_ZCON_MEMBERxX_CHAR, bitRHS);
      bitRHS->inUse = 0;
    }
  // CARD_IMAGE(6) =  32768 | HAL_NAME_LENGTH; (15949)
  {
    int32_t numberRHS = (int32_t)(xOR (
        32768,
        COREHALFWORD (mOBJECT_GENERATORxEMIT_ZCON_MEMBERxHAL_NAME_LENGTH)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (6), bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL MOVE_CHARS(HAL_NAME_CHARS, ADDR(CARD_IMAGE(7)), HAL_NAME_LENGTH +
  // X_CHAR); (15950)
  {
    putFIXED (mOBJECT_GENERATORxMOVE_CHARSxFROM_ADDR,
              getFIXED (mOBJECT_GENERATORxHAL_NAME_CHARS));
    putFIXED (mOBJECT_GENERATORxMOVE_CHARSxTO_ADDR,
              ADDR (NULL, 0, "OBJECT_GENERATORxCARD_IMAGE", 7));
    putBITp (
        16, mOBJECT_GENERATORxMOVE_CHARSxBYTE_COUNT,
        fixedToBit (
            32,
            (int32_t)(xadd (
                COREHALFWORD (
                    mOBJECT_GENERATORxEMIT_ZCON_MEMBERxHAL_NAME_LENGTH),
                COREHALFWORD (mOBJECT_GENERATORxEMIT_ZCON_MEMBERxX_CHAR)))));
    OBJECT_GENERATORxMOVE_CHARS (0);
  }
  // RECORD_ID =  1024 | (SHR(HAL_NAME_LENGTH + X_CHAR, 1) + 7); (15951)
  {
    int32_t numberRHS = (int32_t)(xOR (
        1024,
        xadd (
            SHR (xadd (
                     COREHALFWORD (
                         mOBJECT_GENERATORxEMIT_ZCON_MEMBERxHAL_NAME_LENGTH),
                     COREHALFWORD (mOBJECT_GENERATORxEMIT_ZCON_MEMBERxX_CHAR)),
                 1),
            7)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE, bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL EMIT_CARD; (15952)
  OBJECT_GENERATORxEMIT_CARD (0);
  // CALL MOVE_CHARS(POUND_Z || SUBSTR(OBJECT_MODULE_NAME, 2),
  // ADDR(CARD_IMAGE(1)), 8); (15953)
  {
    putFIXED (mOBJECT_GENERATORxMOVE_CHARSxFROM_ADDR,
              makeDescriptor (
                  xsCAT (getCHARACTER (mPOUND_Z),
                         SUBSTR2 (getCHARACTER (mOBJECT_MODULE_NAME), 2))));
    putFIXED (mOBJECT_GENERATORxMOVE_CHARSxTO_ADDR,
              ADDR (NULL, 0, "OBJECT_GENERATORxCARD_IMAGE", 1));
    putBITp (16, mOBJECT_GENERATORxMOVE_CHARSxBYTE_COUNT,
             fixedToBit (32, (int32_t)(8)));
    OBJECT_GENERATORxMOVE_CHARS (0);
  }
  // CARD_IMAGE(5) = 2; (15954)
  {
    int32_t numberRHS = (int32_t)(2);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (5), bitRHS);
    bitRHS->inUse = 0;
  }
  // CARD_IMAGE(6) = 1; (15955)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (6), bitRHS);
    bitRHS->inUse = 0;
  }
  // CARD_IMAGE(7) =  16384 | HAL_NAME_LENGTH; (15956)
  {
    int32_t numberRHS = (int32_t)(xOR (
        16384,
        COREHALFWORD (mOBJECT_GENERATORxEMIT_ZCON_MEMBERxHAL_NAME_LENGTH)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (7), bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL MOVE_CHARS(HAL_NAME_CHARS, ADDR(CARD_IMAGE(8)), HAL_NAME_LENGTH +
  // X_CHAR); (15957)
  {
    putFIXED (mOBJECT_GENERATORxMOVE_CHARSxFROM_ADDR,
              getFIXED (mOBJECT_GENERATORxHAL_NAME_CHARS));
    putFIXED (mOBJECT_GENERATORxMOVE_CHARSxTO_ADDR,
              ADDR (NULL, 0, "OBJECT_GENERATORxCARD_IMAGE", 8));
    putBITp (
        16, mOBJECT_GENERATORxMOVE_CHARSxBYTE_COUNT,
        fixedToBit (
            32,
            (int32_t)(xadd (
                COREHALFWORD (
                    mOBJECT_GENERATORxEMIT_ZCON_MEMBERxHAL_NAME_LENGTH),
                COREHALFWORD (mOBJECT_GENERATORxEMIT_ZCON_MEMBERxX_CHAR)))));
    OBJECT_GENERATORxMOVE_CHARS (0);
  }
  // RECORD_ID =  512 | (SHR(HAL_NAME_LENGTH + X_CHAR, 1) + 8); (15958)
  {
    int32_t numberRHS = (int32_t)(xOR (
        512,
        xadd (
            SHR (xadd (
                     COREHALFWORD (
                         mOBJECT_GENERATORxEMIT_ZCON_MEMBERxHAL_NAME_LENGTH),
                     COREHALFWORD (mOBJECT_GENERATORxEMIT_ZCON_MEMBERxX_CHAR)),
                 1),
            8)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE, bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL EMIT_CARD; (15959)
  OBJECT_GENERATORxEMIT_CARD (0);
  // RECORD_ID =  1541; (15960)
  {
    int32_t numberRHS = (int32_t)(1541);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE, bitRHS);
    bitRHS->inUse = 0;
  }
  // CARD_IMAGE(1) = 0; (15961)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (1), bitRHS);
    bitRHS->inUse = 0;
  }
  // CARD_IMAGE(2) = 2; (15962)
  {
    int32_t numberRHS = (int32_t)(2);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (2), bitRHS);
    bitRHS->inUse = 0;
  }
  // CARD_IMAGE(3) = 0; (15963)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (3), bitRHS);
    bitRHS->inUse = 0;
  }
  // CARD_IMAGE(4) =  3584; (15964)
  {
    int32_t numberRHS = (int32_t)(3584);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (4), bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL EMIT_CARD; (15965)
  OBJECT_GENERATORxEMIT_CARD (0);
  // RECORD_ID =  2053; (15966)
  {
    int32_t numberRHS = (int32_t)(2053);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE, bitRHS);
    bitRHS->inUse = 0;
  }
  // CARD_IMAGE(1) = 1; (15967)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (1), bitRHS);
    bitRHS->inUse = 0;
  }
  // CARD_IMAGE(2) = 2; (15968)
  {
    int32_t numberRHS = (int32_t)(2);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (2), bitRHS);
    bitRHS->inUse = 0;
  }
  // CARD_IMAGE(3) =  44; (15969)
  {
    int32_t numberRHS = (int32_t)(44);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (3), bitRHS);
    bitRHS->inUse = 0;
  }
  // CARD_IMAGE(4) = 0; (15970)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxCARD_IMAGE + 2 * (4), bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL EMIT_CARD; (15971)
  OBJECT_GENERATORxEMIT_CARD (0);
  // CALL EMIT_END_CARD; (15972)
  OBJECT_GENERATORxEMIT_END_CARD (0);
  // X_CHAR = MONITOR(1, 3, POUND_Z || SUBSTR(OBJECT_MODULE_NAME, 2)); (15973)
  {
    int32_t numberRHS = (int32_t)(MONITOR1 (
        3, xsCAT (getCHARACTER (mPOUND_Z),
                  SUBSTR2 (getCHARACTER (mOBJECT_MODULE_NAME), 2))));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_GENERATORxEMIT_ZCON_MEMBERxX_CHAR, bitRHS);
    bitRHS->inUse = 0;
  }
  // OBJECT_MODULE_STATUS = OBJECT_MODULE_STATUS | SHL(X_CHAR, 1); (15974)
  {
    int32_t numberRHS = (int32_t)(xOR (
        BYTE0 (mOBJECT_MODULE_STATUS),
        SHL (COREHALFWORD (mOBJECT_GENERATORxEMIT_ZCON_MEMBERxX_CHAR), 1)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mOBJECT_MODULE_STATUS, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
