/*
  File GENERATExLAST_SUBSCRIPT.c generated by XCOM-I, 2024-08-09 12:41:32.
*/

#include "runtimeC.h"

descriptor_t *
GENERATExLAST_SUBSCRIPT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExLAST_SUBSCRIPT");
  // IF SUBOP >= NUMOP THEN (8103)
  if (1 & (xGE (COREHALFWORD (mGENERATExSUBOP), COREHALFWORD (mNUMOP))))
    // RETURN TRUE; (8104)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(1));
    }
  // IF DATATYPE(TYPE(ALCOP)) = BITS THEN (8105)
  if (1
      & (xEQ (BYTE0 (mDATATYPE
                     + 1
                           * COREHALFWORD (getFIXED (mIND_STACK)
                                           + 73 * (COREHALFWORD (mRESULT)) + 50
                                           + 2 * (0))),
              BYTE0 (mBITS))))
    // IF TYPE_BITS(SUBOP+1) < 4 THEN (8106)
    if (1
        & (xLT (bitToFixed ((
                    putBITp (16, mGENERATExTYPE_BITSxOP,
                             fixedToBit (
                                 32, (int32_t)(xadd (
                                         COREHALFWORD (mGENERATExSUBOP), 1)))),
                    GENERATExTYPE_BITS (0))),
                4)))
      // RETURN TYPE_BITS(NUMOP) < 4; (8107)
      {
        reentryGuard = 0;
        return fixedToBit (
            32,
            (int32_t)(xLT (bitToFixed ((putBITp (16, mGENERATExTYPE_BITSxOP,
                                                 getBIT (16, mNUMOP)),
                                        GENERATExTYPE_BITS (0))),
                           4)));
      }
  // RETURN FALSE; (8108)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(0));
  }
}
