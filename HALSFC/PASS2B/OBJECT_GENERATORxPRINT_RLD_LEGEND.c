/*
  File OBJECT_GENERATORxPRINT_RLD_LEGEND.c generated by XCOM-I, 2024-08-08
  04:34:25.
*/

#include "runtimeC.h"

int32_t
OBJECT_GENERATORxPRINT_RLD_LEGEND (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard
      = guardReentry (reentryGuard, "OBJECT_GENERATORxPRINT_RLD_LEGEND");
  // ABSLIST_COUNT = LINE_LIM; (15795)
  {
    descriptor_t *bitRHS = getBIT (16, mOBJECT_GENERATORxLINE_LIM);
    putBIT (16, mOBJECT_GENERATORxABSLIST_COUNT, bitRHS);
    bitRHS->inUse = 0;
  }
  // OUTPUT(1) = AHEAD(3); (15796)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mOBJECT_GENERATORxAHEAD + 4 * 3);
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = '1'; (15797)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "1");
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = AHEAD(2); (15798)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mOBJECT_GENERATORxAHEAD + 4 * 2);
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = ' RLD INFORMATION'; (15799)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, " RLD INFORMATION");
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = ''; (15800)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "");
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S1 = ' POS.ID (P) IS THE ESDID OF SD FOR THE CONTROL '; (15801)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (
        NULL, " POS.ID (P) IS THE ESDID OF SD FOR THE CONTROL ");
    putCHARACTER (mOBJECT_GENERATORxS1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S1 = S1||'SECTION THAT CONTAINS THE ADDRESS CONSTANT'; (15802)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (
        getCHARACTER (mOBJECT_GENERATORxS1),
        cToDescriptor (NULL, "SECTION THAT CONTAINS THE ADDRESS CONSTANT"));
    putCHARACTER (mOBJECT_GENERATORxS1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = S1; (15803)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mOBJECT_GENERATORxS1);
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S1 = ' REL.ID (R) IS THE ESDID OF ESD ENTRY FOR THE '; (15804)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (
        NULL, " REL.ID (R) IS THE ESDID OF ESD ENTRY FOR THE ");
    putCHARACTER (mOBJECT_GENERATORxS1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S1 = S1||'SYMBOL BEING REFERRED TO'; (15805)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (getCHARACTER (mOBJECT_GENERATORxS1),
                       cToDescriptor (NULL, "SYMBOL BEING REFERRED TO"));
    putCHARACTER (mOBJECT_GENERATORxS1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = S1; (15806)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mOBJECT_GENERATORxS1);
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = ' FLAGS IS OF THE FOLLOWING FORM:'; (15807)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, " FLAGS IS OF THE FOLLOWING FORM:");
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = ''; (15808)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "");
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S1 = 'V0101100 - CODE WITH ZCON SECTOR'; (15809)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "V0101100 - CODE WITH ZCON SECTOR");
    putCHARACTER (mOBJECT_GENERATORxS1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S2 = 'V IS THE SIGN OF THE HALFWORD ADDRESS CONSTANT AS FOLLOWS:'; (15810)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (
        NULL, "V IS THE SIGN OF THE HALFWORD ADDRESS CONSTANT AS FOLLOWS:");
    putCHARACTER (mOBJECT_GENERATORxS2, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = SUBSTR(X72,0,8)||S1||SUBSTR(X72,0,17)||S2; (15811)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (xsCAT (xsCAT (SUBSTR (getCHARACTER (mX72), 0, 8),
                                     getCHARACTER (mOBJECT_GENERATORxS1)),
                              SUBSTR (getCHARACTER (mX72), 0, 17)),
                       getCHARACTER (mOBJECT_GENERATORxS2));
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S1 = 'V0101000 - CODE WITH ZCON SECTOR AND ADDRESS'; (15812)
  {
    descriptor_t *stringRHS;
    stringRHS
        = cToDescriptor (NULL, "V0101000 - CODE WITH ZCON SECTOR AND ADDRESS");
    putCHARACTER (mOBJECT_GENERATORxS1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S2 = '0 = POSITIVE'; (15813)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "0 = POSITIVE");
    putCHARACTER (mOBJECT_GENERATORxS2, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = SUBSTR(X72,0,8)||S1||SUBSTR(X72,0,10)||S2; (15814)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (xsCAT (xsCAT (SUBSTR (getCHARACTER (mX72), 0, 8),
                                     getCHARACTER (mOBJECT_GENERATORxS1)),
                              SUBSTR (getCHARACTER (mX72), 0, 10)),
                       getCHARACTER (mOBJECT_GENERATORxS2));
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S1 = 'V0001100 - DATA WITH ZCON SECTOR'; (15815)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "V0001100 - DATA WITH ZCON SECTOR");
    putCHARACTER (mOBJECT_GENERATORxS1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S2 = '1 = THE HALFWORD IS THE ABSOLUTE VALUE OF A NEGATIVE NUMBER';
  // (15816)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (
        NULL, "1 = THE HALFWORD IS THE ABSOLUTE VALUE OF A NEGATIVE NUMBER");
    putCHARACTER (mOBJECT_GENERATORxS2, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = SUBSTR(X72,0,8)||S1||SUBSTR(X72,0,22)||S2; (15817)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (xsCAT (xsCAT (SUBSTR (getCHARACTER (mX72), 0, 8),
                                     getCHARACTER (mOBJECT_GENERATORxS1)),
                              SUBSTR (getCHARACTER (mX72), 0, 22)),
                       getCHARACTER (mOBJECT_GENERATORxS2));
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S1 = 'V0001000 - DATA WITH ZCON SECTOR AND ADDRESS'; (15818)
  {
    descriptor_t *stringRHS;
    stringRHS
        = cToDescriptor (NULL, "V0001000 - DATA WITH ZCON SECTOR AND ADDRESS");
    putCHARACTER (mOBJECT_GENERATORxS1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S2 = 'XX CONTAINS ESD_CSECT_TYPE(R) AS FOLLOWS:'; (15819)
  {
    descriptor_t *stringRHS;
    stringRHS
        = cToDescriptor (NULL, "XX CONTAINS ESD_CSECT_TYPE(R) AS FOLLOWS:");
    putCHARACTER (mOBJECT_GENERATORxS2, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = SUBSTR(X72,0,8)||S1||SUBSTR(X72,0,5)||S2; (15820)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (xsCAT (xsCAT (SUBSTR (getCHARACTER (mX72), 0, 8),
                                     getCHARACTER (mOBJECT_GENERATORxS1)),
                              SUBSTR (getCHARACTER (mX72), 0, 5)),
                       getCHARACTER (mOBJECT_GENERATORxS2));
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S1 = 'V0XX0000 - 2 BYTE STANDARD ADDRESS TYPE'; (15821)
  {
    descriptor_t *stringRHS;
    stringRHS
        = cToDescriptor (NULL, "V0XX0000 - 2 BYTE STANDARD ADDRESS TYPE");
    putCHARACTER (mOBJECT_GENERATORxS1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S2 = '01 = ZCON_CSECT_TYPE'; (15822)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "01 = ZCON_CSECT_TYPE");
    putCHARACTER (mOBJECT_GENERATORxS2, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = SUBSTR(X72,0,8)||S1||SUBSTR(X72,0,15)||S2; (15823)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (xsCAT (xsCAT (SUBSTR (getCHARACTER (mX72), 0, 8),
                                     getCHARACTER (mOBJECT_GENERATORxS1)),
                              SUBSTR (getCHARACTER (mX72), 0, 15)),
                       getCHARACTER (mOBJECT_GENERATORxS2));
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S1 = 'V0XX0100 - 4 BYTE STANDARD ADDRESS TYPE'; (15824)
  {
    descriptor_t *stringRHS;
    stringRHS
        = cToDescriptor (NULL, "V0XX0100 - 4 BYTE STANDARD ADDRESS TYPE");
    putCHARACTER (mOBJECT_GENERATORxS1, stringRHS);
    stringRHS->inUse = 0;
  }
  // S2 = '10 = LIBRARY_CSECT_TYPE'; (15825)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "10 = LIBRARY_CSECT_TYPE");
    putCHARACTER (mOBJECT_GENERATORxS2, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = SUBSTR(X72,0,8)||S1||SUBSTR(X72,0,15)||S2; (15826)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (xsCAT (xsCAT (SUBSTR (getCHARACTER (mX72), 0, 8),
                                     getCHARACTER (mOBJECT_GENERATORxS1)),
                              SUBSTR (getCHARACTER (mX72), 0, 15)),
                       getCHARACTER (mOBJECT_GENERATORxS2));
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = ''; (15827)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "");
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = X2||SUBSTR(AHEAD(2),2); (15828)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (getCHARACTER (mX2),
                 SUBSTR2 (getCHARACTER (mOBJECT_GENERATORxAHEAD + 4 * 2), 2));
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT(1) = ''; (15829)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "");
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
