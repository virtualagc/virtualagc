/*
  File INITIALISExSETUP_DATABASE.c generated by XCOM-I, 2024-08-08 04:34:25.
*/

#include "runtimeC.h"

int32_t
INITIALISExSETUP_DATABASE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "INITIALISExSETUP_DATABASE");
  // DO OP2 = PROGPOINT TO PROCLIMIT; (2028)
  {
    int32_t from32, to32, by32;
    from32 = bitToFixed (getBIT (16, mPROGPOINT));
    to32 = bitToFixed (getBIT (16, mPROCLIMIT));
    by32 = 1;
    for (putBIT (16, mOP2, fixedToBit (16, from32));
         bitToFixed (getBIT (16, mOP2)) <= to32; putBIT (
             16, mOP2, fixedToBit (16, bitToFixed (getBIT (16, mOP2)) + by32)))
      {
        // DATABASE(OP2) = DATABASE; (2029)
        {
          descriptor_t *bitRHS = getBIT (16, mDATABASE);
          putBIT (16, mDATABASE + 2 * (COREHALFWORD (mOP2)), bitRHS);
          bitRHS->inUse = 0;
        }
        // REMOTE_LEVEL(OP2) = REMOTE_LEVEL; (2030)
        {
          descriptor_t *bitRHS = getBIT (8, mREMOTE_LEVEL);
          putBIT (8, mREMOTE_LEVEL + 1 * (COREHALFWORD (mOP2)), bitRHS);
          bitRHS->inUse = 0;
        }
      }
  } // End of DO for-loop block
  {
    reentryGuard = 0;
    return 0;
  }
}
