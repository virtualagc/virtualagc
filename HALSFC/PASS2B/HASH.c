/*
  File HASH.c generated by XCOM-I, 2024-08-08 04:34:25.
*/

#include "runtimeC.h"

int32_t
HASH (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "HASH");
  // HASHIT = 0; (895)
  {
    int32_t numberRHS = (int32_t)(0);
    putFIXED (mHASHxHASHIT, numberRHS);
  }
  // DO I = 0 TO LENGTH(NAME)-1; (896)
  {
    int32_t from18, to18, by18;
    from18 = 0;
    to18 = xsubtract (LENGTH (getCHARACTER (mHASHxNAME)), 1);
    by18 = 1;
    for (putFIXED (mHASHxI, from18); getFIXED (mHASHxI) <= to18;
         putFIXED (mHASHxI, getFIXED (mHASHxI) + by18))
      {
        // HASHIT = BYTE(NAME, I) + SHL(HASHIT, 1); (897)
        {
          int32_t numberRHS = (int32_t)(xadd (
              BYTE (getCHARACTER (mHASHxNAME), getFIXED (mHASHxI)),
              SHL (getFIXED (mHASHxHASHIT), 1)));
          putFIXED (mHASHxHASHIT, numberRHS);
        }
      }
  } // End of DO for-loop block
  // RETURN HASHIT MOD HASHSIZE; (898)
  {
    reentryGuard = 0;
    return xmod (getFIXED (mHASHxHASHIT), 49);
  }
}
