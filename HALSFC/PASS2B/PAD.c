/*
  File PAD.c generated by XCOM-I, 2024-08-08 04:34:25.
*/

#include "runtimeC.h"

descriptor_t *
PAD (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PAD");
  // RETURN STRING || SUBSTR(X72, 72+LENGTH(STRING)-WIDTH); (894)
  {
    reentryGuard = 0;
    return xsCAT (
        getCHARACTER (mPADxSTRING),
        SUBSTR2 (getCHARACTER (mX72),
                 xsubtract (xadd (72, LENGTH (getCHARACTER (mPADxSTRING))),
                            getFIXED (mPADxWIDTH))));
  }
}
