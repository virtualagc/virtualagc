/*
  File SET_LINKREG.c generated by XCOM-I, 2024-08-09 12:41:32.
*/

#include "runtimeC.h"

int32_t
SET_LINKREG (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SET_LINKREG");
  // IF NOT_LEAF(NARGINDEX) THEN (1034)
  if (1 & (bitToFixed (getBIT (1, mNOT_LEAF + 1 * COREHALFWORD (mNARGINDEX)))))
    // USAGE(LINKREG) = 0; (1035)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mUSAGE + 2 * (BYTE0 (mLINKREG)), bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (1036)
  else
    // DO; (1037)
    {
    rs1:;
      // USAGE(LINKREG) = 1; (1038)
      {
        int32_t numberRHS = (int32_t)(1);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mUSAGE + 2 * (BYTE0 (mLINKREG)), bitRHS);
        bitRHS->inUse = 0;
      }
      // USAGE_LINE(LINKREG) = -1; (1039)
      {
        int32_t numberRHS = (int32_t)(-1);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mUSAGE_LINE + 2 * (BYTE0 (mLINKREG)), bitRHS);
        bitRHS->inUse = 0;
      }
      // R_CONTENTS(LINKREG) = OFFSET; (1040)
      {
        descriptor_t *bitRHS = getBIT (8, mOFFSET);
        putBIT (8, mR_CONTENTS + 1 * (BYTE0 (mLINKREG)), bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
