/*
  File OBJECT_GENERATORxEMIT_TEXT_CARDxCHECK_STORE_PROTECT.c generated by
  XCOM-I, 2024-08-08 04:34:25.
*/

#include "runtimeC.h"

int32_t
OBJECT_GENERATORxEMIT_TEXT_CARDxCHECK_STORE_PROTECT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (
      reentryGuard, "OBJECT_GENERATORxEMIT_TEXT_CARDxCHECK_STORE_PROTECT");
  // IF CURR_STORE_PROTECT THEN (15444)
  if (1 & (bitToFixed (getBIT (1, mCURR_STORE_PROTECT))))
    // RECORD_ID = PTX_ID; (15445)
    {
      int32_t numberRHS = (int32_t)(1536);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mOBJECT_GENERATORxCARD_IMAGE, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (15446)
  else
    // RECORD_ID = UTX_ID; (15447)
    {
      int32_t numberRHS = (int32_t)(1792);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mOBJECT_GENERATORxCARD_IMAGE, bitRHS);
      bitRHS->inUse = 0;
    }
  {
    reentryGuard = 0;
    return 0;
  }
}
