/*
  File GENERATExDROPTEMP.c generated by XCOM-I, 2024-08-09 12:41:32.
*/

#include "runtimeC.h"

int32_t
GENERATExDROPTEMP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExDROPTEMP");
  // IX2 = PROC_LINK(INDEXNEST); (3903)
  {
    descriptor_t *bitRHS
        = getBIT (16, mPROC_LINK + 2 * COREHALFWORD (mINDEXNEST));
    putBIT (16, mIX2, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO WHILE IX2 ~= ENTRY; (3904)
  while (
      1
      & (xNEQ (COREHALFWORD (mIX2), COREHALFWORD (mGENERATExDROPTEMPxENTRY))))
    {
      // IX1 = IX2; (3905)
      {
        descriptor_t *bitRHS = getBIT (16, mIX2);
        putBIT (16, mIX1, bitRHS);
        bitRHS->inUse = 0;
      }
      // IX2 = POINT(IX2); (3906)
      {
        descriptor_t *bitRHS = getBIT (16, mPOINT + 2 * COREHALFWORD (mIX2));
        putBIT (16, mIX2, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF IX2 = PROC_LINK(INDEXNEST) THEN (3907)
      if (1
          & (xEQ (COREHALFWORD (mIX2),
                  COREHALFWORD (mPROC_LINK + 2 * COREHALFWORD (mINDEXNEST)))))
        // RETURN; (3908)
        {
          reentryGuard = 0;
          return 0;
        }
    } // End of DO WHILE block
  // UPPER(IX2) = -1; (3909)
  {
    int32_t numberRHS = (int32_t)(-1);
    putFIXED (mUPPER + 4 * (COREHALFWORD (mIX2)), numberRHS);
  }
  // POINT(IX1) = POINT(IX2); (3910)
  {
    descriptor_t *bitRHS = getBIT (16, mPOINT + 2 * COREHALFWORD (mIX2));
    putBIT (16, mPOINT + 2 * (COREHALFWORD (mIX1)), bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
