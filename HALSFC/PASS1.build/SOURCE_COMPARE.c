/*
  File SOURCE_COMPARE.c generated by XCOM-I, 2024-08-09 12:38:15.
*/

#include "runtimeC.h"

int32_t
SOURCE_COMPARE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SOURCE_COMPARE");
  // IF NO_MORE_SOURCE THEN (2872)
  if (1 & (bitToFixed (getBIT (1, mNO_MORE_SOURCE))))
    // RETURN; (2873)
    {
      reentryGuard = 0;
      return 0;
    }
  // IF ~FIRST_CARD THEN (2874)
  if (1 & (xNOT (BYTE0 (mFIRST_CARD))))
    // IF ~INITIAL_INCLUDE_RECORD THEN (2875)
    if (1 & (xNOT (BYTE0 (mINITIAL_INCLUDE_RECORD))))
      // CURRENT_CARD=INPUT(INPUT_DEV); (2876)
      {
        descriptor_t *stringRHS;
        stringRHS = INPUT (COREHALFWORD (mINPUT_DEV));
        putCHARACTER (mCURRENT_CARD, stringRHS);
        stringRHS->inUse = 0;
      }
    // ELSE (2877)
    else
      // DO; (2878)
      {
      rs1:;
        // IF ~COMPARE_SOURCE THEN (2879)
        if (1 & (xNOT (BYTE0 (mCOMPARE_SOURCE))))
          // DO; (2880)
          {
          rs1s1:;
            // INITIAL_INCLUDE_RECORD=FALSE; (2881)
            {
              int32_t numberRHS = (int32_t)(0);
              descriptor_t *bitRHS;
              bitRHS = fixedToBit (32, (int32_t)(numberRHS));
              putBIT (1, mINITIAL_INCLUDE_RECORD, bitRHS);
              bitRHS->inUse = 0;
            }
            // RETURN; (2882)
            {
              reentryGuard = 0;
              return 0;
            }
          es1s1:;
          } // End of DO block
      es1:;
      } // End of DO block
  // IF ~COMPARE_SOURCE THEN (2883)
  if (1 & (xNOT (BYTE0 (mCOMPARE_SOURCE))))
    // RETURN; (2884)
    {
      reentryGuard = 0;
      return 0;
    }
  // IF LENGTH(CURRENT_CARD)~=0 THEN (2885)
  if (1 & (xNEQ (LENGTH (getCHARACTER (mCURRENT_CARD)), 0)))
    // DO; (2886)
    {
    rs2:;
      // CUR_CARD=SUBSTR(CURRENT_CARD,0,TEXT_LIMIT); (2887)
      {
        descriptor_t *stringRHS;
        stringRHS = SUBSTR (getCHARACTER (mCURRENT_CARD), 0,
                            COREHALFWORD (mTEXT_LIMIT));
        putCHARACTER (mCUR_CARD, stringRHS);
        stringRHS->inUse = 0;
      }
      // CURRENT_SRN=SUBSTR(CURRENT_CARD,TEXT_LIMIT+1,6); (2888)
      {
        descriptor_t *stringRHS;
        stringRHS = SUBSTR (getCHARACTER (mCURRENT_CARD),
                            xadd (COREHALFWORD (mTEXT_LIMIT), 1), 6);
        putCHARACTER (mCURRENT_SRN, stringRHS);
        stringRHS->inUse = 0;
      }
    es2:;
    } // End of DO block
  // ELSE (2889)
  else
    // NO_MORE_SOURCE=TRUE; (2890)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mNO_MORE_SOURCE, bitRHS);
      bitRHS->inUse = 0;
    }
  // MORE=TRUE; (2891)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mMORE, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO WHILE MORE; (2892)
  while (1 & (bitToFixed (getBIT (1, mMORE))))
    {
      // IF NO_MORE_PATCH THEN (2893)
      if (1 & (bitToFixed (getBIT (1, mNO_MORE_PATCH))))
        // RETURN; (2894)
        {
          reentryGuard = 0;
          return 0;
        }
      // IF ~DELETING THEN (2895)
      if (1 & (xNOT (BYTE0 (mDELETING))))
        // DO; (2896)
        {
        rs3s1:;
          // IF ~FIRST_CARD THEN (2897)
          if (1 & (xNOT (BYTE0 (mFIRST_CARD))))
            // IF ~INITIAL_INCLUDE_RECORD THEN (2898)
            if (1 & (xNOT (BYTE0 (mINITIAL_INCLUDE_RECORD))))
              // DO; (2899)
              {
              rs3s1s1:;
                // PATCH_CARD=INPUT(UPDATE_INPUT_DEV); (2900)
                {
                  descriptor_t *stringRHS;
                  stringRHS = INPUT (COREHALFWORD (mUPDATE_INPUT_DEV));
                  putCHARACTER (mPATCH_CARD, stringRHS);
                  stringRHS->inUse = 0;
                }
              es3s1s1:;
              } // End of DO block
            // ELSE (2901)
            else
              // DO; (2902)
              {
              rs3s1s2:;
                // INITIAL_INCLUDE_RECORD=FALSE; (2903)
                {
                  int32_t numberRHS = (int32_t)(0);
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (1, mINITIAL_INCLUDE_RECORD, bitRHS);
                  bitRHS->inUse = 0;
                }
                // PATCH_SRN_COUNT=0; (2904)
                {
                  int32_t numberRHS = (int32_t)(0);
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (16, mSOURCE_COMPARExPATCH_SRN_COUNT, bitRHS);
                  bitRHS->inUse = 0;
                }
              es3s1s2:;
              } // End of DO block
          // FIRST_CARD=FALSE; (2905)
          {
            int32_t numberRHS = (int32_t)(0);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (1, mFIRST_CARD, bitRHS);
            bitRHS->inUse = 0;
          }
          // IF LENGTH(PATCH_CARD) = 0 THEN (2906)
          if (1 & (xEQ (LENGTH (getCHARACTER (mPATCH_CARD)), 0)))
            // DO; (2907)
            {
            rs3s1s3:;
              // IF ~INCLUDING THEN (2908)
              if (1 & (xNOT (BYTE0 (mINCLUDING))))
                // COMPARE_SOURCE=FALSE; (2909)
                {
                  int32_t numberRHS = (int32_t)(0);
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (1, mCOMPARE_SOURCE, bitRHS);
                  bitRHS->inUse = 0;
                }
              // NO_MORE_PATCH=TRUE; (2910)
              {
                int32_t numberRHS = (int32_t)(1);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mNO_MORE_PATCH, bitRHS);
                bitRHS->inUse = 0;
              }
              // IF REPLCNT ~= 0 THEN (2911)
              if (1 & (xNEQ (COREHALFWORD (mSOURCE_COMPARExREPLCNT), 0)))
                // DO I=0 TO REPLCNT-1; (2912)
                {
                  int32_t from58, to58, by58;
                  from58 = 0;
                  to58 = xsubtract (COREHALFWORD (mSOURCE_COMPARExREPLCNT), 1);
                  by58 = 1;
                  for (putFIXED (mI, from58); getFIXED (mI) <= to58;
                       putFIXED (mI, getFIXED (mI) + by58))
                    {
                      // OUTPUT(9)=PATCHSAVE(I); (2913)
                      {
                        descriptor_t *stringRHS;
                        stringRHS = getCHARACTER (getFIXED (mSAVE_PATCH)
                                                  + 4 * (getFIXED (mI)) + 0
                                                  + 4 * (0));
                        OUTPUT (9, stringRHS);
                        stringRHS->inUse = 0;
                      }
                    }
                } // End of DO for-loop block
              // REPLCNT=0; (2914)
              {
                int32_t numberRHS = (int32_t)(0);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16, mSOURCE_COMPARExREPLCNT, bitRHS);
                bitRHS->inUse = 0;
              }
              // RETURN; (2915)
              {
                reentryGuard = 0;
                return 0;
              }
            es3s1s3:;
            } // End of DO block
          // ELSE (2916)
          else
            // DO; (2917)
            {
            rs3s1s4:;
              // PAT_CARD=SUBSTR(PATCH_CARD,0,TEXT_LIMIT); (2918)
              {
                descriptor_t *stringRHS;
                stringRHS = SUBSTR (getCHARACTER (mPATCH_CARD), 0,
                                    COREHALFWORD (mTEXT_LIMIT));
                putCHARACTER (mPAT_CARD, stringRHS);
                stringRHS->inUse = 0;
              }
              // PATCH_SRN=SUBSTR(PATCH_CARD,PATCH_TEXT_LIMIT+1,6); (2919)
              {
                descriptor_t *stringRHS;
                stringRHS
                    = SUBSTR (getCHARACTER (mPATCH_CARD),
                              xadd (COREHALFWORD (mPATCH_TEXT_LIMIT), 1), 6);
                putCHARACTER (mPATCH_SRN, stringRHS);
                stringRHS->inUse = 0;
              }
            es3s1s4:;
            } // End of DO block
        es3s1:;
        } // End of DO block
      // IF INCLUDING THEN (2920)
      if (1 & (bitToFixed (getBIT (1, mINCLUDING))))
        // DO; (2921)
        {
        rs3s2:;
          // IF (BYTE(SUBSTR(CUR_CARD,0,1))~=BYTE('D')) &
          // (BYTE(SUBSTR(CUR_CARD,0,1)) ~=BYTE('C')) THEN (2922)
          if (1
              & (xAND (xNEQ (BYTE1 (SUBSTR (getCHARACTER (mCUR_CARD), 0, 1)),
                             BYTE1 (cToDescriptor (NULL, "D"))),
                       xNEQ (BYTE1 (SUBSTR (getCHARACTER (mCUR_CARD), 0, 1)),
                             BYTE1 (cToDescriptor (NULL, "C"))))))
            // INCL_NUM=LEFT_PAD('('||PLUS||(SRN_COUNT+1)||')',6)|| X1; (2923)
            {
              descriptor_t *stringRHS;
              stringRHS = xsCAT (
                  (putCHARACTERp (
                       mLEFT_PADxSTRING,
                       xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "("),
                                            getCHARACTER (mPLUS)),
                                     fixedToCharacter (
                                         xadd (COREHALFWORD (mSRN_COUNT), 1))),
                              cToDescriptor (NULL, ")"))),
                   putFIXED (mLEFT_PADxWIDTH, 6), LEFT_PAD (0)),
                  getCHARACTER (mX1));
              putCHARACTER (mSOURCE_COMPARExINCL_NUM, stringRHS);
              stringRHS->inUse = 0;
            }
          // ELSE (2924)
          else
            // INCL_NUM=X8; (2925)
            {
              descriptor_t *stringRHS;
              stringRHS = getCHARACTER (mX8);
              putCHARACTER (mSOURCE_COMPARExINCL_NUM, stringRHS);
              stringRHS->inUse = 0;
            }
          // IF (BYTE(SUBSTR(PAT_CARD,0,1)) ~=BYTE('D')) &
          // (BYTE(SUBSTR(PAT_CARD,0,1)) ~= BYTE('C')) THEN (2926)
          if (1
              & (xAND (xNEQ (BYTE1 (SUBSTR (getCHARACTER (mPAT_CARD), 0, 1)),
                             BYTE1 (cToDescriptor (NULL, "D"))),
                       xNEQ (BYTE1 (SUBSTR (getCHARACTER (mPAT_CARD), 0, 1)),
                             BYTE1 (cToDescriptor (NULL, "C"))))))
            // DO; (2927)
            {
            rs3s2s1:;
              // IF ~DELETING THEN (2928)
              if (1 & (xNOT (BYTE0 (mDELETING))))
                // DO; (2929)
                {
                rs3s2s1s1:;
                  // PATCH_SRN_COUNT=PATCH_SRN_COUNT+1; (2930)
                  {
                    int32_t numberRHS = (int32_t)(xadd (
                        COREHALFWORD (mSOURCE_COMPARExPATCH_SRN_COUNT), 1));
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (16, mSOURCE_COMPARExPATCH_SRN_COUNT, bitRHS);
                    bitRHS->inUse = 0;
                  }
                  // PATCH_INCL_NUM=LEFT_PAD(PLUS||PATCH_SRN_COUNT,6)|| X1;
                  // (2931)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = xsCAT (
                        (putCHARACTERp (
                             mLEFT_PADxSTRING,
                             xsCAT (
                                 getCHARACTER (mPLUS),
                                 bitToCharacter (getBIT (
                                     16, mSOURCE_COMPARExPATCH_SRN_COUNT)))),
                         putFIXED (mLEFT_PADxWIDTH, 6), LEFT_PAD (0)),
                        getCHARACTER (mX1));
                    putCHARACTER (mSOURCE_COMPARExPATCH_INCL_NUM, stringRHS);
                    stringRHS->inUse = 0;
                  }
                es3s2s1s1:;
                } // End of DO block
            es3s2s1:;
            } // End of DO block
          // ELSE (2932)
          else
            // PATCH_INCL_NUM=X8; (2933)
            {
              descriptor_t *stringRHS;
              stringRHS = getCHARACTER (mX8);
              putCHARACTER (mSOURCE_COMPARExPATCH_INCL_NUM, stringRHS);
              stringRHS->inUse = 0;
            }
        es3s2:;
        } // End of DO block
      // ELSE (2934)
      else
        // DO; (2935)
        {
        rs3s3:;
          // INCL_NUM=''; (2936)
          {
            descriptor_t *stringRHS;
            stringRHS = cToDescriptor (NULL, "");
            putCHARACTER (mSOURCE_COMPARExINCL_NUM, stringRHS);
            stringRHS->inUse = 0;
          }
          // PATCH_INCL_NUM=''; (2937)
          {
            descriptor_t *stringRHS;
            stringRHS = cToDescriptor (NULL, "");
            putCHARACTER (mSOURCE_COMPARExPATCH_INCL_NUM, stringRHS);
            stringRHS->inUse = 0;
          }
        es3s3:;
        } // End of DO block
      // DO CASE SRNCMP(CURRENT_SRN,PATCH_SRN); (2938)
      {
      rs3s4:
        switch (bitToFixed (
            (putCHARACTERp (mSRNCMPxTEXT1, getCHARACTER (mCURRENT_SRN)),
             putCHARACTERp (mSRNCMPxTEXT2, getCHARACTER (mPATCH_SRN)),
             SRNCMP (0))))
          {
          case 0:
            // DO; (2940)
            {
            rs3s4s1:;
              // IF PAT_CARD~=CUR_CARD THEN (2940)
              if (1
                  & (xsNEQ (getCHARACTER (mPAT_CARD),
                            getCHARACTER (mCUR_CARD))))
                // DO; (2941)
                {
                rs3s4s1s1:;
                  // IF UPDATING THEN (2942)
                  if (1 & (bitToFixed (getBIT (1, mUPDATING))))
                    // DO; (2943)
                    {
                    rs3s4s1s1s1:;
                      // IF ~REPLACING THEN (2944)
                      if (1 & (xNOT (BYTE0 (mREPLACING))))
                        // DO; (2945)
                        {
                        rs3s4s1s1s1s1:;
                          // OUTPUT(9)=X70; (2946)
                          {
                            descriptor_t *stringRHS;
                            stringRHS = getCHARACTER (mX70);
                            OUTPUT (9, stringRHS);
                            stringRHS->inUse = 0;
                          }
                          // ADDING,DELETING=FALSE; (2947)
                          {
                            int32_t numberRHS = (int32_t)(0);
                            descriptor_t *bitRHS;
                            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                            putBIT (1, mADDING, bitRHS);
                            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                            putBIT (1, mDELETING, bitRHS);
                            bitRHS->inUse = 0;
                          }
                        es3s4s1s1s1s1:;
                        } // End of DO block
                    es3s4s1s1s1:;
                    } // End of DO block
                  // UPDATING=TRUE; (2948)
                  {
                    int32_t numberRHS = (int32_t)(1);
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (1, mUPDATING, bitRHS);
                    bitRHS->inUse = 0;
                  }
                  // REPLACING=TRUE; (2949)
                  {
                    int32_t numberRHS = (int32_t)(1);
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (1, mREPLACING, bitRHS);
                    bitRHS->inUse = 0;
                  }
                  // IF INCLUDING & PRINT_INCL_HEAD THEN (2950)
                  if (1
                      & (xAND (BYTE0 (mINCLUDING), BYTE0 (mPRINT_INCL_HEAD))))
                    // DO; (2951)
                    {
                    rs3s4s1s1s2:;
                      // OUTPUT(9)=X1||PATCH_INCL_HEAD; (2952)
                      {
                        descriptor_t *stringRHS;
                        stringRHS = xsCAT (getCHARACTER (mX1),
                                           getCHARACTER (mPATCH_INCL_HEAD));
                        OUTPUT (9, stringRHS);
                        stringRHS->inUse = 0;
                      }
                      // OUTPUT(9)='I'||STARS||' START '||INCL_LOG_MSG; (2953)
                      {
                        descriptor_t *stringRHS;
                        stringRHS
                            = xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "I"),
                                                   getCHARACTER (mSTARS)),
                                            cToDescriptor (NULL, " START ")),
                                     getCHARACTER (mINCL_LOG_MSG));
                        OUTPUT (9, stringRHS);
                        stringRHS->inUse = 0;
                      }
                      // OUTPUT(9)=X1; (2954)
                      {
                        descriptor_t *stringRHS;
                        stringRHS = getCHARACTER (mX1);
                        OUTPUT (9, stringRHS);
                        stringRHS->inUse = 0;
                      }
                      // PRINT_INCL_HEAD=FALSE; (2955)
                      {
                        int32_t numberRHS = (int32_t)(0);
                        descriptor_t *bitRHS;
                        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                        putBIT (1, mPRINT_INCL_HEAD, bitRHS);
                        bitRHS->inUse = 0;
                      }
                      // PRINT_INCL_TAIL=TRUE; (2956)
                      {
                        int32_t numberRHS = (int32_t)(1);
                        descriptor_t *bitRHS;
                        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                        putBIT (1, mPRINT_INCL_TAIL, bitRHS);
                        bitRHS->inUse = 0;
                      }
                    es3s4s1s1s2:;
                    } // End of DO block
                  // DELTA_LEN=LENGTH(CURRENT_CARD)+LENGTH(INCL_NUM); (2957)
                  {
                    int32_t numberRHS = (int32_t)(xadd (
                        LENGTH (getCHARACTER (mCURRENT_CARD)),
                        LENGTH (getCHARACTER (mSOURCE_COMPARExINCL_NUM))));
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (16, mSOURCE_COMPARExDELTA_LEN, bitRHS);
                    bitRHS->inUse = 0;
                  }
                  // PATCH_COUNT=PATCH_COUNT+1; (2958)
                  {
                    int32_t numberRHS
                        = (int32_t)(xadd (getFIXED (mPATCH_COUNT), 1));
                    putFIXED (mPATCH_COUNT, numberRHS);
                  }
                  // OUTPUT(9)=X1||CURRENT_SRN||X1||INCL_NUM||CUR_CARD||
                  // SUBSTR(X109,DELTA_LEN)||DELETED; (2959)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = xsCAT (
                        xsCAT (
                            xsCAT (
                                xsCAT (
                                    xsCAT (xsCAT (getCHARACTER (mX1),
                                                  getCHARACTER (mCURRENT_SRN)),
                                           getCHARACTER (mX1)),
                                    getCHARACTER (mSOURCE_COMPARExINCL_NUM)),
                                getCHARACTER (mCUR_CARD)),
                            SUBSTR2 (
                                getCHARACTER (mX109),
                                COREHALFWORD (mSOURCE_COMPARExDELTA_LEN))),
                        getCHARACTER (mDELETED));
                    OUTPUT (9, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // DELTA_LEN=LENGTH(PATCH_CARD)+LENGTH(PATCH_INCL_NUM);
                  // (2960)
                  {
                    int32_t numberRHS = (int32_t)(xadd (
                        LENGTH (getCHARACTER (mPATCH_CARD)),
                        LENGTH (
                            getCHARACTER (mSOURCE_COMPARExPATCH_INCL_NUM))));
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (16, mSOURCE_COMPARExDELTA_LEN, bitRHS);
                    bitRHS->inUse = 0;
                  }
                  // PATCHSAVE(REPLCNT)='R'||PATCH_SRN||X1||PATCH_INCL_NUM||
                  // PAT_CARD||SUBSTR(X109,DELTA_LEN)||ADDED; (2961)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = xsCAT (
                        xsCAT (
                            xsCAT (
                                xsCAT (
                                    xsCAT (xsCAT (cToDescriptor (NULL, "R"),
                                                  getCHARACTER (mPATCH_SRN)),
                                           getCHARACTER (mX1)),
                                    getCHARACTER (
                                        mSOURCE_COMPARExPATCH_INCL_NUM)),
                                getCHARACTER (mPAT_CARD)),
                            SUBSTR2 (
                                getCHARACTER (mX109),
                                COREHALFWORD (mSOURCE_COMPARExDELTA_LEN))),
                        getCHARACTER (mADDED));
                    putCHARACTER (
                        getFIXED (mSAVE_PATCH)
                            + 4 * (COREHALFWORD (mSOURCE_COMPARExREPLCNT)) + 0
                            + 4 * (0),
                        stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // REPLCNT=REPLCNT+1; (2962)
                  {
                    int32_t numberRHS = (int32_t)(xadd (
                        COREHALFWORD (mSOURCE_COMPARExREPLCNT), 1));
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (16, mSOURCE_COMPARExREPLCNT, bitRHS);
                    bitRHS->inUse = 0;
                  }
                  // DO WHILE RECORD_USED(SAVE_PATCH)<=REPLCNT; (2963)
                  while (1
                         & (xLE (COREWORD (xadd (
                                     ADDR ("SAVE_PATCH", 0x80000000, NULL, 0),
                                     12)),
                                 COREHALFWORD (mSOURCE_COMPARExREPLCNT))))
                    {
                      // DO ; (2964)
                      {
                      rs3s4s1s1s3s1:;
                        // IF COREWORD ( ADDR ( SAVE_PATCH ) + 12 ) >= COREWORD
                        // ( ADDR ( SAVE_PATCH ) + 8 ) THEN (2965)
                        if (1
                            & (xGE (COREWORD (xadd (ADDR ("SAVE_PATCH",
                                                          0x80000000, NULL, 0),
                                                    12)),
                                    COREWORD (xadd (ADDR ("SAVE_PATCH",
                                                          0x80000000, NULL, 0),
                                                    8)))))
                          // CALL _NEEDMORE_SPACE ( ADDR ( SAVE_PATCH )  ) ;
                          // (2966)
                          {
                            putFIXED (
                                m_NEEDMORE_SPACExDOPE,
                                ADDR ("SAVE_PATCH", 0x80000000, NULL, 0));
                            _NEEDMORE_SPACE (0);
                          }
                        // COREWORD ( ADDR ( SAVE_PATCH ) + 12 ) = COREWORD (
                        // ADDR ( SAVE_PATCH ) + 12 ) + 1 ; (2967)
                        {
                          int32_t numberRHS = (int32_t)(xadd (
                              COREWORD (xadd (
                                  ADDR ("SAVE_PATCH", 0x80000000, NULL, 0),
                                  12)),
                              1));
                          COREWORD2 (
                              xadd (ADDR ("SAVE_PATCH", 0x80000000, NULL, 0),
                                    12),
                              numberRHS);
                        }
                      es3s4s1s1s3s1:;
                      } // End of DO block
                    }   // End of DO WHILE block
                es3s4s1s1:;
                } // End of DO block
              // ELSE (2968)
              else
                // DO; (2969)
                {
                rs3s4s1s2:;
                  // IF UPDATING THEN (2970)
                  if (1 & (bitToFixed (getBIT (1, mUPDATING))))
                    // DO; (2971)
                    {
                    rs3s4s1s2s1:;
                      // IF REPLACING THEN (2972)
                      if (1 & (bitToFixed (getBIT (1, mREPLACING))))
                        // DO; (2973)
                        {
                        rs3s4s1s2s1s1:;
                          // DO I=0 TO REPLCNT-1; (2974)
                          {
                            int32_t from59, to59, by59;
                            from59 = 0;
                            to59 = xsubtract (
                                COREHALFWORD (mSOURCE_COMPARExREPLCNT), 1);
                            by59 = 1;
                            for (putFIXED (mI, from59); getFIXED (mI) <= to59;
                                 putFIXED (mI, getFIXED (mI) + by59))
                              {
                                // OUTPUT(9)=PATCHSAVE(I); (2975)
                                {
                                  descriptor_t *stringRHS;
                                  stringRHS = getCHARACTER (
                                      getFIXED (mSAVE_PATCH)
                                      + 4 * (getFIXED (mI)) + 0 + 4 * (0));
                                  OUTPUT (9, stringRHS);
                                  stringRHS->inUse = 0;
                                }
                              }
                          } // End of DO for-loop block
                          // REPLCNT=0; (2976)
                          {
                            int32_t numberRHS = (int32_t)(0);
                            descriptor_t *bitRHS;
                            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                            putBIT (16, mSOURCE_COMPARExREPLCNT, bitRHS);
                            bitRHS->inUse = 0;
                          }
                        es3s4s1s2s1s1:;
                        } // End of DO block
                      // UPDATING=FALSE; (2977)
                      {
                        int32_t numberRHS = (int32_t)(0);
                        descriptor_t *bitRHS;
                        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                        putBIT (1, mUPDATING, bitRHS);
                        bitRHS->inUse = 0;
                      }
                      // ADDING,DELETING,REPLACING=FALSE; (2978)
                      {
                        int32_t numberRHS = (int32_t)(0);
                        descriptor_t *bitRHS;
                        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                        putBIT (1, mADDING, bitRHS);
                        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                        putBIT (1, mDELETING, bitRHS);
                        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                        putBIT (1, mREPLACING, bitRHS);
                        bitRHS->inUse = 0;
                      }
                      // OUTPUT(9)=X70; (2979)
                      {
                        descriptor_t *stringRHS;
                        stringRHS = getCHARACTER (mX70);
                        OUTPUT (9, stringRHS);
                        stringRHS->inUse = 0;
                      }
                    es3s4s1s2s1:;
                    } // End of DO block
                es3s4s1s2:;
                } // End of DO block
              // IF NO_MORE_SOURCE THEN (2980)
              if (1 & (bitToFixed (getBIT (1, mNO_MORE_SOURCE))))
                // MORE=TRUE; (2981)
                {
                  int32_t numberRHS = (int32_t)(1);
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (1, mMORE, bitRHS);
                  bitRHS->inUse = 0;
                }
              // ELSE (2982)
              else
                // MORE=FALSE; (2983)
                {
                  int32_t numberRHS = (int32_t)(0);
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (1, mMORE, bitRHS);
                  bitRHS->inUse = 0;
                }
            es3s4s1:;
            } // End of DO block
            break;
          case 1:
            // DO; (2985)
            {
            rs3s4s2:;
              // IF UPDATING THEN (2985)
              if (1 & (bitToFixed (getBIT (1, mUPDATING))))
                // DO; (2986)
                {
                rs3s4s2s1:;
                  // IF ~DELETING THEN (2987)
                  if (1 & (xNOT (BYTE0 (mDELETING))))
                    // DO; (2988)
                    {
                    rs3s4s2s1s1:;
                      // IF REPLACING THEN (2989)
                      if (1 & (bitToFixed (getBIT (1, mREPLACING))))
                        // DO; (2990)
                        {
                        rs3s4s2s1s1s1:;
                          // DO I=0 TO REPLCNT-1; (2991)
                          {
                            int32_t from60, to60, by60;
                            from60 = 0;
                            to60 = xsubtract (
                                COREHALFWORD (mSOURCE_COMPARExREPLCNT), 1);
                            by60 = 1;
                            for (putFIXED (mI, from60); getFIXED (mI) <= to60;
                                 putFIXED (mI, getFIXED (mI) + by60))
                              {
                                // OUTPUT(9)=PATCHSAVE(I); (2992)
                                {
                                  descriptor_t *stringRHS;
                                  stringRHS = getCHARACTER (
                                      getFIXED (mSAVE_PATCH)
                                      + 4 * (getFIXED (mI)) + 0 + 4 * (0));
                                  OUTPUT (9, stringRHS);
                                  stringRHS->inUse = 0;
                                }
                              }
                          } // End of DO for-loop block
                          // REPLCNT=0; (2993)
                          {
                            int32_t numberRHS = (int32_t)(0);
                            descriptor_t *bitRHS;
                            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                            putBIT (16, mSOURCE_COMPARExREPLCNT, bitRHS);
                            bitRHS->inUse = 0;
                          }
                        es3s4s2s1s1s1:;
                        } // End of DO block
                      // OUTPUT(9)=X70; (2994)
                      {
                        descriptor_t *stringRHS;
                        stringRHS = getCHARACTER (mX70);
                        OUTPUT (9, stringRHS);
                        stringRHS->inUse = 0;
                      }
                    es3s4s2s1s1:;
                    } // End of DO block
                es3s4s2s1:;
                } // End of DO block
              // DELETING=TRUE; (2995)
              {
                int32_t numberRHS = (int32_t)(1);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mDELETING, bitRHS);
                bitRHS->inUse = 0;
              }
              // UPDATING=TRUE; (2996)
              {
                int32_t numberRHS = (int32_t)(1);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mUPDATING, bitRHS);
                bitRHS->inUse = 0;
              }
              // REPLACING,ADDING=FALSE; (2997)
              {
                int32_t numberRHS = (int32_t)(0);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mREPLACING, bitRHS);
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mADDING, bitRHS);
                bitRHS->inUse = 0;
              }
              // IF INCLUDING & PRINT_INCL_HEAD THEN (2998)
              if (1 & (xAND (BYTE0 (mINCLUDING), BYTE0 (mPRINT_INCL_HEAD))))
                // DO; (2999)
                {
                rs3s4s2s2:;
                  // OUTPUT(9)=X1||PATCH_INCL_HEAD; (3000)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = xsCAT (getCHARACTER (mX1),
                                       getCHARACTER (mPATCH_INCL_HEAD));
                    OUTPUT (9, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // OUTPUT(9)='I'||STARS||' START '||INCL_LOG_MSG; (3001)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "I"),
                                                     getCHARACTER (mSTARS)),
                                              cToDescriptor (NULL, " START ")),
                                       getCHARACTER (mINCL_LOG_MSG));
                    OUTPUT (9, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // OUTPUT(9)=X1; (3002)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = getCHARACTER (mX1);
                    OUTPUT (9, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // PRINT_INCL_HEAD=FALSE; (3003)
                  {
                    int32_t numberRHS = (int32_t)(0);
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (1, mPRINT_INCL_HEAD, bitRHS);
                    bitRHS->inUse = 0;
                  }
                  // PRINT_INCL_TAIL=TRUE; (3004)
                  {
                    int32_t numberRHS = (int32_t)(1);
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (1, mPRINT_INCL_TAIL, bitRHS);
                    bitRHS->inUse = 0;
                  }
                es3s4s2s2:;
                } // End of DO block
              // DELTA_LEN=LENGTH(CURRENT_CARD)+LENGTH(INCL_NUM); (3005)
              {
                int32_t numberRHS = (int32_t)(xadd (
                    LENGTH (getCHARACTER (mCURRENT_CARD)),
                    LENGTH (getCHARACTER (mSOURCE_COMPARExINCL_NUM))));
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16, mSOURCE_COMPARExDELTA_LEN, bitRHS);
                bitRHS->inUse = 0;
              }
              // PATCH_COUNT=PATCH_COUNT+1; (3006)
              {
                int32_t numberRHS
                    = (int32_t)(xadd (getFIXED (mPATCH_COUNT), 1));
                putFIXED (mPATCH_COUNT, numberRHS);
              }
              // OUTPUT(9)='D'||CURRENT_SRN||X1||INCL_NUM||CUR_CARD||
              // SUBSTR(X109,DELTA_LEN)||DELETED; (3007)
              {
                descriptor_t *stringRHS;
                stringRHS = xsCAT (
                    xsCAT (
                        xsCAT (
                            xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "D"),
                                                 getCHARACTER (mCURRENT_SRN)),
                                          getCHARACTER (mX1)),
                                   getCHARACTER (mSOURCE_COMPARExINCL_NUM)),
                            getCHARACTER (mCUR_CARD)),
                        SUBSTR2 (getCHARACTER (mX109),
                                 COREHALFWORD (mSOURCE_COMPARExDELTA_LEN))),
                    getCHARACTER (mDELETED));
                OUTPUT (9, stringRHS);
                stringRHS->inUse = 0;
              }
              // IF NO_MORE_SOURCE THEN (3008)
              if (1 & (bitToFixed (getBIT (1, mNO_MORE_SOURCE))))
                // MORE=TRUE; (3009)
                {
                  int32_t numberRHS = (int32_t)(1);
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (1, mMORE, bitRHS);
                  bitRHS->inUse = 0;
                }
              // ELSE (3010)
              else
                // MORE=FALSE; (3011)
                {
                  int32_t numberRHS = (int32_t)(0);
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (1, mMORE, bitRHS);
                  bitRHS->inUse = 0;
                }
            es3s4s2:;
            } // End of DO block
            break;
          case 2:
            // DO; (3013)
            {
            rs3s4s3:;
              // MORE=TRUE; (3013)
              {
                int32_t numberRHS = (int32_t)(1);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mMORE, bitRHS);
                bitRHS->inUse = 0;
              }
              // IF UPDATING THEN (3014)
              if (1 & (bitToFixed (getBIT (1, mUPDATING))))
                // DO; (3015)
                {
                rs3s4s3s1:;
                  // IF ~ADDING THEN (3016)
                  if (1 & (xNOT (BYTE0 (mADDING))))
                    // DO; (3017)
                    {
                    rs3s4s3s1s1:;
                      // IF REPLACING THEN (3018)
                      if (1 & (bitToFixed (getBIT (1, mREPLACING))))
                        // DO; (3019)
                        {
                        rs3s4s3s1s1s1:;
                          // DO I=0 TO REPLCNT-1; (3020)
                          {
                            int32_t from61, to61, by61;
                            from61 = 0;
                            to61 = xsubtract (
                                COREHALFWORD (mSOURCE_COMPARExREPLCNT), 1);
                            by61 = 1;
                            for (putFIXED (mI, from61); getFIXED (mI) <= to61;
                                 putFIXED (mI, getFIXED (mI) + by61))
                              {
                                // OUTPUT(9)=PATCHSAVE(I); (3021)
                                {
                                  descriptor_t *stringRHS;
                                  stringRHS = getCHARACTER (
                                      getFIXED (mSAVE_PATCH)
                                      + 4 * (getFIXED (mI)) + 0 + 4 * (0));
                                  OUTPUT (9, stringRHS);
                                  stringRHS->inUse = 0;
                                }
                              }
                          } // End of DO for-loop block
                          // REPLCNT=0; (3022)
                          {
                            int32_t numberRHS = (int32_t)(0);
                            descriptor_t *bitRHS;
                            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                            putBIT (16, mSOURCE_COMPARExREPLCNT, bitRHS);
                            bitRHS->inUse = 0;
                          }
                        es3s4s3s1s1s1:;
                        } // End of DO block
                      // ADDING=TRUE; (3023)
                      {
                        int32_t numberRHS = (int32_t)(1);
                        descriptor_t *bitRHS;
                        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                        putBIT (1, mADDING, bitRHS);
                        bitRHS->inUse = 0;
                      }
                      // REPLACING,DELETING=FALSE; (3024)
                      {
                        int32_t numberRHS = (int32_t)(0);
                        descriptor_t *bitRHS;
                        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                        putBIT (1, mREPLACING, bitRHS);
                        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                        putBIT (1, mDELETING, bitRHS);
                        bitRHS->inUse = 0;
                      }
                      // OUTPUT(9)=X70; (3025)
                      {
                        descriptor_t *stringRHS;
                        stringRHS = getCHARACTER (mX70);
                        OUTPUT (9, stringRHS);
                        stringRHS->inUse = 0;
                      }
                    es3s4s3s1s1:;
                    } // End of DO block
                es3s4s3s1:;
                } // End of DO block
              // IF INCLUDING & PRINT_INCL_HEAD THEN (3026)
              if (1 & (xAND (BYTE0 (mINCLUDING), BYTE0 (mPRINT_INCL_HEAD))))
                // DO; (3027)
                {
                rs3s4s3s2:;
                  // OUTPUT(9)=X1||PATCH_INCL_HEAD; (3028)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = xsCAT (getCHARACTER (mX1),
                                       getCHARACTER (mPATCH_INCL_HEAD));
                    OUTPUT (9, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // OUTPUT(9)='I'||STARS||' START '|| INCL_LOG_MSG; (3029)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "I"),
                                                     getCHARACTER (mSTARS)),
                                              cToDescriptor (NULL, " START ")),
                                       getCHARACTER (mINCL_LOG_MSG));
                    OUTPUT (9, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // OUTPUT(9)=X1; (3030)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = getCHARACTER (mX1);
                    OUTPUT (9, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // PRINT_INCL_HEAD=FALSE; (3031)
                  {
                    int32_t numberRHS = (int32_t)(0);
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (1, mPRINT_INCL_HEAD, bitRHS);
                    bitRHS->inUse = 0;
                  }
                  // PRINT_INCL_TAIL=TRUE; (3032)
                  {
                    int32_t numberRHS = (int32_t)(1);
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (1, mPRINT_INCL_TAIL, bitRHS);
                    bitRHS->inUse = 0;
                  }
                es3s4s3s2:;
                } // End of DO block
              // DELTA_LEN=LENGTH(PATCH_CARD)+LENGTH(PATCH_INCL_NUM); (3033)
              {
                int32_t numberRHS = (int32_t)(xadd (
                    LENGTH (getCHARACTER (mPATCH_CARD)),
                    LENGTH (getCHARACTER (mSOURCE_COMPARExPATCH_INCL_NUM))));
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16, mSOURCE_COMPARExDELTA_LEN, bitRHS);
                bitRHS->inUse = 0;
              }
              // PATCH_COUNT=PATCH_COUNT+1; (3034)
              {
                int32_t numberRHS
                    = (int32_t)(xadd (getFIXED (mPATCH_COUNT), 1));
                putFIXED (mPATCH_COUNT, numberRHS);
              }
              // OUTPUT(9)='A'||PATCH_SRN||X1||PATCH_INCL_NUM||PAT_CARD||
              // SUBSTR(X109,DELTA_LEN)||ADDED; (3035)
              {
                descriptor_t *stringRHS;
                stringRHS = xsCAT (
                    xsCAT (
                        xsCAT (xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "A"),
                                                    getCHARACTER (mPATCH_SRN)),
                                             getCHARACTER (mX1)),
                                      getCHARACTER (
                                          mSOURCE_COMPARExPATCH_INCL_NUM)),
                               getCHARACTER (mPAT_CARD)),
                        SUBSTR2 (getCHARACTER (mX109),
                                 COREHALFWORD (mSOURCE_COMPARExDELTA_LEN))),
                    getCHARACTER (mADDED));
                OUTPUT (9, stringRHS);
                stringRHS->inUse = 0;
              }
              // UPDATING,ADDING=TRUE; (3036)
              {
                int32_t numberRHS = (int32_t)(1);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mUPDATING, bitRHS);
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mADDING, bitRHS);
                bitRHS->inUse = 0;
              }
            es3s4s3:;
            } // End of DO block
            break;
          }
      } // End of DO CASE block
    }   // End of DO WHILE block
  // RETURN; (3037)
  {
    reentryGuard = 0;
    return 0;
  }
}
