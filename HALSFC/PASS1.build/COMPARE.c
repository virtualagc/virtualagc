/*
  File COMPARE.c generated by XCOM-I, 2024-08-09 12:38:15.
*/

#include "runtimeC.h"

int32_t
COMPARE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "COMPARE");
  // IF F_DIM = 0 THEN (9179)
  if (1 & (xEQ (getFIXED (mCOMPARExF_DIM), 0)))
    // RETURN DIM; (9180)
    {
      reentryGuard = 0;
      return getFIXED (mCOMPARExDIM);
    }
  // IF DIM = 0 THEN (9181)
  if (1 & (xEQ (getFIXED (mCOMPARExDIM), 0)))
    // RETURN F_DIM; (9182)
    {
      reentryGuard = 0;
      return getFIXED (mCOMPARExF_DIM);
    }
  // IF DIM = F_DIM THEN (9183)
  if (1 & (xEQ (getFIXED (mCOMPARExDIM), getFIXED (mCOMPARExF_DIM))))
    // RETURN DIM; (9184)
    {
      reentryGuard = 0;
      return getFIXED (mCOMPARExDIM);
    }
  // CALL ERROR(CLASS_DS, ERROR#, SYT_NAME(ID_LOC)); (9185)
  {
    putBITp (16, mERRORxCLASS, getBIT (16, mCLASS_DS));
    putBITp (8, mERRORxNUM,
             fixedToBit (32, (int32_t)(getFIXED (mCOMPARExERRORp))));
    putCHARACTERp (mERRORxTEXT,
                   getCHARACTER (getFIXED (mSYM_TAB)
                                 + 34 * (getFIXED (mID_LOC)) + 0 + 4 * (0)));
    ERROR (0);
  }
  // RETURN DIM; (9186)
  {
    reentryGuard = 0;
    return getFIXED (mCOMPARExDIM);
  }
}
