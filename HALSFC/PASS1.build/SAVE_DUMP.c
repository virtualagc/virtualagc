/*
  File SAVE_DUMP.c generated by XCOM-I, 2024-08-09 12:38:15.
*/

#include "runtimeC.h"

int32_t
SAVE_DUMP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SAVE_DUMP");
  // STACK_DUMP_PTR = STACK_DUMP_PTR + 1; (1158)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mSTACK_DUMP_PTR), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mSTACK_DUMP_PTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF STACK_DUMP_PTR >= STACK_DUMP_MAX THEN (1159)
  if (1 & (xGE (COREHALFWORD (mSTACK_DUMP_PTR), 10)))
    // DO; (1160)
    {
    rs1:;
      // SAVE_STACK_DUMP(STACK_DUMP_MAX) = '***** DUMP INCOMPLETE.'; (1161)
      {
        descriptor_t *stringRHS;
        stringRHS = cToDescriptor (NULL, "***** DUMP INCOMPLETE.");
        putCHARACTER (mSAVE_STACK_DUMP + 4 * (10), stringRHS);
        stringRHS->inUse = 0;
      }
      // STACK_DUMP_PTR = STACK_DUMP_MAX; (1162)
      {
        int32_t numberRHS = (int32_t)(10);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mSTACK_DUMP_PTR, bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // SAVE_STACK_DUMP(STACK_DUMP_PTR) = MSG; (1163)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mSAVE_DUMPxMSG);
    putCHARACTER (mSAVE_STACK_DUMP + 4 * (COREHALFWORD (mSTACK_DUMP_PTR)),
                  stringRHS);
    stringRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
