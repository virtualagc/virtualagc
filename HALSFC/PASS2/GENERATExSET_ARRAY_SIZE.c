/*
  File GENERATExSET_ARRAY_SIZE.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

descriptor_t *
GENERATExSET_ARRAY_SIZE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExSET_ARRAY_SIZE");
  // PTR = GET_STACK_ENTRY; (5171)
  {
    int32_t numberRHS = (int32_t)(GENERATExGET_STACK_ENTRY (0));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mGENERATExSET_ARRAY_SIZExPTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // FORM(PTR) = SYM; (5172)
  {
    descriptor_t *bitRHS = getBIT (8, mSYM);
    putBIT (16,
            getFIXED (mIND_STACK)
                + 73 * (COREHALFWORD (mGENERATExSET_ARRAY_SIZExPTR)) + 32
                + 2 * (0),
            bitRHS);
    bitRHS->inUse = 0;
  }
  // LOC(PTR) = OP; (5173)
  {
    descriptor_t *bitRHS = getBIT (16, mGENERATExSET_ARRAY_SIZExOP);
    putBIT (16,
            getFIXED (mIND_STACK)
                + 73 * (COREHALFWORD (mGENERATExSET_ARRAY_SIZExPTR)) + 40
                + 2 * (0),
            bitRHS);
    bitRHS->inUse = 0;
  }
  // TYPE(PTR) = DINTEGER; (5174)
  {
    descriptor_t *bitRHS = getBIT (8, mDINTEGER);
    putBIT (16,
            getFIXED (mIND_STACK)
                + 73 * (COREHALFWORD (mGENERATExSET_ARRAY_SIZExPTR)) + 50
                + 2 * (0),
            bitRHS);
    bitRHS->inUse = 0;
  }
  // IF CON ~= 0 THEN (5175)
  if (1 & (xNEQ (COREHALFWORD (mGENERATExSET_ARRAY_SIZExCON), 0)))
    // INX_CON(PTR) = CON; (5176)
    {
      descriptor_t *bitRHS = getBIT (16, mGENERATExSET_ARRAY_SIZExCON);
      putFIXED (getFIXED (mIND_STACK)
                    + 73 * (COREHALFWORD (mGENERATExSET_ARRAY_SIZExPTR)) + 4
                    + 4 * (0),
                bitToFixed (bitRHS));
      bitRHS->inUse = 0;
    }
  // ELSE (5177)
  else
    // INX_CON(PTR) = SHL(SYT_LEVEL(OP), 1); (5178)
    {
      int32_t numberRHS = (int32_t)(SHL (
          COREHALFWORD (getFIXED (mP2SYMS)
                        + 12 * (COREHALFWORD (mGENERATExSET_ARRAY_SIZExOP))
                        + 10 + 2 * (0)),
          1));
      putFIXED (getFIXED (mIND_STACK)
                    + 73 * (COREHALFWORD (mGENERATExSET_ARRAY_SIZExPTR)) + 4
                    + 4 * (0),
                numberRHS);
    }
  // CON = 0; (5179)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mGENERATExSET_ARRAY_SIZExCON, bitRHS);
    bitRHS->inUse = 0;
  }
  // RETURN PTR; (5180)
  {
    reentryGuard = 0;
    return getBIT (16, mGENERATExSET_ARRAY_SIZExPTR);
  }
}
