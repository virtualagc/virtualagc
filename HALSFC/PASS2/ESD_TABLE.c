/*
  File ESD_TABLE.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

descriptor_t *
ESD_TABLE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "ESD_TABLE");
  // I = SHR(PTR, 5); (923)
  {
    int32_t numberRHS = (int32_t)(SHR (COREHALFWORD (mESD_TABLExPTR), 5));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mESD_TABLExI, bitRHS);
    bitRHS->inUse = 0;
  }
  // J = SHL(PTR-SHL(I,5), 3); (924)
  {
    int32_t numberRHS
        = (int32_t)(SHL (xsubtract (COREHALFWORD (mESD_TABLExPTR),
                                    SHL (COREHALFWORD (mESD_TABLExI), 5)),
                         3));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mESD_TABLExJ, bitRHS);
    bitRHS->inUse = 0;
  }
  // K = ESD_NAME_LENGTH(PTR) &  127; (925)
  {
    int32_t numberRHS = (int32_t)(xAND (
        BYTE0 (mESD_NAME_LENGTH + 1 * COREHALFWORD (mESD_TABLExPTR)), 127));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mESD_TABLExK, bitRHS);
    bitRHS->inUse = 0;
  }
  // RETURN SUBSTR(ESD_NAME(I), J, K); (926)
  {
    reentryGuard = 0;
    return SUBSTR (getCHARACTER (mESD_NAME + 4 * COREHALFWORD (mESD_TABLExI)),
                   COREHALFWORD (mESD_TABLExJ), COREHALFWORD (mESD_TABLExK));
  }
}
