/*
  File GENERATExGET_OPERANDS.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
GENERATExGET_OPERANDS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExGET_OPERANDS");
  // IF NUMOP=2 THEN (6542)
  if (1 & (xEQ (COREHALFWORD (mNUMOP), 2)))
    // DO; (6543)
    {
    rs1:;
      // IF TAG_BITS(1)=LIT THEN (6544)
      if (1
          & (xEQ (bitToFixed ((putBITp (16, mGENERATExTAG_BITSxOP,
                                        fixedToBit (32, (int32_t)(1))),
                               GENERATExTAG_BITS (0))),
                  BYTE0 (mLIT))))
        // DO; (6545)
        {
        rs1s1:;
          // RIGHTOP=GET_OPERAND(2); (6546)
          {
            descriptor_t *bitRHS = (putBITp (16, mGENERATExGET_OPERANDxOP,
                                             fixedToBit (32, (int32_t)(2))),
                                    GENERATExGET_OPERAND (0));
            putBIT (16, mRIGHTOP, bitRHS);
            bitRHS->inUse = 0;
          }
          // LITTYPE=TYPE(RIGHTOP); (6547)
          {
            descriptor_t *bitRHS = getBIT (
                16, getFIXED (mIND_STACK) + 73 * (COREHALFWORD (mRIGHTOP)) + 50
                        + 2 * (0));
            putBIT (16, mLITTYPE, bitRHS);
            bitRHS->inUse = 0;
          }
          // LEFTOP=GET_OPERAND(1); (6548)
          {
            descriptor_t *bitRHS = (putBITp (16, mGENERATExGET_OPERANDxOP,
                                             fixedToBit (32, (int32_t)(1))),
                                    GENERATExGET_OPERAND (0));
            putBIT (16, mLEFTOP, bitRHS);
            bitRHS->inUse = 0;
          }
        es1s1:;
        } // End of DO block
      // ELSE (6549)
      else
        // DO; (6550)
        {
        rs1s2:;
          // LEFTOP=GET_OPERAND(1); (6551)
          {
            descriptor_t *bitRHS = (putBITp (16, mGENERATExGET_OPERANDxOP,
                                             fixedToBit (32, (int32_t)(1))),
                                    GENERATExGET_OPERAND (0));
            putBIT (16, mLEFTOP, bitRHS);
            bitRHS->inUse = 0;
          }
          // LITTYPE=TYPE(LEFTOP); (6552)
          {
            descriptor_t *bitRHS = getBIT (
                16, getFIXED (mIND_STACK) + 73 * (COREHALFWORD (mLEFTOP)) + 50
                        + 2 * (0));
            putBIT (16, mLITTYPE, bitRHS);
            bitRHS->inUse = 0;
          }
          // RIGHTOP=GET_OPERAND(2); (6553)
          {
            descriptor_t *bitRHS = (putBITp (16, mGENERATExGET_OPERANDxOP,
                                             fixedToBit (32, (int32_t)(2))),
                                    GENERATExGET_OPERAND (0));
            putBIT (16, mRIGHTOP, bitRHS);
            bitRHS->inUse = 0;
          }
        es1s2:;
        } // End of DO block
      // IF OPMODE(TYPE(LEFTOP)) >= OPMODE(TYPE(RIGHTOP)) THEN (6554)
      if (1
          & (xGE (BYTE0 (mOPMODE
                         + 1
                               * COREHALFWORD (getFIXED (mIND_STACK)
                                               + 73 * (COREHALFWORD (mLEFTOP))
                                               + 50 + 2 * (0))),
                  BYTE0 (mOPMODE
                         + 1
                               * COREHALFWORD (getFIXED (mIND_STACK)
                                               + 73 * (COREHALFWORD (mRIGHTOP))
                                               + 50 + 2 * (0))))))
        // OPTYPE = TYPE(LEFTOP); (6555)
        {
          descriptor_t *bitRHS = getBIT (
              16, getFIXED (mIND_STACK) + 73 * (COREHALFWORD (mLEFTOP)) + 50
                      + 2 * (0));
          putBIT (16, mOPTYPE, bitRHS);
          bitRHS->inUse = 0;
        }
      // ELSE (6556)
      else
        // OPTYPE = TYPE(RIGHTOP); (6557)
        {
          descriptor_t *bitRHS = getBIT (
              16, getFIXED (mIND_STACK) + 73 * (COREHALFWORD (mRIGHTOP)) + 50
                      + 2 * (0));
          putBIT (16, mOPTYPE, bitRHS);
          bitRHS->inUse = 0;
        }
      // IF FORM(LEFTOP)=LIT THEN (6558)
      if (1
          & (xEQ (COREHALFWORD (getFIXED (mIND_STACK)
                                + 73 * (COREHALFWORD (mLEFTOP)) + 32
                                + 2 * (0)),
                  BYTE0 (mLIT))))
        // TYPE(LEFTOP) = OPTYPE; (6559)
        {
          descriptor_t *bitRHS = getBIT (16, mOPTYPE);
          putBIT (16,
                  getFIXED (mIND_STACK) + 73 * (COREHALFWORD (mLEFTOP)) + 50
                      + 2 * (0),
                  bitRHS);
          bitRHS->inUse = 0;
        }
      // IF FORM(RIGHTOP)=LIT THEN (6560)
      if (1
          & (xEQ (COREHALFWORD (getFIXED (mIND_STACK)
                                + 73 * (COREHALFWORD (mRIGHTOP)) + 32
                                + 2 * (0)),
                  BYTE0 (mLIT))))
        // TYPE(RIGHTOP) = OPTYPE; (6561)
        {
          descriptor_t *bitRHS = getBIT (16, mOPTYPE);
          putBIT (16,
                  getFIXED (mIND_STACK) + 73 * (COREHALFWORD (mRIGHTOP)) + 50
                      + 2 * (0),
                  bitRHS);
          bitRHS->inUse = 0;
        }
    es1:;
    } // End of DO block
  // ELSE (6562)
  else
    // DO; (6563)
    {
    rs2:;
      // LEFTOP = GET_OPERAND(1); (6564)
      {
        descriptor_t *bitRHS = (putBITp (16, mGENERATExGET_OPERANDxOP,
                                         fixedToBit (32, (int32_t)(1))),
                                GENERATExGET_OPERAND (0));
        putBIT (16, mLEFTOP, bitRHS);
        bitRHS->inUse = 0;
      }
      // RIGHTOP = 0; (6565)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mRIGHTOP, bitRHS);
        bitRHS->inUse = 0;
      }
      // OPTYPE = TYPE(LEFTOP); (6566)
      {
        descriptor_t *bitRHS
            = getBIT (16, getFIXED (mIND_STACK) + 73 * (COREHALFWORD (mLEFTOP))
                              + 50 + 2 * (0));
        putBIT (16, mOPTYPE, bitRHS);
        bitRHS->inUse = 0;
      }
    es2:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
