/*
  File GENERATExRETURN_EXP_OR_FN.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

descriptor_t *
GENERATExRETURN_EXP_OR_FN (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExRETURN_EXP_OR_FN");
  // RETURN_FLAG=FALSE; (8727)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExRETURN_FLAG, bitRHS);
    bitRHS->inUse = 0;
  }
  // OPCODE = POPCODE(SKIP_NOP(PTR)); (8728)
  {
    int32_t numberRHS = (int32_t)((
        putBITp (16, mPOPCODExCTR,
                 (putBITp (16, mGENERATExSKIP_NOPxPTR,
                           getBIT (16, mGENERATExRETURN_EXP_OR_FNxPTR)),
                  GENERATExSKIP_NOP (0))),
        POPCODE (0)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mGENERATExRETURN_EXP_OR_FNxOPCODE, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF OPCODE = RTURN THEN (8729)
  if (1
      & (xEQ (COREHALFWORD (mGENERATExRETURN_EXP_OR_FNxOPCODE),
              COREHALFWORD (mGENERATExRETURN_EXP_OR_FNxRTURN))))
    // DO; (8730)
    {
    rs1:;
      // INDEX=PROC_LEVEL(INDEXNEST); (8731)
      {
        descriptor_t *bitRHS
            = getBIT (16, mPROC_LEVEL + 2 * COREHALFWORD (mINDEXNEST));
        putBIT (16, mINDEX, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF SYT_TYPE(INDEX)~=OPTYPE THEN (8732)
      if (1
          & (xNEQ (BYTE0 (getFIXED (mSYM_TAB) + 34 * (COREHALFWORD (mINDEX))
                          + 32 + 1 * (0)),
                   COREHALFWORD (mOPTYPE))))
        // RETURN FALSE; (8733)
        {
          reentryGuard = 0;
          return fixedToBit (32, (int32_t)(0));
        }
      // RESULT=SET_OPERAND(INDEX); (8734)
      {
        descriptor_t *bitRHS
            = (putBITp (16, mGENERATExSET_OPERANDxOP, getBIT (16, mINDEX)),
               GENERATExSET_OPERAND (0));
        putBIT (16, mRESULT, bitRHS);
        bitRHS->inUse = 0;
      }
      // RETURN_FLAG=TRUE; (8735)
      {
        int32_t numberRHS = (int32_t)(1);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mGENERATExRETURN_FLAG, bitRHS);
        bitRHS->inUse = 0;
      }
      // RETURN TRUE; (8736)
      {
        reentryGuard = 0;
        return fixedToBit (32, (int32_t)(1));
      }
    es1:;
    } // End of DO block
  // ELSE (8737)
  else
    // RETURN FALSE; (8738)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
}
