/*
  File GENERATExGEN_CLASS0xPUSH_ADOLEVEL.c generated by XCOM-I, 2024-08-09
  12:39:31.
*/

#include "runtimeC.h"

int32_t
GENERATExGEN_CLASS0xPUSH_ADOLEVEL (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard
      = guardReentry (reentryGuard, "GENERATExGEN_CLASS0xPUSH_ADOLEVEL");
  // CALL_LEVEL = CALL_LEVEL + 1; (12312)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mCALL_LEVEL), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mCALL_LEVEL, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF CALL_LEVEL > DONEST THEN (12313)
  if (1 & (xGT (COREHALFWORD (mCALL_LEVEL), 5)))
    // CALL ERRORS(CLASS_BS,115); (12314)
    {
      putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_BS));
      putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(115)));
      ERRORS (0);
    }
  // DOCOPY(CALL_LEVEL) = NCOPIES; (12315)
  {
    descriptor_t *bitRHS
        = getBIT (16, mGENERATExGEN_CLASS0xPUSH_ADOLEVELxNCOPIES);
    putBIT (16, mDOCOPY + 2 * (COREHALFWORD (mCALL_LEVEL)), bitRHS);
    bitRHS->inUse = 0;
  }
  // DOFORM(CALL_LEVEL) = 0; (12316)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mGENERATExDOFORM + 2 * (COREHALFWORD (mCALL_LEVEL)), bitRHS);
    bitRHS->inUse = 0;
  }
  // DOPTR(CALL_LEVEL), SDOPTR(CALL_LEVEL) = ADOPTR; (12317)
  {
    descriptor_t *bitRHS = getBIT (16, mGENERATExADOPTR);
    putBIT (16, mGENERATExDOPTR + 2 * (COREHALFWORD (mCALL_LEVEL)), bitRHS);
    putBIT (16, mGENERATExSDOPTR + 2 * (COREHALFWORD (mCALL_LEVEL)), bitRHS);
    bitRHS->inUse = 0;
  }
  // DOTOT(CALL_LEVEL) = ADOPTR + NCOPIES; (12318)
  {
    int32_t numberRHS = (int32_t)(xadd (
        COREHALFWORD (mGENERATExADOPTR),
        COREHALFWORD (mGENERATExGEN_CLASS0xPUSH_ADOLEVELxNCOPIES)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mGENERATExDOTOT + 2 * (COREHALFWORD (mCALL_LEVEL)), bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
