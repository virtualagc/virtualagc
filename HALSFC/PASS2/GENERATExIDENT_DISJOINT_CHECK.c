/*
  File GENERATExIDENT_DISJOINT_CHECK.c generated by XCOM-I, 2024-08-08
  04:32:26.
*/

#include "runtimeC.h"

int32_t
GENERATExIDENT_DISJOINT_CHECK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExIDENT_DISJOINT_CHECK");
  // OPER_PARM_FLAG,NON_IDENT,DISJOINT,CONSEC_VAC=FALSE; (8876)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExOPER_PARM_FLAG, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxNON_IDENT, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxCONSEC_VAC, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF SYMFORM(FORM(OP)) THEN (8877)
  if (1
      & (bitToFixed (getBIT (
          1, mSYMFORM
                 + 1
                       * COREHALFWORD (
                           getFIXED (mIND_STACK)
                           + 73
                                 * (COREHALFWORD (
                                     mGENERATExIDENT_DISJOINT_CHECKxOP))
                           + 32 + 2 * (0))))))
    // DO; (8878)
    {
    rs1:;
      // OPER_PARM_FLAG=(SYT_FLAGS(LOC(OP)) & PARM_FLAGS)~=0; (8879)
      {
        int32_t numberRHS = (int32_t)(xNEQ (
            xAND (getFIXED (
                      getFIXED (mSYM_TAB)
                      + 34
                            * (COREHALFWORD (
                                getFIXED (mIND_STACK)
                                + 73
                                      * (COREHALFWORD (
                                          mGENERATExIDENT_DISJOINT_CHECKxOP))
                                + 40 + 2 * (0)))
                      + 8 + 4 * (0)),
                  getFIXED (mPARM_FLAGS)),
            0));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mGENERATExOPER_PARM_FLAG, bitRHS);
        bitRHS->inUse = 0;
      }
      // OPER_SYMPTR=LOC(OP); (8880)
      {
        descriptor_t *bitRHS = getBIT (
            16, getFIXED (mIND_STACK)
                    + 73 * (COREHALFWORD (mGENERATExIDENT_DISJOINT_CHECKxOP))
                    + 40 + 2 * (0));
        putBIT (16, mGENERATExOPER_SYMPTR, bitRHS);
        bitRHS->inUse = 0;
      }
      // OPER_STRUCT = SYT_TYPE(LOC(OP)) = STRUCTURE; (8881)
      {
        int32_t numberRHS = (int32_t)(xEQ (
            BYTE0 (getFIXED (mSYM_TAB)
                   + 34
                         * (COREHALFWORD (
                             getFIXED (mIND_STACK)
                             + 73
                                   * (COREHALFWORD (
                                       mGENERATExIDENT_DISJOINT_CHECKxOP))
                             + 40 + 2 * (0)))
                   + 32 + 1 * (0)),
            BYTE0 (mSTRUCTURE)));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mGENERATExOPER_STRUCT, bitRHS);
        bitRHS->inUse = 0;
      }
      // OPER_SYMPTR2 = LOC2(OP); (8882)
      {
        descriptor_t *bitRHS = getBIT (
            16, getFIXED (mIND_STACK)
                    + 73 * (COREHALFWORD (mGENERATExIDENT_DISJOINT_CHECKxOP))
                    + 42 + 2 * (0));
        putBIT (16, mGENERATExOPER_SYMPTR2, bitRHS);
        bitRHS->inUse = 0;
      }
      // CONSEC_VAC=TAG_BITS(N)=VAC; (8883)
      {
        int32_t numberRHS = (int32_t)(xEQ (
            bitToFixed (
                (putBITp (16, mGENERATExTAG_BITSxOP,
                          getBIT (16, mGENERATExIDENT_DISJOINT_CHECKxN)),
                 GENERATExTAG_BITS (0))),
            BYTE0 (mVAC)));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxCONSEC_VAC, bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // ELSE (8884)
  else
    // NON_IDENT,DISJOINT=TRUE; (8885)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxNON_IDENT, bitRHS);
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF ~NON_IDENT & RECVR_SYMPTR~=OPER_SYMPTR THEN (8886)
  if (1
      & (xAND (xNOT (BYTE0 (mGENERATExIDENT_DISJOINT_CHECKxNON_IDENT)),
               xNEQ (COREHALFWORD (mGENERATExRECVR_SYMPTR),
                     COREHALFWORD (mGENERATExOPER_SYMPTR)))))
    // DO; (8887)
    {
    rs2:;
      // IF (~ASSIGN_PARM_FLAG & ~OPER_PARM_FLAG) THEN (8888)
      if (1
          & (xAND (xNOT (BYTE0 (mGENERATExASSIGN_PARM_FLAG)),
                   xNOT (BYTE0 (mGENERATExOPER_PARM_FLAG)))))
        // NON_IDENT,DISJOINT=TRUE; (8889)
        {
          int32_t numberRHS = (int32_t)(1);
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxNON_IDENT, bitRHS);
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT, bitRHS);
          bitRHS->inUse = 0;
        }
      // IF ASSIGN_PARM_FLAG & ~OPER_PARM_FLAG THEN (8890)
      if (1
          & (xAND (BYTE0 (mGENERATExASSIGN_PARM_FLAG),
                   xNOT (BYTE0 (mGENERATExOPER_PARM_FLAG)))))
        // IF SYT_NEST(LOC(OP))>=RECVR_NEST_LEVEL THEN (8891)
        if (1
            & (xGE (
                BYTE0 (getFIXED (mSYM_TAB)
                       + 34
                             * (COREHALFWORD (
                                 getFIXED (mIND_STACK)
                                 + 73
                                       * (COREHALFWORD (
                                           mGENERATExIDENT_DISJOINT_CHECKxOP))
                                 + 40 + 2 * (0)))
                       + 28 + 1 * (0)),
                BYTE0 (mGENERATExRECVR_NEST_LEVEL))))
          // NON_IDENT,DISJOINT=TRUE; (8892)
          {
            int32_t numberRHS = (int32_t)(1);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxNON_IDENT, bitRHS);
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT, bitRHS);
            bitRHS->inUse = 0;
          }
      // IF OPER_PARM_FLAG & ~ASSIGN_PARM_FLAG THEN (8893)
      if (1
          & (xAND (BYTE0 (mGENERATExOPER_PARM_FLAG),
                   xNOT (BYTE0 (mGENERATExASSIGN_PARM_FLAG)))))
        // IF RECVR_NEST_LEVEL>=SYT_NEST(LOC(OP)) THEN (8894)
        if (1
            & (xGE (
                BYTE0 (mGENERATExRECVR_NEST_LEVEL),
                BYTE0 (getFIXED (mSYM_TAB)
                       + 34
                             * (COREHALFWORD (
                                 getFIXED (mIND_STACK)
                                 + 73
                                       * (COREHALFWORD (
                                           mGENERATExIDENT_DISJOINT_CHECKxOP))
                                 + 40 + 2 * (0)))
                       + 28 + 1 * (0)))))
          // NON_IDENT,DISJOINT=TRUE; (8895)
          {
            int32_t numberRHS = (int32_t)(1);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxNON_IDENT, bitRHS);
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT, bitRHS);
            bitRHS->inUse = 0;
          }
    es2:;
    } // End of DO block
  // IF ~DISJOINT THEN (8896)
  if (1 & (xNOT (BYTE0 (mGENERATExIDENT_DISJOINT_CHECKxDISJOINT))))
    // DO; (8897)
    {
    rs3:;
      // IF STMT_PREC~=0 THEN (8898)
      if (1 & (xNEQ (COREHALFWORD (mGENERATExSTMT_PREC), 0)))
        // DATA_WIDTH=4; (8899)
        {
          int32_t numberRHS = (int32_t)(4);
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (16, mGENERATExDATA_WIDTH, bitRHS);
          bitRHS->inUse = 0;
        }
      // ELSE (8900)
      else
        // DATA_WIDTH=2; (8901)
        {
          int32_t numberRHS = (int32_t)(2);
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (16, mGENERATExDATA_WIDTH, bitRHS);
          bitRHS->inUse = 0;
        }
      // START_PART=INX_CON(OP); (8902)
      {
        int32_t numberRHS = (int32_t)(getFIXED (
            getFIXED (mIND_STACK)
            + 73 * (COREHALFWORD (mGENERATExIDENT_DISJOINT_CHECKxOP)) + 4
            + 4 * (0)));
        putFIXED (mGENERATExSTART_PART, numberRHS);
      }
      // SRCEPART_SIZE = START_PART + ROW(OP) * COLUMN(OP) * DATA_WIDTH; (8903)
      {
        int32_t numberRHS = (int32_t)(xadd (
            getFIXED (mGENERATExSTART_PART),
            xmultiply (
                xmultiply (
                    COREHALFWORD (getFIXED (mIND_STACK)
                                  + 73
                                        * (COREHALFWORD (
                                            mGENERATExIDENT_DISJOINT_CHECKxOP))
                                  + 48 + 2 * (0)),
                    COREHALFWORD (getFIXED (mIND_STACK)
                                  + 73
                                        * (COREHALFWORD (
                                            mGENERATExIDENT_DISJOINT_CHECKxOP))
                                  + 24 + 2 * (0))),
                COREHALFWORD (mGENERATExDATA_WIDTH))));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mGENERATExSRCEPART_SIZE, bitRHS);
        bitRHS->inUse = 0;
      }
      // SRCEPART_SIZE = SRCEPART_SIZE + (ROW(OP) - 1) * DEL(OP); (8904)
      {
        int32_t numberRHS = (int32_t)(xadd (
            COREHALFWORD (mGENERATExSRCEPART_SIZE),
            xmultiply (
                xsubtract (
                    COREHALFWORD (getFIXED (mIND_STACK)
                                  + 73
                                        * (COREHALFWORD (
                                            mGENERATExIDENT_DISJOINT_CHECKxOP))
                                  + 48 + 2 * (0)),
                    1),
                COREHALFWORD (
                    getFIXED (mIND_STACK)
                    + 73 * (COREHALFWORD (mGENERATExIDENT_DISJOINT_CHECKxOP))
                    + 28 + 2 * (0)))));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mGENERATExSRCEPART_SIZE, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF ~RECVR_STRUCT & ~OPER_STRUCT THEN (8905)
      if (1
          & (xAND (xNOT (BYTE0 (mGENERATExRECVR_STRUCT)),
                   xNOT (BYTE0 (mGENERATExOPER_STRUCT)))))
        // DO; (8906)
        {
        rs3s1:;
          // IF CONSEC_VAC & ~NONPART THEN (8907)
          if (1
              & (xAND (BYTE0 (mGENERATExIDENT_DISJOINT_CHECKxCONSEC_VAC),
                       xNOT (BYTE0 (mGENERATExNONPART)))))
            // IF INX_OK & INX(OP) = 0 THEN (8908)
            if (1
                & (xAND (BYTE0 (mGENERATExINX_OK),
                         xEQ (COREHALFWORD (
                                  getFIXED (mIND_STACK)
                                  + 73
                                        * (COREHALFWORD (
                                            mGENERATExIDENT_DISJOINT_CHECKxOP))
                                  + 34 + 2 * (0)),
                              0))))
              // IF RECVR_SYMPTR = OPER_SYMPTR THEN (8909)
              if (1
                  & (xEQ (COREHALFWORD (mGENERATExRECVR_SYMPTR),
                          COREHALFWORD (mGENERATExOPER_SYMPTR))))
                // DISJOINT = PART_SIZE <= START_PART | SRCEPART_SIZE <=
                // START_OFF; (8910)
                {
                  int32_t numberRHS = (int32_t)(xOR (
                      xLE (COREHALFWORD (mGENERATExPART_SIZE),
                           getFIXED (mGENERATExSTART_PART)),
                      xLE (COREHALFWORD (mGENERATExSRCEPART_SIZE),
                           getFIXED (mGENERATExSTART_OFF))));
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT, bitRHS);
                  bitRHS->inUse = 0;
                }
              // ELSE (8911)
              else
                // DISJOINT = PART_SIZE <= START_PART; (8912)
                {
                  int32_t numberRHS
                      = (int32_t)(xLE (COREHALFWORD (mGENERATExPART_SIZE),
                                       getFIXED (mGENERATExSTART_PART)));
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT, bitRHS);
                  bitRHS->inUse = 0;
                }
        es3s1:;
        } // End of DO block
      // ELSE (8913)
      else
        // IF RECVR_STRUCT & OPER_STRUCT THEN (8914)
        if (1
            & (xAND (BYTE0 (mGENERATExRECVR_STRUCT),
                     BYTE0 (mGENERATExOPER_STRUCT))))
          // DO; (8915)
          {
          rs3s2:;
            // IF RECVR_SYMPTR = OPER_SYMPTR THEN (8916)
            if (1
                & (xEQ (COREHALFWORD (mGENERATExRECVR_SYMPTR),
                        COREHALFWORD (mGENERATExOPER_SYMPTR))))
              // DO; (8917)
              {
              rs3s2s1:;
                // DISJOINT = ~(RECVR_SYMPTR2 = OPER_SYMPTR2); (8918)
                {
                  int32_t numberRHS = (int32_t)(xNOT (
                      xEQ (COREHALFWORD (mGENERATExRECVR_SYMPTR2),
                           COREHALFWORD (mGENERATExOPER_SYMPTR2))));
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT, bitRHS);
                  bitRHS->inUse = 0;
                }
                // IF ~DISJOINT & INX_OK & INX(OP) = 0 THEN (8919)
                if (1
                    & (xAND (
                        xAND (xNOT (BYTE0 (
                                  mGENERATExIDENT_DISJOINT_CHECKxDISJOINT)),
                              BYTE0 (mGENERATExINX_OK)),
                        xEQ (COREHALFWORD (
                                 getFIXED (mIND_STACK)
                                 + 73
                                       * (COREHALFWORD (
                                           mGENERATExIDENT_DISJOINT_CHECKxOP))
                                 + 34 + 2 * (0)),
                             0))))
                  // DISJOINT = PART_SIZE <= START_PART | SRCEPART_SIZE <=
                  // START_OFF; (8920)
                  {
                    int32_t numberRHS = (int32_t)(xOR (
                        xLE (COREHALFWORD (mGENERATExPART_SIZE),
                             getFIXED (mGENERATExSTART_PART)),
                        xLE (COREHALFWORD (mGENERATExSRCEPART_SIZE),
                             getFIXED (mGENERATExSTART_OFF))));
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT,
                            bitRHS);
                    bitRHS->inUse = 0;
                  }
              es3s2s1:;
              } // End of DO block
            // ELSE (8921)
            else
              // IF INX_OK & INX(OP) = 0 THEN (8922)
              if (1
                  & (xAND (
                      BYTE0 (mGENERATExINX_OK),
                      xEQ (COREHALFWORD (
                               getFIXED (mIND_STACK)
                               + 73
                                     * (COREHALFWORD (
                                         mGENERATExIDENT_DISJOINT_CHECKxOP))
                               + 34 + 2 * (0)),
                           0))))
                // IF ASSIGN_PARM_FLAG & ~OPER_PARM_FLAG THEN (8923)
                if (1
                    & (xAND (BYTE0 (mGENERATExASSIGN_PARM_FLAG),
                             xNOT (BYTE0 (mGENERATExOPER_PARM_FLAG)))))
                  // DISJOINT = SRCEPART_SIZE <= START_OFF; (8924)
                  {
                    int32_t numberRHS = (int32_t)(xLE (
                        COREHALFWORD (mGENERATExSRCEPART_SIZE),
                        getFIXED (mGENERATExSTART_OFF)));
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT,
                            bitRHS);
                    bitRHS->inUse = 0;
                  }
                // ELSE (8925)
                else
                  // IF ~ASSIGN_PARM_FLAG & OPER_PARM_FLAG THEN (8926)
                  if (1
                      & (xAND (xNOT (BYTE0 (mGENERATExASSIGN_PARM_FLAG)),
                               BYTE0 (mGENERATExOPER_PARM_FLAG))))
                    // DISJOINT = PART_SIZE <= START_PART; (8927)
                    {
                      int32_t numberRHS
                          = (int32_t)(xLE (COREHALFWORD (mGENERATExPART_SIZE),
                                           getFIXED (mGENERATExSTART_PART)));
                      descriptor_t *bitRHS;
                      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                      putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT,
                              bitRHS);
                      bitRHS->inUse = 0;
                    }
          es3s2:;
          } // End of DO block
        // ELSE (8928)
        else
          // DO; (8929)
          {
          rs3s3:;
            // IF ~(ASSIGN_PARM_FLAG & OPER_PARM_FLAG) THEN (8930)
            if (1
                & (xNOT (xAND (BYTE0 (mGENERATExASSIGN_PARM_FLAG),
                               BYTE0 (mGENERATExOPER_PARM_FLAG)))))
              // DISJOINT = (ASSIGN_PARM_FLAG & RECVR_STRUCT) | (OPER_PARM_FLAG
              // & OPER_STRUCT); (8931)
              {
                int32_t numberRHS
                    = (int32_t)(xOR (xAND (BYTE0 (mGENERATExASSIGN_PARM_FLAG),
                                           BYTE0 (mGENERATExRECVR_STRUCT)),
                                     xAND (BYTE0 (mGENERATExOPER_PARM_FLAG),
                                           BYTE0 (mGENERATExOPER_STRUCT))));
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT, bitRHS);
                bitRHS->inUse = 0;
              }
          es3s3:;
          } // End of DO block
    es3:;
    } // End of DO block
  // IF N=1 THEN (8932)
  if (1 & (xEQ (COREHALFWORD (mGENERATExIDENT_DISJOINT_CHECKxN), 1)))
    // DO; (8933)
    {
    rs4:;
      // LEFT_DISJOINT=DISJOINT; (8934)
      {
        descriptor_t *bitRHS
            = getBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT);
        putBIT (1, mGENERATExLEFT_DISJOINT, bitRHS);
        bitRHS->inUse = 0;
      }
      // LNON_IDENT=NON_IDENT; (8935)
      {
        descriptor_t *bitRHS
            = getBIT (1, mGENERATExIDENT_DISJOINT_CHECKxNON_IDENT);
        putBIT (1, mGENERATExLNON_IDENT, bitRHS);
        bitRHS->inUse = 0;
      }
    es4:;
    } // End of DO block
  // ELSE (8936)
  else
    // DO; (8937)
    {
    rs5:;
      // RIGHT_DISJOINT=DISJOINT; (8938)
      {
        descriptor_t *bitRHS
            = getBIT (1, mGENERATExIDENT_DISJOINT_CHECKxDISJOINT);
        putBIT (1, mGENERATExRIGHT_DISJOINT, bitRHS);
        bitRHS->inUse = 0;
      }
      // RNON_IDENT=NON_IDENT; (8939)
      {
        descriptor_t *bitRHS
            = getBIT (1, mGENERATExIDENT_DISJOINT_CHECKxNON_IDENT);
        putBIT (1, mGENERATExRNON_IDENT, bitRHS);
        bitRHS->inUse = 0;
      }
    es5:;
    } // End of DO block
  // IF NUMOP=1 THEN (8940)
  if (1 & (xEQ (COREHALFWORD (mNUMOP), 1)))
    // RIGHT_DISJOINT,RNON_IDENT=TRUE; (8941)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mGENERATExRIGHT_DISJOINT, bitRHS);
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mGENERATExRNON_IDENT, bitRHS);
      bitRHS->inUse = 0;
    }
  {
    reentryGuard = 0;
    return 0;
  }
}
