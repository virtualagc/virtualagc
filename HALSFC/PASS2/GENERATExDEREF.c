/*
  File GENERATExDEREF.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

descriptor_t *
GENERATExDEREF (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExDEREF");
  // IF NAME_FUNCTION(OP) THEN (2801)
  if (1
      & (bitToFixed ((putBITp (16, mGENERATExNAME_FUNCTIONxOP,
                               getBIT (16, mGENERATExDEREFxOP)),
                      GENERATExNAME_FUNCTION (0)))))
    // DO; (2802)
    {
    rs1:;
      // IF ( ((SYT_FLAGS(LOC2(OP)) & NAME_FLAG) = 0) | (SYT_TYPE(LOC(OP)) ~=
      // STRUCTURE) ) THEN (2803)
      if (1
          & (xOR (
              xEQ (xAND (
                       getFIXED (
                           getFIXED (mSYM_TAB)
                           + 34
                                 * (COREHALFWORD (
                                     getFIXED (mIND_STACK)
                                     + 73 * (COREHALFWORD (mGENERATExDEREFxOP))
                                     + 42 + 2 * (0)))
                           + 8 + 4 * (0)),
                       getFIXED (mNAME_FLAG)),
                   0),
              xNEQ (BYTE0 (getFIXED (mSYM_TAB)
                           + 34
                                 * (COREHALFWORD (
                                     getFIXED (mIND_STACK)
                                     + 73 * (COREHALFWORD (mGENERATExDEREFxOP))
                                     + 40 + 2 * (0)))
                           + 32 + 1 * (0)),
                    BYTE0 (mSTRUCTURE)))))
        // RETURN FALSE; (2804)
        {
          reentryGuard = 0;
          return fixedToBit (32, (int32_t)(0));
        }
      // IF POINTS_REMOTE(OP) = 0 THEN (2805)
      if (1
          & (xEQ (BYTE0 (getFIXED (mIND_STACK)
                         + 73 * (COREHALFWORD (mGENERATExDEREFxOP)) + 68
                         + 1 * (0)),
                  0)))
        // RETURN FALSE ; (2806)
        {
          reentryGuard = 0;
          return fixedToBit (32, (int32_t)(0));
        }
    es1:;
    } // End of DO block
  // RETURN TRUE ; (2807)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(1));
  }
}
