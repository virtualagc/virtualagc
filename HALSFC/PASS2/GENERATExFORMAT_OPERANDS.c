/*
  File GENERATExFORMAT_OPERANDS.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
GENERATExFORMAT_OPERANDS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExFORMAT_OPERANDS");
  // IF FLAG = NRTEMP THEN (3109)
  if (1 & (xEQ (COREHALFWORD (mGENERATExFORMAT_OPERANDSxFLAG), BYTE0 (mWORK))))
    // FLAG = CSYM; (3110)
    {
      descriptor_t *bitRHS = getBIT (8, mCSYM);
      putBIT (16, mGENERATExFORMAT_OPERANDSxFLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF FLAG = AIDX THEN (3111)
  if (1 & (xEQ (COREHALFWORD (mGENERATExFORMAT_OPERANDSxFLAG), BYTE0 (mAIDX))))
    // FLAG = INL; (3112)
    {
      descriptor_t *bitRHS = getBIT (8, mINL);
      putBIT (16, mGENERATExFORMAT_OPERANDSxFLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // DO CASE FLAG; (3113)
  {
  rs1:
    switch (COREHALFWORD (mGENERATExFORMAT_OPERANDSxFLAG))
      {
      case 0:
        // CHARSTRING = PTR; (3115)
        {
          descriptor_t *bitRHS = getBIT (16, mGENERATExFORMAT_OPERANDSxPTR);
          descriptor_t *stringRHS;
          stringRHS = bitToCharacter (bitRHS);
          putCHARACTER (mCHARSTRING, stringRHS);
          bitRHS->inUse = 0;
          stringRHS->inUse = 0;
        }
        break;
      case 1:
        // CHARSTRING = SYT_NAME(PTR); (3116)
        {
          descriptor_t *stringRHS;
          stringRHS = getCHARACTER (
              getFIXED (mSYM_TAB)
              + 34 * (COREHALFWORD (mGENERATExFORMAT_OPERANDSxPTR)) + 0
              + 4 * (0));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 2:
        // CHARSTRING = SYT_NAME(PTR); (3117)
        {
          descriptor_t *stringRHS;
          stringRHS = getCHARACTER (
              getFIXED (mSYM_TAB)
              + 34 * (COREHALFWORD (mGENERATExFORMAT_OPERANDSxPTR)) + 0
              + 4 * (0));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 3:
          // ; (3118)
          ;
        break;
      case 4:
        // CHARSTRING = 'BASE_REG#' || PTR; (3119)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              cToDescriptor (NULL, "BASE_REG#"),
              bitToCharacter (getBIT (16, mGENERATExFORMAT_OPERANDSxPTR)));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 5:
          // ; (3120)
          ;
        break;
      case 6:
        // CHARSTRING = SYT_NAME(PTR); (3121)
        {
          descriptor_t *stringRHS;
          stringRHS = getCHARACTER (
              getFIXED (mSYM_TAB)
              + 34 * (COREHALFWORD (mGENERATExFORMAT_OPERANDSxPTR)) + 0
              + 4 * (0));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 7:
          // ; (3122)
          ;
        break;
      case 8:
        // CHARSTRING = '=C''' || DESC(CONSTANTS(PTR)) || QUOTE; (3123)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              xsCAT (cToDescriptor (NULL, "=C'"),
                     getCHARACTER (
                         mCONSTANTS
                         + 4 * COREHALFWORD (mGENERATExFORMAT_OPERANDSxPTR))),
              getCHARACTER (mQUOTE));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 9:
        // CHARSTRING = '=H''' || CONSTANTS(PTR) || QUOTE; (3124)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              xsCAT (cToDescriptor (NULL, "=H'"),
                     fixedToCharacter (getFIXED (
                         mCONSTANTS
                         + 4 * COREHALFWORD (mGENERATExFORMAT_OPERANDSxPTR)))),
              getCHARACTER (mQUOTE));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 10:
        // CHARSTRING = '=F''' || CONSTANTS(PTR) || QUOTE; (3125)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              xsCAT (cToDescriptor (NULL, "=F'"),
                     fixedToCharacter (getFIXED (
                         mCONSTANTS
                         + 4 * COREHALFWORD (mGENERATExFORMAT_OPERANDSxPTR)))),
              getCHARACTER (mQUOTE));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 11:
        // CHARSTRING = '=XL4''' || HEX(CONSTANTS(PTR),8) || QUOTE; (3126)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              xsCAT (cToDescriptor (NULL, "=XL4'"),
                     (putFIXED (
                          mHEXxHVAL,
                          getFIXED (mCONSTANTS
                                    + 4
                                          * COREHALFWORD (
                                              mGENERATExFORMAT_OPERANDSxPTR))),
                      putFIXED (mHEXxN, 8), HEX (0))),
              getCHARACTER (mQUOTE));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 12:
        // CHARSTRING = '=XL8''' || HEX(CONSTANTS(PTR),8) ||
        // HEX(CONSTANTS(PTR+1),8) || QUOTE; (3127)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              xsCAT (
                  xsCAT (cToDescriptor (NULL, "=XL8'"),
                         (putFIXED (
                              mHEXxHVAL,
                              getFIXED (
                                  mCONSTANTS
                                  + 4
                                        * COREHALFWORD (
                                            mGENERATExFORMAT_OPERANDSxPTR))),
                          putFIXED (mHEXxN, 8), HEX (0))),
                  (putFIXED (
                       mHEXxHVAL,
                       getFIXED (
                           mCONSTANTS
                           + 4
                                 * xadd (COREHALFWORD (
                                             mGENERATExFORMAT_OPERANDSxPTR),
                                         1))),
                   putFIXED (mHEXxN, 8), HEX (0))),
              getCHARACTER (mQUOTE));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 13:
        // CHARSTRING = '=A(#' || PTR || RIGHTBRACKET; (3128)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (xsCAT (cToDescriptor (NULL, "=A(#"),
                                    bitToCharacter (getBIT (
                                        16, mGENERATExFORMAT_OPERANDSxPTR))),
                             getCHARACTER (mRIGHTBRACKET));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 14:
          // ; (3129)
          ;
        break;
      case 15:
          // ; (3130)
          ;
        break;
      case 16:
          // ; (3131)
          ;
        break;
      case 17:
        // CHARSTRING = '*+'||PTR; (3132)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              cToDescriptor (NULL, "*+"),
              bitToCharacter (getBIT (16, mGENERATExFORMAT_OPERANDSxPTR)));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 18:
        // CHARSTRING = SYT_NAME(PTR); (3133)
        {
          descriptor_t *stringRHS;
          stringRHS = getCHARACTER (
              getFIXED (mSYM_TAB)
              + 34 * (COREHALFWORD (mGENERATExFORMAT_OPERANDSxPTR)) + 0
              + 4 * (0));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 19:
        // CHARSTRING = 'L#' || PTR; (3134)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              cToDescriptor (NULL, "L#"),
              bitToCharacter (getBIT (16, mGENERATExFORMAT_OPERANDSxPTR)));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 20:
        // CHARSTRING = 'P#' || PTR; (3135)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              cToDescriptor (NULL, "P#"),
              bitToCharacter (getBIT (16, mGENERATExFORMAT_OPERANDSxPTR)));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 21:
          // ; (3136)
          ;
        break;
      case 22:
        // CHARSTRING = ESD_TABLE(PTR); (3137)
        {
          descriptor_t *stringRHS;
          stringRHS = (putBITp (16, mESD_TABLExPTR,
                                getBIT (16, mGENERATExFORMAT_OPERANDSxPTR)),
                       ESD_TABLE (0));
          putCHARACTER (mCHARSTRING, stringRHS);
          stringRHS->inUse = 0;
        }
        break;
      case 23:
        // CHARSTRING = PTR; (3138)
        {
          descriptor_t *bitRHS = getBIT (16, mGENERATExFORMAT_OPERANDSxPTR);
          descriptor_t *stringRHS;
          stringRHS = bitToCharacter (bitRHS);
          putCHARACTER (mCHARSTRING, stringRHS);
          bitRHS->inUse = 0;
          stringRHS->inUse = 0;
        }
        break;
      }
  } // End of DO CASE block
  // CHARSTRING = CHARSTRING || INFO; (3138)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (getCHARACTER (mCHARSTRING), getCHARACTER (mINFO));
    putCHARACTER (mCHARSTRING, stringRHS);
    stringRHS->inUse = 0;
  }
  // INFO = ''; (3139)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "");
    putCHARACTER (mINFO, stringRHS);
    stringRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
