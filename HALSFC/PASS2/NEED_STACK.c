/*
  File NEED_STACK.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

int32_t
NEED_STACK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "NEED_STACK");
  // NOT_LEAF(IX) = NOT_LEAF(IX) | TRUE; (1191)
  {
    int32_t numberRHS = (int32_t)(xOR (
        BYTE0 (mNOT_LEAF + 1 * COREHALFWORD (mNEED_STACKxIX)), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mNOT_LEAF + 1 * (COREHALFWORD (mNEED_STACKxIX)), bitRHS);
    bitRHS->inUse = 0;
  }
  // IF MAXTEMP(IX) = 0 THEN (1192)
  if (1 & (xEQ (getFIXED (mMAXTEMP + 4 * COREHALFWORD (mNEED_STACKxIX)), 0)))
    // WORKSEG(IX), MAXTEMP(IX) = STACK_FREEPOINT; (1193)
    {
      descriptor_t *bitRHS = getBIT (8, mSTACK_FREEPOINT);
      int32_t numberRHS;
      numberRHS = bitToFixed (bitRHS);
      putFIXED (mWORKSEG + 4 * (COREHALFWORD (mNEED_STACKxIX)), numberRHS);
      numberRHS = bitToFixed (bitRHS);
      putFIXED (mMAXTEMP + 4 * (COREHALFWORD (mNEED_STACKxIX)), numberRHS);
      bitRHS->inUse = 0;
    }
  // IF GENERATING THEN (1194)
  if (1 & (bitToFixed (getBIT (1, mGENERATING))))
    // IF ORIGIN(IX) > 0 THEN (1195)
    if (1 & (xGT (getFIXED (mORIGIN + 4 * COREHALFWORD (mNEED_STACKxIX)), 0)))
      // ORIGIN(IX) = NO_BRANCH_AROUND(ORIGIN(IX)); (1196)
      {
        int32_t numberRHS = (int32_t)((
            putFIXED (mNO_BRANCH_AROUNDxLINE,
                      getFIXED (mORIGIN + 4 * COREHALFWORD (mNEED_STACKxIX))),
            NO_BRANCH_AROUND (0)));
        putFIXED (mORIGIN + 4 * (COREHALFWORD (mNEED_STACKxIX)), numberRHS);
      }
  {
    reentryGuard = 0;
    return 0;
  }
}
