/*
  File GENERATExOFF_TARGET.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

int32_t
GENERATExOFF_TARGET (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExOFF_TARGET");
  // USAGE(TARGET_REGISTER) = USAGE(TARGET_REGISTER) - 2; (4240)
  {
    int32_t numberRHS = (int32_t)(xsubtract (
        COREHALFWORD (mUSAGE + 2 * COREHALFWORD (mTARGET_REGISTER)), 2));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mUSAGE + 2 * (COREHALFWORD (mTARGET_REGISTER)), bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL SET_REG_NEXT_USE(TARGET_REGISTER, OP); (4241)
  {
    putBITp (16, mGENERATExSET_REG_NEXT_USExR, getBIT (16, mTARGET_REGISTER));
    putBITp (16, mGENERATExSET_REG_NEXT_USExOP,
             getBIT (16, mGENERATExOFF_TARGETxOP));
    GENERATExSET_REG_NEXT_USE (0);
  }
  // IF ~USAGE(TARGET_REGISTER) THEN (4242)
  if (1 & (xNOT (COREHALFWORD (mUSAGE + 2 * COREHALFWORD (mTARGET_REGISTER)))))
    // DO; (4243)
    {
    rs1:;
      // USAGE(TARGET_REGISTER) = USAGE(TARGET_REGISTER) + 1; (4244)
      {
        int32_t numberRHS = (int32_t)(xadd (
            COREHALFWORD (mUSAGE + 2 * COREHALFWORD (mTARGET_REGISTER)), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mUSAGE + 2 * (COREHALFWORD (mTARGET_REGISTER)), bitRHS);
        bitRHS->inUse = 0;
      }
      // R_CONTENTS(TARGET_REGISTER) = IMD; (4245)
      {
        descriptor_t *bitRHS = getBIT (8, mIMD);
        putBIT (8, mR_CONTENTS + 1 * (COREHALFWORD (mTARGET_REGISTER)),
                bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // TARGET_REGISTER = -1; (4246)
  {
    int32_t numberRHS = (int32_t)(-1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mTARGET_REGISTER, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
