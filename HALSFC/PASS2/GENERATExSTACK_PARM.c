/*
  File GENERATExSTACK_PARM.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
GENERATExSTACK_PARM (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExSTACK_PARM");
  // R_PARM# = R_PARM# + 1; (5967)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mR_PARMp), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mR_PARMp, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF R_PARM# > REG_NUM THEN (5968)
  if (1 & (xGT (COREHALFWORD (mR_PARMp), 15)))
    // CALL ERRORS(CLASS_BS, 103); (5969)
    {
      putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_BS));
      putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(103)));
      ERRORS (0);
    }
  // R_PARM(R_PARM#) = OP; (5970)
  {
    descriptor_t *bitRHS = getBIT (16, mGENERATExSTACK_PARMxOP);
    putBIT (16, mR_PARM + 2 * (COREHALFWORD (mR_PARMp)), bitRHS);
    bitRHS->inUse = 0;
  }
  // BACKUP_REG(OP) = REG(OP); (5971)
  {
    descriptor_t *bitRHS
        = getBIT (16, getFIXED (mIND_STACK)
                          + 73 * (COREHALFWORD (mGENERATExSTACK_PARMxOP)) + 46
                          + 2 * (0));
    putBIT (16,
            getFIXED (mIND_STACK)
                + 73 * (COREHALFWORD (mGENERATExSTACK_PARMxOP)) + 20 + 2 * (0),
            bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
