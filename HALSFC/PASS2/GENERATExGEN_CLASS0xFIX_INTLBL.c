/*
  File GENERATExGEN_CLASS0xFIX_INTLBL.c generated by XCOM-I, 2024-08-09
  12:39:31.
*/

#include "runtimeC.h"

int32_t
GENERATExGEN_CLASS0xFIX_INTLBL (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExGEN_CLASS0xFIX_INTLBL");
  // DO WHILE LBL >= RECORD_TOP(STMTNUM); (11382)
  while (1
         & (xGE (COREHALFWORD (mGENERATExGEN_CLASS0xFIX_INTLBLxLBL),
                 xsubtract (COREWORD (xadd (
                                ADDR ("STMTNUM", 0x80000000, NULL, 0), 12)),
                            1))))
    {
      // DO ; (11383)
      {
      rs1s1:;
        // IF COREWORD ( ADDR ( STMTNUM ) + 12 ) >= COREWORD ( ADDR ( STMTNUM )
        // + 8 ) THEN (11384)
        if (1
            & (xGE (
                COREWORD (xadd (ADDR ("STMTNUM", 0x80000000, NULL, 0), 12)),
                COREWORD (xadd (ADDR ("STMTNUM", 0x80000000, NULL, 0), 8)))))
          // CALL _NEEDMORE_SPACE ( ADDR ( STMTNUM )  ) ; (11385)
          {
            putFIXED (m_NEEDMORE_SPACExDOPE,
                      ADDR ("STMTNUM", 0x80000000, NULL, 0));
            _NEEDMORE_SPACE (0);
          }
        // COREWORD ( ADDR ( STMTNUM ) + 12 ) = COREWORD ( ADDR ( STMTNUM ) +
        // 12 ) + 1 ; (11386)
        {
          int32_t numberRHS = (int32_t)(xadd (
              COREWORD (xadd (ADDR ("STMTNUM", 0x80000000, NULL, 0), 12)), 1));
          COREWORD2 (xadd (ADDR ("STMTNUM", 0x80000000, NULL, 0), 12),
                     numberRHS);
        }
      es1s1:;
      } // End of DO block
    }   // End of DO WHILE block
  // IF LABEL_ARRAY(LBL) ~= 0 THEN (11387)
  if (1
      & (xNEQ (COREHALFWORD (
                   getFIXED (mSTMTNUM)
                   + 2 * (COREHALFWORD (mGENERATExGEN_CLASS0xFIX_INTLBLxLBL))
                   + 0 + 2 * (0)),
               0)))
    // DO; (11388)
    {
    rs2:;
      // CALL SET_LABEL(STATNO); (11389)
      {
        putBITp (16, mGENERATExSET_LABELxSTMTNO,
                 getBIT (16, mGENERATExGEN_CLASS0xFIX_INTLBLxSTATNO));
        GENERATExSET_LABEL (0);
      }
      // CALL EMITBFW(ALWAYS, GETINTLBL(LBL)); (11390)
      {
        putBITp (16, mGENERATExEMITBFWxCOND, getBIT (8, mALWAYS));
        putBITp (
            16, mGENERATExEMITBFWxPTR,
            fixedToBit (
                32,
                (int32_t)((
                    putBITp (16, mGENERATExGEN_CLASS0xGETINTLBLxLABELp,
                             getBIT (16, mGENERATExGEN_CLASS0xFIX_INTLBLxLBL)),
                    GENERATExGEN_CLASS0xGETINTLBL (0)))));
        GENERATExEMITBFW (0);
      }
    es2:;
    } // End of DO block
  // ELSE (11391)
  else
    // DO; (11392)
    {
    rs3:;
      // IF ASSEMBLER_CODE THEN (11393)
      if (1 & (bitToFixed (getBIT (1, mASSEMBLER_CODE))))
        // OUTPUT=HEX_LOCCTR||'P#'||STATNO||' EQU L#'||LBL; (11394)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              xsCAT (xsCAT (xsCAT (HEX_LOCCTR (0), cToDescriptor (NULL, "P#")),
                            bitToCharacter (getBIT (
                                16, mGENERATExGEN_CLASS0xFIX_INTLBLxSTATNO))),
                     cToDescriptor (NULL, " EQU L#")),
              bitToCharacter (
                  getBIT (16, mGENERATExGEN_CLASS0xFIX_INTLBLxLBL)));
          OUTPUT (0, stringRHS);
          stringRHS->inUse = 0;
        }
      // LABEL_ARRAY(LBL) = STATNO; (11395)
      {
        descriptor_t *bitRHS
            = getBIT (16, mGENERATExGEN_CLASS0xFIX_INTLBLxSTATNO);
        putBIT (16,
                getFIXED (mSTMTNUM)
                    + 2 * (COREHALFWORD (mGENERATExGEN_CLASS0xFIX_INTLBLxLBL))
                    + 0 + 2 * (0),
                bitRHS);
        bitRHS->inUse = 0;
      }
    es3:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
