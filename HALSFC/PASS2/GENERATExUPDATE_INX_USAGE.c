/*
  File GENERATExUPDATE_INX_USAGE.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
GENERATExUPDATE_INX_USAGE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExUPDATE_INX_USAGE");
  // IF USAGE(REG(OP)) > 1 & TO_BE_MODIFIED THEN (5671)
  if (1
      & (xAND (xGT (COREHALFWORD (
                        mUSAGE
                        + 2
                              * COREHALFWORD (
                                  getFIXED (mIND_STACK)
                                  + 73
                                        * (COREHALFWORD (
                                            mGENERATExUPDATE_INX_USAGExOP))
                                  + 46 + 2 * (0))),
                    1),
               BYTE0 (mGENERATExTO_BE_MODIFIED))))
    // DO; (5672)
    {
    rs1:;
      // CALL NEW_REG(OP); (5673)
      {
        putBITp (16, mGENERATExNEW_REGxPTR,
                 getBIT (16, mGENERATExUPDATE_INX_USAGExOP));
        GENERATExNEW_REG (0);
      }
    es1:;
    } // End of DO block
  // ELSE (5674)
  else
    // DO; (5675)
    {
    rs2:;
      // CALL INCR_REG(OP); (5676)
      {
        putBITp (16, mGENERATExINCR_REGxOP,
                 getBIT (16, mGENERATExUPDATE_INX_USAGExOP));
        GENERATExINCR_REG (0);
      }
    es2:;
    } // End of DO block
  // TO_BE_MODIFIED = FALSE; (5677)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExTO_BE_MODIFIED, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
