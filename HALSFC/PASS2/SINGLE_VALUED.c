/*
  File SINGLE_VALUED.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

descriptor_t *
SINGLE_VALUED (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SINGLE_VALUED");
  // IF (SYT_FLAGS(PTR) & NAME_FLAG) ~= 0 THEN (864)
  if (1
      & (xNEQ (xAND (getFIXED (getFIXED (mSYM_TAB)
                               + 34 * (COREHALFWORD (mSINGLE_VALUEDxPTR)) + 8
                               + 4 * (0)),
                     getFIXED (mNAME_FLAG)),
               0)))
    // RETURN TRUE; (865)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(1));
    }
  // IF PACKTYPE(SYT_TYPE(PTR)) THEN (866)
  if (1
      & (bitToFixed (getBIT (
          8, mPACKTYPE
                 + 1
                       * BYTE0 (getFIXED (mSYM_TAB)
                                + 34 * (COREHALFWORD (mSINGLE_VALUEDxPTR)) + 32
                                + 1 * (0))))))
    // RETURN SYT_ARRAY(PTR) = 0; (867)
    {
      reentryGuard = 0;
      return fixedToBit (
          32, (int32_t)(xEQ (
                  COREHALFWORD (getFIXED (mSYM_TAB)
                                + 34 * (COREHALFWORD (mSINGLE_VALUEDxPTR)) + 20
                                + 2 * (0)),
                  0)));
    }
  // ELSE (868)
  else
    // RETURN FALSE; (869)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
}
