/*
  File GENERATExMASK_BIT_LIT.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

descriptor_t *
GENERATExMASK_BIT_LIT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExMASK_BIT_LIT");
  // IF SIZE(ROP) > SIZE(LOP) THEN (7404)
  if (1
      & (xGT (COREHALFWORD (getFIXED (mIND_STACK)
                            + 73 * (COREHALFWORD (mGENERATExMASK_BIT_LITxROP))
                            + 48 + 2 * (0)),
              COREHALFWORD (getFIXED (mIND_STACK)
                            + 73 * (COREHALFWORD (mGENERATExMASK_BIT_LITxLOP))
                            + 48 + 2 * (0)))))
    // DO; (7405)
    {
    rs1:;
      // VAL(ROP) = VAL(ROP) & XITAB(SIZE(LOP)); (7406)
      {
        int32_t numberRHS = (int32_t)(xAND (
            getFIXED (getFIXED (mIND_STACK)
                      + 73 * (COREHALFWORD (mGENERATExMASK_BIT_LITxROP)) + 12
                      + 4 * (0)),
            getFIXED (
                mGENERATExXITAB
                + 4
                      * COREHALFWORD (
                          getFIXED (mIND_STACK)
                          + 73 * (COREHALFWORD (mGENERATExMASK_BIT_LITxLOP))
                          + 48 + 2 * (0)))));
        putFIXED (getFIXED (mIND_STACK)
                      + 73 * (COREHALFWORD (mGENERATExMASK_BIT_LITxROP)) + 12
                      + 4 * (0),
                  numberRHS);
      }
      // SIZE(ROP) = SIZE(LOP); (7407)
      {
        descriptor_t *bitRHS = getBIT (
            16, getFIXED (mIND_STACK)
                    + 73 * (COREHALFWORD (mGENERATExMASK_BIT_LITxLOP)) + 48
                    + 2 * (0));
        putBIT (16,
                getFIXED (mIND_STACK)
                    + 73 * (COREHALFWORD (mGENERATExMASK_BIT_LITxROP)) + 48
                    + 2 * (0),
                bitRHS);
        bitRHS->inUse = 0;
      }
      // LOC(ROP) = -1; (7408)
      {
        int32_t numberRHS = (int32_t)(-1);
        putBIT (16,
                getFIXED (mIND_STACK)
                    + 73 * (COREHALFWORD (mGENERATExMASK_BIT_LITxROP)) + 40
                    + 2 * (0),
                fixedToBit (16, numberRHS));
      }
    es1:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
