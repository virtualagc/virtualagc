/*
  File GENERATExGEN_CLASS6xINTEGER_MULTIPLYxPAIRED.c generated by XCOM-I,
  2024-08-09 12:39:31.
*/

#include "runtimeC.h"

descriptor_t *
GENERATExGEN_CLASS6xINTEGER_MULTIPLYxPAIRED (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard,
                               "GENERATExGEN_CLASS6xINTEGER_MULTIPLYxPAIRED");
  // IF USAGE(REG(OP)) > 3 THEN (13547)
  if (1
      & (xGT (
          COREHALFWORD (
              mUSAGE
              + 2
                    * COREHALFWORD (
                        getFIXED (mIND_STACK)
                        + 73
                              * (COREHALFWORD (
                                  mGENERATExGEN_CLASS6xINTEGER_MULTIPLYxPAIREDxOP))
                        + 46 + 2 * (0))),
          3)))
    // RETURN FALSE; (13548)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
  // IF REG(OP) THEN (13549)
  if (1
      & (bitToFixed (getBIT (
          16, getFIXED (mIND_STACK)
                  + 73
                        * (COREHALFWORD (
                            mGENERATExGEN_CLASS6xINTEGER_MULTIPLYxPAIREDxOP))
                  + 46 + 2 * (0)))))
    // DO; (13550)
    {
    rs1:;
      // IF OPTYPE = INTEGER THEN (13551)
      if (1 & (xEQ (COREHALFWORD (mOPTYPE), BYTE0 (mINTEGER))))
        // RETURN TRUE; (13552)
        {
          reentryGuard = 0;
          return fixedToBit (32, (int32_t)(1));
        }
      // MATE = REG(OP) - 1; (13553)
      {
        int32_t numberRHS = (int32_t)(xsubtract (
            COREHALFWORD (
                getFIXED (mIND_STACK)
                + 73
                      * (COREHALFWORD (
                          mGENERATExGEN_CLASS6xINTEGER_MULTIPLYxPAIREDxOP))
                + 46 + 2 * (0)),
            1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mGENERATExGEN_CLASS6xINTEGER_MULTIPLYxPAIREDxMATE, bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // ELSE (13554)
  else
    // MATE = REG(OP) + 1; (13555)
    {
      int32_t numberRHS = (int32_t)(xadd (
          COREHALFWORD (
              getFIXED (mIND_STACK)
              + 73
                    * (COREHALFWORD (
                        mGENERATExGEN_CLASS6xINTEGER_MULTIPLYxPAIREDxOP))
              + 46 + 2 * (0)),
          1));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mGENERATExGEN_CLASS6xINTEGER_MULTIPLYxPAIREDxMATE, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF USAGE(MATE) < 2 THEN (13556)
  if (1
      & (xLT (
          COREHALFWORD (
              mUSAGE
              + 2
                    * COREHALFWORD (
                        mGENERATExGEN_CLASS6xINTEGER_MULTIPLYxPAIREDxMATE)),
          2)))
    // RETURN TRUE; (13557)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(1));
    }
  // RETURN FALSE; (13558)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(0));
  }
}
