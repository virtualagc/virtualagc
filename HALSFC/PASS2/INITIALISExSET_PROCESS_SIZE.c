/*
  File INITIALISExSET_PROCESS_SIZE.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
INITIALISExSET_PROCESS_SIZE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "INITIALISExSET_PROCESS_SIZE");
  // IF SYT_TYPE(OP) = TASK_LABEL | SYT_TYPE(OP) = PROG_LABEL THEN (1988)
  if (1
      & (xOR (
          xEQ (BYTE0 (getFIXED (mSYM_TAB)
                      + 34 * (COREHALFWORD (mINITIALISExSET_PROCESS_SIZExOP))
                      + 32 + 1 * (0)),
               BYTE0 (mTASK_LABEL)),
          xEQ (BYTE0 (getFIXED (mSYM_TAB)
                      + 34 * (COREHALFWORD (mINITIALISExSET_PROCESS_SIZExOP))
                      + 32 + 1 * (0)),
               BYTE0 (mPROG_LABEL)))))
    // SYT_DIMS(OP) =  65281; (1989)
    {
      int32_t numberRHS = (int32_t)(65281);
      putBIT (16,
              getFIXED (mSYM_TAB)
                  + 34 * (COREHALFWORD (mINITIALISExSET_PROCESS_SIZExOP)) + 18
                  + 2 * (0),
              fixedToBit (16, numberRHS));
    }
  {
    reentryGuard = 0;
    return 0;
  }
}
