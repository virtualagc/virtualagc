/*
  File OBJECT_GENERATORxCHECK_NO_BASE_RS.c generated by XCOM-I, 2024-08-08
  04:32:26.
*/

#include "runtimeC.h"

descriptor_t *
OBJECT_GENERATORxCHECK_NO_BASE_RS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard
      = guardReentry (reentryGuard, "OBJECT_GENERATORxCHECK_NO_BASE_RS");
  // IF LHS ~= SRSTYPE & B = 3 THEN (15445)
  if (1
      & (xAND (xNEQ (COREHALFWORD (mLHS), BYTE0 (mSRSTYPE)),
               xEQ (getFIXED (mB), 3))))
    // RETURN( LEFTBRACKET||RIGHTBRACKET ); (15446)
    {
      reentryGuard = 0;
      return xsCAT (getCHARACTER (mLEFTBRACKET), getCHARACTER (mRIGHTBRACKET));
    }
  // ELSE (15447)
  else
    // RETURN( LEFTBRACKET||'R'||B||RIGHTBRACKET); (15448)
    {
      reentryGuard = 0;
      return xsCAT (xsCAT (xsCAT (getCHARACTER (mLEFTBRACKET),
                                  cToDescriptor (NULL, "R")),
                           fixedToCharacter (getFIXED (mB))),
                    getCHARACTER (mRIGHTBRACKET));
    }
}
