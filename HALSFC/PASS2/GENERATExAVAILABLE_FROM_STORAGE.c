/*
  File GENERATExAVAILABLE_FROM_STORAGE.c generated by XCOM-I, 2024-08-08
  04:32:26.
*/

#include "runtimeC.h"

int32_t
GENERATExAVAILABLE_FROM_STORAGE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard
      = guardReentry (reentryGuard, "GENERATExAVAILABLE_FROM_STORAGE");
  // IF SYMFORM(FORM(OP)) | FORM(OP) = WORK | FORM(OP) = LIT THEN (6567)
  if (1
      & (xOR (
          xOR (BYTE0 (mSYMFORM
                      + 1
                            * COREHALFWORD (
                                getFIXED (mIND_STACK)
                                + 73
                                      * (COREHALFWORD (
                                          mGENERATExAVAILABLE_FROM_STORAGExOP))
                                + 32 + 2 * (0))),
               xEQ (COREHALFWORD (
                        getFIXED (mIND_STACK)
                        + 73
                              * (COREHALFWORD (
                                  mGENERATExAVAILABLE_FROM_STORAGExOP))
                        + 32 + 2 * (0)),
                    BYTE0 (mWORK))),
          xEQ (COREHALFWORD (
                   getFIXED (mIND_STACK)
                   + 73 * (COREHALFWORD (mGENERATExAVAILABLE_FROM_STORAGExOP))
                   + 32 + 2 * (0)),
               BYTE0 (mLIT)))))
    // DO; (6568)
    {
    rs1:;
      // ITYPE = RX; (6569)
      {
        descriptor_t *bitRHS = getBIT (16, mGENERATExRX);
        putBIT (16, mGENERATExAVAILABLE_FROM_STORAGExITYPE, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF FORM(OP) = LIT THEN (6570)
      if (1
          & (xEQ (
              COREHALFWORD (
                  getFIXED (mIND_STACK)
                  + 73 * (COREHALFWORD (mGENERATExAVAILABLE_FROM_STORAGExOP))
                  + 32 + 2 * (0)),
              BYTE0 (mLIT))))
        // IF OPMODE(OPTYPE) = 1 THEN (6571)
        if (1 & (xEQ (BYTE0 (mOPMODE + 1 * COREHALFWORD (mOPTYPE)), 1)))
          // ITYPE = RI; (6572)
          {
            descriptor_t *bitRHS = getBIT (16, mGENERATExRI);
            putBIT (16, mGENERATExAVAILABLE_FROM_STORAGExITYPE, bitRHS);
            bitRHS->inUse = 0;
          }
      // IF MODE_MOD(OPMODE(OPTYPE)+ITYPE) THEN (6573)
      if (1
          & (bitToFixed (getBIT (
              16,
              mGENERATExMODE_MOD
                  + 2
                        * xadd (
                            BYTE0 (mOPMODE + 1 * COREHALFWORD (mOPTYPE)),
                            COREHALFWORD (
                                mGENERATExAVAILABLE_FROM_STORAGExITYPE))))))
        // RETURN FALSE; (6574)
        {
          reentryGuard = 0;
          return 0;
        }
      // IF ~OPERATOR(MAKE_INST(OPCODE, OPTYPE, ITYPE)) THEN (6575)
      if (1
          & (xNOT (BYTE0 (
              mOPERATOR
              + 1
                    * bitToFixed (
                        (putBITp (
                             16, mGENERATExMAKE_INSTxOPCODE,
                             getBIT (16,
                                     mGENERATExAVAILABLE_FROM_STORAGExOPCODE)),
                         putBITp (16, mGENERATExMAKE_INSTxOPTYPE,
                                  getBIT (16, mOPTYPE)),
                         putBITp (
                             16, mGENERATExMAKE_INSTxOPFORM,
                             getBIT (16,
                                     mGENERATExAVAILABLE_FROM_STORAGExITYPE)),
                         GENERATExMAKE_INST (0)))))))
        // RETURN FALSE; (6576)
        {
          reentryGuard = 0;
          return 0;
        }
      // IF BIT_PICK(OP) ~= 0 THEN (6577)
      if (1
          & (xNEQ ((putBITp (16, mGENERATExBIT_PICKxOP,
                             getBIT (16, mGENERATExAVAILABLE_FROM_STORAGExOP)),
                    GENERATExBIT_PICK (0)),
                   0)))
        // RETURN FALSE; (6578)
        {
          reentryGuard = 0;
          return 0;
        }
      // IF (CONST(OP) | DEL(OP)) ~= 0 THEN (6579)
      if (1
          & (xNEQ (
              xOR (
                  getFIXED (getFIXED (mIND_STACK)
                            + 73
                                  * (COREHALFWORD (
                                      mGENERATExAVAILABLE_FROM_STORAGExOP))
                            + 0 + 4 * (0)),
                  COREHALFWORD (getFIXED (mIND_STACK)
                                + 73
                                      * (COREHALFWORD (
                                          mGENERATExAVAILABLE_FROM_STORAGExOP))
                                + 28 + 2 * (0))),
              0)))
        // RETURN 2; (6580)
        {
          reentryGuard = 0;
          return 2;
        }
      // IF (FORM(OP)=SYM) THEN (6581)
      if (1
          & (xEQ (
              COREHALFWORD (
                  getFIXED (mIND_STACK)
                  + 73 * (COREHALFWORD (mGENERATExAVAILABLE_FROM_STORAGExOP))
                  + 32 + 2 * (0)),
              BYTE0 (mSYM))))
        // IF (SYT_BASE(LOC(OP))=REMOTE_BASE | NR_DEREF(OP)) &  (INX(OP)=0)
        // THEN (6582)
        if (1
            & (xAND (
                xOR (
                    xEQ (
                        COREHALFWORD (
                            getFIXED (mP2SYMS)
                            + 12
                                  * (COREHALFWORD (
                                      getFIXED (mIND_STACK)
                                      + 73
                                            * (COREHALFWORD (
                                                mGENERATExAVAILABLE_FROM_STORAGExOP))
                                      + 40 + 2 * (0)))
                            + 4 + 2 * (0)),
                        BYTE0 (mREMOTE_BASE)),
                    BYTE0 (getFIXED (mIND_STACK)
                           + 73
                                 * (COREHALFWORD (
                                     mGENERATExAVAILABLE_FROM_STORAGExOP))
                           + 60 + 1 * (0))),
                xEQ (COREHALFWORD (
                         getFIXED (mIND_STACK)
                         + 73
                               * (COREHALFWORD (
                                   mGENERATExAVAILABLE_FROM_STORAGExOP))
                         + 34 + 2 * (0)),
                     0))))
          // DO; (6583)
          {
          rs1s1:;
            // TEMP2 = TARGET_REGISTER; (6584)
            {
              descriptor_t *bitRHS = getBIT (16, mTARGET_REGISTER);
              int32_t numberRHS;
              numberRHS = bitToFixed (bitRHS);
              putFIXED (mTEMP2, numberRHS);
              bitRHS->inUse = 0;
            }
            // TARGET_REGISTER = -1; (6585)
            {
              int32_t numberRHS = (int32_t)(-1);
              descriptor_t *bitRHS;
              bitRHS = fixedToBit (32, (int32_t)(numberRHS));
              putBIT (16, mTARGET_REGISTER, bitRHS);
              bitRHS->inUse = 0;
            }
            // CALL SUBSCRIPT_RANGE_CHECK(OP,1); (6586)
            {
              putBITp (16, mGENERATExSUBSCRIPT_RANGE_CHECKxOP,
                       getBIT (16, mGENERATExAVAILABLE_FROM_STORAGExOP));
              putBITp (1, mGENERATExSUBSCRIPT_RANGE_CHECKxNEED_INDEXING,
                       fixedToBit (32, (int32_t)(1)));
              GENERATExSUBSCRIPT_RANGE_CHECK (0);
            }
            // TARGET_REGISTER = TEMP2; (6587)
            {
              int32_t numberRHS = (int32_t)(getFIXED (mTEMP2));
              descriptor_t *bitRHS;
              bitRHS = fixedToBit (32, (int32_t)(numberRHS));
              putBIT (16, mTARGET_REGISTER, bitRHS);
              bitRHS->inUse = 0;
            }
          es1s1:;
          } // End of DO block
      // RETURN TRUE; (6588)
      {
        reentryGuard = 0;
        return 1;
      }
    es1:;
    } // End of DO block
  // RETURN FALSE; (6589)
  {
    reentryGuard = 0;
    return 0;
  }
}
