/*
  File GENERATExCHECK_SRCE.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
GENERATExCHECK_SRCE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExCHECK_SRCE");
  // OK_TO_ASSIGN=FALSE; (8819)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExOK_TO_ASSIGN, bitRHS);
    bitRHS->inUse = 0;
  }
  // LEFTOPER=MOD_GET_OPERAND(1); (8820)
  {
    descriptor_t *bitRHS = (putBITp (16, mGENERATExMOD_GET_OPERANDxOP,
                                     fixedToBit (32, (int32_t)(1))),
                            GENERATExMOD_GET_OPERAND (0));
    putBIT (16, mGENERATExCHECK_SRCExLEFTOPER, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF VAC_FLAG THEN (8821)
  if (1 & (bitToFixed (getBIT (1, mGENERATExVAC_FLAG))))
    // NAME_OP_FLAG=FALSE; (8822)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mGENERATExNAME_OP_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (8823)
  else
    // CALL RETURN_STACK_ENTRY(LEFTOPER); (8824)
    {
      putBITp (16, mGENERATExRETURN_STACK_ENTRYxP,
               getBIT (16, mGENERATExCHECK_SRCExLEFTOPER));
      GENERATExRETURN_STACK_ENTRY (0);
    }
  // IF (~NAME_OP_FLAG & ~SUBSTRUCT_FLAG) THEN (8825)
  if (1
      & (xAND (xNOT (BYTE0 (mGENERATExNAME_OP_FLAG)),
               xNOT (BYTE0 (mGENERATExSUBSTRUCT_FLAG)))))
    // DO; (8826)
    {
    rs1:;
      // IF NUMOP=2 THEN (8827)
      if (1 & (xEQ (COREHALFWORD (mNUMOP), 2)))
        // DO; (8828)
        {
        rs1s1:;
          // RIGHTOPER=MOD_GET_OPERAND(2); (8829)
          {
            descriptor_t *bitRHS = (putBITp (16, mGENERATExMOD_GET_OPERANDxOP,
                                             fixedToBit (32, (int32_t)(2))),
                                    GENERATExMOD_GET_OPERAND (0));
            putBIT (16, mGENERATExCHECK_SRCExRIGHTOPER, bitRHS);
            bitRHS->inUse = 0;
          }
          // IF VAC_FLAG THEN (8830)
          if (1 & (bitToFixed (getBIT (1, mGENERATExVAC_FLAG))))
            // NAME_OP_FLAG=FALSE; (8831)
            {
              int32_t numberRHS = (int32_t)(0);
              descriptor_t *bitRHS;
              bitRHS = fixedToBit (32, (int32_t)(numberRHS));
              putBIT (1, mGENERATExNAME_OP_FLAG, bitRHS);
              bitRHS->inUse = 0;
            }
          // ELSE (8832)
          else
            // CALL RETURN_STACK_ENTRY(RIGHTOPER); (8833)
            {
              putBITp (16, mGENERATExRETURN_STACK_ENTRYxP,
                       getBIT (16, mGENERATExCHECK_SRCExRIGHTOPER));
              GENERATExRETURN_STACK_ENTRY (0);
            }
          // IF (~NAME_OP_FLAG & ~SUBSTRUCT_FLAG) THEN (8834)
          if (1
              & (xAND (xNOT (BYTE0 (mGENERATExNAME_OP_FLAG)),
                       xNOT (BYTE0 (mGENERATExSUBSTRUCT_FLAG)))))
            // IF RTYPE=STMT_PREC THEN (8835)
            if (1
                & (xEQ (COREHALFWORD (mGENERATExRTYPE),
                        COREHALFWORD (mGENERATExSTMT_PREC))))
              // OK_TO_ASSIGN=TRUE; (8836)
              {
                int32_t numberRHS = (int32_t)(1);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mGENERATExOK_TO_ASSIGN, bitRHS);
                bitRHS->inUse = 0;
              }
        es1s1:;
        } // End of DO block
      // ELSE (8837)
      else
        // IF RTYPE=STMT_PREC THEN (8838)
        if (1
            & (xEQ (COREHALFWORD (mGENERATExRTYPE),
                    COREHALFWORD (mGENERATExSTMT_PREC))))
          // OK_TO_ASSIGN=TRUE; (8839)
          {
            int32_t numberRHS = (int32_t)(1);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (1, mGENERATExOK_TO_ASSIGN, bitRHS);
            bitRHS->inUse = 0;
          }
    es1:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
