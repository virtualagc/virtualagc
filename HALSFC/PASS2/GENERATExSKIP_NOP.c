/*
  File GENERATExSKIP_NOP.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

descriptor_t *
GENERATExSKIP_NOP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExSKIP_NOP");
  // DO WHILE NEXTPOPCODE(PTR) <= XEXTN; (6531)
  while (1
         & (xLE ((putBITp (16, mGENERATExNEXTPOPCODExCTR,
                           getBIT (16, mGENERATExSKIP_NOPxPTR)),
                  GENERATExNEXTPOPCODE (0)),
                 COREHALFWORD (mXEXTN))))
    {
      // PTR = NEXTCTR; (6532)
      {
        descriptor_t *bitRHS = getBIT (16, mNEXTCTR);
        putBIT (16, mGENERATExSKIP_NOPxPTR, bitRHS);
        bitRHS->inUse = 0;
      }
    } // End of DO WHILE block
  // RETURN NEXTCTR; (6533)
  {
    reentryGuard = 0;
    return getBIT (16, mNEXTCTR);
  }
}
