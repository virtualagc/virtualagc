/*
  File PROGNAME.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

descriptor_t *
PROGNAME (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PROGNAME");
  // TEMPSTRING=SYT_NAME(ENTRY); (1427)
  {
    descriptor_t *stringRHS;
    stringRHS
        = getCHARACTER (getFIXED (mSYM_TAB)
                        + 34 * (COREHALFWORD (mPROGNAMExENTRY)) + 0 + 4 * (0));
    putCHARACTER (mPROGNAMExTEMPSTRING, stringRHS);
    stringRHS->inUse = 0;
  }
  // WORK1=CHAR_INDEX(TEMPSTRING,'_'); (1428)
  {
    descriptor_t *bitRHS
        = (putCHARACTERp (mCHAR_INDEXxSTRING1,
                          getCHARACTER (mPROGNAMExTEMPSTRING)),
           putCHARACTERp (mCHAR_INDEXxSTRING2, cToDescriptor (NULL, "_")),
           CHAR_INDEX (0));
    int32_t numberRHS;
    numberRHS = bitToFixed (bitRHS);
    putFIXED (mWORK1, numberRHS);
    bitRHS->inUse = 0;
  }
  // DO WHILE WORK1>0; (1429)
  while (1 & (xGT (getFIXED (mWORK1), 0)))
    {
      // TEMPSTRING=SUBSTR(TEMPSTRING,0,WORK1)||SUBSTR(TEMPSTRING,WORK1+1);
      // (1430)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            SUBSTR (getCHARACTER (mPROGNAMExTEMPSTRING), 0, getFIXED (mWORK1)),
            SUBSTR2 (getCHARACTER (mPROGNAMExTEMPSTRING),
                     xadd (getFIXED (mWORK1), 1)));
        putCHARACTER (mPROGNAMExTEMPSTRING, stringRHS);
        stringRHS->inUse = 0;
      }
      // WORK1=CHAR_INDEX(TEMPSTRING,'_'); (1431)
      {
        descriptor_t *bitRHS
            = (putCHARACTERp (mCHAR_INDEXxSTRING1,
                              getCHARACTER (mPROGNAMExTEMPSTRING)),
               putCHARACTERp (mCHAR_INDEXxSTRING2, cToDescriptor (NULL, "_")),
               CHAR_INDEX (0));
        int32_t numberRHS;
        numberRHS = bitToFixed (bitRHS);
        putFIXED (mWORK1, numberRHS);
        bitRHS->inUse = 0;
      }
    } // End of DO WHILE block
  // IX1=LENGTH(TEMPSTRING); (1432)
  {
    int32_t numberRHS
        = (int32_t)(LENGTH (getCHARACTER (mPROGNAMExTEMPSTRING)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mIX1, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF IX1 > 6 THEN (1433)
  if (1 & (xGT (COREHALFWORD (mIX1), 6)))
    // IX1 = 6; (1434)
    {
      int32_t numberRHS = (int32_t)(6);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mIX1, bitRHS);
      bitRHS->inUse = 0;
    }
  // TEMPSTRING = SUBSTR(TEMPSTRING, 0, IX1); (1435)
  {
    descriptor_t *stringRHS;
    stringRHS
        = SUBSTR (getCHARACTER (mPROGNAMExTEMPSTRING), 0, COREHALFWORD (mIX1));
    putCHARACTER (mPROGNAMExTEMPSTRING, stringRHS);
    stringRHS->inUse = 0;
  }
  // IF (SYT_FLAGS2(ENTRY) & NONHAL_FLAG) ~= 0 THEN (1436)
  if (1
      & (xNEQ (
          xAND (BYTE0 (getFIXED (mSYM_TAB)
                       + 34 * (COREHALFWORD (mPROGNAMExENTRY)) + 33 + 1 * (0)),
                BYTE0 (mNONHAL_FLAG)),
          0)))
    // RETURN TEMPSTRING; (1437)
    {
      reentryGuard = 0;
      return getCHARACTER (mPROGNAMExTEMPSTRING);
    }
  // IF TASK = 0 THEN (1438)
  if (1 & (xEQ (COREHALFWORD (mPROGNAMExTASK), 0)))
    // IF FLAG = 1 & PROGPOINT = 0 & Z_LINKAGE THEN (1439)
    if (1
        & (xAND (xAND (xEQ (COREHALFWORD (mPROGNAMExFLAG), 1),
                       xEQ (COREHALFWORD (mPROGPOINT), 0)),
                 BYTE0 (mZ_LINKAGE))))
      // RETURN NAMETYPE(8) || TEMPSTRING; (1440)
      {
        reentryGuard = 0;
        return xsCAT (getCHARACTER (mPROGNAMExNAMETYPE + 4 * 8),
                      getCHARACTER (mPROGNAMExTEMPSTRING));
      }
    // ELSE (1441)
    else
      // RETURN NAMETYPE(FLAG) || TEMPSTRING; (1442)
      {
        reentryGuard = 0;
        return xsCAT (getCHARACTER (mPROGNAMExNAMETYPE
                                    + 4 * COREHALFWORD (mPROGNAMExFLAG)),
                      getCHARACTER (mPROGNAMExTEMPSTRING));
      }
  // IF FLAG = 7 THEN (1443)
  if (1 & (xEQ (COREHALFWORD (mPROGNAMExFLAG), 7)))
    // DO; (1444)
    {
    rs2:;
      // TASK = TASK - PROGPOINT; (1445)
      {
        int32_t numberRHS = (int32_t)(xsubtract (COREHALFWORD (mPROGNAMExTASK),
                                                 COREHALFWORD (mPROGPOINT)));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mPROGNAMExTASK, bitRHS);
        bitRHS->inUse = 0;
      }
      // IX1 = TASK / 10; (1446)
      {
        int32_t numberRHS
            = (int32_t)(xdivide (COREHALFWORD (mPROGNAMExTASK), 10));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mIX1, bitRHS);
        bitRHS->inUse = 0;
      }
      // IX2 = TASK MOD 10; (1447)
      {
        int32_t numberRHS
            = (int32_t)(xmod (COREHALFWORD (mPROGNAMExTASK), 10));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mIX2, bitRHS);
        bitRHS->inUse = 0;
      }
      // TASK = 0; (1448)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mPROGNAMExTASK, bitRHS);
        bitRHS->inUse = 0;
      }
      // RETURN SUBSTR(ALPHSEQ, IX1, 1) || SUBSTR(NUMSEQ, IX2, 1) ||
      // TEMPSTRING; (1449)
      {
        reentryGuard = 0;
        return xsCAT (xsCAT (SUBSTR (getCHARACTER (mPROGNAMExALPHSEQ),
                                     COREHALFWORD (mIX1), 1),
                             SUBSTR (getCHARACTER (mPROGNAMExNUMSEQ),
                                     COREHALFWORD (mIX2), 1)),
                      getCHARACTER (mPROGNAMExTEMPSTRING));
      }
    es2:;
    } // End of DO block
  // TEMPSTRING = SUBSTR(NAMETYPE(FLAG),0,1) || SUBSTR(NUMSEQ, TASK, 1) ||
  // TEMPSTRING; (1450)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (
        xsCAT (SUBSTR (getCHARACTER (mPROGNAMExNAMETYPE
                                     + 4 * COREHALFWORD (mPROGNAMExFLAG)),
                       0, 1),
               SUBSTR (getCHARACTER (mPROGNAMExNUMSEQ),
                       COREHALFWORD (mPROGNAMExTASK), 1)),
        getCHARACTER (mPROGNAMExTEMPSTRING));
    putCHARACTER (mPROGNAMExTEMPSTRING, stringRHS);
    stringRHS->inUse = 0;
  }
  // TASK = 0; (1451)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPROGNAMExTASK, bitRHS);
    bitRHS->inUse = 0;
  }
  // RETURN TEMPSTRING; (1452)
  {
    reentryGuard = 0;
    return getCHARACTER (mPROGNAMExTEMPSTRING);
  }
}
