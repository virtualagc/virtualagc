/*
  File OBJECT_CONDENSERxBRANCH_CONDENSE.c generated by XCOM-I, 2024-08-08
  04:32:26.
*/

#include "runtimeC.h"

int32_t
OBJECT_CONDENSERxBRANCH_CONDENSE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard
      = guardReentry (reentryGuard, "OBJECT_CONDENSERxBRANCH_CONDENSE");
  // CONDENSED = FALSE; (14965)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mOBJECT_CONDENSERxCONDENSED, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO UNTIL STABLE; (14966)
  do
    {
      // STABLE = TRUE; (14967)
      {
        int32_t numberRHS = (int32_t)(1);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mOBJECT_CONDENSERxBRANCH_CONDENSExSTABLE, bitRHS);
        bitRHS->inUse = 0;
      }
      // BRANCH_NUM = FIRSTBRANCH(CURRENT_ESDID); (14968)
      {
        descriptor_t *bitRHS
            = getBIT (16, mFIRSTBRANCH + 2 * getFIXED (mCURRENT_ESDID));
        putBIT (16, mOBJECT_CONDENSERxBRANCH_NUM, bitRHS);
        bitRHS->inUse = 0;
      }
      // DO WHILE BRANCH_NUM > 0; (14969)
      while (1 & (xGT (COREHALFWORD (mOBJECT_CONDENSERxBRANCH_NUM), 0)))
        {
          // DEST = BRANCH_ADDR(BRANCH_NUM) + 1 -
          // LOCATION(BRANCH_TARG(BRANCH_NUM)); (14970)
          {
            int32_t numberRHS = (int32_t)(xsubtract (
                xadd (getFIXED (
                          getFIXED (mBRANCH_TBL)
                          + 14 * (COREHALFWORD (mOBJECT_CONDENSERxBRANCH_NUM))
                          + 4 + 4 * (0)),
                      1),
                getFIXED (getFIXED (mLAB_LOC)
                          + 8
                                * (COREHALFWORD (
                                    getFIXED (mBRANCH_TBL)
                                    + 14
                                          * (COREHALFWORD (
                                              mOBJECT_CONDENSERxBRANCH_NUM))
                                    + 8 + 2 * (0)))
                          + 0 + 4 * (0))));
            putFIXED (mOBJECT_CONDENSERxBRANCH_CONDENSExDEST, numberRHS);
          }
          // IF DEST <= 55 & DEST >= -56 THEN (14971)
          if (1
              & (xAND (
                  xLE (getFIXED (mOBJECT_CONDENSERxBRANCH_CONDENSExDEST), 55),
                  xGE (getFIXED (mOBJECT_CONDENSERxBRANCH_CONDENSExDEST),
                       -56))))
            // DO; (14972)
            {
            rs1s1s1:;
              // CALL CONDENSE_A_BRANCH(BRANCH_NUM); (14973)
              {
                putBITp (16, mOBJECT_CONDENSERxCONDENSE_A_BRANCHxBRANCH_COND,
                         getBIT (16, mOBJECT_CONDENSERxBRANCH_NUM));
                OBJECT_CONDENSERxCONDENSE_A_BRANCH (0);
              }
              // STABLE = FALSE; (14974)
              {
                int32_t numberRHS = (int32_t)(0);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mOBJECT_CONDENSERxBRANCH_CONDENSExSTABLE, bitRHS);
                bitRHS->inUse = 0;
              }
            es1s1s1:;
            } // End of DO block
          // BRANCH_NUM = BRANCH_FLINK(BRANCH_NUM); (14975)
          {
            descriptor_t *bitRHS = getBIT (
                16, getFIXED (mBRANCH_TBL)
                        + 14 * (COREHALFWORD (mOBJECT_CONDENSERxBRANCH_NUM))
                        + 10 + 2 * (0));
            putBIT (16, mOBJECT_CONDENSERxBRANCH_NUM, bitRHS);
            bitRHS->inUse = 0;
          }
        } // End of DO WHILE block
    }
  while (!(1
           & (bitToFixed (getBIT (
               1, mOBJECT_CONDENSERxBRANCH_CONDENSExSTABLE))))); // End of DO
                                                                 // UNTIL block
  // BRANCH_NUM = 0; (14976)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOBJECT_CONDENSERxBRANCH_NUM, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
