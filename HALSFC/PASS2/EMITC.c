/*
  File EMITC.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
EMITC (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "EMITC");
  // CODE(GET_CODE(CODE_LINE)) = SHL(TYPE, 16) | INST &  65535; (1089)
  {
    int32_t numberRHS
        = (int32_t)(xOR (SHL (COREHALFWORD (mEMITCxTYPE), 16),
                         xAND (COREHALFWORD (mEMITCxINST), 65535)));
    putFIXED (mCODE
                  + 4
                        * ((putFIXED (mGET_CODExCTR, getFIXED (mCODE_LINE)),
                            GET_CODE (0))),
              numberRHS);
  }
  // IF BINARY_CODE THEN (1090)
  if (1 & (bitToFixed (getBIT (1, mBINARY_CODE))))
    // DO; (1091)
    {
    rs1:;
      // MESSAGE = HEX(INST& 65535, 4); (1092)
      {
        descriptor_t *stringRHS;
        stringRHS
            = (putFIXED (mHEXxHVAL, xAND (COREHALFWORD (mEMITCxINST), 65535)),
               putFIXED (mHEXxN, 4), HEX (0));
        putCHARACTER (mMESSAGE, stringRHS);
        stringRHS->inUse = 0;
      }
      // MESSAGE = FORMAT(TYPE, 6) || COMMA || X3 || MESSAGE; (1093)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            xsCAT (xsCAT ((putFIXED (mFORMATxIVAL, COREHALFWORD (mEMITCxTYPE)),
                           putFIXED (mFORMATxN, 6), FORMAT (0)),
                          getCHARACTER (mCOMMA)),
                   getCHARACTER (mX3)),
            getCHARACTER (mMESSAGE));
        putCHARACTER (mMESSAGE, stringRHS);
        stringRHS->inUse = 0;
      }
      // OUTPUT = HEX_LOCCTR || MESSAGE; (1094)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (HEX_LOCCTR (0), getCHARACTER (mMESSAGE));
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // IF NOT_MODIFIER(TYPE) THEN (1095)
  if (1
      & (bitToFixed (
          getBIT (8, mNOT_MODIFIER + 1 * COREHALFWORD (mEMITCxTYPE)))))
    // DO; (1096)
    {
    rs2:;
      // LOCCTR(INDEXNEST) = LOCCTR(INDEXNEST) + 1; (1097)
      {
        int32_t numberRHS = (int32_t)(xadd (
            getFIXED (mLOCCTR + 4 * COREHALFWORD (mINDEXNEST)), 1));
        putFIXED (mLOCCTR + 4 * (COREHALFWORD (mINDEXNEST)), numberRHS);
      }
      // STOPPERFLAG = FALSE; (1098)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mSTOPPERFLAG, bitRHS);
        bitRHS->inUse = 0;
      }
    es2:;
    } // End of DO block
  // CODE_LINE = CODE_LINE + 1; (1099)
  {
    int32_t numberRHS = (int32_t)(xadd (getFIXED (mCODE_LINE), 1));
    putFIXED (mCODE_LINE, numberRHS);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
