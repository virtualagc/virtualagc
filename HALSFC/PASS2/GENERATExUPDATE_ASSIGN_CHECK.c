/*
  File GENERATExUPDATE_ASSIGN_CHECK.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
GENERATExUPDATE_ASSIGN_CHECK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExUPDATE_ASSIGN_CHECK");
  // IF UPDATING > 0 THEN (6056)
  if (1 & (xGT (COREHALFWORD (mUPDATING), 0)))
    // IF SYMFORM(FORM(OP)) THEN (6057)
    if (1
        & (bitToFixed (getBIT (
            1, mSYMFORM
                   + 1
                         * COREHALFWORD (
                             getFIXED (mIND_STACK)
                             + 73
                                   * (COREHALFWORD (
                                       mGENERATExUPDATE_ASSIGN_CHECKxOP))
                             + 32 + 2 * (0))))))
      // IF (SYT_FLAGS(LOC(OP)) & LOCK_BITS) ~= 0 THEN (6058)
      if (1
          & (xNEQ (
              xAND (getFIXED (
                        getFIXED (mSYM_TAB)
                        + 34
                              * (COREHALFWORD (
                                  getFIXED (mIND_STACK)
                                  + 73
                                        * (COREHALFWORD (
                                            mGENERATExUPDATE_ASSIGN_CHECKxOP))
                                  + 40 + 2 * (0)))
                        + 8 + 4 * (0)),
                    getFIXED (mLOCK_BITS)),
              0)))
        // UPDATE_FLAGS = UPDATE_FLAGS | NEGMAX; (6059)
        {
          int32_t numberRHS = (int32_t)(xOR (
              getFIXED (getFIXED (mP2SYMS) + 12 * (COREHALFWORD (mUPDATING))
                        + 0 + 4 * (0)),
              getFIXED (mNEGMAX)));
          putFIXED (getFIXED (mP2SYMS) + 12 * (COREHALFWORD (mUPDATING)) + 0
                        + 4 * (0),
                    numberRHS);
        }
  {
    reentryGuard = 0;
    return 0;
  }
}
