/*
  File GENERATExMAKE_BIT_LIT.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

descriptor_t *
GENERATExMAKE_BIT_LIT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExMAKE_BIT_LIT");
  // PTR = GET_STACK_ENTRY; (5433)
  {
    int32_t numberRHS = (int32_t)(GENERATExGET_STACK_ENTRY (0));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mGENERATExMAKE_BIT_LITxPTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // TYPE(PTR) = TYPE(OP); (5434)
  {
    descriptor_t *bitRHS
        = getBIT (16, getFIXED (mIND_STACK)
                          + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxOP))
                          + 50 + 2 * (0));
    putBIT (16,
            getFIXED (mIND_STACK)
                + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxPTR)) + 50
                + 2 * (0),
            bitRHS);
    bitRHS->inUse = 0;
  }
  // SIZE(PTR) = SIZE(OP); (5435)
  {
    descriptor_t *bitRHS
        = getBIT (16, getFIXED (mIND_STACK)
                          + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxOP))
                          + 48 + 2 * (0));
    putBIT (16,
            getFIXED (mIND_STACK)
                + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxPTR)) + 48
                + 2 * (0),
            bitRHS);
    bitRHS->inUse = 0;
  }
  // FORM(PTR) = LIT; (5436)
  {
    descriptor_t *bitRHS = getBIT (8, mLIT);
    putBIT (16,
            getFIXED (mIND_STACK)
                + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxPTR)) + 32
                + 2 * (0),
            bitRHS);
    bitRHS->inUse = 0;
  }
  // LOC(PTR) = -1; (5437)
  {
    int32_t numberRHS = (int32_t)(-1);
    putBIT (16,
            getFIXED (mIND_STACK)
                + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxPTR)) + 40
                + 2 * (0),
            fixedToBit (16, numberRHS));
  }
  // IF COLUMN(OP) = 0 THEN (5438)
  if (1
      & (xEQ (COREHALFWORD (getFIXED (mIND_STACK)
                            + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxOP))
                            + 24 + 2 * (0)),
              0)))
    // VAL(PTR) = VALUE; (5439)
    {
      int32_t numberRHS = (int32_t)(getFIXED (mGENERATExMAKE_BIT_LITxVALUE));
      putFIXED (getFIXED (mIND_STACK)
                    + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxPTR)) + 12
                    + 4 * (0),
                numberRHS);
    }
  // ELSE (5440)
  else
    // IF OPC = AND THEN (5441)
    if (1
        & (xEQ (COREHALFWORD (mGENERATExMAKE_BIT_LITxOPC),
                COREHALFWORD (mGENERATExAND))))
      // DO; (5442)
      {
      rs1:;
        // VAL(PTR) = ~VALUE & XITAB(SIZE(PTR)); (5443)
        {
          int32_t numberRHS = (int32_t)(xAND (
              xNOT (getFIXED (mGENERATExMAKE_BIT_LITxVALUE)),
              getFIXED (
                  mGENERATExXITAB
                  + 4
                        * COREHALFWORD (
                            getFIXED (mIND_STACK)
                            + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxPTR))
                            + 48 + 2 * (0)))));
          putFIXED (getFIXED (mIND_STACK)
                        + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxPTR)) + 12
                        + 4 * (0),
                    numberRHS);
        }
        // CALL BIT_SHIFT(SLL, PTR, COLUMN(OP)); (5444)
        {
          putBITp (16, mGENERATExBIT_SHIFTxOPCODE, getBIT (8, mSLL));
          putBITp (16, mGENERATExBIT_SHIFTxR,
                   getBIT (16, mGENERATExMAKE_BIT_LITxPTR));
          putBITp (
              16, mGENERATExBIT_SHIFTxOP,
              getBIT (16, getFIXED (mIND_STACK)
                              + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxOP))
                              + 24 + 2 * (0)));
          GENERATExBIT_SHIFT (0);
        }
        // VAL(PTR) = ~VAL(PTR); (5445)
        {
          int32_t numberRHS = (int32_t)(xNOT (
              getFIXED (getFIXED (mIND_STACK)
                        + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxPTR)) + 12
                        + 4 * (0))));
          putFIXED (getFIXED (mIND_STACK)
                        + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxPTR)) + 12
                        + 4 * (0),
                    numberRHS);
        }
      es1:;
      } // End of DO block
    // ELSE (5446)
    else
      // DO; (5447)
      {
      rs2:;
        // VAL(PTR) = VALUE; (5448)
        {
          int32_t numberRHS
              = (int32_t)(getFIXED (mGENERATExMAKE_BIT_LITxVALUE));
          putFIXED (getFIXED (mIND_STACK)
                        + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxPTR)) + 12
                        + 4 * (0),
                    numberRHS);
        }
        // CALL BIT_SHIFT(SLL, PTR, COLUMN(OP)); (5449)
        {
          putBITp (16, mGENERATExBIT_SHIFTxOPCODE, getBIT (8, mSLL));
          putBITp (16, mGENERATExBIT_SHIFTxR,
                   getBIT (16, mGENERATExMAKE_BIT_LITxPTR));
          putBITp (
              16, mGENERATExBIT_SHIFTxOP,
              getBIT (16, getFIXED (mIND_STACK)
                              + 73 * (COREHALFWORD (mGENERATExMAKE_BIT_LITxOP))
                              + 24 + 2 * (0)));
          GENERATExBIT_SHIFT (0);
        }
      es2:;
      } // End of DO block
  // RETURN PTR; (5450)
  {
    reentryGuard = 0;
    return getBIT (16, mGENERATExMAKE_BIT_LITxPTR);
  }
}
