/*
  File GENERATExGEN_CLASS0xINIT_TEMPORARY.c generated by XCOM-I, 2024-08-08
  04:32:26.
*/

#include "runtimeC.h"

int32_t
GENERATExGEN_CLASS0xINIT_TEMPORARY (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard
      = guardReentry (reentryGuard, "GENERATExGEN_CLASS0xINIT_TEMPORARY");
  // DOTEMP(DOLEVEL) = 0; (12196)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mGENERATExDOTEMP + 2 * (COREHALFWORD (mDOLEVEL)), bitRHS);
    bitRHS->inUse = 0;
  }
  // DOTEMPBLK(DOLEVEL) = CURCBLK; (12197)
  {
    descriptor_t *bitRHS = getBIT (8, mCURCBLK);
    putBIT (16, mGENERATExDOTEMPBLK + 2 * (COREHALFWORD (mDOLEVEL)), bitRHS);
    bitRHS->inUse = 0;
  }
  // DOTEMPCTR(DOLEVEL) = CTR; (12198)
  {
    descriptor_t *bitRHS = getBIT (16, mCTR);
    putBIT (16, mGENERATExDOTEMPCTR + 2 * (COREHALFWORD (mDOLEVEL)), bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
