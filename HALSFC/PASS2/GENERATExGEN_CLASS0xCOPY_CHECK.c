/*
  File GENERATExGEN_CLASS0xCOPY_CHECK.c generated by XCOM-I, 2024-08-08
  04:32:26.
*/

#include "runtimeC.h"

descriptor_t *
GENERATExGEN_CLASS0xCOPY_CHECK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExGEN_CLASS0xCOPY_CHECK");
  // IF ~KNOWN_SYM(CHECKEE) THEN (12519)
  if (1
      & (xNOT (bitToFixed (
          (putBITp (16, mGENERATExKNOWN_SYMxOP,
                    getBIT (16, mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE)),
           GENERATExKNOWN_SYM (0))))))
    // RETURN NAME_VAR_DEREF; (12520)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(105));
    }
  // IF DIAGNOSTICS THEN (12521)
  if (1 & (bitToFixed (getBIT (1, mDIAGNOSTICS))))
    // OUTPUT = '%COPY_CHECK FOR A KNOWN SYMBOL '; (12522)
    {
      descriptor_t *stringRHS;
      stringRHS = cToDescriptor (NULL, "%COPY_CHECK FOR A KNOWN SYMBOL ");
      OUTPUT (0, stringRHS);
      stringRHS->inUse = 0;
    }
  // FLAGS = SYT_FLAGS(LOC(CHECKEE)); (12523)
  {
    int32_t numberRHS = (int32_t)(getFIXED (
        getFIXED (mSYM_TAB)
        + 34
              * (COREHALFWORD (
                  getFIXED (mIND_STACK)
                  + 73
                        * (COREHALFWORD (
                            mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
                  + 40 + 2 * (0)))
        + 8 + 4 * (0)));
    putFIXED (mGENERATExGEN_CLASS0xCOPY_CHECKxFLAGS, numberRHS);
  }
  // IF SYT_TYPE(LOC(CHECKEE)) = STRUCTURE THEN (12524)
  if (1
      & (xEQ (
          BYTE0 (getFIXED (mSYM_TAB)
                 + 34
                       * (COREHALFWORD (
                           getFIXED (mIND_STACK)
                           + 73
                                 * (COREHALFWORD (
                                     mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
                           + 40 + 2 * (0)))
                 + 32 + 1 * (0)),
          BYTE0 (mSTRUCTURE))))
    // IF ~MAJOR_STRUCTURE(CHECKEE) THEN (12525)
    if (1
        & (xNOT (bitToFixed (
            (putBITp (16, mGENERATExMAJOR_STRUCTURExOP,
                      getBIT (16, mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE)),
             GENERATExMAJOR_STRUCTURE (0))))))
      // IF (SYT_FLAGS(LOC2(CHECKEE)) & NAME_FLAG) ~= 0 THEN (12526)
      if (1
          & (xNEQ (
              xAND (
                  getFIXED (
                      getFIXED (mSYM_TAB)
                      + 34
                            * (COREHALFWORD (
                                getFIXED (mIND_STACK)
                                + 73
                                      * (COREHALFWORD (
                                          mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
                                + 42 + 2 * (0)))
                      + 8 + 4 * (0)),
                  getFIXED (mNAME_FLAG)),
              0)))
        // RETURN NAME_VAR_DEREF; (12527)
        {
          reentryGuard = 0;
          return fixedToBit (32, (int32_t)(105));
        }
  // IF (FLAGS & NAME_FLAG) ~= 0 THEN (12528)
  if (1
      & (xNEQ (xAND (getFIXED (mGENERATExGEN_CLASS0xCOPY_CHECKxFLAGS),
                     getFIXED (mNAME_FLAG)),
               0)))
    // RETURN NAME_VAR_DEREF; (12529)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(105));
    }
  // IF (FLAGS & POINTER_FLAG) ~= 0 THEN (12530)
  if (1
      & (xNEQ (xAND (getFIXED (mGENERATExGEN_CLASS0xCOPY_CHECKxFLAGS),
                     getFIXED (mPOINTER_FLAG)),
               0)))
    // RETURN PARAMETER_DEREF; (12531)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(108));
    }
  // LOCAL = (LOC(CHECKEE) >= SELFNAMELOC) & ((FLAGS & REMOTE_FLAG) = 0);
  // (12532)
  {
    int32_t numberRHS = (int32_t)(xAND (
        xGE (
            COREHALFWORD (
                getFIXED (mIND_STACK)
                + 73 * (COREHALFWORD (mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
                + 40 + 2 * (0)),
            COREHALFWORD (mSELFNAMELOC)),
        xEQ (xAND (getFIXED (mGENERATExGEN_CLASS0xCOPY_CHECKxFLAGS),
                   getFIXED (mREMOTE_FLAG)),
             0)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mGENERATExGEN_CLASS0xCOPY_CHECKxLOCAL, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF ~(LOCAL) THEN (12533)
  if (1 & (xNOT (COREHALFWORD (mGENERATExGEN_CLASS0xCOPY_CHECKxLOCAL))))
    // RETURN OKAY; (12534)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
  // SCOPE = SYT_SCOPE(LOC(CHECKEE)); (12535)
  {
    descriptor_t *bitRHS = getBIT (
        8, getFIXED (mSYM_TAB)
               + 34
                     * (COREHALFWORD (
                         getFIXED (mIND_STACK)
                         + 73
                               * (COREHALFWORD (
                                   mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
                         + 40 + 2 * (0)))
               + 29 + 1 * (0));
    putBIT (16, mGENERATExGEN_CLASS0xCOPY_CHECKxSCOPE, bitRHS);
    bitRHS->inUse = 0;
  }
  // BOUND = SYT_ADDR(LOC(CHECKEE)) + EXTENT(LOC(CHECKEE)); (12536)
  {
    int32_t numberRHS = (int32_t)(xadd (
        getFIXED (getFIXED (mSYM_TAB)
                  + 34
                        * (COREHALFWORD (
                            getFIXED (mIND_STACK)
                            + 73
                                  * (COREHALFWORD (
                                      mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
                            + 40 + 2 * (0)))
                  + 4 + 4 * (0)),
        getFIXED (getFIXED (mSYM_TAB)
                  + 34
                        * (COREHALFWORD (
                            getFIXED (mIND_STACK)
                            + 73
                                  * (COREHALFWORD (
                                      mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
                            + 40 + 2 * (0)))
                  + 12 + 4 * (0))));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mGENERATExGEN_CLASS0xCOPY_CHECKxBOUND, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF INX(CHECKEE) = 0 THEN (12537)
  if (1
      & (xEQ (
          COREHALFWORD (
              getFIXED (mIND_STACK)
              + 73 * (COREHALFWORD (mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
              + 34 + 2 * (0)),
          0)))
    // START = SYT_DISP(LOC(CHECKEE)) + INX_CON(CHECKEE) +
    // R_BASE(ABS(SYT_BASE(LOC(CHECKEE)))); (12538)
    {
      int32_t numberRHS = (int32_t)(xadd (
          xadd (
              COREHALFWORD (
                  getFIXED (mP2SYMS)
                  + 12
                        * (COREHALFWORD (
                            getFIXED (mIND_STACK)
                            + 73
                                  * (COREHALFWORD (
                                      mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
                            + 40 + 2 * (0)))
                  + 6 + 2 * (0)),
              getFIXED (getFIXED (mIND_STACK)
                        + 73
                              * (COREHALFWORD (
                                  mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
                        + 4 + 4 * (0))),
          getFIXED (
              getFIXED (mBASE_REGS)
              + 11
                    * (ABS (COREHALFWORD (
                        getFIXED (mP2SYMS)
                        + 12
                              * (COREHALFWORD (
                                  getFIXED (mIND_STACK)
                                  + 73
                                        * (COREHALFWORD (
                                            mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
                                  + 40 + 2 * (0)))
                        + 4 + 2 * (0))))
              + 0 + 4 * (0))));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mGENERATExGEN_CLASS0xCOPY_CHECKxSTART, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (12539)
  else
    // START = SYT_ADDR(LOC(CHECKEE)); (12540)
    {
      int32_t numberRHS = (int32_t)(getFIXED (
          getFIXED (mSYM_TAB)
          + 34
                * (COREHALFWORD (
                    getFIXED (mIND_STACK)
                    + 73
                          * (COREHALFWORD (
                              mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
                    + 40 + 2 * (0)))
          + 4 + 4 * (0)));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mGENERATExGEN_CLASS0xCOPY_CHECKxSTART, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF DIAGNOSTICS THEN (12541)
  if (1 & (bitToFixed (getBIT (1, mDIAGNOSTICS))))
    // DO; (12542)
    {
    rs1:;
      // OUTPUT = 'BOUND = ' || HEX(BOUND,4) || ', LOCAL = ' || LOCAL || ',
      // FLAGS = ' || HEX(FLAGS,8) || ', START = ' || HEX(START,4); (12543)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            xsCAT (
                xsCAT (
                    xsCAT (
                        xsCAT (
                            xsCAT (
                                xsCAT (
                                    cToDescriptor (NULL, "BOUND = "),
                                    (putFIXED (
                                         mHEXxHVAL,
                                         COREHALFWORD (
                                             mGENERATExGEN_CLASS0xCOPY_CHECKxBOUND)),
                                     putFIXED (mHEXxN, 4), HEX (0))),
                                cToDescriptor (NULL, ", LOCAL = ")),
                            bitToCharacter (getBIT (
                                16, mGENERATExGEN_CLASS0xCOPY_CHECKxLOCAL))),
                        cToDescriptor (NULL, ", FLAGS = ")),
                    (putFIXED (
                         mHEXxHVAL,
                         getFIXED (mGENERATExGEN_CLASS0xCOPY_CHECKxFLAGS)),
                     putFIXED (mHEXxN, 8), HEX (0))),
                cToDescriptor (NULL, ", START = ")),
            (putFIXED (mHEXxHVAL,
                       COREHALFWORD (mGENERATExGEN_CLASS0xCOPY_CHECKxSTART)),
             putFIXED (mHEXxN, 4), HEX (0)));
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
      // OUTPUT = 'CHECKEE'; (12544)
      {
        descriptor_t *stringRHS;
        stringRHS = cToDescriptor (NULL, "CHECKEE");
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
      // CALL DUMP_STACK(CHECKEE); (12545)
      {
        putBITp (16, mGENERATExDUMP_STACKxOP,
                 getBIT (16, mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE));
        GENERATExDUMP_STACK (0);
      }
      // OUTPUT = 'LENGTH'; (12546)
      {
        descriptor_t *stringRHS;
        stringRHS = cToDescriptor (NULL, "LENGTH");
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
      // CALL DUMP_STACK(LENGTH); (12547)
      {
        putBITp (16, mGENERATExDUMP_STACKxOP,
                 getBIT (16, mGENERATExGEN_CLASS0xCOPY_CHECKxLENGTH));
        GENERATExDUMP_STACK (0);
      }
    es1:;
    } // End of DO block
  // IF (START + VAL(LENGTH)) > BOUND THEN (12548)
  if (1
      & (xGT (
          xadd (COREHALFWORD (mGENERATExGEN_CLASS0xCOPY_CHECKxSTART),
                getFIXED (getFIXED (mIND_STACK)
                          + 73
                                * (COREHALFWORD (
                                    mGENERATExGEN_CLASS0xCOPY_CHECKxLENGTH))
                          + 12 + 4 * (0))),
          COREHALFWORD (mGENERATExGEN_CLASS0xCOPY_CHECKxBOUND))))
    // DO; (12549)
    {
    rs2:;
      // RETURN BOUND_EXCEEDED; (12550)
      {
        reentryGuard = 0;
        return fixedToBit (32, (int32_t)(106));
      }
    es2:;
    } // End of DO block
  // IF INX(CHECKEE) ~= 0 THEN (12551)
  if (1
      & (xNEQ (
          COREHALFWORD (
              getFIXED (mIND_STACK)
              + 73 * (COREHALFWORD (mGENERATExGEN_CLASS0xCOPY_CHECKxCHECKEE))
              + 34 + 2 * (0)),
          0)))
    // RETURN BOUND_NOT_CHECKED; (12552)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(107));
    }
  // RETURN OKAY; (12553)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(0));
  }
}
