/*
  File GENERATExOPSTAT.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
GENERATExOPSTAT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExOPSTAT");
  // DO CASE OPCC(INST); (2987)
  {
  rs1:
    switch (BYTE0 (mOPCC + 1 * COREHALFWORD (mGENERATExOPSTATxINST)))
      {
      case 0:
          // ; (2989)
          ;
        break;
      case 1:
        // CCREG = XREG; (2990)
        {
          descriptor_t *bitRHS = getBIT (16, mGENERATExOPSTATxXREG);
          putBIT (16, mCCREG, bitRHS);
          bitRHS->inUse = 0;
        }
        break;
      case 2:
        // CCREG = 0; (2991)
        {
          int32_t numberRHS = (int32_t)(0);
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (16, mCCREG, bitRHS);
          bitRHS->inUse = 0;
        }
        break;
      case 3:
        // CCREG = -XREG; (2992)
        {
          int32_t numberRHS
              = (int32_t)(xminus (COREHALFWORD (mGENERATExOPSTATxXREG)));
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (16, mCCREG, bitRHS);
          bitRHS->inUse = 0;
        }
        break;
      }
  } // End of DO CASE block
  // IF INST =  105 | INST =  41 THEN (2992)
  if (1
      & (xOR (xEQ (COREHALFWORD (mGENERATExOPSTATxINST), 105),
              xEQ (COREHALFWORD (mGENERATExOPSTATxINST), 41))))
    // CALL ERRORS(CLASS_C,5); (2993)
    {
      putBITp (16, mERRORSxCLASS, getBIT (8, mGENERATExOPSTATxCLASS_C));
      putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(5)));
      ERRORS (0);
    }
  {
    reentryGuard = 0;
    return 0;
  }
}
