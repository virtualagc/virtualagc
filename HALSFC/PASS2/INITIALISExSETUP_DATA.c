/*
  File INITIALISExSETUP_DATA.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

int32_t
INITIALISExSETUP_DATA (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "INITIALISExSETUP_DATA");
  // ESD_MAX, PCEBASE = DATALIMIT + 1; (2071)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mDATALIMIT), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mESD_MAX, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPCEBASE, bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL ENTER_ESD(PROGNAME(SELFNAMELOC, 8 + PCE), PCEBASE, 0); (2072)
  {
    putCHARACTERp (
        mENTER_ESDxNAME,
        (putBITp (16, mPROGNAMExENTRY, getBIT (16, mSELFNAMELOC)),
         putBITp (
             16, mPROGNAMExFLAG,
             fixedToBit (
                 32, (int32_t)(xadd (8, BYTE0 (mINITIALISExSETUP_DATAxPCE))))),
         PROGNAME (0)));
    putBITp (16, mENTER_ESDxPTR, getBIT (16, mPCEBASE));
    putBITp (16, mENTER_ESDxTYP, fixedToBit (32, (int32_t)(0)));
    ENTER_ESD (0);
  }
  // IF EXCLUSIVE# > 0 THEN (2073)
  if (1 & (xGT (COREHALFWORD (mEXCLUSIVEp), 0)))
    // DO; (2074)
    {
    rs1:;
      // ESD_MAX, EXCLBASE = ESD_MAX + 1; (2075)
      {
        int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mESD_MAX), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mESD_MAX, bitRHS);
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mEXCLBASE, bitRHS);
        bitRHS->inUse = 0;
      }
      // CALL ENTER_ESD(PROGNAME(SELFNAMELOC, 10), EXCLBASE, 0); (2076)
      {
        putCHARACTERp (
            mENTER_ESDxNAME,
            (putBITp (16, mPROGNAMExENTRY, getBIT (16, mSELFNAMELOC)),
             putBITp (16, mPROGNAMExFLAG, fixedToBit (32, (int32_t)(10))),
             PROGNAME (0)));
        putBITp (16, mENTER_ESDxPTR, getBIT (16, mEXCLBASE));
        putBITp (16, mENTER_ESDxTYP, fixedToBit (32, (int32_t)(0)));
        ENTER_ESD (0);
      }
      // LOCCTR(EXCLBASE) = SHL(EXCLUSIVE#, 1); (2077)
      {
        int32_t numberRHS = (int32_t)(SHL (COREHALFWORD (mEXCLUSIVEp), 1));
        putFIXED (mLOCCTR + 4 * (COREHALFWORD (mEXCLBASE)), numberRHS);
      }
    es1:;
    } // End of DO block
  // IF DATA_REMOTE THEN (2078)
  if (1 & (bitToFixed (getBIT (1, mDATA_REMOTE))))
    // DO; (2079)
    {
    rs2:;
      // ESD_MAX, DSESET = ESD_MAX + 1; (2080)
      {
        int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mESD_MAX), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mESD_MAX, bitRHS);
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (8, mDSESET, bitRHS);
        bitRHS->inUse = 0;
      }
      // CALL ENTER_ESD('$ZDSESET',DSESET,2); (2081)
      {
        putCHARACTERp (mENTER_ESDxNAME, cToDescriptor (NULL, "$ZDSESET"));
        putBITp (16, mENTER_ESDxPTR, getBIT (8, mDSESET));
        putBITp (16, mENTER_ESDxTYP, fixedToBit (32, (int32_t)(2)));
        ENTER_ESD (0);
      }
      // ESD_MAX, DSECLR = ESD_MAX + 1; (2082)
      {
        int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mESD_MAX), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mESD_MAX, bitRHS);
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (8, mDSECLR, bitRHS);
        bitRHS->inUse = 0;
      }
      // CALL ENTER_ESD('$ZDSECLR',DSECLR,2); (2083)
      {
        putCHARACTERp (mENTER_ESDxNAME, cToDescriptor (NULL, "$ZDSECLR"));
        putBITp (16, mENTER_ESDxPTR, getBIT (8, mDSECLR));
        putBITp (16, mENTER_ESDxTYP, fixedToBit (32, (int32_t)(2)));
        ENTER_ESD (0);
      }
    es2:;
    } // End of DO block
  // ESD_MAX, DATABASE = ESD_MAX + 1; (2084)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mESD_MAX), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mESD_MAX, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mDATABASE, bitRHS);
    bitRHS->inUse = 0;
  }
  // FSIMBASE = 0; (2085)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mFSIMBASE, bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL ENTER_ESD(PROGNAME(SELFNAMELOC, 4), DATABASE, 0); (2086)
  {
    putCHARACTERp (
        mENTER_ESDxNAME,
        (putBITp (16, mPROGNAMExENTRY, getBIT (16, mSELFNAMELOC)),
         putBITp (16, mPROGNAMExFLAG, fixedToBit (32, (int32_t)(4))),
         PROGNAME (0)));
    putBITp (16, mENTER_ESDxPTR, getBIT (16, mDATABASE));
    putBITp (16, mENTER_ESDxTYP, fixedToBit (32, (int32_t)(0)));
    ENTER_ESD (0);
  }
  // CALL SETUP_REMOTE_DATA; (2087)
  INITIALISExSETUP_REMOTE_DATA (0);
  // CALL SETUP_DATABASE; (2088)
  INITIALISExSETUP_DATABASE (0);
  // IF ESD_MAX > PROC# THEN (2089)
  if (1 & (xGT (COREHALFWORD (mESD_MAX), 255)))
    // CALL ERRORS(CLASS_BS,102); (2090)
    {
      putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_BS));
      putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(102)));
      ERRORS (0);
    }
  {
    reentryGuard = 0;
    return 0;
  }
}
