/*
  File OBJECT_GENERATORxGENERATE_OPERANDS.c generated by XCOM-I, 2024-08-08
  04:32:26.
*/

#include "runtimeC.h"

descriptor_t *
OBJECT_GENERATORxGENERATE_OPERANDS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard
      = guardReentry (reentryGuard, "OBJECT_GENERATORxGENERATE_OPERANDS");
// CASE0001: (15449)
CASE0001:
  // DO CASE SHR(INST, 6); (15450)
  {
  rs1:
    switch (SHR (getFIXED (mINST), 6))
      {
      case 0:
        // DO; (15452)
        {
        rs1s1:;
          // IF INST = LFXI THEN (15452)
          if (1 & (xEQ (getFIXED (mINST), BYTE0 (mLFXI))))
            // T = 'R'||R||COMMA||IX-2; (15453)
            {
              descriptor_t *stringRHS;
              stringRHS
                  = xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "R"),
                                         fixedToCharacter (getFIXED (mR))),
                                  getCHARACTER (mCOMMA)),
                           fixedToCharacter (xsubtract (getFIXED (mIX), 2)));
              putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT, stringRHS);
              stringRHS->inUse = 0;
            }
          // ELSE (15454)
          else
            // IF INST = LFLI THEN (15455)
            if (1 & (xEQ (getFIXED (mINST), BYTE0 (mLFLI))))
              // T = 'F'||R||COMMA||IX; (15456)
              {
                descriptor_t *stringRHS;
                stringRHS
                    = xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "F"),
                                           fixedToCharacter (getFIXED (mR))),
                                    getCHARACTER (mCOMMA)),
                             fixedToCharacter (getFIXED (mIX)));
                putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                              stringRHS);
                stringRHS->inUse = 0;
              }
            // ELSE (15457)
            else
              // IF INST = BCR | INST = BCRE | INST = SRET  | INST = SPM THEN
              // (15458)
              if (1
                  & (xOR (xOR (xOR (xEQ (getFIXED (mINST), BYTE0 (mBCR)),
                                    xEQ (getFIXED (mINST), BYTE0 (mBCRE))),
                               xEQ (getFIXED (mINST), BYTE0 (mSRET))),
                          xEQ (getFIXED (mINST), BYTE0 (mSPM)))))
                // T = R||COMMA||'R'||IX; (15459)
                {
                  descriptor_t *stringRHS;
                  stringRHS
                      = xsCAT (xsCAT (xsCAT (fixedToCharacter (getFIXED (mR)),
                                             getCHARACTER (mCOMMA)),
                                      cToDescriptor (NULL, "R")),
                               fixedToCharacter (getFIXED (mIX)));
                  putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                stringRHS);
                  stringRHS->inUse = 0;
                }
              // ELSE (15460)
              else
                // IF INST = CVFX THEN (15461)
                if (1 & (xEQ (getFIXED (mINST), BYTE0 (mCVFX))))
                  // T = 'R'||R||COMMA||'F'||IX; (15462)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = xsCAT (
                        xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "R"),
                                             fixedToCharacter (getFIXED (mR))),
                                      getCHARACTER (mCOMMA)),
                               cToDescriptor (NULL, "F")),
                        fixedToCharacter (getFIXED (mIX)));
                    putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                  stringRHS);
                    stringRHS->inUse = 0;
                  }
                // ELSE (15463)
                else
                  // IF INST = CVFL THEN (15464)
                  if (1 & (xEQ (getFIXED (mINST), BYTE0 (mCVFL))))
                    // T = 'F'||R||COMMA||'R'||IX; (15465)
                    {
                      descriptor_t *stringRHS;
                      stringRHS = xsCAT (
                          xsCAT (
                              xsCAT (xsCAT (cToDescriptor (NULL, "F"),
                                            fixedToCharacter (getFIXED (mR))),
                                     getCHARACTER (mCOMMA)),
                              cToDescriptor (NULL, "R")),
                          fixedToCharacter (getFIXED (mIX)));
                      putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                    stringRHS);
                      stringRHS->inUse = 0;
                    }
                  // ELSE (15466)
                  else
                    // IF INST > CVFX & INST < CVFL THEN (15467)
                    if (1
                        & (xAND (xGT (getFIXED (mINST), BYTE0 (mCVFX)),
                                 xLT (getFIXED (mINST), BYTE0 (mCVFL)))))
                      // T = 'F'||R||COMMA||'F'||IX; (15468)
                      {
                        descriptor_t *stringRHS;
                        stringRHS = xsCAT (
                            xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "F"),
                                                 fixedToCharacter (
                                                     getFIXED (mR))),
                                          getCHARACTER (mCOMMA)),
                                   cToDescriptor (NULL, "F")),
                            fixedToCharacter (getFIXED (mIX)));
                        putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                      stringRHS);
                        stringRHS->inUse = 0;
                      }
                    // ELSE (15469)
                    else
                      // T = 'R'||R||COMMA||'R'||IX; (15470)
                      {
                        descriptor_t *stringRHS;
                        stringRHS = xsCAT (
                            xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "R"),
                                                 fixedToCharacter (
                                                     getFIXED (mR))),
                                          getCHARACTER (mCOMMA)),
                                   cToDescriptor (NULL, "R")),
                            fixedToCharacter (getFIXED (mIX)));
                        putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                      stringRHS);
                        stringRHS->inUse = 0;
                      }
          // CALL EXECUTION_TIMES(0); (15471)
          {
            putBITp (4, mOBJECT_GENERATORxGENERATE_OPERANDSxEXECUTION_TIMESxM,
                     fixedToBit (32, (int32_t)(0)));
            OBJECT_GENERATORxGENERATE_OPERANDSxEXECUTION_TIMES (0);
          }
        es1s1:;
        } // End of DO block
        break;
      case 1:
      // CASE_RX: (15473)
      CASE_RX:
        {
        rs1s2:;
          // IF IX=0 THEN (15474)
          if (1 & (xEQ (getFIXED (mIX), 0)))
            // T = CHECK_NO_BASE_RS; (15475)
            {
              descriptor_t *stringRHS;
              stringRHS = OBJECT_GENERATORxCHECK_NO_BASE_RS (0);
              putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT, stringRHS);
              stringRHS->inUse = 0;
            }
          // ELSE (15476)
          else
            // T = LEFTBRACKET||'R'||IX||COMMA||'R'||B||RIGHTBRACKET; (15477)
            {
              descriptor_t *stringRHS;
              stringRHS = xsCAT (
                  xsCAT (
                      xsCAT (xsCAT (xsCAT (xsCAT (getCHARACTER (mLEFTBRACKET),
                                                  cToDescriptor (NULL, "R")),
                                           fixedToCharacter (getFIXED (mIX))),
                                    getCHARACTER (mCOMMA)),
                             cToDescriptor (NULL, "R")),
                      fixedToCharacter (getFIXED (mB))),
                  getCHARACTER (mRIGHTBRACKET));
              putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT, stringRHS);
              stringRHS->inUse = 0;
            }
          // T = R||COMMA||D||T; (15478)
          {
            descriptor_t *stringRHS;
            stringRHS
                = xsCAT (xsCAT (xsCAT (fixedToCharacter (getFIXED (mR)),
                                       getCHARACTER (mCOMMA)),
                                fixedToCharacter (getFIXED (mD))),
                         getCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT));
            putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT, stringRHS);
            stringRHS->inUse = 0;
          }
          // IF INST >=  96 & INST <=  126 THEN (15479)
          if (1
              & (xAND (xGE (getFIXED (mINST), 96),
                       xLE (getFIXED (mINST), 126))))
            // T = 'F'||T; (15480)
            {
              descriptor_t *stringRHS;
              stringRHS = xsCAT (
                  cToDescriptor (NULL, "F"),
                  getCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT));
              putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT, stringRHS);
              stringRHS->inUse = 0;
            }
          // ELSE (15481)
          else
            // IF INST ~= BC    THEN (15482)
            if (1 & (xNEQ (getFIXED (mINST), BYTE0 (mBC))))
              // T = 'R'||T; (15483)
              {
                descriptor_t *stringRHS;
                stringRHS = xsCAT (
                    cToDescriptor (NULL, "R"),
                    getCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT));
                putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                              stringRHS);
                stringRHS->inUse = 0;
              }
          // IF IX ~= 0 & D > 2047 THEN (15484)
          if (1 & (xAND (xNEQ (getFIXED (mIX), 0), xGT (getFIXED (mD), 2047))))
            // CALL ERRORS(CLASS_BI,512); (15485)
            {
              putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_BI));
              putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(512)));
              ERRORS (0);
            }
          // IF IX = 0 & D > 65535 THEN (15486)
          if (1 & (xAND (xEQ (getFIXED (mIX), 0), xGT (getFIXED (mD), 65535))))
            // CALL ERRORS(CLASS_BI,514); (15487)
            {
              putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_BI));
              putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(514)));
              ERRORS (0);
            }
          // IF LHS = SRSTYPE & D >= 56 THEN (15488)
          if (1
              & (xAND (xEQ (COREHALFWORD (mLHS), BYTE0 (mSRSTYPE)),
                       xGE (getFIXED (mD), 56))))
            // IF (INST >=  80 & INST <  128 & (SHR(D,1) >= 56)) |  ~(INST >=
            // 80 & INST <  128) THEN (15489)
            if (1
                & (xOR (xAND (xAND (xGE (getFIXED (mINST), 80),
                                    xLT (getFIXED (mINST), 128)),
                              xGE (SHR (getFIXED (mD), 1), 56)),
                        xNOT (xAND (xGE (getFIXED (mINST), 80),
                                    xLT (getFIXED (mINST), 128))))))
              // CALL ERRORS(CLASS_BI,515,'SRS'); (15490)
              {
                putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_BI));
                putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(515)));
                putCHARACTERp (mERRORSxTEXT, cToDescriptor (NULL, "SRS"));
                ERRORS (0);
              }
          // CALL EXECUTION_TIMES(1); (15491)
          {
            putBITp (4, mOBJECT_GENERATORxGENERATE_OPERANDSxEXECUTION_TIMESxM,
                     fixedToBit (32, (int32_t)(1)));
            OBJECT_GENERATORxGENERATE_OPERANDSxEXECUTION_TIMES (0);
          }
        es1s2:;
        } // End of DO block
        break;
      case 2:
        // DO; (15493)
        {
        rs1s3:;
          // IF INST >=  144 & INST <=  159 THEN (15493)
          if (1
              & (xAND (xGE (getFIXED (mINST), 144),
                       xLE (getFIXED (mINST), 159))))
            // DO; (15494)
            {
            rs1s3s1:;
              // IF IX = 0 THEN (15495)
              if (1 & (xEQ (getFIXED (mIX), 0)))
                // T = D||CHECK_NO_BASE_RS; (15496)
                {
                  descriptor_t *stringRHS;
                  stringRHS = xsCAT (fixedToCharacter (getFIXED (mD)),
                                     OBJECT_GENERATORxCHECK_NO_BASE_RS (0));
                  putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                stringRHS);
                  stringRHS->inUse = 0;
                }
              // ELSE (15497)
              else
                // T = D||LEFTBRACKET||'R'||IX||COMMA||'R'||B||RIGHTBRACKET;
                // (15498)
                {
                  descriptor_t *stringRHS;
                  stringRHS = xsCAT (
                      xsCAT (
                          xsCAT (
                              xsCAT (xsCAT (xsCAT (xsCAT (fixedToCharacter (
                                                              getFIXED (mD)),
                                                          getCHARACTER (
                                                              mLEFTBRACKET)),
                                                   cToDescriptor (NULL, "R")),
                                            fixedToCharacter (getFIXED (mIX))),
                                     getCHARACTER (mCOMMA)),
                              cToDescriptor (NULL, "R")),
                          fixedToCharacter (getFIXED (mB))),
                      getCHARACTER (mRIGHTBRACKET));
                  putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                stringRHS);
                  stringRHS->inUse = 0;
                }
              // IF IX ~= 0 & D > 2047 THEN (15499)
              if (1
                  & (xAND (xNEQ (getFIXED (mIX), 0),
                           xGT (getFIXED (mD), 2047))))
                // CALL ERRORS(CLASS_BI,512); (15500)
                {
                  putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_BI));
                  putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(512)));
                  ERRORS (0);
                }
            es1s3s1:;
            } // End of DO block
          // ELSE (15501)
          else
            // IF INST >=  176 & INST <=  191 THEN (15502)
            if (1
                & (xAND (xGE (getFIXED (mINST), 176),
                         xLE (getFIXED (mINST), 191))))
              // T=D||LEFTBRACKET||'R'||B||'),'||RHS(1); (15503)
              {
                descriptor_t *stringRHS;
                stringRHS = xsCAT (
                    xsCAT (
                        xsCAT (xsCAT (xsCAT (fixedToCharacter (getFIXED (mD)),
                                             getCHARACTER (mLEFTBRACKET)),
                                      cToDescriptor (NULL, "R")),
                               fixedToCharacter (getFIXED (mB))),
                        cToDescriptor (NULL, "),")),
                    bitToCharacter (getBIT (16, mRHS + 2 * 1)));
                putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                              stringRHS);
                stringRHS->inUse = 0;
              }
            // ELSE (15504)
            else
              // IF B = 0 THEN (15505)
              if (1 & (xEQ (getFIXED (mB), 0)))
                // T='R'||R||COMMA||D; (15506)
                {
                  descriptor_t *stringRHS;
                  stringRHS
                      = xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "R"),
                                             fixedToCharacter (getFIXED (mR))),
                                      getCHARACTER (mCOMMA)),
                               fixedToCharacter (getFIXED (mD)));
                  putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                stringRHS);
                  stringRHS->inUse = 0;
                }
              // ELSE (15507)
              else
                // IF INST = LHI THEN (15508)
                if (1 & (xEQ (getFIXED (mINST), BYTE0 (mLHI))))
                  // T='R'||R||COMMA||D||LEFTBRACKET||RIGHTBRACKET; (15509)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = xsCAT (
                        xsCAT (xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "R"),
                                                    fixedToCharacter (
                                                        getFIXED (mR))),
                                             getCHARACTER (mCOMMA)),
                                      fixedToCharacter (getFIXED (mD))),
                               getCHARACTER (mLEFTBRACKET)),
                        getCHARACTER (mRIGHTBRACKET));
                    putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                  stringRHS);
                    stringRHS->inUse = 0;
                  }
                // ELSE (15510)
                else
                  // IF INST <  128 | INST >  135 THEN (15511)
                  if (1
                      & (xOR (xLT (getFIXED (mINST), 128),
                              xGT (getFIXED (mINST), 135))))
                    // T='R'||R||COMMA||D||LEFTBRACKET||'R'||ABS(B)||RIGHTBRACKET;
                    // (15512)
                    {
                      descriptor_t *stringRHS;
                      stringRHS = xsCAT (
                          xsCAT (
                              xsCAT (
                                  xsCAT (
                                      xsCAT (xsCAT (xsCAT (cToDescriptor (NULL,
                                                                          "R"),
                                                           fixedToCharacter (
                                                               getFIXED (mR))),
                                                    getCHARACTER (mCOMMA)),
                                             fixedToCharacter (getFIXED (mD))),
                                      getCHARACTER (mLEFTBRACKET)),
                                  cToDescriptor (NULL, "R")),
                              fixedToCharacter (ABS (getFIXED (mB)))),
                          getCHARACTER (mRIGHTBRACKET));
                      putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                    stringRHS);
                      stringRHS->inUse = 0;
                    }
                  // ELSE (15513)
                  else
                    // DO; (15514)
                    {
                    rs1s3s2:;
                      // T = R||COMMA||'*'; (15515)
                      {
                        descriptor_t *stringRHS;
                        stringRHS
                            = xsCAT (xsCAT (fixedToCharacter (getFIXED (mR)),
                                            getCHARACTER (mCOMMA)),
                                     cToDescriptor (NULL, "*"));
                        putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                      stringRHS);
                        stringRHS->inUse = 0;
                      }
                      // IF (RHS & 2) = 0 | D = 0 THEN (15516)
                      if (1
                          & (xOR (xEQ (xAND (COREHALFWORD (mRHS), 2), 0),
                                  xEQ (getFIXED (mD), 0))))
                        // T = T || PLUS || D+1; (15517)
                        {
                          descriptor_t *stringRHS;
                          stringRHS = xsCAT (
                              xsCAT (
                                  getCHARACTER (
                                      mOBJECT_GENERATORxGENERATE_OPERANDSxT),
                                  getCHARACTER (mPLUS)),
                              fixedToCharacter (xadd (getFIXED (mD), 1)));
                          putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                        stringRHS);
                          stringRHS->inUse = 0;
                        }
                      // ELSE (15518)
                      else
                        // T = T || -D+1; (15519)
                        {
                          descriptor_t *stringRHS;
                          stringRHS = xsCAT (
                              getCHARACTER (
                                  mOBJECT_GENERATORxGENERATE_OPERANDSxT),
                              fixedToCharacter (
                                  xadd (xminus (getFIXED (mD)), 1)));
                          putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                        stringRHS);
                          stringRHS->inUse = 0;
                        }
                      // IF INST = BCTB THEN (15520)
                      if (1 & (xEQ (getFIXED (mINST), BYTE0 (mBCTB))))
                        // T = 'R'||T; (15521)
                        {
                          descriptor_t *stringRHS;
                          stringRHS = xsCAT (
                              cToDescriptor (NULL, "R"),
                              getCHARACTER (
                                  mOBJECT_GENERATORxGENERATE_OPERANDSxT));
                          putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                        stringRHS);
                          stringRHS->inUse = 0;
                        }
                    es1s3s2:;
                    } // End of DO block
          // IF IX = 0 & D > 65535 THEN (15522)
          if (1 & (xAND (xEQ (getFIXED (mIX), 0), xGT (getFIXED (mD), 65535))))
            // CALL ERRORS(CLASS_BI,514); (15523)
            {
              putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_BI));
              putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(514)));
              ERRORS (0);
            }
          // IF LHS = SRSTYPE & D >= 56 THEN (15524)
          if (1
              & (xAND (xEQ (COREHALFWORD (mLHS), BYTE0 (mSRSTYPE)),
                       xGE (getFIXED (mD), 56))))
            // CALL ERRORS(CLASS_BI,515,'SRS'); (15525)
            {
              putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_BI));
              putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(515)));
              putCHARACTERp (mERRORSxTEXT, cToDescriptor (NULL, "SRS"));
              ERRORS (0);
            }
          // IF LHS = SSTYPE & D >= 56 THEN (15526)
          if (1
              & (xAND (xEQ (COREHALFWORD (mLHS), BYTE0 (mSSTYPE)),
                       xGE (getFIXED (mD), 56))))
            // CALL ERRORS(CLASS_BI,515,'SI'); (15527)
            {
              putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_BI));
              putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(515)));
              putCHARACTERp (mERRORSxTEXT, cToDescriptor (NULL, "SI"));
              ERRORS (0);
            }
          // CALL EXECUTION_TIMES(2); (15528)
          {
            putBITp (4, mOBJECT_GENERATORxGENERATE_OPERANDSxEXECUTION_TIMESxM,
                     fixedToBit (32, (int32_t)(2)));
            OBJECT_GENERATORxGENERATE_OPERANDSxEXECUTION_TIMES (0);
          }
        es1s3:;
        } // End of DO block
        break;
      case 3:
        // DO; (15530)
        {
        rs1s4:;
          // IF (INST = LDM) THEN (15530)
          if (1 & (xEQ (getFIXED (mINST), BYTE0 (mLDM))))
            // DO; (15531)
            {
            rs1s4s1:;
              // IF IX = 0 THEN (15532)
              if (1 & (xEQ (getFIXED (mIX), 0)))
                // T = D||CHECK_NO_BASE_RS; (15533)
                {
                  descriptor_t *stringRHS;
                  stringRHS = xsCAT (fixedToCharacter (getFIXED (mD)),
                                     OBJECT_GENERATORxCHECK_NO_BASE_RS (0));
                  putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                stringRHS);
                  stringRHS->inUse = 0;
                }
              // ELSE (15534)
              else
                // DO; (15535)
                {
                rs1s4s1s1:;
                  // T = 'R'||IX||COMMA||'R'||B; (15536)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = xsCAT (
                        xsCAT (
                            xsCAT (xsCAT (cToDescriptor (NULL, "R"),
                                          fixedToCharacter (getFIXED (mIX))),
                                   getCHARACTER (mCOMMA)),
                            cToDescriptor (NULL, "R")),
                        fixedToCharacter (getFIXED (mB)));
                    putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                  stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // T = D||LEFTBRACKET||T||RIGHTBRACKET; (15537)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = xsCAT (
                        xsCAT (xsCAT (fixedToCharacter (getFIXED (mD)),
                                      getCHARACTER (mLEFTBRACKET)),
                               getCHARACTER (
                                   mOBJECT_GENERATORxGENERATE_OPERANDSxT)),
                        getCHARACTER (mRIGHTBRACKET));
                    putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                                  stringRHS);
                    stringRHS->inUse = 0;
                  }
                es1s4s1s1:;
                } // End of DO block
              // IF IX ~= 0 & D > 2047 THEN (15538)
              if (1
                  & (xAND (xNEQ (getFIXED (mIX), 0),
                           xGT (getFIXED (mD), 2047))))
                // CALL ERRORS(CLASS_BI,512); (15539)
                {
                  putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_BI));
                  putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(512)));
                  ERRORS (0);
                }
              // IF IX = 0 & D > 65535 THEN (15540)
              if (1
                  & (xAND (xEQ (getFIXED (mIX), 0),
                           xGT (getFIXED (mD), 65535))))
                // CALL ERRORS(CLASS_BI,514); (15541)
                {
                  putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_BI));
                  putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(514)));
                  ERRORS (0);
                }
            es1s4s1:;
            } // End of DO block
          // ELSE (15542)
          else
            // IF (INST = SRR) THEN (15543)
            if (1 & (xEQ (getFIXED (mINST), BYTE0 (mSRR))))
              // T='R'||R||COMMA||D; (15544)
              {
                descriptor_t *stringRHS;
                stringRHS
                    = xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "R"),
                                           fixedToCharacter (getFIXED (mR))),
                                    getCHARACTER (mCOMMA)),
                             fixedToCharacter (getFIXED (mD)));
                putCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT,
                              stringRHS);
                stringRHS->inUse = 0;
              }
            // ELSE (15545)
            else
              // GO TO CASE_RX; (15546)
              goto CASE_RX;
          // CALL EXECUTION_TIMES(3); (15547)
          {
            putBITp (4, mOBJECT_GENERATORxGENERATE_OPERANDSxEXECUTION_TIMESxM,
                     fixedToBit (32, (int32_t)(3)));
            OBJECT_GENERATORxGENERATE_OPERANDSxEXECUTION_TIMES (0);
          }
        es1s4:;
        } // End of DO block
        break;
      }
  } // End of DO CASE block
  // RETURN PAD(T, 20); (15548)
  {
    reentryGuard = 0;
    return (
        putCHARACTERp (mPADxSTRING,
                       getCHARACTER (mOBJECT_GENERATORxGENERATE_OPERANDSxT)),
        putFIXED (mPADxWIDTH, 20), PAD (0));
  }
}
