/*
  File CS.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
CS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "CS");
  // RETURN SHR(LEN+1, 1); (1088)
  {
    reentryGuard = 0;
    return SHR (xadd (getFIXED (mCSxLEN), 1), 1);
  }
}
