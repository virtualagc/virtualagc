/*
  File GENERATExPARM_STAT.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
GENERATExPARM_STAT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExPARM_STAT");
  // IF OP <= 0 THEN (2870)
  if (1 & (xLE (COREHALFWORD (mGENERATExPARM_STATxOP), 0)))
    // RETURN; (2871)
    {
      reentryGuard = 0;
      return 0;
    }
  // IF (CSECT_TYPE(LOC(OP),OP) = LOCAL#D) THEN (2872)
  if (1
      & (xEQ (bitToFixed (
                  (putBITp (16, mCSECT_TYPExPTR,
                            getBIT (16, getFIXED (mIND_STACK)
                                            + 73
                                                  * (COREHALFWORD (
                                                      mGENERATExPARM_STATxOP))
                                            + 40 + 2 * (0))),
                   putBITp (16, mCSECT_TYPExOP,
                            getBIT (16, mGENERATExPARM_STATxOP)),
                   CSECT_TYPE (0))),
              6)))
    // CALL ERRORS(CLASS_FT, 108); (2873)
    {
      putBITp (16, mERRORSxCLASS, getBIT (8, mCLASS_FT));
      putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(108)));
      ERRORS (0);
    }
  {
    reentryGuard = 0;
    return 0;
  }
}
