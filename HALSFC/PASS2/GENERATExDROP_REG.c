/*
  File GENERATExDROP_REG.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

int32_t
GENERATExDROP_REG (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExDROP_REG");
  // USAGE(REG(OP)) = USAGE(REG(OP)) - 2; (4238)
  {
    int32_t numberRHS = (int32_t)(xsubtract (
        COREHALFWORD (
            mUSAGE
            + 2
                  * COREHALFWORD (getFIXED (mIND_STACK)
                                  + 73 * (COREHALFWORD (mGENERATExDROP_REGxOP))
                                  + 46 + 2 * (0))),
        2));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16,
            mUSAGE
                + 2
                      * (COREHALFWORD (
                          getFIXED (mIND_STACK)
                          + 73 * (COREHALFWORD (mGENERATExDROP_REGxOP)) + 46
                          + 2 * (0))),
            bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL SET_REG_NEXT_USE(REG(OP), OP); (4239)
  {
    putBITp (16, mGENERATExSET_REG_NEXT_USExR,
             getBIT (16, getFIXED (mIND_STACK)
                             + 73 * (COREHALFWORD (mGENERATExDROP_REGxOP)) + 46
                             + 2 * (0)));
    putBITp (16, mGENERATExSET_REG_NEXT_USExOP,
             getBIT (16, mGENERATExDROP_REGxOP));
    GENERATExSET_REG_NEXT_USE (0);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
