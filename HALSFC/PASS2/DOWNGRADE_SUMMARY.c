/*
  File DOWNGRADE_SUMMARY.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
DOWNGRADE_SUMMARY (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "DOWNGRADE_SUMMARY");
  // END_OF_LIST = 0; (1211)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mDOWNGRADE_SUMMARYxEND_OF_LIST, bitRHS);
    bitRHS->inUse = 0;
  }
  // NOT_DOWNGRADED = 0; (1212)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mNOT_DOWNGRADED, bitRHS);
    bitRHS->inUse = 0;
  }
  // DOWN_COUNT = 1; (1213)
  {
    int32_t numberRHS = (int32_t)(1);
    putFIXED (mDOWNGRADE_SUMMARYxDOWN_COUNT, numberRHS);
  }
  // IF RECORD_TOP(DOWN_INFO) >= 1 THEN (1214)
  if (1
      & (xGE (xsubtract (COREWORD (xadd (
                             ADDR ("DOWN_INFO", 0x80000000, NULL, 0), 12)),
                         1),
              1)))
    // DO; (1215)
    {
    rs1:;
      // OUTPUT = ' '; (1216)
      {
        descriptor_t *stringRHS;
        stringRHS = cToDescriptor (NULL, " ");
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
      // OUTPUT = ' '; (1217)
      {
        descriptor_t *stringRHS;
        stringRHS = cToDescriptor (NULL, " ");
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
      // OUTPUT = ' '; (1218)
      {
        descriptor_t *stringRHS;
        stringRHS = cToDescriptor (NULL, " ");
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
      // OUTPUT = ' **********  DOWNGRADE SUMMARY   *********************';
      // (1219)
      {
        descriptor_t *stringRHS;
        stringRHS = cToDescriptor (
            NULL, " **********  DOWNGRADE SUMMARY   *********************");
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
      // OUTPUT = ' '; (1220)
      {
        descriptor_t *stringRHS;
        stringRHS = cToDescriptor (NULL, " ");
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
      // OUTPUT = ' '; (1221)
      {
        descriptor_t *stringRHS;
        stringRHS = cToDescriptor (NULL, " ");
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
      // DO WHILE END_OF_LIST = 0 & DOWN_COUNT < = RECORD_TOP(DOWN_INFO);
      // (1222)
      while (
          1
          & (xAND (xEQ (BYTE0 (mDOWNGRADE_SUMMARYxEND_OF_LIST), 0),
                   xLE (getFIXED (mDOWNGRADE_SUMMARYxDOWN_COUNT),
                        xsubtract (
                            COREWORD (xadd (
                                ADDR ("DOWN_INFO", 0x80000000, NULL, 0), 12)),
                            1)))))
        {
          // IF DWN_ERR(DOWN_COUNT) > ' ' THEN (1223)
          if (1
              & (xsGT (getCHARACTER (
                           getFIXED (mDOWN_INFO)
                           + 20 * (getFIXED (mDOWNGRADE_SUMMARYxDOWN_COUNT))
                           + 4 + 4 * (0)),
                       cToDescriptor (NULL, " "))))
            // DO; (1224)
            {
            rs1s1s1:;
              // IF DWN_VER(DOWN_COUNT) = 1 THEN (1225)
              if (1
                  & (xsEQ (
                      getCHARACTER (
                          getFIXED (mDOWN_INFO)
                          + 20 * (getFIXED (mDOWNGRADE_SUMMARYxDOWN_COUNT))
                          + 16 + 4 * (0)),
                      fixedToCharacter (1))))
                // DO; (1226)
                {
                rs1s1s1s1:;
                  // SEARCH_FOR_CLS = 1; (1227)
                  {
                    int32_t numberRHS = (int32_t)(1);
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (1, mDOWNGRADE_SUMMARYxSEARCH_FOR_CLS, bitRHS);
                    bitRHS->inUse = 0;
                  }
                  // COUNT = 0; (1228)
                  {
                    int32_t numberRHS = (int32_t)(0);
                    putFIXED (mDOWNGRADE_SUMMARYxCOUNT, numberRHS);
                  }
                  // DO WHILE SEARCH_FOR_CLS = 1; (1229)
                  while (
                      1 & (xEQ (BYTE0 (mDOWNGRADE_SUMMARYxSEARCH_FOR_CLS), 1)))
                    {
                      // IF DWN_CLS(DOWN_COUNT) = ERR_VALUE(COUNT) THEN (1230)
                      if (1
                          & (xsEQ (
                              getCHARACTER (
                                  getFIXED (mDOWN_INFO)
                                  + 20
                                        * (getFIXED (
                                            mDOWNGRADE_SUMMARYxDOWN_COUNT))
                                  + 8 + 4 * (0)),
                              fixedToCharacter (getFIXED (
                                  mERR_VALUE
                                  + 4
                                        * getFIXED (
                                            mDOWNGRADE_SUMMARYxCOUNT))))))
                        // DO; (1231)
                        {
                        rs1s1s1s1s1s1:;
                          // TEMP_CLS = SUBSTR(ERROR_INDEX(COUNT),6,2); (1232)
                          {
                            descriptor_t *stringRHS;
                            stringRHS = SUBSTR (
                                getCHARACTER (
                                    mERROR_INDEX
                                    + 4 * getFIXED (mDOWNGRADE_SUMMARYxCOUNT)),
                                6, 2);
                            putCHARACTER (mDOWNGRADE_SUMMARYxTEMP_CLS,
                                          stringRHS);
                            stringRHS->inUse = 0;
                          }
                          // SEARCH_FOR_CLS = 0; (1233)
                          {
                            int32_t numberRHS = (int32_t)(0);
                            descriptor_t *bitRHS;
                            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                            putBIT (1, mDOWNGRADE_SUMMARYxSEARCH_FOR_CLS,
                                    bitRHS);
                            bitRHS->inUse = 0;
                          }
                        es1s1s1s1s1s1:;
                        } // End of DO block
                      // ELSE (1234)
                      else
                        // DO; (1235)
                        {
                        rs1s1s1s1s1s2:;
                          // COUNT = COUNT + 1; (1236)
                          {
                            int32_t numberRHS = (int32_t)(xadd (
                                getFIXED (mDOWNGRADE_SUMMARYxCOUNT), 1));
                            putFIXED (mDOWNGRADE_SUMMARYxCOUNT, numberRHS);
                          }
                        es1s1s1s1s1s2:;
                        } // End of DO block
                    }     // End of DO WHILE block
                  // TEMP1 = SUBSTR(TEMP_CLS,0,1); (1237)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = SUBSTR (
                        getCHARACTER (mDOWNGRADE_SUMMARYxTEMP_CLS), 0, 1);
                    putCHARACTER (mDOWNGRADE_SUMMARYxTEMP1, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // TEMP2 = SUBSTR(TEMP_CLS,1,1); (1238)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = SUBSTR (
                        getCHARACTER (mDOWNGRADE_SUMMARYxTEMP_CLS), 1, 1);
                    putCHARACTER (mDOWNGRADE_SUMMARYxTEMP2, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // IF TEMP2 = ' ' THEN (1239)
                  if (1
                      & (xsEQ (getCHARACTER (mDOWNGRADE_SUMMARYxTEMP2),
                               cToDescriptor (NULL, " "))))
                    // TEMP3 = TEMP1 || DWN_ERR(DOWN_COUNT); (1240)
                    {
                      descriptor_t *stringRHS;
                      stringRHS = xsCAT (
                          getCHARACTER (mDOWNGRADE_SUMMARYxTEMP1),
                          getCHARACTER (
                              getFIXED (mDOWN_INFO)
                              + 20 * (getFIXED (mDOWNGRADE_SUMMARYxDOWN_COUNT))
                              + 4 + 4 * (0)));
                      putCHARACTER (mDOWNGRADE_SUMMARYxTEMP3, stringRHS);
                      stringRHS->inUse = 0;
                    }
                  // ELSE (1241)
                  else
                    // TEMP3 = TEMP_CLS || DWN_ERR(DOWN_COUNT); (1242)
                    {
                      descriptor_t *stringRHS;
                      stringRHS = xsCAT (
                          getCHARACTER (mDOWNGRADE_SUMMARYxTEMP_CLS),
                          getCHARACTER (
                              getFIXED (mDOWN_INFO)
                              + 20 * (getFIXED (mDOWNGRADE_SUMMARYxDOWN_COUNT))
                              + 4 + 4 * (0)));
                      putCHARACTER (mDOWNGRADE_SUMMARYxTEMP3, stringRHS);
                      stringRHS->inUse = 0;
                    }
                  // OUTPUT = '*** ERROR NUMBER ' || TEMP3||' AT STATEMENT
                  // NUMBER '|| DWN_STMT(DOWN_COUNT) || ' ***'; (1243)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = xsCAT (
                        xsCAT (
                            xsCAT (
                                xsCAT (
                                    cToDescriptor (NULL, "*** ERROR NUMBER "),
                                    getCHARACTER (mDOWNGRADE_SUMMARYxTEMP3)),
                                cToDescriptor (NULL, " AT STATEMENT NUMBER ")),
                            getCHARACTER (
                                getFIXED (mDOWN_INFO)
                                + 20
                                      * (getFIXED (
                                          mDOWNGRADE_SUMMARYxDOWN_COUNT))
                                + 0 + 4 * (0))),
                        cToDescriptor (NULL, " ***"));
                    OUTPUT (0, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // OUTPUT = '*** WAS DOWNGRADED FROM A SEVERITY ONE ERROR TO'
                  // || ' A SEVERITY ZERO ERROR MESSAGE ***'; (1244)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = cToDescriptor (
                        NULL, "*** WAS DOWNGRADED FROM A SEVERITY ONE ERROR "
                              "TO A SEVERITY ZERO ERROR MESSAGE ***");
                    OUTPUT (0, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // OUTPUT = '  '; (1245)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = cToDescriptor (NULL, "  ");
                    OUTPUT (0, stringRHS);
                    stringRHS->inUse = 0;
                  }
                  // OUTPUT = '  '; (1246)
                  {
                    descriptor_t *stringRHS;
                    stringRHS = cToDescriptor (NULL, "  ");
                    OUTPUT (0, stringRHS);
                    stringRHS->inUse = 0;
                  }
                es1s1s1s1:;
                } // End of DO block
              // ELSE (1247)
              else
                // NOT_DOWNGRADED = 1; (1248)
                {
                  int32_t numberRHS = (int32_t)(1);
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (1, mNOT_DOWNGRADED, bitRHS);
                  bitRHS->inUse = 0;
                }
            es1s1s1:;
            } // End of DO block
          // ELSE (1249)
          else
            // DO; (1250)
            {
            rs1s1s2:;
              // END_OF_LIST = 1; (1251)
              {
                int32_t numberRHS = (int32_t)(1);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mDOWNGRADE_SUMMARYxEND_OF_LIST, bitRHS);
                bitRHS->inUse = 0;
              }
            es1s1s2:;
            } // End of DO block
          // DOWN_COUNT = DOWN_COUNT + 1; (1252)
          {
            int32_t numberRHS = (int32_t)(xadd (
                getFIXED (mDOWNGRADE_SUMMARYxDOWN_COUNT), 1));
            putFIXED (mDOWNGRADE_SUMMARYxDOWN_COUNT, numberRHS);
          }
        } // End of DO WHILE block
      // IF NOT_DOWNGRADED THEN (1253)
      if (1 & (bitToFixed (getBIT (1, mNOT_DOWNGRADED))))
        // DO; (1254)
        {
        rs1s2:;
          // OUTPUT = '  '; (1255)
          {
            descriptor_t *stringRHS;
            stringRHS = cToDescriptor (NULL, "  ");
            OUTPUT (0, stringRHS);
            stringRHS->inUse = 0;
          }
          // OUTPUT = '  '; (1256)
          {
            descriptor_t *stringRHS;
            stringRHS = cToDescriptor (NULL, "  ");
            OUTPUT (0, stringRHS);
            stringRHS->inUse = 0;
          }
          // OUTPUT = '*****  DOWNGRADE DIRECTIVES THAT WERE NOT DOWNGRADED
          // *****'; (1257)
          {
            descriptor_t *stringRHS;
            stringRHS = cToDescriptor (
                NULL,
                "*****  DOWNGRADE DIRECTIVES THAT WERE NOT DOWNGRADED *****");
            OUTPUT (0, stringRHS);
            stringRHS->inUse = 0;
          }
          // OUTPUT = '  '; (1258)
          {
            descriptor_t *stringRHS;
            stringRHS = cToDescriptor (NULL, "  ");
            OUTPUT (0, stringRHS);
            stringRHS->inUse = 0;
          }
          // OUTPUT = '  '; (1259)
          {
            descriptor_t *stringRHS;
            stringRHS = cToDescriptor (NULL, "  ");
            OUTPUT (0, stringRHS);
            stringRHS->inUse = 0;
          }
          // DO I = 1 TO RECORD_TOP(DOWN_INFO); (1260)
          {
            int32_t from25, to25, by25;
            from25 = 1;
            to25 = xsubtract (
                COREWORD (xadd (ADDR ("DOWN_INFO", 0x80000000, NULL, 0), 12)),
                1);
            by25 = 1;
            for (putFIXED (mDOWNGRADE_SUMMARYxI, from25);
                 getFIXED (mDOWNGRADE_SUMMARYxI) <= to25;
                 putFIXED (mDOWNGRADE_SUMMARYxI,
                           getFIXED (mDOWNGRADE_SUMMARYxI) + by25))
              {
                // IF DWN_VER(I) ~= 1 THEN (1261)
                if (1
                    & (xsNEQ (
                        getCHARACTER (getFIXED (mDOWN_INFO)
                                      + 20 * (getFIXED (mDOWNGRADE_SUMMARYxI))
                                      + 16 + 4 * (0)),
                        fixedToCharacter (1))))
                  // DO; (1262)
                  {
                  rs1s2s1s1:;
                    // DOWN_COUNT = I; (1263)
                    {
                      int32_t numberRHS
                          = (int32_t)(getFIXED (mDOWNGRADE_SUMMARYxI));
                      putFIXED (mDOWNGRADE_SUMMARYxDOWN_COUNT, numberRHS);
                    }
                    // SEARCH_FOR_CLS = 1; (1264)
                    {
                      int32_t numberRHS = (int32_t)(1);
                      descriptor_t *bitRHS;
                      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                      putBIT (1, mDOWNGRADE_SUMMARYxSEARCH_FOR_CLS, bitRHS);
                      bitRHS->inUse = 0;
                    }
                    // COUNT = 0; (1265)
                    {
                      int32_t numberRHS = (int32_t)(0);
                      putFIXED (mDOWNGRADE_SUMMARYxCOUNT, numberRHS);
                    }
                    // IF DWN_UNKN(I) ~= '' THEN (1266)
                    if (1
                        & (xsNEQ (getCHARACTER (
                                      getFIXED (mDOWN_INFO)
                                      + 20 * (getFIXED (mDOWNGRADE_SUMMARYxI))
                                      + 12 + 4 * (0)),
                                  cToDescriptor (NULL, ""))))
                      // DO; (1267)
                      {
                      rs1s2s1s1s1:;
                        // TEMP3 = DWN_UNKN(I); (1268)
                        {
                          descriptor_t *stringRHS;
                          stringRHS = getCHARACTER (
                              getFIXED (mDOWN_INFO)
                              + 20 * (getFIXED (mDOWNGRADE_SUMMARYxI)) + 12
                              + 4 * (0));
                          putCHARACTER (mDOWNGRADE_SUMMARYxTEMP3, stringRHS);
                          stringRHS->inUse = 0;
                        }
                        // GOTO PRINT_ERR_MSG; (1269)
                        goto PRINT_ERR_MSG;
                      es1s2s1s1s1:;
                      } // End of DO block
                    // ELSE (1270)
                    else
                      // DO WHILE SEARCH_FOR_CLS = 1; (1271)
                      while (1
                             & (xEQ (BYTE0 (mDOWNGRADE_SUMMARYxSEARCH_FOR_CLS),
                                     1)))
                        {
                          // IF DWN_CLS(DOWN_COUNT) = ERR_VALUE(COUNT) THEN
                          // (1272)
                          if (1
                              & (xsEQ (
                                  getCHARACTER (
                                      getFIXED (mDOWN_INFO)
                                      + 20
                                            * (getFIXED (
                                                mDOWNGRADE_SUMMARYxDOWN_COUNT))
                                      + 8 + 4 * (0)),
                                  fixedToCharacter (getFIXED (
                                      mERR_VALUE
                                      + 4
                                            * getFIXED (
                                                mDOWNGRADE_SUMMARYxCOUNT))))))
                            // DO; (1273)
                            {
                            rs1s2s1s1s2s1:;
                              // TEMP_CLS = SUBSTR(ERROR_INDEX(COUNT),6,2);
                              // (1274)
                              {
                                descriptor_t *stringRHS;
                                stringRHS = SUBSTR (
                                    getCHARACTER (
                                        mERROR_INDEX
                                        + 4
                                              * getFIXED (
                                                  mDOWNGRADE_SUMMARYxCOUNT)),
                                    6, 2);
                                putCHARACTER (mDOWNGRADE_SUMMARYxTEMP_CLS,
                                              stringRHS);
                                stringRHS->inUse = 0;
                              }
                              // SEARCH_FOR_CLS = 0; (1275)
                              {
                                int32_t numberRHS = (int32_t)(0);
                                descriptor_t *bitRHS;
                                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                                putBIT (1, mDOWNGRADE_SUMMARYxSEARCH_FOR_CLS,
                                        bitRHS);
                                bitRHS->inUse = 0;
                              }
                            es1s2s1s1s2s1:;
                            } // End of DO block
                          // ELSE (1276)
                          else
                            // DO; (1277)
                            {
                            rs1s2s1s1s2s2:;
                              // COUNT = COUNT + 1; (1278)
                              {
                                int32_t numberRHS = (int32_t)(xadd (
                                    getFIXED (mDOWNGRADE_SUMMARYxCOUNT), 1));
                                putFIXED (mDOWNGRADE_SUMMARYxCOUNT, numberRHS);
                              }
                            es1s2s1s1s2s2:;
                            } // End of DO block
                        }     // End of DO WHILE block
                    // TEMP1 = SUBSTR(TEMP_CLS,0,1); (1279)
                    {
                      descriptor_t *stringRHS;
                      stringRHS = SUBSTR (
                          getCHARACTER (mDOWNGRADE_SUMMARYxTEMP_CLS), 0, 1);
                      putCHARACTER (mDOWNGRADE_SUMMARYxTEMP1, stringRHS);
                      stringRHS->inUse = 0;
                    }
                    // TEMP2 = SUBSTR(TEMP_CLS,1,1); (1280)
                    {
                      descriptor_t *stringRHS;
                      stringRHS = SUBSTR (
                          getCHARACTER (mDOWNGRADE_SUMMARYxTEMP_CLS), 1, 1);
                      putCHARACTER (mDOWNGRADE_SUMMARYxTEMP2, stringRHS);
                      stringRHS->inUse = 0;
                    }
                    // IF TEMP2 = ' ' THEN (1281)
                    if (1
                        & (xsEQ (getCHARACTER (mDOWNGRADE_SUMMARYxTEMP2),
                                 cToDescriptor (NULL, " "))))
                      // TEMP3 = TEMP1 || DWN_ERR(DOWN_COUNT); (1282)
                      {
                        descriptor_t *stringRHS;
                        stringRHS = xsCAT (
                            getCHARACTER (mDOWNGRADE_SUMMARYxTEMP1),
                            getCHARACTER (
                                getFIXED (mDOWN_INFO)
                                + 20
                                      * (getFIXED (
                                          mDOWNGRADE_SUMMARYxDOWN_COUNT))
                                + 4 + 4 * (0)));
                        putCHARACTER (mDOWNGRADE_SUMMARYxTEMP3, stringRHS);
                        stringRHS->inUse = 0;
                      }
                    // ELSE (1283)
                    else
                      // TEMP3 = TEMP_CLS || DWN_ERR(DOWN_COUNT); (1284)
                      {
                        descriptor_t *stringRHS;
                        stringRHS = xsCAT (
                            getCHARACTER (mDOWNGRADE_SUMMARYxTEMP_CLS),
                            getCHARACTER (
                                getFIXED (mDOWN_INFO)
                                + 20
                                      * (getFIXED (
                                          mDOWNGRADE_SUMMARYxDOWN_COUNT))
                                + 4 + 4 * (0)));
                        putCHARACTER (mDOWNGRADE_SUMMARYxTEMP3, stringRHS);
                        stringRHS->inUse = 0;
                      }
                  // PRINT_ERR_MSG: (1285)
                  PRINT_ERR_MSG:
                    // OUTPUT = '*** ERROR NUMBER ' || TEMP3 || ' FOR STATEMENT
                    // NUMBER ' || DWN_STMT(DOWN_COUNT) || ' WAS NOT
                    // DOWNGRADED, REMOVE DOWNGRADE' || ' DIRECTIVE AND
                    // RECOMPILE *** '; (1286)
                    {
                      descriptor_t *stringRHS;
                      stringRHS = xsCAT (
                          xsCAT (
                              xsCAT (
                                  xsCAT (xsCAT (cToDescriptor (
                                                    NULL, "*** ERROR NUMBER "),
                                                getCHARACTER (
                                                    mDOWNGRADE_SUMMARYxTEMP3)),
                                         cToDescriptor (
                                             NULL, " FOR STATEMENT NUMBER ")),
                                  getCHARACTER (
                                      getFIXED (mDOWN_INFO)
                                      + 20
                                            * (getFIXED (
                                                mDOWNGRADE_SUMMARYxDOWN_COUNT))
                                      + 0 + 4 * (0))),
                              cToDescriptor (
                                  NULL,
                                  " WAS NOT DOWNGRADED, REMOVE DOWNGRADE")),
                          cToDescriptor (NULL,
                                         " DIRECTIVE AND RECOMPILE *** "));
                      OUTPUT (0, stringRHS);
                      stringRHS->inUse = 0;
                    }
                    // OUTPUT = '  '; (1287)
                    {
                      descriptor_t *stringRHS;
                      stringRHS = cToDescriptor (NULL, "  ");
                      OUTPUT (0, stringRHS);
                      stringRHS->inUse = 0;
                    }
                  es1s2s1s1:;
                  } // End of DO block
              }
          } // End of DO for-loop block
        es1s2:;
        } // End of DO block
    es1:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
