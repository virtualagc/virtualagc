/*
  File GETARRAYp.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

descriptor_t *
GETARRAYp (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GETARRAYp");
  // IF SYT_ARRAY(OP) <= 0 THEN (1419)
  if (1
      & (xLE (COREHALFWORD (getFIXED (mSYM_TAB)
                            + 34 * (COREHALFWORD (mGETARRAYpxOP)) + 20
                            + 2 * (0)),
              0)))
    // RETURN 0; (1420)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
  // ELSE (1421)
  else
    // RETURN EXT_ARRAY(SYT_ARRAY(OP)); (1422)
    {
      reentryGuard = 0;
      return getBIT (
          16, mEXT_ARRAY
                  + 2
                        * COREHALFWORD (getFIXED (mSYM_TAB)
                                        + 34 * (COREHALFWORD (mGETARRAYpxOP))
                                        + 20 + 2 * (0)));
    }
}
