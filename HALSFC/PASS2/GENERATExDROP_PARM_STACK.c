/*
  File GENERATExDROP_PARM_STACK.c generated by XCOM-I, 2024-08-08 04:32:26.
*/

#include "runtimeC.h"

int32_t
GENERATExDROP_PARM_STACK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExDROP_PARM_STACK");
  // DO WHILE R_PARM# >= 0; (5982)
  while (1 & (xGE (COREHALFWORD (mR_PARMp), 0)))
    {
      // CALL CHECK_AND_DROP_VAC(R_PARM(R_PARM#), BACKUP_REG(R_PARM(R_PARM#)));
      // (5983)
      {
        putBITp (16, mGENERATExCHECK_AND_DROP_VACxOP,
                 getBIT (16, mR_PARM + 2 * COREHALFWORD (mR_PARMp)));
        putBITp (
            16, mGENERATExCHECK_AND_DROP_VACxR,
            getBIT (16, getFIXED (mIND_STACK)
                            + 73
                                  * (COREHALFWORD (
                                      mR_PARM + 2 * COREHALFWORD (mR_PARMp)))
                            + 20 + 2 * (0)));
        GENERATExCHECK_AND_DROP_VAC (0);
      }
      // R_PARM# = R_PARM# - 1; (5984)
      {
        int32_t numberRHS = (int32_t)(xsubtract (COREHALFWORD (mR_PARMp), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mR_PARMp, bitRHS);
        bitRHS->inUse = 0;
      }
    } // End of DO WHILE block
  {
    reentryGuard = 0;
    return 0;
  }
}
