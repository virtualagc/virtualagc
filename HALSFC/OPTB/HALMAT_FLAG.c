/*
  File HALMAT_FLAG.c generated by XCOM-I, 2024-08-08 04:34:00.
*/

#include "runtimeC.h"

descriptor_t *
HALMAT_FLAG (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "HALMAT_FLAG");
  // IF ~OPR(PTR) THEN (1660)
  if (1 & (xNOT (getFIXED (mOPR + 4 * COREHALFWORD (mHALMAT_FLAGxPTR)))))
    // RETURN SHR(OPR(PTR),3) &  1; (1661)
    {
      reentryGuard = 0;
      return fixedToBit (
          32,
          (int32_t)(xAND (
              SHR (getFIXED (mOPR + 4 * COREHALFWORD (mHALMAT_FLAGxPTR)), 3),
              1)));
    }
  // IF VAC_OR_XPT(PTR) THEN (1662)
  if (1
      & (bitToFixed (
          (putBITp (16, mVAC_OR_XPTxPTR, getBIT (16, mHALMAT_FLAGxPTR)),
           VAC_OR_XPT (0)))))
    // RETURN (OPR(PTR) &  12) =  12; (1663)
    {
      reentryGuard = 0;
      return fixedToBit (
          32,
          (int32_t)(xEQ (
              xAND (getFIXED (mOPR + 4 * COREHALFWORD (mHALMAT_FLAGxPTR)), 12),
              12)));
    }
  // RETURN FALSE; (1664)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(0));
  }
}
