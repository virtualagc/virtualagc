/*
  File BUMP_BLOCK.c generated by XCOM-I, 2024-08-09 12:41:08.
*/

#include "runtimeC.h"

descriptor_t *
BUMP_BLOCK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "BUMP_BLOCK");
  // BLOCK_TOP = BLOCK_TOP + 1; (712)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mBLOCK_TOP), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBLOCK_TOP, bitRHS);
    bitRHS->inUse = 0;
  }
  // RETURN BLOCK_TOP; (713)
  {
    reentryGuard = 0;
    return getBIT (16, mBLOCK_TOP);
  }
}
