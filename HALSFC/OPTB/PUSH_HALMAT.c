/*
  File PUSH_HALMAT.c generated by XCOM-I, 2024-08-09 12:41:08.
*/

#include "runtimeC.h"

descriptor_t *
PUSH_HALMAT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PUSH_HALMAT");
  // IF TRACE THEN (1759)
  if (1 & (bitToFixed (getBIT (8, mTRACE))))
    // OUTPUT = 'PUSH_HALMAT:  (' || START||','||DISP||','|| FINAL_PUSH||')';
    // (1760)
    {
      descriptor_t *stringRHS;
      stringRHS = xsCAT (
          xsCAT (
              xsCAT (
                  xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "PUSH_HALMAT:  ("),
                                       bitToCharacter (
                                           getBIT (16, mPUSH_HALMATxSTART))),
                                cToDescriptor (NULL, ",")),
                         bitToCharacter (getBIT (16, mPUSH_HALMATxDISP))),
                  cToDescriptor (NULL, ",")),
              bitToCharacter (getBIT (8, mPUSH_HALMATxFINAL_PUSH))),
          cToDescriptor (NULL, ")"));
      OUTPUT (0, stringRHS);
      stringRHS->inUse = 0;
    }
  // TOTAL,RET = 0; (1761)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mTOTAL, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mPUSH_HALMATxRET, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF EXTN_CHK THEN (1762)
  if (1 & (bitToFixed (getBIT (8, mPUSH_HALMATxEXTN_CHK))))
    // D_N_INX = 0; (1763)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mD_N_INX, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF DISP <= ROOM AND (OPR(SMRK_CTR - ROOM) &  65521) = XNOP AND ~FINAL_PUSH
  // AND ~EXTN_CHK THEN (1764)
  if (1
      & (xAND (
          xAND (xAND (xLE (COREHALFWORD (mPUSH_HALMATxDISP),
                           COREHALFWORD (mROOM)),
                      xEQ (xAND (getFIXED (mOPR
                                           + 4
                                                 * xsubtract (
                                                     COREHALFWORD (mSMRK_CTR),
                                                     COREHALFWORD (mROOM))),
                                 65521),
                           COREHALFWORD (mXNOP))),
                xNOT (BYTE0 (mPUSH_HALMATxFINAL_PUSH))),
          xNOT (BYTE0 (mPUSH_HALMATxEXTN_CHK)))))
    // DO; (1765)
    {
    rs1:;
      // OLD_XREC = XREC_PTR; (1766)
      {
        descriptor_t *bitRHS = getBIT (16, mXREC_PTR);
        putBIT (16, mPUSH_HALMATxOLD_XREC, bitRHS);
        bitRHS->inUse = 0;
      }
      // XREC_PTR = SMRK_CTR - ROOM - 1; (1767)
      {
        int32_t numberRHS = (int32_t)(xsubtract (
            xsubtract (COREHALFWORD (mSMRK_CTR), COREHALFWORD (mROOM)), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mXREC_PTR, bitRHS);
        bitRHS->inUse = 0;
      }
      // ROOM = ROOM - DISP; (1768)
      {
        int32_t numberRHS = (int32_t)(xsubtract (
            COREHALFWORD (mROOM), COREHALFWORD (mPUSH_HALMATxDISP)));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mROOM, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF ROOM > 0 THEN (1769)
      if (1 & (xGT (COREHALFWORD (mROOM), 0)))
        // DO; (1770)
        {
        rs1s1:;
          // OPR(SMRK_CTR - ROOM) = SHL(ROOM - 1,16); (1771)
          {
            int32_t numberRHS
                = (int32_t)(SHL (xsubtract (COREHALFWORD (mROOM), 1), 16));
            putFIXED (mOPR
                          + 4
                                * (xsubtract (COREHALFWORD (mSMRK_CTR),
                                              COREHALFWORD (mROOM))),
                      numberRHS);
          }
        es1s1:;
        } // End of DO block
    es1:;
    } // End of DO block
  // ELSE (1772)
  else
    // OLD_XREC = XREC_PTR + DISP; (1773)
    {
      int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mXREC_PTR),
                                          COREHALFWORD (mPUSH_HALMATxDISP)));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mPUSH_HALMATxOLD_XREC, bitRHS);
      bitRHS->inUse = 0;
    }
  // NEW_XREC = XREC_PTR + DISP; (1774)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mXREC_PTR),
                                        COREHALFWORD (mPUSH_HALMATxDISP)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPUSH_HALMATxNEW_XREC, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF NEW_XREC > DOUBLEBLOCK_SIZE THEN (1775)
  if (1 & (xGT (COREHALFWORD (mPUSH_HALMATxNEW_XREC), 3599)))
    // CALL ERRORS (CLASS_BI, 309); (1776)
    {
      putBITp (16, mERRORSxCLASS, getBIT (16, mCLASS_BI));
      putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(309)));
      ERRORS (0);
    }
  // IF STATISTICS THEN (1777)
  if (1 & (bitToFixed (getBIT (16, mSTATISTICS))))
    // IF ~FINAL_PUSH THEN (1778)
    if (1 & (xNOT (BYTE0 (mPUSH_HALMATxFINAL_PUSH))))
      // DO; (1779)
      {
      rs2:;
        // PUSH# = PUSH# + 1; (1780)
        {
          int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mPUSHp), 1));
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (16, mPUSHp, bitRHS);
          bitRHS->inUse = 0;
        }
        // PUSH_SIZE = PUSH_SIZE + XREC_PTR -START + 1; (1781)
        {
          int32_t numberRHS
              = (int32_t)(xadd (xsubtract (xadd (getFIXED (mPUSH_SIZE),
                                                 COREHALFWORD (mXREC_PTR)),
                                           COREHALFWORD (mPUSH_HALMATxSTART)),
                                1));
          putFIXED (mPUSH_SIZE, numberRHS);
        }
      es2:;
      } // End of DO block
  // DO FOR TEMP = 0 TO XREC_PTR - START; (1782)
  {
    int32_t from45, to45, by45;
    from45 = 0;
    to45 = xsubtract (COREHALFWORD (mXREC_PTR),
                      COREHALFWORD (mPUSH_HALMATxSTART));
    by45 = 1;
    for (putBIT (16, mPUSH_HALMATxTEMP, fixedToBit (16, from45));
         bitToFixed (getBIT (16, mPUSH_HALMATxTEMP)) <= to45;
         putBIT (16, mPUSH_HALMATxTEMP,
                 fixedToBit (16, bitToFixed (getBIT (16, mPUSH_HALMATxTEMP))
                                     + by45)))
      {
        // I = XREC_PTR - TEMP; (1783)
        {
          int32_t numberRHS = (int32_t)(xsubtract (
              COREHALFWORD (mXREC_PTR), COREHALFWORD (mPUSH_HALMATxTEMP)));
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (16, mPUSH_HALMATxI, bitRHS);
          bitRHS->inUse = 0;
        }
        // J = NEW_XREC - TEMP; (1784)
        {
          int32_t numberRHS
              = (int32_t)(xsubtract (COREHALFWORD (mPUSH_HALMATxNEW_XREC),
                                     COREHALFWORD (mPUSH_HALMATxTEMP)));
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (16, mPUSH_HALMATxJ, bitRHS);
          bitRHS->inUse = 0;
        }
        // IF VAC_OR_XPT(I) & (~((OPR(I)& 12) =  12) OR FINAL_PUSH OR EXTN_CHK)
        // THEN (1785)
        if (1
            & (xAND (
                bitToFixed ((
                    putBITp (16, mVAC_OR_XPTxPTR, getBIT (16, mPUSH_HALMATxI)),
                    VAC_OR_XPT (0))),
                xOR (xOR (xNOT (xEQ (
                              xAND (getFIXED (
                                        mOPR
                                        + 4 * COREHALFWORD (mPUSH_HALMATxI)),
                                    12),
                              12)),
                          BYTE0 (mPUSH_HALMATxFINAL_PUSH)),
                     BYTE0 (mPUSH_HALMATxEXTN_CHK)))))
          // DO; (1786)
          {
          rs3s1:;
            // PTR2, PTR = SHR(OPR(I),16); (1787)
            {
              int32_t numberRHS = (int32_t)(SHR (
                  getFIXED (mOPR + 4 * COREHALFWORD (mPUSH_HALMATxI)), 16));
              descriptor_t *bitRHS;
              bitRHS = fixedToBit (32, (int32_t)(numberRHS));
              putBIT (16, mPUSH_HALMATxPTR2, bitRHS);
              bitRHS = fixedToBit (32, (int32_t)(numberRHS));
              putBIT (16, mPUSH_HALMATxPTR, bitRHS);
              bitRHS->inUse = 0;
            }
            // IF PTR >= START THEN (1788)
            if (1
                & (xGE (COREHALFWORD (mPUSH_HALMATxPTR),
                        COREHALFWORD (mPUSH_HALMATxSTART))))
              // PTR = PTR + DISP; (1789)
              {
                int32_t numberRHS
                    = (int32_t)(xadd (COREHALFWORD (mPUSH_HALMATxPTR),
                                      COREHALFWORD (mPUSH_HALMATxDISP)));
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16, mPUSH_HALMATxPTR, bitRHS);
                bitRHS->inUse = 0;
              }
            // IF FINAL_PUSH THEN (1790)
            if (1 & (bitToFixed (getBIT (8, mPUSH_HALMATxFINAL_PUSH))))
              // IF PTR > BLOCK_END THEN (1791)
              if (1 & (xGT (COREHALFWORD (mPUSH_HALMATxPTR), 1799)))
                // PTR = PTR - BLOCK_PLUS_1; (1792)
                {
                  int32_t numberRHS = (int32_t)(xsubtract (
                      COREHALFWORD (mPUSH_HALMATxPTR), 1800));
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (16, mPUSH_HALMATxPTR, bitRHS);
                  bitRHS->inUse = 0;
                }
              // ELSE (1793)
              else
                // DO; (1794)
                {
                rs3s1s1:;
                  // IF (OPR(PTR) & ACROSS_BLOCK_TAG) = 0 THEN (1795)
                  if (1
                      & (xEQ (xAND (getFIXED (
                                        mOPR
                                        + 4 * COREHALFWORD (mPUSH_HALMATxPTR)),
                                    getFIXED (mACROSS_BLOCK_TAG)),
                              0)))
                    // CROSS_BLOCK_OPERATORS = CROSS_BLOCK_OPERATORS + 1;
                    // (1796)
                    {
                      int32_t numberRHS = (int32_t)(xadd (
                          COREHALFWORD (mCROSS_BLOCK_OPERATORS), 1));
                      descriptor_t *bitRHS;
                      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                      putBIT (16, mCROSS_BLOCK_OPERATORS, bitRHS);
                      bitRHS->inUse = 0;
                    }
                  // OPR(PTR) = OPR(PTR) | ACROSS_BLOCK_TAG; (1797)
                  {
                    int32_t numberRHS = (int32_t)(xOR (
                        getFIXED (mOPR + 4 * COREHALFWORD (mPUSH_HALMATxPTR)),
                        getFIXED (mACROSS_BLOCK_TAG)));
                    putFIXED (mOPR + 4 * (COREHALFWORD (mPUSH_HALMATxPTR)),
                              numberRHS);
                  }
                  // PTR = PTR |  32768; (1798)
                  {
                    int32_t numberRHS = (int32_t)(xOR (
                        COREHALFWORD (mPUSH_HALMATxPTR), 32768));
                    descriptor_t *bitRHS;
                    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                    putBIT (16, mPUSH_HALMATxPTR, bitRHS);
                    bitRHS->inUse = 0;
                  }
                es3s1s1:;
                } // End of DO block
            // OPR(J) = SHL(PTR,16) | OPR(I) &  65535; (1799)
            {
              int32_t numberRHS = (int32_t)(xOR (
                  SHL (COREHALFWORD (mPUSH_HALMATxPTR), 16),
                  xAND (getFIXED (mOPR + 4 * COREHALFWORD (mPUSH_HALMATxI)),
                        65535)));
              putFIXED (mOPR + 4 * (COREHALFWORD (mPUSH_HALMATxJ)), numberRHS);
            }
            // IF EXTN_CHK THEN (1800)
            if (1 & (bitToFixed (getBIT (8, mPUSH_HALMATxEXTN_CHK))))
              // IF PTR <= BLOCK_END THEN (1801)
              if (1 & (xLE (COREHALFWORD (mPUSH_HALMATxPTR), 1799)))
                // IF J > BLOCK_END THEN (1802)
                if (1 & (xGT (COREHALFWORD (mPUSH_HALMATxJ), 1799)))
                  // IF OPOP(PTR2) = EXTN THEN (1803)
                  if (1
                      & (xEQ (bitToFixed (
                                  (putBITp (16, mOPOPxPTR,
                                            getBIT (16, mPUSH_HALMATxPTR2)),
                                   OPOP (0))),
                              COREHALFWORD (mEXTN))))
                    // DO; (1804)
                    {
                    rs3s1s2:;
                      // RET = TRUE; (1805)
                      {
                        int32_t numberRHS = (int32_t)(1);
                        descriptor_t *bitRHS;
                        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                        putBIT (8, mPUSH_HALMATxRET, bitRHS);
                        bitRHS->inUse = 0;
                      }
                      // CALL BUMP_D_N(J - I + LAST_OP(I),NO_OPERANDS(PTR2) +
                      // 1); (1806)
                      {
                        putBITp (
                            16, mBUMP_D_NxPTR,
                            fixedToBit (
                                32,
                                (int32_t)(xadd (
                                    xsubtract (COREHALFWORD (mPUSH_HALMATxJ),
                                               COREHALFWORD (mPUSH_HALMATxI)),
                                    (putBITp (16, mLAST_OPxPTR,
                                              getBIT (16, mPUSH_HALMATxI)),
                                     LAST_OP (0))))));
                        putBITp (
                            16, mBUMP_D_NxD_PTR,
                            fixedToBit (
                                32,
                                (int32_t)(xadd (
                                    bitToFixed (
                                        (putBITp (
                                             16, mNO_OPERANDSxPTR,
                                             getBIT (16, mPUSH_HALMATxPTR2)),
                                         NO_OPERANDS (0))),
                                    1))));
                        BUMP_D_N (0);
                      }
                      // IF TRACE THEN (1807)
                      if (1 & (bitToFixed (getBIT (8, mTRACE))))
                        // OUTPUT = '   J=' || J || ', I=' || I|| ',
                        // LAST_OP(I)=' || LAST_OP(I) || ', OPR(PTR2)=' ||
                        // HEX(OPR(PTR2),8); (1808)
                        {
                          descriptor_t *stringRHS;
                          stringRHS = xsCAT (
                              xsCAT (
                                  xsCAT (
                                      xsCAT (
                                          xsCAT (
                                              xsCAT (
                                                  xsCAT (
                                                      cToDescriptor (NULL,
                                                                     "   J="),
                                                      bitToCharacter (getBIT (
                                                          16,
                                                          mPUSH_HALMATxJ))),
                                                  cToDescriptor (NULL,
                                                                 ", I=")),
                                              bitToCharacter (getBIT (
                                                  16, mPUSH_HALMATxI))),
                                          cToDescriptor (NULL,
                                                         ", LAST_OP(I)=")),
                                      fixedToCharacter (
                                          (putBITp (
                                               16, mLAST_OPxPTR,
                                               getBIT (16, mPUSH_HALMATxI)),
                                           LAST_OP (0)))),
                                  cToDescriptor (NULL, ", OPR(PTR2)=")),
                              (putFIXED (
                                   mHEXxHVAL,
                                   getFIXED (mOPR
                                             + 4
                                                   * COREHALFWORD (
                                                       mPUSH_HALMATxPTR2))),
                               putFIXED (mHEXxN, 8), HEX (0)));
                          OUTPUT (0, stringRHS);
                          stringRHS->inUse = 0;
                        }
                      // ADD(D_N_INX) = J; (1809)
                      {
                        descriptor_t *bitRHS = getBIT (16, mPUSH_HALMATxJ);
                        putBIT (16, mADD + 2 * (COREHALFWORD (mD_N_INX)),
                                bitRHS);
                        bitRHS->inUse = 0;
                      }
                      // TOTAL = TOTAL + DIFF_PTR(D_N_INX); (1810)
                      {
                        int32_t numberRHS = (int32_t)(xadd (
                            COREHALFWORD (mTOTAL),
                            COREHALFWORD (mDIFF_PTR
                                          + 2 * COREHALFWORD (mD_N_INX))));
                        descriptor_t *bitRHS;
                        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                        putBIT (16, mTOTAL, bitRHS);
                        bitRHS->inUse = 0;
                      }
                    es3s1s2:;
                    } // End of DO block
          es3s1:;
          } // End of DO block
        // ELSE (1811)
        else
          // DO; (1812)
          {
          rs3s2:;
            // OPR(J) = OPR(I); (1813)
            {
              int32_t numberRHS = (int32_t)(getFIXED (
                  mOPR + 4 * COREHALFWORD (mPUSH_HALMATxI)));
              putFIXED (mOPR + 4 * (COREHALFWORD (mPUSH_HALMATxJ)), numberRHS);
            }
          es3s2:;
          } // End of DO block
        // IF ~FINAL_PUSH AND ~EXTN_CHK THEN (1814)
        if (1
            & (xAND (xNOT (BYTE0 (mPUSH_HALMATxFINAL_PUSH)),
                     xNOT (BYTE0 (mPUSH_HALMATxEXTN_CHK)))))
          // DO; (1815)
          {
          rs3s3:;
            // FLAG(J) = FLAG(I); (1816)
            {
              descriptor_t *bitRHS
                  = getBIT (8, mFLAG + 1 * COREHALFWORD (mPUSH_HALMATxI));
              putBIT (8, mFLAG + 1 * (COREHALFWORD (mPUSH_HALMATxJ)), bitRHS);
              bitRHS->inUse = 0;
            }
            // IF (OPR(J) &  12) =  12 THEN (1817)
            if (1
                & (xEQ (
                    xAND (getFIXED (mOPR + 4 * COREHALFWORD (mPUSH_HALMATxJ)),
                          12),
                    12)))
              // CALL ENTER(J); (1818)
              {
                putBITp (16, mENTERxPTR, getBIT (16, mPUSH_HALMATxJ));
                ENTER (0);
              }
          es3s3:;
          } // End of DO block
      }
  } // End of DO for-loop block
  // DO FOR TEMP = START TO START + DISP - 1; (1819)
  {
    int32_t from46, to46, by46;
    from46 = bitToFixed (getBIT (16, mPUSH_HALMATxSTART));
    to46 = xsubtract (xadd (COREHALFWORD (mPUSH_HALMATxSTART),
                            COREHALFWORD (mPUSH_HALMATxDISP)),
                      1);
    by46 = 1;
    for (putBIT (16, mPUSH_HALMATxTEMP, fixedToBit (16, from46));
         bitToFixed (getBIT (16, mPUSH_HALMATxTEMP)) <= to46;
         putBIT (16, mPUSH_HALMATxTEMP,
                 fixedToBit (16, bitToFixed (getBIT (16, mPUSH_HALMATxTEMP))
                                     + by46)))
      {
        // OPR(TEMP) = 0; (1820)
        {
          int32_t numberRHS = (int32_t)(0);
          putFIXED (mOPR + 4 * (COREHALFWORD (mPUSH_HALMATxTEMP)), numberRHS);
        }
      }
  } // End of DO for-loop block
  // IF ~FINAL_PUSH THEN (1821)
  if (1 & (xNOT (BYTE0 (mPUSH_HALMATxFINAL_PUSH))))
    // CALL MOVE_LIMB(START,XREC_PTR + 1,DISP,1,0,EXTN_CHK); (1822)
    {
      putBITp (16, mMOVE_LIMBxLOW, getBIT (16, mPUSH_HALMATxSTART));
      putBITp (16, mMOVE_LIMBxHIGH,
               fixedToBit (32, (int32_t)(xadd (COREHALFWORD (mXREC_PTR), 1))));
      putBITp (16, mMOVE_LIMBxBIG, getBIT (16, mPUSH_HALMATxDISP));
      putBITp (8, mMOVE_LIMBxQUICK_MOVE, fixedToBit (32, (int32_t)(1)));
      putBITp (8, mMOVE_LIMBxRELOCATE_ADD, fixedToBit (32, (int32_t)(0)));
      putBITp (8, mMOVE_LIMBxKEEP_D_N, getBIT (8, mPUSH_HALMATxEXTN_CHK));
      MOVE_LIMB (0);
    }
  // ELSE (1823)
  else
    // DO; (1824)
    {
    rs5:;
      // IF CROSS_BLOCK_OPERATORS > CROSS_BLOCK_OPERATORS(1) THEN (1825)
      if (1
          & (xGT (COREHALFWORD (mCROSS_BLOCK_OPERATORS),
                  COREHALFWORD (mCROSS_BLOCK_OPERATORS + 2 * 1))))
        // CROSS_BLOCK_OPERATORS(1) = CROSS_BLOCK_OPERATORS; (1826)
        {
          descriptor_t *bitRHS = getBIT (16, mCROSS_BLOCK_OPERATORS);
          putBIT (16, mCROSS_BLOCK_OPERATORS + 2 * (1), bitRHS);
          bitRHS->inUse = 0;
        }
      // CROSS_BLOCK_OPERATORS = 0; (1827)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mCROSS_BLOCK_OPERATORS, bitRHS);
        bitRHS->inUse = 0;
      }
    es5:;
    } // End of DO block
  // DSUB_LOC = RELO(DSUB_LOC); (1828)
  {
    descriptor_t *bitRHS
        = (putBITp (16, mPUSH_HALMATxRELOxPTR, getBIT (16, mDSUB_LOC)),
           PUSH_HALMATxRELO (0));
    putBIT (16, mDSUB_LOC, bitRHS);
    bitRHS->inUse = 0;
  }
  // DSUB_INX = RELO(DSUB_INX); (1829)
  {
    descriptor_t *bitRHS
        = (putBITp (16, mPUSH_HALMATxRELOxPTR, getBIT (16, mDSUB_INX)),
           PUSH_HALMATxRELO (0));
    putBIT (16, mDSUB_INX, bitRHS);
    bitRHS->inUse = 0;
  }
  // FINAL_PUSH = 0; (1830)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mPUSH_HALMATxFINAL_PUSH, bitRHS);
    bitRHS->inUse = 0;
  }
  // EXTN_CHK = 0; (1831)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mPUSH_HALMATxEXTN_CHK, bitRHS);
    bitRHS->inUse = 0;
  }
  // XREC_PTR = OLD_XREC; (1832)
  {
    descriptor_t *bitRHS = getBIT (16, mPUSH_HALMATxOLD_XREC);
    putBIT (16, mXREC_PTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // RETURN RET; (1833)
  {
    reentryGuard = 0;
    return getBIT (8, mPUSH_HALMATxRET);
  }
}
