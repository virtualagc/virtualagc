/*
  File HEX.c generated by XCOM-I, 2024-08-09 12:40:41.
*/

#include "runtimeC.h"

descriptor_t *
HEX (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "HEX");
  // CHAR_TEMP = ''; (1104)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "");
    putCHARACTER (mHEXxCHAR_TEMP, stringRHS);
    stringRHS->inUse = 0;
  }
// H_LOOP: (1105)
H_LOOP:
  // CHAR_TEMP = SUBSTR('0123456789ABCDEF', NUM &  15, 1) || CHAR_TEMP; (1106)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (SUBSTR (cToDescriptor (NULL, "0123456789ABCDEF"),
                               xAND (getFIXED (mHEXxNUM), 15), 1),
                       getCHARACTER (mHEXxCHAR_TEMP));
    putCHARACTER (mHEXxCHAR_TEMP, stringRHS);
    stringRHS->inUse = 0;
  }
  // NUM = SHR(NUM, 4); (1107)
  {
    int32_t numberRHS = (int32_t)(SHR (getFIXED (mHEXxNUM), 4));
    putFIXED (mHEXxNUM, numberRHS);
  }
  // IF NUM ~= 0 THEN (1108)
  if (1 & (xNEQ (getFIXED (mHEXxNUM), 0)))
    // GO TO H_LOOP; (1109)
    goto H_LOOP;
  // NUM = LENGTH(CHAR_TEMP); (1110)
  {
    int32_t numberRHS = (int32_t)(LENGTH (getCHARACTER (mHEXxCHAR_TEMP)));
    putFIXED (mHEXxNUM, numberRHS);
  }
  // IF NUM < WIDTH THEN (1111)
  if (1 & (xLT (getFIXED (mHEXxNUM), getFIXED (mHEXxWIDTH))))
    // CHAR_TEMP = SUBSTR(X256, 0, WIDTH - NUM) || CHAR_TEMP; (1112)
    {
      descriptor_t *stringRHS;
      stringRHS = xsCAT (
          SUBSTR (getCHARACTER (mX256), 0,
                  xsubtract (getFIXED (mHEXxWIDTH), getFIXED (mHEXxNUM))),
          getCHARACTER (mHEXxCHAR_TEMP));
      putCHARACTER (mHEXxCHAR_TEMP, stringRHS);
      stringRHS->inUse = 0;
    }
  // WIDTH = 0; (1113)
  {
    int32_t numberRHS = (int32_t)(0);
    putFIXED (mHEXxWIDTH, numberRHS);
  }
  // RETURN CHAR_TEMP; (1114)
  {
    reentryGuard = 0;
    return getCHARACTER (mHEXxCHAR_TEMP);
  }
}
