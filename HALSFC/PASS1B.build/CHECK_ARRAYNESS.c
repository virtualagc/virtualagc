/*
  File CHECK_ARRAYNESS.c generated by XCOM-I, 2024-08-09 12:40:41.
*/

#include "runtimeC.h"

int32_t
CHECK_ARRAYNESS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "CHECK_ARRAYNESS");
  // I=CURRENT_ARRAYNESS>0; (7325)
  {
    int32_t numberRHS = (int32_t)(xGT (COREHALFWORD (mCURRENT_ARRAYNESS), 0));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mCHECK_ARRAYNESSxI, bitRHS);
    bitRHS->inUse = 0;
  }
  // CURRENT_ARRAYNESS,ARRAYNESS_FLAG=0; (7326)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mCURRENT_ARRAYNESS, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mARRAYNESS_FLAG, bitRHS);
    bitRHS->inUse = 0;
  }
  // RETURN I; (7327)
  {
    reentryGuard = 0;
    return COREHALFWORD (mCHECK_ARRAYNESSxI);
  }
}
