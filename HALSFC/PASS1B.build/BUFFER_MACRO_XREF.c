/*
  File BUFFER_MACRO_XREF.c generated by XCOM-I, 2024-08-09 12:40:41.
*/

#include "runtimeC.h"

int32_t
BUFFER_MACRO_XREF (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "BUFFER_MACRO_XREF");
  // MAC_CTR=MAC_CTR+1; (4867)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mMAC_CTR), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mMAC_CTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // MAC_XREF(MAC_CTR)=I; (4868)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mBUFFER_MACRO_XREFxI));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mMAC_XREF + 2 * (COREHALFWORD (mMAC_CTR)), bitRHS);
    bitRHS->inUse = 0;
  }
  // RETURN; (4869)
  {
    reentryGuard = 0;
    return 0;
  }
}
