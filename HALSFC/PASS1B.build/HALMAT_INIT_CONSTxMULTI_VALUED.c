/*
  File HALMAT_INIT_CONSTxMULTI_VALUED.c generated by XCOM-I, 2024-08-09
  12:40:41.
*/

#include "runtimeC.h"

descriptor_t *
HALMAT_INIT_CONSTxMULTI_VALUED (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "HALMAT_INIT_CONSTxMULTI_VALUED");
  // IF NAME_IMPLIED THEN (9142)
  if (1 & (bitToFixed (getBIT (1, mNAME_IMPLIED))))
    // RETURN 2; (9143)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(2));
    }
  // IF SYT_ARRAY(ID_LOC)~=0 THEN (9144)
  if (1
      & (xNEQ (COREHALFWORD (getFIXED (mSYM_TAB) + 34 * (getFIXED (mID_LOC))
                             + 20 + 2 * (0)),
               0)))
    // RETURN TRUE; (9145)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(1));
    }
  // IF SYT_TYPE(ID_LOC)<CHAR_TYPE THEN (9146)
  if (1
      & (xLT (BYTE0 (getFIXED (mSYM_TAB) + 34 * (getFIXED (mID_LOC)) + 32
                     + 1 * (0)),
              2)))
    // RETURN 2; (9147)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(2));
    }
  // RETURN SHR(MONO_VAL,SYT_TYPE(ID_LOC))&1; (9148)
  {
    reentryGuard = 0;
    return fixedToBit (
        32, (int32_t)(xAND (
                SHR (COREHALFWORD (mHALMAT_INIT_CONSTxMULTI_VALUEDxMONO_VAL),
                     BYTE0 (getFIXED (mSYM_TAB) + 34 * (getFIXED (mID_LOC))
                            + 32 + 1 * (0))),
                1)));
  }
}
