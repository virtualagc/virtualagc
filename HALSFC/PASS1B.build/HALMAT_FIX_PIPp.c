/*
  File HALMAT_FIX_PIPp.c generated by XCOM-I, 2024-08-09 12:40:41.
*/

#include "runtimeC.h"

int32_t
HALMAT_FIX_PIPp (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "HALMAT_FIX_PIPp");
  // IF HALMAT_OK THEN (7242)
  if (1 & (bitToFixed (getBIT (1, mHALMAT_OK))))
    // ATOMS(POP_LOC)=(ATOMS(POP_LOC)& 4278255615)| SHL(PIP#,16); (7243)
    {
      int32_t numberRHS = (int32_t)(xOR (
          xAND (getFIXED (getFIXED (mFOR_ATOMS)
                          + 4 * (getFIXED (mHALMAT_FIX_PIPpxPOP_LOC)) + 0
                          + 4 * (0)),
                4278255615),
          SHL (BYTE0 (mHALMAT_FIX_PIPpxPIPp), 16)));
      putFIXED (getFIXED (mFOR_ATOMS)
                    + 4 * (getFIXED (mHALMAT_FIX_PIPpxPOP_LOC)) + 0 + 4 * (0),
                numberRHS);
    }
  {
    reentryGuard = 0;
    return 0;
  }
}
