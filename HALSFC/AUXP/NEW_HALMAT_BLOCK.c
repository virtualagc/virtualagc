/*
  File NEW_HALMAT_BLOCK.c generated by XCOM-I, 2024-08-08 04:32:08.
*/

#include "../AUXP/runtimeC.h"

int32_t
NEW_HALMAT_BLOCK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "NEW_HALMAT_BLOCK");
  // IF DO_PRINT & PRETTY_PRINT_REQUESTED THEN (983)
  if (1
      & (xAND (BYTE0 (mNEW_HALMAT_BLOCKxDO_PRINT),
               BYTE0 (mPRETTY_PRINT_REQUESTED))))
    // CALL PRETTY_PRINT_MAT; (984)
    PRETTY_PRINT_MAT (0);
  // HALMAT(START) = FILE(HALMAT_FILE, CURR_HALMAT_BLOCK); (985)
  {
    rFILE (ADDR (NULL, 0, "HALMAT", COREHALFWORD (mNEW_HALMAT_BLOCKxSTART)),
           BYTE0 (mHALMAT_FILE), COREHALFWORD (mCURR_HALMAT_BLOCK));
  }
  // CURR_HALMAT_BLOCK = CURR_HALMAT_BLOCK + 1; (986)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mCURR_HALMAT_BLOCK), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mCURR_HALMAT_BLOCK, bitRHS);
    bitRHS->inUse = 0;
  }
  // HALMAT_PTR = START; (987)
  {
    descriptor_t *bitRHS = getBIT (16, mNEW_HALMAT_BLOCKxSTART);
    putBIT (16, mHALMAT_PTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // FIRST_PRINT = ON; (988)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mFIRST_PRINT, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
