/*
  File ERRORS.c generated by XCOM-I, 2024-08-09 12:39:13.
*/

#include "runtimeC.h"

int32_t
ERRORS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "ERRORS");
  // IF ~HEADER_ISSUED THEN (970)
  if (1 & (xNOT (BYTE0 (mHEADER_ISSUED))))
    // DO; (971)
    {
    rs1:;
      // CALL PRINT_PHASE_HEADER; (972)
      PRINT_PHASE_HEADER (0);
      // HEADER_ISSUED = ON; (973)
      {
        int32_t numberRHS = (int32_t)(1);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mHEADER_ISSUED, bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // OUTPUT = ''; (974)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "");
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // ERROR# = ERROR# + 1; (975)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mERRORSxERRORp), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mERRORSxERRORp, bitRHS);
    bitRHS->inUse = 0;
  }
  // SEVERITY = COMMON_ERRORS (CLASS, NUM, TEXT, ERROR#, CURRENT_STMT); (976)
  {
    int32_t numberRHS = (int32_t)((
        putBITp (16, mCOMMON_ERRORSxCLASS, getBIT (16, mERRORSxCLASS)),
        putBITp (16, mCOMMON_ERRORSxNUM, getBIT (16, mERRORSxNUM)),
        putCHARACTERp (mCOMMON_ERRORSxTEXT, getCHARACTER (mERRORSxTEXT)),
        putBITp (16, mCOMMON_ERRORSxERRORp, getBIT (16, mERRORSxERRORp)),
        putFIXED (mCOMMON_ERRORSxSTMTp, COREHALFWORD (mCURRENT_STMT)),
        COMMON_ERRORS (0)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mERRORSxSEVERITY, bitRHS);
    bitRHS->inUse = 0;
  }
  // OUTPUT = AST || 'HALMAT LINE = ' || HALMAT_PTR || COMMA || ' HALMAT BLOCK
  // = ' || CURR_HALMAT_BLOCK - 1 || AST; (977)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (
        xsCAT (xsCAT (xsCAT (xsCAT (xsCAT (getCHARACTER (mERRORSxAST),
                                           cToDescriptor (NULL,
                                                          "HALMAT LINE = ")),
                                    bitToCharacter (getBIT (16, mHALMAT_PTR))),
                             getCHARACTER (mCOMMA)),
                      cToDescriptor (NULL, " HALMAT BLOCK = ")),
               fixedToCharacter (
                   xsubtract (COREHALFWORD (mCURR_HALMAT_BLOCK), 1))),
        getCHARACTER (mERRORSxAST));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // IF SEVERITY > MAX_SEVERITY THEN (978)
  if (1
      & (xGT (COREHALFWORD (mERRORSxSEVERITY), COREHALFWORD (mMAX_SEVERITY))))
    // MAX_SEVERITY = SEVERITY; (979)
    {
      descriptor_t *bitRHS = getBIT (16, mERRORSxSEVERITY);
      putBIT (16, mMAX_SEVERITY, bitRHS);
      bitRHS->inUse = 0;
    }
  // COMMON_RETURN_CODE = SHL(MAX_SEVERITY,2); (980)
  {
    int32_t numberRHS = (int32_t)(SHL (COREHALFWORD (mMAX_SEVERITY), 2));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mCOMMON_RETURN_CODE, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF SEVERITY > = 2 THEN (981)
  if (1 & (xGE (COREHALFWORD (mERRORSxSEVERITY), 2)))
    // GO TO BUMMER; (982)
    {
      resetAllReentryGuards ();
      longjmp (jbBUMMER, 1);
    }
  {
    reentryGuard = 0;
    return 0;
  }
}
