/*
  File PRINT_PHASE_HEADER.c generated by XCOM-I, 2024-08-08 04:32:08.
*/

#include "../AUXP/runtimeC.h"

int32_t
PRINT_PHASE_HEADER (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PRINT_PHASE_HEADER");
  // OUTPUT(1) = '1'; (692)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "1");
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // HEADER_ISSUED = 1; (693)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mHEADER_ISSUED, bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL PRINT_DATE_AND_TIME(' HAL/S AUXILIARY HALMAT GENERATOR -- VERSION OF
  // ', DATE_OF_GENERATION, TIME_OF_GENERATION); (694)
  {
    putCHARACTERp (
        mPRINT_DATE_AND_TIMExMESSAGE,
        cToDescriptor (NULL,
                       " HAL/S AUXILIARY HALMAT GENERATOR -- VERSION OF "));
    putFIXED (mPRINT_DATE_AND_TIMExD, DATE_OF_GENERATION ());
    putFIXED (mPRINT_DATE_AND_TIMExT, TIME_OF_GENERATION ());
    PRINT_DATE_AND_TIME (0);
  }
  // OUTPUT = ''; (695)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "");
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // CALL PRINT_DATE_AND_TIME(' HAL/S AUXILIARY HALMAT GENERATOR ENTERED ',
  // DATE, TIME); (696)
  {
    putCHARACTERp (
        mPRINT_DATE_AND_TIMExMESSAGE,
        cToDescriptor (NULL, " HAL/S AUXILIARY HALMAT GENERATOR ENTERED "));
    putFIXED (mPRINT_DATE_AND_TIMExD, DATE ());
    putFIXED (mPRINT_DATE_AND_TIMExT, TIME ());
    PRINT_DATE_AND_TIME (0);
  }
  // OUTPUT = ''; (697)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "");
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
