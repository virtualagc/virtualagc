/*
  File CHECK_SRS.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

descriptor_t *
CHECK_SRS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "CHECK_SRS");
  // IF IX ~= 0 THEN (1075)
  if (1 & (xNEQ (COREHALFWORD (mCHECK_SRSxIX), 0)))
    // RETURN RXTYPE; (1076)
    {
      reentryGuard = 0;
      return getBIT (8, mRXTYPE);
    }
  // IF BASE = PROGBASE THEN (1077)
  if (1 & (xEQ (COREHALFWORD (mCHECK_SRSxBASE), BYTE0 (mPROGBASE))))
    // RETURN RXTYPE; (1078)
    {
      reentryGuard = 0;
      return getBIT (8, mRXTYPE);
    }
  // IF BASE = PRELBASE THEN (1079)
  if (1 & (xEQ (COREHALFWORD (mCHECK_SRSxBASE), BYTE0 (mPRELBASE))))
    // RETURN RXTYPE; (1080)
    {
      reentryGuard = 0;
      return getBIT (8, mRXTYPE);
    }
  // IF DISP < 0 | DISP > RANGE(INST>= 80 & INST< 128) THEN (1081)
  if (1
      & (xOR (xLT (getFIXED (mCHECK_SRSxDISP), 0),
              xGT (getFIXED (mCHECK_SRSxDISP),
                   COREHALFWORD (
                       mCHECK_SRSxRANGE
                       + 2
                             * xAND (xGE (COREHALFWORD (mCHECK_SRSxINST), 80),
                                     xLT (COREHALFWORD (mCHECK_SRSxINST),
                                          128)))))))
    // RETURN RXTYPE; (1082)
    {
      reentryGuard = 0;
      return getBIT (8, mRXTYPE);
    }
  // IF (AP101INST(INST)& 248) = 0 THEN (1083)
  if (1
      & (xEQ (
          xAND (COREHALFWORD (mAP101INST + 2 * COREHALFWORD (mCHECK_SRSxINST)),
                248),
          0)))
    // RETURN SRSTYPE; (1084)
    {
      reentryGuard = 0;
      return getBIT (8, mSRSTYPE);
    }
  // RETURN RXTYPE; (1085)
  {
    reentryGuard = 0;
    return getBIT (8, mRXTYPE);
  }
}
