/*
  File PRINTSUMMARYxSTACK_DUMP.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

int32_t
PRINTSUMMARYxSTACK_DUMP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PRINTSUMMARYxSTACK_DUMP");
  // OUTPUT(1) = '- STACK   NAME'; (14545)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "- STACK   NAME");
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // DO I = PROGPOINT TO PROCLIMIT; (14546)
  {
    int32_t from143, to143, by143;
    from143 = bitToFixed (getBIT (16, mPROGPOINT));
    to143 = bitToFixed (getBIT (16, mPROCLIMIT));
    by143 = 1;
    for (putBIT (16, mPRINTSUMMARYxSTACK_DUMPxI, fixedToBit (16, from143));
         bitToFixed (getBIT (16, mPRINTSUMMARYxSTACK_DUMPxI)) <= to143;
         putBIT (16, mPRINTSUMMARYxSTACK_DUMPxI,
                 fixedToBit (
                     16, bitToFixed (getBIT (16, mPRINTSUMMARYxSTACK_DUMPxI))
                             + by143)))
      {
        // OUTPUT = FORMAT(MAXTEMP(I),6)||X2||SYT_NAME(PROC_LEVEL(I)); (14547)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              xsCAT (
                  (putFIXED (
                       mFORMATxIVAL,
                       getFIXED (
                           mMAXTEMP
                           + 4 * COREHALFWORD (mPRINTSUMMARYxSTACK_DUMPxI))),
                   putFIXED (mFORMATxN, 6), FORMAT (0)),
                  getCHARACTER (mX2)),
              getCHARACTER (
                  getFIXED (mSYM_TAB)
                  + 34
                        * (COREHALFWORD (
                            mPROC_LEVEL
                            + 2 * COREHALFWORD (mPRINTSUMMARYxSTACK_DUMPxI)))
                  + 0 + 4 * (0)));
          OUTPUT (0, stringRHS);
          stringRHS->inUse = 0;
        }
      }
  } // End of DO for-loop block
  // DO I = 0 TO OPMAX; (14548)
  {
    int32_t from144, to144, by144;
    from144 = 0;
    to144 = 205;
    by144 = 1;
    for (putBIT (16, mPRINTSUMMARYxSTACK_DUMPxI, fixedToBit (16, from144));
         bitToFixed (getBIT (16, mPRINTSUMMARYxSTACK_DUMPxI)) <= to144;
         putBIT (16, mPRINTSUMMARYxSTACK_DUMPxI,
                 fixedToBit (
                     16, bitToFixed (getBIT (16, mPRINTSUMMARYxSTACK_DUMPxI))
                             + by144)))
      {
        // OBJECT_INSTRUCTIONS = OBJECT_INSTRUCTIONS + OPCOUNT(I); (14549)
        {
          int32_t numberRHS = (int32_t)(xadd (
              getFIXED (mCOMM + 4 * 21),
              COREHALFWORD (mOPCOUNT
                            + 2 * COREHALFWORD (mPRINTSUMMARYxSTACK_DUMPxI))));
          putFIXED (mCOMM + 4 * (21), numberRHS);
        }
      }
  } // End of DO for-loop block
  {
    reentryGuard = 0;
    return 0;
  }
}
