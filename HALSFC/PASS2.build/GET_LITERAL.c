/*
  File GET_LITERAL.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

int32_t
GET_LITERAL (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GET_LITERAL");
  // IF PTR = 0 THEN (1027)
  if (1 & (xEQ (getFIXED (mGET_LITERALxPTR), 0)))
    // RETURN 0; (1028)
    {
      reentryGuard = 0;
      return 0;
    }
  // IF PTR >= LITORG THEN (1029)
  if (1 & (xGE (getFIXED (mGET_LITERALxPTR), getFIXED (mLITORG))))
    // IF PTR < LITLIM THEN (1030)
    if (1 & (xLT (getFIXED (mGET_LITERALxPTR), getFIXED (mLITLIM))))
      // RETURN PTR - LITORG; (1031)
      {
        reentryGuard = 0;
        return xsubtract (getFIXED (mGET_LITERALxPTR), getFIXED (mLITORG));
      }
  // COUNT#GETL = COUNT#GETL + 1; (1032)
  {
    int32_t numberRHS = (int32_t)(xadd (getFIXED (mCOUNTpGETL), 1));
    putFIXED (mCOUNTpGETL, numberRHS);
  }
  // IF FLAG THEN (1033)
  if (1 & (bitToFixed (getBIT (1, mGET_LITERALxFLAG))))
    // FILE(LITFILE,CURLBLK)=LIT1(0); (1034)
    {
      lFILE (2, getFIXED (mCURLBLK), ADDR ("LIT_PG", 0, "LITERAL1", 0));
    }
  // CURLBLK = PTR / LITSIZ; (1035)
  {
    int32_t numberRHS = (int32_t)(xdivide (getFIXED (mGET_LITERALxPTR), 130));
    putFIXED (mCURLBLK, numberRHS);
  }
  // LITORG = CURLBLK * LITSIZ; (1036)
  {
    int32_t numberRHS = (int32_t)(xmultiply (getFIXED (mCURLBLK), 130));
    putFIXED (mLITORG, numberRHS);
  }
  // LITLIM = LITORG + LITSIZ; (1037)
  {
    int32_t numberRHS = (int32_t)(xadd (getFIXED (mLITORG), 130));
    putFIXED (mLITLIM, numberRHS);
  }
  // IF ~FLAG | CURLBLK <= LITMAX THEN (1038)
  if (1
      & (xOR (xNOT (BYTE0 (mGET_LITERALxFLAG)),
              xLE (getFIXED (mCURLBLK), getFIXED (mGET_LITERALxLITMAX)))))
    // LIT1(0)=FILE(LITFILE,CURLBLK); (1039)
    {
      rFILE (ADDR ("LIT_PG", 0, "LITERAL1", 0), 2, getFIXED (mCURLBLK));
    }
  // LITMAX = MAX(LITMAX, CURLBLK); (1040)
  {
    int32_t numberRHS
        = (int32_t)((putFIXED (mMAXxVAL1, getFIXED (mGET_LITERALxLITMAX)),
                     putFIXED (mMAXxVAL2, getFIXED (mCURLBLK)), MAX (0)));
    putFIXED (mGET_LITERALxLITMAX, numberRHS);
  }
  // RETURN PTR - LITORG; (1041)
  {
    reentryGuard = 0;
    return xsubtract (getFIXED (mGET_LITERALxPTR), getFIXED (mLITORG));
  }
}
