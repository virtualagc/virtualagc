/*
  File EMIT_NOP.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

int32_t
EMIT_NOP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "EMIT_NOP");
  // CALL EMITC(NOP, 0); (1167)
  {
    putBITp (16, mEMITCxTYPE, getBIT (8, mNOP));
    putBITp (16, mEMITCxINST, fixedToBit (32, (int32_t)(0)));
    EMITC (0);
  }
  // CALL EMITW(0, 1); (1168)
  {
    putFIXED (mEMITWxDATA, 0);
    putBITp (1, mEMITWxMODIFIER, fixedToBit (32, (int32_t)(1)));
    EMITW (0);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
