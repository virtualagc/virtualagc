/*
  File OBJECT_CONDENSER.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

int32_t
OBJECT_CONDENSER (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "OBJECT_CONDENSER");
  // CALL PASS1_OBJ_CONDENSER; (14658)
  OBJECT_CONDENSERxPASS1_OBJ_CONDENSER (0);
  // DO CURRENT_ESDID = PROGPOINT TO PROCLIMIT; (14659)
  {
    int32_t from148, to148, by148;
    from148 = bitToFixed (getBIT (16, mPROGPOINT));
    to148 = bitToFixed (getBIT (16, mPROCLIMIT));
    by148 = 1;
    for (putFIXED (mCURRENT_ESDID, from148);
         getFIXED (mCURRENT_ESDID) <= to148;
         putFIXED (mCURRENT_ESDID, getFIXED (mCURRENT_ESDID) + by148))
      {
        // CALL BRANCH_CONDENSE; (14660)
        OBJECT_CONDENSERxBRANCH_CONDENSE (0);
      }
  } // End of DO for-loop block
  {
    reentryGuard = 0;
    return 0;
  }
}
