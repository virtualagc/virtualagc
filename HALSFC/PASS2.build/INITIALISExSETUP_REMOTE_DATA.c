/*
  File INITIALISExSETUP_REMOTE_DATA.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

int32_t
INITIALISExSETUP_REMOTE_DATA (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "INITIALISExSETUP_REMOTE_DATA");
  // IF REMOTE_LEVEL THEN (2064)
  if (1 & (bitToFixed (getBIT (8, mREMOTE_LEVEL))))
    // DO; (2065)
    {
    rs1:;
      // ESD_MAX, REMOTE_LEVEL, REMOTE_LEVEL(DATABASE) = ESD_MAX + 1; (2066)
      {
        int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mESD_MAX), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mESD_MAX, bitRHS);
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (8, mREMOTE_LEVEL, bitRHS);
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (8, mREMOTE_LEVEL + 1 * (COREHALFWORD (mDATABASE)), bitRHS);
        bitRHS->inUse = 0;
      }
      // CALL ENTER_ESD(PROGNAME(SELFNAMELOC, 11), ESD_MAX, 0); (2067)
      {
        putCHARACTERp (
            mENTER_ESDxNAME,
            (putBITp (16, mPROGNAMExENTRY, getBIT (16, mSELFNAMELOC)),
             putBITp (16, mPROGNAMExFLAG, fixedToBit (32, (int32_t)(11))),
             PROGNAME (0)));
        putBITp (16, mENTER_ESDxPTR, getBIT (16, mESD_MAX));
        putBITp (16, mENTER_ESDxTYP, fixedToBit (32, (int32_t)(0)));
        ENTER_ESD (0);
      }
    es1:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
