/*
  File GENERATExSAFE_INX.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

descriptor_t *
GENERATExSAFE_INX (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExSAFE_INX");
  // IF ~KNOWN_SYM(OP) THEN (3521)
  if (1
      & (xNOT (bitToFixed ((putBITp (16, mGENERATExKNOWN_SYMxOP,
                                     getBIT (16, mGENERATExSAFE_INXxOP)),
                            GENERATExKNOWN_SYM (0))))))
    // RETURN FALSE; (3522)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
  // IF INX(OP) = 0 THEN (3523)
  if (1
      & (xEQ (COREHALFWORD (getFIXED (mIND_STACK)
                            + 73 * (COREHALFWORD (mGENERATExSAFE_INXxOP)) + 34
                            + 2 * (0)),
              0)))
    // RETURN TRUE; (3524)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(1));
    }
  // IF INX(OP) < 0 THEN (3525)
  if (1
      & (xLT (COREHALFWORD (getFIXED (mIND_STACK)
                            + 73 * (COREHALFWORD (mGENERATExSAFE_INXxOP)) + 34
                            + 2 * (0)),
              0)))
    // RETURN FALSE; (3526)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
  // RETURN USAGE(INX(OP)) & 1; (3527)
  {
    reentryGuard = 0;
    return fixedToBit (
        32, (int32_t)(xAND (
                COREHALFWORD (
                    mUSAGE
                    + 2
                          * COREHALFWORD (
                              getFIXED (mIND_STACK)
                              + 73 * (COREHALFWORD (mGENERATExSAFE_INXxOP))
                              + 34 + 2 * (0))),
                1)));
  }
}
