/*
  File GENERATExCHECK_ASSIGN.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

int32_t
GENERATExCHECK_ASSIGN (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExCHECK_ASSIGN");
  // ASSIGN_PARM_FLAG,RECVR_OK,OK_TO_ASSIGN,NONPART,INX_OK=FALSE; (8855)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExASSIGN_PARM_FLAG, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExRECVR_OK, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExOK_TO_ASSIGN, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExNONPART, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExINX_OK, bitRHS);
    bitRHS->inUse = 0;
  }
  // ALL_FAILS=TRUE; (8856)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mGENERATExALL_FAILS, bitRHS);
    bitRHS->inUse = 0;
  }
  // ASNCTR, PTR = VECMAT_ASN(PTR); (8857)
  {
    descriptor_t *bitRHS = (putBITp (16, mGENERATExVECMAT_ASNxCTR,
                                     getBIT (16, mGENERATExCHECK_ASSIGNxPTR)),
                            GENERATExVECMAT_ASN (0));
    putBIT (16, mASNCTR, bitRHS);
    putBIT (16, mGENERATExCHECK_ASSIGNxPTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF PTR >= 0 THEN (8858)
  if (1 & (xGE (COREHALFWORD (mGENERATExCHECK_ASSIGNxPTR), 0)))
    // DO; (8859)
    {
    rs1:;
      // HOLDCTR=CTR; (8860)
      {
        descriptor_t *bitRHS = getBIT (16, mCTR);
        putBIT (16, mGENERATExCHECK_ASSIGNxHOLDCTR, bitRHS);
        bitRHS->inUse = 0;
      }
      // CTR=PTR; (8861)
      {
        descriptor_t *bitRHS = getBIT (16, mGENERATExCHECK_ASSIGNxPTR);
        putBIT (16, mCTR, bitRHS);
        bitRHS->inUse = 0;
      }
      // RECVR=MOD_GET_OPERAND(2); (8862)
      {
        descriptor_t *bitRHS = (putBITp (16, mGENERATExMOD_GET_OPERANDxOP,
                                         fixedToBit (32, (int32_t)(2))),
                                GENERATExMOD_GET_OPERAND (0));
        putBIT (16, mGENERATExRECVR, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF (~NAME_OP_FLAG & ~REMOTE_RECVR) THEN (8863)
      if (1
          & (xAND (xNOT (BYTE0 (mGENERATExNAME_OP_FLAG)),
                   xNOT (BYTE0 (mGENERATExREMOTE_RECVR)))))
        // IF ~SUBSTRUCT_FLAG THEN (8864)
        if (1 & (xNOT (BYTE0 (mGENERATExSUBSTRUCT_FLAG))))
          // IF DEL(RECVR)=0 THEN (8865)
          if (1
              & (xEQ (COREHALFWORD (getFIXED (mIND_STACK)
                                    + 73 * (COREHALFWORD (mGENERATExRECVR))
                                    + 28 + 2 * (0)),
                      0)))
            // DO; (8866)
            {
            rs1s1:;
              // RECVR_OK = TRUE ; (8867)
              {
                int32_t numberRHS = (int32_t)(1);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (1, mGENERATExRECVR_OK, bitRHS);
                bitRHS->inUse = 0;
              }
            es1s1:;
            } // End of DO block
      // IF RECVR_OK THEN (8868)
      if (1 & (bitToFixed (getBIT (1, mGENERATExRECVR_OK))))
        // DO; (8869)
        {
        rs1s2:;
          // CALL GET_RECVR_INFO(RECVR); (8870)
          {
            putBITp (16, mGENERATExGET_RECVR_INFOxRECVR,
                     getBIT (16, mGENERATExRECVR));
            GENERATExGET_RECVR_INFO (0);
          }
        es1s2:;
        } // End of DO block
      // IF ~VAC_FLAG THEN (8871)
      if (1 & (xNOT (BYTE0 (mGENERATExVAC_FLAG))))
        // CALL RETURN_STACK_ENTRY(RECVR); (8872)
        {
          putBITp (16, mGENERATExRETURN_STACK_ENTRYxP,
                   getBIT (16, mGENERATExRECVR));
          GENERATExRETURN_STACK_ENTRY (0);
        }
      // CTR=HOLDCTR; (8873)
      {
        descriptor_t *bitRHS = getBIT (16, mGENERATExCHECK_ASSIGNxHOLDCTR);
        putBIT (16, mCTR, bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // IF RECVR_OK THEN (8874)
  if (1 & (bitToFixed (getBIT (1, mGENERATExRECVR_OK))))
    // CALL CHECK_SRCE; (8875)
    GENERATExCHECK_SRCE (0);
  {
    reentryGuard = 0;
    return 0;
  }
}
