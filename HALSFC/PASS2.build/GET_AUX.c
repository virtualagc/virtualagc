/*
  File GET_AUX.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

descriptor_t *
GET_AUX (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GET_AUX");
  // IF CTR >= AUX_BASE THEN (992)
  if (1 & (xGE (getFIXED (mGET_AUXxCTR), getFIXED (mAUX_BASE))))
    // IF CTR < AUX_LIM THEN (993)
    if (1 & (xLT (getFIXED (mGET_AUXxCTR), getFIXED (mAUX_LIM))))
      // RETURN CTR - AUX_BASE; (994)
      {
        reentryGuard = 0;
        return fixedToBit (32, (int32_t)(xsubtract (getFIXED (mGET_AUXxCTR),
                                                    getFIXED (mAUX_BASE))));
      }
  // AUX_BLK = CTR / AUX_SIZE; (995)
  {
    int32_t numberRHS = (int32_t)(xdivide (getFIXED (mGET_AUXxCTR), 1800));
    putFIXED (mAUX_BLK, numberRHS);
  }
  // AUX_BASE = AUX_BLK * AUX_SIZE; (996)
  {
    int32_t numberRHS = (int32_t)(xmultiply (getFIXED (mAUX_BLK), 1800));
    putFIXED (mAUX_BASE, numberRHS);
  }
  // AUX = FILE(AUX_FILE, AUX_BLK); (997)
  {
    rFILE (ADDR (NULL, 0, "AUX", 0), BYTE0 (mAUX_FILE), getFIXED (mAUX_BLK));
  }
  // AUX_LIM = AUX_BASE + AUX_SIZE; (998)
  {
    int32_t numberRHS = (int32_t)(xadd (getFIXED (mAUX_BASE), 1800));
    putFIXED (mAUX_LIM, numberRHS);
  }
  // RETURN CTR - AUX_BASE; (999)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(xsubtract (getFIXED (mGET_AUXxCTR),
                                                getFIXED (mAUX_BASE))));
  }
}
