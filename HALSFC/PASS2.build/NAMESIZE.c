/*
  File NAMESIZE.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

descriptor_t *
NAMESIZE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "NAMESIZE");
  // IF (SYT_FLAGS(OP) & REMOTE_FLAG) = 0 THEN (1423)
  if (1
      & (xEQ (
          xAND (getFIXED (getFIXED (mSYM_TAB)
                          + 34 * (COREHALFWORD (mNAMESIZExOP)) + 8 + 4 * (0)),
                getFIXED (mREMOTE_FLAG)),
          0)))
    // RETURN 1; (1424)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(1));
    }
  // ELSE (1425)
  else
    // RETURN 2; (1426)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(2));
    }
}
