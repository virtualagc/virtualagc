/*
  File RELEASETEMP.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

int32_t
RELEASETEMP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "RELEASETEMP");
  // ARRAY_FLAG, STRI_ACTIVE = FALSE; (1197)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mARRAY_FLAG, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mSTRI_ACTIVE, bitRHS);
    bitRHS->inUse = 0;
  }
  // BNOT_FLAG, REMOTE_ADDRS = FALSE; (1198)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mBNOT_FLAG, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (1, mREMOTE_ADDRS, bitRHS);
    bitRHS->inUse = 0;
  }
  // SAVEPTR, PMINDEX = 0; (1199)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mSAVEPTR, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPMINDEX, bitRHS);
    bitRHS->inUse = 0;
  }
  // ARG_STACK_PTR, DOCOPY, CALL_LEVEL, STACK# = 0; (1200)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mARG_STACK_PTR, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mDOCOPY, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mCALL_LEVEL, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mSTACKp, bitRHS);
    bitRHS->inUse = 0;
  }
  // R_PARM# = -1; (1201)
  {
    int32_t numberRHS = (int32_t)(-1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mR_PARMp, bitRHS);
    bitRHS->inUse = 0;
  }
  // IX1 = POINT(PROC_LINK(NARGINDEX)); (1202)
  {
    descriptor_t *bitRHS = getBIT (
        16,
        mPOINT
            + 2 * COREHALFWORD (mPROC_LINK + 2 * COREHALFWORD (mNARGINDEX)));
    putBIT (16, mIX1, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO WHILE IX1 ~= PROC_LINK(NARGINDEX); (1203)
  while (1
         & (xNEQ (COREHALFWORD (mIX1),
                  COREHALFWORD (mPROC_LINK + 2 * COREHALFWORD (mNARGINDEX)))))
    {
      // UPPER(IX1)=-1; (1204)
      {
        int32_t numberRHS = (int32_t)(-1);
        putFIXED (mUPPER + 4 * (COREHALFWORD (mIX1)), numberRHS);
      }
      // IX1=POINT(IX1); (1205)
      {
        descriptor_t *bitRHS = getBIT (16, mPOINT + 2 * COREHALFWORD (mIX1));
        putBIT (16, mIX1, bitRHS);
        bitRHS->inUse = 0;
      }
    } // End of DO WHILE block
  // POINT(PROC_LINK(NARGINDEX)) = PROC_LINK(NARGINDEX); (1206)
  {
    descriptor_t *bitRHS
        = getBIT (16, mPROC_LINK + 2 * COREHALFWORD (mNARGINDEX));
    putBIT (
        16,
        mPOINT
            + 2 * (COREHALFWORD (mPROC_LINK + 2 * COREHALFWORD (mNARGINDEX))),
        bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL CLEAR_REGS; (1207)
  CLEAR_REGS (0);
  // CALL SETUP_STACK; (1208)
  SETUP_STACK (0);
  // DO IX1 = 0 TO DOLEVEL; (1209)
  {
    int32_t from24, to24, by24;
    from24 = 0;
    to24 = bitToFixed (getBIT (16, mDOLEVEL));
    by24 = 1;
    for (putBIT (16, mIX1, fixedToBit (16, from24));
         bitToFixed (getBIT (16, mIX1)) <= to24; putBIT (
             16, mIX1, fixedToBit (16, bitToFixed (getBIT (16, mIX1)) + by24)))
      {
        // DOTYPE(IX1) =  255; (1210)
        {
          int32_t numberRHS = (int32_t)(255);
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (8, mDOTYPE + 1 * (COREHALFWORD (mIX1)), bitRHS);
          bitRHS->inUse = 0;
        }
      }
  } // End of DO for-loop block
  {
    reentryGuard = 0;
    return 0;
  }
}
