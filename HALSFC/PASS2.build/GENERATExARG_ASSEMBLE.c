/*
  File GENERATExARG_ASSEMBLE.c generated by XCOM-I, 2024-08-09 12:39:31.
*/

#include "runtimeC.h"

int32_t
GENERATExARG_ASSEMBLE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GENERATExARG_ASSEMBLE");
  // IF NUMOP = 2 THEN (7249)
  if (1 & (xEQ (COREHALFWORD (mNUMOP), 2)))
    // DO; (7250)
    {
    rs1:;
      // LEFTOP = GET_OPERAND(1); (7251)
      {
        descriptor_t *bitRHS = (putBITp (16, mGENERATExGET_OPERANDxOP,
                                         fixedToBit (32, (int32_t)(1))),
                                GENERATExGET_OPERAND (0));
        putBIT (16, mLEFTOP, bitRHS);
        bitRHS->inUse = 0;
      }
      // RIGHTOP = GET_OPERAND(2); (7252)
      {
        descriptor_t *bitRHS = (putBITp (16, mGENERATExGET_OPERANDxOP,
                                         fixedToBit (32, (int32_t)(2))),
                                GENERATExGET_OPERAND (0));
        putBIT (16, mRIGHTOP, bitRHS);
        bitRHS->inUse = 0;
      }
      // LEFTMODE = OPMODE(TYPE(LEFTOP)); (7253)
      {
        descriptor_t *bitRHS = getBIT (
            8, mOPMODE
                   + 1
                         * COREHALFWORD (getFIXED (mIND_STACK)
                                         + 73 * (COREHALFWORD (mLEFTOP)) + 50
                                         + 2 * (0)));
        putBIT (16, mGENERATExARG_ASSEMBLExLEFTMODE, bitRHS);
        bitRHS->inUse = 0;
      }
      // RIGHTMODE = OPMODE(TYPE(RIGHTOP)); (7254)
      {
        descriptor_t *bitRHS = getBIT (
            8, mOPMODE
                   + 1
                         * COREHALFWORD (getFIXED (mIND_STACK)
                                         + 73 * (COREHALFWORD (mRIGHTOP)) + 50
                                         + 2 * (0)));
        putBIT (16, mGENERATExARG_ASSEMBLExRIGHTMODE, bitRHS);
        bitRHS->inUse = 0;
      }
      // STMT_PREC=(TYPE(LEFTOP) & 8)|(TYPE(RIGHTOP) & 8); (7255)
      {
        int32_t numberRHS = (int32_t)(xOR (
            xAND (COREHALFWORD (getFIXED (mIND_STACK)
                                + 73 * (COREHALFWORD (mLEFTOP)) + 50
                                + 2 * (0)),
                  8),
            xAND (COREHALFWORD (getFIXED (mIND_STACK)
                                + 73 * (COREHALFWORD (mRIGHTOP)) + 50
                                + 2 * (0)),
                  8)));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mGENERATExSTMT_PREC, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF ~VDLP_ACTIVE THEN (7256)
      if (1 & (xNOT (BYTE0 (mVDLP_ACTIVE))))
        // DO; (7257)
        {
        rs1s1:;
          // IF LEFTMODE ~= RIGHTMODE THEN (7258)
          if (1
              & (xNEQ (COREHALFWORD (mGENERATExARG_ASSEMBLExLEFTMODE),
                       COREHALFWORD (mGENERATExARG_ASSEMBLExRIGHTMODE))))
            // DO; (7259)
            {
            rs1s1s1:;
              // IF LEFTMODE > RIGHTMODE THEN (7260)
              if (1
                  & (xGT (COREHALFWORD (mGENERATExARG_ASSEMBLExLEFTMODE),
                          COREHALFWORD (mGENERATExARG_ASSEMBLExRIGHTMODE))))
                // RIGHTOP = VECMAT_CONVERT(RIGHTOP, 8); (7261)
                {
                  int32_t numberRHS = (int32_t)((
                      putBITp (16, mGENERATExVECMAT_CONVERTxOP,
                               getBIT (16, mRIGHTOP)),
                      putBITp (16, mGENERATExVECMAT_CONVERTxPREC_SPEC,
                               fixedToBit (32, (int32_t)(8))),
                      GENERATExVECMAT_CONVERT (0)));
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (16, mRIGHTOP, bitRHS);
                  bitRHS->inUse = 0;
                }
              // ELSE (7262)
              else
                // LEFTOP = VECMAT_CONVERT(LEFTOP, 8); (7263)
                {
                  int32_t numberRHS = (int32_t)((
                      putBITp (16, mGENERATExVECMAT_CONVERTxOP,
                               getBIT (16, mLEFTOP)),
                      putBITp (16, mGENERATExVECMAT_CONVERTxPREC_SPEC,
                               fixedToBit (32, (int32_t)(8))),
                      GENERATExVECMAT_CONVERT (0)));
                  descriptor_t *bitRHS;
                  bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                  putBIT (16, mLEFTOP, bitRHS);
                  bitRHS->inUse = 0;
                }
            es1s1s1:;
            } // End of DO block
          // IF CHECK_REMOTE(RIGHTOP) | DEL(RIGHTOP) > 0 THEN (7264)
          if (1
              & (xOR (bitToFixed ((putBITp (16, mGENERATExCHECK_REMOTExOP,
                                            getBIT (16, mRIGHTOP)),
                                   GENERATExCHECK_REMOTE (0))),
                      xGT (COREHALFWORD (getFIXED (mIND_STACK)
                                         + 73 * (COREHALFWORD (mRIGHTOP)) + 28
                                         + 2 * (0)),
                           0))))
            // RIGHTOP = VECMAT_CONVERT(RIGHTOP); (7265)
            {
              int32_t numberRHS
                  = (int32_t)((putBITp (16, mGENERATExVECMAT_CONVERTxOP,
                                        getBIT (16, mRIGHTOP)),
                               GENERATExVECMAT_CONVERT (0)));
              descriptor_t *bitRHS;
              bitRHS = fixedToBit (32, (int32_t)(numberRHS));
              putBIT (16, mRIGHTOP, bitRHS);
              bitRHS->inUse = 0;
            }
          // IF DATA_REMOTE & (CSECT_TYPE(LOC(RIGHTOP),RIGHTOP)=LOCAL#D) THEN
          // (7266)
          if (1
              & (xAND (
                  BYTE0 (mDATA_REMOTE),
                  xEQ (bitToFixed ((
                           putBITp (
                               16, mCSECT_TYPExPTR,
                               getBIT (16, getFIXED (mIND_STACK)
                                               + 73 * (COREHALFWORD (mRIGHTOP))
                                               + 40 + 2 * (0))),
                           putBITp (16, mCSECT_TYPExOP, getBIT (16, mRIGHTOP)),
                           CSECT_TYPE (0))),
                       6))))
            // RIGHTOP = VECMAT_CONVERT(RIGHTOP); (7267)
            {
              int32_t numberRHS
                  = (int32_t)((putBITp (16, mGENERATExVECMAT_CONVERTxOP,
                                        getBIT (16, mRIGHTOP)),
                               GENERATExVECMAT_CONVERT (0)));
              descriptor_t *bitRHS;
              bitRHS = fixedToBit (32, (int32_t)(numberRHS));
              putBIT (16, mRIGHTOP, bitRHS);
              bitRHS->inUse = 0;
            }
          // CALL DROPSAVE(RIGHTOP); (7268)
          {
            putBITp (16, mGENERATExDROPSAVExENTRY, getBIT (16, mRIGHTOP));
            GENERATExDROPSAVE (0);
          }
        es1s1:;
        } // End of DO block
    es1:;
    } // End of DO block
  // ELSE (7269)
  else
    // DO; (7270)
    {
    rs2:;
      // LEFTOP = GET_OPERAND(1); (7271)
      {
        descriptor_t *bitRHS = (putBITp (16, mGENERATExGET_OPERANDxOP,
                                         fixedToBit (32, (int32_t)(1))),
                                GENERATExGET_OPERAND (0));
        putBIT (16, mLEFTOP, bitRHS);
        bitRHS->inUse = 0;
      }
      // RIGHTOP = 0; (7272)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mRIGHTOP, bitRHS);
        bitRHS->inUse = 0;
      }
      // STMT_PREC=(TYPE(LEFTOP) & 8); (7273)
      {
        int32_t numberRHS = (int32_t)(xAND (
            COREHALFWORD (getFIXED (mIND_STACK) + 73 * (COREHALFWORD (mLEFTOP))
                          + 50 + 2 * (0)),
            8));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mGENERATExSTMT_PREC, bitRHS);
        bitRHS->inUse = 0;
      }
    es2:;
    } // End of DO block
  // IF ~VDLP_ACTIVE THEN (7274)
  if (1 & (xNOT (BYTE0 (mVDLP_ACTIVE))))
    // DO; (7275)
    {
    rs3:;
      // IF CHECK_REMOTE(LEFTOP) | DEL(LEFTOP) > 0 THEN (7276)
      if (1
          & (xOR (bitToFixed ((putBITp (16, mGENERATExCHECK_REMOTExOP,
                                        getBIT (16, mLEFTOP)),
                               GENERATExCHECK_REMOTE (0))),
                  xGT (COREHALFWORD (getFIXED (mIND_STACK)
                                     + 73 * (COREHALFWORD (mLEFTOP)) + 28
                                     + 2 * (0)),
                       0))))
        // LEFTOP = VECMAT_CONVERT(LEFTOP); (7277)
        {
          int32_t numberRHS = (int32_t)((
              putBITp (16, mGENERATExVECMAT_CONVERTxOP, getBIT (16, mLEFTOP)),
              GENERATExVECMAT_CONVERT (0)));
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (16, mLEFTOP, bitRHS);
          bitRHS->inUse = 0;
        }
      // IF DATA_REMOTE & (CSECT_TYPE(LOC(LEFTOP),LEFTOP)=LOCAL#D) THEN (7278)
      if (1
          & (xAND (
              BYTE0 (mDATA_REMOTE),
              xEQ (bitToFixed ((
                       putBITp (16, mCSECT_TYPExPTR,
                                getBIT (16, getFIXED (mIND_STACK)
                                                + 73 * (COREHALFWORD (mLEFTOP))
                                                + 40 + 2 * (0))),
                       putBITp (16, mCSECT_TYPExOP, getBIT (16, mLEFTOP)),
                       CSECT_TYPE (0))),
                   6))))
        // LEFTOP = VECMAT_CONVERT(LEFTOP); (7279)
        {
          int32_t numberRHS = (int32_t)((
              putBITp (16, mGENERATExVECMAT_CONVERTxOP, getBIT (16, mLEFTOP)),
              GENERATExVECMAT_CONVERT (0)));
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (16, mLEFTOP, bitRHS);
          bitRHS->inUse = 0;
        }
      // CALL DROPSAVE(LEFTOP); (7280)
      {
        putBITp (16, mGENERATExDROPSAVExENTRY, getBIT (16, mLEFTOP));
        GENERATExDROPSAVE (0);
      }
    es3:;
    } // End of DO block
  // ROW(0) = ROW(LEFTOP); (7281)
  {
    descriptor_t *bitRHS
        = getBIT (16, getFIXED (mIND_STACK) + 73 * (COREHALFWORD (mLEFTOP))
                          + 48 + 2 * (0));
    putBIT (16, getFIXED (mIND_STACK) + 73 * (0) + 48 + 2 * (0), bitRHS);
    bitRHS->inUse = 0;
  }
  // COLUMN(0) = COLUMN(LEFTOP); (7282)
  {
    descriptor_t *bitRHS
        = getBIT (16, getFIXED (mIND_STACK) + 73 * (COREHALFWORD (mLEFTOP))
                          + 24 + 2 * (0));
    putBIT (16, getFIXED (mIND_STACK) + 73 * (0) + 24 + 2 * (0), bitRHS);
    bitRHS->inUse = 0;
  }
  // OPTYPE = TYPE(LEFTOP) | STMT_PREC; (7283)
  {
    int32_t numberRHS = (int32_t)(xOR (
        COREHALFWORD (getFIXED (mIND_STACK) + 73 * (COREHALFWORD (mLEFTOP))
                      + 50 + 2 * (0)),
        COREHALFWORD (mGENERATExSTMT_PREC)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOPTYPE, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
