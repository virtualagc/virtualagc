/*
  File GET_NODExBUMP_SEARCH.c generated by XCOM-I, 2024-08-09 12:38:53.
*/

#include "runtimeC.h"

int32_t
GET_NODExBUMP_SEARCH (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "GET_NODExBUMP_SEARCH");
  // SEARCH_INX = SEARCH_INX + 1; (5534)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mSEARCH_INX), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mSEARCH_INX, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF TAG THEN (5535)
  if (1 & (bitToFixed (getBIT (8, mGET_NODExBUMP_SEARCHxTAG))))
    // DO; (5536)
    {
    rs1:;
      // SEARCH(SEARCH_INX) = CATALOG_PTR(SYT_POINT) | CONTROL(NODE) |
      // VALUE_NO; (5537)
      {
        int32_t numberRHS = (int32_t)(xOR (
            xOR (
                bitToFixed (
                    (putBITp (16, mCATALOG_PTRxPTR, getBIT (16, mSYT_POINT)),
                     CATALOG_PTR (0))),
                (putFIXED (mCONTROLxCSE_WORD, getFIXED (mNODE)), CONTROL (0))),
            getFIXED (mVALUE_NO)));
        putFIXED (mSEARCH + 4 * (COREHALFWORD (mSEARCH_INX)), numberRHS);
      }
      // SEARCH2(SEARCH_INX) = WIPEOUT#(SYT_POINT); (5538)
      {
        descriptor_t *bitRHS
            = (putBITp (16, mVALIDITYxPTR, getBIT (16, mSYT_POINT)),
               VALIDITY (0));
        putBIT (16, mSEARCH2 + 2 * (COREHALFWORD (mSEARCH_INX)), bitRHS);
        bitRHS->inUse = 0;
      }
      // TAG = 0; (5539)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (8, mGET_NODExBUMP_SEARCHxTAG, bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // ELSE (5540)
  else
    // DO; (5541)
    {
    rs2:;
      // SEARCH(SEARCH_INX) = NODE; (5542)
      {
        int32_t numberRHS = (int32_t)(getFIXED (mNODE));
        putFIXED (mSEARCH + 4 * (COREHALFWORD (mSEARCH_INX)), numberRHS);
      }
      // SEARCH2(SEARCH_INX) = NODE2; (5543)
      {
        descriptor_t *bitRHS = getBIT (16, mNODE2);
        putBIT (16, mSEARCH2 + 2 * (COREHALFWORD (mSEARCH_INX)), bitRHS);
        bitRHS->inUse = 0;
      }
    es2:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
