/*
  File CSE_WORD_FORMAT.c generated by XCOM-I, 2024-08-09 12:38:53.
*/

#include "runtimeC.h"

descriptor_t *
CSE_WORD_FORMAT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "CSE_WORD_FORMAT");
  // IF (SHR(WORD,16) &  255) = 0 THEN (1617)
  if (1 & (xEQ (xAND (SHR (getFIXED (mCSE_WORD_FORMATxWORD), 16), 255), 0)))
    // DO; (1618)
    {
    rs1:;
      // MSG = HEX(WORD,8); (1619)
      {
        descriptor_t *stringRHS;
        stringRHS = (putFIXED (mHEXxHVAL, getFIXED (mCSE_WORD_FORMATxWORD)),
                     putFIXED (mHEXxN, 8), HEX (0));
        putCHARACTER (mCSE_WORD_FORMATxMSG, stringRHS);
        stringRHS->inUse = 0;
      }
      // RETURN '   '||MSG; (1620)
      {
        reentryGuard = 0;
        return xsCAT (cToDescriptor (NULL, "   "),
                      getCHARACTER (mCSE_WORD_FORMATxMSG));
      }
    es1:;
    } // End of DO block
  // ELSE (1621)
  else
    // RETURN HEX(SHR(WORD,16),4)||' '||(WORD &  65535); (1622)
    {
      reentryGuard = 0;
      return xsCAT (
          xsCAT ((putFIXED (mHEXxHVAL,
                            SHR (getFIXED (mCSE_WORD_FORMATxWORD), 16)),
                  putFIXED (mHEXxN, 4), HEX (0)),
                 cToDescriptor (NULL, " ")),
          fixedToCharacter (xAND (getFIXED (mCSE_WORD_FORMATxWORD), 65535)));
    }
}
