/*
  File MULTIPLY_DIMS.c generated by XCOM-I, 2024-08-09 12:38:53.
*/

#include "runtimeC.h"

int32_t
MULTIPLY_DIMS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "MULTIPLY_DIMS");
  // OPR(LAST) = SHL(SHR(OPR(LAST),16) * SHR(OPR(PTR),16),16) |  ((OPR(LAST) |
  // OPR(PTR)) &  65535); (2554)
  {
    int32_t numberRHS = (int32_t)(xOR (
        SHL (xmultiply (
                 SHR (getFIXED (mOPR + 4 * COREHALFWORD (mMULTIPLY_DIMSxLAST)),
                      16),
                 SHR (getFIXED (mOPR + 4 * COREHALFWORD (mMULTIPLY_DIMSxPTR)),
                      16)),
             16),
        xAND (xOR (getFIXED (mOPR + 4 * COREHALFWORD (mMULTIPLY_DIMSxLAST)),
                   getFIXED (mOPR + 4 * COREHALFWORD (mMULTIPLY_DIMSxPTR))),
              65535)));
    putFIXED (mOPR + 4 * (COREHALFWORD (mMULTIPLY_DIMSxLAST)), numberRHS);
  }
  // OPR(PTR) = 0; (2555)
  {
    int32_t numberRHS = (int32_t)(0);
    putFIXED (mOPR + 4 * (COREHALFWORD (mMULTIPLY_DIMSxPTR)), numberRHS);
  }
  // IF C_TRACE THEN (2556)
  if (1 & (bitToFixed (getBIT (8, mC_TRACE))))
    // DO; (2557)
    {
    rs1:;
      // OUTPUT = 'MULTIPLY_DIMS:  '||LAST||','||PTR; (2558)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            xsCAT (xsCAT (cToDescriptor (NULL, "MULTIPLY_DIMS:  "),
                          bitToCharacter (getBIT (16, mMULTIPLY_DIMSxLAST))),
                   cToDescriptor (NULL, ",")),
            bitToCharacter (getBIT (16, mMULTIPLY_DIMSxPTR)));
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
      // CALL VU(LAST,1); (2559)
      {
        putBITp (16, mVUxBEGIN, getBIT (16, mMULTIPLY_DIMSxLAST));
        putBITp (16, mVUxNO, fixedToBit (32, (int32_t)(1)));
        VU (0);
      }
      // CALL VU(PTR,1); (2560)
      {
        putBITp (16, mVUxBEGIN, getBIT (16, mMULTIPLY_DIMSxPTR));
        putBITp (16, mVUxNO, fixedToBit (32, (int32_t)(1)));
        VU (0);
      }
    es1:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
