/*
  File MESSAGE_FORMAT.c generated by XCOM-I, 2024-08-09 12:38:53.
*/

#include "runtimeC.h"

descriptor_t *
MESSAGE_FORMAT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "MESSAGE_FORMAT");
  // MSG=HEX(WORD,8); (984)
  {
    descriptor_t *stringRHS;
    stringRHS = (putFIXED (mHEXxHVAL, getFIXED (mMESSAGE_FORMATxWORD)),
                 putFIXED (mHEXxN, 8), HEX (0));
    putCHARACTER (mMESSAGE_FORMATxMSG, stringRHS);
    stringRHS->inUse = 0;
  }
  // RETURN '  ' || MSG; (985)
  {
    reentryGuard = 0;
    return xsCAT (cToDescriptor (NULL, "  "),
                  getCHARACTER (mMESSAGE_FORMATxMSG));
  }
}
