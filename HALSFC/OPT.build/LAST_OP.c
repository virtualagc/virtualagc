/*
  File LAST_OP.c generated by XCOM-I, 2024-08-09 12:38:53.
*/

#include "runtimeC.h"

int32_t
LAST_OP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "LAST_OP");
  // DO WHILE OPR(PTR); (1230)
  while (1 & (getFIXED (mOPR + 4 * COREHALFWORD (mLAST_OPxPTR))))
    {
      // PTR = PTR - 1; (1231)
      {
        int32_t numberRHS
            = (int32_t)(xsubtract (COREHALFWORD (mLAST_OPxPTR), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mLAST_OPxPTR, bitRHS);
        bitRHS->inUse = 0;
      }
    } // End of DO WHILE block
  // RETURN PTR; (1232)
  {
    reentryGuard = 0;
    return COREHALFWORD (mLAST_OPxPTR);
  }
}
