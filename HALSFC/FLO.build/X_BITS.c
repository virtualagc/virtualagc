/*
  File X_BITS.c generated by XCOM-I, 2024-08-09 12:38:39.
*/

#include "runtimeC.h"

descriptor_t *
X_BITS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "X_BITS");
  // RETURN SHR(OPR(CTR),1) &  7; (1064)
  {
    reentryGuard = 0;
    return fixedToBit (
        32,
        (int32_t)(xAND (
            SHR (getFIXED (mOPR + 4 * COREHALFWORD (mX_BITSxCTR)), 1), 7)));
  }
}
