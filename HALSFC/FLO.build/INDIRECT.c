/*
  File INDIRECT.c generated by XCOM-I, 2024-08-09 12:38:39.
*/

#include "runtimeC.h"

descriptor_t *
INDIRECT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "INDIRECT");
  // DO WHILE SYT_TYPE(OP) = IND_CALL_LAB; (1077)
  while (1
         & (xEQ (BYTE0 (getFIXED (mSYM_TAB)
                        + 34 * (COREHALFWORD (mINDIRECTxOP)) + 32 + 1 * (0)),
                 COREHALFWORD (mIND_CALL_LAB))))
    {
      // OP = SYT_PTR(OP); (1078)
      {
        descriptor_t *bitRHS = getBIT (
            16, getFIXED (mSYM_TAB) + 34 * (COREHALFWORD (mINDIRECTxOP)) + 22
                    + 2 * (0));
        putBIT (16, mINDIRECTxOP, bitRHS);
        bitRHS->inUse = 0;
      }
    } // End of DO WHILE block
  // RETURN OP; (1079)
  {
    reentryGuard = 0;
    return getBIT (16, mINDIRECTxOP);
  }
}
