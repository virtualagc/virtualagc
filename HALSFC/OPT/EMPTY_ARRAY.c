/*
  File EMPTY_ARRAY.c generated by XCOM-I, 2024-08-08 04:31:49.
*/

#include "runtimeC.h"

descriptor_t *
EMPTY_ARRAY (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "EMPTY_ARRAY");
  // ADLP_PTR = PTR; (2701)
  {
    descriptor_t *bitRHS = getBIT (16, mEMPTY_ARRAYxPTR);
    putBIT (16, mEMPTY_ARRAYxADLP_PTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO UNTIL OPOP(PTR) ~= NOP AND OPOP(PTR) ~= EXTN; (2702)
  do
    {
      // PTR = LAST_OPERAND(PTR) + 1; (2703)
      {
        int32_t numberRHS = (int32_t)(xadd (
            bitToFixed ((
                putBITp (16, mLAST_OPERANDxPTR, getBIT (16, mEMPTY_ARRAYxPTR)),
                LAST_OPERAND (0))),
            1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mEMPTY_ARRAYxPTR, bitRHS);
        bitRHS->inUse = 0;
      }
    }
  while (!(1
           & (xAND (xNEQ (bitToFixed ((putBITp (16, mOPOPxPTR,
                                                getBIT (16, mEMPTY_ARRAYxPTR)),
                                       OPOP (0))),
                          COREHALFWORD (mNOP)),
                    xNEQ (bitToFixed ((putBITp (16, mOPOPxPTR,
                                                getBIT (16, mEMPTY_ARRAYxPTR)),
                                       OPOP (0))),
                          COREHALFWORD (mEXTN)))))); // End of DO UNTIL block
  // IF OPOP(PTR) ~= DLPE THEN (2704)
  if (1
      & (xNEQ (
          bitToFixed ((putBITp (16, mOPOPxPTR, getBIT (16, mEMPTY_ARRAYxPTR)),
                       OPOP (0))),
          COREHALFWORD (mDLPE))))
    // RETURN FALSE; (2705)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
  // OPR(PTR) = 0; (2706)
  {
    int32_t numberRHS = (int32_t)(0);
    putFIXED (mOPR + 4 * (COREHALFWORD (mEMPTY_ARRAYxPTR)), numberRHS);
  }
  // OPR(ADLP_PTR) = OPR(ADLP_PTR) &  16711680; (2707)
  {
    int32_t numberRHS = (int32_t)(xAND (
        getFIXED (mOPR + 4 * COREHALFWORD (mEMPTY_ARRAYxADLP_PTR)), 16711680));
    putFIXED (mOPR + 4 * (COREHALFWORD (mEMPTY_ARRAYxADLP_PTR)), numberRHS);
  }
  // DO FOR PTR = ADLP_PTR TO LAST_OPERAND(ADLP_PTR); (2708)
  {
    int32_t from61, to61, by61;
    from61 = bitToFixed (getBIT (16, mEMPTY_ARRAYxADLP_PTR));
    to61 = bitToFixed (
        (putBITp (16, mLAST_OPERANDxPTR, getBIT (16, mEMPTY_ARRAYxADLP_PTR)),
         LAST_OPERAND (0)));
    by61 = 1;
    for (putBIT (16, mEMPTY_ARRAYxPTR, fixedToBit (16, from61));
         bitToFixed (getBIT (16, mEMPTY_ARRAYxPTR)) <= to61;
         putBIT (16, mEMPTY_ARRAYxPTR,
                 fixedToBit (16, bitToFixed (getBIT (16, mEMPTY_ARRAYxPTR))
                                     + by61)))
      {
        // OPR(PTR) = 0; (2709)
        {
          int32_t numberRHS = (int32_t)(0);
          putFIXED (mOPR + 4 * (COREHALFWORD (mEMPTY_ARRAYxPTR)), numberRHS);
        }
      }
  } // End of DO for-loop block
  // IF SUB_TRACE THEN (2710)
  if (1 & (bitToFixed (getBIT (8, mSUB_TRACE))))
    // OUTPUT = 'EMPTY_ARRAY REMOVED:  ' || ADLP_PTR; (2711)
    {
      descriptor_t *stringRHS;
      stringRHS = xsCAT (cToDescriptor (NULL, "EMPTY_ARRAY REMOVED:  "),
                         bitToCharacter (getBIT (16, mEMPTY_ARRAYxADLP_PTR)));
      OUTPUT (0, stringRHS);
      stringRHS->inUse = 0;
    }
  // RETURN TRUE; (2712)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(1));
  }
}
