/*
  File VAC_OR_XPT.c generated by XCOM-I, 2024-08-09 12:38:53.
*/

#include "runtimeC.h"

descriptor_t *
VAC_OR_XPT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "VAC_OR_XPT");
  // PTR = OPR(PTR) &  241; (729)
  {
    int32_t numberRHS = (int32_t)(xAND (
        getFIXED (mOPR + 4 * COREHALFWORD (mVAC_OR_XPTxPTR)), 241));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mVAC_OR_XPTxPTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF PTR = XVAC OR PTR = XXPT THEN (730)
  if (1
      & (xOR (xEQ (COREHALFWORD (mVAC_OR_XPTxPTR), COREHALFWORD (mXVAC)),
              xEQ (COREHALFWORD (mVAC_OR_XPTxPTR), COREHALFWORD (mXXPT)))))
    // RETURN TRUE; (731)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(1));
    }
  // RETURN FALSE; (732)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(0));
  }
}
