/*
  File INSERT.c generated by XCOM-I, 2024-08-09 12:38:53.
*/

#include "runtimeC.h"

int32_t
INSERT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "INSERT");
  // CALL MOVE_LIMB(PTR, HIGH, LOOP_START - HIGH,0,1); (3776)
  {
    putBITp (16, mMOVE_LIMBxLOW, getBIT (16, mINSERTxPTR));
    putBITp (16, mMOVE_LIMBxHIGH, getBIT (16, mINSERTxHIGH));
    putBITp (
        16, mMOVE_LIMBxBIG,
        fixedToBit (32, (int32_t)(xsubtract (COREHALFWORD (mLOOP_START),
                                             COREHALFWORD (mINSERTxHIGH)))));
    putBITp (8, mMOVE_LIMBxQUICK_MOVE, fixedToBit (32, (int32_t)(0)));
    putBITp (8, mMOVE_LIMBxRELOCATE_ADD, fixedToBit (32, (int32_t)(1)));
    MOVE_LIMB (0);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
