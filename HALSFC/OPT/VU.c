/*
  File VU.c generated by XCOM-I, 2024-08-09 12:38:53.
*/

#include "runtimeC.h"

int32_t
VU (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "VU");
  // DO FOR NO = BEGIN TO BEGIN + NO; (1656)
  {
    int32_t from40, to40, by40;
    from40 = bitToFixed (getBIT (16, mVUxBEGIN));
    to40 = xadd (COREHALFWORD (mVUxBEGIN), COREHALFWORD (mVUxNO));
    by40 = 1;
    for (putBIT (16, mVUxNO, fixedToBit (16, from40));
         bitToFixed (getBIT (16, mVUxNO)) <= to40;
         putBIT (16, mVUxNO,
                 fixedToBit (16, bitToFixed (getBIT (16, mVUxNO)) + by40)))
      {
        // OUTPUT = '   HALMAT('|| NO || '):  ' || HEX(OPR(NO),8); (1657)
        {
          descriptor_t *stringRHS;
          stringRHS
              = xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "   HALMAT("),
                                     bitToCharacter (getBIT (16, mVUxNO))),
                              cToDescriptor (NULL, "):  ")),
                       (putFIXED (mHEXxHVAL,
                                  getFIXED (mOPR + 4 * COREHALFWORD (mVUxNO))),
                        putFIXED (mHEXxN, 8), HEX (0)));
          OUTPUT (0, stringRHS);
          stringRHS->inUse = 0;
        }
      }
  } // End of DO for-loop block
  // OUTPUT = ''; (1658)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "");
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // NO = 0; (1659)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mVUxNO, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
