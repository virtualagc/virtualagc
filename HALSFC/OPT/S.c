/*
  File S.c generated by XCOM-I, 2024-08-09 12:38:53.
*/

#include "runtimeC.h"

descriptor_t *
S (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "S");
  // IF NO = 1 THEN (980)
  if (1 & (xEQ (COREHALFWORD (mSxNO), 1)))
    // RETURN ''; (981)
    {
      reentryGuard = 0;
      return cToDescriptor (NULL, "");
    }
  // ELSE (982)
  else
    // RETURN 'S'; (983)
    {
      reentryGuard = 0;
      return cToDescriptor (NULL, "S");
    }
}
