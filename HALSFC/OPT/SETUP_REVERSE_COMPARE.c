/*
  File SETUP_REVERSE_COMPARE.c generated by XCOM-I, 2024-08-08 04:31:49.
*/

#include "runtimeC.h"

descriptor_t *
SETUP_REVERSE_COMPARE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SETUP_REVERSE_COMPARE");
  // IF TRACE THEN (4606)
  if (1 & (bitToFixed (getBIT (8, mTRACE))))
    // OUTPUT = 'SETUP_REVERSE_COMPARE'; (4607)
    {
      descriptor_t *stringRHS;
      stringRHS = cToDescriptor (NULL, "SETUP_REVERSE_COMPARE");
      OUTPUT (0, stringRHS);
      stringRHS->inUse = 0;
    }
  // CHANGE = 1; (4608)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mSETUP_REVERSE_COMPARExCHANGE, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO WHILE (SEARCH(CHANGE) &  15728640) = 0; (4609)
  while (1
         & (xEQ (xAND (getFIXED (
                           mSEARCH
                           + 4 * COREHALFWORD (mSETUP_REVERSE_COMPARExCHANGE)),
                       15728640),
                 0)))
    {
      // CHANGE = CHANGE + 1; (4610)
      {
        int32_t numberRHS = (int32_t)(xadd (
            COREHALFWORD (mSETUP_REVERSE_COMPARExCHANGE), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mSETUP_REVERSE_COMPARExCHANGE, bitRHS);
        bitRHS->inUse = 0;
      }
    } // End of DO WHILE block
  // DO FOR I = 0 TO 1; (4611)
  {
    int32_t from105, to105, by105;
    from105 = 0;
    to105 = 1;
    by105 = 1;
    for (putBIT (16, mSETUP_REVERSE_COMPARExI, fixedToBit (16, from105));
         bitToFixed (getBIT (16, mSETUP_REVERSE_COMPARExI)) <= to105; putBIT (
             16, mSETUP_REVERSE_COMPARExI,
             fixedToBit (16, bitToFixed (getBIT (16, mSETUP_REVERSE_COMPARExI))
                                 + by105)))
      {
        // LITFLAG(I) = (SEARCH(SEARCH_INX - 1 - I) &  15663104) = LITERAL;
        // (4612)
        {
          int32_t numberRHS = (int32_t)(xEQ (
              xAND (getFIXED (
                        mSEARCH
                        + 4
                              * xsubtract (
                                  xsubtract (COREHALFWORD (mSEARCH_INX), 1),
                                  COREHALFWORD (mSETUP_REVERSE_COMPARExI))),
                    15663104),
              getFIXED (mLITERAL)));
          descriptor_t *bitRHS;
          bitRHS = fixedToBit (32, (int32_t)(numberRHS));
          putBIT (8,
                  mSETUP_REVERSE_COMPARExLITFLAG
                      + 1 * (COREHALFWORD (mSETUP_REVERSE_COMPARExI)),
                  bitRHS);
          bitRHS->inUse = 0;
        }
      }
  } // End of DO for-loop block
  // IF LITFLAG & LITFLAG(1) THEN (4613)
  if (1
      & (xAND (BYTE0 (mSETUP_REVERSE_COMPARExLITFLAG),
               BYTE0 (mSETUP_REVERSE_COMPARExLITFLAG + 1 * 1))))
    // RETURN FALSE; (4614)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
  // TEMP = SEARCH_INX - CHANGE - LITFLAG; (4615)
  {
    int32_t numberRHS = (int32_t)(xsubtract (
        xsubtract (COREHALFWORD (mSEARCH_INX),
                   COREHALFWORD (mSETUP_REVERSE_COMPARExCHANGE)),
        BYTE0 (mSETUP_REVERSE_COMPARExLITFLAG)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mSETUP_REVERSE_COMPARExTEMP, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO FOR I=1 TO CHANGE - 1; (4616)
  {
    int32_t from106, to106, by106;
    from106 = 1;
    to106 = xsubtract (COREHALFWORD (mSETUP_REVERSE_COMPARExCHANGE), 1);
    by106 = 1;
    for (putBIT (16, mSETUP_REVERSE_COMPARExI, fixedToBit (16, from106));
         bitToFixed (getBIT (16, mSETUP_REVERSE_COMPARExI)) <= to106; putBIT (
             16, mSETUP_REVERSE_COMPARExI,
             fixedToBit (16, bitToFixed (getBIT (16, mSETUP_REVERSE_COMPARExI))
                                 + by106)))
      {
        // SEARCH_REV(TEMP + I) = SEARCH(I) |  1048576; (4617)
        {
          int32_t numberRHS = (int32_t)(xOR (
              getFIXED (mSEARCH + 4 * COREHALFWORD (mSETUP_REVERSE_COMPARExI)),
              1048576));
          putFIXED (
              mSEARCH_REV
                  + 4
                        * (xadd (COREHALFWORD (mSETUP_REVERSE_COMPARExTEMP),
                                 COREHALFWORD (mSETUP_REVERSE_COMPARExI))),
              numberRHS);
        }
        // SEARCH2_REV(TEMP + I) = SEARCH2(I); (4618)
        {
          descriptor_t *bitRHS = getBIT (
              16, mSEARCH2 + 2 * COREHALFWORD (mSETUP_REVERSE_COMPARExI));
          putBIT (
              16,
              mSEARCH2_REV
                  + 2
                        * (xadd (COREHALFWORD (mSETUP_REVERSE_COMPARExTEMP),
                                 COREHALFWORD (mSETUP_REVERSE_COMPARExI))),
              bitRHS);
          bitRHS->inUse = 0;
        }
      }
  } // End of DO for-loop block
  // DO FOR I = CHANGE TO SEARCH_INX - LITFLAG - 1; (4619)
  {
    int32_t from107, to107, by107;
    from107 = bitToFixed (getBIT (16, mSETUP_REVERSE_COMPARExCHANGE));
    to107 = xsubtract (xsubtract (COREHALFWORD (mSEARCH_INX),
                                  BYTE0 (mSETUP_REVERSE_COMPARExLITFLAG)),
                       1);
    by107 = 1;
    for (putBIT (16, mSETUP_REVERSE_COMPARExI, fixedToBit (16, from107));
         bitToFixed (getBIT (16, mSETUP_REVERSE_COMPARExI)) <= to107; putBIT (
             16, mSETUP_REVERSE_COMPARExI,
             fixedToBit (16, bitToFixed (getBIT (16, mSETUP_REVERSE_COMPARExI))
                                 + by107)))
      {
        // SEARCH_REV(I - CHANGE + 1) = SEARCH(I) &  15728639; (4620)
        {
          int32_t numberRHS = (int32_t)(xAND (
              getFIXED (mSEARCH + 4 * COREHALFWORD (mSETUP_REVERSE_COMPARExI)),
              15728639));
          putFIXED (
              mSEARCH_REV
                  + 4
                        * (xadd (
                            xsubtract (
                                COREHALFWORD (mSETUP_REVERSE_COMPARExI),
                                COREHALFWORD (mSETUP_REVERSE_COMPARExCHANGE)),
                            1)),
              numberRHS);
        }
        // SEARCH2_REV(I - CHANGE + 1) = SEARCH2(I); (4621)
        {
          descriptor_t *bitRHS = getBIT (
              16, mSEARCH2 + 2 * COREHALFWORD (mSETUP_REVERSE_COMPARExI));
          putBIT (
              16,
              mSEARCH2_REV
                  + 2
                        * (xadd (
                            xsubtract (
                                COREHALFWORD (mSETUP_REVERSE_COMPARExI),
                                COREHALFWORD (mSETUP_REVERSE_COMPARExCHANGE)),
                            1)),
              bitRHS);
          bitRHS->inUse = 0;
        }
      }
  } // End of DO for-loop block
  // IF LITFLAG THEN (4622)
  if (1 & (bitToFixed (getBIT (8, mSETUP_REVERSE_COMPARExLITFLAG))))
    // DO; (4623)
    {
    rs5:;
      // SEARCH_REV(SEARCH_INX - 1) = LITERAL | (CONTROL(SEARCH(SEARCH_INX -
      // 1)) +  1048576) &  1048576; (4624)
      {
        int32_t numberRHS = (int32_t)(xOR (
            getFIXED (mLITERAL),
            xAND (
                xadd (
                    (putFIXED (
                         mCONTROLxCSE_WORD,
                         getFIXED (
                             mSEARCH
                             + 4 * xsubtract (COREHALFWORD (mSEARCH_INX), 1))),
                     CONTROL (0)),
                    1048576),
                1048576)));
        putFIXED (mSEARCH_REV
                      + 4 * (xsubtract (COREHALFWORD (mSEARCH_INX), 1)),
                  numberRHS);
      }
      // SEARCH2_REV(SEARCH_INX - 1) = SEARCH2(SEARCH_INX - 1); (4625)
      {
        descriptor_t *bitRHS = getBIT (
            16, mSEARCH2 + 2 * xsubtract (COREHALFWORD (mSEARCH_INX), 1));
        putBIT (16,
                mSEARCH2_REV + 2 * (xsubtract (COREHALFWORD (mSEARCH_INX), 1)),
                bitRHS);
        bitRHS->inUse = 0;
      }
    es5:;
    } // End of DO block
  // SEARCH_REV(SEARCH_INX) = END_OF_NODE; (4626)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mEND_OF_NODE));
    putFIXED (mSEARCH_REV + 4 * (COREHALFWORD (mSEARCH_INX)), numberRHS);
  }
  // SEARCH2_REV(SEARCH_INX) = 0; (4627)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mSEARCH2_REV + 2 * (COREHALFWORD (mSEARCH_INX)), bitRHS);
    bitRHS->inUse = 0;
  }
  // IF TRACE THEN (4628)
  if (1 & (bitToFixed (getBIT (8, mTRACE))))
    // DO FOR I = 1 TO SEARCH_INX; (4629)
    {
      int32_t from108, to108, by108;
      from108 = 1;
      to108 = bitToFixed (getBIT (16, mSEARCH_INX));
      by108 = 1;
      for (putBIT (16, mSETUP_REVERSE_COMPARExI, fixedToBit (16, from108));
           bitToFixed (getBIT (16, mSETUP_REVERSE_COMPARExI)) <= to108;
           putBIT (16, mSETUP_REVERSE_COMPARExI,
                   fixedToBit (
                       16, bitToFixed (getBIT (16, mSETUP_REVERSE_COMPARExI))
                               + by108)))
        {
          // OUTPUT = CSE_WORD_FORMAT(SEARCH_REV(I)) || ' *** '
          // ||SEARCH2_REV(I); (4630)
          {
            descriptor_t *stringRHS;
            stringRHS = xsCAT (
                xsCAT (
                    (putFIXED (
                         mCSE_WORD_FORMATxWORD,
                         getFIXED (
                             mSEARCH_REV
                             + 4 * COREHALFWORD (mSETUP_REVERSE_COMPARExI))),
                     CSE_WORD_FORMAT (0)),
                    cToDescriptor (NULL, " *** ")),
                bitToCharacter (getBIT (
                    16, mSEARCH2_REV
                            + 2 * COREHALFWORD (mSETUP_REVERSE_COMPARExI))));
            OUTPUT (0, stringRHS);
            stringRHS->inUse = 0;
          }
        }
    } // End of DO for-loop block
  // RETURN TRUE; (4631)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(1));
  }
}
