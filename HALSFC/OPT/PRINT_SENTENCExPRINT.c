/*
  File PRINT_SENTENCExPRINT.c generated by XCOM-I, 2024-08-08 04:31:49.
*/

#include "runtimeC.h"

int32_t
PRINT_SENTENCExPRINT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PRINT_SENTENCExPRINT");
  // IF OPR(PTR) & SHR(OPR(PTR),3) THEN (1532)
  if (1
      & (xAND (
          getFIXED (mOPR + 4 * COREHALFWORD (mPRINT_SENTENCExPTR)),
          SHR (getFIXED (mOPR + 4 * COREHALFWORD (mPRINT_SENTENCExPTR)), 3))))
    // MSG = FORMAT(NODE(SHR(OPR(PTR),16))& 65535,5); (1533)
    {
      descriptor_t *stringRHS;
      stringRHS
          = (putFIXED (
                 mFORMATxIVAL,
                 xAND (getFIXED (
                           mNODE
                           + 4
                                 * SHR (
                                     getFIXED (mOPR
                                               + 4
                                                     * COREHALFWORD (
                                                         mPRINT_SENTENCExPTR)),
                                     16)),
                       65535)),
             putFIXED (mFORMATxN, 5), FORMAT (0));
      putCHARACTER (mPRINT_SENTENCExMSG, stringRHS);
      stringRHS->inUse = 0;
    }
  // ELSE (1534)
  else
    // IF (OPR(PTR)& 65521)=XSMRK THEN (1535)
    if (1
        & (xEQ (xAND (getFIXED (mOPR + 4 * COREHALFWORD (mPRINT_SENTENCExPTR)),
                      65521),
                COREHALFWORD (mXSMRK))))
      // MSG=' ST#='||SHR(OPR(PTR+1),16); (1536)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            cToDescriptor (NULL, " ST#="),
            fixedToCharacter (SHR (
                getFIXED (mOPR
                          + 4 * xadd (COREHALFWORD (mPRINT_SENTENCExPTR), 1)),
                16)));
        putCHARACTER (mPRINT_SENTENCExMSG, stringRHS);
        stringRHS->inUse = 0;
      }
    // ELSE (1537)
    else
      // MSG = ''; (1538)
      {
        descriptor_t *stringRHS;
        stringRHS = cToDescriptor (NULL, "");
        putCHARACTER (mPRINT_SENTENCExMSG, stringRHS);
        stringRHS->inUse = 0;
      }
  // IF OPR(PTR) THEN (1539)
  if (1 & (getFIXED (mOPR + 4 * COREHALFWORD (mPRINT_SENTENCExPTR))))
    // MSG = HEX(SHR(OPR(PTR),8) &  255,2) ||'   '||MSG; (1540)
    {
      descriptor_t *stringRHS;
      stringRHS = xsCAT (
          xsCAT ((putFIXED (
                      mHEXxHVAL,
                      xAND (SHR (getFIXED (
                                     mOPR
                                     + 4 * COREHALFWORD (mPRINT_SENTENCExPTR)),
                                 8),
                            255)),
                  putFIXED (mHEXxN, 2), HEX (0)),
                 cToDescriptor (NULL, "   ")),
          getCHARACTER (mPRINT_SENTENCExMSG));
      putCHARACTER (mPRINT_SENTENCExMSG, stringRHS);
      stringRHS->inUse = 0;
    }
  // ELSE (1541)
  else
    // MSG = HEX(SHR(OPR(PTR),24),2) || '   ' || MSG; (1542)
    {
      descriptor_t *stringRHS;
      stringRHS = xsCAT (
          xsCAT ((putFIXED (
                      mHEXxHVAL,
                      SHR (getFIXED (mOPR
                                     + 4 * COREHALFWORD (mPRINT_SENTENCExPTR)),
                           24)),
                  putFIXED (mHEXxN, 2), HEX (0)),
                 cToDescriptor (NULL, "   ")),
          getCHARACTER (mPRINT_SENTENCExMSG));
      putCHARACTER (mPRINT_SENTENCExMSG, stringRHS);
      stringRHS->inUse = 0;
    }
  // MSG = FORMAT(SHR(OPR(PTR),1) &  7,2) ||'   '||MSG; (1543)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (
        xsCAT (
            (putFIXED (
                 mFORMATxIVAL,
                 xAND (SHR (getFIXED (
                                mOPR + 4 * COREHALFWORD (mPRINT_SENTENCExPTR)),
                            1),
                       7)),
             putFIXED (mFORMATxN, 2), FORMAT (0)),
            cToDescriptor (NULL, "   ")),
        getCHARACTER (mPRINT_SENTENCExMSG));
    putCHARACTER (mPRINT_SENTENCExMSG, stringRHS);
    stringRHS->inUse = 0;
  }
  // MSG = FORMAT(FLAG(PTR),2) || '     ' || MSG; (1544)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (
        xsCAT (
            (putFIXED (mFORMATxIVAL,
                       BYTE0 (mFLAG + 1 * COREHALFWORD (mPRINT_SENTENCExPTR))),
             putFIXED (mFORMATxN, 2), FORMAT (0)),
            cToDescriptor (NULL, "     ")),
        getCHARACTER (mPRINT_SENTENCExMSG));
    putCHARACTER (mPRINT_SENTENCExMSG, stringRHS);
    stringRHS->inUse = 0;
  }
  // IF ~ OPR(PTR) THEN (1545)
  if (1 & (xNOT (getFIXED (mOPR + 4 * COREHALFWORD (mPRINT_SENTENCExPTR)))))
    // DO; (1546)
    {
    rs1:;
      // MSG = HEX(SHR(OPR(PTR),4)& 4095,3) || '                '||MSG; (1547)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            xsCAT (
                (putFIXED (
                     mHEXxHVAL,
                     xAND (SHR (getFIXED (
                                    mOPR
                                    + 4 * COREHALFWORD (mPRINT_SENTENCExPTR)),
                                4),
                           4095)),
                 putFIXED (mHEXxN, 3), HEX (0)),
                cToDescriptor (NULL, "                ")),
            getCHARACTER (mPRINT_SENTENCExMSG));
        putCHARACTER (mPRINT_SENTENCExMSG, stringRHS);
        stringRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // ELSE (1548)
  else
    // DO; (1549)
    {
    rs2:;
      // MSG = HEX(SHR(OPR(PTR),4),1) || '   ' ||MSG; (1550)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            xsCAT (
                (putFIXED (
                     mHEXxHVAL,
                     SHR (getFIXED (mOPR
                                    + 4 * COREHALFWORD (mPRINT_SENTENCExPTR)),
                          4)),
                 putFIXED (mHEXxN, 1), HEX (0)),
                cToDescriptor (NULL, "   ")),
            getCHARACTER (mPRINT_SENTENCExMSG));
        putCHARACTER (mPRINT_SENTENCExMSG, stringRHS);
        stringRHS->inUse = 0;
      }
      // MSG = FORMAT(SHR(OPR(PTR),16),5) || '    ' ||MSG; (1551)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            xsCAT (
                (putFIXED (
                     mFORMATxIVAL,
                     SHR (getFIXED (mOPR
                                    + 4 * COREHALFWORD (mPRINT_SENTENCExPTR)),
                          16)),
                 putFIXED (mFORMATxN, 5), FORMAT (0)),
                cToDescriptor (NULL, "    ")),
            getCHARACTER (mPRINT_SENTENCExMSG));
        putCHARACTER (mPRINT_SENTENCExMSG, stringRHS);
        stringRHS->inUse = 0;
      }
      // MSG = '      ' || MSG; (1552)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (cToDescriptor (NULL, "      "),
                           getCHARACTER (mPRINT_SENTENCExMSG));
        putCHARACTER (mPRINT_SENTENCExMSG, stringRHS);
        stringRHS->inUse = 0;
      }
    es2:;
    } // End of DO block
  // OUTPUT = FORMAT(PTR,5) || ' :   ' || MSG; (1553)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (
        xsCAT ((putFIXED (mFORMATxIVAL, COREHALFWORD (mPRINT_SENTENCExPTR)),
                putFIXED (mFORMATxN, 5), FORMAT (0)),
               cToDescriptor (NULL, " :   ")),
        getCHARACTER (mPRINT_SENTENCExMSG));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
