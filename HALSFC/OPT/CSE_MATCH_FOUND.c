/*
  File CSE_MATCH_FOUND.c generated by XCOM-I, 2024-08-08 04:31:49.
*/

#include "runtimeC.h"

descriptor_t *
CSE_MATCH_FOUND (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "CSE_MATCH_FOUND");
  // IF ARRAYED_TSUB THEN (4944)
  if (1 & (bitToFixed (getBIT (8, mARRAYED_TSUB))))
    // PRESENT_ADLP = GET_ADLP(NODE(PRESENT_NODE_PTR) &  65535); (4945)
    {
      descriptor_t *bitRHS
          = (putBITp (
                 16, mGET_ADLPxPTR,
                 fixedToBit (
                     32, (int32_t)(xAND (
                             getFIXED (mNODE
                                       + 4 * COREHALFWORD (mPRESENT_NODE_PTR)),
                             65535)))),
             GET_ADLP (0));
      putBIT (16, mCSE_MATCH_FOUNDxPRESENT_ADLP, bitRHS);
      bitRHS->inUse = 0;
    }
  // TWIN_MATCH, REVERSE = 0; (4946)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mTWIN_MATCH, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mREVERSE, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF COMPARE THEN (4947)
  if (1 & (CSE_MATCH_FOUNDxCOMPARE (0)))
    // RETURN TRUE; (4948)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(1));
    }
  // IF TWIN_OP THEN (4949)
  if (1 & (bitToFixed (getBIT (8, mTWIN_OP))))
    // DO; (4950)
    {
    rs1:;
      // OPTYPE, OPCODE = REVERSE_OP; (4951)
      {
        descriptor_t *bitRHS = getBIT (16, mREVERSE_OP);
        putBIT (16, mOPTYPE, bitRHS);
        putBIT (16, mCSE_MATCH_FOUNDxOPCODE, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF COMPARE THEN (4952)
      if (1 & (CSE_MATCH_FOUNDxCOMPARE (0)))
        // DO; (4953)
        {
        rs1s1:;
          // TWIN_MATCH = TRUE; (4954)
          {
            int32_t numberRHS = (int32_t)(1);
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (8, mTWIN_MATCH, bitRHS);
            bitRHS->inUse = 0;
          }
          // RETURN TRUE; (4955)
          {
            reentryGuard = 0;
            return fixedToBit (32, (int32_t)(1));
          }
        es1s1:;
        } // End of DO block
      // CALL ERRORS (CLASS_ZO,1,' '||STT#); (4956)
      {
        putBITp (16, mERRORSxCLASS, getBIT (16, mCLASS_ZO));
        putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(1)));
        putCHARACTERp (mERRORSxTEXT,
                       xsCAT (cToDescriptor (NULL, " "),
                              bitToCharacter (getBIT (16, mSTTp))));
        ERRORS (0);
      }
      // OPTYPE = OPCODE; (4957)
      {
        descriptor_t *bitRHS = getBIT (16, mCSE_MATCH_FOUNDxOPCODE);
        putBIT (16, mOPTYPE, bitRHS);
        bitRHS->inUse = 0;
      }
      // RETURN FALSE; (4958)
      {
        reentryGuard = 0;
        return fixedToBit (32, (int32_t)(0));
      }
    es1:;
    } // End of DO block
  // IF NONCOMMUTATIVE | BIT_TYPE THEN (4959)
  if (1 & (xOR (BYTE0 (mNONCOMMUTATIVE), BYTE0 (mBIT_TYPE))))
    // RETURN FALSE; (4960)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
  // REVERSE = 1; (4961)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mREVERSE, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF SETUP_REVERSE_COMPARE THEN (4962)
  if (1 & (bitToFixed (SETUP_REVERSE_COMPARE (0))))
    // IF COMPARE THEN (4963)
    if (1 & (CSE_MATCH_FOUNDxCOMPARE (0)))
      // RETURN TRUE; (4964)
      {
        reentryGuard = 0;
        return fixedToBit (32, (int32_t)(1));
      }
  // RETURN FALSE; (4965)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(0));
  }
}
