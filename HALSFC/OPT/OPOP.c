/*
  File OPOP.c generated by XCOM-I, 2024-08-08 04:31:49.
*/

#include "runtimeC.h"

descriptor_t *
OPOP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "OPOP");
  // RETURN SHR(OPR(PTR),4) &  4095; (715)
  {
    reentryGuard = 0;
    return fixedToBit (
        32,
        (int32_t)(xAND (
            SHR (getFIXED (mOPR + 4 * COREHALFWORD (mOPOPxPTR)), 4), 4095)));
  }
}
