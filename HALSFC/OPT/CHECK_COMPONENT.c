/*
  File CHECK_COMPONENT.c generated by XCOM-I, 2024-08-08 04:31:49.
*/

#include "runtimeC.h"

int32_t
CHECK_COMPONENT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "CHECK_COMPONENT");
  // PRESENT_DIMENSION = PRESENT_DIMENSION + 1; (3703)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mPRESENT_DIMENSION), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPRESENT_DIMENSION, bitRHS);
    bitRHS->inUse = 0;
  }
  // OPERAND_TYPE = XHALMAT_QUAL(DSUB_INX); (3704)
  {
    descriptor_t *bitRHS
        = (putBITp (16, mXHALMAT_QUALxPTR, getBIT (16, mDSUB_INX)),
           XHALMAT_QUAL (0));
    putBIT (16, mOPERAND_TYPE, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF OPERAND_TYPE = XSYT OR OPERAND_TYPE = XVAC OR OPERAND_TYPE = XXPT THEN
  // (3705)
  if (1
      & (xOR (xOR (xEQ (COREHALFWORD (mOPERAND_TYPE), COREHALFWORD (mXSYT)),
                   xEQ (COREHALFWORD (mOPERAND_TYPE), COREHALFWORD (mXVAC))),
              xEQ (COREHALFWORD (mOPERAND_TYPE), COREHALFWORD (mXXPT)))))
    // DO; (3706)
    {
    rs1:;
      // IF OPERAND_TYPE = XVAC THEN (3707)
      if (1 & (xEQ (COREHALFWORD (mOPERAND_TYPE), COREHALFWORD (mXVAC))))
        // IF CONVERSION_TYPE(SHR(OPR(DSUB_INX),16)) THEN (3708)
        if (1
            & (bitToFixed ((
                putBITp (
                    16, mCONVERSION_TYPExPTR,
                    fixedToBit (
                        32, (int32_t)(SHR (
                                getFIXED (mOPR + 4 * COREHALFWORD (mDSUB_INX)),
                                16)))),
                CONVERSION_TYPE (0)))))
          // RETURN; (3709)
          {
            reentryGuard = 0;
            return 0;
          }
      // CALL GENERATE_DSUB_COMPUTATION; (3710)
      GENERATE_DSUB_COMPUTATION (0);
    es1:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
