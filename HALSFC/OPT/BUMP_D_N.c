/*
  File BUMP_D_N.c generated by XCOM-I, 2024-08-09 12:38:53.
*/

#include "runtimeC.h"

int32_t
BUMP_D_N (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "BUMP_D_N");
  // D_N_INX = D_N_INX + 1; (798)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mD_N_INX), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mD_N_INX, bitRHS);
    bitRHS->inUse = 0;
  }
  // DIFF_NODE(D_N_INX) = PTR; (799)
  {
    descriptor_t *bitRHS = getBIT (16, mBUMP_D_NxPTR);
    putBIT (16, mDIFF_NODE + 2 * (COREHALFWORD (mD_N_INX)), bitRHS);
    bitRHS->inUse = 0;
  }
  // DIFF_PTR(D_N_INX) = D_PTR; (800)
  {
    descriptor_t *bitRHS = getBIT (16, mBUMP_D_NxD_PTR);
    putBIT (16, mDIFF_PTR + 2 * (COREHALFWORD (mD_N_INX)), bitRHS);
    bitRHS->inUse = 0;
  }
  // D_PTR = 0; (801)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBUMP_D_NxD_PTR, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
