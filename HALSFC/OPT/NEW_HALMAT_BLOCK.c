/*
  File NEW_HALMAT_BLOCK.c generated by XCOM-I, 2024-08-08 04:31:49.
*/

#include "runtimeC.h"

int32_t
NEW_HALMAT_BLOCK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "NEW_HALMAT_BLOCK");
  // OPR=FILE(CODEFILE,CURCBLK); (772)
  {
    rFILE (ADDR (NULL, 0, "OPR", 0), BYTE0 (mCODEFILE), BYTE0 (mCURCBLK));
  }
  // CURCBLK=CURCBLK+1; (773)
  {
    int32_t numberRHS = (int32_t)(xadd (BYTE0 (mCURCBLK), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mCURCBLK, bitRHS);
    bitRHS->inUse = 0;
  }
  // CTR=0; (774)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mCTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // LAST_ZAP = 0; (775)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mLAST_ZAP, bitRHS);
    bitRHS->inUse = 0;
  }
  // LAST_SMRK = 0; (776)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mLAST_SMRK, bitRHS);
    bitRHS->inUse = 0;
  }
  // STACKED_BLOCK#(0),BLOCK#,BLOCK_TOP = 1; (777)
  {
    int32_t numberRHS = (int32_t)(1);
    putBIT (16, getFIXED (mLEVEL_STACK_VARS) + 11 * (0) + 8 + 2 * (0),
            fixedToBit (16, numberRHS));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBLOCKp, bitRHS);
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBLOCK_TOP, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF (OPR &  65521) = XPXRC THEN (778)
  if (1 & (xEQ (xAND (getFIXED (mOPR), 65521), COREHALFWORD (mXPXRC))))
    // XREC_PTR = SHR(OPR(1),16); (779)
    {
      int32_t numberRHS = (int32_t)(SHR (getFIXED (mOPR + 4 * 1), 16));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mXREC_PTR, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (780)
  else
    // XREC_PTR = BLOCK_END; (781)
    {
      int32_t numberRHS = (int32_t)(1799);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mXREC_PTR, bitRHS);
      bitRHS->inUse = 0;
    }
  // PULL_LOOP_HEAD(0) = -1; (782)
  {
    int32_t numberRHS = (int32_t)(-1);
    putBIT (16, getFIXED (mLEVEL_STACK_VARS) + 11 * (0) + 4 + 2 * (0),
            fixedToBit (16, numberRHS));
  }
  // NUMOP = SHR(OPR, 16) &  255; (783)
  {
    int32_t numberRHS = (int32_t)(xAND (SHR (getFIXED (mOPR), 16), 255));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mNUMOP, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
