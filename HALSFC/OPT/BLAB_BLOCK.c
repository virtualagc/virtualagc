/*
  File BLAB_BLOCK.c generated by XCOM-I, 2024-08-08 04:31:49.
*/

#include "runtimeC.h"

int32_t
BLAB_BLOCK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "BLAB_BLOCK");
  // CTR = START; (784)
  {
    descriptor_t *bitRHS = getBIT (16, mBLAB_BLOCKxSTART);
    putBIT (16, mCTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // HR_SV = HALMAT_REQUESTED; (785)
  {
    descriptor_t *bitRHS = getBIT (8, mHALMAT_REQUESTED);
    putBIT (8, mBLAB_BLOCKxHR_SV, bitRHS);
    bitRHS->inUse = 0;
  }
  // HALMAT_REQUESTED = TRUE; (786)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mHALMAT_REQUESTED, bitRHS);
    bitRHS->inUse = 0;
  }
  // CLASS = 1; (787)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mCLASS, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO WHILE CLASS ~= 0 OR OPCODE ~= XREC; (788)
  while (1
         & (xOR (xNEQ (COREHALFWORD (mCLASS), 0),
                 xNEQ (COREHALFWORD (mOPCODE), COREHALFWORD (mXREC)))))
    {
      // CALL DECODEPOP(CTR); (789)
      {
        putBITp (16, mDECODEPOPxCTR, getBIT (16, mCTR));
        DECODEPOP (0);
      }
      // IF OPCODE ~= 0 THEN (790)
      if (1 & (xNEQ (COREHALFWORD (mOPCODE), 0)))
        // DO FOR I = 1 TO NUMOP; (791)
        {
          int32_t from20, to20, by20;
          from20 = 1;
          to20 = bitToFixed (getBIT (16, mNUMOP));
          by20 = 1;
          for (putBIT (16, mBLAB_BLOCKxI, fixedToBit (16, from20));
               bitToFixed (getBIT (16, mBLAB_BLOCKxI)) <= to20;
               putBIT (16, mBLAB_BLOCKxI,
                       fixedToBit (16, bitToFixed (getBIT (16, mBLAB_BLOCKxI))
                                           + by20)))
            {
              // CALL DECODEPIP(I); (792)
              {
                putBITp (16, mDECODEPIPxOP, getBIT (16, mBLAB_BLOCKxI));
                DECODEPIP (0);
              }
            }
        } // End of DO for-loop block
      // CTR = CTR + NUMOP + 1; (793)
      {
        int32_t numberRHS = (int32_t)(xadd (
            xadd (COREHALFWORD (mCTR), COREHALFWORD (mNUMOP)), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mCTR, bitRHS);
        bitRHS->inUse = 0;
      }
    } // End of DO WHILE block
  // HALMAT_REQUESTED = HR_SV; (794)
  {
    descriptor_t *bitRHS = getBIT (8, mBLAB_BLOCKxHR_SV);
    putBIT (8, mHALMAT_REQUESTED, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
