/*
  File PREPARE_HALMATxOPDECODE.c generated by XCOM-I, 2024-08-09 12:38:53.
*/

#include "runtimeC.h"

int32_t
PREPARE_HALMATxOPDECODE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PREPARE_HALMATxOPDECODE");
  // OPRTR = OPR(CTR) &  65521; (1510)
  {
    int32_t numberRHS = (int32_t)(xAND (
        getFIXED (mOPR + 4 * COREHALFWORD (mPREPARE_HALMATxOPDECODExCTR)),
        65521));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPREPARE_HALMATxOPRTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // OPNUM = SHR(OPR(CTR), 16) &  255; (1511)
  {
    int32_t numberRHS = (int32_t)(xAND (
        SHR (getFIXED (mOPR + 4 * COREHALFWORD (mPREPARE_HALMATxOPDECODExCTR)),
             16),
        255));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPREPARE_HALMATxOPNUM, bitRHS);
    bitRHS->inUse = 0;
  }
  // OPTAG = SHR(OPR(CTR), 24) &  63; (1512)
  {
    int32_t numberRHS = (int32_t)(xAND (
        SHR (getFIXED (mOPR + 4 * COREHALFWORD (mPREPARE_HALMATxOPDECODExCTR)),
             24),
        63));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPREPARE_HALMATxOPTAG, bitRHS);
    bitRHS->inUse = 0;
  }
  // OPCOPT = SHR(OPR(CTR), 1) &  7; (1513)
  {
    int32_t numberRHS = (int32_t)(xAND (
        SHR (getFIXED (mOPR + 4 * COREHALFWORD (mPREPARE_HALMATxOPDECODExCTR)),
             1),
        7));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPREPARE_HALMATxOPCOPT, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
