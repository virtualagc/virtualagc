/*
  File SET_LOOP_END.c generated by XCOM-I, 2024-08-09 12:38:53.
*/

#include "runtimeC.h"

int32_t
SET_LOOP_END (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SET_LOOP_END");
  // K = N_INX; (2624)
  {
    descriptor_t *bitRHS = getBIT (16, mN_INX);
    putBIT (16, mSET_LOOP_ENDxK, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO WHILE A_PARITY(K); (2625)
  while (1
         & (bitToFixed (
             getBIT (8, mA_PARITY + 1 * COREHALFWORD (mSET_LOOP_ENDxK)))))
    {
      // K = K - 1; (2626)
      {
        int32_t numberRHS
            = (int32_t)(xsubtract (COREHALFWORD (mSET_LOOP_ENDxK), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mSET_LOOP_ENDxK, bitRHS);
        bitRHS->inUse = 0;
      }
    } // End of DO WHILE block
  // NODE(K) = NODE(K) | PTR; (2627)
  {
    int32_t numberRHS
        = (int32_t)(xOR (getFIXED (mNODE + 4 * COREHALFWORD (mSET_LOOP_ENDxK)),
                         COREHALFWORD (mSET_LOOP_ENDxPTR)));
    putFIXED (mNODE + 4 * (COREHALFWORD (mSET_LOOP_ENDxK)), numberRHS);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
