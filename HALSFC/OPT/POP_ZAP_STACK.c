/*
  File POP_ZAP_STACK.c generated by XCOM-I, 2024-08-08 04:31:49.
*/

#include "runtimeC.h"

int32_t
POP_ZAP_STACK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "POP_ZAP_STACK");
  // IF STACK_TRACE THEN (3271)
  if (1 & (bitToFixed (getBIT (8, mSTACK_TRACE))))
    // OUTPUT='POP_ZAP_STACK('||COMBINE_LEVELS||'):  '||STT#; (3272)
    {
      descriptor_t *stringRHS;
      stringRHS = xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, "POP_ZAP_STACK("),
                                       bitToCharacter (getBIT (
                                           8, mPOP_ZAP_STACKxCOMBINE_LEVELS))),
                                cToDescriptor (NULL, "):  ")),
                         bitToCharacter (getBIT (16, mSTTp)));
      OUTPUT (0, stringRHS);
      stringRHS->inUse = 0;
    }
  // IF ZAP_LEVEL <= 0 THEN (3273)
  if (1 & (xLE (COREHALFWORD (mZAP_LEVEL), 0)))
    // RETURN; (3274)
    {
      reentryGuard = 0;
      return 0;
    }
  // ZAP_LEVEL = ZAP_LEVEL - 1; (3275)
  {
    int32_t numberRHS = (int32_t)(xsubtract (COREHALFWORD (mZAP_LEVEL), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mZAP_LEVEL, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF COMBINE_LEVELS THEN (3276)
  if (1 & (bitToFixed (getBIT (8, mPOP_ZAP_STACKxCOMBINE_LEVELS))))
    // DO; (3277)
    {
    rs1:;
      // COMBINE_LEVELS = 0; (3278)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (8, mPOP_ZAP_STACKxCOMBINE_LEVELS, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF OBPS(ZAP_LEVEL+1).TSAPS(0) THEN (3279)
      if (1
          & (getFIXED (getFIXED (mOBPS)
                       + 4 * (xadd (COREHALFWORD (mZAP_LEVEL), 1)) + 0
                       + 4 * (0))))
        // ZAPS(0) = 1; (3280)
        {
          int32_t numberRHS = (int32_t)(1);
          putFIXED (getFIXED (mOBPS) + 4 * (COREHALFWORD (mZAP_LEVEL)) + 0
                        + 4 * (0),
                    numberRHS);
        }
      // ELSE (3281)
      else
        // DO FOR K = 0 TO SYT_WORDS; (3282)
        {
          int32_t from79, to79, by79;
          from79 = 0;
          to79 = SHR (COREHALFWORD (mSYT_USED), 5);
          by79 = 1;
          for (putBIT (16, mPOP_ZAP_STACKxK, fixedToBit (16, from79));
               bitToFixed (getBIT (16, mPOP_ZAP_STACKxK)) <= to79; putBIT (
                   16, mPOP_ZAP_STACKxK,
                   fixedToBit (16, bitToFixed (getBIT (16, mPOP_ZAP_STACKxK))
                                       + by79)))
            {
              // OBPS(ZAP_LEVEL).TSAPS(K) = OBPS(ZAP_LEVEL).TSAPS(K) |
              // OBPS(ZAP_LEVEL+1).TSAPS(K); (3283)
              {
                int32_t numberRHS = (int32_t)(xOR (
                    getFIXED (getFIXED (mOBPS)
                              + 4 * (COREHALFWORD (mZAP_LEVEL)) + 0
                              + 4 * (COREHALFWORD (mPOP_ZAP_STACKxK))),
                    getFIXED (getFIXED (mOBPS)
                              + 4 * (xadd (COREHALFWORD (mZAP_LEVEL), 1)) + 0
                              + 4 * (COREHALFWORD (mPOP_ZAP_STACKxK)))));
                putFIXED (getFIXED (mOBPS) + 4 * (COREHALFWORD (mZAP_LEVEL))
                              + 0 + 4 * (COREHALFWORD (mPOP_ZAP_STACKxK)),
                          numberRHS);
              }
            }
        } // End of DO for-loop block
    es1:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
