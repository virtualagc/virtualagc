/*
  File INITIALIZExUSED.c generated by XCOM-I, 2024-08-09 12:40:16.
*/

#include "runtimeC.h"

descriptor_t *
INITIALIZExUSED (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "INITIALIZExUSED");
  // OP4 = SYT_XREF(OP1); (2394)
  {
    descriptor_t *bitRHS = getBIT (
        16, getFIXED (mSYM_TAB) + 34 * (COREHALFWORD (mOP1)) + 16 + 2 * (0));
    putBIT (16, mOP4, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO WHILE OP4 ~= 0; (2395)
  while (1 & (xNEQ (COREHALFWORD (mOP4), 0)))
    {
      // TMP = XREF(OP4)& 65535; (2396)
      {
        int32_t numberRHS = (int32_t)(xAND (
            getFIXED (getFIXED (mCROSS_REF) + 4 * (COREHALFWORD (mOP4)) + 0
                      + 4 * (0)),
            65535));
        putFIXED (mTMP, numberRHS);
      }
      // IF ((TMP&XREF_MASK)~=0)&((TMP& 8191)>=REF_STMT) THEN (2397)
      if (1
          & (xAND (
              xNEQ (xAND (getFIXED (mTMP), COREHALFWORD (mXREF_MASK)), 0),
              xGE (xAND (getFIXED (mTMP), 8191), COREHALFWORD (mREF_STMT)))))
        // RETURN TRUE; (2398)
        {
          reentryGuard = 0;
          return fixedToBit (32, (int32_t)(1));
        }
      // OP4 = SHR(XREF(OP4),16); (2399)
      {
        int32_t numberRHS = (int32_t)(SHR (
            getFIXED (getFIXED (mCROSS_REF) + 4 * (COREHALFWORD (mOP4)) + 0
                      + 4 * (0)),
            16));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mOP4, bitRHS);
        bitRHS->inUse = 0;
      }
    } // End of DO WHILE block
  // RETURN FALSE; (2400)
  {
    reentryGuard = 0;
    return fixedToBit (32, (int32_t)(0));
  }
}
