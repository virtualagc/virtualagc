/*
  File BUILD_SDFxMOVE_CELL_TREE.c generated by XCOM-I, 2024-08-09 12:40:16.
*/

#include "runtimeC.h"

int32_t
BUILD_SDFxMOVE_CELL_TREE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "BUILD_SDFxMOVE_CELL_TREE");
  // IF ~DOING_NAMETERMS THEN (3785)
  if (1 & (xNOT (COREHALFWORD (mBUILD_SDFxMOVE_CELL_TREExDOING_NAMETERMS))))
    // DO; (3786)
    {
    rs1:;
      // PTR_INX = 0; (3787)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mBUILD_SDFxPTR_INX, bitRHS);
        bitRHS->inUse = 0;
      }
      // RETURN_PTR = GET_SDF_CELL(PTR); (3788)
      {
        int32_t numberRHS
            = (int32_t)((putFIXED (mBUILD_SDFxGET_SDF_CELLxVMEM_PTR,
                                   getFIXED (mBUILD_SDFxMOVE_CELL_TREExPTR)),
                         BUILD_SDFxGET_SDF_CELL (0)));
        putFIXED (mBUILD_SDFxMOVE_CELL_TREExRETURN_PTR, numberRHS);
      }
      // PTRS(PTR_INX) = PTRS(PTR_INX) |  2147483648; (3789)
      {
        int32_t numberRHS = (int32_t)(xOR (
            getFIXED (mBUILD_SDFxPTRS + 4 * COREHALFWORD (mBUILD_SDFxPTR_INX)),
            2147483648));
        putFIXED (mBUILD_SDFxPTRS + 4 * (COREHALFWORD (mBUILD_SDFxPTR_INX)),
                  numberRHS);
      }
    es1:;
    } // End of DO block
  // ELSE (3790)
  else
    // DOING_NAMETERMS = FALSE; (3791)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mBUILD_SDFxMOVE_CELL_TREExDOING_NAMETERMS, bitRHS);
      bitRHS->inUse = 0;
    }
  // DO WHILE PTR_INX > 0; (3792)
  while (1 & (xGT (COREHALFWORD (mBUILD_SDFxPTR_INX), 0)))
    {
      // PTR = PTRS(PTR_INX); (3793)
      {
        int32_t numberRHS = (int32_t)(getFIXED (
            mBUILD_SDFxPTRS + 4 * COREHALFWORD (mBUILD_SDFxPTR_INX)));
        putFIXED (mBUILD_SDFxMOVE_CELL_TREExPTR, numberRHS);
      }
      // PTR_INX = PTR_INX - 1; (3794)
      {
        int32_t numberRHS
            = (int32_t)(xsubtract (COREHALFWORD (mBUILD_SDFxPTR_INX), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mBUILD_SDFxPTR_INX, bitRHS);
        bitRHS->inUse = 0;
      }
      // PTR_TYPE = SHR(PTR,30) & 3; (3795)
      {
        int32_t numberRHS = (int32_t)(xAND (
            SHR (getFIXED (mBUILD_SDFxMOVE_CELL_TREExPTR), 30), 3));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mBUILD_SDFxPTR_TYPE, bitRHS);
        bitRHS->inUse = 0;
      }
      // PTR = PTR &  1073741823; (3796)
      {
        int32_t numberRHS = (int32_t)(xAND (
            getFIXED (mBUILD_SDFxMOVE_CELL_TREExPTR), 1073741823));
        putFIXED (mBUILD_SDFxMOVE_CELL_TREExPTR, numberRHS);
      }
      // DO CASE PTR_TYPE; (3797)
      {
      rs2s1:
        switch (COREHALFWORD (mBUILD_SDFxPTR_TYPE))
          {
          case 0:
            // CALL DO_VAR_REF_CELL; (3799)
            BUILD_SDFxMOVE_CELL_TREExDO_VAR_REF_CELL (0);
            break;
          case 1:
            // CALL DO_PF_INV_CELL; (3800)
            BUILD_SDFxMOVE_CELL_TREExDO_PF_INV_CELL (0);
            break;
          case 2:
            // CALL DO_EXP_VARS_CELL; (3801)
            BUILD_SDFxMOVE_CELL_TREExDO_EXP_VARS_CELL (0);
            break;
          case 3:
              // ; (3802)
              ;
            break;
          }
      } // End of DO CASE block
    }   // End of DO WHILE block
  // RETURN RETURN_PTR; (3802)
  {
    reentryGuard = 0;
    return getFIXED (mBUILD_SDFxMOVE_CELL_TREExRETURN_PTR);
  }
}
