/*
  File SYT_NAME1.c generated by XCOM-I, 2024-08-09 12:40:16.
*/

#include "runtimeC.h"

descriptor_t *
SYT_NAME1 (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SYT_NAME1");
  // K = LENGTH(SYT_NAME(PTR)); (995)
  {
    int32_t numberRHS = (int32_t)(LENGTH (
        getCHARACTER (getFIXED (mSYM_TAB) + 34 * (getFIXED (mSYT_NAME1xPTR))
                      + 0 + 4 * (0))));
    putFIXED (mSYT_NAME1xK, numberRHS);
  }
  // IF K >= 28 THEN (996)
  if (1 & (xGE (getFIXED (mSYT_NAME1xK), 28)))
    // RETURN SUBSTR(SYT_NAME(PTR), 0, 28); (997)
    {
      reentryGuard = 0;
      return SUBSTR (getCHARACTER (getFIXED (mSYM_TAB)
                                   + 34 * (getFIXED (mSYT_NAME1xPTR)) + 0
                                   + 4 * (0)),
                     0, 28);
    }
  // ELSE (998)
  else
    // RETURN SYT_NAME(PTR) || SUBSTR(X72, 0, 28-K); (999)
    {
      reentryGuard = 0;
      return xsCAT (getCHARACTER (getFIXED (mSYM_TAB)
                                  + 34 * (getFIXED (mSYT_NAME1xPTR)) + 0
                                  + 4 * (0)),
                    SUBSTR (getCHARACTER (mX72), 0,
                            xsubtract (28, getFIXED (mSYT_NAME1xK))));
    }
}
