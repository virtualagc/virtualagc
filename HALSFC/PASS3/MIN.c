/*
  File MIN.c generated by XCOM-I, 2024-08-09 12:40:16.
*/

#include "runtimeC.h"

int32_t
MIN (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "MIN");
  // IF A<B THEN (754)
  if (1 & (xLT (getFIXED (mMINxA), getFIXED (mMINxB))))
    // RETURN A; (755)
    {
      reentryGuard = 0;
      return getFIXED (mMINxA);
    }
  // ELSE (756)
  else
    // RETURN B; (757)
    {
      reentryGuard = 0;
      return getFIXED (mMINxB);
    }
}
