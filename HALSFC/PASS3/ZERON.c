/*
  File ZERON.c generated by XCOM-I, 2024-08-08 04:33:10.
*/

#include "runtimeC.h"

int32_t
ZERON (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "ZERON");
  // FLAGS = FLAGS | MODF; (1223)
  {
    int32_t numberRHS = (int32_t)(xOR (BYTE0 (mZERONxFLAGS), BYTE0 (mMODF)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mZERONxFLAGS, bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL P3_PTR_LOCATE(PTR,FLAGS); (1224)
  {
    putFIXED (mP3_PTR_LOCATExPTR, getFIXED (mZERONxPTR));
    putBITp (8, mP3_PTR_LOCATExFLAGS, getBIT (8, mZERONxFLAGS));
    P3_PTR_LOCATE (0);
  }
  // CALL ZERO_CORE(LOC_ADDR+OFFSET,COUNT); (1225)
  {
    putFIXED (mZERO_CORExCORE_ADDR,
              xadd (getFIXED (mLOC_ADDR), COREHALFWORD (mZERONxOFFSET)));
    putFIXED (mZERO_CORExCOUNT, COREHALFWORD (mZERONxCOUNT));
    ZERO_CORE (0);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
