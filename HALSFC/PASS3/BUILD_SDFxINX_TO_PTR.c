/*
  File BUILD_SDFxINX_TO_PTR.c generated by XCOM-I, 2024-08-09 12:40:16.
*/

#include "runtimeC.h"

int32_t
BUILD_SDFxINX_TO_PTR (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "BUILD_SDFxINX_TO_PTR");
  // K = STMT_EXT_NODE(I); (3944)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mBUILD_SDFxSTMT_EXT_NODE)
                                           + 2 * COREHALFWORD (mBUILD_SDFxI));
    putBIT (16, mBUILD_SDFxK, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF K > 0 THEN (3945)
  if (1 & (xGT (COREHALFWORD (mBUILD_SDFxK), 0)))
    // DO; (3946)
    {
    rs1:;
      // M = SYT_SORT1(K); (3947)
      {
        descriptor_t *bitRHS = getBIT (
            16, getFIXED (mSORTING) + 12 * (COREHALFWORD (mBUILD_SDFxK)) + 0
                    + 2 * (0));
        putBIT (16, mBUILD_SDFxM, bitRHS);
        bitRHS->inUse = 0;
      }
      // STMT_EXT_NODE(I) = M; (3948)
      {
        descriptor_t *bitRHS = getBIT (16, mBUILD_SDFxM);
        putBIT (16,
                getFIXED (mBUILD_SDFxSTMT_EXT_NODE)
                    + 2 * (COREHALFWORD (mBUILD_SDFxI)),
                bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // I = I + 1; (3949)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mBUILD_SDFxI), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBUILD_SDFxI, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
