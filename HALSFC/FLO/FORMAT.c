/*
  File FORMAT.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

descriptor_t *
FORMAT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "FORMAT");
  // STRING1 = IVAL; (1035)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mFORMATxIVAL));
    descriptor_t *stringRHS;
    stringRHS = fixedToCharacter (numberRHS);
    putCHARACTER (mFORMATxSTRING1, stringRHS);
    stringRHS->inUse = 0;
  }
  // STRING2 = ''; (1036)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "");
    putCHARACTER (mFORMATxSTRING2, stringRHS);
    stringRHS->inUse = 0;
  }
  // J = LENGTH(STRING1) - 1; (1037)
  {
    int32_t numberRHS
        = (int32_t)(xsubtract (LENGTH (getCHARACTER (mFORMATxSTRING1)), 1));
    putFIXED (mFORMATxJ, numberRHS);
  }
  // DO I = 0 TO J; (1038)
  {
    int32_t from22, to22, by22;
    from22 = 0;
    to22 = getFIXED (mFORMATxJ);
    by22 = 1;
    for (putFIXED (mFORMATxI, from22); getFIXED (mFORMATxI) <= to22;
         putFIXED (mFORMATxI, getFIXED (mFORMATxI) + by22))
      {
        // CHAR = SUBSTR(STRING1,I,1); (1039)
        {
          descriptor_t *stringRHS;
          stringRHS = SUBSTR (getCHARACTER (mFORMATxSTRING1),
                              getFIXED (mFORMATxI), 1);
          putCHARACTER (mFORMATxCHAR, stringRHS);
          stringRHS->inUse = 0;
        }
        // IF CHAR ~= X1 THEN (1040)
        if (1 & (xsNEQ (getCHARACTER (mFORMATxCHAR), getCHARACTER (mX1))))
          // STRING2 = STRING2||CHAR; (1041)
          {
            descriptor_t *stringRHS;
            stringRHS = xsCAT (getCHARACTER (mFORMATxSTRING2),
                               getCHARACTER (mFORMATxCHAR));
            putCHARACTER (mFORMATxSTRING2, stringRHS);
            stringRHS->inUse = 0;
          }
      }
  } // End of DO for-loop block
  // J = LENGTH(STRING2); (1042)
  {
    int32_t numberRHS = (int32_t)(LENGTH (getCHARACTER (mFORMATxSTRING2)));
    putFIXED (mFORMATxJ, numberRHS);
  }
  // IF J < N THEN (1043)
  if (1 & (xLT (getFIXED (mFORMATxJ), getFIXED (mFORMATxN))))
    // STRING2 = SUBSTR(X70,0,N-J)||STRING2; (1044)
    {
      descriptor_t *stringRHS;
      stringRHS = xsCAT (
          SUBSTR (getCHARACTER (mX70), 0,
                  xsubtract (getFIXED (mFORMATxN), getFIXED (mFORMATxJ))),
          getCHARACTER (mFORMATxSTRING2));
      putCHARACTER (mFORMATxSTRING2, stringRHS);
      stringRHS->inUse = 0;
    }
  // RETURN STRING2; (1045)
  {
    reentryGuard = 0;
    return getCHARACTER (mFORMATxSTRING2);
  }
}
