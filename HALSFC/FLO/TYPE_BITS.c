/*
  File TYPE_BITS.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

descriptor_t *
TYPE_BITS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "TYPE_BITS");
  // RETURN SHR(OPR(CTR),4) &  15; (1063)
  {
    reentryGuard = 0;
    return fixedToBit (
        32, (int32_t)(xAND (
                SHR (getFIXED (mOPR + 4 * COREHALFWORD (mTYPE_BITSxCTR)), 4),
                15)));
  }
}
