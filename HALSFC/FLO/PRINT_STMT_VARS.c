/*
  File PRINT_STMT_VARS.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

int32_t
PRINT_STMT_VARS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PRINT_STMT_VARS");
  // OUTPUT = X1; (2742)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mX1);
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'STMT VARIABLES:'; (2743)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "STMT VARIABLES:");
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = X1; (2744)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mX1);
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // STMT_PTR = STMT_DATA_HEAD; (2745)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mCOMM + 4 * 16));
    putFIXED (mPRINT_STMT_VARSxSTMT_PTR, numberRHS);
  }
  // DO WHILE STMT_PTR ~= -1; (2746)
  while (1 & (xNEQ (getFIXED (mPRINT_STMT_VARSxSTMT_PTR), -1)))
    {
      // CALL LOCATE(STMT_PTR,ADDR(VMEM_F),0); (2747)
      {
        putFIXED (mLOCATExPTR, getFIXED (mPRINT_STMT_VARSxSTMT_PTR));
        putFIXED (mLOCATExBVAR, ADDR ("VMEM_F", 0x80000000, NULL, 0));
        putBITp (8, mLOCATExFLAGS, fixedToBit (32, (int32_t)(0)));
        LOCATE (0);
      }
      // COREWORD(ADDR(VMEM_H)) = VMEM_LOC_ADDR; (2748)
      {
        int32_t numberRHS = (int32_t)(getFIXED (mVMEM_LOC_ADDR));
        COREWORD2 (ADDR ("VMEM_H", 0x80000000, NULL, 0), numberRHS);
      }
      // STMT_PTR = VMEM_F(0); (2749)
      {
        int32_t numberRHS = (int32_t)(getFIXED (getFIXED (mVMEM_F) + 4 * 0));
        putFIXED (mPRINT_STMT_VARSxSTMT_PTR, numberRHS);
      }
      // IF VMEM_F(4) = VMEM_F(5) THEN (2750)
      if (1
          & (xEQ (getFIXED (getFIXED (mVMEM_F) + 4 * 4),
                  getFIXED (getFIXED (mVMEM_F) + 4 * 5))))
        // REPEAT; (2751)
        continue;
      // OUTPUT = X10||'STMT#'||VMEM_H(14)||'-- LHS: '||HEX(VMEM_F(4),8) ||'
      // RHS: '||HEX(VMEM_F(5),8); (2752)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            xsCAT (xsCAT (xsCAT (xsCAT (xsCAT (getCHARACTER (mX10),
                                               cToDescriptor (NULL, "STMT#")),
                                        bitToCharacter (getBIT (
                                            16, getFIXED (mVMEM_H) + 2 * 14))),
                                 cToDescriptor (NULL, "-- LHS: ")),
                          (putFIXED (mHEXxHVAL,
                                     getFIXED (getFIXED (mVMEM_F) + 4 * 4)),
                           putFIXED (mHEXxN, 8), HEX (0))),
                   cToDescriptor (NULL, "  RHS: ")),
            (putFIXED (mHEXxHVAL, getFIXED (getFIXED (mVMEM_F) + 4 * 5)),
             putFIXED (mHEXxN, 8), HEX (0)));
        OUTPUT (0, stringRHS);
        stringRHS->inUse = 0;
      }
      // PTR_INX = 2; (2753)
      {
        int32_t numberRHS = (int32_t)(2);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mPTR_INX, bitRHS);
        bitRHS->inUse = 0;
      }
      // EXP_PTRS(1) = VMEM_F(5) |  2147483648; (2754)
      {
        int32_t numberRHS = (int32_t)(xOR (
            getFIXED (getFIXED (mVMEM_F) + 4 * 5), 2147483648));
        putFIXED (mEXP_PTRS + 4 * (1), numberRHS);
      }
      // EXP_PTRS(2) = VMEM_F(4) |  2147483648; (2755)
      {
        int32_t numberRHS = (int32_t)(xOR (
            getFIXED (getFIXED (mVMEM_F) + 4 * 4), 2147483648));
        putFIXED (mEXP_PTRS + 4 * (2), numberRHS);
      }
      // EXP_VARS(1),EXP_VARS(2) = 1; (2756)
      {
        int32_t numberRHS = (int32_t)(1);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mEXP_VARS + 2 * (1), bitRHS);
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mEXP_VARS + 2 * (2), bitRHS);
        bitRHS->inUse = 0;
      }
      // CALL FORMAT_CELL_TREE; (2757)
      FORMAT_CELL_TREE (0);
    } // End of DO WHILE block
  {
    reentryGuard = 0;
    return 0;
  }
}
