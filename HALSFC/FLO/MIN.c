/*
  File MIN.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

descriptor_t *
MIN (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "MIN");
  // IF A<B THEN (1046)
  if (1 & (xLT (COREHALFWORD (mMINxA), COREHALFWORD (mMINxB))))
    // RETURN A; (1047)
    {
      reentryGuard = 0;
      return getBIT (16, mMINxA);
    }
  // ELSE (1048)
  else
    // RETURN B; (1049)
    {
      reentryGuard = 0;
      return getBIT (16, mMINxB);
    }
}
