/*
  File TRAVERSE_INIT_LISTxPUSH_LOOP_START_OP.c generated by XCOM-I, 2024-08-09
  12:38:39.
*/

#include "runtimeC.h"

int32_t
TRAVERSE_INIT_LISTxPUSH_LOOP_START_OP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard
      = guardReentry (reentryGuard, "TRAVERSE_INIT_LISTxPUSH_LOOP_START_OP");
  // WORD_INX = WORD_INX + 2; (1949)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mWORD_INX), 2));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mWORD_INX, bitRHS);
    bitRHS->inUse = 0;
  }
  // NEST_LEVEL = NEST_LEVEL + 1; (1950)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mNEST_LEVEL), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mNEST_LEVEL, bitRHS);
    bitRHS->inUse = 0;
  }
  // WORD_STACK(WORD_INX-1) = LOOP_START_OP | NEST_LEVEL; (1951)
  {
    int32_t numberRHS = (int32_t)(xOR (getFIXED (mLOOP_START_OP),
                                       COREHALFWORD (mNEST_LEVEL)));
    putFIXED (mWORD_STACK + 4 * (xsubtract (COREHALFWORD (mWORD_INX), 1)),
              numberRHS);
  }
  // WORD_STACK(WORD_INX) = SHL(CELL_LOOP_REPEAT(INX),16) |
  // CELL_LOOP_INCR(INX); (1952)
  {
    int32_t numberRHS = (int32_t)(xOR (
        SHL (COREHALFWORD (
                 mTRAVERSE_INIT_LISTxCELL_LOOP_REPEAT
                 + 2
                       * COREHALFWORD (
                           mTRAVERSE_INIT_LISTxPUSH_LOOP_START_OPxINX)),
             16),
        COREHALFWORD (
            mTRAVERSE_INIT_LISTxCELL_LOOP_INCR
            + 2 * COREHALFWORD (mTRAVERSE_INIT_LISTxPUSH_LOOP_START_OPxINX))));
    putFIXED (mWORD_STACK + 4 * (COREHALFWORD (mWORD_INX)), numberRHS);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
