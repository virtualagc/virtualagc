/*
  File NEW_HALMAT_BLOCK.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

int32_t
NEW_HALMAT_BLOCK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "NEW_HALMAT_BLOCK");
  // BLOCK# = BLOCK# + 1; (1031)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mBLOCKp), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBLOCKp, bitRHS);
    bitRHS->inUse = 0;
  }
  // OPR = FILE(CODEFILE,CURCBLK); (1032)
  {
    rFILE (ADDR (NULL, 0, "OPR", 0), COREHALFWORD (mCODEFILE),
           COREHALFWORD (mCURCBLK));
  }
  // CURCBLK = CURCBLK + 1; (1033)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mCURCBLK), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mCURCBLK, bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL ZERO_CORE(ADDR(HALMAT_PTR),7200); (1034)
  {
    putFIXED (mZERO_CORExCORE_ADDR, ADDR (NULL, 0, "HALMAT_PTR", 0));
    putFIXED (mZERO_CORExCOUNT, 7200);
    ZERO_CORE (0);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
