/*
  File SUCCESSOR.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

descriptor_t *
SUCCESSOR (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SUCCESSOR");
  // KIN = SYT_LINK2(LOC); (1362)
  {
    descriptor_t *bitRHS = getBIT (
        16, getFIXED (mSYM_TAB) + 34 * (COREHALFWORD (mSUCCESSORxLOC)) + 26
                + 2 * (0));
    putBIT (16, mKIN, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF KIN >= 0 THEN (1363)
  if (1 & (xGE (COREHALFWORD (mKIN), 0)))
    // RETURN KIN; (1364)
    {
      reentryGuard = 0;
      return getBIT (16, mKIN);
    }
  // IF SYT_TYPE(-KIN) = TEMPL_NAME THEN (1365)
  if (1
      & (xEQ (BYTE0 (getFIXED (mSYM_TAB) + 34 * (xminus (COREHALFWORD (mKIN)))
                     + 32 + 1 * (0)),
              COREHALFWORD (mTEMPL_NAME))))
    // DO; (1366)
    {
    rs1:;
      // KIN = -KIN; (1367)
      {
        int32_t numberRHS = (int32_t)(xminus (COREHALFWORD (mKIN)));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mKIN, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF SYT_LINK2(KIN) = 0 THEN (1368)
      if (1
          & (xEQ (COREHALFWORD (getFIXED (mSYM_TAB)
                                + 34 * (COREHALFWORD (mKIN)) + 26 + 2 * (0)),
                  0)))
        // RETURN -KIN; (1369)
        {
          reentryGuard = 0;
          return fixedToBit (32, (int32_t)(xminus (COREHALFWORD (mKIN))));
        }
      // LOC = KIN; (1370)
      {
        descriptor_t *bitRHS = getBIT (16, mKIN);
        putBIT (16, mSUCCESSORxLOC, bitRHS);
        bitRHS->inUse = 0;
      }
      // KIN = SYT_LINK2(KIN); (1371)
      {
        descriptor_t *bitRHS
            = getBIT (16, getFIXED (mSYM_TAB) + 34 * (COREHALFWORD (mKIN)) + 26
                              + 2 * (0));
        putBIT (16, mKIN, bitRHS);
        bitRHS->inUse = 0;
      }
      // SYT_LINK2(LOC) = 0; (1372)
      {
        int32_t numberRHS = (int32_t)(0);
        putBIT (16,
                getFIXED (mSYM_TAB) + 34 * (COREHALFWORD (mSUCCESSORxLOC)) + 26
                    + 2 * (0),
                fixedToBit (16, numberRHS));
      }
      // TEMPL_INX = TEMPL_INX -1; (1373)
      {
        int32_t numberRHS
            = (int32_t)(xsubtract (COREHALFWORD (mTEMPL_INX), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mTEMPL_INX, bitRHS);
        bitRHS->inUse = 0;
      }
      // RETURN -KIN; (1374)
      {
        reentryGuard = 0;
        return fixedToBit (32, (int32_t)(xminus (COREHALFWORD (mKIN))));
      }
    es1:;
    } // End of DO block
  // RETURN KIN; (1375)
  {
    reentryGuard = 0;
    return getBIT (16, mKIN);
  }
}
