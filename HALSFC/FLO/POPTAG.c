/*
  File POPTAG.c generated by XCOM-I, 2024-08-09 12:38:39.
*/

#include "runtimeC.h"

descriptor_t *
POPTAG (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "POPTAG");
  // RETURN SHR(OPR(CTR),24) &  255; (1062)
  {
    reentryGuard = 0;
    return fixedToBit (
        32,
        (int32_t)(xAND (
            SHR (getFIXED (mOPR + 4 * COREHALFWORD (mPOPTAGxCTR)), 24), 255)));
  }
}
