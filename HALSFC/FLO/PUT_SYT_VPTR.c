/*
  File PUT_SYT_VPTR.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

int32_t
PUT_SYT_VPTR (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PUT_SYT_VPTR");
  // VPTR_INX = VPTR_INX + 1; (1199)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mVPTR_INX), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mVPTR_INX, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO WHILE VPTR_INX>RECORD_TOP(SYM_ADD); (1200)
  while (1
         & (xGT (COREHALFWORD (mVPTR_INX),
                 xsubtract (COREWORD (xadd (
                                ADDR ("SYM_ADD", 0x80000000, NULL, 0), 12)),
                            1))))
    {
      // DO ; (1201)
      {
      rs1s1:;
        // IF COREWORD ( ADDR ( SYM_ADD ) + 12 ) >= COREWORD ( ADDR ( SYM_ADD )
        // + 8 ) THEN (1202)
        if (1
            & (xGE (
                COREWORD (xadd (ADDR ("SYM_ADD", 0x80000000, NULL, 0), 12)),
                COREWORD (xadd (ADDR ("SYM_ADD", 0x80000000, NULL, 0), 8)))))
          // CALL _NEEDMORE_SPACE ( ADDR ( SYM_ADD )  ) ; (1203)
          {
            putFIXED (m_NEEDMORE_SPACExDOPE,
                      ADDR ("SYM_ADD", 0x80000000, NULL, 0));
            _NEEDMORE_SPACE (0);
          }
        // COREWORD ( ADDR ( SYM_ADD ) + 12 ) = COREWORD ( ADDR ( SYM_ADD ) +
        // 12 ) + 1 ; (1204)
        {
          int32_t numberRHS = (int32_t)(xadd (
              COREWORD (xadd (ADDR ("SYM_ADD", 0x80000000, NULL, 0), 12)), 1));
          COREWORD2 (xadd (ADDR ("SYM_ADD", 0x80000000, NULL, 0), 12),
                     numberRHS);
        }
      es1s1:;
      } // End of DO block
    }   // End of DO WHILE block
  // SYT_NUM(VPTR_INX) = SYMB#; (1205)
  {
    descriptor_t *bitRHS = getBIT (16, mPUT_SYT_VPTRxSYMBp);
    putBIT (16,
            getFIXED (mSYM_ADD) + 6 * (COREHALFWORD (mVPTR_INX)) + 4 + 2 * (0),
            bitRHS);
    bitRHS->inUse = 0;
  }
  // SYT_VPTR(VPTR_INX) = PTR; (1206)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mPUT_SYT_VPTRxPTR));
    putFIXED (getFIXED (mSYM_ADD) + 6 * (COREHALFWORD (mVPTR_INX)) + 0
                  + 4 * (0),
              numberRHS);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
