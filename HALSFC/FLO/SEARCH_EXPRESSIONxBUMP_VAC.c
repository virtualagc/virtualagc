/*
  File SEARCH_EXPRESSIONxBUMP_VAC.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

int32_t
SEARCH_EXPRESSIONxBUMP_VAC (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SEARCH_EXPRESSIONxBUMP_VAC");
  // SAVE_INX = SAVE_INX + 1; (1509)
  {
    int32_t numberRHS
        = (int32_t)(xadd (COREHALFWORD (mSEARCH_EXPRESSIONxSAVE_INX), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mSEARCH_EXPRESSIONxSAVE_INX, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF SAVE_INX > SAVE_LIMIT THEN (1510)
  if (1 & (xGT (COREHALFWORD (mSEARCH_EXPRESSIONxSAVE_INX), 400)))
    // CALL ERRORS (CLASS_BI, 220); (1511)
    {
      putBITp (16, mERRORSxCLASS, getBIT (16, mCLASS_BI));
      putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(220)));
      ERRORS (0);
    }
  // SAVE_VACS(SAVE_INX) = SHR(OPR(OPERAND),16); (1512)
  {
    int32_t numberRHS = (int32_t)(SHR (
        getFIXED (mOPR
                  + 4 * COREHALFWORD (mSEARCH_EXPRESSIONxBUMP_VACxOPERAND)),
        16));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16,
            mSEARCH_EXPRESSIONxSAVE_VACS
                + 2 * (COREHALFWORD (mSEARCH_EXPRESSIONxSAVE_INX)),
            bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
