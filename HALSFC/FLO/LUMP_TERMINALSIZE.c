/*
  File LUMP_TERMINALSIZE.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

int32_t
LUMP_TERMINALSIZE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "LUMP_TERMINALSIZE");
  // IF SYT_TYPE(OP) = VECTOR THEN (1342)
  if (1
      & (xEQ (BYTE0 (getFIXED (mSYM_TAB)
                     + 34 * (COREHALFWORD (mLUMP_TERMINALSIZExOP)) + 32
                     + 1 * (0)),
              COREHALFWORD (mVECTOR))))
    // RETURN SYT_DIMS(OP) &  255; (1343)
    {
      reentryGuard = 0;
      return xAND (COREHALFWORD (getFIXED (mSYM_TAB)
                                 + 34 * (COREHALFWORD (mLUMP_TERMINALSIZExOP))
                                 + 18 + 2 * (0)),
                   255);
    }
  // IF SYT_TYPE(OP) = MATRIX THEN (1344)
  if (1
      & (xEQ (BYTE0 (getFIXED (mSYM_TAB)
                     + 34 * (COREHALFWORD (mLUMP_TERMINALSIZExOP)) + 32
                     + 1 * (0)),
              COREHALFWORD (mMATRIX))))
    // RETURN SHR(SYT_DIMS(OP),8) * (SYT_DIMS(OP) &  255); (1345)
    {
      reentryGuard = 0;
      return xmultiply (
          SHR (COREHALFWORD (getFIXED (mSYM_TAB)
                             + 34 * (COREHALFWORD (mLUMP_TERMINALSIZExOP)) + 18
                             + 2 * (0)),
               8),
          xAND (COREHALFWORD (getFIXED (mSYM_TAB)
                              + 34 * (COREHALFWORD (mLUMP_TERMINALSIZExOP))
                              + 18 + 2 * (0)),
                255));
    }
  // RETURN 1; (1346)
  {
    reentryGuard = 0;
    return 1;
  }
}
