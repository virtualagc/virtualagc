/*
  File DUMP_ALL.c generated by XCOM-I, 2024-08-09 12:38:39.
*/

#include "runtimeC.h"

int32_t
DUMP_ALL (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "DUMP_ALL");
  // OUTPUT(1) = '1'; (2782)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "1");
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'INCLUDE LIST HEAD: '|| HEX(COMM(14)); (2783)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (cToDescriptor (NULL, "INCLUDE LIST HEAD: "),
                 (putFIXED (mHEXxHVAL, getFIXED (mCOMM + 4 * 14)), HEX (0)));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'STMT DATA HEAD: '|| HEX(COMM(16)); (2784)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (cToDescriptor (NULL, "STMT DATA HEAD: "),
                 (putFIXED (mHEXxHVAL, getFIXED (mCOMM + 4 * 16)), HEX (0)));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = X1; (2785)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mX1);
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'SYT VPTRS('||VPTR_INX||'):'; (2786)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (xsCAT (cToDescriptor (NULL, "SYT VPTRS("),
                              bitToCharacter (getBIT (16, mVPTR_INX))),
                       cToDescriptor (NULL, "):"));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = X1; (2787)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mX1);
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // DO I = 1 TO VPTR_INX; (2788)
  {
    int32_t from63, to63, by63;
    from63 = 1;
    to63 = bitToFixed (getBIT (16, mVPTR_INX));
    by63 = 1;
    for (putBIT (16, mDUMP_ALLxI, fixedToBit (16, from63));
         bitToFixed (getBIT (16, mDUMP_ALLxI)) <= to63; putBIT (
             16, mDUMP_ALLxI,
             fixedToBit (16, bitToFixed (getBIT (16, mDUMP_ALLxI)) + by63)))
      {
        // MSG(1) = SYT_NAME(SYT_NUM(I))||':'||HEX(SYT_VPTR(I),8)||X3; (2789)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              xsCAT (xsCAT (getCHARACTER (
                                getFIXED (mSYM_TAB)
                                + 34
                                      * (COREHALFWORD (
                                          getFIXED (mSYM_ADD)
                                          + 6 * (COREHALFWORD (mDUMP_ALLxI))
                                          + 4 + 2 * (0)))
                                + 0 + 4 * (0)),
                            cToDescriptor (NULL, ":")),
                     (putFIXED (mHEXxHVAL,
                                getFIXED (getFIXED (mSYM_ADD)
                                          + 6 * (COREHALFWORD (mDUMP_ALLxI))
                                          + 0 + 4 * (0))),
                      putFIXED (mHEXxN, 8), HEX (0))),
              getCHARACTER (mX3));
          putCHARACTER (mDUMP_ALLxMSG + 4 * (1), stringRHS);
          stringRHS->inUse = 0;
        }
        // IF LENGTH(MSG) + LENGTH(MSG(1)) > LINELENGTH THEN (2790)
        if (1
            & (xGT (xadd (LENGTH (getCHARACTER (mDUMP_ALLxMSG)),
                          LENGTH (getCHARACTER (mDUMP_ALLxMSG + 4 * 1))),
                    131)))
          // DO; (2791)
          {
          rs1s1:;
            // OUTPUT = MSG; (2792)
            {
              descriptor_t *stringRHS;
              stringRHS = getCHARACTER (mDUMP_ALLxMSG);
              OUTPUT (0, stringRHS);
              stringRHS->inUse = 0;
            }
            // MSG = MSG(1); (2793)
            {
              descriptor_t *stringRHS;
              stringRHS = getCHARACTER (mDUMP_ALLxMSG + 4 * 1);
              putCHARACTER (mDUMP_ALLxMSG, stringRHS);
              stringRHS->inUse = 0;
            }
          es1s1:;
          } // End of DO block
        // ELSE (2794)
        else
          // MSG = MSG || MSG(1); (2795)
          {
            descriptor_t *stringRHS;
            stringRHS = xsCAT (getCHARACTER (mDUMP_ALLxMSG),
                               getCHARACTER (mDUMP_ALLxMSG + 4 * 1));
            putCHARACTER (mDUMP_ALLxMSG, stringRHS);
            stringRHS->inUse = 0;
          }
      }
  } // End of DO for-loop block
  // OUTPUT = MSG; (2796)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mDUMP_ALLxMSG);
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // IF FORMAT THEN (2797)
  if (1 & (bitToFixed (getBIT (16, mDUMP_ALLxFORMAT))))
    // DO; (2798)
    {
    rs2:;
      // CALL FORMAT_SYT_VPTRS; (2799)
      FORMAT_SYT_VPTRS (0);
      // OUTPUT(1) = '1'; (2800)
      {
        descriptor_t *stringRHS;
        stringRHS = cToDescriptor (NULL, "1");
        OUTPUT (1, stringRHS);
        stringRHS->inUse = 0;
      }
      // CALL PRINT_STMT_VARS; (2801)
      PRINT_STMT_VARS (0);
    es2:;
    } // End of DO block
  // IF VMEM_DUMP THEN (2802)
  if (1 & (bitToFixed (getBIT (8, mVMEM_DUMP))))
    // DO; (2803)
    {
    rs3:;
      // OUTPUT(1) = '1'; (2804)
      {
        descriptor_t *stringRHS;
        stringRHS = cToDescriptor (NULL, "1");
        OUTPUT (1, stringRHS);
        stringRHS->inUse = 0;
      }
      // DO I = 0 TO VMEM_LAST_PAGE; (2805)
      {
        int32_t from64, to64, by64;
        from64 = 0;
        to64 = bitToFixed (getBIT (16, mVMEM_LAST_PAGE));
        by64 = 1;
        for (putBIT (16, mDUMP_ALLxI, fixedToBit (16, from64));
             bitToFixed (getBIT (16, mDUMP_ALLxI)) <= to64;
             putBIT (16, mDUMP_ALLxI,
                     fixedToBit (16, bitToFixed (getBIT (16, mDUMP_ALLxI))
                                         + by64)))
          {
            // CALL PAGE_DUMP(I); (2806)
            {
              putBITp (16, mPAGE_DUMPxPAGE, getBIT (16, mDUMP_ALLxI));
              PAGE_DUMP (0);
            }
          }
      } // End of DO for-loop block
    es3:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
