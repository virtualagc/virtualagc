/*
  File TRAVERSE_INIT_LISTxPUSH_LOOP_END_OP.c generated by XCOM-I, 2024-08-09
  12:38:39.
*/

#include "runtimeC.h"

int32_t
TRAVERSE_INIT_LISTxPUSH_LOOP_END_OP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard
      = guardReentry (reentryGuard, "TRAVERSE_INIT_LISTxPUSH_LOOP_END_OP");
  // WORD_INX = WORD_INX + 1; (1953)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mWORD_INX), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mWORD_INX, bitRHS);
    bitRHS->inUse = 0;
  }
  // WORD_STACK(WORD_INX) = LOOP_END_OP | NEST_LEVEL; (1954)
  {
    int32_t numberRHS
        = (int32_t)(xOR (getFIXED (mLOOP_END_OP), COREHALFWORD (mNEST_LEVEL)));
    putFIXED (mWORD_STACK + 4 * (COREHALFWORD (mWORD_INX)), numberRHS);
  }
  // NEST_LEVEL = NEST_LEVEL - 1; (1955)
  {
    int32_t numberRHS = (int32_t)(xsubtract (COREHALFWORD (mNEST_LEVEL), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mNEST_LEVEL, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF NEST_LEVEL < 0 THEN (1956)
  if (1 & (xLT (COREHALFWORD (mNEST_LEVEL), 0)))
    // CALL ERRORS (CLASS_BI, 213); (1957)
    {
      putBITp (16, mERRORSxCLASS, getBIT (16, mCLASS_BI));
      putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(213)));
      ERRORS (0);
    }
  {
    reentryGuard = 0;
    return 0;
  }
}
