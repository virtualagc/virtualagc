/*
  File KEEP_POINTERS.c generated by XCOM-I, 2024-08-09 12:38:39.
*/

#include "runtimeC.h"

int32_t
KEEP_POINTERS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "KEEP_POINTERS");
  // END_NODE = CTR-1; (1145)
  {
    int32_t numberRHS = (int32_t)(xsubtract (COREHALFWORD (mCTR), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mEND_NODE, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF STMT# ~= OLD_STMT# THEN (1146)
  if (1 & (xNEQ (COREHALFWORD (mSTMTp), COREHALFWORD (mOLD_STMTp))))
    // DO; (1147)
    {
    rs1:;
      // CALL CREATE_STMT; (1148)
      CREATE_STMT (0);
      // IF OLD_SMRK_NODE ~= NILL THEN (1149)
      if (1 & (xNEQ (getFIXED (mOLD_SMRK_NODE), -1)))
        // CALL PTR_LOCATE(OLD_SMRK_NODE,RELS); (1150)
        {
          putFIXED (mPTR_LOCATExPTR, getFIXED (mOLD_SMRK_NODE));
          putBITp (8, mPTR_LOCATExFLAGS, getBIT (8, mRELS));
          PTR_LOCATE (0);
        }
      // #CELLS = 0; (1151)
      {
        int32_t numberRHS = (int32_t)(0);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mpCELLS, bitRHS);
        bitRHS->inUse = 0;
      }
      // INITIAL_CASE = TRUE; (1152)
      {
        int32_t numberRHS = (int32_t)(1);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (1, mINITIAL_CASE, bitRHS);
        bitRHS->inUse = 0;
      }
      // SMRK_LIST = NILL; (1153)
      {
        int32_t numberRHS = (int32_t)(-1);
        putFIXED (mSMRK_LIST, numberRHS);
      }
      // SMRK_LINK = INIT_SMRK_LINK; (1154)
      {
        int32_t numberRHS = (int32_t)(ADDR (NULL, 0, "SMRK_LIST", 0));
        putFIXED (mSMRK_LINK, numberRHS);
      }
      // OLD_SMRK_NODE = NILL; (1155)
      {
        int32_t numberRHS = (int32_t)(-1);
        putFIXED (mOLD_SMRK_NODE, numberRHS);
      }
    es1:;
    } // End of DO block
  // CALL ADD_SMRK_NODE(START_NODE,END_NODE); (1156)
  {
    putBITp (16, mADD_SMRK_NODExSTART, getBIT (16, mSTART_NODE));
    putBITp (16, mADD_SMRK_NODExSTOP, getBIT (16, mEND_NODE));
    ADD_SMRK_NODE (0);
  }
  // START_NODE = NEXT_OP(CTR); (1157)
  {
    descriptor_t *bitRHS
        = (putBITp (16, mNEXT_OPxLOC, getBIT (16, mCTR)), NEXT_OP (0));
    putBIT (16, mSTART_NODE, bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
