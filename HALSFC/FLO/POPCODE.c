/*
  File POPCODE.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

descriptor_t *
POPCODE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "POPCODE");
  // RETURN SHR(OPR(CTR),4) &  4095; (1060)
  {
    reentryGuard = 0;
    return fixedToBit (
        32, (int32_t)(xAND (
                SHR (getFIXED (mOPR + 4 * COREHALFWORD (mPOPCODExCTR)), 4),
                4095)));
  }
}
