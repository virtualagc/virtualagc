/*
  File ADD_SMRK_NODExPUSH_NODE.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

int32_t
ADD_SMRK_NODExPUSH_NODE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "ADD_SMRK_NODExPUSH_NODE");
  // COREWORD(SMRK_LINK) = NODE; (1120)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mADD_SMRK_NODExNODE));
    COREWORD2 (getFIXED (mSMRK_LINK), numberRHS);
  }
  // IF OLD_SMRK_NODE ~= NILL THEN (1121)
  if (1 & (xNEQ (getFIXED (mOLD_SMRK_NODE), -1)))
    // CALL PTR_LOCATE(OLD_SMRK_NODE,RELS); (1122)
    {
      putFIXED (mPTR_LOCATExPTR, getFIXED (mOLD_SMRK_NODE));
      putBITp (8, mPTR_LOCATExFLAGS, getBIT (8, mRELS));
      PTR_LOCATE (0);
    }
  // OLD_SMRK_NODE = NODE; (1123)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mADD_SMRK_NODExNODE));
    putFIXED (mOLD_SMRK_NODE, numberRHS);
  }
  // SMRK_NODE(CELL) = XREC_WORD; (1124)
  {
    int32_t numberRHS = (int32_t)(32);
    putFIXED (getFIXED (mADD_SMRK_NODExSMRK_NODE)
                  + 4 * (COREHALFWORD (mADD_SMRK_NODExCELL)),
              numberRHS);
  }
  // SMRK_NODE(CELL+1) = NILL; (1125)
  {
    int32_t numberRHS = (int32_t)(-1);
    putFIXED (getFIXED (mADD_SMRK_NODExSMRK_NODE)
                  + 4 * (xadd (COREHALFWORD (mADD_SMRK_NODExCELL), 1)),
              numberRHS);
  }
  // SMRK_LINK = ADDR(SMRK_NODE(CELL+1)); (1126)
  {
    int32_t numberRHS = (int32_t)(ADDR (
        "ADD_SMRK_NODExSMRK_NODE",
        xadd (COREHALFWORD (mADD_SMRK_NODExCELL), 1), NULL, 0));
    putFIXED (mSMRK_LINK, numberRHS);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
