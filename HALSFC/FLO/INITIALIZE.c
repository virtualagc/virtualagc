/*
  File INITIALIZE.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

int32_t
INITIALIZE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "INITIALIZE");
  // IF (OPTIONS_CODE &  2048) = 0 | (TOGGLE &  8) ~=0 THEN (1321)
  if (1
      & (xOR (xEQ (xAND (getFIXED (mCOMM + 4 * 7), 2048), 0),
              xNEQ (xAND (getFIXED (mCOMM + 4 * 6), 8), 0))))
    // CALL RECORD_LINK; (1322)
    RECORD_LINK (0);
  // IF (OPTIONS_CODE &  262144) ~= 0 THEN (1323)
  if (1 & (xNEQ (xAND (getFIXED (mCOMM + 4 * 7), 262144), 0)))
    // HMAT_OPT = TRUE; (1324)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mHMAT_OPT, bitRHS);
      bitRHS->inUse = 0;
    }
  // RECORD_UNSEAL(SYM_ADD); (1325)
  {
    putFIXED (m_RECORD_UNSEALxDOPE, ADDR ("SYM_ADD", 0x80000000, NULL, 0));
    _RECORD_UNSEAL (0);
  }
  // CALL SYMBOL_TABLE_PREPASS; (1326)
  SYMBOL_TABLE_PREPASS (0);
  // CALL NEW_HALMAT_BLOCK; (1327)
  NEW_HALMAT_BLOCK (0);
  // CTR = 2; (1328)
  {
    int32_t numberRHS = (int32_t)(2);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mCTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // BLOCK# = 0; (1329)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBLOCKp, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF HMAT_OPT THEN (1330)
  if (1 & (bitToFixed (getBIT (1, mHMAT_OPT))))
    // DO; (1331)
    {
    rs1:;
      // START_NODE = 2; (1332)
      {
        int32_t numberRHS = (int32_t)(2);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mSTART_NODE, bitRHS);
        bitRHS->inUse = 0;
      }
      // SMRK_LINK = INIT_SMRK_LINK; (1333)
      {
        int32_t numberRHS = (int32_t)(ADDR (NULL, 0, "SMRK_LIST", 0));
        putFIXED (mSMRK_LINK, numberRHS);
      }
    es1:;
    } // End of DO block
  // OLD_STMT# = 1; (1334)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mOLD_STMTp, bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL GET_CELL(4,ADDR(VMEM_F),0); (1335)
  {
    putFIXED (mGET_CELLxCELL_SIZE, 4);
    putFIXED (mGET_CELLxBVAR, ADDR ("VMEM_F", 0x80000000, NULL, 0));
    putBITp (8, mGET_CELLxFLAGS, fixedToBit (32, (int32_t)(0)));
    GET_CELL (0);
  }
  // STMT_DATA_CELL = STMT_DATA_HEAD; (1336)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mCOMM + 4 * 16));
    putFIXED (mSTMT_DATA_CELL, numberRHS);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
