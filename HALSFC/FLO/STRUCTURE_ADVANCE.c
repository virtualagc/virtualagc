/*
  File STRUCTURE_ADVANCE.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

descriptor_t *
STRUCTURE_ADVANCE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "STRUCTURE_ADVANCE");
  // IF STRUCT_REF > 0 THEN (1376)
  if (1 & (xGT (COREHALFWORD (mSTRUCT_REF), 0)))
    // GO TO RE_ENTER; (1377)
    goto RE_ENTER;
  // STRUCT_REF, A = STRUCT_TEMPL; (1378)
  {
    descriptor_t *bitRHS = getBIT (16, mSTRUCT_TEMPL);
    putBIT (16, mSTRUCT_REF, bitRHS);
    putBIT (16, mSTRUCTURE_ADVANCExA, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF (SYT_LOCK#(A)& 128) ~= 0 THEN (1379)
  if (1
      & (xNEQ (xAND (BYTE0 (getFIXED (mSYM_TAB)
                            + 34 * (COREHALFWORD (mSTRUCTURE_ADVANCExA)) + 31
                            + 1 * (0)),
                     128),
               0)))
    // SYT_LINK2(A) = 0; (1380)
    {
      int32_t numberRHS = (int32_t)(0);
      putBIT (16,
              getFIXED (mSYM_TAB) + 34 * (COREHALFWORD (mSTRUCTURE_ADVANCExA))
                  + 26 + 2 * (0),
              fixedToBit (16, numberRHS));
    }
  // DO FOREVER; (1381)
  while (1 & (1))
    {
      // DO WHILE DESCENDENT(A) > 0; (1382)
      while (1
             & (xGT (bitToFixed ((putBITp (16, mDESCENDENTxLOC,
                                           getBIT (16, mSTRUCTURE_ADVANCExA)),
                                  DESCENDENT (0))),
                     0)))
        {
          // A = KIN; (1383)
          {
            descriptor_t *bitRHS = getBIT (16, mKIN);
            putBIT (16, mSTRUCTURE_ADVANCExA, bitRHS);
            bitRHS->inUse = 0;
          }
        } // End of DO WHILE block
      // RETURN A; (1384)
      {
        reentryGuard = 0;
        return getBIT (16, mSTRUCTURE_ADVANCExA);
      }
    // RE_ENTER: (1385)
    RE_ENTER:
      // A = SUCCESSOR(A); (1386)
      {
        descriptor_t *bitRHS
            = (putBITp (16, mSUCCESSORxLOC, getBIT (16, mSTRUCTURE_ADVANCExA)),
               SUCCESSOR (0));
        putBIT (16, mSTRUCTURE_ADVANCExA, bitRHS);
        bitRHS->inUse = 0;
      }
      // IF A < 0 THEN (1387)
      if (1 & (xLT (COREHALFWORD (mSTRUCTURE_ADVANCExA), 0)))
        // DO; (1388)
        {
        rs1s2:;
          // A = -A; (1389)
          {
            int32_t numberRHS
                = (int32_t)(xminus (COREHALFWORD (mSTRUCTURE_ADVANCExA)));
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (16, mSTRUCTURE_ADVANCExA, bitRHS);
            bitRHS->inUse = 0;
          }
          // IF A = STRUCT_REF THEN (1390)
          if (1
              & (xEQ (COREHALFWORD (mSTRUCTURE_ADVANCExA),
                      COREHALFWORD (mSTRUCT_REF))))
            // DO; (1391)
            {
            rs1s2s1:;
              // STRUCT_REF = 0; (1392)
              {
                int32_t numberRHS = (int32_t)(0);
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16, mSTRUCT_REF, bitRHS);
                bitRHS->inUse = 0;
              }
              // RETURN 0; (1393)
              {
                reentryGuard = 0;
                return fixedToBit (32, (int32_t)(0));
              }
            es1s2s1:;
            } // End of DO block
          // GO TO RE_ENTER; (1394)
          goto RE_ENTER;
        es1s2:;
        } // End of DO block
    }     // End of DO WHILE block
  {
    reentryGuard = 0;
    return 0;
  }
}
