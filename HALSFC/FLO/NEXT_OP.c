/*
  File NEXT_OP.c generated by XCOM-I, 2024-08-08 04:31:35.
*/

#include "runtimeC.h"

descriptor_t *
NEXT_OP (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "NEXT_OP");
  // LOC = LOC + 1; (1069)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mNEXT_OPxLOC), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mNEXT_OPxLOC, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO WHILE OPR(LOC); (1070)
  while (1 & (getFIXED (mOPR + 4 * COREHALFWORD (mNEXT_OPxLOC))))
    {
      // LOC = LOC + 1; (1071)
      {
        int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mNEXT_OPxLOC), 1));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mNEXT_OPxLOC, bitRHS);
        bitRHS->inUse = 0;
      }
    } // End of DO WHILE block
  // RETURN LOC; (1072)
  {
    reentryGuard = 0;
    return getBIT (16, mNEXT_OPxLOC);
  }
}
