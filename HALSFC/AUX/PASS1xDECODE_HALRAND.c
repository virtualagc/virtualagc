/*
  File PASS1xDECODE_HALRAND.c generated by XCOM-I, 2024-08-08 04:32:08.
*/

#include "runtimeC.h"

int32_t
PASS1xDECODE_HALRAND (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PASS1xDECODE_HALRAND");
  // OP = OP + HALMAT_PTR; (1261)
  {
    int32_t numberRHS = (int32_t)(xadd (
        COREHALFWORD (mPASS1xDECODE_HALRANDxOP), COREHALFWORD (mHALMAT_PTR)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mPASS1xDECODE_HALRANDxOP, bitRHS);
    bitRHS->inUse = 0;
  }
  // TEMP_MAT = HALMAT(OP); (1262)
  {
    int32_t numberRHS = (int32_t)(getFIXED (
        mHALMAT + 4 * COREHALFWORD (mPASS1xDECODE_HALRANDxOP)));
    putFIXED (mTEMP_MAT, numberRHS);
  }
  // IF (TEMP_MAT & 1) ~= 1 THEN (1263)
  if (1 & (xNEQ (xAND (getFIXED (mTEMP_MAT), 1), 1)))
    // CALL ERRORS (CLASS_BI, 404, ' '||CURR_HALMAT_BLOCK - 1||':'||OP); (1264)
    {
      putBITp (16, mERRORSxCLASS, getBIT (16, mCLASS_BI));
      putBITp (16, mERRORSxNUM, fixedToBit (32, (int32_t)(404)));
      putCHARACTERp (
          mERRORSxTEXT,
          xsCAT (xsCAT (xsCAT (cToDescriptor (NULL, " "),
                               fixedToCharacter (xsubtract (
                                   COREHALFWORD (mCURR_HALMAT_BLOCK), 1))),
                        cToDescriptor (NULL, ":")),
                 bitToCharacter (getBIT (16, mPASS1xDECODE_HALRANDxOP))));
      ERRORS (0);
    }
  // HALRAND_TAG2 = (TEMP_MAT &  15) | (SHR(TEMP_MAT, 27) &  16); (1265)
  {
    int32_t numberRHS
        = (int32_t)(xOR (xAND (getFIXED (mTEMP_MAT), 15),
                         xAND (SHR (getFIXED (mTEMP_MAT), 27), 16)));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mHALRAND_TAG2, bitRHS);
    bitRHS->inUse = 0;
  }
  // PTR_TYPE(OP), HALRAND_QUALIFIER = SHR(TEMP_MAT, 4) &  15; (1266)
  {
    int32_t numberRHS = (int32_t)(xAND (SHR (getFIXED (mTEMP_MAT), 4), 15));
    putBIT (8,
            getFIXED (mWORK_VARS)
                + 11 * (COREHALFWORD (mPASS1xDECODE_HALRANDxOP)) + 9 + 1 * (0),
            fixedToBit (8, numberRHS));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mHALRAND_QUALIFIER, bitRHS);
    bitRHS->inUse = 0;
  }
  // HALRAND_TAG1 = SHR(TEMP_MAT, 8) &  255; (1267)
  {
    int32_t numberRHS = (int32_t)(xAND (SHR (getFIXED (mTEMP_MAT), 8), 255));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (8, mHALRAND_TAG1, bitRHS);
    bitRHS->inUse = 0;
  }
  // HALRAND = SHR(TEMP_MAT, 16) &  32767; (1268)
  {
    int32_t numberRHS
        = (int32_t)(xAND (SHR (getFIXED (mTEMP_MAT), 16), 32767));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mHALRAND, bitRHS);
    bitRHS->inUse = 0;
  }
  // PTR(OP) = SHR(TEMP_MAT, 16) &  65535; (1269)
  {
    int32_t numberRHS
        = (int32_t)(xAND (SHR (getFIXED (mTEMP_MAT), 16), 65535));
    putBIT (16,
            getFIXED (mWORK_VARS)
                + 11 * (COREHALFWORD (mPASS1xDECODE_HALRANDxOP)) + 0 + 2 * (0),
            fixedToBit (16, numberRHS));
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
