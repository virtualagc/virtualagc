/*
  File NEW_ZERO_SYT_REF_FRAME.c generated by XCOM-I, 2024-08-08 04:32:08.
*/

#include "runtimeC.h"

descriptor_t *
NEW_ZERO_SYT_REF_FRAME (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "NEW_ZERO_SYT_REF_FRAME");
  // TEMP_PTR = NEW_SYT_REF_FRAME; (1041)
  {
    descriptor_t *bitRHS = NEW_SYT_REF_FRAME (0);
    putBIT (16, mNEW_ZERO_SYT_REF_FRAMExTEMP_PTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // DO FOR WORK1 = 0 TO SYT_REF_POOL_FRAME_SIZE; (1042)
  {
    int32_t from25, to25, by25;
    from25 = 0;
    to25 = bitToFixed (getBIT (16, mSYT_REF_POOL_FRAME_SIZE));
    by25 = 1;
    for (putFIXED (mWORK1, from25); getFIXED (mWORK1) <= to25;
         putFIXED (mWORK1, getFIXED (mWORK1) + by25))
      {
        // SYT_REF_POOL(TEMP_PTR + WORK1) = 0; (1043)
        {
          int32_t numberRHS = (int32_t)(0);
          putBIT (32,
                  getFIXED (mS_POOL)
                      + 4
                            * (xadd (COREHALFWORD (
                                         mNEW_ZERO_SYT_REF_FRAMExTEMP_PTR),
                                     getFIXED (mWORK1)))
                      + 0 + 4 * (0),
                  fixedToBit (32, numberRHS));
        }
      }
  } // End of DO for-loop block
  // RETURN TEMP_PTR; (1044)
  {
    reentryGuard = 0;
    return getBIT (16, mNEW_ZERO_SYT_REF_FRAMExTEMP_PTR);
  }
}
