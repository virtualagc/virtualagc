/*
  File LINK_CELL_AREA.c generated by XCOM-I, 2024-08-08 04:32:08.
*/

#include "runtimeC.h"

int32_t
LINK_CELL_AREA (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "LINK_CELL_AREA");
  // LINK_UP_TIME = MONITOR(18); (1126)
  {
    int32_t numberRHS = (int32_t)(MONITOR18 ());
    putFIXED (mLINK_CELL_AREAxLINK_UP_TIME, numberRHS);
  }
  // DO FOR WORK1=1 TO RECORD_TOP(LIST_STRUX); (1127)
  {
    int32_t from31, to31, by31;
    from31 = 1;
    to31 = xsubtract (
        COREWORD (xadd (ADDR ("LIST_STRUX", 0x80000000, NULL, 0), 12)), 1);
    by31 = 1;
    for (putFIXED (mWORK1, from31); getFIXED (mWORK1) <= to31;
         putFIXED (mWORK1, getFIXED (mWORK1) + by31))
      {
        // CDR_CELL(WORK1) = WORK1 + 1; (1128)
        {
          int32_t numberRHS = (int32_t)(xadd (getFIXED (mWORK1), 1));
          putBIT (16,
                  getFIXED (mLIST_STRUX) + 8 * (getFIXED (mWORK1)) + 4
                      + 2 * (0),
                  fixedToBit (16, numberRHS));
        }
      }
  } // End of DO for-loop block
  // FREE_CELL_PTR = 1; (1129)
  {
    int32_t numberRHS = (int32_t)(1);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mFREE_CELL_PTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // LINK_UP_TIME(1) = MONITOR(18); (1130)
  {
    int32_t numberRHS = (int32_t)(MONITOR18 ());
    putFIXED (mLINK_CELL_AREAxLINK_UP_TIME + 4 * (1), numberRHS);
  }
  // #GCS = #GCS + 1; (1131)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mpGCS), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mpGCS, bitRHS);
    bitRHS->inUse = 0;
  }
  // TOTAL_GC_TIME = TOTAL_GC_TIME + (LINK_UP_TIME(1) - LINK_UP_TIME); (1132)
  {
    int32_t numberRHS = (int32_t)(xadd (
        getFIXED (mTOTAL_GC_TIME),
        xsubtract (getFIXED (mLINK_CELL_AREAxLINK_UP_TIME + 4 * 1),
                   getFIXED (mLINK_CELL_AREAxLINK_UP_TIME))));
    putFIXED (mTOTAL_GC_TIME, numberRHS);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
