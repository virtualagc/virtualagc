/*
  File PASS1xSET_DEBUG_SWITCH.c generated by XCOM-I, 2024-08-08 04:32:08.
*/

#include "runtimeC.h"

int32_t
PASS1xSET_DEBUG_SWITCH (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PASS1xSET_DEBUG_SWITCH");
  // IF SWITCH = 128 THEN (1270)
  if (1 & (xEQ (BYTE0 (mPASS1xSET_DEBUG_SWITCHxSWITCH), 128)))
    // AUXMAT_REQUESTED = ~AUXMAT_REQUESTED; (1271)
    {
      int32_t numberRHS = (int32_t)(xNOT (BYTE0 (mAUXMAT_REQUESTED)));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mAUXMAT_REQUESTED, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF SWITCH = 129 THEN (1272)
  if (1 & (xEQ (BYTE0 (mPASS1xSET_DEBUG_SWITCHxSWITCH), 129)))
    // PRETTY_PRINT_REQUESTED = ~PRETTY_PRINT_REQUESTED; (1273)
    {
      int32_t numberRHS = (int32_t)(xNOT (BYTE0 (mPRETTY_PRINT_REQUESTED)));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mPRETTY_PRINT_REQUESTED, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF SWITCH = 130 THEN (1274)
  if (1 & (xEQ (BYTE0 (mPASS1xSET_DEBUG_SWITCHxSWITCH), 130)))
    // STACK_DUMP = ~STACK_DUMP; (1275)
    {
      int32_t numberRHS = (int32_t)(xNOT (BYTE0 (mSTACK_DUMP)));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mSTACK_DUMP, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF SWITCH = 131 THEN (1276)
  if (1 & (xEQ (BYTE0 (mPASS1xSET_DEBUG_SWITCHxSWITCH), 131)))
    // OPCODE_TRACE = ~OPCODE_TRACE; (1277)
    {
      int32_t numberRHS = (int32_t)(xNOT (BYTE0 (mOPCODE_TRACE)));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mOPCODE_TRACE, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF SWITCH = 132 THEN (1278)
  if (1 & (xEQ (BYTE0 (mPASS1xSET_DEBUG_SWITCHxSWITCH), 132)))
    // TARGET_TRACE = ~TARGET_TRACE; (1279)
    {
      int32_t numberRHS = (int32_t)(xNOT (BYTE0 (mTARGET_TRACE)));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mTARGET_TRACE, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF SWITCH = 133 THEN (1280)
  if (1 & (xEQ (BYTE0 (mPASS1xSET_DEBUG_SWITCHxSWITCH), 133)))
    // NOOSE_TRACE = ~NOOSE_TRACE; (1281)
    {
      int32_t numberRHS = (int32_t)(xNOT (BYTE0 (mNOOSE_TRACE)));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mNOOSE_TRACE, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF SWITCH = 134 THEN (1282)
  if (1 & (xEQ (BYTE0 (mPASS1xSET_DEBUG_SWITCHxSWITCH), 134)))
    // TIME_EXIT = ~TIME_EXIT; (1283)
    {
      int32_t numberRHS = (int32_t)(xNOT (BYTE0 (mTIME_EXIT)));
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mTIME_EXIT, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF SWITCH = 135 THEN (1284)
  if (1 & (xEQ (BYTE0 (mPASS1xSET_DEBUG_SWITCHxSWITCH), 135)))
    // DO; (1285)
    {
    rs1:;
      // IF ~HEADER_ISSUED THEN (1286)
      if (1 & (xNOT (BYTE0 (mHEADER_ISSUED))))
        // CALL PRINT_PHASE_HEADER; (1287)
        PRINT_PHASE_HEADER (0);
      // OUTPUT(1) = '-' || BLANK; (1288)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (cToDescriptor (NULL, "-"), getCHARACTER (mBLANK));
        OUTPUT (1, stringRHS);
        stringRHS->inUse = 0;
      }
      // DO FOR I = 1 TO MAX_REF_SYT_SIZE; (1289)
      {
        int32_t from37, to37, by37;
        from37 = 1;
        to37 = bitToFixed (getBIT (16, mMAX_REF_SYT_SIZE));
        by37 = 1;
        for (putBIT (16, mPASS1xSET_DEBUG_SWITCHxI, fixedToBit (16, from37));
             bitToFixed (getBIT (16, mPASS1xSET_DEBUG_SWITCHxI)) <= to37;
             putBIT (16, mPASS1xSET_DEBUG_SWITCHxI,
                     fixedToBit (16, bitToFixed (getBIT (
                                         16, mPASS1xSET_DEBUG_SWITCHxI))
                                         + by37)))
          {
            // OUTPUT = 'SYMBOL(' || #RJUST(SYT_EXPAND_INDEX(I), 5) || ') =' ||
            // #RJUST(I, 5); (1290)
            {
              descriptor_t *stringRHS;
              stringRHS = xsCAT (
                  xsCAT (
                      xsCAT (cToDescriptor (NULL, "SYMBOL("),
                             (putBITp (
                                  16, mpRJUSTxNUMBER,
                                  getBIT (
                                      16,
                                      getFIXED (mSXPND)
                                          + 2
                                                * (COREHALFWORD (
                                                    mPASS1xSET_DEBUG_SWITCHxI))
                                          + 0 + 2 * (0))),
                              putBITp (16, mpRJUSTxTOTAL_LENGTH,
                                       fixedToBit (32, (int32_t)(5))),
                              pRJUST (0))),
                      cToDescriptor (NULL, ") =")),
                  (putBITp (16, mpRJUSTxNUMBER,
                            getBIT (16, mPASS1xSET_DEBUG_SWITCHxI)),
                   putBITp (16, mpRJUSTxTOTAL_LENGTH,
                            fixedToBit (32, (int32_t)(5))),
                   pRJUST (0)));
              OUTPUT (0, stringRHS);
              stringRHS->inUse = 0;
            }
          }
      } // End of DO for-loop block
    es1:;
    } // End of DO block
  {
    reentryGuard = 0;
    return 0;
  }
}
