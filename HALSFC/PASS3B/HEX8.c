/*
  File HEX8.c generated by XCOM-I, 2024-08-08 04:35:09.
*/

#include "runtimeC.h"

descriptor_t *
HEX8 (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "HEX8");
  // T = ''; (750)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "");
    putCHARACTER (mHEX8xT, stringRHS);
    stringRHS->inUse = 0;
  }
  // DO I = 0 TO 7; (751)
  {
    int32_t from22, to22, by22;
    from22 = 0;
    to22 = 7;
    by22 = 1;
    for (putFIXED (mHEX8xI, from22); getFIXED (mHEX8xI) <= to22;
         putFIXED (mHEX8xI, getFIXED (mHEX8xI) + by22))
      {
        // T = T || SUBSTR(HEXCODES, SHR(HVAL, SHL(7-I,2))& 15, 1); (752)
        {
          descriptor_t *stringRHS;
          stringRHS = xsCAT (
              getCHARACTER (mHEX8xT),
              SUBSTR (getCHARACTER (mHEXCODES),
                      xAND (SHR (getFIXED (mHEX8xHVAL),
                                 SHL (xsubtract (7, getFIXED (mHEX8xI)), 2)),
                            15),
                      1));
          putCHARACTER (mHEX8xT, stringRHS);
          stringRHS->inUse = 0;
        }
      }
  } // End of DO for-loop block
  // RETURN T; (753)
  {
    reentryGuard = 0;
    return getCHARACTER (mHEX8xT);
  }
}
