/*
  File VALIDITY.c generated by XCOM-I, 2024-08-09 12:41:08.
*/

#include "runtimeC.h"

descriptor_t *
VALIDITY (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "VALIDITY");
  // PTR = REL(PTR); (957)
  {
    descriptor_t *bitRHS
        = getBIT (16, getFIXED (mSYM_SHRINK)
                          + 2 * (COREHALFWORD (mVALIDITYxPTR)) + 0 + 2 * (0));
    putBIT (16, mVALIDITYxPTR, bitRHS);
    bitRHS->inUse = 0;
  }
  // WD# = SHR(PTR,5); (958)
  {
    int32_t numberRHS = (int32_t)(SHR (COREHALFWORD (mVALIDITYxPTR), 5));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mVALIDITYxWDp, bitRHS);
    bitRHS->inUse = 0;
  }
  // RETURN SHR(VALIDITY_ARRAY(WD#),PTR &  31) &  1; (959)
  {
    reentryGuard = 0;
    return fixedToBit (
        32,
        (int32_t)(xAND (
            SHR (getFIXED (getFIXED (mVAL_TABLE) + 4 * (COREHALFWORD (mLEVEL))
                           + 0 + 4 * (COREHALFWORD (mVALIDITYxWDp))),
                 xAND (COREHALFWORD (mVALIDITYxPTR), 31)),
            1)));
  }
}
