/*
  File SEARCH_DIMENSION.c generated by XCOM-I, 2024-08-09 12:41:08.
*/

#include "runtimeC.h"

int32_t
SEARCH_DIMENSION (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SEARCH_DIMENSION");
  // DO CASE SHR(XHALMAT_QUAL(PTR),4); (3813)
  {
  rs1:
    switch (SHR (bitToFixed ((putBITp (16, mXHALMAT_QUALxPTR,
                                       getBIT (16, mSEARCH_DIMENSIONxPTR)),
                              XHALMAT_QUAL (0))),
                 4))
      {
      case 0:
          // ; (3815)
          ;
        break;
      case 1:
        // CALL GET_LOOP_DIMENSION(PTR); (3816)
        {
          putBITp (16, mGET_LOOP_DIMENSIONxPTR,
                   getBIT (16, mSEARCH_DIMENSIONxPTR));
          GET_LOOP_DIMENSION (0);
        }
        break;
      case 2:
          // ; (3817)
          ;
        break;
      case 3:
        // DO; (3818)
        {
        rs1s1:;
          // PTR = SHR(OPR(PTR),16); (3818)
          {
            int32_t numberRHS = (int32_t)(SHR (
                getFIXED (mOPR + 4 * COREHALFWORD (mSEARCH_DIMENSIONxPTR)),
                16));
            descriptor_t *bitRHS;
            bitRHS = fixedToBit (32, (int32_t)(numberRHS));
            putBIT (16, mSEARCH_DIMENSIONxPTR, bitRHS);
            bitRHS->inUse = 0;
          }
          // IF OPOP(PTR) = DSUB THEN (3819)
          if (1
              & (xEQ (
                  bitToFixed ((putBITp (16, mOPOPxPTR,
                                        getBIT (16, mSEARCH_DIMENSIONxPTR)),
                               OPOP (0))),
                  COREHALFWORD (mDSUB))))
            // CALL GET_LOOP_DIMENSION(PTR,1); (3820)
            {
              putBITp (16, mGET_LOOP_DIMENSIONxPTR,
                       getBIT (16, mSEARCH_DIMENSIONxPTR));
              putBITp (8, mGET_LOOP_DIMENSIONxDSUB_FLAG,
                       fixedToBit (32, (int32_t)(1)));
              GET_LOOP_DIMENSION (0);
            }
        es1s1:;
        } // End of DO block
        break;
      case 4:
        // CALL GET_LOOP_DIMENSION(LAST_OPERAND(SHR(OPR(PTR),16))); (3822)
        {
          putBITp (
              16, mGET_LOOP_DIMENSIONxPTR,
              (putBITp (
                   16, mLAST_OPERANDxPTR,
                   fixedToBit (
                       32, (int32_t)(SHR (
                               getFIXED (
                                   mOPR
                                   + 4 * COREHALFWORD (mSEARCH_DIMENSIONxPTR)),
                               16)))),
               LAST_OPERAND (0)));
          GET_LOOP_DIMENSION (0);
        }
        break;
      case 5:
          // ; (3823)
          ;
        break;
      case 6:
          // ; (3824)
          ;
        break;
      case 7:
          // ; (3825)
          ;
        break;
      case 8:
          // ; (3826)
          ;
        break;
      case 9:
          // ; (3827)
          ;
        break;
      case 10:
          // ; (3828)
          ;
        break;
      case 11:
          // ; (3829)
          ;
        break;
      }
  } // End of DO CASE block
  {
    reentryGuard = 0;
    return 0;
  }
}
