/*
  File CHECK_INVARxBUMP_CSE.c generated by XCOM-I, 2024-08-09 12:41:08.
*/

#include "runtimeC.h"

int32_t
CHECK_INVARxBUMP_CSE (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "CHECK_INVARxBUMP_CSE");
  // CSE_FOUND_INX = CSE_FOUND_INX + 1; (5326)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mCSE_FOUND_INX), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mCSE_FOUND_INX, bitRHS);
    bitRHS->inUse = 0;
  }
  // CSE(CSE_FOUND_INX) = NODE(INX); (5327)
  {
    int32_t numberRHS
        = (int32_t)(getFIXED (mNODE + 4 * COREHALFWORD (mCHECK_INVARxINX)));
    putFIXED (mCSE + 4 * (COREHALFWORD (mCSE_FOUND_INX)), numberRHS);
  }
  // CSE2(CSE_FOUND_INX) = NODE2(INX); (5328)
  {
    descriptor_t *bitRHS
        = getBIT (16, mNODE2 + 2 * COREHALFWORD (mCHECK_INVARxINX));
    putBIT (16, mCSE2 + 2 * (COREHALFWORD (mCSE_FOUND_INX)), bitRHS);
    bitRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
