/*
  File SYMBOL_NAME.c generated by XCOM-I, 2024-08-08 04:33:20.
*/

#include "runtimeC.h"

descriptor_t *
SYMBOL_NAME (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SYMBOL_NAME");
  // SYMBNO = SYMB#; (866)
  {
    descriptor_t *bitRHS = getBIT (16, mSYMBOL_NAMExSYMBp);
    putBIT (16, getFIXED (mCOMMTABL_HALFWORD) + 2 * (9), bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL MONITOR(22,9); (867)
  MONITOR22 (9);
  // LOC_PTR = PNTR; (868)
  {
    int32_t numberRHS
        = (int32_t)(getFIXED (getFIXED (mCOMMTABL_FULLWORD) + 4 * 6));
    putFIXED (mLOC_PTR, numberRHS);
  }
  // LOC_ADDR = ADDRESS; (869)
  {
    int32_t numberRHS
        = (int32_t)(getFIXED (getFIXED (mCOMMTABL_FULLWORD) + 4 * 7));
    putFIXED (mLOC_ADDR, numberRHS);
  }
  // TMP = SHL((SYMBNLEN - 1),24); (870)
  {
    int32_t numberRHS = (int32_t)(SHL (
        xsubtract (BYTE0 (getFIXED (mCOMMTABL_BYTE) + 1 * 23), 1), 24));
    putFIXED (mTMP, numberRHS);
  }
  // COREWORD(ADDR(SNAME)) = SYMBNAM + TMP; (871)
  {
    int32_t numberRHS = (int32_t)(xadd (xadd (getFIXED (mCOMMTABL_ADDR), 88),
                                        getFIXED (mTMP)));
    COREWORD2 (ADDR (NULL, 0, "SYMBOL_NAMExSNAME", 0), numberRHS);
  }
  // RETURN SNAME; (872)
  {
    reentryGuard = 0;
    return getCHARACTER (mSYMBOL_NAMExSNAME);
  }
}
