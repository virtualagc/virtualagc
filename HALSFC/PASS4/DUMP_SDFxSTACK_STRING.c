/*
  File DUMP_SDFxSTACK_STRING.c generated by XCOM-I, 2024-08-08 04:33:20.
*/

#include "runtimeC.h"

int32_t
DUMP_SDFxSTACK_STRING (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "DUMP_SDFxSTACK_STRING");
  // VN_INX = VN_INX + 1; (2087)
  {
    int32_t numberRHS = (int32_t)(xadd (COREHALFWORD (mVN_INX), 1));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mVN_INX, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF VN_INX > RECORD_TOP(VARNAME_REC) THEN (2088)
  if (1
      & (xGT (COREHALFWORD (mVN_INX),
              xsubtract (COREWORD (xadd (
                             ADDR ("VARNAME_REC", 0x80000000, NULL, 0), 12)),
                         1))))
    // DO ; (2089)
    {
    rs1:;
      // IF COREWORD ( ADDR ( VARNAME_REC ) + 12 ) >= COREWORD ( ADDR (
      // VARNAME_REC ) + 8 ) THEN (2090)
      if (1
          & (xGE (
              COREWORD (xadd (ADDR ("VARNAME_REC", 0x80000000, NULL, 0), 12)),
              COREWORD (xadd (ADDR ("VARNAME_REC", 0x80000000, NULL, 0), 8)))))
        // CALL _NEEDMORE_SPACE ( ADDR ( VARNAME_REC )  ) ; (2091)
        {
          putFIXED (m_NEEDMORE_SPACExDOPE,
                    ADDR ("VARNAME_REC", 0x80000000, NULL, 0));
          _NEEDMORE_SPACE (0);
        }
      // COREWORD ( ADDR ( VARNAME_REC ) + 12 ) = COREWORD ( ADDR ( VARNAME_REC
      // ) + 12 ) + 1 ; (2092)
      {
        int32_t numberRHS = (int32_t)(xadd (
            COREWORD (xadd (ADDR ("VARNAME_REC", 0x80000000, NULL, 0), 12)),
            1));
        COREWORD2 (xadd (ADDR ("VARNAME_REC", 0x80000000, NULL, 0), 12),
                   numberRHS);
      }
    es1:;
    } // End of DO block
  // VARNAME(VN_INX) = STRING; (2093)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mDUMP_SDFxSTACK_STRINGxSTRING);
    putCHARACTER (getFIXED (mVARNAME_REC) + 4 * (0) + 0
                      + 4 * (COREHALFWORD (mVN_INX)),
                  stringRHS);
    stringRHS->inUse = 0;
  }
  // CALL STACK_PTR( 3221225472 | VN_INX); (2094)
  {
    putFIXED (mDUMP_SDFxSTACK_PTRxPTR,
              xOR (3221225472, COREHALFWORD (mVN_INX)));
    DUMP_SDFxSTACK_PTR (0);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
