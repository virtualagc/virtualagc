/*
  File PRINTSUMMARY.c generated by XCOM-I, 2024-08-09 12:40:27.
*/

#include "runtimeC.h"

int32_t
PRINTSUMMARY (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PRINTSUMMARY");
  // OUTPUT(1) = '1'; (2615)
  {
    descriptor_t *stringRHS;
    stringRHS = cToDescriptor (NULL, "1");
    OUTPUT (1, stringRHS);
    stringRHS->inUse = 0;
  }
  // T = TIME; (2616)
  {
    int32_t numberRHS = (int32_t)(TIME ());
    putFIXED (mPRINTSUMMARYxT, numberRHS);
  }
  // CALL PRINT_DATE_AND_TIME('END OF SDFLIST ',DATE,T); (2617)
  {
    putCHARACTERp (mPRINT_DATE_AND_TIMExMESSAGE,
                   cToDescriptor (NULL, "END OF SDFLIST "));
    putFIXED (mPRINT_DATE_AND_TIMExD, DATE ());
    putFIXED (mPRINT_DATE_AND_TIMExT, getFIXED (mPRINTSUMMARYxT));
    PRINT_DATE_AND_TIME (0);
  }
  // SPACE_1; (2618)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mX1);
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'NUMBER OF SDFPKG LOCATES          = '|| SDFPKG_LOCATES; (2619)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (cToDescriptor (NULL, "NUMBER OF SDFPKG LOCATES          = "),
                 fixedToCharacter (getFIXED (mSDFPKG_LOCATES)));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'NUMBER OF SDFPKG READS            = '|| SDFPKG_READS; (2620)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (cToDescriptor (NULL, "NUMBER OF SDFPKG READS            = "),
                 fixedToCharacter (getFIXED (mSDFPKG_READS)));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'NUMBER OF SDFPKG SELECTS          = '|| SDFPKG_SLECTCNT; (2621)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (cToDescriptor (NULL, "NUMBER OF SDFPKG SELECTS          = "),
                 fixedToCharacter (getFIXED (mSDFPKG_SLECTCNT)));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'NUMBER OF SDFPKG GETMAINS         = '|| SDFPKG_NUMGETM; (2622)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (cToDescriptor (NULL, "NUMBER OF SDFPKG GETMAINS         = "),
                 bitToCharacter (getBIT (16, mSDFPKG_NUMGETM)));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'SDFPKG FCB AREA SIZE (BYTES)      = '|| SDFPKG_FCBAREA; (2623)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (cToDescriptor (NULL, "SDFPKG FCB AREA SIZE (BYTES)      = "),
                 fixedToCharacter (getFIXED (mSDFPKG_FCBAREA)));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'SDFPKG PAGING AREA SIZE (PAGES)   = '|| SDFPKG_PGAREA; (2624)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (cToDescriptor (NULL, "SDFPKG PAGING AREA SIZE (PAGES)   = "),
                 bitToCharacter (getBIT (16, mSDFPKG_PGAREA)));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = X1; (2625)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mX1);
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'NUMBER OF MINOR COMPACTIFIES      = '|| COMPACTIFIES; (2626)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (cToDescriptor (NULL, "NUMBER OF MINOR COMPACTIFIES      = "),
                 bitToCharacter (getBIT (16, mCOMPACTIFIES)));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'NUMBER OF MAJOR COMPACTIFIES      = '|| COMPACTIFIES(1); (2627)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (cToDescriptor (NULL, "NUMBER OF MAJOR COMPACTIFIES      = "),
                 bitToCharacter (getBIT (16, mCOMPACTIFIES + 2 * 1)));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = 'NUMBER OF REALLOCATIONS           = '|| REALLOCATIONS; (2628)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (cToDescriptor (NULL, "NUMBER OF REALLOCATIONS           = "),
                 bitToCharacter (getBIT (16, mREALLOCATIONS)));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = X1; (2629)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mX1);
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  // DO I = 1 TO 2; (2630)
  {
    int32_t from48, to48, by48;
    from48 = 1;
    to48 = 2;
    by48 = 1;
    for (putBIT (16, mPRINTSUMMARYxI, fixedToBit (16, from48));
         bitToFixed (getBIT (16, mPRINTSUMMARYxI)) <= to48;
         putBIT (16, mPRINTSUMMARYxI,
                 fixedToBit (16, bitToFixed (getBIT (16, mPRINTSUMMARYxI))
                                     + by48)))
      {
        // IF CLOCK(I) < CLOCK(I - 1) THEN (2631)
        if (1
            & (xLT (getFIXED (mCLOCK + 4 * COREHALFWORD (mPRINTSUMMARYxI)),
                    getFIXED (
                        mCLOCK
                        + 4 * xsubtract (COREHALFWORD (mPRINTSUMMARYxI), 1)))))
          // CLOCK(I) = CLOCK(I) + 8640000; (2632)
          {
            int32_t numberRHS = (int32_t)(xadd (
                getFIXED (mCLOCK + 4 * COREHALFWORD (mPRINTSUMMARYxI)),
                8640000));
            putFIXED (mCLOCK + 4 * (COREHALFWORD (mPRINTSUMMARYxI)),
                      numberRHS);
          }
      }
  } // End of DO for-loop block
  // CALL PRINT_TIME('TOTAL CPU TIME IN SDFLIST              ', CLOCK(2) -
  // CLOCK); (2633)
  {
    putCHARACTERp (
        mPRINT_TIMExMESSAGE,
        cToDescriptor (NULL, "TOTAL CPU TIME IN SDFLIST              "));
    putFIXED (mPRINT_TIMExT,
              xsubtract (getFIXED (mCLOCK + 4 * 2), getFIXED (mCLOCK)));
    PRINT_TIME (0);
  }
  // OUTPUT = X1; (2634)
  {
    descriptor_t *stringRHS;
    stringRHS = getCHARACTER (mX1);
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
