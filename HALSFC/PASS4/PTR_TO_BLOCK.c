/*
  File PTR_TO_BLOCK.c generated by XCOM-I, 2024-08-09 12:40:27.
*/

#include "runtimeC.h"

descriptor_t *
PTR_TO_BLOCK (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PTR_TO_BLOCK");
  // IF PTR = 0 THEN (873)
  if (1 & (xEQ (getFIXED (mPTR_TO_BLOCKxPTR), 0)))
    // RETURN 0; (874)
    {
      reentryGuard = 0;
      return fixedToBit (32, (int32_t)(0));
    }
  // CALL SDF_LOCATE(PTR,ADDR(NODE_H),0); (875)
  {
    putFIXED (mSDF_LOCATExPTR, getFIXED (mPTR_TO_BLOCKxPTR));
    putFIXED (mSDF_LOCATExBVAR,
              ADDR ("PTR_TO_BLOCKxNODE_H", 0x80000000, NULL, 0));
    putBITp (8, mSDF_LOCATExFLAGS, fixedToBit (32, (int32_t)(0)));
    SDF_LOCATE (0);
  }
  // RETURN NODE_H(13); (876)
  {
    reentryGuard = 0;
    return getBIT (16, getFIXED (mPTR_TO_BLOCKxNODE_H) + 2 * 13);
  }
}
