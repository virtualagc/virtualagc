/*
  File DUMP_SDFxPRINT_ADDRS.c generated by XCOM-I, 2024-08-08 04:33:20.
*/

#include "runtimeC.h"

int32_t
DUMP_SDFxPRINT_ADDRS (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "DUMP_SDFxPRINT_ADDRS");
  // ADDR1_HEX = HEX8(SADDR); (1996)
  {
    descriptor_t *stringRHS;
    stringRHS = (putFIXED (mHEX8xHVAL, getFIXED (mDUMP_SDFxSADDR)), HEX8 (0));
    putCHARACTER (mDUMP_SDFxADDR1_HEX, stringRHS);
    stringRHS->inUse = 0;
  }
  // ADDR2_HEX = HEX8(SEXTENT); (1997)
  {
    descriptor_t *stringRHS;
    stringRHS
        = (putFIXED (mHEX8xHVAL, getFIXED (mDUMP_SDFxSEXTENT)), HEX8 (0));
    putCHARACTER (mDUMP_SDFxADDR2_HEX, stringRHS);
    stringRHS->inUse = 0;
  }
  // ADDR1_DEC = FORMAT(SADDR,6); (1998)
  {
    descriptor_t *stringRHS;
    stringRHS = (putFIXED (mFORMATxIVAL, getFIXED (mDUMP_SDFxSADDR)),
                 putFIXED (mFORMATxN, 6), FORMAT (0));
    putCHARACTER (mDUMP_SDFxADDR1_DEC, stringRHS);
    stringRHS->inUse = 0;
  }
  // ADDR2_DEC = FORMAT(SEXTENT,6); (1999)
  {
    descriptor_t *stringRHS;
    stringRHS = (putFIXED (mFORMATxIVAL, getFIXED (mDUMP_SDFxSEXTENT)),
                 putFIXED (mFORMATxN, 6), FORMAT (0));
    putCHARACTER (mDUMP_SDFxADDR2_DEC, stringRHS);
    stringRHS->inUse = 0;
  }
  // IF SOFFSET > 0 THEN (2000)
  if (1 & (xGT (getFIXED (mDUMP_SDFxSOFFSET), 0)))
    // DO; (2001)
    {
    rs1:;
      // ADDR3_HEX = HEX8(SOFFSET); (2002)
      {
        descriptor_t *stringRHS;
        stringRHS
            = (putFIXED (mHEX8xHVAL, getFIXED (mDUMP_SDFxSOFFSET)), HEX8 (0));
        putCHARACTER (mDUMP_SDFxADDR3_HEX, stringRHS);
        stringRHS->inUse = 0;
      }
      // ADDR3_DEC = FORMAT(SOFFSET,6); (2003)
      {
        descriptor_t *stringRHS;
        stringRHS = (putFIXED (mFORMATxIVAL, getFIXED (mDUMP_SDFxSOFFSET)),
                     putFIXED (mFORMATxN, 6), FORMAT (0));
        putCHARACTER (mDUMP_SDFxADDR3_DEC, stringRHS);
        stringRHS->inUse = 0;
      }
      // TS(2) = X2 || 'BIAS = ' || ADDR3_HEX || '(' || ADDR3_DEC || ')';
      // (2004)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            xsCAT (xsCAT (xsCAT (xsCAT (getCHARACTER (mX2),
                                        cToDescriptor (NULL, "BIAS = ")),
                                 getCHARACTER (mDUMP_SDFxADDR3_HEX)),
                          cToDescriptor (NULL, "(")),
                   getCHARACTER (mDUMP_SDFxADDR3_DEC)),
            cToDescriptor (NULL, ")"));
        putCHARACTER (mDUMP_SDFxTS + 4 * (2), stringRHS);
        stringRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // ELSE (2005)
  else
    // TS(2) = ''; (2006)
    {
      descriptor_t *stringRHS;
      stringRHS = cToDescriptor (NULL, "");
      putCHARACTER (mDUMP_SDFxTS + 4 * (2), stringRHS);
      stringRHS->inUse = 0;
    }
  // TS = X10 || 'ADDRESS = '; (2007)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (getCHARACTER (mX10), cToDescriptor (NULL, "ADDRESS = "));
    putCHARACTER (mDUMP_SDFxTS, stringRHS);
    stringRHS->inUse = 0;
  }
  // TS(1) = X2 || 'SIZE = '; (2008)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (getCHARACTER (mX2), cToDescriptor (NULL, "SIZE = "));
    putCHARACTER (mDUMP_SDFxTS + 4 * (1), stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = TS || ADDR1_HEX || '(' || ADDR1_DEC || ')' || TS(1) || ADDR2_HEX
  // || '(' || ADDR2_DEC || ')' || TS(2); (2009)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (
        xsCAT (
            xsCAT (
                xsCAT (
                    xsCAT (
                        xsCAT (
                            xsCAT (
                                xsCAT (
                                    xsCAT (xsCAT (getCHARACTER (mDUMP_SDFxTS),
                                                  getCHARACTER (
                                                      mDUMP_SDFxADDR1_HEX)),
                                           cToDescriptor (NULL, "(")),
                                    getCHARACTER (mDUMP_SDFxADDR1_DEC)),
                                cToDescriptor (NULL, ")")),
                            getCHARACTER (mDUMP_SDFxTS + 4 * 1)),
                        getCHARACTER (mDUMP_SDFxADDR2_HEX)),
                    cToDescriptor (NULL, "(")),
                getCHARACTER (mDUMP_SDFxADDR2_DEC)),
            cToDescriptor (NULL, ")")),
        getCHARACTER (mDUMP_SDFxTS + 4 * 2));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
