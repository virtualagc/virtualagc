/*
  File DUMP_SDFxFORMAT_FORM_PARM_CELL.c generated by XCOM-I, 2024-08-09
  12:40:27.
*/

#include "runtimeC.h"

int32_t
DUMP_SDFxFORMAT_FORM_PARM_CELL (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "DUMP_SDFxFORMAT_FORM_PARM_CELL");
  // S = S || SYMBOL_NAME(SYMB#); (2095)
  {
    descriptor_t *stringRHS;
    stringRHS
        = xsCAT (getCHARACTER (mS),
                 (putBITp (16, mSYMBOL_NAMExSYMBp,
                           getBIT (16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxSYMBp)),
                  SYMBOL_NAME (0)));
    putCHARACTER (mS, stringRHS);
    stringRHS->inUse = 0;
  }
  // CALL SDF_LOCATE(PTR,ADDR(VMEM_H),RESV); (2096)
  {
    putFIXED (mSDF_LOCATExPTR, getFIXED (mDUMP_SDFxFORMAT_FORM_PARM_CELLxPTR));
    putFIXED (mSDF_LOCATExBVAR, ADDR ("VMEM_H", 0x80000000, NULL, 0));
    putBITp (8, mSDF_LOCATExFLAGS, getBIT (8, mRESV));
    SDF_LOCATE (0);
  }
  // COREWORD(ADDR(VMEM_B)) = LOC_ADDR; (2097)
  {
    int32_t numberRHS = (int32_t)(getFIXED (mLOC_ADDR));
    COREWORD2 (ADDR ("VMEM_B", 0x80000000, NULL, 0), numberRHS);
  }
  // K = 0; (2098)
  {
    int32_t numberRHS = (int32_t)(0);
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxK, bitRHS);
    bitRHS->inUse = 0;
  }
  // S = S || '('; (2099)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (getCHARACTER (mS), cToDescriptor (NULL, "("));
    putCHARACTER (mS, stringRHS);
    stringRHS->inUse = 0;
  }
  // IF VMEM_B(3) > 0 THEN (2100)
  if (1 & (xGT (BYTE0 (getFIXED (mVMEM_B) + 1 * 3), 0)))
    // DO; (2101)
    {
    rs1:;
      // DO J = 2 TO VMEM_B(3) + 1; (2102)
      {
        int32_t from37, to37, by37;
        from37 = 2;
        to37 = xadd (BYTE0 (getFIXED (mVMEM_B) + 1 * 3), 1);
        by37 = 1;
        for (putBIT (16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxJ,
                     fixedToBit (16, from37));
             bitToFixed (getBIT (16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxJ))
             <= to37;
             putBIT (
                 16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxJ,
                 fixedToBit (16, bitToFixed (getBIT (
                                     16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxJ))
                                     + by37)))
          {
            // IF LENGTH(S(K)) > LINELENGTH THEN (2103)
            if (1
                & (xGT (LENGTH (getCHARACTER (
                            mS
                            + 4
                                  * COREHALFWORD (
                                      mDUMP_SDFxFORMAT_FORM_PARM_CELLxK))),
                        131)))
              // K = K + 1; (2104)
              {
                int32_t numberRHS = (int32_t)(xadd (
                    COREHALFWORD (mDUMP_SDFxFORMAT_FORM_PARM_CELLxK), 1));
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxK, bitRHS);
                bitRHS->inUse = 0;
              }
            // S(K) = S(K) || SYMBOL_NAME(VMEM_H(J)) || ','; (2105)
            {
              descriptor_t *stringRHS;
              stringRHS = xsCAT (
                  xsCAT (
                      getCHARACTER (
                          mS
                          + 4
                                * COREHALFWORD (
                                    mDUMP_SDFxFORMAT_FORM_PARM_CELLxK)),
                      (putBITp (
                           16, mSYMBOL_NAMExSYMBp,
                           getBIT (
                               16,
                               getFIXED (mVMEM_H)
                                   + 2
                                         * COREHALFWORD (
                                             mDUMP_SDFxFORMAT_FORM_PARM_CELLxJ))),
                       SYMBOL_NAME (0))),
                  cToDescriptor (NULL, ","));
              putCHARACTER (
                  mS + 4 * (COREHALFWORD (mDUMP_SDFxFORMAT_FORM_PARM_CELLxK)),
                  stringRHS);
              stringRHS->inUse = 0;
            }
          }
      } // End of DO for-loop block
      // BYTE(S(K),LENGTH(S(K))-1) = BYTE(')'); (2106)
      {
        int32_t numberRHS = (int32_t)(BYTE1 (cToDescriptor (NULL, ")")));
        lBYTEb (
            ADDR (NULL, 0, "S",
                  COREHALFWORD (mDUMP_SDFxFORMAT_FORM_PARM_CELLxK)),
            xsubtract (
                LENGTH (getCHARACTER (
                    mS
                    + 4 * COREHALFWORD (mDUMP_SDFxFORMAT_FORM_PARM_CELLxK))),
                1),
            BYTE1 (cToDescriptor (NULL, ")")));
      }
    es1:;
    } // End of DO block
  // ELSE (2107)
  else
    // S = S || ')'; (2108)
    {
      descriptor_t *stringRHS;
      stringRHS = xsCAT (getCHARACTER (mS), cToDescriptor (NULL, ")"));
      putCHARACTER (mS, stringRHS);
      stringRHS->inUse = 0;
    }
  // IF VMEM_B(2) > VMEM_B(3) THEN (2109)
  if (1
      & (xGT (BYTE0 (getFIXED (mVMEM_B) + 1 * 2),
              BYTE0 (getFIXED (mVMEM_B) + 1 * 3))))
    // DO; (2110)
    {
    rs2:;
      // S(K) = S(K) || ' ASSIGN('; (2111)
      {
        descriptor_t *stringRHS;
        stringRHS = xsCAT (
            getCHARACTER (
                mS + 4 * COREHALFWORD (mDUMP_SDFxFORMAT_FORM_PARM_CELLxK)),
            cToDescriptor (NULL, " ASSIGN("));
        putCHARACTER (
            mS + 4 * (COREHALFWORD (mDUMP_SDFxFORMAT_FORM_PARM_CELLxK)),
            stringRHS);
        stringRHS->inUse = 0;
      }
      // DO J = VMEM_B(3)+2 TO VMEM_B(2)+1; (2112)
      {
        int32_t from38, to38, by38;
        from38 = xadd (BYTE0 (getFIXED (mVMEM_B) + 1 * 3), 2);
        to38 = xadd (BYTE0 (getFIXED (mVMEM_B) + 1 * 2), 1);
        by38 = 1;
        for (putBIT (16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxJ,
                     fixedToBit (16, from38));
             bitToFixed (getBIT (16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxJ))
             <= to38;
             putBIT (
                 16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxJ,
                 fixedToBit (16, bitToFixed (getBIT (
                                     16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxJ))
                                     + by38)))
          {
            // IF LENGTH(S(K)) > LINELENGTH THEN (2113)
            if (1
                & (xGT (LENGTH (getCHARACTER (
                            mS
                            + 4
                                  * COREHALFWORD (
                                      mDUMP_SDFxFORMAT_FORM_PARM_CELLxK))),
                        131)))
              // K = K + 1; (2114)
              {
                int32_t numberRHS = (int32_t)(xadd (
                    COREHALFWORD (mDUMP_SDFxFORMAT_FORM_PARM_CELLxK), 1));
                descriptor_t *bitRHS;
                bitRHS = fixedToBit (32, (int32_t)(numberRHS));
                putBIT (16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxK, bitRHS);
                bitRHS->inUse = 0;
              }
            // S(K) = S(K) || SYMBOL_NAME(VMEM_H(J)) || ','; (2115)
            {
              descriptor_t *stringRHS;
              stringRHS = xsCAT (
                  xsCAT (
                      getCHARACTER (
                          mS
                          + 4
                                * COREHALFWORD (
                                    mDUMP_SDFxFORMAT_FORM_PARM_CELLxK)),
                      (putBITp (
                           16, mSYMBOL_NAMExSYMBp,
                           getBIT (
                               16,
                               getFIXED (mVMEM_H)
                                   + 2
                                         * COREHALFWORD (
                                             mDUMP_SDFxFORMAT_FORM_PARM_CELLxJ))),
                       SYMBOL_NAME (0))),
                  cToDescriptor (NULL, ","));
              putCHARACTER (
                  mS + 4 * (COREHALFWORD (mDUMP_SDFxFORMAT_FORM_PARM_CELLxK)),
                  stringRHS);
              stringRHS->inUse = 0;
            }
          }
      } // End of DO for-loop block
      // BYTE(S(K),LENGTH(S(K))-1) = BYTE(')'); (2116)
      {
        int32_t numberRHS = (int32_t)(BYTE1 (cToDescriptor (NULL, ")")));
        lBYTEb (
            ADDR (NULL, 0, "S",
                  COREHALFWORD (mDUMP_SDFxFORMAT_FORM_PARM_CELLxK)),
            xsubtract (
                LENGTH (getCHARACTER (
                    mS
                    + 4 * COREHALFWORD (mDUMP_SDFxFORMAT_FORM_PARM_CELLxK))),
                1),
            BYTE1 (cToDescriptor (NULL, ")")));
      }
    es2:;
    } // End of DO block
  // CALL SDF_PTR_LOCATE(PTR,RELS); (2117)
  {
    putFIXED (mSDF_PTR_LOCATExPTR,
              getFIXED (mDUMP_SDFxFORMAT_FORM_PARM_CELLxPTR));
    putBITp (8, mSDF_PTR_LOCATExFLAGS, getBIT (8, mRELS));
    SDF_PTR_LOCATE (0);
  }
  // CALL FLUSH(K); (2118)
  {
    putBITp (16, mDUMP_SDFxFLUSHxLAST,
             getBIT (16, mDUMP_SDFxFORMAT_FORM_PARM_CELLxK));
    DUMP_SDFxFLUSH (0);
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
