/*
  File BLOCK_NAME.c generated by XCOM-I, 2024-08-09 12:40:27.
*/

#include "runtimeC.h"

descriptor_t *
BLOCK_NAME (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (descriptor_t *)0;
    }
  reentryGuard = guardReentry (reentryGuard, "BLOCK_NAME");
  // BLKNO = BLK#; (859)
  {
    descriptor_t *bitRHS = getBIT (16, mBLOCK_NAMExBLKp);
    putBIT (16, getFIXED (mCOMMTABL_HALFWORD) + 2 * (8), bitRHS);
    bitRHS->inUse = 0;
  }
  // CALL MONITOR(22,8); (860)
  MONITOR22 (8);
  // LOC_PTR = PNTR; (861)
  {
    int32_t numberRHS
        = (int32_t)(getFIXED (getFIXED (mCOMMTABL_FULLWORD) + 4 * 6));
    putFIXED (mLOC_PTR, numberRHS);
  }
  // LOC_ADDR = ADDRESS; (862)
  {
    int32_t numberRHS
        = (int32_t)(getFIXED (getFIXED (mCOMMTABL_FULLWORD) + 4 * 7));
    putFIXED (mLOC_ADDR, numberRHS);
  }
  // TMP = SHL((BLKNLEN - 1),24); (863)
  {
    int32_t numberRHS = (int32_t)(SHL (
        xsubtract (BYTE0 (getFIXED (mCOMMTABL_BYTE) + 1 * 22), 1), 24));
    putFIXED (mTMP, numberRHS);
  }
  // COREWORD(ADDR(BNAME)) = BLKNAM + TMP; (864)
  {
    int32_t numberRHS = (int32_t)(xadd (xadd (getFIXED (mCOMMTABL_ADDR), 56),
                                        getFIXED (mTMP)));
    COREWORD2 (ADDR (NULL, 0, "BLOCK_NAMExBNAME", 0), numberRHS);
  }
  // RETURN BNAME; (865)
  {
    reentryGuard = 0;
    return getCHARACTER (mBLOCK_NAMExBNAME);
  }
}
