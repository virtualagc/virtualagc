/*
  File SDF_SELECT.c generated by XCOM-I, 2024-08-08 04:33:20.
*/

#include "runtimeC.h"

int32_t
SDF_SELECT (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "SDF_SELECT");
  // CALL MONITOR(22, 2147483655); (749)
  MONITOR22 (2147483655);
  // LOC_ADDR = ADDRESS; (750)
  {
    int32_t numberRHS
        = (int32_t)(getFIXED (getFIXED (mCOMMTABL_FULLWORD) + 4 * 7));
    putFIXED (mLOC_ADDR, numberRHS);
  }
  // COREWORD(ADDR(NODE_H)) = ADDRESS; (751)
  {
    int32_t numberRHS
        = (int32_t)(getFIXED (getFIXED (mCOMMTABL_FULLWORD) + 4 * 7));
    COREWORD2 (ADDR ("SDF_SELECTxNODE_H", 0x80000000, NULL, 0), numberRHS);
  }
  // COREWORD(ADDR(NODE_F)) = ADDRESS; (752)
  {
    int32_t numberRHS
        = (int32_t)(getFIXED (getFIXED (mCOMMTABL_FULLWORD) + 4 * 7));
    COREWORD2 (ADDR ("SDF_SELECTxNODE_F", 0x80000000, NULL, 0), numberRHS);
  }
  // LAST_PAGE = NODE_H(1); (753)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 1);
    putBIT (16, mLAST_PAGE, bitRHS);
    bitRHS->inUse = 0;
  }
  // EMITTED_CNT = NODE_F(6); (754)
  {
    int32_t numberRHS
        = (int32_t)(getFIXED (getFIXED (mSDF_SELECTxNODE_F) + 4 * 6));
    putFIXED (mEMITTED_CNT, numberRHS);
  }
  // #EXTERNALS = NODE_H(7); (755)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 7);
    putBIT (16, mpEXTERNALS, bitRHS);
    bitRHS->inUse = 0;
  }
  // #PROCS = NODE_H(8); (756)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 8);
    putBIT (16, mpPROCS, bitRHS);
    bitRHS->inUse = 0;
  }
  // #SYMBOLS = NODE_H(9); (757)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 9);
    putBIT (16, mpSYMBOLS, bitRHS);
    bitRHS->inUse = 0;
  }
  // FIRST_STMT = NODE_H(26); (758)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 26);
    putBIT (16, mFIRST_STMT, bitRHS);
    bitRHS->inUse = 0;
  }
  // LAST_STMT = NODE_H(27); (759)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 27);
    putBIT (16, mLAST_STMT, bitRHS);
    bitRHS->inUse = 0;
  }
  // #EXECS = NODE_H(28); (760)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 28);
    putBIT (16, mpEXECS, bitRHS);
    bitRHS->inUse = 0;
  }
  // #STMTS = NODE_H(29); (761)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 29);
    putBIT (16, mpSTMTS, bitRHS);
    bitRHS->inUse = 0;
  }
  // KEY_BLOCK = NODE_H(44); (762)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 44);
    putBIT (16, mKEY_BLOCK, bitRHS);
    bitRHS->inUse = 0;
  }
  // KEY_SYMB = NODE_H(14); (763)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 14);
    putBIT (16, mKEY_SYMB, bitRHS);
    bitRHS->inUse = 0;
  }
  // COMPUNIT = NODE_H(45); (764)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 45);
    putBIT (16, mCOMPUNIT, bitRHS);
    bitRHS->inUse = 0;
  }
  // TEMP = NODE_H(0); (765)
  {
    descriptor_t *bitRHS = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 0);
    putBIT (16, mSDF_SELECTxTEMP, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF (TEMP &  32768) ~= 0 THEN (766)
  if (1 & (xNEQ (xAND (COREHALFWORD (mSDF_SELECTxTEMP), 32768), 0)))
    // SRN_FLAG = TRUE; (767)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mSRN_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (768)
  else
    // SRN_FLAG = FALSE; (769)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mSRN_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF (TEMP &  16384) ~= 0 THEN (770)
  if (1 & (xNEQ (xAND (COREHALFWORD (mSDF_SELECTxTEMP), 16384), 0)))
    // ADDR_FLAG = TRUE; (771)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mADDR_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (772)
  else
    // ADDR_FLAG = FALSE; (773)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mADDR_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF (TEMP &  8192) ~= 0 THEN (774)
  if (1 & (xNEQ (xAND (COREHALFWORD (mSDF_SELECTxTEMP), 8192), 0)))
    // COMPOOL_FLAG = TRUE; (775)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mCOMPOOL_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (776)
  else
    // COMPOOL_FLAG = FALSE; (777)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mCOMPOOL_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF (TEMP &  4096) ~= 0 THEN (778)
  if (1 & (xNEQ (xAND (COREHALFWORD (mSDF_SELECTxTEMP), 4096), 0)))
    // FC_FLAG = TRUE; (779)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mFC_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (780)
  else
    // FC_FLAG = FALSE; (781)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mFC_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF (TEMP &  2048) ~= 0 THEN (782)
  if (1 & (xNEQ (xAND (COREHALFWORD (mSDF_SELECTxTEMP), 2048), 0)))
    // OVERFLOW_FLAG = TRUE; (783)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mOVERFLOW_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (784)
  else
    // OVERFLOW_FLAG = FALSE; (785)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mOVERFLOW_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF (TEMP &  1024) ~= 0 THEN (786)
  if (1 & (xNEQ (xAND (COREHALFWORD (mSDF_SELECTxTEMP), 1024), 0)))
    // SRN_FLAG1 = TRUE; (787)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mSRN_FLAG1, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (788)
  else
    // SRN_FLAG1 = FALSE; (789)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mSRN_FLAG1, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF (TEMP &  512) ~= 0 THEN (790)
  if (1 & (xNEQ (xAND (COREHALFWORD (mSDF_SELECTxTEMP), 512), 0)))
    // SRN_FLAG2 = TRUE; (791)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mSRN_FLAG2, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (792)
  else
    // SRN_FLAG2 = FALSE; (793)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mSRN_FLAG2, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF (TEMP &  256) ~= 0 THEN (794)
  if (1 & (xNEQ (xAND (COREHALFWORD (mSDF_SELECTxTEMP), 256), 0)))
    // NOTRACE_FLAG = TRUE; (795)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mNOTRACE_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (796)
  else
    // NOTRACE_FLAG = FALSE; (797)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mNOTRACE_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF (TEMP &  16) ~= 0 THEN (798)
  if (1 & (xNEQ (xAND (COREHALFWORD (mSDF_SELECTxTEMP), 16), 0)))
    // FCDATA_FLAG = TRUE; (799)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mFCDATA_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (800)
  else
    // FCDATA_FLAG = FALSE; (801)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mFCDATA_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF (TEMP &  8) ~= 0 THEN (802)
  if (1 & (xNEQ (xAND (COREHALFWORD (mSDF_SELECTxTEMP), 8), 0)))
    // SDL_FLAG = TRUE; (803)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mSDL_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (804)
  else
    // SDL_FLAG = FALSE; (805)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mSDL_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // IF (TEMP &  1) ~= 0 THEN (806)
  if (1 & (xNEQ (xAND (COREHALFWORD (mSDF_SELECTxTEMP), 1), 0)))
    // NEW_FLAG = TRUE; (807)
    {
      int32_t numberRHS = (int32_t)(1);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mNEW_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // ELSE (808)
  else
    // NEW_FLAG = FALSE; (809)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (1, mNEW_FLAG, bitRHS);
      bitRHS->inUse = 0;
    }
  // BASE_SYMB_PAGE = SHR(NODE_F(9),16) &  65535; (810)
  {
    int32_t numberRHS = (int32_t)(xAND (
        SHR (getFIXED (getFIXED (mSDF_SELECTxNODE_F) + 4 * 9), 16), 65535));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBASE_SYMB_PAGE, bitRHS);
    bitRHS->inUse = 0;
  }
  // BASE_SYMB_OFFSET = NODE_F(9) &  65535; (811)
  {
    int32_t numberRHS = (int32_t)(xAND (
        getFIXED (getFIXED (mSDF_SELECTxNODE_F) + 4 * 9), 65535));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBASE_SYMB_OFFSET, bitRHS);
    bitRHS->inUse = 0;
  }
  // BASE_STMT_PAGE = SHR(NODE_F(15),16) &  65535; (812)
  {
    int32_t numberRHS = (int32_t)(xAND (
        SHR (getFIXED (getFIXED (mSDF_SELECTxNODE_F) + 4 * 15), 16), 65535));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBASE_STMT_PAGE, bitRHS);
    bitRHS->inUse = 0;
  }
  // BASE_STMT_OFFSET = NODE_F(15) &  65535; (813)
  {
    int32_t numberRHS = (int32_t)(xAND (
        getFIXED (getFIXED (mSDF_SELECTxNODE_F) + 4 * 15), 65535));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBASE_STMT_OFFSET, bitRHS);
    bitRHS->inUse = 0;
  }
  // BASE_BLOCK_PAGE = SHR(NODE_F(5),16) &  65535; (814)
  {
    int32_t numberRHS = (int32_t)(xAND (
        SHR (getFIXED (getFIXED (mSDF_SELECTxNODE_F) + 4 * 5), 16), 65535));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBASE_BLOCK_PAGE, bitRHS);
    bitRHS->inUse = 0;
  }
  // BASE_BLOCK_OFFSET = NODE_F(5) &  65535; (815)
  {
    int32_t numberRHS = (int32_t)(xAND (
        getFIXED (getFIXED (mSDF_SELECTxNODE_F) + 4 * 5), 65535));
    descriptor_t *bitRHS;
    bitRHS = fixedToBit (32, (int32_t)(numberRHS));
    putBIT (16, mBASE_BLOCK_OFFSET, bitRHS);
    bitRHS->inUse = 0;
  }
  // IF SRN_FLAG THEN (816)
  if (1 & (bitToFixed (getBIT (1, mSRN_FLAG))))
    // DO; (817)
    {
    rs1:;
      // STMT_NODES = 140; (818)
      {
        int32_t numberRHS = (int32_t)(140);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mSTMT_NODES, bitRHS);
        bitRHS->inUse = 0;
      }
      // STMT_NODE_SIZE = 12; (819)
      {
        int32_t numberRHS = (int32_t)(12);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mSTMT_NODE_SIZE, bitRHS);
        bitRHS->inUse = 0;
      }
    es1:;
    } // End of DO block
  // ELSE (820)
  else
    // DO; (821)
    {
    rs2:;
      // STMT_NODES = 420; (822)
      {
        int32_t numberRHS = (int32_t)(420);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mSTMT_NODES, bitRHS);
        bitRHS->inUse = 0;
      }
      // STMT_NODE_SIZE = 4; (823)
      {
        int32_t numberRHS = (int32_t)(4);
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (16, mSTMT_NODE_SIZE, bitRHS);
        bitRHS->inUse = 0;
      }
    es2:;
    } // End of DO block
  // IF VERSION >= 35 THEN (824)
  if (1 & (xGE (COREHALFWORD (getFIXED (mDATABUF_HALFWORD) + 2 * 47), 35)))
    // DO; (825)
    {
    rs3:;
      // #BIFUNCS = NODE_H(92); (826)
      {
        descriptor_t *bitRHS
            = getBIT (16, getFIXED (mSDF_SELECTxNODE_H) + 2 * 92);
        putBIT (16, mpBIFUNCS, bitRHS);
        bitRHS->inUse = 0;
      }
      // BASE_BI_PTR = NODE_F(45); (827)
      {
        int32_t numberRHS
            = (int32_t)(getFIXED (getFIXED (mSDF_SELECTxNODE_F) + 4 * 45));
        descriptor_t *bitRHS;
        bitRHS = fixedToBit (32, (int32_t)(numberRHS));
        putBIT (32, mBASE_BI_PTR, bitRHS);
        bitRHS->inUse = 0;
      }
    es3:;
    } // End of DO block
  // ELSE (828)
  else
    // #BIFUNCS = 0; (829)
    {
      int32_t numberRHS = (int32_t)(0);
      descriptor_t *bitRHS;
      bitRHS = fixedToBit (32, (int32_t)(numberRHS));
      putBIT (16, mpBIFUNCS, bitRHS);
      bitRHS->inUse = 0;
    }
  {
    reentryGuard = 0;
    return 0;
  }
}
