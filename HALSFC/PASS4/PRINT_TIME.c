/*
  File PRINT_TIME.c generated by XCOM-I, 2024-08-09 12:40:27.
*/

#include "runtimeC.h"

int32_t
PRINT_TIME (int reset)
{
  static int reentryGuard = 0;
  if (reset)
    {
      reentryGuard = 0;
      return (int32_t)0;
    }
  reentryGuard = guardReentry (reentryGuard, "PRINT_TIME");
  // C = CHARTIME(T); (682)
  {
    descriptor_t *stringRHS;
    stringRHS
        = (putFIXED (mCHARTIMExT, getFIXED (mPRINT_TIMExT)), CHARTIME (0));
    putCHARACTER (mPRINT_TIMExC, stringRHS);
    stringRHS->inUse = 0;
  }
  // OUTPUT = MESSAGE || C; (683)
  {
    descriptor_t *stringRHS;
    stringRHS = xsCAT (getCHARACTER (mPRINT_TIMExMESSAGE),
                       getCHARACTER (mPRINT_TIMExC));
    OUTPUT (0, stringRHS);
    stringRHS->inUse = 0;
  }
  {
    reentryGuard = 0;
    return 0;
  }
}
