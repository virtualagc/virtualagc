#!/usr/bin/env python3

# WARNING: CAVEAT UTILITOR
#
#  This file was automatically generated by TatSu.
#
#     https://pypi.python.org/pypi/tatsu/
#
#  Any changes you make to it will be overwritten the next time
#  the file is generated.

# ruff: noqa: C405, COM812, I001, F401, PLR1702, PLC2801, SIM117

import sys
from pathlib import Path

from tatsu.buffering import Buffer
from tatsu.parsing import Parser
from tatsu.parsing import tatsumasu
from tatsu.parsing import leftrec, nomemo, isname
from tatsu.infos import ParserConfig
from tatsu.util import re, generic_main


KEYWORDS: set[str] = set()


class asmBuffer(Buffer):
    def __init__(self, text, /, config: ParserConfig | None = None, **settings):
        config = ParserConfig.new(
            config,
            owner=self,
            whitespace='',
            nameguard=None,
            ignorecase=False,
            namechars='',
            parseinfo=True,
            comments_re=None,
            eol_comments_re=re.compile('@g@a@r@b@a@g@e@'),
            keywords=KEYWORDS,
            start='operandPrototype0',
        )
        config = config.replace(**settings)

        super().__init__(text, config=config)


class asmParser(Parser):
    def __init__(self, /, config: ParserConfig | None = None, **settings):
        config = ParserConfig.new(
            config,
            owner=self,
            whitespace='',
            nameguard=None,
            ignorecase=False,
            namechars='',
            parseinfo=True,
            comments_re=None,
            eol_comments_re=re.compile('@g@a@r@b@a@g@e@'),
            keywords=KEYWORDS,
            start='operandPrototype0',
        )
        config = config.replace(**settings)

        super().__init__(config=config)

    @tatsumasu()
    def _operandPrototype0_(self):
        with self._choice():
            with self._option():
                with self._group():
                    self._parameter_()

                    def block0():
                        self._token(',')
                        self._parameter_()
                    self._closure(block0)
                    self._pattern(' ')
                self.add_last_node_to_name('end0')
            with self._option():
                with self._group():
                    self._parameter_()

                    def block1():
                        self._token(',')
                        self._parameter_()
                    self._closure(block1)
                    self._token(',')
                    self._pattern(' ')
                self.add_last_node_to_name('end1')
            with self._option():
                with self._group():
                    self._parameter_()

                    def block2():
                        self._token(',')
                        self._parameter_()
                    self._closure(block2)
                    with self._optional():
                        self._token(',')
                        with self._optional():
                            self._token('&')
                self.add_last_node_to_name('end2')
                self._check_eof()
                self._define(
                    [],
                    ['end2'],
                )
            with self._option():
                with self._group():
                    self._pattern(' *')
                self.add_last_node_to_name('end3')
                self._check_eof()
                self._define(
                    [],
                    ['end3'],
                )
            self._error(
                'expecting one of: '
                ' * <parameter> <sv>'
            )

    @tatsumasu()
    def _operandInvocation0_(self):
        with self._choice():
            with self._option():
                with self._group():
                    self._replacement_()

                    def block0():
                        self._token(',')
                        self._replacement_()
                    self._closure(block0)
                    self._pattern(' ')
                self.add_last_node_to_name('end0')
            with self._option():
                with self._group():
                    self._replacement_()

                    def block1():
                        self._token(',')
                        self._replacement_()
                    self._closure(block1)
                    with self._optional():
                        self._token(',')
                        with self._optional():
                            self._token("'")
                            self._pattern("[^']*")
                self.add_last_node_to_name('end2')
                self._check_eof()
                self._define(
                    [],
                    ['end2'],
                )
            with self._option():
                with self._group():
                    self._pattern(' *')
                self.add_last_node_to_name('end3')
                self._check_eof()
                self._define(
                    [],
                    ['end3'],
                )
            with self._option():
                with self._group():
                    self._pattern('[^ ]+')
                    self._pattern(' *')
                self.add_last_node_to_name('end4')
                self._check_eof()
                self._define(
                    [],
                    ['end4'],
                )
            self._error(
                'expecting one of: '
                '   * "\'" \'(\' \',\''
                '(?<![@#$A-Z0-9&])[@#$A-Z][@#$A-Z0-9]*'
                '<char> <identifier> <quotedString>'
                '<replacement> <substringExpression>'
                '[0-9]+ [^ ]+ [^, ()]*'
            )

    @tatsumasu()
    def _operandPrototype_(self):
        with self._choice():
            with self._option():
                self._parameter_()
                self.add_last_node_to_name('pi')

                def block0():
                    self._token(',')
                    self._parameter_()
                    self.add_last_node_to_name('pi')
                    self._define(
                        [],
                        ['pi'],
                    )
                self._closure(block0)
                self._pattern(' ')
                self._define(
                    [],
                    ['pi'],
                )
            with self._option():
                self._parameter_()
                self.add_last_node_to_name('pi')

                def block1():
                    self._token(',')
                    self._parameter_()
                    self.add_last_node_to_name('pi')
                    self._define(
                        [],
                        ['pi'],
                    )
                self._closure(block1)
                self._check_eof()
                self._define(
                    [],
                    ['pi'],
                )
            with self._option():
                self._check_eof()
            self._error(
                'expecting one of: '
                '<parameter> <sv>'
            )

    @tatsumasu()
    def _operandInvocation_(self):
        with self._optional():
            self._replacement_()
            self.add_last_node_to_name('pi')

            def block0():
                self._token(',')
                self._replacement_()
                self.add_last_node_to_name('pi')
                self._define(
                    [],
                    ['pi'],
                )
            self._closure(block0)
            self._define(
                [],
                ['pi'],
            )

    @tatsumasu()
    def _nameCode_(self):
        with self._choice():
            with self._option():
                self._variable_()
                self.add_last_node_to_name('sv')
            with self._option():
                self._identifier_()
                self.add_last_node_to_name('id')
            self._error(
                'expecting one of: '
                '(?<![@#$A-Z0-9&])[@#$A-Z][@#$A-Z0-9]*'
                '<identifier> <subvar> <variable>'
            )

    @tatsumasu()
    def _nameSet_(self):
        with self._choice():
            with self._option():
                self._sv_()
                self.add_last_node_to_name('sv')
                self._check_eof()
                self._define(
                    [],
                    ['sv'],
                )
            with self._option():
                self._sv_()
                self.add_last_node_to_name('sv')
                self._token('(')
                self._arithmeticExpression_()
                self.add_last_node_to_name('exp')
                self._token(')')
                self._check_eof()
                self._define(
                    [],
                    ['exp', 'sv'],
                )
            self._error(
                'expecting one of: '
                '&[@#$A-Z][@#$A-Z0-9]* <sv>'
            )

    @tatsumasu()
    def _nameSet0_(self):
        with self._choice():
            with self._option():
                self._sv_()
                self.add_last_node_to_name('sv')
                self._token('(')
                self._arithmeticExpression_()
                self.add_last_node_to_name('exp')
                self._token(')')
                self._define(
                    [],
                    ['exp', 'sv'],
                )
            with self._option():
                self._sv_()
                self.add_last_node_to_name('sv')
            self._error(
                'expecting one of: '
                '&[@#$A-Z][@#$A-Z0-9]* <sv>'
            )

    @tatsumasu()
    def _nameMacrodef_(self):
        with self._choice():
            with self._option():
                self._sequenceSymbol_()
                self._check_eof()
            with self._option():
                self._subName_()

                def block0():
                    self._token('.')
                    self._subName_()
                self._closure(block0)
                self._check_eof()
            self._error(
                'expecting one of: '
                '<identifier> <sequenceSymbol> <subName>'
                '<variable> [.][@#$A-Z][@#$A-Z0-9]*'
            )

    @tatsumasu()
    def _subName_(self):
        with self._choice():
            with self._option():
                self._variable_()
            with self._option():
                self._identifier_()
            self._error(
                'expecting one of: '
                '(?<![@#$A-Z0-9&])[@#$A-Z][@#$A-Z0-9]*'
                '<identifier> <subvar> <variable>'
            )

    @tatsumasu()
    def _operationAll_(self):
        self._subOperation_()

        def block0():
            self._token('.')
            self._subOperation_()
        self._closure(block0)
        self._check_eof()

    @tatsumasu()
    def _subOperation_(self):
        with self._choice():
            with self._option():
                self._variable_()
            with self._option():
                self._identifier_()
            self._error(
                'expecting one of: '
                '(?<![@#$A-Z0-9&])[@#$A-Z][@#$A-Z0-9]*'
                '<identifier> <subvar> <variable>'
            )

    @tatsumasu()
    def _rrAll_(self):
        with self._optional():
            self._arithmeticExpression_()
            self.add_last_node_to_name('R1')
            self._token(',')
            self._define(
                [],
                ['R1'],
            )
        self._arithmeticExpression_()
        self.add_last_node_to_name('R2')
        with self._group():
            with self._choice():
                with self._option():
                    self._pattern(' ')
                with self._option():
                    self._check_eof()
                self._error(
                    'expecting one of: '
                )
        self._define(
            [],
            ['R1', 'R2'],
        )

    @tatsumasu()
    def _lfxiAll_(self):
        with self._optional():
            self._register_()
            self.add_last_node_to_name('R1')
            self._token(',')
            self._define(
                [],
                ['R1'],
            )
        with self._group():
            with self._choice():
                with self._option():
                    self._token('-2')
                with self._option():
                    self._token('-1')
                with self._option():
                    self._register_()
                self._error(
                    'expecting one of: '
                    "'-1' '-2' <register>"
                )
        self.add_last_node_to_name('R2')
        with self._group():
            with self._choice():
                with self._option():
                    self._pattern(' ')
                with self._option():
                    self._check_eof()
                self._error(
                    'expecting one of: '
                )
        self._define(
            [],
            ['R1', 'R2'],
        )

    @tatsumasu()
    def _rsAll_(self):
        with self._choice():
            with self._option():
                with self._optional():
                    self._register_()
                    self.add_last_node_to_name('R1')
                    self._token(',')
                    self._define(
                        [],
                        ['R1'],
                    )
                self._token('=')
                self._lconstant_()
                self.add_last_node_to_name('L2')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
                self._define(
                    [],
                    ['L2', 'R1'],
                )
            with self._option():
                with self._optional():
                    self._register_()
                    self.add_last_node_to_name('R1')
                    self._token(',')
                    self._define(
                        [],
                        ['R1'],
                    )
                self._arithmeticExpression_()
                self.add_last_node_to_name('D2')
                self._token('(')
                self._register_()
                self.add_last_node_to_name('B2')
                self._token(')')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
                self._define(
                    [],
                    ['B2', 'D2', 'R1'],
                )
            with self._option():
                with self._optional():
                    self._register_()
                    self.add_last_node_to_name('R1')
                    self._token(',')
                    self._define(
                        [],
                        ['R1'],
                    )
                self._arithmeticExpression_()
                self.add_last_node_to_name('D2')
                self._token('(')
                self._register_()
                self.add_last_node_to_name('X2')
                self._token(',')
                self._register_()
                self.add_last_node_to_name('B2')
                self._token(')')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
                self._define(
                    [],
                    ['B2', 'D2', 'R1', 'X2'],
                )
            with self._option():
                with self._optional():
                    self._register_()
                    self.add_last_node_to_name('R1')
                    self._token(',')
                    self._define(
                        [],
                        ['R1'],
                    )
                self._arithmeticExpression_()
                self.add_last_node_to_name('D2')
                self._token('(')
                self._token(')')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
                self._define(
                    [],
                    ['D2', 'R1'],
                )
            with self._option():
                with self._optional():
                    self._register_()
                    self.add_last_node_to_name('R1')
                    self._token(',')
                    self._define(
                        [],
                        ['R1'],
                    )
                self._arithmeticExpression_()
                self.add_last_node_to_name('D2')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
                self._define(
                    [],
                    ['D2', 'R1'],
                )
            self._error(
                'expecting one of: '
                '"B\'" "L\'" "X\'" \'*\' \'=\''
                '(?<![@#$A-Z0-9&])[@#$A-Z][@#$A-Z0-9]*'
                '-?[0-9]+ <arithmeticExpression>'
                '<constant> <identifier> <register>'
                '<subvar> <term> <variable>'
            )

    @tatsumasu()
    def _riAll_(self):
        self._register_()
        self.add_last_node_to_name('R2')
        self._token(',')
        self._arithmeticExpression_()
        self.add_last_node_to_name('I1')
        with self._optional():
            self._token('(')
            self._token(')')
        with self._group():
            with self._choice():
                with self._option():
                    self._pattern(' ')
                with self._option():
                    self._check_eof()
                self._error(
                    'expecting one of: '
                )
        self._define(
            [],
            ['I1', 'R2'],
        )

    @tatsumasu()
    def _siAll_(self):
        with self._choice():
            with self._option():
                self._arithmeticExpression_()
                self.add_last_node_to_name('D2')
                self._token('(')
                self._register_()
                self.add_last_node_to_name('B2')
                self._token('),')
                self._arithmeticExpression_()
                self.add_last_node_to_name('I1')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
                self._define(
                    [],
                    ['B2', 'D2', 'I1'],
                )
            with self._option():
                self._arithmeticExpression_()
                self.add_last_node_to_name('D2')
                self._token(',')
                self._arithmeticExpression_()
                self.add_last_node_to_name('I1')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
                self._define(
                    [],
                    ['D2', 'I1'],
                )
            self._error(
                'expecting one of: '
                '<arithmeticExpression> <term>'
            )

    @tatsumasu()
    def _mscAll_(self):
        with self._choice():
            with self._option():
                self._constant_()
                self._token(',')
                self._identifier_()
                self._token('(')
                self._constant_()
                self._token(')')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
            with self._option():
                self._constant_()
                self._token(',')
                self._identifier_()
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
            with self._option():
                self._arithmeticExpression_()
                self._token('(')
                self._constant_()
                self._token(')')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
            with self._option():
                self._arithmeticExpression_()
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
            self._error(
                'expecting one of: '
                '"B\'" "L\'" "X\'" \'*\' -?[0-9]+'
                '<arithmeticExpression> <constant> <term>'
            )

    @tatsumasu()
    def _bceAll_(self):
        with self._choice():
            with self._option():
                self._arithmeticExpression_()
                self._token('(')
                self._constant_()
                self._token(')')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
            with self._option():
                self._arithmeticExpression_()
                self._token(',')
                self._arithmeticExpression_()
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
            with self._option():
                self._arithmeticExpression_()
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern(' ')
                        with self._option():
                            self._check_eof()
                        self._error(
                            'expecting one of: '
                        )
            self._error(
                'expecting one of: '
                '<arithmeticExpression> <term>'
            )

    @tatsumasu()
    def _aifAll_(self):
        self._token('(')
        self._booleanExpression_()
        self._token(')')
        self._sequenceSymbol_()

    @tatsumasu()
    def _quotedString_(self):
        self._token("'")
        self._pattern("[^']*")

        def block0():
            self._token("''")
            self._pattern("[^']*")
        self._closure(block0)
        self._token("'")

    @tatsumasu()
    def _identifier_(self):
        self._pattern('(?<![@#$A-Z0-9&])[@#$A-Z][@#$A-Z0-9]*')

    @tatsumasu()
    def _variable_(self):
        with self._choice():
            with self._option():
                self._subvar_()
                self._variable_()
            with self._option():
                self._subvar_()
                self._token('.')
                self._variable_()
            with self._option():
                self._subvar_()
                self._token('(')
                self._arithmeticExpression_()
                self._token(')')
            with self._option():
                self._subvar_()
            self._error(
                'expecting one of: '
                '<subvar> <sv>'
            )

    @tatsumasu()
    def _subvar_(self):
        self._sv_()

    @tatsumasu()
    def _sequenceSymbol_(self):
        self._pattern('[.][@#$A-Z][@#$A-Z0-9]*')

    @tatsumasu()
    def _constant_(self):
        with self._choice():
            with self._option():
                self._pattern('-?[0-9]+')
            with self._option():
                self._token("X'")
                self._pattern('[0-9A-F]+')
                self._token("'")
            with self._option():
                self._token("B'")
                self._pattern('[0-1]+')
                self._token("'")
            with self._option():
                self._token("L'")
                self._identifier_()
            with self._option():
                self._token('*')
            self._error(
                'expecting one of: '
                '"B\'" "L\'" "X\'" \'*\' -?[0-9]+'
            )

    @tatsumasu()
    def _lconstant_(self):
        with self._choice():
            with self._option():
                self._pattern('C')
                self.add_last_node_to_name('T')
                with self._optional():
                    self._token('L')
                    self._pattern('[0-9]+')
                    self.add_last_node_to_name('L')
                    self._define(
                        [],
                        ['L'],
                    )
                self._token("'")
                with self._group():
                    self._pattern("[^']*")

                    def block0():
                        self._token("''")
                        self._pattern("[^']*")
                    self._closure(block0)
                self.add_last_node_to_name('C')
                self._token("'")
                self._define(
                    [],
                    ['C', 'L', 'T'],
                )
            with self._option():
                self._pattern('X')
                self.add_last_node_to_name('T')
                with self._optional():
                    self._token('L')
                    self._pattern('[0-9]+')
                    self.add_last_node_to_name('L')
                    self._define(
                        [],
                        ['L'],
                    )
                self._token("'")
                self._pattern('[0-9A-F]+')
                self.add_last_node_to_name('X')
                self._token("'")
                self._define(
                    [],
                    ['L', 'T', 'X'],
                )
            with self._option():
                self._pattern('B')
                self.add_last_node_to_name('T')
                with self._optional():
                    self._token('L')
                    self._pattern('[0-9]+')
                    self.add_last_node_to_name('L')
                    self._define(
                        [],
                        ['L'],
                    )
                self._token("'")
                self._pattern('[0-1]+')
                self.add_last_node_to_name('B')
                self._token("'")
                self._define(
                    [],
                    ['B', 'L', 'T'],
                )
            with self._option():
                self._pattern('F')
                self.add_last_node_to_name('T')
                with self._optional():
                    self._token('L')
                    self._pattern('[0-9]+')
                    self.add_last_node_to_name('L')
                    self._define(
                        [],
                        ['L'],
                    )
                with self._optional():
                    self._token('S')
                    self._pattern('-?[0-9]+')
                    self.add_last_node_to_name('S')
                    self._define(
                        [],
                        ['S'],
                    )
                self._token("'")
                self._floatNumber_()
                self.add_last_node_to_name('F')
                self._token("'")
                self._define(
                    [],
                    ['F', 'L', 'S', 'T'],
                )
            with self._option():
                self._pattern('H')
                self.add_last_node_to_name('T')
                with self._optional():
                    self._token('L')
                    self._pattern('[0-9]+')
                    self.add_last_node_to_name('L')
                    self._define(
                        [],
                        ['L'],
                    )
                with self._optional():
                    self._token('S')
                    self._pattern('-?[0-9]+')
                    self.add_last_node_to_name('S')
                    self._define(
                        [],
                        ['S'],
                    )
                self._token("'")
                self._floatNumber_()
                self.add_last_node_to_name('H')
                self._token("'")
                self._define(
                    [],
                    ['H', 'L', 'S', 'T'],
                )
            with self._option():
                self._pattern('E')
                self.add_last_node_to_name('T')
                with self._optional():
                    self._token('L')
                    self._pattern('[0-9]+')
                    self.add_last_node_to_name('L')
                    self._define(
                        [],
                        ['L'],
                    )
                with self._optional():
                    self._token('S')
                    self._pattern('-?[0-9]+')
                    self.add_last_node_to_name('S')
                    self._define(
                        [],
                        ['S'],
                    )
                self._token("'")
                self._floatNumber_()
                self.add_last_node_to_name('E')
                self._token("'")
                self._define(
                    [],
                    ['E', 'L', 'S', 'T'],
                )
            with self._option():
                self._pattern('D')
                self.add_last_node_to_name('T')
                with self._optional():
                    self._token('L')
                    self._pattern('[0-9]+')
                    self.add_last_node_to_name('L')
                    self._define(
                        [],
                        ['L'],
                    )
                with self._optional():
                    self._token('S')
                    self._pattern('-?[0-9]+')
                    self.add_last_node_to_name('S')
                    self._define(
                        [],
                        ['S'],
                    )
                self._token("'")
                self._floatNumber_()
                self.add_last_node_to_name('D')
                self._token("'")
                self._define(
                    [],
                    ['D', 'L', 'S', 'T'],
                )
            with self._option():
                self._pattern('Y')
                self.add_last_node_to_name('T')
                with self._optional():
                    self._token('L')
                    self._pattern('[0-9]+')
                    self.add_last_node_to_name('L')
                    self._define(
                        [],
                        ['L'],
                    )
                self._token('(')
                self._identifier_()
                self.add_last_node_to_name('T')
                self._token(')')
                self._define(
                    [],
                    ['L', 'T'],
                )
            with self._option():
                self._pattern('Z')
                self.add_last_node_to_name('T')
                with self._optional():
                    self._token('L')
                    self._pattern('[0-9]+')
                    self.add_last_node_to_name('L')
                    self._define(
                        [],
                        ['L'],
                    )
                self._token('(')
                self._token(',')
                self._arithmeticExpression_()
                self.add_last_node_to_name('A1')
                self._token(',')
                self._arithmeticExpression_()
                self.add_last_node_to_name('A2')
                self._token(')')
                self._define(
                    [],
                    ['A1', 'A2', 'L', 'T'],
                )
            self._error(
                'expecting one of: '
                'B C D E F H X Y Z'
            )

    @tatsumasu()
    def _char_(self):
        with self._choice():
            with self._option():
                self._substringExpression_()
            with self._option():
                self._quotedString_()
            self._error(
                'expecting one of: '
                '"\'" <quotedString> <substringExpression>'
            )

    @tatsumasu()
    def _substringExpression_(self):
        self._quotedString_()
        self._token('(')
        self._arithmeticExpression_()
        self._token(',')
        self._arithmeticExpression_()
        self._token(')')

    @tatsumasu()
    def _sdTerm_(self):
        with self._choice():
            with self._option():
                self._pattern('[0-9]+')
            with self._option():
                self._token("X'")
                self._pattern('[0-9A-F]+')
                self._token("'")
            with self._option():
                self._token("B'")
                self._pattern('[0-1]+')
                self._token("'")
            with self._option():
                self._token('C')
                self._char_()
            self._error(
                'expecting one of: '
                '"B\'" "X\'" \'C\' [0-9]+'
            )

    @tatsumasu()
    def _dcOperands_(self):
        self._dcOperand_()

        def block0():
            self._token(',')
            self._dcOperand_()
        self._closure(block0)
        with self._group():
            with self._choice():
                with self._option():
                    self._pattern(' ')
                with self._option():
                    self._check_eof()
                self._error(
                    'expecting one of: '
                )

    @tatsumasu()
    def _dcOperand_(self):
        with self._choice():
            with self._option():
                with self._optional():
                    self._number_()
                    self.add_last_node_to_name('d')
                self._token('C')
                self.add_last_node_to_name('t')
                with self._optional():
                    self._len_()
                    self.add_last_node_to_name('l')
                self._quotedString_()
                self.add_last_node_to_name('v')
                self._define(
                    [],
                    ['d', 'l', 't', 'v'],
                )
            with self._option():
                with self._optional():
                    self._number_()
                    self.add_last_node_to_name('d')
                self._token('X')
                self.add_last_node_to_name('t')
                with self._optional():
                    self._len_()
                    self.add_last_node_to_name('l')
                self._quotedHexString_()
                self.add_last_node_to_name('v')
                self._define(
                    [],
                    ['d', 'l', 't', 'v'],
                )
            with self._option():
                with self._optional():
                    self._number_()
                    self.add_last_node_to_name('d')
                self._token('B')
                self.add_last_node_to_name('t')
                with self._optional():
                    self._len_()
                    self.add_last_node_to_name('l')
                self._quotedBinaryString_()
                self.add_last_node_to_name('v')
                self._define(
                    [],
                    ['d', 'l', 't', 'v'],
                )
            with self._option():
                with self._optional():
                    self._number_()
                    self.add_last_node_to_name('d')
                self._pattern('[FHED]')
                self.add_last_node_to_name('t')
                with self._optional():
                    self._len_()
                    self.add_last_node_to_name('l')
                self._quotedFloatList_()
                self.add_last_node_to_name('v')
                self._define(
                    [],
                    ['d', 'l', 't', 'v'],
                )
            with self._option():
                with self._optional():
                    self._number_()
                    self.add_last_node_to_name('d')
                self._pattern('[AY]')
                self.add_last_node_to_name('t')
                with self._optional():
                    self._len_()
                    self.add_last_node_to_name('l')
                self._addresses_()
                self.add_last_node_to_name('v')
                self._define(
                    [],
                    ['d', 'l', 't', 'v'],
                )
            with self._option():
                with self._optional():
                    self._number_()
                    self.add_last_node_to_name('d')
                self._token('A')
                self.add_last_node_to_name('t')
                with self._optional():
                    self._len_()
                    self.add_last_node_to_name('l')
                self._quotedHexString_()
                self.add_last_node_to_name('h')
                self._define(
                    [],
                    ['d', 'h', 'l', 't'],
                )
            with self._option():
                with self._optional():
                    self._number_()
                    self.add_last_node_to_name('d')
                self._token('Z')
                self.add_last_node_to_name('t')
                self._token('(')
                self._identifier_()
                self.name_last_node('z')
                with self._optional():
                    self._token(',')
                    with self._optional():
                        self._arithmeticExpression_()
                    with self._optional():
                        self._token(',')
                        self._arithmeticExpression_()
                        self.name_last_node('f')
                        self._define(['f'], [])
                    self._define(['f'], [])
                self._token(')')
                self._define(
                    ['f', 'z'],
                    ['d', 't'],
                )
            self._error(
                'expecting one of: '
                "'(' 'A' 'B' 'C' 'X' 'Z' <number> [0-9]+"
                '[AY] [FHED]'
            )

    @tatsumasu()
    def _dsOperands_(self):
        self._dsOperand_()

        def block0():
            self._token(',')
            self._dsOperand_()
        self._closure(block0)
        with self._group():
            with self._choice():
                with self._option():
                    self._pattern(' ')
                with self._option():
                    self._check_eof()
                self._error(
                    'expecting one of: '
                )

    @tatsumasu()
    def _dsOperand_(self):
        with self._choice():
            with self._option():
                with self._optional():
                    self._number_()
                    self.add_last_node_to_name('d')
                self._token('C')
                self.add_last_node_to_name('t')
                with self._optional():
                    self._len_()
                    self.add_last_node_to_name('l')
                with self._optional():
                    self._quotedString_()
                    self.add_last_node_to_name('v')
                self._define(
                    [],
                    ['d', 'l', 't', 'v'],
                )
            with self._option():
                with self._optional():
                    self._number_()
                    self.add_last_node_to_name('d')
                self._token('X')
                self.add_last_node_to_name('t')
                with self._optional():
                    self._len_()
                    self.add_last_node_to_name('l')
                with self._optional():
                    self._quotedHexString_()
                    self.add_last_node_to_name('v')
                self._define(
                    [],
                    ['d', 'l', 't', 'v'],
                )
            with self._option():
                with self._optional():
                    self._number_()
                    self.add_last_node_to_name('d')
                self._token('B')
                self.add_last_node_to_name('t')
                with self._optional():
                    self._len_()
                    self.add_last_node_to_name('l')
                with self._optional():
                    self._quotedBinaryString_()
                    self.add_last_node_to_name('v')
                self._define(
                    [],
                    ['d', 'l', 't', 'v'],
                )
            with self._option():
                with self._optional():
                    self._number_()
                    self.add_last_node_to_name('d')
                self._pattern('[FHED]')
                self.add_last_node_to_name('t')
                with self._optional():
                    self._len_()
                    self.add_last_node_to_name('l')
                with self._optional():
                    self._quotedFloatList_()
                    self.add_last_node_to_name('v')
                self._define(
                    [],
                    ['d', 'l', 't', 'v'],
                )
            with self._option():
                with self._optional():
                    self._number_()
                    self.add_last_node_to_name('d')
                self._pattern('[AY]')
                self.add_last_node_to_name('t')
                with self._optional():
                    self._len_()
                    self.add_last_node_to_name('l')
                with self._optional():
                    self._addresses_()
                    self.add_last_node_to_name('v')
                self._define(
                    [],
                    ['d', 'l', 't', 'v'],
                )
            self._error(
                'expecting one of: '
                "'(' 'B' 'C' 'X' <number> [0-9]+ [AY]"
                '[FHED]'
            )

    @tatsumasu()
    def _addresses_(self):
        self._token('(')
        self._arithmeticExpression_()

        def block0():
            self._token(',')
            self._arithmeticExpression_()
        self._closure(block0)
        self._token(')')

    @tatsumasu()
    def _len_(self):
        self._token('L')
        with self._optional():
            self._token('.')
        with self._optional():
            with self._choice():
                with self._option():
                    self._token('+')
                with self._option():
                    self._token('-')
                self._error(
                    'expecting one of: '
                    "'+' '-'"
                )
        self._number_()

    @tatsumasu()
    def _scale_(self):
        self._token('S')
        with self._optional():
            with self._choice():
                with self._option():
                    self._token('+')
                with self._option():
                    self._token('-')
                self._error(
                    'expecting one of: '
                    "'+' '-'"
                )
        self._number_()

    @tatsumasu()
    def _exp_(self):
        self._token('E')
        with self._optional():
            with self._choice():
                with self._option():
                    self._token('+')
                with self._option():
                    self._token('-')
                self._error(
                    'expecting one of: '
                    "'+' '-'"
                )
        self._number_()

    @tatsumasu()
    def _number_(self):
        with self._group():
            with self._choice():
                with self._option():
                    self._pattern('[0-9]+')
                with self._option():
                    self._token('(')
                    self._arithmeticExpression_()
                    self._token(')')
                self._error(
                    'expecting one of: '
                    "'(' [0-9]+"
                )

    @tatsumasu()
    def _quotedHexString_(self):
        self._token("'")
        self._pattern('[A-F0-9]+')
        self._token("'")

    @tatsumasu()
    def _quotedBinaryString_(self):
        self._token("'")
        self._pattern('[01]+')
        self._token("'")

    @tatsumasu()
    def _quotedFloatList_(self):
        self._token("'")
        self._floatNumber_()

        def block0():
            self._token(',')
            self._floatNumber_()
        self._closure(block0)
        self._token("'")

    @tatsumasu()
    def _floatNumber_(self):
        with self._choice():
            with self._option():
                with self._optional():
                    self._pattern('[-+]')
                self._pattern('[0-9]+')
                with self._optional():
                    self._token('.')
                    self._pattern('[0-9]*')
                with self._optional():
                    self._token('E')
                    self._pattern('[-+]?[0-9]+')
            with self._option():
                with self._optional():
                    self._pattern('[-+]')
                self._token('.')
                self._pattern('[0-9]+')
                with self._optional():
                    self._token('E')
                    self._pattern('[-+]?[0-9]+')
            self._error(
                'expecting one of: '
                "'.' [-+] [0-9]+"
            )

    @tatsumasu()
    def _quotedFixedList_(self):
        self._token("'")
        self._fixedNumber_()

        def block0():
            self._token(',')
            self._fixedNumber_()
        self._closure(block0)
        self._token("'")

    @tatsumasu()
    def _fixedNumber_(self):
        with self._choice():
            with self._option():
                with self._optional():
                    self._pattern('[-+]')
                self._pattern('[0-9]+')
                with self._optional():
                    self._token('.')
                    self._pattern('[0-9]*')
            with self._option():
                with self._optional():
                    self._pattern('[-+]')
                self._token('.')
                self._pattern('[0-9]+')
            self._error(
                'expecting one of: '
                "'.' [-+] [0-9]+"
            )

    @tatsumasu()
    def _register_(self):
        with self._choice():
            with self._option():
                self._constant_()
            with self._option():
                self._identifier_()
            with self._option():
                self._variable_()
            self._error(
                'expecting one of: '
                '"B\'" "L\'" "X\'" \'*\''
                '(?<![@#$A-Z0-9&])[@#$A-Z][@#$A-Z0-9]*'
                '-?[0-9]+ <constant> <identifier>'
                '<subvar> <sv> <variable>'
            )

    @tatsumasu()
    def _immediate_(self):
        with self._choice():
            with self._option():
                self._constant_()
            with self._option():
                self._identifier_()
            with self._option():
                self._variable_()
            self._error(
                'expecting one of: '
                '"B\'" "L\'" "X\'" \'*\''
                '(?<![@#$A-Z0-9&])[@#$A-Z][@#$A-Z0-9]*'
                '-?[0-9]+ <constant> <identifier>'
                '<subvar> <sv> <variable>'
            )

    @tatsumasu()
    def _arithmeticExpressionOnly_(self):
        self._arithmeticExpression_()
        self._check_eof()

    @tatsumasu()
    def _arithmeticExpression_(self):
        self._term_()

        def block0():
            with self._group():
                with self._choice():
                    with self._option():
                        self._token('+')
                    with self._option():
                        self._token('-')
                    self._error(
                        'expecting one of: '
                        "'+' '-'"
                    )
            self._term_()
        self._closure(block0)

    @tatsumasu()
    def _term_(self):
        self._factor_()

        def block0():
            with self._group():
                with self._choice():
                    with self._option():
                        self._token('*')
                    with self._option():
                        self._token('/')
                    self._error(
                        'expecting one of: '
                        "'*' '/'"
                    )
            self._factor_()
        self._closure(block0)

    @tatsumasu()
    def _factor_(self):
        with self._choice():
            with self._option():
                self._pattern("[NKLSI]'")
                self._variable_()
            with self._option():
                self._constant_()
            with self._option():
                self._identifier_()
            with self._option():
                self._variable_()
            with self._option():
                self._token('(')
                self._arithmeticExpression_()
                self._token(')')
            with self._option():
                self._token('*')
            self._error(
                'expecting one of: '
                '"B\'" "L\'" "X\'" \'(\' \'*\''
                '(?<![@#$A-Z0-9&])[@#$A-Z][@#$A-Z0-9]*'
                '-?[0-9]+ <constant> <identifier>'
                "<subvar> <sv> <variable> [NKLSI]'"
            )

    @tatsumasu()
    def _booleanExpressionOnly_(self):
        self._booleanExpression_()
        self._check_eof()

    @tatsumasu()
    def _booleanExpression_(self):
        self._booleanTerm_()

        def block0():
            self._pattern(' *')
            self._token('OR')
            self._pattern(' *')
            self._booleanTerm_()
        self._closure(block0)

    @tatsumasu()
    def _booleanTerm_(self):
        self._notFactor_()

        def block0():
            self._pattern(' *')
            self._token('AND')
            self._pattern(' *')
            self._notFactor_()
        self._closure(block0)

    @tatsumasu()
    def _notFactor_(self):
        with self._optional():
            self._pattern(' *')
            self._token('NOT')
            self._pattern(' *')
        self._booleanFactor_()

    @tatsumasu()
    def _booleanFactor_(self):
        with self._choice():
            with self._option():
                self._token("D'")
                self._identifier_()
            with self._option():
                self._token("D'")
                self._sv_()
            with self._option():
                self._relationalExpression_()
            with self._option():
                self._variable_()
            with self._option():
                self._token('(')
                self._booleanExpression_()
                self._token(')')
            with self._option():
                self._booleanLiteral_()
            self._error(
                'expecting one of: '
                '"D\'" \'(\' \'0\' \'1\' <arithmeticExpression>'
                '<booleanLiteral> <characterExpression>'
                '<relationalExpression> <subvar> <sv>'
                '<term> <variable>'
            )

    @tatsumasu()
    def _booleanLiteral_(self):
        with self._choice():
            with self._option():
                self._token('0')
            with self._option():
                self._token('1')
            self._error(
                'expecting one of: '
                "'0' '1'"
            )

    @tatsumasu()
    def _relationalExpression_(self):
        with self._choice():
            with self._option():
                self._arithmeticExpression_()
                self._pattern(' *')
                self._relOp_()
                self._pattern(' *')
                self._arithmeticExpression_()
            with self._option():
                self._characterExpression_()
                self._pattern(' *')
                self._relOp_()
                self._pattern(' *')
                self._characterExpression_()
            self._error(
                'expecting one of: '
                '"\'" "T\'" <arithmeticExpression>'
                '<characterExpression> <factor>'
                '<quotedString> <term>'
            )

    @tatsumasu()
    def _relOp_(self):
        with self._choice():
            with self._option():
                self._token('EQ')
            with self._option():
                self._token('NE')
            with self._option():
                self._token('LT')
            with self._option():
                self._token('GT')
            with self._option():
                self._token('LE')
            with self._option():
                self._token('GE')
            self._error(
                'expecting one of: '
                "'EQ' 'GE' 'GT' 'LE' 'LT' 'NE'"
            )

    @tatsumasu()
    def _characterExpressionOnly_(self):
        self._characterExpression_()
        self._check_eof()

    @tatsumasu()
    def _characterExpression_(self):
        with self._choice():
            with self._option():
                self._quotedString_()
                with self._optional():
                    self._substringNotation_()

                def block0():
                    with self._optional():
                        self._token('.')
                    self._characterExpression_()
                self._closure(block0)
            with self._option():
                self._token("T'")
                self._identifier_()
            with self._option():
                self._token("T'")
                self._sv_()
            self._error(
                'expecting one of: '
                '"\'" "T\'" <quotedString>'
            )

    @tatsumasu()
    def _substringNotation_(self):
        self._token('(')
        self._arithmeticExpression_()
        self._token(',')
        self._arithmeticExpression_()
        self._token(')')

    @tatsumasu()
    def _parameter_(self):
        with self._choice():
            with self._option():
                self._sv_()
                self._token('=')
                self._pattern('[^, ]*')
            with self._option():
                self._sv_()
            self._error(
                'expecting one of: '
                '&[@#$A-Z][@#$A-Z0-9]* <sv>'
            )

    @tatsumasu()
    def _sv_(self):
        self._pattern('&[@#$A-Z][@#$A-Z0-9]*')

    @tatsumasu()
    def _list_(self):
        self._listItem_()

        def block0():
            self._token(',')
            self._listItem_()
        self._closure(block0)

    @tatsumasu()
    def _listItem_(self):
        with self._choice():
            with self._option():
                self._token('(')
                self._list_()
                self._token(')')
            with self._option():
                self._pattern('[^ ,()]*')
            self._error(
                'expecting one of: '
                "'(' [^ ,()]*"
            )

    @tatsumasu()
    def _replacement_(self):
        with self._choice():
            with self._option():
                self._identifier_()
                self._token('=')
                self._token("'")
                self._pattern("[^']*")

                def block0():
                    self._token("''")
                    self._pattern('[^;]*')
                self._closure(block0)
                self._token("'")
            with self._option():
                self._identifier_()
                self._token('=')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern('[0-9]+')
                        with self._option():
                            self._identifier_()
                        self._error(
                            'expecting one of: '
                            '<identifier> [0-9]+'
                        )
                self._token('(')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern('[0-9]+')
                        with self._option():
                            self._identifier_()
                        self._error(
                            'expecting one of: '
                            '<identifier> [0-9]+'
                        )
                self._token(')')
            with self._option():
                self._identifier_()
                self._token('=')
                self._token('(')
                self._list_()
                self._token(')')
            with self._option():
                self._identifier_()
                self._token('=')
                self._pattern('[^, ()]*')
            with self._option():
                self._char_()
            with self._option():
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern('[0-9]+')
                        with self._option():
                            self._identifier_()
                        self._error(
                            'expecting one of: '
                            '<identifier> [0-9]+'
                        )
                self._token('(')
                with self._group():
                    with self._choice():
                        with self._option():
                            self._pattern('[0-9]+')
                        with self._option():
                            self._identifier_()
                        self._error(
                            'expecting one of: '
                            '<identifier> [0-9]+'
                        )
                self._token(')')
            with self._option():
                self._token('(')
                self._list_()
                self._token(')')
            with self._option():
                self._pattern('[^, ()]*')
            self._error(
                'expecting one of: '
                '"\'" \'(\''
                '(?<![@#$A-Z0-9&])[@#$A-Z][@#$A-Z0-9]*'
                '<char> <identifier> <quotedString>'
                '<substringExpression> [0-9]+ [^, ()]*'
            )

    @tatsumasu()
    def _mnote_(self):
        with self._choice():
            with self._option():
                self._pattern('[0-9]+')
                self.add_last_node_to_name('sev')
                self._token(',')
                self._quotedString_()
                self.add_last_node_to_name('msg')
                self._define(
                    [],
                    ['msg', 'sev'],
                )
            with self._option():
                self._token('*')
                self.add_last_node_to_name('com')
                self._token(',')
                self._quotedString_()
                self.add_last_node_to_name('msg')
                self._define(
                    [],
                    ['com', 'msg'],
                )
            with self._option():
                self._quotedString_()
                self.add_last_node_to_name('msg')
            self._error(
                'expecting one of: '
                '"\'" \'*\' <quotedString> [0-9]+'
            )

    @tatsumasu()
    def _identifierList_(self):
        self._pidentifier_()

        def block0():
            self._token(',')
            self._pidentifier_()
        self._closure(block0)
        self._check_eof()

    @tatsumasu()
    def _pidentifier_(self):
        self._pattern('[#@$A-Z][#@$A-Z0-9]*')

    @tatsumasu()
    def _anything_(self):
        self._pattern('.*')
        self._check_eof()

    @tatsumasu()
    def _equOperand_(self):
        self._arithmeticExpression_()
        self.add_last_node_to_name('v')
        with self._group():
            with self._choice():
                with self._option():
                    self._pattern(' ')
                with self._option():
                    self._check_eof()
                self._error(
                    'expecting one of: '
                )
        self._define(
            [],
            ['v'],
        )

    @tatsumasu()
    def _expressions_(self):
        self._arithmeticExpression_()
        self.add_last_node_to_name('r')

        def block0():
            self._token(',')
            self._arithmeticExpression_()
            self.add_last_node_to_name('r')
            self._define(
                [],
                ['r'],
            )
        self._closure(block0)
        with self._group():
            with self._choice():
                with self._option():
                    self._pattern(' ')
                with self._option():
                    self._check_eof()
                self._error(
                    'expecting one of: '
                )
        self._define(
            [],
            ['r'],
        )


def main(filename, **kwargs):
    if not filename or filename == '-':
        text = sys.stdin.read()
    else:
        text = Path(filename).read_text()
    parser = asmParser()
    return parser.parse(
        text,
        filename=filename,
        **kwargs,
    )


if __name__ == '__main__':
    import json
    from tatsu.util import asjson

    ast = generic_main(main, asmParser, name='asm')
    data = asjson(ast)
    print(json.dumps(data, indent=2))
