 TEST_SCHED_WHILE:
 PROGRAM;
    DECLARE EV1 EVENT;
    DECLARE INTEGER, N INITIAL(0);
    WORKER:
    TASK;
       N = N + 1;
    CLOSE WORKER;

    SCHEDULE WORKER PRIORITY(60), REPEAT WHILE EV1;
    WAIT(50);
    WRITE(6) 'N=', N;
 CLOSE TEST_SCHED_WHILE;
