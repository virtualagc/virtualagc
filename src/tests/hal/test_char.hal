 TEST_CHAR:
 PROGRAM;
    DECLARE CHARACTER(10), C1 INITIAL('HELLO');
    DECLARE CHARACTER(10), C2 INITIAL('HELLO');
    DECLARE CHARACTER(20), C3;
    C3 = C1 || C2;
    WRITE(6) C3;
    IF C1 = C2 THEN
       WRITE(6) 'EQUAL';
    IF C1 < 'WORLD' THEN
       WRITE(6) 'LESS';
 CLOSE TEST_CHAR;
