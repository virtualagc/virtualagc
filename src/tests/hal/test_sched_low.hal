 TEST_SCHED_LOW:
 PROGRAM;
    WORKER:
    TASK;
       WRITE(6) 'WORKER RUNNING';
    CLOSE WORKER;

    WRITE(6) 'BEFORE SCHEDULE';
    SCHEDULE WORKER PRIORITY(30) DEPENDENT;
    WRITE(6) 'AFTER SCHEDULE';
 CLOSE TEST_SCHED_LOW;
