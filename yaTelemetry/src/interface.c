/*
 * DO NOT EDIT THIS FILE - it is generated by Glade.
 */

#ifdef HAVE_CONFIG_H
#  include <config.h>
#endif

#include <sys/types.h>
#include <sys/stat.h>
#include <unistd.h>
#include <string.h>
#include <stdio.h>

#include <gdk/gdkkeysyms.h>
#include <gtk/gtk.h>

#include "callbacks.h"
#include "interface.h"
#include "support.h"

#define GLADE_HOOKUP_OBJECT(component,widget,name) \
  g_object_set_data_full (G_OBJECT (component), name, \
    gtk_widget_ref (widget), (GDestroyNotify) gtk_widget_unref)

#define GLADE_HOOKUP_OBJECT_NO_REF(component,widget,name) \
  g_object_set_data (G_OBJECT (component), name, widget)

GtkWidget*
create_MainWindow (void)
{
  GtkWidget *MainWindow;
  GtkWidget *MonitorScroll;
  GtkWidget *MonitorTreeview;

  MainWindow = gtk_window_new (GTK_WINDOW_TOPLEVEL);
  gtk_window_set_title (GTK_WINDOW (MainWindow), "yaTelemetry by Ron Burkey");

  MonitorScroll = gtk_scrolled_window_new (NULL, NULL);
  gtk_widget_show (MonitorScroll);
  gtk_container_add (GTK_CONTAINER (MainWindow), MonitorScroll);
  GTK_WIDGET_UNSET_FLAGS (MonitorScroll, GTK_CAN_FOCUS);
  gtk_scrolled_window_set_policy (GTK_SCROLLED_WINDOW (MonitorScroll), GTK_POLICY_AUTOMATIC, GTK_POLICY_AUTOMATIC);

  MonitorTreeview = gtk_tree_view_new ();
  gtk_widget_show (MonitorTreeview);
  gtk_container_add (GTK_CONTAINER (MonitorScroll), MonitorTreeview);
  GTK_WIDGET_UNSET_FLAGS (MonitorTreeview, GTK_CAN_FOCUS);
  gtk_tree_view_set_headers_visible (GTK_TREE_VIEW (MonitorTreeview), FALSE);
  gtk_tree_view_set_enable_search (GTK_TREE_VIEW (MonitorTreeview), FALSE);

  /* Store pointers to all widgets, for use by lookup_widget(). */
  GLADE_HOOKUP_OBJECT_NO_REF (MainWindow, MainWindow, "MainWindow");
  GLADE_HOOKUP_OBJECT (MainWindow, MonitorScroll, "MonitorScroll");
  GLADE_HOOKUP_OBJECT (MainWindow, MonitorTreeview, "MonitorTreeview");

  return MainWindow;
}

