
### [2026-08-05] Target: [HANDOFF.md]
- Of the 12 OI301700 failures, the 4 CDAP DI11s are CASCADES, not roots: CDAP04 fails on "NAME(GO1_ASCENT_OPS), NAME(GO6_RTLS_OPS)" and both providers are themselves in the failing set.  Fix the roots first and re-test before touching them.
- FT101 group FIXED (DM5NEW, DM6OPS, GO6RTL), PFS be35c9e4.  All three pass the INTEGER literal 0 to a BIT(16) formal -- ARX_RS_BUS_CHG's NEW_SET_MASK, GUS_CS_COM_PROC's MSK_INT_ITI -- which is DR121254 again, in the opposite direction from PMQTEC where a BIT literal reached an INTEGER formal.  OI-34.06's DM6OPS carries the same call at the same SRN 033700 and passes HEX'0000'.  Applied as W/V pairs; the corpus CARDTYPE FCRMUDXCVMWC already maps V->M and W->C, so no parm change was needed.
- M3 "COMMENT LONGER THAN 256 CHARACTERS" is a COMPILER bug, not an extraction error, and the four M3 files (GO1ASC, GO2ORB, GO3ENT, VG9OPS9) are all OPS modules using the OPSINIT/SIO macros.  GO1ASC's comment is 218 characters and appears ONCE in the source; the compiler's own listing shows it concatenated with itself -- "... BUFFER NAME  EVENT  I/O SVC NUMBER=24  UNUSED  SYNC TYPE  C**/" -- and 218 + 38 = 256 exactly, the truncation point.  It follows a macro invocation, which our listing marks with ^^^^.  The original build listing for GO1ASC contains NO error messages at all, so the original compiler did not double it.  Look at comment accumulation during macro expansion; note PRINT_COMMENT already had a related port bug (D_TOKEN passed one argument where two were needed).
- VAASEQUE's P8 is NOT an extraction error and remains unexplained.  The extracted "CVAA_READY$(N+2:)= OFF;" is confirmed correct twice over: the original listing carries an S card reading "N+2:" in the blank gap of the M card, and OI-34.06's VAASEQUE writes the identical "CVAA_READY$(N+2:)=OFF;".  Neither version declares N locally.  OI-34.06 compiles it; OI-30.17 rejects the "+" with P8.

### [2026-08-05] Target: [compileLinkCompare.md]
- SCOPE.  The denominator is not the 3859 distinct CSECTs in mafgen/csects-*.json.  CSECTs fall in three groups, and only the first is in scope: HAL/S-derived from PASS source, assembly-derived (we cannot yet assemble), and HAL/S RUNTIME LIBRARY routines linked in rather than compiled -- #0ETOH, #0ITOD, #0ROUND, #LACOS, #LDATAN2 and the like, 443 distinct.  The document's own GSRRSL example includes one, "#0ITOD @ 00588", marked OK.  Nothing we do to our compilation changes a library routine, so say whether they count.  Per configuration, total / HAL/S / assembly / runtime:
      SSW  660/387/154/119   P9   686/418/143/125   G8  1227/849/140/238
      S2  1227/865/144/218   G9  1272/896/158/218   G2  1417/1010/140/267
      G3  1629/1228/140/261  G16 1819/1406/141/272
  Starting at SSW is therefore 387 CSECTs, not 660, and the whole job is 3212 distinct, not 3859.  (Match a CSECT to its source by stripping the 2-character prefix -- #C, #D, #Z, #0, A1..A9 -- and comparing the remainder against the descored, 6-character source name.  Matching the full filename fails for stems longer than six characters, e.g. #CARDCSB from ARDCSBUS.hal.)
- SMALLEST-FIRST IS WELL FOUNDED, and there is a number for it: the per-configuration HAL/S counts sum to 7059 instances but only 3212 are distinct, so each CSECT appears in an average of 2.2 configurations.  A mechanism knocked down in SSW is therefore already fixed wherever else it occurs.
- ORDER OF WORK, from the user and it corrects an instinct worth resisting: mass-testing every HAL/S file first is PREMATURE.  Only a few failure mechanisms are expected, so take each one intensively as it is met and knock it down before proceeding.  compilePASS existed as a mass driver only because that triage had already been done for the corpus phase.  A driver here is worth building AFTER the first mechanism falls, to measure how far the fix reached -- not before, to hunt for work.
- Known mechanisms so far, to be grown by observation rather than fixed in advance: linker errors (hand off to the lnk101 developer), differing register selection, and constant versus immediate operand.  The last displaces every instruction after it, so count ROOT CAUSES, not differing halfwords -- the corpus phase's twelve failures were six roots and four pure cascades.
- The compiler options were already swept by the user, who found none better than the built-in defaults.  Treat that as a lead to redo rather than a closed question -- his own caveat -- and note that halsParms.py now makes an option change apply identically to all three scripts, so the sweep is cheaper to repeat than it was.
- DASS scraping is a WEAKER suspect than the OI301700 extraction was: unMAFGEN2.py parses a regular syntax, where the OI301700 work was string matching against listings.  Keep it in mind only for a discrepancy that makes no sense from either side.
- OI301700 is out of scope for want of DASS dumps, with no prospect of any; the door stays open if some appear.
- Do not work in PFS/OI340600.  compileLinkCompare leaves archive.results/ behind, and that accumulation is what froze the IDE; use a copy under ~/ForClaude as the corpus runs now do.
- Corrections: "data<i>" should close the tag; "assembly languate"; the glob is DASS_*.ASC with an underscore and one member is DASS_SSW_(PostIPL).ASC; the file mixes HTML and Markdown.  Worth keeping as-is: the GSRRSL worked example, and its observation that the code CSECT matched while the data CSECT did not -- whether that code/data split holds generally is a cheap early measurement that would aim the whole phase.
