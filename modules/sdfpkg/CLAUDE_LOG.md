### [2026-08-02] Target: [familiarization-findings.md]
- Both remaining latent defects fixed, and fixing them turned up four more in the same never-executed stretch of HALINCL/VMEM3.py.
- The MISC family (GET_MISCF/SET_MISCF/GET_MISCH/SET_MISCH/GET_MISCB/SET_MISCB) now reaches its datum with COREWORD/COREHALFWORD/COREBYTE over the core address LOC_MISC returns, instead of doing NODE_F(0) on a Python list. SET_MISCF's v2.ODF (no such name; VMEM2 defines only MODF) corrected. SET_MISCH keeps the XPL's vestigial OLD_VALUE parameter but defaults it, since XPL had no other way to declare a procedure-level local. LOC_MISC's `OFFSET / VMEM_PAGE_SIZE` was Python float division where XPL's FIXED division truncates, so SHL() had nothing it could shift.
- MISC_ALLOCATE marked a multi-page table with `| 0x80000000`, which makes a 32-bit FIXED negative but leaves a Python int positive, so LOC_MISC took the single-page branch and added the whole byte offset to one page's offset field. Now coerced to signed 32-bit.
- PTR_LOCATE, all in code no compile had reached: `BAD_PTR;` twice as a bare name rather than a call, so an invalid pointer was diagnosed nowhere; `VMEM_TOTAL_PAGES` unqualified (NameError); `EXIT;` bare; `d` never imported, so the CLASS_BI diagnostics could not have been issued anyway; `global PREV_COUNT` misspelling PREV_CNT; and SET_INDEX declaring `global PREV_CNT, CUR_INDEX` where CUR_NDX was meant, so CUR_NDX stayed -1 and PAGING_STRATEGY always reported error 700.
- The structural one: the XPL's `GO TO LOC_COMMON1` leaves the new-page branch and lands inside the *other* branch's `IF CUR_NDX = -1 THEN DO`, just past the FILE read. The port set `goto` and then simply fell out of the if/else, so a newly created page was never recorded in VMEM_PAGE_TO_NDX or VMEM_PAD_PAGE and VMEM_PAD_CNT was not updated. Consequence: the next reference to that page took the reload path and PAGING_STRATEGY eventually tried to write a page out to page number -1. The label's body is now lifted out below both branches, which is where all three paths converge in the XPL.
- GET_CELL's page-search loop was missing the exit that `GO TO GET_SPACE` performs, so it ran to its last iteration and handed PTR_LOCATE the wrong page's PAGE/AVAIL_SIZE -- PAGE 0 with a negative AVAIL_SIZE, i.e. an offset past the end of the page. This one was *not* latent: it happens on 029-DATATYPES with SIMULATING on. It went unnoticed only because BAD_PTR was a no-op and because normalize() treats a core address flatly, so the malformed pointer happened to land on the right byte anyway. Making BAD_PTR a real call is what exposed it; the compile aborted with BI701 until the loop was fixed.
- DECOMPRE.py ported from DECOMPRE.xpl; NEXT_RECORD now imports DECOMPRESS instead of calling a nonexistent g.DECOMPRESS. IN_REC_PTR is module-level, XPL locals being static, which is what lets one call resume where the last stopped. The duplicate-character loop is written out because XPL's `DO OUT_REC_PTR = OUT_REC_PTR TO OUT_REC_PTR + (CNTL_BYTE & '3F') + 1` evaluates its limit once and leaves the variable one past it.
- Verified by round trip against a compressor written to the same format: six cards including blank runs, literal runs, a 66-character repeat, an all-blank card and a run straddling a record boundary. The EOF branch had to be driven directly, for the reason below.
- Porting DECOMPRE is necessary but not sufficient for compressed source, and the docstring now says so. A compressed record is binary, but the port carries a record as a Python str and recovers bytes with BYTE(), i.e. asciiToEbcdic[ord(c)] -- which has no preimage for 130 of the 256 values, 0xFF (the EOF control byte) among them. Reading compressed source also wants the input path to carry bytes rather than characters, which is a change to the I/O model, not to DECOMPRE.
- Regression: 98 sources byte-identical to the pre-change baseline, identical statuses, empty stderr. New probes: 30 MISC checks, 11 DECOMPRESS checks. tests/runall.sh 13/13, cmem 78, sdfpkg 3451.

### [2026-08-02] Target: [sdfpkg-rationale.md]
- DEFERRED WORK, agreed with the user 2026-08-02: SPACELIB errors are skipped rather than fixed, and must be come back to. SPACELIB is linked into every pass and can abend with BI002 (bug in the space-management system), BI003 (the same with too little space left to describe it), BI004 (too many such errors) or BI010 (yoyoing -- try a larger region), all severity 3 and all naming "SPACE MANAGEMENT". The user reports having seen several kinds and never having corrected any. First one met in practice: APPLSRC/RVMCON.hal of OI340600, "BI002 SEVERITY 3 BUG IN SPACE MANAGEMENT SYSTEM ->->NON COMMON RECORD AT LINK, DOPE=4448", raised by HALSFC-PASS3 on a file PASS1 compiled with zero errors.
- The deferred work is on the C side only. Correction from the user 2026-08-02: since HAL_S_FC.py has no memory model at all, and SPACELIB exists to manage allocation and garbage collection within one, SPACELIB functionality is *meaningless* in the Python port. What references survive there are only because deleting them was more trouble than leaving them. So the Python side wants deletion, not repair, and removing the SDF path's SPACELIB calls from INCSDF.py -- the paging-area augment, the FORFCB/PGING allocate-and-free pairs, the SYM_TAB capacity reservation, each replaced by a comment quoting the XPL it stood in for -- was the correct treatment rather than a compromise.
- What HALINCL/SPACELIB.py still does is not space management. RECORD_CONSTANT appends Python objects to a list (bytearrays of VMEM_PAGE_SIZE for VMEMREC, class instances for FOR_ATOMS and LIT_PG, None otherwise), RECORD_ALLOC returns len(), and RECORD_USED is already a no-op. Only the VMEMREC allocation is load-bearing, VMEM3 taking VMEM_PAD_ADDR[I] from it. Whoever eventually deletes the rest should keep that in mind.

### [2026-08-02] Target: [sdfpkg-rationale.md]
- sdfpkg ported to C and integrated into HALSFC. It lives in the XCOM-I runtime library, XCOM-I/sdfpkg.{c,h}, because the C that XCOM-I emits from the XPL/I cannot be edited and MONITOR(22) is a runtime service. The master copy is XCOM-I's; the copies that appear in PASS1.PROCS/PASS1.build/ are overwritten on every build, so must never be edited. XCOM-I.py's framework-copy list gained the two files, and Makefile.template already globs *.c, so nothing else needed changing to get them compiled.
- runtimeC.c's MONITOR22/MONITOR22A were the "not yet implemented" stubs; they now call sdfpkgService/sdfpkgInitialize, short-circuiting to the old return value when no --sdfi was given, so a compilation that does not ask for SDFs behaves exactly as before. ebcdicToAscii had to lose its `static` (SDFNAM in COMMTABL is EBCDIC like everything else in the XPL/I program's memory).
- The C walks the SDF's raw System/360 bytes directly, as SDFPKG.ASM did, rather than porting sdf.py's parser to serve as a search index. The caller only ever reads raw bytes at the returned ADDR, so the index bought nothing in C. Modes implemented: 0, 1, 4, 5, 7, 8, 9, 13 -- everything INCSDF uses. Anything else abends by name rather than returning quietly, since a silent stub leaves the previous call's COMMTABL values in place and therefore reads as success, which is exactly how the Python's unimplemented modes once looked as though they worked.
- One bug worth recording, found the moment it first ran: modes 8 and 9 must report far more than PNTR/ADDR. Mode 9 also sets SYMBNAM, SYMBNLEN and BLKNO, and mode 8 sets BLKNAM, BLKNLEN and CSECTNAM, because INCSDF reads the located symbol's *name* back out of COMMTABL in order to enter it. Returning only the pointer gave "PM2 DUPLICATE DEFINITION OF STRUCTURE TEMPLATE" as INCSDF worked from whatever the previous call had left in the field.
- The RVL divergence was mine, in the Python. BLKNO carries the revision level as two EBCDIC characters, not as a number, so "absent" is the characters "00" and not binary zero -- which the XPL's STRING("01000000"|ADDR(REV)) renders as two NULs. cmem's mode 4 now reports 0xF0F0 in both implementations and INCSDF.py's RVL_STRING is a plain decoder rather than a special case.
- Result: with both compilers importing SDFs, `HALSFC --test --sdfi=SDFLIB` reports pass1.rpt, FILE1.bin (HALMAT), FILE3.bin (IC), literals and LISTING2 all *identical* between HALSFC-PASS1 and HAL_S_FC.py, on 10 of the 12 tests/ fixtures. The other two, USEL2 and USEL3, are negative cases whose expected DU1/XI7 errors abort HALSFC before the cross-comparison runs; the C produces exactly those errors.
- This also disposes of compilePASS.md's point 11 entirely. The concern was that the C listing a whole TEMPLATE while the Python listed none would make the reports incomparable -- ~4000 lines on CSDMDT.hal. With the C importing SDFs too, neither lists anything and no report-filter surgery is needed.
- Stale Makefile reference cleaned up: PASS.REL32V0/Makefile still had SDFPKG := ../../ported/sdfpkg, the directory deleted earlier, and `clean` ran `make -C` on it.

### [2026-08-02] Target: [sdfpkg-rationale.md]
- Corpus run with BOTH compilers importing SDFs: 994 attempts, 946 SDFs and templates, Done. DI11 skips 43 -> 2 and PM2 79 -> 0 against the template-route run, so the errors go away from the *build*, not merely from the Python's report. Cross-comparison over 928 files: pass1.rpt and HALMAT identical on every one, 0 differences.
- Failures classified against the ordering bugs the user warned of: of 60, three are ordering (GKIORB wants GKRORB; REXRMS wants RQCIPC and RHMHLT; GKEKIP wants GKGMNV -- all giving XI3, the SDF-not-found diagnostic, which is the signature you would expect), 13 are cascades, and 44 are "genuine" but dominated by SPACELIB. None is attributable to SDF import.
- CORRECTION to the earlier entry blaming SPACELIB as a pre-existing C defect. Only RVMCON's is in PASS3, which is the pre-existing one. The other 47 are in *PASS1*, and PASS1 is where the new SDF path runs, so they are very likely mine. RECORD_LINK insists every still-allocated record is COMMON (_IS_COMMON(CUR) is CUR < ADDR(DESCRIPTOR), and XCOM-I's generateADDR returns regions[1][1], the top of COMMON, for that). INCSDF.xpl:897-900 frees FORFCB and PGING precisely so that holds. The SDF path now really allocates them in the C, so a path that leaves either allocated at LINK gives exactly "BI002 ... NON COMMON RECORD AT LINK, DOPE=". Next step is to find which INCSDF exit misses the free, not to go digging in SPACELIB.
- spacelib.c in XCOM-I is a comment block with no code, so SPACELIB in the C build is the translated SPACELIB.xpl and not a hand-written replacement.
- compilePASS's skip policy widened three times during the run: D15, then a general "any PASS1 error is delayable" catch-all, because importing SDFs lets files reach source-level errors of their own that the template route never got to (PGPPLD: D15; GKFHOR: M1). Both showed the two compilers agreeing exactly while failing. The catch-all names the codes it found. This is a real loosening and wants the user's review.
- User's note for later, on which files actually belong to PASS: OI340600 also holds auxiliary code, and compilePASS simply compiles every .hal in APPLSRC/ and SSSRC/ regardless. INCL80/ holds HAL/S that is not a compilation unit at all -- included whole by "D INCLUDE name", no TEMPLATE keyword -- and is never compiled directly. Membership can be determined indirectly from PFS/mafgen/csects-*.json (CSECT indexes per configuration G16, G2, G3, G8, G9, P9, S2, SSW, distilled from the DASS_*.ASC disassemblies), or reportedly recursively from PFS/OI340600/CON80/GNC1..GNC9, though the user has no ready recipe for either. Both index CSECTs rather than HAL/S files, so a mapping would have to be worked out.
- PGING/FORFCB leak theory investigated and NOT confirmed. Repro: APPLSRC/CS4IX3.hal of OI340600, two "D INCLUDE TEMPLATE" directives (CSA_PDT then CS4_PDT), both SDFs and both templates present. The first prints "INCLUDED FROM SDF ##CSAPDT"; the second prints no import message at all, yet its symbols clearly are entered, because it raises DQ7 "the declared name CSAS_PDT_PAR_ENTRY duplicates the name of a node of the template of an unqualified structure previously declared in the same name scope" against names from the first. Compilation then abandons at LINK with BI002 NON COMMON RECORD AT LINK, DOPE=4336.
- What rules the simple leak out: INCSDF.xpl has no RETURN or GO TO between "CALL ENTER_COMPOOL_VARS" (862) and the frees (897-900), and both frees are guarded by RECORD_ALLOC, so on that path they must run. The contradiction to resolve is that the second include's symbols are entered (DQ7 proves it) while its "INCLUDED FROM SDF" message at 893-894 never appears -- so control is not flowing the way a straight read of INCLUDE_SDF suggests. Suspect PATCH_INCLUDE handles the second include by a different route, or ENTER_COMPOOL_VARS is reached other than from 862. Two includes alone do not reproduce: the USEAB fixture (COMPA + COMPB) imports both SDFs and exits 0.
- Note that CS4IX3 got PM2 under the template route in the previous run and DQ7 under the SDF route now, so this family of files has a genuine duplicate-name problem between its compools either way; the open question is only why it turns fatal at LINK.
- Eviction logging: ZERO evictions in either include of CS4IX3, so the page-thrashing theory for the DQ7 duplicates is dead. The duplicates are genuine: CSAS_PDT_PAR_ENTRY occurs 188 times in APPLSRC/CSAPDT.hal and 23 in APPLSRC/CS4PDT.hal, so a file including both compools really does collide and DQ7 is correct. The only defect left in that file is that a survivable severity-2 DQ7 turns fatal via the unfreed PGING at LINK.
- PASS membership solved, which reframes the whole failure list. A compilation unit's CSECT is "#P" plus its descored name truncated to 8; verified against SDFs that exist (CDR06D -> #PCDR06D, RVMCON -> #PCRATE/#PCRCCOT/#PCRDCIL, all present in the indexes). Of the 60 failures, only 16 are in PASS -- the CS2 family plus CPTOSV and CPUSLS. The other 44 are auxiliary, and the split is self-confirming: every CS2* is in PASS and every CS4* counterpart is not, which is exactly why they collide on structure names. CS4IX3, chased at length, was never part of PASS. GKEKIP/GKFHOR/GKIORB are also outside it, which disposes of two of the three ordering faults and of the M1 that forced the general skip-policy widening.
- compilePASS now consults ../mafgen/csects-*.json (--csects=D, --no-csects to disable) and reports "Not part of PASS, skipping" instead of compiling. Membership is the union of all eight configurations, per the user. Loads 3859 CSECTs; verified it skips CS4IX3.
- RETRACTION and fix of the PASS-membership check. The "#P + name" mapping was wrong and the run built on it excluded 717 of 1167 files; the earlier "44 of 60 failures are not part of PASS" claim rested on the same bad heuristic and is withdrawn. The evidence had been on screen and was misread: RVMCON's own SDF gives its CSECTs as #PCRATE/#PCRCCOT/#PCRDCIL, none of them #PRVMCON.
- Correct method, from the SDL ICD (USA001556) pages 163-164: every CSECT name is two characters of type followed by the six-character HAL/S compilation-unit name. $0 PROGRAM, $1-$F task, #C COMSUB, an (a=A-M, n=0-9) internal procedure, aa library routine, @c stack, #D DECLARE data, #P COMPOOL data, #Z/#Q/#0/#E/#L/#X the ZCON and special data sections, ## SDF, @@ TEMPLATE. #P alone is only COMPOOL *data*, which is why testing it rejected every PROGRAM. So strip the two-character prefix from every CSECT in the indexes and keep the names: a unit is in PASS if a CSECT of any type was emitted for it. 3859 CSECTs -> 1654 distinct unit names, against 1167 source files, which is the right shape since one file can hold several units.
- Now correct on the cases that exposed the bug: RVMCON, CDR06D, DKFCM8 and CRATE all in PASS; CS4IX3 out while CS2IX3 is in, so the CS2/CS4 split survives the corrected method and now has real evidence behind it.

### [2026-08-03] Target: [sdfpkg-rationale.md]
- HANDOVER. Open question: BI002 "NON COMMON RECORD AT LINK" on ~30 PASS files that import SDFs. Everything below is measured, not inferred.

**The finding.** `_IS_COMMON(CUR)` in SPACELIB.xpl:994 is `CUR < ADDR(DESCRIPTOR)`, and XCOM-I's generateADDR (generateC.py:776) returns `regions[1][1]` for that. In PASS1 the regions are in PASS1.build/memory.c:13498; region 1 is {1318, 4196}, so the COMMON boundary is **4196**. RECORD_LINK abends on any record still allocated at LINK whose dope is >= 4196.

**Measured dopes** (instrumented every RECORD_CONSTANT/RECORD_FREE site in PASS1.build; 17 sites):
  - Normal compile, no --sdfi (SSSRC/DUMULK.hal): five records allocated, none freed, no BI002 -- LIT_NDX 1456, FOR_DW 1484, FOR_ATOMS 1512, LIT_PG 2428, VMEMREC 2456. All **below** 4196. So surviving to LINK is normal and expected; RECORD_LINK hands each to _RETURN_UNUSED.
  - SDF compile (APPLSRC/RVMCON.hal): INIT_APGAREA 4336, INIT_AFCBAREA 4364, PGING 4784. All **above** 4196. PGING is balanced 6 allocations / 6 frees. INIT_APGAREA and INIT_AFCBAREA are never freed anywhere -- INCSDF.xpl has RECORD_FREE only for FORFCB (752, 898) and PGING (900).

**Conclusion.** Not an INCSDF bug and not a SPACELIB bug. The five records declared in ##DRIVER.xpl/COMMON.xpl land in region 1; the three declared in HALINCL/INCSDF.xpl land in region 2. The discriminator is an XCOM-I region-assignment question, not a boundary question. Start at generateC.py `allocateVariable` and the `baseRestriction` argument (generateC.py:2755), and at how a `BASED ... RECORD` declared in an included file is placed versus one declared in ##DRIVER. Note generateADDR's own comment calls the regions[1][1] choice "a workaround ... in lieu of altering the design".

**Retractions to carry forward, so the wrong turns are not repeated.**
  - PGING is NOT the leaked record. Asserted, disproved, revived on a mode tally, finally disproved by the dope trace. It balances 6/6.
  - "44 of 60 failures are not part of PASS" was based on testing `#P`+name, which is only the COMPOOL *data* convention and rejected every PROGRAM (717 of 1167 files). Withdrawn. Corrected method below; real figure is 59 excluded.
  - SPACELIB was called a pre-existing deferred C defect. Only RVMCON's PASS3 instance is; the PASS1 ones appeared with the SDF path.
  - Page thrashing / LRU eviction was a theory; eviction logging shows ZERO evictions. Dead.

**PASS membership, corrected.** SDL ICD (USA001556) pp.163-164: every CSECT name is two characters of type plus the six-character compilation-unit name -- $0 PROGRAM, $1-$F task, #C COMSUB, an internal procedure, aa library routine, @c stack, #D DECLARE data, #P COMPOOL data, #Z/#Q/#0/#E/#L/#X ZCON and special data, ## SDF, @@ TEMPLATE. compilePASS now strips the prefix from every CSECT in ../mafgen/csects-*.json and keeps the names (--csects=D, --no-csects). 3859 CSECTs -> 1654 names. One .hal outside INCL80 = exactly one compilation unit (user), so halname is unambiguous; the surplus over 1167 files is library and assembly CSECTs, making the test permissive in the safe direction.

**Last good corpus run** (both compilers importing SDFs, membership check on): 917 compiled, 59 not in PASS, 885 SDFs, Done. PM2 0 and DI11 1 against 79 and 43 under the template route. 870 files cross-compared, **0 differences** in pass1.rpt and in HALMAT. Residue: ZO3 90, SPACELIB 30, XI3 12, other PASS1 1, 193 never attempted.

**Tools and state.**
  - Repro for BI002: `cd ~/workspace/PFS/OI340600-sdftest` then HALSFC-PASS1 on APPLSRC/RVMCON.hal with --sdfi=SDFLIB and the usual --pdsi switches (see the command in this log's history).
  - `SDFPKG_TRACE=1` env var makes XCOM-I/sdfpkg.c log every MONITOR(22) mode, its COMMTABL inputs, and every page eviction.
  - Fast rebuild: `cd PASS.REL32V0/PASS1.PROCS/PASS1.build && make`, then `cp ../PASS1 ../../HALSFC-PASS1`. ~20s. Full rebuild `make -s XEXTRA=--quiet PASS1` from PASS.REL32V0 (~30s) regenerates the C and DISCARDS temporary edits there.
  - **PASS1.build currently holds temporary debug edits** (17 DOPETRACE fprintf calls) and HALSFC-PASS1 is that instrumented build. Rebuild fully before any corpus run.
  - Work directory ~/workspace/PFS/OI340600-sdftest is a copy of OI340600's APPLSRC/SSSRC/INCL80; the user's own OI340600 is untouched.

**TO RESUME IN A CLEARED SESSION**, say something like: "Read modules/sdfpkg/CLAUDE_LOG.md, last entry, and resume the BI002 investigation: find why XCOM-I places INCSDF.xpl's RECORD_CONSTANT records (INIT_APGAREA 4336, INIT_AFCBAREA 4364, PGING 4784) in region 2 while ##DRIVER.xpl's land in region 1 below the 4196 COMMON boundary."
