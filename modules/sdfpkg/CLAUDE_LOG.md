

### [2026-08-07] Target: [HANDOFF.md]
- Queue step 0 done: foreign-symbol recovery is now per-site and evidence-driven (commit c90e0f9f9). A site counts only if ANCHORED — its own section must be in the CSECT index at exactly the address lnk101 placed it. An anchored site holding the bare addend, or the addend with bit 15 set, says the build left it alone. Recover only on strict majority + single implied address + independent corroboration. `--foreign-symbols` became `--no-foreign-symbols`. Net: #EVRPRAM recovered in 7 configs, #EASCTIM in G3/G16, nothing withdrawn.
- Queue step 1 done: `run-configs.sh` snapshots `dass-*.py` into a mktemp dir at launch and runs from it (trap-cleaned on exit), and prints the sdfpkg + nsts-sdl-dps commits. Investigations can now edit the working tree while a sweep runs. Proved itself immediately — three commits landed mid-sweep with no effect on the run.
- Queue step 2 done, and better than planned (commit 932f48219). Rather than reimplementing sector decoding, `dass-versions.py` now takes the target CSECT from lnk101's own RLD at a relocation site, and decodes the dump's halfword by the same amount lnk101 shifted ours. Containment is judged against the DUMP's extent, since our copy of a revised COMPOOL is a different size. S2 gains 82 attributed halfwords: 214 in #PCSSSPA→#PCSAPDT (CC→CD), and the six #PCSASAT references (BZ→CA).
- Queue step 5 solved (commit 8857e5f3c). S2's unresolved class is NONHAL COMPOOLs: HALSTAT's compilation layout for SAFACQ marks CSA_DART, CSA_INB, CSA_IXP, CSA_PART as NONHAL, so nothing in HAL/S defines them and the configuration supplies the storage — #PCS2DAR, #PCS2INB, #PCS2IXP, #PCS2PAR. All 13 address references land exactly on those CSECT starts, including two negative displacements and one sector-encoded ZCON. Nine aliases recovered in S2; none fires in any other configuration.
- New in dass-syms: `basesFrom()` honours RLD bit 7 (sign) and bit-15 sector encoding, and excludes BSR-only/DSR-only relocations, which patch a register field not an address. A uniqueness guard refuses an address several symbols derive — that is what refuses S2's seven #ZP symbols all deriving 0x298.
- lnk101 does not announce a NONHAL COMPOOL as undefined, so the foreign pass is now driven from the relocations as well as from the log.

### [2026-08-07] Target: [mafgenComparison.md]
- The "not attributable at all" FCOS category was partly an artefact. References that looked like they pointed into FIOCBLKS were sector-encoded ZCON halfwords; decoded via lnk101's own RLD they point into #PCSASAT, a revised HAL/S COMPOOL. Revise the claim: FIOCDATS/FIOMODSM/FCMPSA may still be unattributable, but FIOCBLKS was a decoding artefact and is now resolved.
- Add the NONHAL COMPOOL mechanism to "Mechanisms found".
- Findings still open, recorded so they are not lost: (a) S2's augmented index places #PCVTTCS at 0xBE1A–0xD718, overlapping the scraped #PCSAPDT and #PCSAPCT — two CSECTs cannot share memory, so the recovered one is wrong and corrupts owner(); a guard rejecting a recovered address that overlaps a scraped entry is worth adding. (b) G16 #DGFKGRT @07DFE holds 4232 against the dump's 0000 with no relocation at that site, so it is compile-time data, not a link artefact — possibly an unmarked I-LOAD. (c) The A*SPSPSP/$0* family in G9 and S2 differs by small deltas (0x6–0x1C) at sites with no RLD record at all, so neither the relocation path nor owner() has anything to work with.

### [2026-08-07] Target: [mafgenComparison.md]
- The SPSPSP family is characterised. All 26 unexplained halfwords across G9 and S2 ($0SPSPSP, A4/A5/A6SPSPSP, $0VG9OPS, $0SM2OPS) share one signature: the HIGH byte is identical in every case and only the low byte differs, by +0x6, +0xC, +0x14, +0x18 or +0x1C — always positive, always even. The halfword is an instruction whose opcode and register are unchanged and whose DISPLACEMENT grew, so it is a base-register-relative access, not an address. That is why there is no RLD at the site and why owner() can never see it: there is nothing to relocate.
- Corroborated at S2 A6SPSPSP 0x10E18, where the adjacent halfword DOES carry an RLD into #PCSPCLB (revised BN→BO) and is already excused as revision drift. Positive even deltas are the insertion signature: the build's variables sit further from the base than ours.
- The rigorous route to attributing these is HALSTAT's own per-variable offset table for the revised COMPOOL against ours — the same evidence already used for CSPCLB (dump at offsets 67/86/88 where we place the same variables at 62/80/82). If a variable we place at displacement 0x86 is at 0x92 in HALSTAT, the difference is confirmed rather than inferred. Not yet done; it is the correct next step, not a blocker.
- Correction to the earlier entry: the claim that a recovered address overlapping a scraped CSECT must be wrong is FALSE. Nesting is the norm — a COMPOOL whose storage is owned by an assembly module lives inside that module's CSECT, which is dass-syms.py's entire premise. A guard on that basis rejected #PCDHMMU, the documented type case, and was withdrawn. What was real is that owner() picked the outermost of several containing spans; it now takes the narrowest (commit ae3dee531). #PCVTTCS at 0xBE1A-0xD718 across #PCSAPDT genuinely does conflict, but that is a partial overlap, not nesting, and it is not evidence that the recovery is wrong.
- HALSFC is a plain Python program invoked by path, not a pip install, so it CAN be copied and instrumented while a sweep runs. Only lnk101 and fcmcmp are genuinely blocked by an in-flight sweep. The PASS1 via-double literal call site is therefore not blocked after all.

### [2026-08-07] Target: [mafgenComparison.md]
- NEGATIVE RESULT, recorded so it is not repeated. Testing the SPSPSP displacements against HALSTAT's global variable table (variable -> CSECT + OFFSET + per-phase ADDR, 1105 CSECTs and 68476 offsets) proves nothing as constructed. With that many offsets, almost any (ours, dump) displacement pair appears in SOME CSECT, and the hits name unrelated variables — 0x86->0x92 "matches" #DGDNHSI GSI_PRE->HEADING, #DGDORTL PRIBEAR_HSI->CDI and #DGCOASC KL_MPS_PPD->CGCV_YREF, three different explanations for one pair. This is precisely the coincidence the same-CSECT rule exists to exclude.
- The test was confounded twice over: it also looked up BOTH sides in HALSTAT, so the "ours" column was the build's layout rather than our own.
- What would be evidence is the variable NAME matching across the two layouts: our displacement must be variable V in OUR offset table and the dump's must be the same V in HALSTAT's. That needs our own offsets, which are in the compiled SDF (SDFLIB/##CSPCLB.sdf, GLS_BINARY_MSB_FIRST). modules/sdf parses these — `sdf.sdf(cmemInstance)` then `parseSDF()` — so the route exists but needs a cmem instance wired up. Not yet done.
- Even with both tables, the base register still has to be pinned to a CSECT, since the halfword is a displacement and nothing in it names a target. Nearby RLDs give candidates (at S2 A6SPSPSP the neighbours load #PCSPCLB and A2SPSPSP), so the honest framing is: name-matching would CONFIRM a candidate the RLDs propose, not identify one on its own.

### [2026-08-07] Target: [HANDOFF.md]
- BUG I INTRODUCED, and the fix. Snapshotting dass-*.py broke result recording: dass-db.py sets DEFAULT_DB = Path(__file__).resolve().parent / "dass-compare.db", so running from the snapshot wrote every result into the snapshot's own database — which the EXIT trap then deleted. Worse, the inter-sweep membership/run resets inside run-configs.sh use $DB, the REAL path, so sweeps 2 and 3 found nothing marked 'todo' and never ran at all. The tell was `RESULT S2: 0/0 sections match`; four configurations had been swept into a throwaway DB before it was noticed.
- Fixed by passing --db="$DB" explicitly to both dass-run.py and dass-db.py, and by removing any dass-compare.db the copy drags into the snapshot. Lesson worth keeping: a snapshot is only isolated if EVERY path the snapshotted code derives from its own location is also redirected — __file__-relative defaults are the trap.
- The rescued snapshot DB's numbers (SSW 423/476, P9 471/509, G8 1700/1739, S2 1118/1252) are sweep-1-only figures — plain index, no symbol recovery, no version no-claims — NOT a regression. SSW at 423/476 before recovery is the normal starting point for the pipeline that ends at 476/476.

### [2026-08-07] Target: [README.md]
- Reading a compiled SDF directly, without going through MONITOR(22): `cmem(bytearray(0x100000), "SDFLIB")`, then `fromNative({MISC, APGAREA, AFCBAREA, NPAGES, NBYTES, ADDR, PNTR}, commtabl=addr)` and `monitor22(0, addr)` to initialise, then `fromNative({"SDFNAM": "##CSPCLB"})` and `monitor22(4)` to select, then `sdf(c).parseSDF()`. The reader never opens the file — cmem pages it into the bytearray and sdf walks it from the Master Directory Cell. Working script in scratchpad/ouroffsets.py.
- Yields masterDirectoryCell, directoryRootCell, blockIndexTable, symbolIndexTable, statementIndexTable, initializationTable, titleDataCell, cardtypeDataCell, includeTextData. `s.fullSymbolASCII(entry)` decodes the EBCDIC names.

### [2026-08-07] Target: [mafgenComparison.md]
- NEGATIVE RESULT: the SDF does NOT carry per-variable memory offsets, so it cannot supply the "ours" side of the SPSPSP name-matching test. ##CSPCLB's 58 symbols all have pAuxiliarySymbolInformation = 0, and the only non-zero zeroOrOffsetToExtension values (32, 36, on 9 symbols) are offsets within the data cell, not addresses. That is consistent with what an SDF is for: it is the cross-compilation DECLARATION database, and address allocation belongs to the code generator. Our offsets will have to come from the compilation listing (the original build used FLAGS: SRN,ADDRS per HALSTAT) or from the object file, not from SDFLIB.
- Incidental corroboration: ##CSPCLB's initializationTable has exactly 134 entries, matching the independently-noted "our COMPOOL is 134 halfwords where the index says 140" for CSPCLB. Two unrelated routes to the same number.
- ##CSPCLB's blockIndexTable[0].blockCsectName decodes from EBCDIC to "#PCSPCLB", so the SDF does name the CSECT even though it does not place its contents.

### [2026-08-07] Target: [mafgenComparison.md]
- SUPERSEDES the earlier "positive and even" characterisation of SPSPSP, which was imprecise, and which also wrongly merged displacements from different sections into one sequence. Per section, the generating rule is a DOUBLED ARRAY STRIDE: wherever consecutive references walk an array, our stride is 8 halfwords and the dump's is 16 (in G9 A6SPSPSP, which skips an element, 0x10 against 0x20 — the same 2x ratio).
- Two fields are referenced per element, at +0 and +4, and those intra-element positions are UNCHANGED: field@+0 is ours 0x86+8i -> dump 0x92+16i, field@+4 is ours 0x8a+8i -> dump 0x96+16i. One formula covers the whole first group: delta = 0xC + 8i. That is why the deltas run 0xC, 0x14, 0x1C — they increase linearly while walking an array and are constant elsewhere, not monotonically overall.
- Reading: an array of structures whose element grew from 8 to 16 halfwords, the referenced members keeping their positions at the front of the element (new members appended, or inserted after +4).
- The evidence is the cross-section consistency: 0x9a->0xb6 appears in $0SPSPSP, A5SPSPSP and G9 A6SPSPSP; 0x92->0xa6 in $0SPSPSP and A5SPSPSP; 0x8a->0x96 in A4SPSPSP and G9 A6SPSPSP; 0x72->0x7e and 0x73->0x7f in $0SM2OPS and $0VG9OPS. Five sections, two configurations, one mapping. The transform is a FUNCTION of the displacement — same input, same output — which coincidence does not produce and which the earlier HALSTAT offset test could not establish.
- Not everything fits one formula, and it should not be forced to. The 0xa2-0xba group shifts by a constant 0x18, which cannot be "after the array": that needs 0xC + 8N = 0x18, i.e. N = 1.5 elements. It is a separate region with its own shift. 0x28 -> 0x2e (+6) is a third.
- CONSEQUENCE for the next step: attribution no longer needs a general instruction decoder with base-register tracking. It needs one narrow question answered — which structure array, in a COMPOOL that S2 and G9 both carry and that HALSTAT marks revised, went from 8-halfword to 16-halfword elements between our revision and the build's? Finding it identifies the target CSECT that the missing RLD cannot supply.

### [2026-08-07] Target: [mafgenComparison.md]
- CORRECTION, important: the earlier "the SDF does NOT carry per-variable memory offsets" entry is WRONG and should be struck. Variables (symbolClass 1) carry `symbolDataCell.relativeMemoryAddressOfSymbol`. I had inspected only pAuxiliarySymbolInformation and zeroOrOffsetToExtension, both zero, and concluded too fast; the LABEL symbols I sampled genuinely lack an address because they carry statementNumber instead. The name-matched offset test IS available from SDFLIB alone, with no listing needed.
- The test, now working (scratchpad/compare-offsets.py): ours from the SDF, the build's from HALSTAT's "(CSECT: X OFFSET: nnnn)" entries, matched by variable NAME. Matching by name is what makes it evidence — any two offset tables share values by coincidence, but the same variable at two different offsets says the layout moved.
- RESULT: all three candidate COMPOOLs are EXCLUDED as the target of the SPSPSP doubled-stride group.
  * #PCSPCLB — delta histogram {0: 20, +3: 1, +5: 1, +6: 11, +7: 1}. None of 0xC/0x14/0x18/0x1C. But it confirms the size story exactly: 11 variables shift by +6 from offset 0x60 onward, and 134 + 6 = 140, the index's figure. Two independent routes to the same number, now three counting the initializationTable's 134 entries.
  * #PFCMCOM — all 53 matched variables at delta 0. The CK->CQ revision moved nothing. (First attempt used the wrong CSECT name #PCFCMCO and matched 0; the real name is #PFCMCOM.)
  * #PCSAPDT — does have +0x14 on 64 variables, in a repeating {base, _FDA at +4, _LIM at +6} pattern that matches the +0/+4 fields the SPSPSP code references. BUT G9 does not carry #PCSAPDT at all — only S2 does — and the same displacement maps identically in both configurations, so it cannot be the shared target.
- Incidental: #PCSAPDT's histogram contains -0x305 on 31 variables, independently confirming the "our references run 0x305 past where the build put the same variables" finding recorded for #PCSSSPA.
- NEXT: the search is now well-posed and mechanical rather than speculative. Find a CSECT that (a) appears in BOTH G9 and S2, (b) shows name-matched deltas of exactly 0xC/0x14/0x1C at offsets 0x86-0x9a, and ideally (c) has a structure array whose element goes 8 -> 16 halfwords. #PFCMCOM is in both at the identical address 0x9c72; #PCSPCLB is in both at different addresses. Neither fits, so it is some third COMPOOL — sweep all SDFs whose CSECT is in both indexes.
