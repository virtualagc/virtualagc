[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "asciiToEbcdic"
version = "1.0.0"
description = "Conversion tables (specific to Virtual AGC) for converting EBCDIC to ASCII and vice-versa"
dependencies = []
