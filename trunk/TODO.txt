Replace yaACA by yaACA2.

Add a new GUI element to replace xterm as the medium via which "yaAGC --debug" and "yaAGS --debug" are run.

Fix the fantastic waste of RAM by misuse of source filenames in symtab storage.

