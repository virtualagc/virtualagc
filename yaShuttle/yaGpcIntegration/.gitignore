# Everything below is copied/built here from yaGPC2/ and yaHALMAT2/'s
# own builds (see Makefile/NMakefile) -- none of it is source, all of it
# is regenerable by `make` (or `nmake /f NMakefile`) from this directory.

# Copied-in libraries and CLI binaries.
libyaGPC2.a
libyaGPC2.lib
yaGPC2
yaGPC2.exe
libyaHALMAT2.a
libyaHALMAT2.lib
yaHALMAT2
yaHALMAT2.exe

# samples/ build outputs.
samples/paced_run
samples/paced_run.exe
samples/multi_gpc_demo
samples/multi_gpc_demo.exe

# MSVC build byproducts (nmake).
*.obj
*.pdb
*.ilk
