The idea behind this directory is to create a C file that allows
me to manipulate SDF's according to the documentation in USA001556
and possibly other places.

Eventually, I'd like this to essentially turn into a modern port of
SDFPKG.asm.

I'd integrate it then into HALSFC-PASS1, -PASS3, and -PASS4.

And then I'd convert it to Python, in at least a semi-automated 
fashion.

Which I'd integrate into HAL_S_FC.py.

The order of the steps is somewhat fluid.
