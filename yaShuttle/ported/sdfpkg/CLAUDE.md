# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Is

A port of `SDFPKG.ASM` (IBM System/360 Basic Assembly Language) to C and Python 3. SDFPKG is the HAL/S Space Shuttle compiler's "Symbol Data File" (SDF) access package — a virtual-memory paging library for reading/writing structured compiler metadata files. All source files in this directory were generated by Claude (claude-sonnet-4-6) under direction from RSB; no file has been hand-edited by humans.

The C port targets integration into HALSFC (C-based HAL/S compiler). The Python port (`sdfpkg.py`) targets integration into `HAL_S_FC.py`.

## Build Commands

```bash
# Clean build of C library, sdfcheck, and sdf_convert
make clean all

# Override compiler (default: auto-detect clang then gcc)
make CC=gcc clean all
make CC=clang clean all

# Build and run C tests (test_sdfpkg + test_sdf_write)
make test

# Run Python tests
./test_sdfpkg.py

# Run Python standalone tools
python3 sdfpkg.py --help
python3 sdfpkg_dump.py sample.sdf
python3 demo_sdfpkg.py

# Regenerate sample.sdf
python3 make_sample_sdf.py
```

## Architecture

### Flat File Format (replaces z/OS PDS)

```
4 bytes   magic 0x53444600 ("SDF\0")
4 bytes   member count N (big-endian u32)
N × 20    name(8 ASCII space-padded) + page_count(4 BE u32) + pg0_offset(8 BE u64)
N × pages member data: page_count × 1680 bytes per member
```

### Virtual Pointer Encoding

`sdf_vptr_t` is a 32-bit big-endian value: high 16 bits = page number, low 16 bits = byte offset within a 1680-byte page. Null pointer = 0. Macros: `SDF_VPTR_PAGE`, `SDF_VPTR_OFFSET`, `SDF_VPTR_MAKE`.

### C Module Responsibilities

| File | Role |
|------|------|
| `sdf_types.h` | All on-disk struct definitions (`SDF_PACKED`), `sdf_vptr_t`, `sdf_rc_t`, `sdf_commtabl_t` |
| `sdf_internal.h` | Private `sdf_ctx_t` internals |
| `sdfpkg.h` | Public C API (modes 0–18 as named functions) |
| `sdf_write.h` | Public write API (`sdf_wblock_t`, `sdf_wsymbol_t`, `SdfWriter`) |
| `sdf_io.c` | File I/O, page loading/eviction (LRU), flat-file index parsing |
| `sdf_pagmod.c` | Paging area management: augment/rescind, PAD slot lifecycle |
| `sdf_locate.c` | Virtual pointer → host pointer translation (mode 5/7) |
| `sdf_select.c` | Member selection, FCB tree management (mode 4) |
| `sdfpkg.c` | All lookup modes (8–18): block/symbol/statement binary+linear search |
| `sdf_util.c` | Name padding, byte-swap helpers, error strings |
| `sdf_write.c` | Write infrastructure: bump allocator, all cell serializers |
| `sdfcheck.c` | Standalone validator program |
| `sdf_convert.c` | Standalone PDS↔flat converter |

### Python Module Structure (`sdfpkg.py`)

Mirrors the C API exactly. Key classes:
- `SdfContext` — main handle; `open()` / `close()` / context manager
- `BlockResult`, `SymbolResult`, `StmtResult` — dataclass return types
- `SdfWriter` — write path; `add_member()` → `add_block()` → `add_symbol()` → `commit()` → `write(path)`
- `WBlock`, `WSymbol` — dataclasses for write-path inputs

### Write Path Design

The write path uses a bump allocator starting at page 1 (page 0 = PAGEZERO + DROOTCEL). Structures never straddle page boundaries. The block name binary search tree uses balanced median-split over the sorted block array. Symbols are sorted block-first, then alphabetically within each block; `fsymb_no..lsymb_no` ranges on each BLKTCELL are contiguous.

## Data Model

### Symbol Classes (`sym_class`)
`SCLASS_VARIABLE=1`, `SCLASS_EQUATE_EXT=2`, `SCLASS_TEMPLATE=3`, `SCLASS_LABEL=4` (NAME vars), `SCLASS_COMPOOL=6`

### Symbol Types (`sym_type`)
`STYPE_SCALAR=1`, `STYPE_INTEGER=2`, `STYPE_BOOLEAN=3`, `STYPE_CHARACTER=4`, `STYPE_BIT=5`, `STYPE_VECTOR=6`, `STYPE_MATRIX=7`, `STYPE_EQUATE_EXT=8`, `STYPE_EVENT=9`, `STYPE_STRUCTURE=10`, `STYPE_TASK=11`

### Block Classes (`blk_class`)
`BCLASS_PROGRAM=1`, `BCLASS_FUNCTION=2`, `BCLASS_PROCEDURE=3` (also STRUCTURE templates), `BCLASS_TASK=4`, `BCLASS_COMPOOL=5`, `BCLASS_CLOSE=6`

### Special Structures
- **STRUCTURE template**: `blk_class=3`; header symbol has `sym_class=3`, `sym_type=10`, `rows=own symb_no`
- **STRUCTURE COPY**: SDC carries a 2-byte STRCDATA block at `struct_of` offset; `copy_blk_no` = source block number
- **ARRAY variable**: SDC has ARRADATA at `array_off` offset (8 bytes: `arraynum(H) + range1(H) + range2(H) + range3(H)`)
- **SDC fixed layout**: 24 bytes fixed, then name continuation (bytes 24–47), then optional ARRADATA, then optional STRCDATA

## Test Status

C: 67 read tests + 93 write tests = 160 passing, zero warnings. Python: 103 tests passing. `sdfcheck` reports GOOD on `sample.sdf`.

## Known Bugs Fixed (History)

The `chatSummary.md` file documents the full history of bugs fixed during this project. Key non-obvious invariants:
- Symbol binary search uses 8-char compare only for lo/hi adjustment; `chk_match` is called only in linear search after an 8-char match.
- `name_cont` in SDC is at byte offset 24 (not 23).
- Extent cell `fst_off`/`lst_off` are absolute byte offsets from the page start.
- Symbol sort order: block-first, then alphabetically within block (not a global alphabetical sort).
- `chk_match` class logic: classes 1–3 accepted unconditionally; classes > 3 only when `flag1 & 0x03` has exactly one bit set.
