DEBUG `E `F

D INCLUDE TEMPLATE CPL189 # For IMU_DATA

 TP: PROGRAM;
    DECLARE IMU_DATA-STRUCTURE, A NAME, 
                                B INITIAL(1, 2, 3, 4, 5, 6, BIN'010101111',
                                          3#TRUE, BIN'0', 12);
    
    NAME(A) = NAME(B);
 CLOSE TP;
 