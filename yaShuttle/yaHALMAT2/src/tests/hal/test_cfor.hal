 TEST_CFOR:
 PROGRAM;
    DECLARE INTEGER, I1;
    DECLARE SCALAR, S1 INITIAL(0.0);
    DECLARE INTEGER, LASTI INITIAL(0);
    DO FOR I1 = 1 TO 10 WHILE S1 < 5.0;
       S1 = S1 + 1.0;
       LASTI = I1;
    END;
    WRITE(6) 'LASTI=', LASTI;
 CLOSE TEST_CFOR;
