 TEST_SUBBIT_SCALAR:
 PROGRAM;
    DECLARE BIT(32), B2;
    DECLARE SCALAR, S1 INITIAL(2.5), S2;
    B2 = SUBBIT(S1);
    WRITE(6) INTEGER(B2);
    SUBBIT(S1) = B2;
    WRITE(6) S1;
    SUBBIT(S2) = B2;
    WRITE(6) S2;
 CLOSE TEST_SUBBIT_SCALAR;
