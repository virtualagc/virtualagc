 TEST_SCHED:
 PROGRAM;
    WORKER:
    TASK;
       WRITE(6) 'WORKER RUNNING';
    CLOSE WORKER;

    WRITE(6) 'BEFORE SCHEDULE';
    SCHEDULE WORKER PRIORITY(60) DEPENDENT;
    WRITE(6) 'AFTER SCHEDULE';
 CLOSE TEST_SCHED;
