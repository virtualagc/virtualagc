 TEST_WAIT_DEPENDENT:
 PROGRAM;
    WORKER:
    TASK;
       WAIT 1;
       WRITE(6) 'WORKER DONE';
    CLOSE WORKER;

    SCHEDULE WORKER PRIORITY(50) DEPENDENT;
    WAIT FOR DEPENDENT;
    WRITE(6) 'PRIMAL DONE';
 CLOSE TEST_WAIT_DEPENDENT;
