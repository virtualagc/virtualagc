 TEST_VEC_ATPARTITION:
 PROGRAM;
    DECLARE V VECTOR(3), V2 VECTOR(2);
    DECLARE SCALAR, S1;
    V = VECTOR(1, 2, 3);
    V2 = V$(2 AT 1);
    WRITE(6) V2;
    S1 = ABVAL(V$(2 AT 2));
    WRITE(6) S1;
 CLOSE TEST_VEC_ATPARTITION;
