 TEST_READ_VECMAT_EDGE:
 PROGRAM;
    DECLARE SCALAR, A, B;
    DECLARE V VECTOR INITIAL(-9.5, -9.5, -9.5);
    A = -7.5; B = -7.5;
    READ(5) A, V, B;
    WRITE(6) A;
    WRITE(6) V;
    WRITE(6) B;
 CLOSE TEST_READ_VECMAT_EDGE;
