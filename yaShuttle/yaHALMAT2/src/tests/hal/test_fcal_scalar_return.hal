 TEST_FCAL_SCALAR_RETURN:
 PROGRAM;
    QUARTER:
    FUNCTION(X) SCALAR;
       DECLARE X SCALAR;
       RETURN X / 4;
    CLOSE QUARTER;

    WRITE(6) QUARTER(5.0);
    WRITE(6) QUARTER(13.0);
 CLOSE TEST_FCAL_SCALAR_RETURN;
