 TEST_DSQRT_PRECISION:
 PROGRAM;
    DECLARE RMS SCALAR;
    DECLARE TOTAL SCALAR DOUBLE INITIAL(338350.0);
    DECLARE COUNT INTEGER CONSTANT(100);
    RMS = SQRT(TOTAL/COUNT);
    WRITE(6) RMS;
 CLOSE TEST_DSQRT_PRECISION;
