 TEST_SUBBIT:
 PROGRAM;
    DECLARE BIT(8), B0 INITIAL(BIN'00000101'), B4;
    DECLARE INTEGER, I1 INITIAL(42);
    DECLARE BIT(16), B1;
    B4 = BIT(B0);
    WRITE(6) INTEGER(B4);
    B1 = SUBBIT(I1);
    WRITE(6) INTEGER(B1);
 CLOSE TEST_SUBBIT;
