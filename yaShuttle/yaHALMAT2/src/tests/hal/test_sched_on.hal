 TEST_SCHED_ON:
 PROGRAM;
    DECLARE EV1 EVENT;
    WORKER:
    TASK;
       WRITE(6) 'WORKER RUNNING';
    CLOSE WORKER;

    WRITE(6) 'BEFORE SCHEDULE';
    SCHEDULE WORKER ON EV1 PRIORITY(60);
    WRITE(6) 'BEFORE SIGNAL';
    SIGNAL EV1;
    WRITE(6) 'AFTER SIGNAL';
 CLOSE TEST_SCHED_ON;
