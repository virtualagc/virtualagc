 TEST_SCHED_EVERY_WAIT:
 PROGRAM;
    DECLARE INTEGER, N INITIAL(0);
    WORKER:
    TASK;
       WAIT(300);
       N = N + 1;
    CLOSE WORKER;

    SCHEDULE WORKER PRIORITY(60), REPEAT EVERY 1000.0 UNTIL 3500.0;
    WAIT(10000);
    WRITE(6) 'N=', N;
 CLOSE TEST_SCHED_EVERY_WAIT;
