 TEST_SCHED_STOPPING_ONLY:
 PROGRAM;
    DECLARE EV1 EVENT;
    DECLARE INTEGER, N INITIAL(0);
    WORKER:
    TASK;
       N = N + 1;
    CLOSE WORKER;

    SCHEDULE WORKER PRIORITY(50) WHILE EV1;
    WAIT(50);
    WRITE(6) 'N=', N;
 CLOSE TEST_SCHED_STOPPING_ONLY;
