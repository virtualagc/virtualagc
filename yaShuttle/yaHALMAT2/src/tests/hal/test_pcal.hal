 TEST_PCAL:
 PROGRAM;
    DECLARE INTEGER, N, RESULT INITIAL(0);

    ADD_TO_RESULT:
    PROCEDURE(N);
       DECLARE INTEGER, N;
       RESULT = RESULT + N;
    CLOSE ADD_TO_RESULT;

    CALL ADD_TO_RESULT(5);
    CALL ADD_TO_RESULT(10);
    WRITE(6) 'RESULT=', RESULT;
 CLOSE TEST_PCAL;
