 TEST_EXIT_LOOP:
 PROGRAM;
    DECLARE INTEGER, I1 INITIAL(0);
    LOOP: DO WHILE TRUE;
        I1 = I1 + 1;
        IF I1 = 3 THEN EXIT LOOP;
    END;
    WRITE(6) 'DONE', I1;
 CLOSE TEST_EXIT_LOOP;
