 TEST_WRITE_BIT_ARRAY:
 PROGRAM;
    DECLARE FLAGS ARRAY(4) BOOLEAN INITIAL(FALSE, TRUE, FALSE, TRUE);
    DECLARE AVERAGE SCALAR INITIAL(3.5);
    WRITE(6) AVERAGE, FLAGS;
 CLOSE TEST_WRITE_BIT_ARRAY;
