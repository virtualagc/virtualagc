 TEST_SCHED_IN:
 PROGRAM;
    WORKER:
    TASK;
       WRITE(6) 'WORKER RUNNING';
    CLOSE WORKER;

    WRITE(6) 'BEFORE SCHEDULE';
    WAIT(10);
    SCHEDULE WORKER IN 5.0 PRIORITY(60);
    WAIT(30);
    WRITE(6) 'AFTER SCHEDULE';
 CLOSE TEST_SCHED_IN;
