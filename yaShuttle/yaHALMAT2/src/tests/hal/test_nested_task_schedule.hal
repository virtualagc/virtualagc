 NESTED_TASK_SCHEDULE_TEST: PROGRAM;
    DECLARE COUNT INTEGER INITIAL(0);

    NEST: TASK;
        COUNT=COUNT+1;
        WRITE(6) COUNT;
        SCHEDULE NEST IN 1.0 PRIORITY(80);
    CLOSE NEST;

    SCHEDULE NEST PRIORITY(80);
    WAIT 3.5;
 CLOSE NESTED_TASK_SCHEDULE_TEST;
