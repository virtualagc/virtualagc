 COUNTUP2: PROGRAM;
    DECLARE I INTEGER INITIAL(1);

    NEXT: TASK;
       WRITE(6) I;
       I = I + 1;
       IF I > 10 THEN
          CANCEL NEXT;
    CLOSE NEXT;

    SCHEDULE NEXT PRIORITY(80) DEPENDENT, REPEAT EVERY 1.0;
 CLOSE COUNTUP2;
