 TEST_PRIO:
 PROGRAM;
    WORKER:
    TASK;
       WRITE(6) 'WORKER RUNNING';
    CLOSE WORKER;

    WRITE(6) 'BEFORE SCHEDULE';
    SCHEDULE WORKER PRIORITY(30) DEPENDENT;
    WRITE(6) 'BEFORE UPDATE';
    UPDATE PRIORITY WORKER TO 90;
    WRITE(6) 'AFTER UPDATE';
 CLOSE TEST_PRIO;
