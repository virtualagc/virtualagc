 TEST_VCRS_PRECISION:
 PROGRAM;
    DECLARE V_PRIME VECTOR INITIAL(14, 32, 50);
    DECLARE E VECTOR INITIAL(3, 2, 1);
    DECLARE RESULT2 VECTOR;
    RESULT2 = V_PRIME * E;
    WRITE(6) RESULT2;
 CLOSE TEST_VCRS_PRECISION;
