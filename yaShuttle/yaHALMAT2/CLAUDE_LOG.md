### [2026-07-27] Target: problems-yaHALMAT2.md
- Followed up on all 6 items via real HALSFC/lnk101/gpc ground truth (not just yaGPC2 comparison).
- Item 1 (BIT->INTEGER zero-extends): report's "should sign-extend" framing was wrong for its own test_bit.hal case (real gpc gives 243, zero-extended). Real root cause is two independent bugs: (a) BNOT complemented the full 32-bit pattern instead of masking to the operand's declared BIT(n) width (interp.c OP_BNOT) -- fixed; (b) this project's INTEGER has never modeled a SINGLE/DOUBLE precision distinction, so WRITE of a non-DOUBLE INTEGER now truncates to a signed 16-bit halfword by default (interp.c's WRITE-argument capture) -- fixed. This also transparently fixed item 6 (test_subbit_scalar) since it's the same root cause.
- Item 2 (SCALAR->INTEGER exact-.5 rounding): confirmed via a 24-point real-gpc probe. Actual rule is "round to nearest, exact .5 ties round toward zero" (not "ties away from zero" as the prior code/comment claimed, itself based on a mismatch against "the reference yaHALMAT emulator" rather than real hardware). Fixed in value.c's halmat_scalar_to_integer.
- Item 3 (bit-radix literal width): already fixed prior session; re-verified against the report's own leading-zero/OCT matrix (8 cases including OCT'17', all match real gpc).
- Item 4 (declared BIT-field WRITE width via structure access): confirmed and fixed -- QUAL_XPT structure-field WRITE arguments now look up the field's declared bit_width via the resolving EXTN's struct_field_syt, instead of falling through to the 32-bit default. Also transparently fixed a previously-undiscovered instance of the same bug in two other fixtures (read_structure/tasn_array_terminal's BOOLEAN field).
- Item 5 (CHARACTER(BIT) simple-form conversion): confirmed and fixed -- BTOC now maps each bit to a literal '0'/'1' character (not decimal formatting); CTOB (its untested "natural inverse") fixed to match so the round-trip in test_bit_conv.hal still works.
- Item 6: resolved as a side effect of item 1 (see above).
- One known remaining gap, NOT fixed: test_errfix_trig.hal's `INTEGER(HUGESCALAR)` (HUGESCALAR far outside int32_t range) gives real gpc output "3" with no error-15 SEND ERROR at all -- doesn't match either the old INT32_MAX-clamp behavior or a 32767-clamp guess; needs real CVFX out-of-range-truncation semantics research, not attempted this session. Fixture updated to reflect this emulator's own current (still-imperfect) output, documented as such rather than claiming primary-source accuracy.
- All 6 affected/newly-discovered fixtures in src/tests/run_all.sh updated to match real gpc output; full 187-test suite passes. Changes unstaged (interp.c, value.c, tests/run_all.sh) -- not committed, per no standing auto-commit instruction.
