### [2026-07-23] Target: reengineered-documentation/class-6/IASN.md
- User-reported (039-CORNERS.hal, `AB = 0;`, AB a VECTOR(2)): IASN's known "whole-number literal source" leak (already documented as affecting SCALAR receivers) also fires for VECTOR/MATRIX receivers, since PASS1 folds `0` into a plain IASN with the whole VECTOR/MATRIX SYT as receiver rather than VASN/MASN, no ADLP wrapping. Confirmed via real compiled HALMAT: `HALMAT #60 IASN` with operand 2 = Symbol AB (VECTOR). Previously fatal: `write_destination`'s whole-array QUAL_SYT branch unconditionally required `arrayed_index >= 0`. Fixed per [USA003087] Sec. 8.2 rule 3 (MATRIX)/rule 3 (VECTOR): literal INTEGER 0 assigned to a whole MATRIX/VECTOR receiver "creates a null matrix"/"creates a null vector" (zeros every element) — any other value is illegal HAL/S and never expected to reach the interpreter. New `syt_is_vector_or_matrix_shaped()` helper (excludes ARRAY, which has no documented equivalent) gates a zero-fill loop in `write_destination` (interp.c) before the existing arrayed-paragraph-replay fail path. Regression fixture: `src/tests/hal/test_vecmat_null_assign.hal`, wired into `run_all.sh`; confirmed to fail on pre-fix code with the exact user-reported error, and confirmed against a real `039-CORNERS.hal` run with the user's reported input (`1 2,0.2`).

### [2026-07-23] Target: reengineered-documentation/STATUS.md
- Same fix as above (IASN.md target) — add a Class 6 / assignment-statements STATUS.md entry cross-referencing IASN.md's writeup, following this project's usual STATUS.md log-entry style (see existing "READ semicolon list-terminator" / "READ missing default line-advance between statements" entries for the pattern to match).

### [2026-07-23] Target: reengineered-documentation/class-2/STOC.md
- User-reported: a SCALAR/SCALAR DOUBLE value of exactly 0.0, whether via WRITE output or STOC (scalar-to-character conversion), was being rendered in the same zero-padded scientific-notation form as any nonzero value (e.g. " 0.0000000E+00") instead of the documented special case. [USA003087] Appendix F ("Scalar Type"): "If the value is exactly zero, it is represented as 0.0" — independently confirmed by Figure 12-3's own worked WRITE example, a 3x3 matrix with a genuine 0.0 element rendered as bare "0.0" beside full-width fields like "5.0000000E-01". This directly **reverses** this file's own existing "Confirmed Output Format" conclusion ("0.0 is not a special case at all... gets the ordinary... full fixed-digit-count scientific notation") — that conclusion was reached from [USA003090]'s apparent silence on the zero case, but was written before `USA003090.txt` was actually available in this project to check directly (says so in its own text). Now directly read: [USA003090] Sec. 6.1.3/Sec. 8.2 rules 13-14 only describe the general *nonzero* field layout and don't address the zero case at all, so they don't actually contradict [USA003087]'s explicit rule — no real primary-source conflict, just an earlier unverified assumption. Fixed in `halmat_scalar_format()` (value.c): a zero value now formats as `" 0.0"` left-justified and blank-padded (not zero-padded) to the same fixed field width as any other value (14 single-precision/23 double-precision, decimal point aligned to the same column nonzero values use, i.e. one leading blank). This file's existing "zero-value edge case"/"Confirmed Output Format" section needs its conclusion corrected (and its own inconsistent worked example, `" 0.00000000E+00"`, was already flagged elsewhere in the file as suspect on unrelated digit-count grounds — that separate 7-vs-8-fractional-digit question is unaffected by this fix and remains resolved as previously noted, 7 fractional digits single-precision). Regression coverage: this session's global correction to every existing fixture's expected zero-value fields in `run_all.sh` (88 occurrences) already exercises this; confirmed against a real `039-CORNERS.hal` run (`AB = 0;`, a VECTOR(2), now prints `" 0.0"` per element instead of `" 0.0000000E+00"`).

### [2026-07-23] Target: Maintenance.md
- User-provided tooling note, add to the "Where things live"/tooling section: a locally-installed `compileLinkRun --filename=FILENAME.hal` tool compiles FILENAME.hal with both HALSFC and HAL_S_FC.py, links the AP-101S object code, and runs it in an AP-101S emulator; because HAL_S_FC.py has run, the same compiled program can immediately also be run through yaHALMAT2 via `yaHALMAT2 @CURRENT`, giving a second independent ground truth (real AP-101S machine-code execution) beyond `unHALMAT.py` static inspection and the `Halmat/` reference OCaml emulator. This is how the user found the SCALAR/SCALAR DOUBLE 0.0 formatting bug logged just above. User's explicit guidance: don't run this routinely (extra time cost, and the AP-101S emulator itself is neither feature-complete nor bug-free, so a mismatch isn't automatically yaHALMAT2's bug) — reach for it only when a specific behavior is suspicious enough to warrant an independent cross-check.

### [2026-07-23] Target: reengineered-documentation/class-0/READ.md
- User-reported (044-ORTHONORMAL.hal's `READ(5) X;`, X a VECTOR(3)): READ against a whole VECTOR/MATRIX destination failed outright with "only CHARACTER/SCALAR/INTEGER arguments are implemented (got HALMAT class 4)". User correctly pointed out the documentation treats multi-element READ targets the same "unrolled" way as WRITE output/INITIAL clauses, before any investigation — confirmed exactly right. [USA003087] Sec. 12.3: "Multi-valued data items cause a series of data fields to be read sequentially... [a] vector data item causes one data field per vector element to be read... [a] matrix... row by row" — the identical per-element order already used for WRITE output (READ.md's existing writeup) and INITIAL (STRI/SLRI machinery elsewhere). [XXAR](class-0/XXAR.md) already documented (from an earlier session) that a whole VECTOR(4)/MATRIX(3) READ argument compiles as a single unreplayed XXAR (`QUAL`=SYT, `TAG1`=the class number, no ADLP/DLPE wrapping) — the same shape already handled for WRITE/CALL whole-container arguments — but the runtime side of that finding was never implemented for READ specifically until now. Fixed: `OP_XXAR`'s READ-destination branch now recognizes `TAG1`∈{3,4} with `state->arrayed_index < 0` as a whole-container destination (new `dest_is_container` flag, state.h) instead of failing; `OP_READ` unrolls it into one field read per element (`ensure_container` + direct `e->elements[k]` writes, `element_count` reads in sequence), sharing the exact same null-field/semicolon-terminate rules as any other item — including across item boundaries (a container sandwiched between two ordinary scalar items in the same READ statement needed `read_skip_separator`'s `require_separator` threading changed from "item index > 0" to "has any field of the whole statement been read yet," since a single item can now expand into several fields). ARRAY needed no equivalent change — confirmed (XXAR.md) it stays ADLP/DLPE-replayed even when read whole, so each element already arrives as its own ordinary-shaped item via the existing per-element replay path. Regression fixtures: `src/tests/hal/test_read_vecmat.hal` (plain VECTOR+MATRIX read/write round trip) and `test_read_vecmat_edge.hal` (scalar-container-scalar sequencing plus semicolon-mid-container termination), both wired into `run_all.sh` and confirmed to fail on pre-fix code with the exact user-reported error; also confirmed against a real `044-ORTHONORMAL.hal` run (its `READ(5) X/Y/Z;` statements now succeed -- the program goes on to fail later at an unrelated, pre-existing gap, `MATRIX(X, Y, Z)` list-form shaping functions not yet implemented, out of scope here).

### [2026-07-23] Target: reengineered-documentation/class-0/XXAR.md
- Cross-reference the READ.md fix just above from this file's existing "Whole VECTOR/MATRIX/ARRAY argument" section (which already documented the compiled HALMAT shape for `READ(5) V1;`/`READ(5) M1;` but not that yaHALMAT2's interpreter now actually implements reading into it) and its "TAG1 type-code enumeration" section. No new primary-source finding here, just linking the already-confirmed compiled shape to its now-implemented runtime behavior.

### [2026-07-23] Target: reengineered-documentation/STATUS.md
- Same fix as the READ.md/XXAR.md entries above — add a Class 0 / READ-statement STATUS.md entry cross-referencing both, following this project's usual STATUS.md log-entry style (see existing "READ semicolon list-terminator" / "READ missing default line-advance between statements" entries for the pattern to match).

### [2026-07-23] Target: reengineered-documentation/class-0/MSHP.md
- User-asked follow-up ("fix the pre-existing gap you mentioned" — 044-ORTHONORMAL.hal's `MATRIX(X, Y, Z)`/`MATRIX(A1, A2, A3)`, previously a hard "not yet implemented" stub for MSHP/SSHP/ISHP together). This file's *existing* model needs substantive correction, not just a "now implemented" note: its "Behavioral Description"/"Unresolved Questions" currently claim `MATRIX(...)`'s arguments are "themselves full VECTORs, one per row" as the *only* form, explicitly setting aside the flat-scalar-list hypothesis as probably-wrong (citing earlier QD1/AV2 compile errors). That row-vector-only model is incomplete: [USA003087] §7.6 ("MATRIX CONVERSION", not previously found/extracted into this project's citation set) and [USA003088] §6.6 ("General <arith conversion>", the formal grammar/semantic-rule version) both confirm the *real*, broader rule — `MATRIX(exp1,exp2,...)` is a flat list where "[t]he data elements in each <expression> are unraveled in their natural sequence" and then "reraveled" into the result shape; a plain scalar/integer expression contributes one element, but a whole VECTOR/MATRIX expression contributes its own elements (`MATRIX` "may have arguments of integer, scalar, vector, and matrix types" — [USA003088] §6.6 semantic rule 5). Confirmed empirically this session: `MATRIX(1,2,3,4,5,6,7,8,9)` (9 flat SCALAR-literal SFARs) and `MATRIX(X,Y,Z)` (3 whole-VECTOR SFARs, X/Y/Z each VECTOR(3)) compile to the **identical** MSHP operand value (decimal 771 = 0x0303), settling the file's own previously-unresolved question. Also newly decoded: that operand is high-byte=rows/low-byte=cols (0x03,0x03 → 3,3) — [USA003088] §6.6 semantic rule 1's documented unsubscripted default ("VECTOR produces a single 3-vector...MATRIX produces a single 3-by-3 matrix... in the absence of a subscript"), not an opaque/undecoded value as the file currently states. Still unresolved: the explicit `MATRIXm,n(...)`/`MATRIX m,n (...)` subscript form's real HAL/S-FC source syntax — several plausible spellings from [USA003088]'s own typeset figures (Figure 6-19's "INTEGER 2,2 (4#I + J)" convention) were tried against the real compiler this session and all rejected (`MATRIX2,3(...)` → "undeclared identifier MATRIX2"; `MATRIX 2,3 (...)` → "symbol 2 syntactically illegal") — so the rows/cols decode above is confirmed only for the one default-3×3 data point, not independently verified for a non-default shape. Fixed in `interp.c`: new shared `unravel_shaping_argument()` helper (handles both plain-scalar and whole-container SFAR arguments) used by OP_MSHP (reraveled 2-D via the decoded rows/cols) and also OP_VSHP/OP_SSHP/OP_ISHP (reraveled flat 1-D) — those three had the identical latent gap (only ever handled plain-scalar arguments, silently wrong for a whole-VECTOR/MATRIX argument, per the same §6.6 rules applying to them too), caught while generalizing MSHP rather than user-reported directly. Regression fixture: `src/tests/hal/test_mshp.hal` (both the row-vector and flat-scalar forms, into the same 3×3 result), wired into `run_all.sh`; confirmed to fail on pre-session code with the original "not yet implemented" message, and confirmed against a real `044-ORTHONORMAL.hal` run (two different bases, including a non-trivial/non-identity one) producing correct DETERMINANT/basis-vector/change-of-basis output.

### [2026-07-23] Target: reengineered-documentation/class-0/VSHP.md, SSHP.md, ISHP.md
- Cross-reference the MSHP.md entry above: all three now share `unravel_shaping_argument()` with MSHP, so a whole-VECTOR/MATRIX SFAR argument (not just plain scalars) is handled correctly per [USA003088] §6.6's general rules, not just MSHP. `VSHP.md`'s existing comment ("each pending SFAR argument resolves as a plain SCALAR (unlike MSHP...)") is now inaccurate and should be corrected to describe the shared unraveling behavior.

### [2026-07-23] Target: reengineered-documentation/HALMAT.md
- Add [USA003087] §7.6 ("VECTOR CONVERSION"/"MATRIX CONVERSION") and [USA003088] §6.6 ("General <arith conversion>") to the citation registry — both newly consulted this session, neither previously cross-referenced from HALMAT.md's References table for the VSHP/SSHP/ISHP/MSHP family.

### [2026-07-23] Target: reengineered-documentation/class-5/SASN.md, class-6/IASN.md
- Found (not user-reported directly — found while regression-testing the MSHP fix above, then fixed per direct user instruction "fix that too, and anything else you encounter along the way"): `SA = SCALAR(S1, S2);`/`IA = INTEGER(I1, I2);` (SA/IA a SCALAR/INTEGER `ARRAY(2)`) silently wrote 0 into every element instead of the real values, no error. Both files should document a new confirmed SASN/IASN usage: assigning a whole-container shaping-function (or other VAC-container-producing opcode's) result into a whole SCALAR/INTEGER `ARRAY` destination. Unlike VECTOR/MATRIX (which get their own dedicated whole-container assign opcodes, VASN/MASN), `ARRAY` has none — HALSFC instead emits the *ordinary* single-value SASN/IASN wrapped in an ADLP/DLPE replay, re-executing that same instruction once per array element (confirmed via a debug trace this session: two real `write_destination` calls for the same destination SYT, `arrayed_index` correctly cycling 0,1 each time) — a linear HALMAT listing makes the ADLP/DLPE bracket look *empty* (nothing textually between it and its DLPE), which is misleading; it's actually replaying the *preceding* SASN/IASN, not literally wrapping forward instructions the way every other confirmed ADLP/DLPE case in this project does. Root cause was on the read side, not the replay mechanism (which already worked correctly): `resolve_operand`'s `QUAL_VAC` case (`interp.c`) never checked `slot->is_container` at all, so a shaping-function result read this way fell through to the plain-INTEGER default branch, silently returning a stale zero (`state->vac[...].integer`, never set by `store_container_result`) instead of failing or reading the right element. Fixed by adding an `is_container` branch that indexes the container by `arrayed_index` (mod element count) when inside a replay, and fails loudly (rather than guessing an element) if reached outside one. Regression fixture: `src/tests/hal/test_sshp_ishp.hal`, wired into `run_all.sh`; confirmed to fail on pre-fix code (silent zeros, no error) and confirmed correct after (`SA`=(1.5,2.5), `IA`=(10,20)).

### [2026-07-23] Target: reengineered-documentation/class-0/ADLP.md, DLPE.md
- Cross-reference the SASN.md/IASN.md entry above: add a note that a *linear* HALMAT listing can make an ADLP/DLPE bracket look empty (nothing textually between ADLP and its matching DLPE) when it's actually replaying the single instruction *immediately preceding* the ADLP, not wrapping anything forward — confirmed this session via the whole-SCALAR/INTEGER-`ARRAY`-assignment case (SASN.md/IASN.md). Worth flagging explicitly since every other ADLP/DLPE case previously documented in this project wraps forward instructions, which could otherwise read as the general rule.
