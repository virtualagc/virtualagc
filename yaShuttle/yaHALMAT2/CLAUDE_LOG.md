### [2026-07-25] Target: class-0/DSUB.md, class-0/RTRN.md, class-0/LFNC.md, STATUS.md
- User-reported (140-STATISTICS.hal's `CALL STATISTICS(DATA) ASSIGN(LO, HI, MN);`, STATISTICS's own `DATA` parameter declared `ARRAY(*) SCALAR` — USA003087 Sec. 7.5/20.11's assumed-size parameter): "procedure/function call: MATRIX/VECTOR/ARRAY argument shape does not match parameter 7" (task #29, previously deferred alongside task #16). Two compounding bugs. (1) `symtab.c`'s own `EXT_ARRAY` dimension-bound parsing never sign-extended from the field's real 16-bit width (confirmed via the raw `COMMON0.out` dump: "EXTuARRAY n BIT xxxx", always 4 hex digits) — an assumed-size parameter's own bound is encoded as a *negative* 16-bit sentinel (0xFFF9 = -7 for `140-STATISTICS.hal`'s `DATA`; cross-checked against `141-VSUM.hal`'s own `ARRAY(*) VECTOR` case, which uses a *different* magnitude, 0xFFFC = -4, confirming "any negative value" rather than one fixed constant), previously misread as a huge unsigned "size" (65529) instead. (2) Even with that fixed, `ensure_container()` had no way to represent "this parameter's real size isn't known until a call actually supplies one" — fixed by having it leave an assumed-size `ARRAY` parameter entirely unallocated, and `bind_call_argument()` allocate it directly from the caller's own actual argument shape instead of requiring a match against a declared size that doesn't exist for this kind of parameter. Output independently hand-verified (LO=MIN=10, HI=MAX=50, MEAN=SUM/SIZE=150/5=30, from `DATA`'s own `INITIAL(10,20,30,40,50)`). Fixture: `test_statistics.hal`.
- Separately confirmed in the same investigation, a different, pre-existing gap unrelated to assumed-size specifically: `141-VSUM.hal`'s own `ARRAY(*) VECTOR` case no longer crashed with the above fix, but computed a wrong answer (3x too large, `SUM=3,6,9` instead of the hand-derivable `1,2,3`). Root cause: `LFNC`'s `SIZE` selector (`interp.c`) returned an `array_of_vector`-shaped argument's *flat scalar* count (9) rather than the "length of array" USA003087 Appendix B's own SIZE FUNCTION table specifies (3, the `VECTOR` count) — inflating a `DO FOR N=1 TO SIZE(V)` loop bound 3x, silently re-summing the same 3 vectors 3 times over. **User-instructed follow-up, now fixed**: `SIZE` now uses `rows` (the array's own length, from `resolve_container`'s own rows/cols output) instead of the flat count whenever a 2D shape (`rows>0 && cols>0`) is reported — unambiguous in practice, since a genuinely 2-dimensional `ARRAY(r,c)` of `SCALAR` shares that same `rows`/`cols` encoding but isn't "one-dimensional," so `SIZE()` on one isn't valid HAL/S to begin with. A plain flat `ARRAY` (not array-of-`VECTOR`) is unaffected. Reviewed `MAX`/`MIN`/`SUM`/`PROD` for the same class of bug: none found, since USA003087 restricts those to `INTEGER`/`SCALAR` array arguments only. Fixture: `test_vsum.hal`.

### [2026-07-25] Target: class-0/DSUB.md, class-1/BAND.md, class-8/TINT.md, class-0/TSUB.md, class-0/WAIT.md, class-0/NASN.md, SAMPLES_CHECKED.md
- User-instructed: tested every "Programming in HAL/S" corpus sample not already reported as problematic, fixed every genuine yaHALMAT2 bug found (commit `5c8a26e70`), and corrected 5 corpus files' header comments claiming array brackets are illegal HAL/S syntax (commit `e2fe703e9`, they demonstrably compile fine). Twelve distinct interpreter bugs fixed, one fixture each — see `5c8a26e70`'s own commit message for the full itemized list (DSUB VECTOR/BIT/CHARACTER at-partition & single-index confusion; OP_READ/RDAL arrayed_index restoration; `bind_call_argument`'s missing BIT/CHARACTER cases; `store_resolved_to_vac`'s RV_STRING use-after-free; `SYT_TYPE_UNKNOWN` default-type misread, including EVENT symbols; a new deferred `is_bitpart_ref` VAC-slot kind for BIT at-partition/index write-through; `write_destination`'s missing `is_container_ref` case for plain SASN/IASN row-select receivers; `WAIT FOR <event>`; TINT's NULL-terminal case; TSUB's variable-copy-index case). Full regression suite (178 fixtures) passes.
- Investigated but NOT fixed, each with a documented reason (see this session's own task-tracking, not reproduced here): (1) `264-INITIALIZE.hal`/`262-TEST7.hal` don't actually compile with the current `HALSFC` build at all — both need multi-file `INCLUDE TEMPLATE`/COMPOOL prerequisites that the installed tooling has no working `--templib` mechanism for (confirmed via `--help`); a speculative `NASN` fix (accepting a `QUAL_XPT` receiver for a bare NAME-typed structure reference) was implemented then reverted since it couldn't be verified against real compiled bytecode. (2) `172-OUTER.hal` needs whole-STRUCTURE `READ` support (new feature work, blocked on the same "no array-shaped structure-terminal write path exists yet" gap already noted for `TASN`). (3) `239-STARTUP.hal`/`238-P.hal`/(by extension) `WAIT FOR`'s own compound form need `SCHEDULE`/`WAIT` to support compound `BAND`/`BOR` event-expressions, not just a plain EVENT symbol — real feature work, deferred. (4) `193-TEST_X.hal` needs `ON ERROR`'s registered recovery handler to actually intercept a `READ` end-of-file condition and redirect execution instead of aborting — confirmed this doesn't happen yet; deferred, needs USA003090 Appendix C's system error-group table cross-referenced first. (5) `194-TEST_X.hal`, `265-ENQUEUE.hal`/`269-STALL.hal`, and parts of `180/184-EXAMPLE_N.hal`/`264-INITIALIZE.hal` are confirmed NOT bugs — illustrative textbook snippets whose real compiled bodies are genuinely empty/elided ("...") or intentionally out-of-spec (e.g. `241-P.hal`'s `PRIORITY(999)`, exceeding USA003087's own documented `0 < P < 255` range), not meant to run to completion standalone. (6) `167-ASSORTEDIO.hal` additionally uses a `%SVC` macro invocation, a permanent, deliberate "out of scope" limitation (raw AP-101S machine code) unrelated to the TINT bug that was fixed.

### [2026-07-25] Target: class-0/SCHD.md, class-0/WAIT.md, class-0/ERON.md, class-0/TASN.md, class-0/EXTN.md, class-0/DSUB.md, SAMPLES_CHECKED.md
- User-instructed follow-up ("implement the deferred items"): closed out 4 of the 5 remaining deferred gaps from the prior sweep, each fixed/verified/committed separately. (1) Compound `BAND`/`BOR`/`BNOT` event-expressions in `SCHEDULE ON`/`STOPPING WHILE`/`UNTIL` and `WAIT FOR` (239-STARTUP.hal, 238-P.hal) — new `reevaluate_live_bit_operand()` re-evaluates the expression live against current EVENT states every time it's consulted (binary-searching `state->prog->instrs[]` by HALMAT word position for a `QUAL_VAC` operand's producing BAND/BOR/BNOT instruction, since that's a *word index*, not the instruction's own array position — the two diverge after the first multi-operand instruction), instead of reading a one-time-captured snapshot; `on_event_syt`/`stop_event_syt` generalized from `uint16_t` to `halmat_operand_t`. Commit `bdc049c43`. (2) `ON ERROR` redirect on `READ` end-of-file (193-TEST_X.hal) — group 10 member 5 ("the end of file error") confirmed directly against `source-documentation/ProgrammingInHALS.txt`'s own Sec. 10 discussion of this exact worked example, doubly confirmed by the real compiled HALMAT showing exactly one `ERON` with group=10/member=5. Commit `eb5a3b1cf`. (3) Whole-structure `READ` arguments, HALMAT class 10 (172-OUTER.hal) — new `symtab.h` `struct_first_field`/`struct_next_field` linked list, parsed from the raw `SYM_LINK1`/`SYM_LINK2` symbol-table fields (confirmed empirically against two different real structure templates' own `COMMON0.out` dumps: template→first-field via LINK1, field→next-field via LINK2, terminating in a value that's negative when reinterpreted as `int16_t`); `OP_READ` walks the chain and writes each terminal into its own shadow field slot, allocating a VECTOR terminal's `elements[]` on first touch — the first mechanism ever to write array-shaped data into a structure-terminal shadow slot. Commit `5d1651a59`. (4) `TASN` copying an ARRAY/MATRIX/VECTOR structure terminal (previously unreachable, per its own comment, until (3) above provided real per-element data to copy) — deep-copy via malloc+memcpy/dup_string rather than aliasing the two entries' element-array pointers together; verified additionally under a dedicated ASan+UBSan build. Commit `d797a3ca0`. All four independently fixture-verified; full regression suite (185 fixtures) passes throughout.
- (5) CSZ (`#`-relative CHARACTER subscript arithmetic, 160-REFORMAT.hal) remains genuinely unresolved (commit `718299064`, documentation only, no code change) — confirmed the LIT feeding the traced `IADD` is exactly 1 (via a standalone litfile.bin probe), reconciling *algebraically* with the corpus file's own documented `#-DECIMALS-1` formula, but reverse-engineering the book's own worked example (`REFORMAT(SQRT(2),3,5)` should yield `'1.414'`) requires the *opposite* sign for the output to come out right. `source-documentation/ProgrammingInHALS.txt` is visibly OCR-corrupted on this exact page, so either the documented formula's own +/- is a misread, or the traced DSUB/IADD pair belongs to a different statement than assumed — genuinely unresolved without a clean source or the real AP-101S object code, deliberately left failing loudly rather than guessing.

### [2026-07-25] Target: class-0/DSUB.md
- CSZ (item (5) above) is now resolved and implemented (commit `4051a91ab`), thanks to the user supplying a local copy of the 1st edition of "Programming in HAL/S" (NASA-CR-151872, Sept. 1978) to cross-check against the 2nd edition's own OCR'd text — page 160 (2nd ed.) corresponds to page 148 (1st ed.) per the user, though the two editions' own internal PDF-reader-page-to-printed-chapter-page offsets differ enough that the REFORMAT text was actually found at 1st-edition reader-page 160, not 148. Both editions show `C(#-DECIMALS-1 TO #)` identically, ruling out an OCR artifact. `DATA`=0=bare `#`, `DATA`=2=`# − subsidiary` (a second operand word immediately following) is now implemented in `interp.c` (`resolve_to_partition_bound()`). The earlier apparent contradiction (item (5)'s own "opposite sign needed") turned out to rest on a wrong assumption: 160-REFORMAT.hal's own corpus file doesn't call `REFORMAT` with the textbook's `SQRT(2)` example at all (it uses 3 different value sets), so that prose claim was never actually checkable against this file's real behavior in the first place. Running the real corpus file also surfaced a second, related gap: `DSUB` rejected a `QUAL_LIT` base operand outright (`ZEROS(1 TO DECIMALS-LENGTH(C))`, `ZEROS` a `CHARACTER CONSTANT`, has no `SYT` storage of its own) — fixed alongside CSZ, same commit. All 3 of 160-REFORMAT.hal's own `WRITE(6) REFORMAT(...)` calls verified digit-by-digit by hand. Fixture: `test_reformat_csz.hal`. `DSUB.md`'s own "Unresolved Questions" section (the `+`-vs-`−` CSZ encoding ambiguity) and its "still isn't handled" to-partition/CSZ status are now outdated and should be updated on the next Full Documentation Sync.
