### [2026-07-27] Target: reengineered-documentation/class-8/TINT.md
- Task #70 (yahalmat2_structure_param_vector_return) resolved a real, previously-open item in TINT.md's own "Unresolved" section: VECTOR structure terminals ARE now handled (not just scalar/integer), confirmed against real gpc via 170-OUTER.hal (`1 V VECTOR, 1 S1 SCALAR, ...` with `INITIAL(0,1,0,0,83,0,OFF)`, one coalesced TINT run of 7 slots for V's 3 components + 4 scalar terminals). Update the "Unresolved (still, after the above)" bullet that currently reads "ARRAY/MATRIX/VECTOR structure terminals ... are not implemented" to say VECTOR is now implemented (via a new tint_locate_slot() slot-to-terminal walk in interp.c, keyed on hal_class==4 && cols>0), MATRIX/ARRAY still open (no confirmed trigger). Also update the main "Operand-Word Format" section's stale claim that only "the plain scalar/integer-terminal case" maps via `TEMPLATE_syt + 1 + OFFSET` -- that formula is now only used when no VECTOR terminal precedes the target slot; the general mapping goes through the new slot-width walk.

### [2026-07-27] Target: reengineered-documentation/class-0/XXAR.md
- Task #63 (function_result_scalar_integer_confusion) discovered and confirmed empirically (127-LIMIT.hal/211-LIMIT.hal, cross-checked with a minimal probe file) a real, previously-undocumented nuance of XXAR's own TAG1 field in a CALL/FUNCTION-invocation argument list specifically (as opposed to the already-documented READ/READALL/WRITE case): TAG1 there is NOT the target parameter's declared type -- it's the *caller's own literal/expression*'s natural HALMAT class, decided independently of the callee. Confirmed by compiling `LIMIT(VALUE, BOUND) SCALAR;` called as both `LIMIT(5.0, 10.0)` (TAG1=6/INTEGER on both XXAR operands, since the literals are whole-number-valued) and `LIMIT(5.5, 10.25)` (TAG1=5/SCALAR, fractional) -- same SCALAR-declared parameters both times, different TAG1 purely from the literal's own value. XXAR.md's current text ("TAG1 is literally the argument's HALMAT class number", documented from READ/WRITE contexts) should get a call-argument-specific caveat: for a CALL argument, TAG1 is the *argument expression's* natural class, not the *parameter's* declared class -- a same-unit call must consult the callee's own symbol table (param_syt's hal_class) to get the parameter's real declared type, which is what bind_call_argument (interp.c) now does via a new SCALAR<->INTEGER cross-coercion. Also consider cross-referencing from FCAL.md/PCAL.md's own argument-binding sections.

### [2026-07-28] Target: reengineered-documentation/class-6/CTOI.md
- Task (DB id 55, ctoi_invalid_digit_substring_wrong_result) empirically resolves CTOI.md's own "Unresolved Questions" bullet ("Exact error behavior for a string that is not in one of the standard input formats... is unconfirmed"): confirmed against real gpc across a 10-case matrix (159-AGE.hal's `X = INTEGER(C(7 TO 10));`) that an invalid string converts to 0, and critically that validation is WHOLE-STRING, not prefix-based -- ANY interior non-blank non-digit character anywhere invalidates the entire conversion (e.g. "7AAA"->0, not 7 the way a strtod()-style partial parse would give), and a LEADING blank before an otherwise-valid digit also invalidates it ("  9 "->0, not 9) -- only TRAILING blanks after a complete number are tolerated ("7   "->7). yaHALMAT2's own OP_CTOI/OP_CTOS previously used plain strtod() (silently prefix-parsing); fixed via a new strict ctoi_parse_scalar() helper in interp.c. This finding was only independently reconfirmed for CTOI/INTEGER specifically -- CTOS/SCALAR is assumed to share it per the doc's own "same parse" framing, not separately verified against real gpc.

### [2026-07-28] Target: reengineered-documentation/class-0/DSUB.md
- Task (DB id 56, wildcard_subscript_matrix_write_loses_row_forcing) found and fixed a real interp.c bug in the asterisk-select DSUB handling's shared store_container_result() tail (previously hardcoded rows=0 for every case, correct for the three genuinely VECTOR-shaped results V$(*)/M$(i,*)/M$(*,j) but wrong for M$(*,*), a genuine 2-D MATRIX result) -- worth a note in DSUB.md's own asterisk-subscript section if/when synced. Also surfaced a genuine, confirmed-both-ways real-hardware distinction worth documenting: M$(*,*) on a plain declared MATRIX variable (SYT base) DOES get flush_write's row-per-line forced-newline WRITE layout (mmwsnp_vector_forces_newline, id 14) when referenced via $(*,*), matching bareword `WRITE(6) X;` -- but the exact same $(*,*) wildcard on a MATRIX *structure-field terminal* (XPT base, e.g. DEMO.hal's `CC$(1;*,*)`, id 49's own fixture) does NOT get row-forcing, staying flat -- both independently confirmed against real gpc. The fix (interp.c) gates the row-forced shape on `base_syt < HALMAT_SYT_MAX` (plain SYT vs. XPT-resolved struct field) to match.

### [2026-07-28] Target: reengineered-documentation (value.c/value.h's own scalar-arithmetic docs, if a broader problems.md-style doc gets synced)
- DB id 51 (yagpc2-side, originally "yaHALMAT2 uses native IEEE double throughout instead of genuine IBM hex float, a real fidelity gap") was based on inferring from the absence of a --hex-float CLI flag, not direct code inspection. Corrected after reading value.c/interp.c directly: halmat_scalar_add/sub/multiply/divide ARE genuine IBM hex-float arithmetic (characteristic/fraction, hex postnormalization), wired into every core SCALAR/MATRIX/VECTOR opcode -- confirmed via call-site inspection, not just the header comment. The real, narrower situation: ~50 call sites (transcendental/special functions -- SIN/LOG/SQRT/ARCTAN/etc., MATRIX inversion, RANDOM, DFOR/CFOR loop control, READ decimal parsing) go through a native-double intermediate, a reasonable choice absent a primary-source hex-float RTL algorithm for most of these (matrix_invert's own comment already self-documents this). DB entry corrected/rescoped rather than treated as a rewrite candidate; worth a note in value.c's own module-level doc or a future problems.md-style summary if one gets synced, since this correction affects how the whole project's floating-point fidelity should be characterized going forward.

### [2026-07-28] Target: reengineered-documentation/class-0/BFNC.md
- Task 108/id 51 (SINH/TANH/ARCCOSH RUNASM ports) found and fixed a real interp.c bug in COSH/SINH's own error-9 overflow threshold: the prior libm-based implementation checked `|x|>175366.0`, but the genuine RUNASM/SINH.asm MAX constant (X'42AF5DC0', hand-decoded) is 175.366, a factor-of-1000 error. Traced to USA003090 Appendix C's own scanned "> 175,366" text (the comma is almost certainly a decimal point mangled by OCR, not a thousands separator) -- BFNC.md's own COSH/SINH entry (line ~183) copied that same "175,366" notation verbatim and should be corrected to "175.366" to match. Harmless under the old libm cosh()/sinh() (graceful overflow-to-inf for any real double), but newly consequential now that hal_cosh_single/hal_sinh_single's own internal ACALL EXP has a real, narrower domain -- confirmed via a real yaGPC2 COSH(175.4) comparison (expected 7.2370051E+75, got garbage before the fix).
