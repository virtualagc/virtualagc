### [2026-07-24] Target: class-6/IASN.md, class-5/SASN.md, STATUS.md
- Implemented multiple-assignment statements (USA003087 Sec. 8.5), user-reported against 104-EXAMPLE_1.hal's `TMAX, TMEAN, TMIN = TIME(1);` ("IASN/SASN: expected 2 operands"). Confirmed via --disasm that multi-assignment compiles to a single IASN/SASN with the source plus every receiver as its own trailing operand (operand_count = 1+N). Fixed by looping over all receivers. Validating against Sec. 8.5's own `C, I = 127.2;` example surfaced a second bug: mixed-type multi-assignment (receivers of different declared types, e.g. CHARACTER + INTEGER) shares one instruction too, and each receiver's own coercion must consult its own symtab class (not the opcode's nominal class) — CHARACTER reuses OP_STOC's formatting. Fixtures: test_multi_assign.hal, test_multi_assign_mixed.hal.

### [2026-07-24] Target: class-0/DSUB.md
- Fixed genuinely 2-dimensional ARRAY(r,c) subscripting (user-reported, 107-EXAMPLE_3.hal: `DECLARE ARRAY(3,3), M1...;`, subscripted as `M1(ROW,COL)`/`M1$(ROW,*)`). ensure_container() only ever read array_dims[0] for any ARRAY shape (symtab.c already parsed every dimension correctly — an interp.c consumption bug), leaving rows/cols at 0. This silently broke the *plain* 2-index case too (placeholder-stride offset instead of real row-major addressing), not just the asterisk-partition WRITE that failed loudly. Fixed by giving a confirmed-2D ARRAY the same rows/cols treatment MATRIX gets, so DSUB's existing MATRIX-shaped logic picks it up for free. Fixture: test_array2d.hal, output independently verified against numpy.

### [2026-07-24] Target: class-6/IASN.md, class-5/SASN.md
- Fixed SCALAR DOUBLE ARRAY elements silently losing precision on write (user-reported, 107-EXAMPLE_4.hal: `ARRAY(5) SCALAR DOUBLE`, rotated via element-to-element DSUB copies — only the one element that happened to pass through an already-normalized plain SCALAR DOUBLE variable printed in double-precision format, the rest single). write_container_element() — the one shared choke point every numeric ARRAY/VECTOR/MATRIX element write funnels through (STRI/SINT's INITIAL() population, DSUB element-to-element copies, ordinary subscripted assignment) — never normalized to the container's own declared SINGLE/DOUBLE precision, unlike the equivalent plain-SCALAR-destination fix already in OP_IASN/OP_SASN. Fixed by applying the same symtab-driven scale_precision() normalization at this one shared site (threading the destination SYT index into the function). Fixture: test_array_double.hal.

### [2026-07-24] Target: class-0/DFOR.md, class-0/EFOR.md, class-0/DSUB.md
- Fixed two bugs found via one user report (113-EXAMPLE_7.hal). (1) Silent data corruption: range-form `DO FOR` never checked whether its *initial* value was already out of range (`DO FOR J=I+1 TO 4;` with I=4 ran once with J=5 anyway), corrupting an unrelated array element via a wrapped out-of-bounds DSUB offset. DFOR.md's own prior "always runs its first cycle without a pre-test" claim was wrong — a real `HALSFC --parms=LSTALL` trace shows DFOR's initial branch actually lands on EFOR's own store+compare code (only the increment is skipped, not the bounds check). Fixed by giving DFOR that same check up front (new dfor_efor_pos[] reverse mapping). Independently confirmed against compileLinkRun. (2) An INTEGER ARRAY's WRITE argument (`MISMATCH$(I,*)`, a confirmed-2D `ARRAY INTEGER`) formatted as SCALAR — container_is_integer was only ever set for a plain whole-SYT reference, and resolve_operand()'s per-element container read had no INTEGER awareness at all. Fixed with a new per-VAC-slot container_is_integer flag sourced from DSUB's own operator-word TAG. Fixtures: test_dfor_zero.hal, test_mismatch_array.hal.

### [2026-07-24] Target: class-0/DSUB.md, class-3/MASN.md, class-4/VASN.md, STATUS.md
- Implemented ARRAY-of-VECTOR shape support (task #16, deferred earlier this session), user-reported against 117-EXAMPLE_8.hal (`POSITIONS ARRAY(5) VECTOR`, indexed `POSITIONS$(I:*)`): "DSUB: asterisk subscript with 2 indices not yet implemented". Root cause: symtab.c discarded a VECTOR/MATRIX symbol's own SYM_LENGTH-encoded element shape whenever SYM_ARRAY also made it ARRAY-shaped (silently undersizing the container 3x, not just failing DSUB's asterisk-select). Fixed by decoding SYM_LENGTH into the ARRAY branch too, and giving ensure_container() a new array_of_vector-flagged rows/cols treatment (same convention as the earlier 2D-ARRAY-of-SCALAR fix) so DSUB's existing MATRIX-shaped logic applies for free. A second bug surfaced verifying the file's whole-array vector arithmetic (`[VELOCITY]=([POSITIONS]-[OLD_POSN])/DELTA_T;`, `[DISTANCE]=ABVAL([POSITIONS]-MY_POSN);`, ADLP/DLPE-replayed VSUB/VSDV/VASN/BFNC): resolve_container() always returned an ARRAY-of-VECTOR SYT operand's whole flat container regardless of the replay's own arrayed_index, which only coincidentally gave the right answer when both operands were same-shaped (pure elementwise math) but broke the mixed-shape ABVAL/VDOT broadcast against a plain VECTOR. Fixed by slicing to one VECTOR per arrayed_index (both resolve_container's read side and VASN's own hand-rolled write side, which bypasses write_destination). All output independently hand-verified (vector subtraction/magnitude/dot-product arithmetic), not just cross-checked against compileLinkRun. ARRAY-of-MATRIX and ARRAY(*) assumed-size VECTOR parameters (141-VSUM.hal) remain deferred (task #29). Fixture: test_array_of_vector.hal.

### [2026-07-24] Target: class-0/LBL.md, class-0/EFOR.md, class-0/DFOR.md, STATUS.md
- Implemented `EXIT <label>;` targeting a labeled `DO FOR` (range or list form), user-reported against 119-EXAMPLE_9.hal (`INNER: DO FOR TEMPORARY J = 1 TO 3; ... EXIT INNER; ... END INNER;`): "branch to undefined label 11". EXIT compiles to a plain BRA whose INL operand is the same construct-id number DFOR/EFOR both carry (matching the already-handled EXIT-of-DTST/ETST case), but precompute_labels() only ever registered OP_LBL and OP_ETST, never DFOR/EFOR's own label. Fixed by registering it from EFOR (already carries the label directly in both forms, per EFOR.md's own LSTALL trace), landing at EFOR's position + 1 (same "just past this instruction" convention as ETST, not landing on EFOR itself, which would just re-run its increment/re-test). Building a regression fixture surfaced a second, real latent bug: precompute_labels()'s OP_LBL handling never checked the operand's qualifier, so a *labeled statement* declaration (`INNER:`, QUAL_SYT, keyed by SYT index) and an ordinary IF/ELSE-join-point LBL (QUAL_INL, keyed by bookkeeping-label number) shared one flat table — two independent numbering spaces that can coincidentally collide in a small enough program, silently mis-registering one as the other with no error. First tried restricting registration to QUAL_INL only, which then broke real `GO TO <label>;` (test_eron_goto.hal/test_eron_goto_appc.hal both regressed, real GOTO uses the QUAL_SYT form) — fixed properly instead by splitting into two parallel tables (new label_pos_syt[], state.h) with OP_BRA/OP_FBRA dispatching on their own operand's qualifier rather than assuming a shared namespace. Fixture: test_exit_dfor_label.hal (also exercises the ARRAY-of-VECTOR fix above, `V ARRAY(5) VECTOR(3)`); expected top-3-by-magnitude output hand-derived by tracing the algorithm.

### [2026-07-24] Target: class-0/DSUB.md, class-0/XXAR.md, STATUS.md
- Fixed two bugs found via one user report (120-EXAMPLE_A.hal's `DATA_VALID$(J:) = FALSE;`, DATA_VALID an `ARRAY(4) BOOLEAN`): "DSUB: asterisk subscript with 2 indices not yet implemented". (1) A BIT/BOOLEAN (or CHARACTER) ARRAY subscript compiles through the same "index + trailing asterisk" DSUB shape `M$(i,*)` uses (DSUB's own tag=1=BIT confirms the element type) — but unlike VECTOR/MATRIX, the asterisk selects no sub-range (a BIT/CHARACTER element has nothing further to select "all of"); it's just how the compiler always spells this subscript. Fixed by treating it as an ordinary single-element writable reference (the same generic per-dimension `is_ref` VAC-slot mechanism DSUB already uses), not a container-producing partition select. (2) Fixing that surfaced a second, independent bug once execution got further: `CALL EXAMPLE_A(SCALAR(9800,...), ...)`, an `ARRAY(4) SCALAR` procedure input parameter fed by a `SCALAR(...)` shaping-function argument, silently bound as all-zero — the compiler wraps that argument's own XXAR in an ADLP(4)/DLPE per-element replay exactly like a plain ARRAY WRITE argument, but the XXAR's operand here is a VAC slot already holding SSHP's own whole computed container, not a plain per-index SYT reference; resolve_operand's QUAL_VAC case has no is_container fallback at all, so every replay pass silently left the parameter's SYT storage at its zero-initialized default (`ALT(J) <= 0` reading 0.0, unconditionally true, rejecting every element with no error — a serious silent-corruption bug). Fixed by letting a VAC container operand bypass the arrayed_index<0 whole-container gate entirely (a VAC container has no per-replay-pass ambiguity the way a plain SYT reference does), captured once on the first replay pass only (later passes skipped, avoiding duplicate items[] entries that would misalign later arguments' own parameter binding). The file's own eventual `AVERAGE = TOTAL / NUMBER_GOOD;` division-by-zero once every reading is screened out as "too old" is a genuine property of the original sample program (no zero-guard before the divide) confirmed via both USA003090 App. C (no standard fixup exists for plain SCALAR/INTEGER division by zero, unlike VECTOR/MATRIX's error 25) and compileLinkRun (the real AP-101S emulator also traps repeatedly on this exact file) — not a yaHALMAT2 bug. Fixture: test_bit_array_dsub.hal, output independently hand-verified.

### [2026-07-24] Target: class-0/PRIO.md, class-0/WAIT.md, STATUS.md
- User-suggested sweep of remaining "not (yet) implemented" fail() sites in interp.c turned up two whole opcodes with fully primary-source-confirmed operand-word formats (real HALSFC LSTALL traces) but zero runtime implementation: PRIO (`UPDATE PRIORITY [label] TO alpha;`, USA003087 Sec. 13.5) and two of WAIT's three forms (`WAIT UNTIL time;`/`WAIT FOR DEPENDENT;`, Sec. 13.5 — only the plain interval form worked). PRIO: writes the resolved value into the target task's own `priority` field, the same field SCHEDULE...PRIORITY(...) sets at creation and sched_pick_next() already consults; named-process form per the confirmed trace, self/unlabeled form (1 operand) inferred from CANC/TERM's own already-confirmed self-vs-named pattern (PRIO.md flags that specific form untested). WAIT UNTIL: reuses schd_seconds_to_ticks() with the same "absolute virtual-time-in-seconds, not relative to now" semantics SCHD's AT/STOPPING...UNTIL clauses already use (confirmed by stop_deadline comparing directly against virtual_time, never adding it). WAIT FOR DEPENDENT: reuses the has_active_dependents()/sched_wake_dependents() mechanism already built for the CLOSE-triggered TASK_WAITING_FOR_DEPENDENTS case, but that existing mechanism unconditionally *terminates* the waiting task once dependents clear — correct when the task's own body is already finished, wrong for a mid-body WAIT statement, which must resume instead. Fixed with a new, distinct TASK_WAITING_FOR_DEPENDENTS_RESUME state (state.h) that transitions back to TASK_READY rather than TASK_TERMINATED. Fixtures: test_prio.hal (demonstrates actual preemption via a priority change), test_wait_until.hal, test_wait_dependent.hal (demonstrates genuine blocking via WORKER's own WAIT ordering its WRITE before the primal's).

### [2026-07-24] Target: class-1/ITOQ.md, class-6/IASN.md, class-5/SASN.md, STATUS.md
- User correction of a wrong claim made while surveying SUBBIT assignment ("needs IEEE-754 modeling this interpreter doesn't have"): AP-101S SCALAR is IBM hexadecimal floating point, not IEEE-754, and this project's own halmat_scalar_t (value.h) already stores the exact bit-for-bit wire format — no new modeling needed for SINGLE-precision SUBBIT at all. Fixed two bugs found implementing it: (1) STOQ's reference-context read (`B = SUBBIT(a_scalar_var);`) previously rounded the scalar to its nearest INTEGER first (rv_to_integer(), borrowed from STOI/STOB's semantically different ordinary conversion functions) instead of reading its real raw bits — a latent, previously-unexercised bug (only SUBBIT(integer) had a fixture before). (2) The assignment-context write (`SUBBIT(a_scalar_var) = ...;`) had no SCALAR case at all — added one that reinterprets the raw bits as `msw` directly. DOUBLE-precision SCALAR (a genuine 64-bit pattern) still fails loudly both directions — this interpreter's BIT value representation is uint32_t everywhere, a project-wide ceiling, not something safe to guess past; CHARACTER remains unimplemented for a different, more specific reason per the user's own clarification of AP-101S's real CHARACTER memory layout (2-byte max/current-length header + EBCDIC bytes) — this interpreter's own char_value/char_elements are plain malloc'd, already-ASCII-decoded, growable C strings with no such byte layout modeled at all (class-2/CASN.md's own longstanding gap), so there's no accurate target to reinterpret bits into. Cross-checking the SCALAR fix against a genuinely DOUBLE-precision variable surfaced a third, independent bug: a plain (non-array) SCALAR DOUBLE's own INITIAL() literal value silently kept SINGLE precision, since real HALSFC emits the shortest exact literal encoding regardless of the destination's declared precision, and write_syt_entry() (SINT's own destination-write path) never normalized to the declared precision the way OP_IASN/OP_SASN's dest_sym coercion and write_container_element() already do — the one remaining plain-SCALAR-destination write path missing it. Fixed by threading the destination SYT index through (mirroring write_container_element's existing convention). Fixtures: test_subbit_scalar.hal (round-trips a known scalar's raw bits through both SUBBIT directions; 1093140480 independently hand-verified as 0x41280000, the correct IBM HFP encoding of 2.5), test_scalar_double_initial.hal.
