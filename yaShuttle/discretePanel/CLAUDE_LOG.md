### [2026-09-14] Target: HANDOFF-meds2-py.md
- §10 departures / new §: IDP buses run on a BusPump thread (class BusPump after Bus; Bus.serviceOffGuiThread; IDP.start() moves its buses; heartbeat via BusPump.every, QTimer only when NSTS_IDP_THREAD=0). Rule: nothing on the GUI thread touches an IDP after start(). sys.setswitchinterval(0.001). Test hook NSTS_GUI_STALL_MS=<ms> (+ NSTS_GUI_STALL_BUSY=1) stalls every 16 ms redraw tick. Why and the A/B numbers: gpc-causes #144. §12 symptom table: "clock falls behind, I/O ERROR CRTn, fail-to-sync after the screen locks" -> fixed by BusPump; NSTS_IDP_THREAD=0 reproduces it under NSTS_GUI_STALL_MS=500.
- Pre-existing, not changed: in the crt2 two-CRT script IDP2's PASS load logs "load started" but never "load complete" (same with old and new MEDS2), though CRT2 draws PASS pages.
