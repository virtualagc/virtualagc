{
  "_comment": "Per-format DPS text adjustments, keyed on the format number the background draws in its title line.  Every key in MEDS2.py's ADJ table may be overridden; anything left out uses the default.  Add a format ONLY after looking at it and finding the defaults poor, and say what was wrong.  Shift+X moves the DEFAULTS live; to try a per-format value, edit here and restart that display.",
  "_example": {"3011": {"underY": -0.05, "vecY": 0.5}}
}
