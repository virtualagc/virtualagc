C/ From PDF p. 242 in "Programming in HAL/S".

  P:
  PROGRAM;
     DECLARE DO_SOMETHING EVENT;
     DECLARE DONE EVENT LATCHED INITIAL(OFF);
     SCHEDULE T PRIORITY(50);
     SIGNAL DO_SOMETHING;
     WAIT FOR DONE;
  T:
  TASK;
     WAIT FOR DO_SOMETHING;
     SET DONE;
  CLOSE T;
  CLOSE P;
  