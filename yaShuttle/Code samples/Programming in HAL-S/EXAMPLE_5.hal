# From PDF p. 108 of "Programming in HAL/S".

  EXAMPLE_5:
  PROGRAM;
     DECLARE A ARRAY(100);
     DECLARE RMS SCALAR;
     DECLARE TOTAL SCALAR DOUBLE INITIAL(0);
     DO FOR TEMPORARY N = 1 TO 100;
E			 2
M	TOTAL = TOTAL + A ;
S			 N

     END;
     RMS = SQRT(TOTAL/100);
  CLOSE EXAMPLE_5;