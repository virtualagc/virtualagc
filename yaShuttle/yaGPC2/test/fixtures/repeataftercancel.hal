 REPACNCL: PROGRAM;
    DECLARE T SCALAR;
    NEXT: TASK;
       WRITE(6) 'TICK';
    CLOSE NEXT;

    SCHEDULE NEXT PRIORITY(80) DEPENDENT, REPEAT AFTER 10.0 UNTIL 3.0;
    WAIT FOR DEPENDENT;
    T = RUNTIME;
    WRITE(6) 'RT', T;
 CLOSE REPACNCL;
