 RTPRIO: PROGRAM;
    DECLARE T SCALAR;
    DECLARE P INTEGER;
    NEXT: TASK;
       P = PRIO;
       WRITE(6) 'PRIO', P;
    CLOSE NEXT;

    SCHEDULE NEXT PRIORITY(80), REPEAT EVERY 1.0;
    T = RUNTIME;
    WRITE(6) 'MAIN RUNTIME', T;
    WAIT 0.5;
 CLOSE RTPRIO;
