 SELFTERM: PROGRAM;
    NEXT: TASK;
       WRITE(6) 'IN TASK';
       TERMINATE;
       WRITE(6) 'UNREACHABLE';
    CLOSE NEXT;

    SCHEDULE NEXT PRIORITY(80), REPEAT EVERY 1.0;
    WAIT 0.5;
    WRITE(6) 'DONE';
 CLOSE SELFTERM;
