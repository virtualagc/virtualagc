 IOFIX: PROGRAM;
    REPLACE INFILE BY "5";
    DECLARE VNAME CHARACTER(8);
    DECLARE X SCALAR;
    DECLARE B BIT(4);

    READALL(INFILE) VNAME;
    WRITE(6) VNAME;

    READ(INFILE) COLUMN(1), TAB(8), X;
    WRITE(6) X;

    READ(INFILE) B;
    WRITE(6) B;
 CLOSE IOFIX;
