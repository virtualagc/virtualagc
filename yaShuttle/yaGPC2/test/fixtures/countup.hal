 COUNTUP: PROGRAM;
    DECLARE I INTEGER INITIAL(1);
 
    NEXT: TASK;
       WRITE(6) I;
       I = I + 1;
    CLOSE NEXT;
    
    SCHEDULE NEXT PRIORITY(80), REPEAT EVERY 1.0;
    WAIT 199.5;
 CLOSE COUNTUP;
