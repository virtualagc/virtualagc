 TERMTEST: PROGRAM;
    DECLARE I INTEGER INITIAL(1);
    NEXT: TASK;
       WRITE(6) I;
       I = I + 1;
    CLOSE NEXT;

    SCHEDULE NEXT PRIORITY(80), REPEAT EVERY 1.0;
    WAIT 3.5;
    WRITE(6) 'TERMINATING';
    TERMINATE NEXT;
    WAIT 3.0;
    WRITE(6) 'DONE';
 CLOSE TERMTEST;
