 REPBARE: PROGRAM;
    DECLARE N INTEGER INITIAL(0);
    NEXT: TASK;
       N = N + 1;
    CLOSE NEXT;

    SCHEDULE NEXT PRIORITY(80), REPEAT UNTIL 0.0005;
    WAIT 1.0;
    WRITE(6) 'COUNT', N;
 CLOSE REPBARE;
