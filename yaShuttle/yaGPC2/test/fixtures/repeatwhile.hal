 REPWHIL2: PROGRAM;
    DECLARE EV1 EVENT LATCHED;
    NEXT: TASK;
       WRITE(6) 'TICK';
    CLOSE NEXT;

    SET EV1;
    SCHEDULE NEXT PRIORITY(80), REPEAT EVERY 1.0 WHILE EV1;
    WAIT 2.5;
    RESET EV1;
    WAIT 5.0;
 CLOSE REPWHIL2;
