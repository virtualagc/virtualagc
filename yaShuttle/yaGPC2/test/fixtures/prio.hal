 PRIOTEST: PROGRAM;
    DECLARE P INTEGER;
    DECLARE Q INTEGER;
    DECLARE R INTEGER;
    NEXT: TASK;
       Q = 1;
       R = 2;
       P = PRIO;
       WRITE(6) 'PRIO', P, Q, R;
    CLOSE NEXT;

    SCHEDULE NEXT PRIORITY(80), REPEAT EVERY 1.0;
    WAIT 0.5;
 CLOSE PRIOTEST;
