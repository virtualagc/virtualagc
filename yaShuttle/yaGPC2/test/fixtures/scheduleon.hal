 SCHON3: PROGRAM;
    A: TASK;
       WRITE(6) 'IN A';
    CLOSE A;
    NEXT: TASK;
       WRITE(6) 'IN NEXT';
    CLOSE NEXT;

    WRITE(6) 'BEFORE';
    SCHEDULE NEXT ON NOT A PRIORITY(80);
    SCHEDULE A PRIORITY(1);
    WAIT 0.001;
    WRITE(6) 'DONE';
 CLOSE SCHON3;
