 SCHINREP: PROGRAM;
    DECLARE I INTEGER INITIAL(0);
    NEXT: TASK;
       I = I + 1;
       WRITE(6) I;
    CLOSE NEXT;

    WRITE(6) 'BEFORE';
    SCHEDULE NEXT IN 1.5 PRIORITY(80), REPEAT EVERY 1.0;
    WAIT 5.0;
    TERMINATE NEXT;
    WRITE(6) 'DONE';
 CLOSE SCHINREP;
