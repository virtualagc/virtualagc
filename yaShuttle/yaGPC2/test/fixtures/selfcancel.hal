 SELFCAN: PROGRAM;
    A: TASK;
       CANCEL;
       WRITE(6) 'UNREACHABLE';
    CLOSE A;

    WRITE(6) 'BEFORE';
    SCHEDULE A PRIORITY(80), REPEAT EVERY 1.0;
    WAIT 1.0;
    WRITE(6) 'DONE';
 CLOSE SELFCAN;
