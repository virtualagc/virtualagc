 CANCELT: PROGRAM;
    A: TASK;
       WRITE(6) 'IN A';
    CLOSE A;

    WRITE(6) 'BEFORE';
    SCHEDULE A PRIORITY(80), REPEAT EVERY 1.0;
    CANCEL A;
    WAIT 1.0;
    WRITE(6) 'DONE';
 CLOSE CANCELT;
