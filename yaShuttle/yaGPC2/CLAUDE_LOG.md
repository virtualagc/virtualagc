# CLAUDE_LOG.md

(Cleared 2026-07-30 by Full Documentation Sync — pending entry applied to problems.md §6.7.)

### [2026-07-30] Target: debugger-planner.md
- User corrected the debugger-planner.md Stage 3 SDF finding (per commit 4467cfa54): the SDF's own SRN field (source columns 73-78) really is a separate, less-reliable concept from the "HAL/S statement number" (1-based position in statementIndexTable, restarting at 1 per SDF) — SRN came back blank only because the test source (HELLO.hal) never had column 73-78 data, not because of a toolchain gap. pass1.rpt's leftmost column already *is* the correct HAL/S statement number, so tools/gen_source_map.py's approach was right all along, just mislabeled "srn" throughout (tools/gen_source_map.py, src/sourcemap.h/.c, src/debugger.c, test/fixtures/hello.srcmap.json) — renamed to "stmt"/HAL/S-statement-number, no logic changes, all tests still pass.
- Also fixed: debugger commands now accept a gdb-style no-space repeat count (`step5` same as `step 5`) via a new split_digit_suffix() in src/debugger.c, with "x32" (an existing alias, not "x"+32) excluded.
- debugger-planner.md's Stage 3 section (the "SDF looked right, but wasn't" update) should be corrected to reflect this: SRN wasn't populated because of the test source, not a compiler-port gap, and SRN was never the right field to key off regardless — the correct/universal identifier is the HAL/S statement number (statementIndexTable position), which is what pass1.rpt's leftmost column already gives directly.
- Follow-up correction (same day): when the HAL/S source *does* populate columns 73-78, pass1.rpt prints the SRN as an *extra leading column* before the statement number (e.g. "000010    1 M|...", not just "     1 M|..."), which tools/gen_source_map.py's parse_pass1() regex didn't account for -- would have silently dropped every statement line in a source file that uses SRNs (the anchored regex simply fails to match, not merely mislabels). Fixed with an optional non-capturing leading digit-run group; verified against the user's example lines and reconfirmed no change in behavior for the (no-SRN) existing test fixture.
- The "Multi-unit memory images" section's "Update (Stage 3 implementation): still open" note is now resolved: gen_source_map.py's --unit MODULE=PASS1:PASS2 (repeatable, or via an @argsfile for hundreds of units) builds one JSON covering every compiled unit in a linked image; src/symboltable.c gained symtable_get_module_at() and src/sourcemap.h/.c dispatch lookups by that module name, exactly as this section's original "dispatched by symtable_get_section_at's existing module resolution" proposal envisioned. CSECT code/data classification (needed by gen_source_map.py's parse_pass2()) turned out not to be a name-prefix question at all (confirmed by reading lnk101's own _ZONE_BY_PREFIX rule in ~/donschmidt/nsts-sdl-dps/src/lnk101/linker.py -- "#C..." names are code only because CODE is the *default* zone) -- resolved instead via each CSECT run's own "TIME:" instruction annotations, self-contained within pass2.rpt. Verified end-to-end against a real 2-unit link already in this repo's corpus (176-P.hal calling 176.1-READ_ACC.hal via SCAL and back -- new test/fixtures/debugger_multiunit_*, 176-P.fcm/176-P-lnk101.json/176-P.srcmap.json). Also fixed a real bug this surfaced: statement numbers are only unique per compiled unit, so debugger.c's "only reprint when the statement changes" tracking needed the module name added alongside the statement number, or two different units' own statement N could look like no change.
