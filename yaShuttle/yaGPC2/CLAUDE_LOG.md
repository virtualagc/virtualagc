# CLAUDE_LOG.md

(Cleared 2026-09-09 by Full Documentation Sync.  Nineteen entries applied.

To `HANDOFF-OPS9.md`: a new "The OPS 901/201/301 blocker, as of 2026-09-09"
section — the invariants, the chain that was traced and then shown to be a
passenger, six findings that contradict earlier entries, and the run-to-run
variability warning; corrections to the DEUKEYS/RUN_AT guidance now that `@Ns`
is known to be WALL seconds against SIMULATED snapshots and windows, that the
IPL source comes off AFTER RUN per the PASS User's Guide Table 2-2 steps 13-14,
and that `SOURCE_RUN=OFF` is the default because leaving it on fails as
silence; and a rewritten "What is still open" with a "Closed since the last
sync" list.

To `HANDOFF-FCMBOOT.md`'s instrumentation list: `YAGPC_PCCOUNT`,
`YAGPC_DKTRACE`, `YAGPC_IMONHIST`, and a new "Behaviour, not instrumentation"
group for `YAGPC_MMU_QUEUE_READY` and `YAGPC_DEU_EXTRA_PRELOADED`.

To `HANDOFF-OI340600.md`, via `dass-handoff.py` because it is GENERATED:
three new entries (#322 the reference-tape control and the ISPB refutation,
#323 the four con80build/lnk101 defects now open as PRs #49-51, #324 tape v36
and the three yaGPC2 defaults that changed under it) and a rewritten #321.
`check` confirms all three documents match the database.)
