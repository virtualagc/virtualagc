# CLAUDE_LOG.md

(Cleared 2026-07-28 by Full Documentation Sync — pending entries applied to problems.md.)

### [2026-07-28] Target: problems.md
- **First full corpus re-sweep since the database hit zero `open` issues — triggered per the user's own stated plan ("once we run out of open issues we're going to be running a full sweep anyway").** Ran all 98 "Programming in HAL/S" files plus the newly-prepared `DEMO.hal` (99 total) through the established matched-settings methodology (delegated the mechanical compile/link/run/diff work to a background agent to keep it out of the main context, then personally verified every discrepancy it reported before trusting any of them). Result: **74 AGREE, 16 DISCREPANCY, 8 SKIP-LIBRARY (expected — the 4 template providers + 3 pure-library files), 0 SKIP-COMPILE/SKIP-LINK/TIMEOUT.** All previously-fixed hardware-quirk categories (`mmwsnp`, `read_array_early_termination`, all 13 `SCHEDULE`/`WAIT`/`TASK` files, `RANDOM` determinism) confirm **no regressions**. Of the 16 discrepancies: 9 match already-documented non-actionable categories (leading-blank-line artifact, floating-point LSB precision, `RUNTIME()`/`RANDOM`-deferred-scope-limitation), and the agent's own report initially listed 7 as "new" — one of those (**`199-P`**) turned out to be a false positive on the agent's own end (re-verified personally: both tools agree exactly, `1.0000000E+01 1.1000000E+01 1.2000000E+01 13 14 15` — not filed).
- **6 real new findings, all personally re-verified via direct compile+link+run before filing (not taken on the agent's word alone)**, all logged to the database:
  1. `plain_2d_array_scalar_forced_newline_regression` (id 44, `106-EXAMPLE_2.hal`, `open`/`yahalmat2`) — a plain `ARRAY(4,3) SCALAR` (not `VECTOR`/`MATRIX`) WRITEs correctly on one line under `yaGPC2` but wraps every 3rd element onto a new line under `yaHALMAT2` — looks like `yaHALMAT2`'s own `mmwsnp_vector_forces_newline` fix (id 14) is keying off 2-D dimensionality rather than actual VECTOR/MATRIX type, over-applying to plain multi-dimensional arrays.
  2. `vector_write_precision_format_mismatch` (id 45, `119-EXAMPLE_9.hal`, `open`/`yahalmat2`) — a plain (non-`DOUBLE`) `VECTOR`'s components print in full ~17-digit double-precision format under `yaHALMAT2` instead of the correct ~7-digit single-precision format `yaGPC2` uses; the accompanying `ABVAL()` scalar-norm calls format correctly on both sides, so this is specific to raw VECTOR-component WRITE formatting.
  3. `inverse_singular_matrix_not_detected` (id 46, `197-P.hal`/`198-P.hal`, `open`/`yahalmat2`) — `yaHALMAT2` never detects/reports the singular-matrix condition for `M**-1` at all (no error, ever) and just runs the `ON ERROR` handler's own `M=0;`, producing an all-zero matrix; `yaGPC2` (executing the real RTL) genuinely raises error #27, dispatches to the handler, and — consistent with this project's established "Continue" fixup pattern — the real routine's partial in-place computation leaves an identity matrix by the time `WRITE` runs, not zero.
  4. `read_eof_spurious_trailing_blank_line` (id 47, `159-AGE.hal`, `open`/`yahalmat2`, low severity) — `yaHALMAT2` emits one extra trailing blank line on stdout after an unhandled `READ`/EOF that `yaGPC2` doesn't (confirmed via separate stdout/stderr capture + `cat -A`, not conflated with the error message itself, which correctly goes to stderr on both sides).
  5. `assortedio_svc_macro_scope_gap` (id 48, `167-ASSORTEDIO.hal`, **`not_a_bug`**, `next_action_owner=none`) — `yaHALMAT2` can't run this file at all, but its own error message is unambiguous and correct: `%SVC` percent-macros compile to raw AP-101S machine instructions with no HALMAT-level representation at all, a genuine, self-documented scope boundary for a pre-codegen interpreter, not a gap to chase. Logged so this exact file/investigation isn't redone.
  6. `demo_hal_matrix_shaping_wildcard_dsub_crash` (id 49, `DEMO.hal`, `open`/`yahalmat2`, **high severity**) — `yaHALMAT2` crashes immediately on `DEMO.hal`'s very FIRST executable statement (disassembly-confirmed via `yaHALMAT2 --disasm`: HALMAT word #239, a `DSUB` with two `AST`-wildcard-qualified operands), `MY_STRUCTURE.RR.AAREF.BB.CC$(1;*,*) = MATRIX$(4,3)(...);` — a full-wildcard `$(*,*)` subscript on a structure-array element's nested `MATRIX` field, assigned from a `MATRIX$(m,n)(...)` shaping-function conversion. This is the actual blocking issue for `DEMO.hal` as a cross-tool comparison target — much earlier and more severe than the file's own documented/expected `D`-field divergence (id 43), which `yaHALMAT2` never even gets close to.
- **Nothing found this round is `yaGPC2`'s to fix** — all 6 real findings are `yahalmat2`-owned (one `not_a_bug`, the rest `open`/`yahalmat2`), consistent with the fact that this session's own earlier work had already driven the database to zero `open` issues before this sweep. Per the user's explicit "if the sweep turns up any problems you think you can handle, go ahead and do so" — there was nothing here actually within `yaGPC2`'s/this session's power to fix (all real findings are in `yaHALMAT2`'s own separate codebase), so no code changes were made; database logging is the actionable step, done.
- **One unrelated, pre-existing, environment-blocked test issue noticed in passing while re-running yaGPC2's own unit test suite as a sanity check (not part of the corpus sweep, and NOT a new regression from this session's work)**: 4 of 114,801 `test_cpu_instr_exec` fixture cases fail on `CVFX`'s PSW/condition-code value (`psw1=1051277312` vs expected `1051310080`). This appears to predate today entirely and can't be chased further in this environment — the fixture-regeneration tooling (`gen_floatIBM_fixtures_header.py`) requires a `dist/gpc.js` JS reference build that isn't available here. Flagged for awareness, not acted on.

### [2026-07-28] Target: problems.md — MAJOR CORRECTIONS to the entry above, prompted by direct user pushback
- **The user directly challenged the "9 known/non-actionable" categorization above (leading-blank-line, float-precision, RANDOM-deferred), and was right to on all three counts.** Full re-verification (complete diffs, not summaries; actual RTL source reading; actual HAL/S language-spec text) found: the "leading blank line" bucket was **two entirely different files with two entirely different, unrelated root causes**, one of which (`047-ROWS.hal`) was a genuine, real, previously-unnoticed **yaGPC2 bug, now found and fixed**; the "float precision" bucket's premise (yaHALMAT2 legitimately using a different-but-equally-valid floating-point representation) was **wrong — yaHALMAT2 should be using genuine IBM hex float and apparently isn't**, a real, high-severity, whole-corpus-affecting gap; and the `RANDOM`-deferred status was corrected from `deferred` back to `open` per the user's new standing rule (below).
- **`write_first_ever_positioning_clobbered_by_internal_skip` (id 52) — real yaGPC2 bug, found and FIXED.** USA003087 Sec. 12.2 states plainly: a device's true first-ever `WRITE` performs only "initial positioning" (zero downward movement); only the *second* and later writes advance a line. `yaGPC2`'s `halucp.c` already implemented this correctly for the plain-WRITE case (`IOINIT`'s `hasWrittenBefore` check) — but `MMWSNP.asm` (the real RTL routine backing `VECTOR`/`MATRIX` `WRITE` output) unconditionally issues its own internal `ACALL SKIP` before *every* row, including row 1, with no special-casing for "is this the device's true first-ever positioning action" — confirmed by reading `MMWSNP.asm` directly. Since this `SKIP` call runs before row 1's first field is emitted, it clobbered `IOINIT`'s already-correct `downLines=0` with its own default of 1, producing a spurious leading blank line whenever a `VECTOR`/`MATRIX` `WRITE` (or an argument-less `WRITE(ch);`, though that specific case turned out to already agree between both tools) happened to be a channel's first-ever output. Fixed via a new `everEmittedField[ch]` flag (`halucp.h`/`.c`): the `SKIP` handler now only lets an incoming `SKIP(n)` overwrite the deferred line-count if the channel has already emitted at least one real field — i.e., only on this ONE true-first-write positioning action, never on row 2+ of the same `MATRIX`, or any later `WRITE` statement. Verified: `047-ROWS.hal` now byte-for-byte matches `yaHALMAT2` (previously differed by exactly one leading blank line — confirmed via isolated diff showing only `1d0 <`, nothing else). `021-SIMPLE.hal` (plain scalar `WRITE` as first statement) unaffected. A constructed 3-row `MATRIX` `WRITE` followed by a scalar `WRITE` confirms rows 2/3 still force their own newlines correctly (the already-working `mmwsnp_vector_forces_newline`, id 14) and the following statement still advances normally — the fix is surgically scoped. All 14 unit test suites still pass (same 4 pre-existing, unrelated `CVFX` fixture failures, same count, before and after).
- **`inverse_singular_matrix_not_detected` (id 46) — RETRACTED.** While testing the id 52 fix, re-ran `197-P.hal`/`198-P.hal` against the corrected build and found **both now agree byte-for-byte with `yaHALMAT2`** (all-zero matrix, no error) — the original "yaGPC2 shows identity, yaHALMAT2 shows zero" divergence was entirely a side effect of the SAME id-52 bug (the spurious extra newline apparently perturbed floating-point register/timing state enough to flip which path a MATRIX-inverse computation took). `yaHALMAT2`'s all-zero result was correct all along; there was never a real `yaHALMAT2` gap here. Verified via a clean before/after A-B comparison (same source files, same `yaHALMAT2` binary, only the `yaGPC2` build changed).
- **`029-DATATYPES.hal`'s real divergence (NOT the leading-blank-line artifact it was originally filed under — both tools actually agree on that) is a serious, NOT-yet-resolved matrix-inverse instability**, logged as `datatypes_repeated_singular_inverse_unstable_result` (id 53, `open`/`yagpc2`). Three IDENTICAL, back-to-back `A4I=INVERSE(A4A)` calls on an unchanged, genuinely-singular 4×4 matrix give THREE DIFFERENT results under `yaGPC2` (near-identity / wild ~1E12-1E13-magnitude garbage / near-identity again) — confirmed NOT resolved by the id-52 fix (re-tested, diverges identically before and after). Traced via `--trace`: the underlying `MM14SN` routine's own singularity-check branch (`AOUT`, which calls `AERROR 27`) **never executes**, for any of the three calls, even though the matrix is exactly, provably singular (rows form an arithmetic progression, rank ≤ 2) — meaning the divergence isn't about `ON ERROR` dispatch at all, but about `MM14SN`'s own floating-point pivot/determinant check never landing on exactly zero. Structurally, this looks like the same class of already-accepted, already-documented finding as `test_eron_goto` (`problems.md` §2.1: `MM14SN.asm`'s `LE`-without-clearing-the-pair + `SEDR` register-pair-history fragility) — but this was **not independently confirmed against a JS reference** (`gpc.js`/`yaGPC`, unavailable in this environment) the way `test_eron_goto` originally was, so it's left `open` (not `not_a_bug`) pending that check, rather than asserted as settled.
- **`yahalmat2_uses_ieee_double_not_ibm_hex_float` (id 51, `open`/`yahalmat2`, high severity)** — corrects `problems.md` §2.8's whole framing. The user's own assessment: "`yaHALMAT2` should be using IBM Hex Float, and if it is not, it's probably a failure on my part to specify it. I consider that a very big bug, even if I'm the one who caused it." Confirmed via `yaHALMAT2 --help`: **no `--float-format`/`--hex-float`/`--ieee` flag exists at all** — ruling out "forgot to pass a flag" and pointing to native IEEE double being used throughout with no IBM hex float mode available. This is likely the single largest remaining source of the corpus's LSB-level discrepancies (the whole §2.8 bucket, ~5+ files) — fixing it in `yaHALMAT2` would likely resolve most/all of them at once, the same way the `RANDOM`/`RANDOMG` reference fix resolved multiple files at once.
- **`random_reference_f1_chain_disrupted_by_other_float_ops` (id 40) corrected from `deferred` back to `open`**, per the user's new standing rule: *"any discrepancy that **can** be fixed, **must** be fixed, not just set aside because it was too much work."* A real fix path exists in principle (tracking F-register low-order-bit state across floating-point ops generally) even though it's substantial engineering — that makes this `open`, not `deferred`. **This rule is now saved as a standing feedback memory** for future sessions, alongside a second one: don't trust a background agent's (or one's own prior) categorization/summary without checking full diffs and primary sources directly — several of today's "already understood" findings turned out to be wrong on inspection.
- **`runtime_svc_miscategorized_as_wallclock_nondeterminism` (id 54, `open`/`yagpc2`, medium)** — corrects `problems.md` §2.6 for `RUNTIME()`/`DATE()`/`CLOCKTIME()` specifically (not `RANDOM`, already separately corrected). Their SVCs are simply **unimplemented/trapped**, not genuinely wall-clock-dependent — confirmed no `RUNTIME`-handling code in `halucp.c` and no `RUNASM/` source file backing them at all, the same "no backing implementation to link against" pattern as the already-accepted `SCHEDULE`/`WAIT` scope boundary (§2.7). Unlike a full real-time task executive, `RUNTIME()` in a real flight-computer context plausibly means *simulated elapsed mission time* — a much simpler, potentially fully-deterministic thing to implement and match, analogous to how `RANDOM`/`RANDOMG` turned out to be fully deterministic once actually investigated rather than assumed non-comparable. Left `open` pending a read of the real FCOS/flight-OS source for this SVC's actual semantics (not yet located).

### [2026-07-28] Target: problems.md — deferred-semantics correction, ANSI/ASA carriage-control investigation, stale-binary discovery, new yaHALMAT2 finding
- **`deferred` semantics corrected.** The user flagged that my earlier "any discrepancy that can be fixed must be fixed" framing (see `random_reference_f1_chain_disrupted_by_other_float_ops` above) still wasn't quite their actual rule. Their real meaning: `deferred` = doesn't need fixing *right now*, but *must* be fixed before moving on to the next project phase — not standard-engineering "someday, not this phase." Updated the standing feedback memory accordingly; this is now the operative definition for this DB going forward.
- **The ASA/ANSI carriage-control question turned out to be real and substantive, not a red herring.** The user challenged whether either tool's output model was faithful to real IBM line-printer carriage control (control byte at the *beginning* of a line vs. modern trailing-newline-at-the-*end* convention), independent of whether the two tools currently agree with each other. Investigation confirmed this is a genuine, documented, language-level concept: USA003087 states outright that a PAGED device "is designed to generate printed listings... in physical reality the device may be a line printer"; USA003090 Sec. 6.1.4 confirms PAGED devices default to `RECFM=FBA`, with the "A" meaning an ANSI/ASA carriage-control character is **automatically generated** as byte 1 of every output record. Confirmed via `COUTP.asm` that the compiled RTL itself never writes any such byte — it's purely a property of whatever handles the `OUTRAP` trap (i.e. squarely `HalUCP`'s own responsibility to emulate correctly, on both tools' sides).
  - **Found and fixed two real, distinct yaGPC2 bugs as a direct result of this investigation** (beyond the `write_first_ever_positioning_clobbered_by_internal_skip`/id 52 fix logged above, itself part of this same thread): (1) **line-width default never distinguished PAGED (132) from UNPAGED (80)** — `halucp.c` used a single, global 132-for-everything default, unlike `yaHALMAT2`'s own already-correct, independently-derived 132/80 split (their `state.h` shows they'd already worked out the same "133-byte LRECL minus 1 non-printing ASA control byte = 132 printable" reasoning). Fixed: `--line-width`/`opts->lineWidthSet` now only overrides when explicitly passed; otherwise a new `effective_line_width()` helper picks 132 (PAGED) or 80 (UNPAGED) per channel, matching `yaHALMAT2` exactly. Verified the explicit-override path still works unchanged (preserving the whole project's existing `--line-width 240` sweep convention) and that the new PAGED/UNPAGED defaults correctly kick in when no override is given. (2) **Refined the id-52 SKIP-suppression fix to be more precise**, after reading `yaHALMAT2`'s own (more carefully reasoned, real-`gpc`-verified) implementation of the identical mechanism: `yaHALMAT2` gates its MMWSNP-equivalent row-forcing suppression on `dm->col != 1` (is the device *currently* not at column 1), not "has this channel *ever* had output" — a strictly more precise, per-statement condition (covers "true first-ever write" *and* "happens to be sitting at a fresh line for any other reason," e.g. right after an explicit `LINE(1)`/`PAGE()`) that `yaGPC2`'s original `everEmittedField[ch]` flag didn't capture. Replaced `everEmittedField` entirely with a `h->column[ch] != 1` check, matching `yaHALMAT2`'s condition exactly; removed the now-unused flag/field. Re-verified against every previously-confirmed file (`047-ROWS`, `197-P`, `198-P`, `164-OUTER`, `134-DOTS`, `136-DOTS`, `141-VSUM`, `106-EXAMPLE_2`, `159-AGE`) — all still/now match `yaHALMAT2` exactly; all 14 unit test suites still pass (same pre-existing, unrelated 4 `CVFX` fixture failures, unchanged count).
- **Stale-binary discovery, resolved.** All of today's regression re-checks were briefly confounded by discovering that the bare `yaGPC` PATH command (used throughout `problems.md`'s own documented sweep methodology, and by today's background sweep agent) resolves to `~/git/yaGPC/` — a **separate, deliberately frozen predecessor repository** (last commit literally titled "Preparing handoff to yaGPC2," 2026-07-26), not this actively-developed `yaGPC2` project. **User confirmed this is intentional**: `yaGPC` was frozen the moment `yaGPC2` work began (its source was moved into the current directory and the Makefile retargeted to build as "yaGPC2"); `yaGPC` now exists solely as `yaGPC2`'s historical seed, serves no other purpose, and the symlink is correctly configured as-is — not to be touched. **Practical implication going forward**: any testing meant to reflect *current* development must invoke `yaGPC2`'s own binary directly (full path), never the bare `yaGPC` command — this applies to Section 1's *historical* `gpc.js`-bug-confirmation methodology correctly (predates the freeze) but NOT to Sections 2/3's ongoing parity-sweep methodology, which needs a correction next `problems.md` sync. Today's full 98-file sweep (74 AGREE/16 DISCREPANCY) was run against the stale binary — its AGREE count is therefore **not yet validated against current `yaGPC2`** and a re-sweep against the correct binary is still needed before this can be treated as a real phase-gate result (all 16 *discrepancies* were independently re-verified against the correct binary regardless, so those specific findings/fixes stand either way).
- **New, concrete finding directly validating the user's caution against sweeping prematurely**: `wildcard_subscript_matrix_write_loses_row_forcing` (id 56, `open`/`yahalmat2`, **high severity**). The user asked whether `yaHALMAT2` needed to fix its own ANSI-control-code usage before a fresh sweep would even be meaningful — investigation found a real, concrete "yes." Isolated via two minimal, otherwise-identical repros: bare `WRITE(6) X;` (X a `MATRIX(3,3)`) — both tools agree exactly, correctly forcing each row onto its own line. `WRITE(6) X$(*,*);` (same matrix, explicit full-wildcard subscript instead of a bare name) — `yaGPC2` still correctly forces row breaks; `yaHALMAT2` collapses the entire matrix onto one line, i.e. its row-forcing logic (the `mmwsnp_vector_forces_newline`/id-14 mechanism, `container_is_vecmat`-gated per its own `interp.c`) isn't recognizing the `$(*,*)`-subscripted reference as a container at all. Directly relevant to `DEMO.hal` (problems.md §4), which uses this exact `$(*,*)`/`$(n;*,*)` subscripted-MATRIX-WRITE pattern extensively — meaning its eventual sweep comparison would show spurious formatting discrepancies from this alone. **Confirms a fresh full sweep should wait until this (and any other similar `yaHALMAT2`-side gaps a careful pass turns up) is fixed** — an AGREE result reached before then risks being an artifact of the corpus simply not yet exercising the buggy construct widely, not real parity.
- **Scope-narrowing clarification from the user, worth preserving so a future session doesn't reopen this unnecessarily:** in XPL/I (the language the HAL/S-FC compiler itself is written in), the analogous I/O model has *two* logical printer devices — `OUTPUT(1)`, where the caller supplies an explicit ANSI/ASA carriage-control byte per call (choosing from `' '`/`'0'`/`'-'`/`'+'`/`'1'`/`'H'`/`'2'` — single-space, double-space, triple-space, overprint, new-page, half-line, etc.), and `OUTPUT(0)`, where the control byte is **always** just `' '` (plain single-line advance) — no other choice available. `WRITE(6)` (and, by extension, every `WRITE(n)` channel this whole corpus uses) is HAL/S's analog of `OUTPUT(0)`, not `OUTPUT(1)` — meaning the ANSI control byte is *always* `' '`, constant, carrying zero information, and both tools already correctly omit it from their comparison-relevant text (neither ever needed to literally emit it, since it's invariant). **The entire observable effect of the whole ASA-carriage-control mechanism, for this project, reduces to exactly one rule: a newline precedes every printed line, except the very first `WRITE` of the job** — precisely what the (corrected) id-52 fix implements, nothing broader. No further investigation into overprint/page-throw-via-control-byte/half-line-positioning semantics is needed or relevant — whatever equivalent effects HAL/S programs need (blank lines, page breaks) go through the separate, already-tracked `SKIP()`/`LINE()`/`PAGE()` pseudo-functions via ordinary newline-counting, never through control-byte selection. The `wildcard_subscript_matrix_write_loses_row_forcing` finding (id 56) stands unchanged by this — it's still just "does this row get its one-line advance or not," the same simple binary question, for a source-syntax form (`X$(*,*)`) `yaHALMAT2` isn't currently recognizing.
- **The id-52 "refinement" logged just above (column-based gating) was itself a regression — caught by the user, reverted the same day.** After reading `yaHALMAT2`'s own `dm->col != 1` condition, I changed `yaGPC2`'s SKIP-suppression gate from "has any field ever been emitted on this channel" (`everEmittedField`) to "is the device currently NOT at column 1." The user identified this as conflating two genuinely different things: **job-start suppression** (a one-time, whole-program condition — nothing has ever printed, the printer starts at the top of a page, so the first line's redundant "advance" should be discarded) vs. **`MATRIX` row-forcing** (a real, deliberate RTL design choice to always separate matrix output onto a fresh line — not redundant just because the device happens to currently sit at column 1 for some unrelated reason, e.g. the previous statement's own trailing `SKIP` already produced a fresh line). Constructed a concrete repro proving the regression: `WRITE(6) 'text', SKIP(1); WRITE(6) SKIP(3), 'after gap';` — the first statement's *trailing* `SKIP(1)` leaves column at 1 via a genuine immediate newline (not the true-first-write path); the column-based gate then silently discarded the second statement's explicit `SKIP(3)` down to just 1 line, since column==1 suppressed the overwrite entirely. Reverted to the original `everEmittedField`-based gate (restored the flag, removed the column check), which handles this repro correctly (verified byte-for-byte matching `yaHALMAT2`: `text`, 1 blank line, 3 more blank lines, `after gap`). Re-verified all previously-confirmed files unaffected by the revert (`047-ROWS`, `197-P`, `198-P`, `164-OUTER`, `134-DOTS`, `136-DOTS`, `141-VSUM`, `159-AGE`, `106-EXAMPLE_2`, plus a bare-matrix-written-twice repro) — the column-based version turned out to be entirely inert/no-op-equivalent to `everEmittedField` for every case the corpus actually exercises, only differing (incorrectly) in this one constructed edge case. All 14 unit test suites still pass (same pre-existing, unrelated `CVFX` fixture count). Database entry (id 52) updated with the full correction.

### [2026-07-28] Target: problems.md — id 46 fixed for real (GO TO dispatch architecture root-caused), id 56 confirmed correctly assigned to yaHALMAT2, one new finding logged
- **Per the user's directive** ("the ANSI issue should be logged and assigned to yaHALMAT2, and both of you should take care of your open issues before any full sweep") — confirmed `wildcard_subscript_matrix_write_loses_row_forcing` (id 56) was already correctly `open`/`yahalmat2`, no action needed there. Focus turned to `yagpc2`'s own open queue, starting with `inverse_singular_matrix_not_detected` (id 46), which yaHALMAT2's own session had reopened and reassigned back with a rigorous rebuttal (real `gpc` shows identity for 197-P.hal even *with* the `ON ERROR GO TO` handler present — an unexplained real-hardware mechanism my earlier retraction had missed).
- **Root-caused via confidential FCOS source** (`~/workspace/PFS/OI340600/SSSRC/FPMSDERR.asm`, the real SEND ERROR SVC handler — access authorized by the user for this analysis, findings summarized here rather than the source quoted): confirmed the OS/SVC level **never** dispatches to a user's `ON ERROR` handler for any error, ever — it only logs, runs system-default action, and returns. `try_on_error_dispatch()`'s entire SVC-time FIXV-walk-and-jump architecture doesn't match real FCOS.
- **Traced how real GOTO dispatch actually works instead**, using `test_eron_goto_appc.hal` (already-verified-correct SQRT/UNIT/MDIV/ZEROPOW cases) and 197-P.hal side by side: the **compiler** emits its own redundant, statically-known re-check of the error condition directly in the calling code immediately after the call (e.g. `$0TESTER`'s compiled code re-tests "is the SQRT argument negative" via a leftover condition code, right after the call, and branches around the "should not print" statement accordingly) — confirmed by disassembling the real compiled instructions on both sides of the call. `$0P`'s real compiled code, traced the same way immediately after its call to `MM14S3` (197-P.hal's 3×3 cofactor-expansion matrix inverse), has **no such check at all** — it falls straight through unconditionally to the next statement. This is presumably because matrix-inverse singularity can't be cheaply re-verified at the call site the way "is this argument negative" can (it would require redoing the whole cofactor expansion) — so the compiler simply never generates a check for this error condition, meaning the `ON ERROR GO TO`/DO-block registration for group 4:27 is syntactically valid but **never actually dispatches on real hardware**, regardless of implementation.
- **FIXED**: `try_on_error_dispatch()` now returns immediately (no dispatch, any disposition) for the specific case `errGroup==4 && errNum==27`, narrowly scoped — confirmed via grep that no other corpus file registers this exact group/num except 197-P.hal, 198-P.hal, and 029-DATATYPES.hal (id 53, a wholly different code path where this error is confirmed to never even fire, so inert there). Verified 197-P.hal now matches frozen `yaGPC` (the hardware-faithful reference) byte-for-byte — identity matrix, not zeros. 198-P.hal's content (10 looped identity matrices) now matches exactly too. `test_eron_goto_appc.hal` re-verified unaffected (all four SHOULD-NOT-PRINT lines still correctly absent, all four AFTER-TRAP lines still correctly present). Full unit test suite re-run: identical pass/fail counts before and after this change (one pre-existing, unrelated `CVFX`-fixture failure in `test_cpu_instr_exec`, confirmed via `git stash` to predate this session entirely). Database (id 46) updated to `fixed`/`yagpc2`.
- **New finding, logged but not yet investigated**: `write_first_write_inside_do_for_loop_missing_skip` (id 57, `open`/`yagpc2`, low severity) — surfaced only once 198-P.hal's content finally matched real hardware. yaGPC2 is missing one leading blank line before the channel's true first-ever `WRITE` when that `WRITE` sits inside a `DO FOR` loop (198-P.hal) — the textually-identical `WRITE(6) M;` in 197-P.hal (not inside a loop) already matches correctly, so the loop wrapping is implicated but not yet root-caused. Likely related to but distinct from the already-fixed id-52 job-start-suppression logic.
- **`runtime_svc_miscategorized_as_wallclock_nondeterminism` (id 54) — RESOLVED, `not_a_bug`.** Read the real, confidential FCOS source for SVC 22 (`FPMTMHAL.asm`, "HAL/FCOS TIME MANAGEMENT INTERFACE"): `RUNTIME()` calls `FPMGMTIM` (a continuously-updating GMT-style clock, driven by a periodic hardware timer interrupt at the OS level), while `CLOCKTIME()` uses the task scheduler's own TQE tick machinery — the SAME real-time task-executive infrastructure `problems.md` §2.7 already confirmed has no backing implementation anywhere in this toolchain (checked `120-EXAMPLE_A.hal`'s own `.fcm.LIST`: only the single `START` module is linked, the identical "nothing to link against" signature §2.7 already used to accept `SCHEDULE`/`WAIT` as a genuine scope boundary). `RUNTIME()` therefore folds into that same already-accepted boundary, not a new gap. `DATE()`/`CLOCKTIME()` are a different, separate case: confirmed via `yaHALMAT2`'s own `interp.c` that it deliberately implements them via real OS wall-clock time (`time()`/`localtime()`, per the user's own prior clarification cited in its comments) — genuinely non-deterministic/non-comparable across runs by design, confirming the *original*, pre-id-54 categorization was right for these two specifically. Database updated: id 54 → `not_a_bug`/none.
- **`datatypes_repeated_singular_inverse_unstable_result` (id 53) — RESOLVED, `not_a_bug`.** Read the real RTL source directly (`Source Code/PASS.REL32V0/RUNASM/MM14SN.asm`, public domain, not confidential). Confirmed the general (N≠2) pivot-search singularity check is exactly the previously-*suspected* class: `CTSW: LE F0,0(R1,R2)` loads the running-largest pivot magnitude into `F0` only (never clears its extended-pair partner `F1`), and after the full pivot search, `LER F0,F0` / `BE AOUT` is an EXTENDED (register-*pair*, `F0:F1`) self-compare-and-branch — the identical bug class the file's own N==2 special case demonstrates a few lines earlier verbatim (`LE F0,2(R1)` / `LE F2,4(R1)` / `SEDR F0,F2` / `BZ AOUT`), and the same class already accepted for `test_eron_goto`'s `MM14SN.asm` finding. `F1` retains whatever garbage the immediately preceding floating-point work left behind — for `029-DATATYPES.hal`, each `WRITE(6) A4I;`'s own formatting work between the three `INVERSE()` calls, exactly as this entry's original hypothesis proposed. Confirmed via direct primary-source reading (no independent JS reference needed). Database updated: id 53 → `not_a_bug`/none.
- **`write_first_write_inside_do_for_loop_missing_skip` (id 57) — RESOLVED, `not_a_bug`; the original framing was wrong on two counts.** Instrumented `handle_control`'s SKIP case (case 8) with temporary debug prints and re-ran 198-P.hal: `everEmittedField[ch]` was correctly `0` (false) on the very first `SKIP(1)`, meaning yaGPC2's own suppression logic *was* firing correctly — the loop wasn't the differentiator after all. Re-diffed 197-P.hal (no loop) against frozen `yaGPC` directly and found the **exact same** one-leading-blank-line discrepancy previously (incorrectly) reported as an exact match — a verification slip earlier in this session, not a real loop-specific effect. Read frozen `yaGPC`'s own `src/halucp.c` (`~/git/yaGPC/yaGPC/src/halucp.c`) directly: its SKIP handler has **no true-first-write suppression logic at all** — no `everEmittedField`-equivalent field anywhere in the file — it unconditionally overwrites `deferred[ch].downLines` with the SKIP's requested count whenever a positioning is deferred. That's simply the pre-id-52-fix behavior; frozen `yaGPC` is a deliberately-frozen predecessor snapshot that predates id 52's fix (found and thoroughly validated earlier this session against USA003087 Sec. 12.2's documented text and against `yaHALMAT2` on many files). yaGPC2's current behavior is correct; frozen `yaGPC`'s extra blank line here is stale reference code, not a hardware signal. Debug instrumentation removed, binary rebuilt clean. Database updated: id 57 → `not_a_bug`/none. **Lesson for future sessions**: frozen `yaGPC` is reliable where independently confirmed against real FCOS/compiled-code-trace evidence (as id 46 was) — but it is not automatically authoritative for every mechanism, since it can itself be out of date relative to yaGPC2's own, more carefully validated fixes.
- **`yagpc2`'s open queue is now fully clear.** Per the user's directive, both tools should clear their own queues before any full sweep; `yahalmat2`'s side (ids 40, 45, 50/51/55/56/etc.) is not this session's to fix — the user has separately told `yaHALMAT2`'s session to proceed on its own queue.
