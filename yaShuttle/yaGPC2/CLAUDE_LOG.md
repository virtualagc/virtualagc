# CLAUDE_LOG.md

(Cleared 2026-09-05 by Full Documentation Sync.  62 entries applied, all to
`problems.md` (8612 → 9154).  They were one continuous story — building,
linking and measuring OI340700 — so they are told as such rather than filed as
62 dated notes, and the several claims that were asserted and later retracted
are recorded at their FINAL state, with the retraction kept only where the wrong
belief is itself instructive.

- **`problems.md` §8.38**, one new subsection.  **Nine generator defects, none
  of them in the dump**: the four the `CS2PXT`/`CSAPCT` work exposed and the
  five in the `CS2IFT` builder, the two cards `CSAPCT` lost while still
  compiling clean, the outside-the-structure scalar check that finds them, and
  the two checking scripts that were themselves wrong.  The "residue is one root
  cause" subsection was rewritten: all seventeen reconstructions now exist and
  compile, so "eleven done, four remain" and "a fresh run is in flight" are
  gone, and the `CS2IFT` template derivation and the `CS2PAT` cross-check moved
  into it.

- **`problems.md` §8.39–8.46**, eight new sections.  **The build completes**
  (1218 compiled, 761 assembled, counts reconciled, the three wrongly-tombstoned
  MM_UTILITY compools and the filename-versus-unit-name test that caused them).
  **Linking, and what a score actually measures** — the five things that must be
  right, the OFTMP-root regression that a tool's pass count justified, how the
  honest denominator is arrived at, and why both overlay alternatives can be
  partly present.  **Two things the dumps do not say** — post-build changes and
  MAFGEN's unprinted all-zero fields, worth 462 sections, and the long
  `#PCPGDPL` detour they explain.  **Three defects found and fixed** — generated
  stacks, cross-phase relocations, and the assembler's unstripped
  macro-substituted name field.  **Where the accuracy stands** (7437/8292,
  89.7%).  **The tape** — plumbed end to end, blocked on the GPT overflow, plus
  the ALLOC-card check, the 17 empty critical-format decks and the silent
  `stamp_ssl_checksum` failure.  **The SDF as an independent oracle**, including
  the ICD figure layouts the text conversion lost.  **Method failures from this
  phase.**)
