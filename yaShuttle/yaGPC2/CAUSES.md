# Causes investigated, and what happened

GENERATED FROM `gpc-causes.db` BY `gpc-causes.py` -- a hand edit here is
invisible to the database and the next command silently overwrites it.

Before spending a run on anything below, ask:

    gpc-causes.py addr 1010d      # is this address already accounted for?
    gpc-causes.py search pacing   # has this idea already been tried?


## Open

- **#1** *(goal)* PARTLY SOLVED, NOT DONE. The @LH sign-extension fix makes the OPS 901/201/301 transitions COMPLETE -- the phases load, FTRMGPOV posts, the DK holds and the IOQE pool exhaustion are gone -- but the machine then CRASHES: 'invalid instruction 0xc6c6 at 0x48da', NIA=648da, BSR=12, in EVERY post-fix run.
  - evidence: I MISSED THIS BY NOT CHECKING THE STOP REASON. Stop reasons: s1 226611108, s2 226797227, s3 226277495, s4 226249685, s5 227645136, s6 225895363, s7 226493598 steps -- all 'invalid instruction 0xc6c6 at 0x48da'. The PRE-fix runs w1 and v91a end on 'interrupted (SIGINT)', the harness timeout, because the machine was parked in FPMIDLE and never crashed. So the runs I read as 'ended at 403 s on the timeout' had in fact CRASHED at a fixed ~226 million steps, and s6/s7 did not run their requested wall time for the same reason. VERIFIED BY THE USER INTERACTIVELY with retest-crt2.sh --v 36 and real MEDS: the OPS 201 load completes -- 250 blocks from 1/5/0/5 exactly as #PFCMGPT predicts, then 41 from 2/5/0/0, and the pair repeats -- and the run then stops with the same invalid instruction. The reported POLL FAIL is the CONSEQUENCE: the emulator halted, so nothing polls the display units. 0x648da lies inside NO load block of any phase -- never-loaded memory, c6c6 fill on both sides -- so the CPU branched into territory that was never loaded. (0x048da IS inside loaded regions of phases 4,5,6,7,8,10,12,15 but also holds a c6c6 padding word.) WHAT THE FIX DID ACHIEVE, and it is real: the transition completes and the phases load correctly where before the machine parked forever in FPMIDLE. The crash is the NEXT defect. CANDIDATE: 818df88's 'The sector replaces the high-order bit of a 16-bit address (sect.2.2.9)' -- PSW1=c8dae0c1 carries address field c8da and BSR=12, giving NIA=648da. METHOD NOTE: my success criterion was the ledger's own '6/5/0/0 appears', which is necessary and NOT sufficient; a run must also be checked for how it ENDED.
  - addrs `648da-648da,48da-48da` &middot; csects `FIOMNTR,FIOMDLY,FIOMCKIO,FTRMGPOV,FCMMGPOV` &middot; instrs `@LH,@RAW` &middot; procs `MSC` &middot; phases `3,8,18` &middot; config `G9` &middot; files `src/iop_msc_instr.c` &middot; symptom `no 6/5/0/0,phase 18 never requested` &middot; tsec `389-400` &middot; doc `nsts-sim-gpc 818df88; IBM-6246556A` &middot; run `s1,s2,s3,s4,s5,s6,s7,w1,v91a`
- **#44** *(flight-sw)* LEADING HYPOTHESIS: phase 8's overlay never completes because FIOMGCV's checksum-verify-and-reprotect PCT is never dispatched. No protection error is needed -- if the PCT does not run, the overlay status table never records the load blocks as SUMMED/VERIFIED/PROTECTED, so FCMMGPOV's tail is never reached and FTRMGPOV never posts the event.
  - evidence: FIOMGCV is the MM/GTG CHECKSUM PROCESSOR (0x1a134, 328 hw), called from FIOMGMTR (0x1a4c2) which sits on the completion path FPMIHPC2 case 9 -> FIOMGMTR -> FIOMGCMP (0x1a0ea). Its own header: 'CALLED WHENEVER CHECKSUM GENERATION AND SUBSEQUENT VERIFICATION IS REQUIRED ... RUNS UNDER A PCT WITH INTERRUPTS ENABLED ... RETURN IS TO FPMIHPC2 AFTER A CHECKSUM PROCESSOR PCT HAS BEEN PLACED ON THE RUN QUEUE ... AN UPDATED OVERLAY STATUS TABLE IDENTIFYING THE SEGMENT(S)(/LOAD BLOCK(S)) SUMMED,VERIFIED,PROTECTED'. So verification IS on the critical path between the transfer and the event -- correcting an earlier claim here that the overlay path only GENERATES checksums; FCMMGBOV's generation is the GTG TRANSMITTER side (gated on 'IF (CHI,R7,EQ,1) THIS GPC THE TRANSMITTER ?'), not a substitute for verification. WHY IT FITS: the data arrives perfectly (30/30 load blocks byte-identical AND passing their own checksums), the CPU is 92-100% idle in FPMIDLE through every hold with wait=0%, and the event is never posted. A queued PCT that is never dispatched looks exactly like that, and needs no protection error. TEST: landmark FIOMGCV/FIOMGMTR/FIOMGCMP and see whether they fire for phase 3 and not for phase 8 (run z5).
  - addrs `1a134-1a27b,1a4c2-1a54a,1a0ea-1a133,1935c-1935c` &middot; csects `FIOMGCV,FIOMGMTR,FIOMGCMP,FCMMGPOV,FTRMGPOV,FCMMGBOV` &middot; phases `3,8` &middot; env `YAGPC_LANDMARKS` &middot; doc `FIOMGCV.asm header` &middot; run `z5`
- **#49** *(emulator)* LEADING CANDIDATE FOR THE LAST LINK: bus 24 has NO MODEL ATTACHED, yet it carries more traffic than any modelled bus -- 4,281 transactions against 804 on each DK bus -- with a 0.51 ms median. Its requests complete almost instantly because nothing is there to take time, so FCOS re-dispatches it at a rate the real vehicle could not sustain, and FIOPDISP keeps it at the head of the active IOQE list ~48% of every DK hold. Bus 6's entry, queued behind it, is monitored roughly once per 1052 ms.
  - evidence: Bus ownership: DEU 6-9, MMU 18, MTU 20-22 (mtumodel.c MTU_BUS_FIRST/LAST). Bus 24 belongs to none of them. Transaction counts over v91a: bus 24 = 4281, bus 20 = 1248, bus 22 = 1248, buses 6/7/8 = 804/804/806, bus 21 = 181. Head writers during 388-400 s (z6, WATCHHW on TCVTMIOA 0x146 with the writing NIA): FIOPDISP at 1bd6f (FIOPSMSC+17) writes 9068 121 times and 905a 50 times; FIOPDISP at 1bd30 (FIORBTST+978) writes 9068 77 times and 90b2 44; FIOCMPLT at 1b559 (FIOCMPL2+100) clears to 0000 134 times. IOQE 9068 decodes (TFIOQ DSECT) to TIOQMNTM=00000000 80 = BUS 24. WHY THIS IS OURS AND NOT THE FLIGHT SOFTWARE'S: FIOPDISP inserting a newly dispatched request at the head is the flight software behaving correctly on the traffic it is given; what it is given is a bus with no subsystem, whose transactions cost almost nothing. Precedent in this ledger: entry 11's 40 RECV timeouts were all BCE20, 'a bus with no model attached', so unmodelled buses demonstrably do generate transactions here. TEST: give bus 24 realistic wire time, or stop dispatching an absent subsystem, and see whether the head rotates fast enough for the DK completions to keep up and the 25-entry pool to survive. CAUTION: this is a hypothesis with the right shape, NOT a measurement, and eight candidates for this link have already been refuted.
  - addrs `146-146,9068-9068,1bd6f-1bd6f,1bd30-1bd30,1b559-1b559` &middot; csects `FIOPDISP,FIOCMPLT,FIOMNTR` &middot; procs `BCE6,BCE24,MSC` &middot; files `src/run.c,src/mtumodel.c,src/iop.c` &middot; tsec `388-400` &middot; run `v91a,z6`
- **#55** *(method)* THE HEADLESS RESULT DOES NOT MATCH THE INTERACTIVE ONE. With the in-process DEU model all three transitions complete cleanly and DK polling continues; with real MEDS the user reports loading activity on MM1 READY followed by POLL FAIL and no menus, on BOTH OPS 901 PRO and OPS 201 PRO.
  - evidence: Headless (s1-s5): loads complete, FTRMGPOV posts, zero sentinel faults, DK transaction rate FLAT across the transition -- ~23 per 5 s at t=370 through t=404 in both 901 and 201 -- and the last DK transaction in every run lands at the run's final instant, so polling had not stopped. THREE CANDIDATE EXPLANATIONS, none yet ruled out. (1) BINARY TIMESTAMP: the @LH fix was built at 17:13; a run started before that has the old behaviour, and POLL FAIL is precisely its signature -- Clock 2 dies at t~397, the CPU parks in FPMIDLE, all bus dispatch stops. Not yet confirmed either way. (2) APPARATUS: deumodel.c's own header says it answers 'synchronously, in the same call, with no socket, no drops and no pacing', built to decide 'with a peripheral that cannot lose anything and answers instantly, does the load finish?' -- so real poll timing, datagram loss and the MEDS protocol are invisible to it BY CONSTRUCTION, and 'polling continues' here is a statement about a peripheral that cannot fail to be polled. (3) A DOWNSTREAM DISPLAY FAULT unrelated to this defect; there is already an open item about MEDS2-port.py rendering a clock but no menu. THE DISCRIMINATOR the user can supply cheaply: whether the MM1 READY activity runs to COMPLETION -- 250 blocks for OPS 201, 34 for 901 -- or stops partway. Completing separates 'the load failed' from 'the load succeeded and the display path failed after it', which need different investigations. ALSO UNMEASURED HERE: survival well past the transition. The headless runs end 1-3 s after it; s6 reached only 13 s past because it shared the machine; s7 runs 1150 wall seconds alone for ~700 s of post-transition time.
  - procs `BCE6,BCE7,BCE8` &middot; phases `5,6,18` &middot; env `DEUMF,DEU2` &middot; files `src/deumodel.c,pass-run/retest-crt2.sh` &middot; run `s1,s2,s3,s4,s5,s6,s7`
- **#91** *(emulator)* Inter-GPC sync codes routed between in-process machines go out over UDP and are picked up only by the receiver's periodic discrete poll, so one machine's STBY/BFS-RUN/RUN/SYNC change may not be visible to its neighbour for longer than FCOS's 3.85 ms sync timeout -- which would present as a spurious fail-to-sync and a GPC voted out, indistinguishable from a flight-software defect.
  - evidence: Not yet measured; no redundant set has been formed. The path is discretes_publish_out -> vehicle_route_out -> discretes_rotate_out -> discretes_publish_to (sendto on the peer's port 6980+n) -> the peer's discretes_poll, which run.c calls every 1024 instructions (batchrunner_step, '(r->step & 0x3ff) == 0'). At roughly 1 instruction per simulated microsecond that poll interval alone is about 1 ms of the 3.85 ms budget, before loopback latency and scheduling. The fix if it bites is to apply the rotated bits DIRECTLY into the peer's Discretes object in-process and keep the datagram only so external monitors still see the traffic; that is not built speculatively because the cost is unmeasured.
  - csects `FCMCSYNC,FCMTSYNC,FCMSSYNC,FCMISYNC,FCMSFAIL,FCMSMASK,FCMASYNC` &middot; env `YAGPC_BARRIER_US` &middot; files `src/discretes.c,src/vehicle.c,src/run.c` &middot; symptom `fail to sync,GPC voted out,3.85 ms timeout`

## Confirmed

- **#2** *(iop)* OVERTURNED BY THE DOCUMENT, and the reason correcting it 'breaks' the machine is now understood: it does not expose a SECOND defect, it exposes THE SAME ONE. exec_RAW must include the MSC's own bit (PROC_ALL). Our PROC_ALL_BCE ends FIOMDLY's delays early, which keeps the machine moving and makes the ~1053 ms holds RARE; with the correct delay they happen constantly.
  - evidence: POO IBM-6246556A, @RAW PROGRAMMING NOTES: 'Since processor 0 corresponds to the MSC, (which is busy during instruction execution), the MSC execution of an @RAW instruction will correspond to a loop until the maximum repeat count is reached.' nsts-sim-gpc 818df88 agrees, citing POO II-80. RUN r1, @RAW corrected, with YAGPC_IOQEDEPTH: the IOQE free pool falls 7 -> 2 -> 0 at t=232-233 and then RECOVERS (1 at 234, 5 at 240, 15 at 261), so the t~231 fault is the pool briefly touching zero, not a fatal second defect. And the DK holds carry THE SAME QUANTUM as the transition window -- 1055, 1053, 2101, 4264, 2164, 6361 ms at t=226.6 through 237.2 -- 9 holds all over 300 ms, against 5 in a whole 963 s baseline run. So the hold pathology is present in BOTH configurations and is independent of @RAW; our bug merely suppresses its frequency by cutting FIOMDLY's delays short. THERE ARE NOT TWO DEFECTS. Fixing the holds is the whole job, and the mask correction becomes safe once they are fixed. The earlier reading in this entry -- 'a second defect underneath' -- was wrong.
  - csects `FIOMDLY,FIOMCKIO,FIOMNTR` &middot; instrs `@RAW,@LI` &middot; procs `MSC` &middot; env `YAGPC_RAW_MSCBIT,YAGPC_IOP_UPSTREAM` &middot; files `src/iop_msc_instr.c` &middot; symptom `I/O engine stops,keys=0` &middot; tsec `230-400` &middot; doc `IBM-6246556A @RAW PROGRAMMING NOTES; nsts-sim-gpc 818df88` &middot; run `v53b,x1,x2,u1,u2,r1`
- **#20** *(method)* pass-910.mmv is NOT an original tape -- every .mmv is one we built, so it cannot serve as a control for 'the real machine did X'.
  - evidence: User correction. No provenance note exists; it merely supplies DEU load modules our build cannot generate.
  - tape `pass-910,v36` &middot; doc `user correction`
- **#21** *(method)* The authoritative hardware manuals are in the local ibiblio mirror and were unused for most of this work.
  - evidence: IBM-74-A31-016 is a SUMMARY that defers BCE detail to the BCE Principles of Operation (IBM-6246556A part 3), which is on disk and OCR'd. Going to it found the #RDL defect in minutes.
  - doc `BCE PoO IBM-6246556A pt3,IOP POO IBM-74-A31-016,AP-101 PoO IBM-6246156`
- **#22** *(iop)* ANSWERED. The ~1053 ms DK hold is ENTIRELY COMPLETION LATENCY, not bus work. The BCE finishes its bus program in about 0.3 ms in every case, long hold and short alike; the hold is the time between the BCE executing #WAT and FCOS learning about it.
  - evidence: ANSWERED IN PART. The hold is ENTIRELY COMPLETION LATENCY, not bus work: in w3 a 1052.64 ms hold and a 0.59 ms hold have identical BCE behaviour -- busy set by the SIO at +0.2 ms, #WAT clearing it at +0.29 ms -- so the bus program finishes in a third of a millisecond every time and the BCE is never the bottleneck. FOUR EXPLANATIONS OF THE LATENCY ARE NOW REFUTED, do not re-adopt: (1) BCE message timeouts, entry 11; (2) the @RAW accumulator mask, entry 2, retested and worse the second time; (3) the MSC parking on an 8.65 s repeat count, entry 39 -- measured median gap between MSC arms is 33.7 ms and only 3 of 7932 exceed 100 ms; (4) queue depth, refuted by ordering -- the first 414 ms hold is at t=390.87 and the pool sentinel fault is at t=396.99, six seconds later, so the holds PRECEDE the pool drain and cause it rather than the reverse. STILL OPEN: what makes the FIRST hold, at t~390.87, immediately after the OPS key at t~390.7 and BEFORE the first overlay entry at t=391.29. One unexplained correlation worth checking properly: in v91a that first hold ends at 391.135 and the MMU's READY->1 for its 26-block read is at 391.131, 4 ms earlier -- but the same correlation does NOT hold for the later holds, so it is suggestive only.
  - addrs `152-152,14d-14d,19b-19c,9068-9068,90b2-9273,80ce-8100` &middot; csects `FIOSVC,FIOPDISP,FIOCMPLT,FIOMNTR,FPMSVCEP,FIOCBLKS,FCMBUSCM,FPMIHPC2` &middot; instrs `#WAT,@RAW` &middot; procs `BCE6,BCE7,BCE8,MSC` &middot; phases `8` &middot; tape `v36` &middot; env `YAGPC_BWTRACE,YAGPC_WATCHHW,DEUMF` &middot; files `src/iop.c,src/iop_msc_instr.c` &middot; symptom `pool overflow,Clock 2 dead` &middot; tsec `391-399` &middot; run `v91a,w3,w4`
- **#23** *(emulator)* ANSWERED. The phase-8 overlay's unprotect walk starts at 0x1010e because 1010e lies 22 halfwords inside a PROTECTED load block based at 100f8 that FOUR phases share at different lengths: phase 4 LB9 100f8..140f5 (16382 hw), phase 6 LB11 100f8..140f3 (16380), phase 15 LB39 100f8..1017d (134), and phase 8 LB11 100f8..10129 (only 50 hw). All four are protected.
  - evidence: Decoded from #PFCMGPT (0x1ccf2) in w1's memory dump. The address had been unexplained since before this session because the load-block table was never read; it is not an arbitrary offset but an interior address of a shared, protected, phase-dependent-length load block.
  - addrs `1010e-1010e,100f8-140f5` &middot; csects `#PFCMGPT` &middot; instrs `ISPB` &middot; phases `4,6,8,15` &middot; config `G9` &middot; tape `pass-910` &middot; env `YAGPC_ISPBTRACE,YAGPC_NIARING` &middot; symptom `store protect violation,Clock 2 dead` &middot; tsec `389-392` &middot; run `w1`
- **#26** *(method)* WATCHHW emits both 'hw' and 'fw' lines; filtering to one kind silently hides half the stores.
  - evidence: A regex matching only 'WATCHHW hw addr=0019c' showed TCVTMNTR written zero 547/547 times and nearly produced a false root cause ('nothing is ever monitored'). Including fw lines showed 988 non-zero writes -- monitoring was healthy all along. Match both kinds, or the analysis is of a subset.
  - addrs `19c-19c` &middot; env `YAGPC_WATCHHW` &middot; run `v89a`
- **#28** *(method)* A complete binary bus log (YAGPC_BUSLOG + gpc-buslog.py) answers retrospectively what the 43 YAGPC_*TRACE variables each need a dedicated run to ask.
  - evidence: ~18 MB for a 1000 s run, less than one re-run. On its FIRST use it established the DK/MM correlation from a question (overlap 6 18) not anticipated when the run started, which no pre-chosen trace could have answered. It also made the overclaim visible quickly: the same data showed the flooding MECHANISM was unsupported.
  - env `YAGPC_BUSLOG` &middot; files `src/run.c,gpc-buslog.py` &middot; run `v90a`
- **#29** *(method)* PHASE 8 LOADS PERFECTLY, BY THE FLIGHT SOFTWARE'S OWN TEST. All 30 load blocks are byte-identical in memory at the destinations #PFCMGPT gives, and all 30 PASS THEIR OWN CHECKSUM there. The transfer was never the problem; the OVERLAY is what never completes.
  - evidence: Run w1. (1) #PFCMGPT (0x1ccf2) decoded from the memory dump gives phase 8 thirty load blocks, mm=0x2a80 = ADDR 52400; phase 18's mm=0x2e00 = 6/5/0/0, confirming the goal criterion from the machine itself. (2) Walking the 238 tape blocks read, each load block padded to a tape-block boundary, consumes exactly 121,856 halfwords with ZERO remainder, and all 30 load blocks are BYTE-IDENTICAL in memory at their table destinations -- 30/30. (3) Each load block ends with a 2-halfword fullword checksum slot that its length INCLUDES (mmbstamp.py: 'length: int  # hw, INCLUDING the 2-hw checksum tail'), and in memory all 30 slots equal a 16-bit sum of their own body -- 30/30 -- which is FCMINSSL's own criterion, the sum-and-compare whose third failure is a deliberate wait state. So the load is correct by the machine's standard, not merely against the tape image. (4) The 30 blocks merge into 20 CONTIGUOUS REGIONS, disjoint, spanning 002ac..38975 with 119,070 hw of gap; splits inside a region are administrative -- the 16384-hw checksum cap (LB_CAP_HW) or a change in the store-protect flag -- not gaps in memory. THE TAPE CARRIES NO METADATA: the ~19 blocks of overhead are padding to block boundaries. EVERY EARLIER CLAIM OF MISSING OR MODIFIED DATA IS RETRACTED -- '24 unaccounted', '42 mismatches', '19 modified after loading', 'the missing block is a header' -- all were artifacts of inferring layout from byte searches and extrapolating a uniform 512-hw pitch across load-block boundaries. THE LESSON: read the machine's own table. THE OVERLAY still never completes: FTRMGPOV (1935c) is reached exactly ONCE in 963 s, for phase 3, while FCMMGPOV (192ce) is entered twice.
  - addrs `1ccf2-1d137,2ac-33f,52400-52400` &middot; csects `#PFCMGPT,FCMMGPOV,FTRMGPOV,FCMMGBOV` &middot; phases `3,8,18` &middot; tape `v36` &middot; env `DEUMF,YAGPC_DEU_EXTRA_PRELOADED,YAGPC_LANDMARKS` &middot; files `src/ap101.c,ap101Utils/mmbstamp.py` &middot; symptom `overlay never completes,no 6/5/0/0` &middot; tsec `391-402` &middot; doc `mmbstamp.py:159 mm16 encoding` &middot; run `w1`
- **#31** *(iop)* The DK holds are a SWEEP, not a per-transaction delay: over the whole 963 s run buses 6/7/8 have 804-806 busy intervals with median 0.85 ms and p90 1.45 ms, and only 5-6 exceed 300 ms -- all inside t=390.7-398.5, quantized to ~1053 ms with clean 2x and 4x multiples (414/415, 1053, 1053, 1055, 1058, 1068, 2101, 2167, 2169, 4264, 4310).
  - evidence: Whole-run TCVTBCEB (0x152 fullword) transition census, v91a. Bus 24 keeps completing normally (median 0.51 ms, 4282 intervals, none over 300 ms) THROUGHOUT every DK hold, so FCOS is alive and FIOCMPLT is running and simply never clears 6/7/8. During a hold bus 6 emits ZERO wire events -- not even the per-revolution RECV_POLL that bus 18 emits while a receive is pending -- so BCE6 is not in a receive wait and is not executing. The busy bit clears and is immediately re-set 0.02-0.03 ms before the next SIO. 1x/2x/4x quantization of a fixed period is the signature of a periodic reclaim, not a timer armed per transaction.
  - addrs `152-152` &middot; csects `FIOCMPLT,FIOSVC` &middot; procs `BCE6,BCE7,BCE8,BCE24` &middot; phases `8` &middot; tape `v36` &middot; env `YAGPC_WATCHHW,YAGPC_BUSLOG` &middot; symptom `pool overflow,Clock 2 dead` &middot; tsec `390-399` &middot; run `v91a`
- **#33** *(method)* headless-gpcmem.sh's first argument is WALL-CLOCK seconds (sleep $SECS), not simulated seconds, and the emulator runs near real time -- so ask for MORE wall seconds than the simulated time you need. Separately, DEUKEYS '@Ns' lands about 20 s EARLIER than N in simulated time (@410s -> 389.65 s; @390s -> 368.01 s).
  - evidence: v92a asked for 430 and reached only 391.6 s simulated. Worse, its OPS 901 PRO went in at simulated t=368.01 and produced NO MMU command at all in the following 23 s, where v91a's key at t=389.65 drove a BITE STATUS within 1 ms -- so PASS is not ready for the transition at 368 s and the run is silently wasted. Validate a transition run by 'mmu1: read' lines after the key, not by keys=2 alone: v92a had keys=2 and no transition.
  - env `DEUKEYS,PORT_BASE` &middot; files `pass-run/headless-gpcmem.sh` &middot; symptom `no transition,keys=2 but no reads` &middot; tsec `368-390` &middot; run `v92a`
- **#34** *(method)* MMU read addresses decode as mm16 = 00|File(3)|Track(3)|Subfile(3)|Block(5) (mmbstamp.py:159), and the model's log prints them as track/file/subfile/block. Against CON80 MMUSYS1/MMUDAT1: 33600=GMAGNFB1 PH=3, 52400=GMAGNC91 PH=8, 56000=GMAG9R1 PH=18. GMAGNC91 is BLKS=384 from 52400 and a load block may not cross the 256-block track boundary, so a full phase-8 read appears as TWO operations, the second at 53000 -- same csect, next track, NOT a different phase.
  - evidence: Decode only -- says where a read was AIMED, not that anything loaded (see #29). This is why 6/5/0/0 is the right criterion for phase 18: MMUSYS1 GMAG9R1 PHASE,PH=18,MC=9,TFL=Y with MMUDAT1 ADDR=56000 gives File 5 / Track 6 / Subfile 0 / Block 0 for area 1.
  - csects `GMAGNC91,GMAGNFB1,GMAG9R1` &middot; phases `3,8,18` &middot; tape `v36` &middot; files `OI340700/CON80/MMUSYS1,OI340700/CON80/MMUDAT1,src/mmumodel.c` &middot; doc `mmbstamp.py:159 mm16 encoding` &middot; run `v91a`
- **#35** *(method)* A TRANSITION RUN NEEDS **TWO** ENVIRONMENT SETTINGS THAT ARE NOT THE DEFAULT: DEUMF=1 (DEU major function GNC; the harness default is 0 = PL) and YAGPC_DEU_EXTRA_PRELOADED=1 (with DEU2="7,8,9"). Miss EITHER and OPS,9,0,1,PRO does nothing at all -- no mass-memory read -- while keys=2 is still reported, so the run looks exactly like a transition that failed.
  - evidence: DEUMF is the decisive one and headless-gpcmem.sh's own comment says so: DM6OPS resolves an OPS request through the GRT keyed on the REQUESTING major function -- MF PL OPS 9 is GRT index 6, GPC set HEX'4400' (GPC 2 alone), while MF GNC OPS 9 is index 9, set HEX'F000' (GPCs 1-4). Running as GPC 1 with MF=PL, every OPS 901 PRO is refused 'NO TARGETS IN RUN', which reads like a missing overlay and is nothing of the kind. CONFIRMED BY LOG: v90a and v91a print 'bus 6 major function 1 (GNC)'; v92a-v99a and both key sweeps print 'major function 0 (PL)'. YAGPC_DEU_EXTRA_PRELOADED is the second, independently confirmed: v90a/v91a show ipled=true on ALL FOUR DEUs including the bus-9 unit with commands=0 (never loaded by the GPC, so it can only have been preloaded), and without it buses 7/8/9 come up unloaded and get BITEd (v97a: 476 BITE on bus 8) -- with it restored, v99a reproduced v91a's DK signature exactly (bus6==bus7, 30929/30929/30929 at t=240 vs v91a's 30607, bus10=16/bus11=32 at t=180, bus24=99750) yet still did not transition, which is what isolated DEUMF as a SEPARATE second requirement. FIVE OTHER EXPLANATIONS WERE TRIED AND ALL REFUTED -- do not re-adopt any: (1) wall-clock key-gate drift; (2) DEU count alone (v97a had four and failed); (3) the ITEM key's poll number (214/215/216/217 byte-identical); (4) the OPS key's poll number (556/557/558/560, simt 388.36-390.26, bracketing v91a's 389.64, all failed); (5) a source regression -- a bisect showed c23856793 AND c23856793-minus-BWTRACE both reproduce the failing behaviour exactly, so no source change this session caused it. The runs are deterministic: v91a and v98a match to the microsecond through all eight IPL reads at t=136.49 s. VALIDATE A RUN BY: 'major function 1 (GNC)', four DEUs all ipled=true, and a 'mmu1: read' later than the key.
  - csects `DM6OPS` &middot; procs `BCE6,BCE7,BCE8,BCE9` &middot; tape `v36` &middot; env `DEUMF,DEU2,YAGPC_DEU_EXTRA_PRELOADED,DEUKEYS,GPC_ID` &middot; files `src/deumodel.c,pass-run/headless-gpcmem.sh` &middot; symptom `no transition,no 6/5/0/0,DEU bite storm` &middot; tsec `389-400` &middot; run `v90a,v91a,v97a,v98a,v99a,vk556,vk558,w1`
- **#37** *(method)* Phase 8's presence in main storage is now DIRECTLY testable: dump memory with GPCARGS="--dump-state <prefix>" + YAGPC_DUMPSTATE_AT=<secs>, then /tmp/claude-1000/verify_phase8.py searches the image for distinctive 96-halfword interior slices taken from phase 8's own blocks on the .mmv tape. The instrument is validated by a positive control in the same image.
  - evidence: v96a (no transition): phase 3's control blocks from 3/3/6/0 are 20/20 FOUND, at addresses exactly 0x200 halfwords apart -- one block apart, contiguous, unmistakably a real load. Phase 8's blocks from 2/5/4/0 and 3/5/0/0 are 6/173, identical in all three snapshots (386, 403, 425 s). So the search finds a load when there is one, and phase 8 is genuinely absent from memory in a run that did not transition. What remains is to run it on a run that DID transition: 6/173 -> ~173/173 means phase 8 loaded, 6/173 means it did not. Complementary test in memdiff.py scores the before/after changed ranges against csects PHASE08 links and PHASE03 does not (opposite failure mode: byte-search can false-negative if the loader transforms data, the diff can false-positive on scratch).
  - phases `3,8` &middot; tape `v36` &middot; env `YAGPC_DUMPSTATE_AT` &middot; files `src/run.c,tmp verify_phase8.py,tmp memdiff.py,tmp tapeblocks.py` &middot; run `v96a`
- **#38** *(flight-sw)* PHASE 8'S OVERLAY DIES IN THE OVERLAY BODY, BEFORE HOUSEKEEPING, KILLED BY THE IOQE SENTINEL. FCMMGPOV's tail is: SVC FCMOVDSA / FCMSSM18 / SSM / release buffer / CALL FPMRLPCT / COPY FIOSTEVT / FTRMGPOV. For phase 3 that whole tail runs. For phase 8 it is never entered at all.
  - evidence: Run w2, landmarks with LANDMARKS_AFTER=380. FPMRLPCT (1af12) is hit 13 times, #12 and #13 at t=392.6159/392.6168 immediately before overlay-complete #1 (FTRMGPOV, 1935c) at 392.6169 -- phase 3's housekeeping running normally. ovp-entry #2 (192ce) at 392.6177 and loadblock #2 (18fbe) at 392.6182 follow, and then FPMRLPCT is NEVER HIT AGAIN for the rest of the run. So the second overlay never reaches CALL FPMRLPCT, which sits before FIOSTEVT and FTRMGPOV. YAGPC_PGMTRACE names the killer in the same run: 'PGMCHK code=0007 at=1be4b lastProt=080d6 t=398.039549' -- store-protect violation, 1be4b = FIOSVC+13 (csect 1be3e), lastProt=080d6 inside FPMSVCEP (080ce). That is the deliberate TIOQNXT sentinel of entry 22: the IOQE pool drains, FIOSVC walks off the end onto 080ce, and the store-protect check kills Clock 2 mid-overlay. CONSEQUENCE: entry 22 (the ~1053 ms DK bus holds) is ON THE CRITICAL PATH for the goal, not a side issue. NOTE a landmark on 080ce does NOT detect this -- 080ce is a VALUE FIOSVC walks onto, not code the CPU executes; the arming address to watch is the program-check handler FPMIHPGM (1ad5c), which does fire.
  - addrs `1be4b-1be4b,080ce-08100,1af12-1af12,1935c-1935c,192ce-193e0` &middot; csects `FCMMGPOV,FTRMGPOV,FPMRLPCT,FIOSVC,FPMSVCEP,FPMIHPGM` &middot; phases `8` &middot; tape `v36` &middot; env `YAGPC_LANDMARKS,YAGPC_PGMTRACE,DEUMF` &middot; symptom `overlay never completes,pool overflow,Clock 2 dead` &middot; tsec `392-399` &middot; run `w2`
- **#41** *(iop)* THE LONG DK HOLDS HAPPEN EXACTLY WHILE MASS MEMORY IS TRANSFERRING. DK completion latency is gated by MM activity: while the MMU is busy, a DK transaction that has already finished its bus program waits, and it is released when the MM transfer ends.
  - evidence: Two independent runs, identical results. v91a and w3, bus 6, 804 busy intervals each: LONG holds (>300 ms) overlapping an MMU-busy window 5 of 5; SHORT holds 2 of 799. The MMU-busy windows are taken from the model's own READY->0 / READY->1 pairs. Hold durations are identical across the two runs (414.1, 1052.6, 1054.7, 2101.1, 4263.8 ms), so this is deterministic. Combined with entry 22 -- the BCE executes #WAT at +0.29 ms in every hold, long or short -- the picture is: the bus program finishes at once, and the COMPLETION is withheld for as long as mass memory is moving data. THIS IS THE CAUSE OF THE FIRST HOLD, which was the last thing left open: the first hold starts at t=390.72/390.87, inside the MMU-busy window for the 26-block phase-3 read (390.480-391.131 in v91a), and ends 4 ms after that read's READY->1. NOTE ON AN EARLIER RETRACTION: 'the MMU floods the servicer' was proposed and retracted in the same turn on the argument that service calls cost wall time rather than simulated time. That argument does not address this correlation and the retraction was premature. WHAT IS NOT YET ESTABLISHED: the mechanism inside our code. Candidates are the shared DMA request path (iop.c, the queue feeding iop_watch_store's 'dma' writes), and the MSC monitoring only the TOP ACTIVE IOQE (FIOMNTR0 takes its mask from TCVTMIOA) so that an MM transaction at the head of the list withholds attention from the DK buses for its whole duration -- which is NOT the general queue-depth idea refuted separately, since it depends on the MM entry being at the head rather than on how many entries exist.
  - addrs `152-152,14d-14d` &middot; csects `FIOMNTR,FIOCMPLT` &middot; procs `BCE6,BCE7,BCE8,BCE18,MSC` &middot; phases `3,8` &middot; env `YAGPC_WATCHHW,YAGPC_BWTRACE` &middot; files `src/iop.c,src/mmumodel.c` &middot; symptom `1053 ms DK holds,pool overflow` &middot; tsec `390-401` &middot; run `v91a,w3`
- **#42** *(method)* YAGPC_POISON (range fill at a simulated time, ap101.c) answers 'did the loader WRITE here' where content comparison structurally cannot -- but it CANNOT be pointed at an overlay's destination, because an overlay's destination is LIVE MEMORY and poisoning it kills the machine before the transfer happens.
  - evidence: Run z1 wrote 0xdead into 14,848 halfwords across 23 ranges at t=386 -- the computed destinations of phase 8's 24 'not found anywhere' blocks plus its 5 fill blocks. The poison took (14,844 still 0xdead at the t=387 snapshot, 4 overwritten by normal activity). But the run produced NO transition at all, zero mass-memory reads after the key, so the headline result -- 100% of the poison still intact at t=403 -- means nothing: there was no transfer to overwrite it. Poisoning 14.8K halfwords of running code and data is what broke it; that is what an overlay destination IS. THE NON-INVASIVE FORM WORKS INSTEAD: YAGPC_WATCHHW already logs IOP DMA writes tagged with pe=, so watching a destination range during the transfer distinguishes the same three cases -- written with tape bytes, written with transformed bytes, never written -- without modifying anything (run z2). KEEP THE HOOK: poison is still the right instrument for a destination that is NOT live before the write, and it is the only way to test a block whose correct content is zeros or fill, where comparing content answers the same either way.
  - phases `8` &middot; env `YAGPC_POISON,YAGPC_WATCHHW` &middot; files `src/ap101.c` &middot; symptom `no transition` &middot; run `z1,z2`
- **#45** *(iop)* THE DK COMPLETION LATENCY IS QUEUE POSITION, NOT QUEUE DEPTH. FIOMNTR0 takes the MSC's monitor mask from the TOP ACTIVE IOQE (TCVTMIOA), so the MSC watches ONE request at a time; during a 1052 ms hold it checks for bus 6's completion exactly ONCE.
  - evidence: Run z4, YAGPC_REPEATTRACE extended to print the accumulator (which IS the monitor mask). Inside the 392.251..393.303 hold, 108 MSC arms: pc=1cb6c acc=ffffffff x72 (FIOMDLY's delay, all buses + MSC, not a monitor), and pc=1cb70 -- the I/O-complete check -- acc=00000080 x27 watching bus 24, acc=00000800 x8 watching bus 20, and acc=02000000 x1 watching BUS 6. Same shape in the other holds (bus 6 once, bus 7 once, bus 8 once). So bus 6's completion is looked at about once per 1052 ms while the MSC spends its passes on buses 24 and 20. THIS IS DISTINCT FROM THE REFUTED QUEUE-DEPTH IDEA: it does not depend on how many IOQEs exist, only on which one is at the HEAD, so it is untouched by the ordering argument (holds precede the pool drain) and by y1 (hold quantum invariant to MM pacing). OPEN: whether monitoring one IOQE at a time is FCOS's design working as intended -- in which case our IOQE ordering or the rate at which the head advances is what differs -- or an artifact of how TCVTMIOA is maintained here.
  - addrs `1cb6c-1cb70,152-152` &middot; csects `FIOMNTR,FIOMCNTL` &middot; procs `MSC,BCE6,BCE20,BCE24` &middot; env `YAGPC_REPEATTRACE` &middot; tsec `390-400` &middot; run `z4`
- **#46** *(flight-sw)* PHASE 8'S OVERLAY STOPS AT AN EXACT GATE: FIOMGMTR never sets TIOQLTMM, so every monitor pass takes the 'ANOTHER I/O TRANS REQUIRED' branch and calls FIOMGDSP instead of FIOMGCV. Verification, and therefore completion, is on the other side of that flag.
  - evidence: Run z5, landmarks on FIOMGCV (1a134), FIOMGMTR (1a4c2), FIOMGCMP (1a0ea). PHASE 3: ovp-entry 394.141, monitor passes at 394.398/394.598/394.798/394.998/395.198, cksum-verify on the FIFTH pass at 395.198, mm-complete 395.337, overlay-complete 395.338. PHASE 8: ovp-entry 395.339, monitor passes every 200 ms from 396.078 through 398.678 and beyond, and cksum-verify NEVER fires, mm-complete never fires, overlay-complete never fires. FIOMGMTR.asm's structure: IF (TB,TIOQFLG1,TIOQLTMM,Z) 'ANOTHER I/O TRANS REQUIRED?' -> ... IF (TB,TIOQFLG1,X'0200',O) 'TERMINATE TRANSACTION?' -> SB TIOQFLG1,TIOQLTMM, ELSE CALL FIOMGDSP 'DISPATCH/START NEXT I/O TRANS'; the ELSE of the outer test is where CALL FIOMGCV lives. WHY TIOQLTMM IS NEVER SET: phase 8 needs TWO reads (123 blocks then 115, because its allocation crosses a track boundary). Read 1 is dispatched at 396.079 and finishes about 399.2. FIOMGDSP then needs a free IOQE to dispatch read 2 and the 25-entry pool is drained by the DK holds, so read 2 does not go until 401.712 -- after the store-protect program checks at 400.881 and 401.238 have killed Clock 2. Phase 3 needs only ONE read (26 blocks) and so reaches the terminate test on its first completion. THIS EXPLAINS WHY PHASE 3 SURVIVES AND PHASE 8 DOES NOT, which nothing before did.
  - addrs `1a134-1a27b,1a4c2-1a54a,1935c-1935c` &middot; csects `FIOMGMTR,FIOMGCV,FIOMGDSP,FTRMGPOV` &middot; phases `3,8` &middot; env `YAGPC_LANDMARKS` &middot; tsec `394-402` &middot; doc `FIOMGMTR.asm MMU/BCE IDLE PROCESSING` &middot; run `z5`
- **#48** *(flight-sw)* THE IOQE CHAIN READ DIRECTLY OUT OF MEMORY. The head-hogging entry is a BUS 24 request, not mass memory; the DEU fill backlog is five IOQEs held by ONE PCT; and the MM transaction's LTMM flag IS eventually set -- so the TIOQLTMM gate is a SYMPTOM, not the defect. The fix must stop the pool draining, not change the gate.
  - evidence: w1 snap-403, decoded with the TFIOQ DSECT from MLIB80/TFIOQ.asm (TIOQNXT +0, TIOQPCT +1, TIOQSELF +2, TIOQMNTM +4 fullword bus mask, TIOQBUFA +6, TIOQFLG1 +7). Entries seen at the head during the holds: 9068 mask=00000080 = BUS 24 (head ~48% of every hold), 905a = bus 20, 90c4/90fa/9130/9178 = bus 7, 910c = bus 8, 9166 = bus 6, 90b2 = bus 18. THREE FINDINGS: (1) the entry that dominates the head is bus 24, not the mass-memory transaction -- so the DK holds are not the MM transfer holding the head. (2) PCT 83a8 owns FIVE simultaneous IOQEs -- buses 6, 7, 7, 7 and 8 -- which is AIG_DEU_LOADER's fill backlog seen directly in the queue rather than inferred from free-pool depth. (3) 910c's TIOQNXT is 80ce, the FPMSVCEP sentinel, so it is the chain tail and FIOSVC walking one past it lands exactly on the store-protect fault we see. (4) MOST IMPORTANT: 90b2, the MM IOQE, has flg1=0c00 at t=403 -- MM-TAPE-POSITION and TIOQLTMM BOTH SET. The 'last MM transaction' gate that FIOMGCV waits behind WAS eventually satisfied; FIOMGMTR would have taken the verification branch on its next pass. The machine had already died. So entry 'phase 8 stops at the TIOQLTMM gate' describes a symptom: the gate closes in time only if the pool survives long enough for FIOMGDSP to dispatch read 2 promptly.
  - addrs `9068-9068,905a-905a,90b2-90b2,9166-9166,910c-910c,80ce-80ce,146-146` &middot; csects `FIOMGMTR,FIOMGCV,FIOSVC,FPMSVCEP,AIGDEU` &middot; procs `BCE6,BCE7,BCE8,BCE18,BCE24` &middot; phases `8` &middot; files `MLIB80/TFIOQ.asm` &middot; run `w1,z6`
- **#50** *(emulator)* WE OVERRIDE THE PER-BUS MESSAGE TIMEOUTS THE FLIGHT SOFTWARE PROGRAMS. FCMINIOP loads every BCE's MTO explicitly, and src/iop.c's RECV_TIMEOUT_FLOOR_US = 2000.0 forces a 2 ms floor over any bus whose programmed value is shorter -- which is every bus except 6-9, 12-13 and 18-19. Upstream has already corrected this: 'The receive time-out floor is zero: the loaded time out governs.'
  - evidence: FCMINIOP.asm programs the timeouts with PC (PCO) commands, one spin = 16.5 us: FCMTMB1 DC F'2' = 33 us for buses 1-5, 10-11, 14-17 and 20-23; FCMTMB2 DC F'37' = 610.5 us for buses 12-13; FCMTMB3 DC F'3' = 49.5 us for BUS 24 ('TIME BUS 24  49.5 MICS'); FCMTMB4 DC F'303' = 5 MILLISECONDS for buses 6-9, the DK buses; FCMTMB5 DC F'118784' = 1.959936 SECONDS for buses 18-19, mass memory. Our floor turns bus 24's 49.5 us into 2 ms (40x) and the 33 us buses into 2 ms (60x). CONFIRMED UPSTREAM: nsts-sim-gpc commit 818df88 'gpc: addressing, timer and bus-rate corrections in the CPU and the IOP' lists 'The receive time-out floor is zero: the loaded time out governs' alongside 'A MIA presents one received word per 33 us bus word time' and '@RAW keeps accumulator bit 0 (POO II-80)'. THAT LAST ITEM MATTERS FOR ENTRY 2: upstream KEEPS the MSC bit, citing the Principles of Operation II-80, which is the change entry 2 records as refuted twice here. Tested in ISOLATION it is catastrophic (run x2). Upstream lands it as part of a COORDINATED SET with receive pacing, a zero timeout floor, and #LBR@/#BU@ taking an 18-bit value from one fullword per BCE at ADDRESS + 2 x BCE#. So entry 2's refutation may be an artifact of changing one item of a set. DO NOT retry the mask alone; port the set.
  - addrs `0-0` &middot; csects `FCMINIOP` &middot; instrs `@RAW` &middot; procs `BCE6,BCE24,BCE18,MSC` &middot; env `YAGPC_RECV_FLOOR_US,YAGPC_RAW_MSCBIT` &middot; files `src/iop.c,SSSRC/FCMINIOP.asm` &middot; doc `nsts-sim-gpc 818df88; POO II-80` &middot; run `x2`
- **#54** *(iop)* THE HOLDS ARE THE INTERVAL BETWEEN SUCCESSIVE FIOMCKIO CHECKS OF THAT BUS. The MSC checks a bus once, with count=0 ('JUST CHECK IT'), at the same instant FIOPDISP marks it busy -- BEFORE the BCE has run -- so the check fails, and the MSC does not look at that bus again for the whole hold.
  - evidence: Run r2, @RAW corrected so FIOMDLY's delays are honoured, YAGPC_REPEATTRACE printing the accumulator (the monitor mask). Checks of bus 6 are the pc=1cb70 arms with acc=02000000, and they fall at t=227.5180, 228.5706, 230.6718 and 234.9357 -- intervals of 1.053 s, 2.101 s and 4.264 s, EXACTLY the measured hold durations. Every one arms count=0. The first lands at 227.5180, the same instant TCVTBCEB's bus-6 bit is set by FIOPDISP, while the BCE does not execute #WAT until +0.29 ms (entry 22), so the check tests a BCE that has not started and fails. THE LONG SLEEPS COMPOUND IT: at t=232.9512 FIOMDLY arms count=60136 = 1984.5 ms, and 232.951 + 1.985 = 234.936, exactly where the next hold begins -- while the MSC sleeps on one entry's time-to-go nothing else is watched. FIOMNTR2's design is to DELAY until the monitor window and THEN check ('@BNN FIOMDLY *BEFORE MONITOR WINDOW. GO DELAY.' / '@B FIOMCKIO'), so a check arriving immediately with count=0 means TCVTMTTG is reporting the request already due. THAT IS THE NEXT QUESTION: why TCVTMTTG says a just-dispatched request is already past its monitor window. Note this is visible ONLY with @RAW corrected -- our PROC_ALL_BCE bug cuts FIOMDLY's delays short, which hides the whole structure.
  - addrs `1cb6c-1cb70,19b-19b,152-152` &middot; csects `FIOMNTR,FIOMDLY,FIOMCKIO,FIOPDISP` &middot; instrs `@RAW,@LI` &middot; procs `MSC,BCE6` &middot; env `YAGPC_REPEATTRACE,YAGPC_RAW_MSCBIT` &middot; tsec `226-238` &middot; run `r1,r2`
- **#56** *(tape)* The 0xc6c6 crash at 0x648da is an UNFILLED ZCON TABLE, not a CPU addressing defect. The SCAL at 0x44839 (d0ff 39de: AM=1 indexed, B2=3 so no base, X=1, IA=1, I=1, disp=0x1de) fetches a fullword indirect address pointer from 0x001de, which is the CSECT #ZDCDDG9. In our image that fullword is c6c6c6c6 -- IPL fill, never written. Decoded as a pointer, c6c6 gives XC=0, C=1, CB=1, CD=0, BSR=12, DSR=6, so C=1/CB=1 replaces the PSW BSR with 12 and XC=0 post-indexes by R1's high half (0x0214): 0xc6c6+0x0214 = 0xc8da, high bit set, sector 12 -> 0x648da, which is fill, hence 'invalid instruction 0xc6c6'. Every digit of the crash is accounted for. THE EMULATOR IS CORRECT HERE; the data it read is not.
  - evidence: All 8 DASS dumps (G2,G3,G8,G9,G16,P9,S2,SSW) agree and have real ZCONs exactly where ours has c6c6 fill. Holes in ours: 0x1d6-0x1e5 (8 fullwords) and 0x1f6-0x1f9 (2). DASS values: 1d6=de62 0e30 (BSR3), 1d8/1da/1dc=ddcc 0e30, 1de=83aa 0e60 (BSR6 -> DCD_DG9 at 0x303aa), 1e0/1e2=c074 0e40 (BSR4), 1e4=8022 0e40, 1f6=888c 0e90 (BSR9), 1f8=80f8 0e20 (BSR2). Neighbours 0x1c0-0x1d5 and 0x1e6-0x1f5 are IDENTICAL to DASS in our image, so the table is loaded but incomplete. Every REAL ZCON here carries XC=1 (no post-indexing) -- only the c6c6 fill has XC=0, which is why the wild branch post-indexes at all, and why unfilled ZCONs at DIFFERENT table slots all land on the SAME 0x648da: the target depends only on the index register, not on which slot was read. That answers the user's objection that OPS 201 PRO fails identically without going through DCD_DG9 -- it need only hit SOME ZCON in the hole. SEPARATE DISCREPANCY, not part of this: 0x1e8 is 117c in ours but 88be in all 8 DASS dumps (both 0e20).
  - addrs `1de,1d6-1e5,1f6-1f9,1e8,44839,648da,303aa` &middot; csects `#ZDCDDG9,#ZDCDDG1,DCD_DG9,#CDCDDOW` &middot; instrs `SCAL` &middot; procs `CPU` &middot; phases `8,18` &middot; config `G9,P9,S2` &middot; tape `v36` &middot; files `src/cpu.c,src/cpu_instr.c` &middot; symptom `invalid instruction 0xc6c6 at 0x48da,POLL FAIL,NIA=648da,BSR=12` &middot; doc `Shuttle GPC Software Model AP-101S.pdf p195 sec 9.7 STACK CALL` &middot; run `c2,c3`
- **#57** *(tape)* ROOT CAUSE of the 0xc6c6 crash: the ROOT LINK leaves every cross-phase Z-CON unresolved. A HAL/S call into a procedure that lives in an OVERLAY phase goes through a fullword indirect address pointer (a Z-CON) held in the RESIDENT root at a fixed low address. The compiler emits the cell as 8000 0E00 -- offset 0 with the sector bit set, XC=1 C=1 CB=1 BSR=0 -- and only the LINKER can finish it, because only the linker knows which sector the overlay's code landed in. Our root link does not: the #C... targets are not in the root's own link, so the cells stay unrelocated and reach the volume as IPL fill C6C6C6C6. 18 Z-CONs are affected, all in 0x1d6-0x243.
  - evidence: /tmp/claude-1000/c80boot/PHASE02.lib (the root image staged for the tape) shows the UNRELOCATED form directly: 8000 0e00 at 0x1d6,1d8,1da,1dc,1de,1e0,1e2,1e4,1ec,1ee,1f0,1f2,1f6,1f8 -- exactly the cells that are fill on the tape. phase/PHASE02.fcm shows the same signature at 0x1f0,1f2,1f6,1f8,204,20e, and link/P15.fcm at 0x1d6. By contrast the WHOLE-MEMORY links (link/G9.fcm, G9-r.fcm, G9-oracle.fcm and every other link/*.fcm) get it RIGHT -- not one of them has fill at 0x1de -- because they link all phases together. NOT a loader defect: the hole is in the .mmv itself (OI340700-v36boot.mmv, slot 768, load block dest=0x1a8 len=160), and 83aa0e60 / de620e30 appear NOWHERE on the tape. NOT an overlay-writes-it-later defect: #PFCMGPT says only phase 10's LB0 (00000..03c1f) covers 0x1de, and OPS 901 loads phases 3, 8 and 18 -- phase 18's 8 load blocks touch nothing below 0x1000, nor do phase 4's (002ac/005a2/00602). So no phase loaded in the run rewrites the cell; it has to be right in the root image.
  - addrs `1d6-1e5,1f6-1f9,234-243,1de` &middot; csects `#ZDCDDG1,#ZDCDDG9,#ZDCDDS4,#CDCDDOW,PHASE02` &middot; phases `2,10,18` &middot; config `G9,P9,S2` &middot; tape `v36,v37` &middot; files `tools/patch_root_zcons.py` &middot; symptom `invalid instruction 0xc6c6 at 0x48da,8000 0e00 unrelocated zcon` &middot; run `c2,c3`
- **#59** *(tape)* THE BUILD FIX: the ROOT (PHASE02) link has 678 UNRESOLVED RELOCATIONS across 304 distinct symbols, and all 18 of the crash-causing Z-CON targets are in that list. The repair is to PIN the overlay-resident #C... targets in the root link's external-symbol list so lnk101 can relocate the Z-CONs it already places. extsyms-02.json exists (556 entries) and contains NOT ONE of them. Entry shape is name -> {start,end,type,hal|contents}, addresses absolute in HALFWORDS -- e.g. ACOS is {'start':291068,'end':291183} = 0x470fc, which matches its relocation target exactly, so the addresses in link/G9-symbols.json 'sections' can be used directly (#CDCDDG9 start=197546=0x303aa).
  - evidence: /tmp/claude-1000/c80boot/PHASE02/PHASE02.sym.json -- the root link that actually fed OI340700-v36boot.mmv -- unresolvedRelocations = 678, 304 distinct symbols, including every one of #CDCDDG1/2/3/8/9, #CDCDDS2/4/8, #CDG9LIG, #CDGNLIG, #CDKFCM1/2/3/4/5/6/8/9. The 18 Z-CONs are only the currently-FATAL subset; the largest holes are #PCDHMMU (179 refs), #PCDCSPC (29), #PCVWMMU (12), FIOTBLBF (12), FIOSWDCT (12), #PCDIMMU (9). CONTRAST: the whole-memory links in link/ have only 22 unresolved (FIOG9OSW x8, FIOMUWB2 x8, six FSVC*) and get all 18 Z-CONs right, because every phase is in the same link. BEWARE STALE TREES: ~/pass-build/OI340700/phase/ (2026-09-06) is NOT the build that cut the v36 tape (2026-09-09) -- its PHASE02 has only 6 unresolved and a DIFFERENTLY PACKED Z-CON table (the missing sections shifted later ones into their slots), and neither phase/ nor c80boot/ reproduces the tape's low memory exactly. Check which tree a claim came from before trusting it.
  - addrs `1d6-1e5,1f6-1f9,234-243` &middot; csects `PHASE02,#CDCDDG9,#PCDHMMU,#PCDCSPC,extsyms-02` &middot; phases `2` &middot; tape `v36` &middot; files `phase/extsyms-02.json` &middot; symptom `unresolved relocation,8000 0e00 unrelocated zcon`
- **#60** *(tape)* A CORRECT ROOT LINK ALREADY EXISTS IN THE TREE: pass-build/OI340700/phase3/PHASE02.lib (2026-09-05) resolves all 18 cross-phase Z-CONs and matches the DASS dumps on 159 of the 160 halfwords of the root load block at 0x1a8-0x247. The root link REGRESSED after that date. Do not hand-write extsyms pins before finding what phase3/ did differently -- that is the cheap repair.
  - evidence: Match counts against mafgen/G9.fcm over 0x1a8..0x247: phase3/PHASE02.lib 159/160 (only 0x1f8, 01f0 vs 80f8 -- the high-bit-clear defect of entry #58); phase/PHASE02.lib (09-06) 96/160; c80boot/PHASE02.lib (09-08, the staging closest to the v36 cut) 96/160 with 16 cells absent entirely; the v36 tape as shipped 120/160. The later builds do not merely leave cells unresolved, they PERMUTE the table: phase/ and c80boot/ disagree with DASS from 0x1ac onward (f104/f04f/f644... against f256/f2f4/f46c...), which is what happens when missing sections let later Z-CONs pack into earlier slots. phase3/PHASE02.lib has 83aa at 0x1de, the correct #ZDCDDG9.
  - addrs `1a8-247,1de,1f8` &middot; csects `PHASE02,#ZDCDDG9` &middot; phases `2` &middot; tape `v36` &middot; files `pass-build/OI340700/phase3/PHASE02.lib` &middot; symptom `unresolved relocation,permuted zcon table`
- **#62** *(tape)* UNRESOLVED CODE OPERANDS in the running image, found by a tree-independent fingerprint: a halfword that is 0000 in our running image where every DASS configuration whose SURROUNDING code matches ours agrees on a non-zero value. 35 hits; 27 in FIOPDSMU ('VCO/SM COMMON IOP DISPATCHER', entered with an IOQE address in R1), which references labels inside the root's FIOCDATS (FIOPLNDX, FIOPL10, FIOPL01, FIOPLCYC, FIOCF40x...). FIOPDSMU is NOT resident: #PFCMGPT shows it carried by overlay phases 4,5,6,7,8,9,15, and of its FOUR copies on OI340700-v37boot.mmv THREE are correctly resolved and only PHASE 8's has the zeros -- so it bites OPS 901 (which loads phase 8) and not OPS 201 (phase 5).
  - evidence: Examples at the 901 fault site: 1ec11 eaf3 0000 vs DASS eaf3 a04c (FIOCDATS+0x174), 1ec19 eef3 0000 vs eef3 9f82 (FIOCDATS+0xaa), 1ec2d aef3 0000 vs aef3 a039 (FIOCDATS+0x161) -- RS operands with B2=3, i.e. the displacement IS the address, so the dispatcher addresses the PSA near 0. Matches the b901 program check PGMCHK code=0007 at=1ec20 (FIOPDSMU+0x76) lastProt=0008f (FCMPSA+0x8f) t=274.46, which PRECEDES the twenty IOQE-sentinel faults at=1be4b lastProt=080d6 from t=291.48 -- the pre-@LH-fix pool-exhaustion signature, returned 9 s after phase 18 loads. The DASS configs split into two layout families at this address (G9/P9/S2/SSW vs G2/G3/G8/G16), so the fingerprint uses only the configs whose neighbourhood matches ours. A first attempt that cross-referenced c80boot's unresolved-relocation list against the running image was INVALID -- c80boot places modules at different addresses from v36 -- and was discarded.
  - addrs `1ebaa-1ee25,1ec20,1ec12,1ec1a,1ec2e,8f,1be4b` &middot; csects `FIOPDSMU,FIOCDATS,FCMPSA,FIOSVC` &middot; phases `8,4,5,6,7,9,15` &middot; tape `v37,v38` &middot; env `YAGPC_PGMTRACE` &middot; symptom `POLL FAIL,PGMCHK 0007,ioqe sentinel` &middot; run `b901`
- **#63** *(tape)* Three RESIDENT FCOS I/O routines call a subroutine at ADDRESS 0: FIOSVCP+0x29 (0x1a7ef), FIOCMPLT+0x1be (0x1b63e) and FIOPDISP+0x27d (0x1bbf7) each hold 'e7f3 0000' = BAL R7 with no base, where all eight DASS dumps have e7f3 f660 / de90 / de62. Also /bin/bashDGRGSE+0x122/+0x12e (ee2a, a324) and an address constant in #DDGRGSE+0x2e (a4aa). Being resident they affect EVERY major mode, so they are the lead for OPS 201's POLL FAIL, which shows no program checks. Two other hits (#PCDLANN+0x122=0002, FIOCBLKS+0x2dc=7c00) are data that may legitimately be runtime state and were NOT filled.
  - evidence: Confirmed by #65: filling these six cells is the one effective difference between OPS 201 runs that fall silent (D201, r201, b201 on v37) and runs that keep polling (B201, B201b, V201, Z201); also fixes OPS 301 (K301 silent on v37, V301/Z301 steady on v39/v41).
  - addrs `1a7ef,1b63e,1bbf7,43c78,43c84,11f0` &middot; csects `FIOSVCP,FIOCMPLT,FIOPDISP,/bin/bashDGRGSE,#DDGRGSE` &middot; instrs `BAL` &middot; tape `v37` &middot; symptom `POLL FAIL`
- **#65** *(tape)* OPS 201's POLL FAIL is caused by the RESIDENT unresolved operands: three FCOS I/O routines doing BAL R7 to ADDRESS 0 (FIOSVCP+0x29, FIOCMPLT+0x1be, FIOPDISP+0x27d, each 'e7f3 0000' where all eight DASS dumps have e7f3 f660/de90/de62), plus /bin/bashDGRGSE+0x122/+0x12e and #DDGRGSE+0x2e. Filling those six cells makes OPS 201 keep polling the displays and run MM 201's flight-critical I/O for the rest of the run; without them every bus falls silent within seconds of the transition, with NO program check.
  - evidence: Bus-log rates (YAGPC_BUSLOG, gpc-buslog.py rate). CONTROL D201 (v37, no fill): DEU buses 6/7/8 at 5,291 per 10 s in the 260 s bin, 2,620 in 270, then NOTHING on any bus to the end of the 393 s run. B201 and B201b (v38 + the six cells filled by YAGPC_POISON at t=200): DEU buses steady at ~4,425 per 10 s (B201) / ~8,850 per 20 s (B201b) through 380 s, and buses 14/16/20/22 at 205k-613k per bin -- MM 201's flight-critical traffic, which the control never shows. v38 differs from v37 only in phase 8, which OPS 201 never loads, so the fill is the only effective difference. Also consistent with the user's interactive v37 OPS 201 run: its DK sniffer shows the UNIV PTG display fill at 127.67 s wall and then no DK event of any kind for the 114 s until the run was stopped. Put on tape as OI340700-v39boot.mmv (9 halfwords from v38: 6 fills + 3 restamped load-block checksums; blocks located by checksum at block-aligned starts in phase 2's extent and verified 100% against the running image, except the 0x11f0 block, 87%, low-memory runtime data).
  - addrs `1a7ef,1b63e,1bbf7,43c78,43c84,11f0` &middot; csects `FIOSVCP,FIOCMPLT,FIOPDISP,/bin/bashDGRGSE,#DDGRGSE` &middot; instrs `BAL` &middot; tape `v37,v38,v39` &middot; env `YAGPC_POISON,YAGPC_BUSLOG` &middot; symptom `POLL FAIL,UNIV PTG` &middot; run `D201,B201,B201b`
- **#68** *(method)* The DASS csect tables (PFS/mafgen/csects-*.json) are INCOMPLETE: FPMIHIM, the Instruction Monitor interrupt handler at 0x1acf8 (PSA new PSW 0x0074, class 17 'CPU Breakpoint'), is missing, so a lookup there answers 'FPMGMTIM+0x1e..' and a ring through FPMIHIM reads as FPMGMTIM falling through into its own error code. Our own link's symbol table has it. Cross-check any surprising 'routine X continues into code X does not contain' against link/*-symbols.json.
  - addrs `1acf8-1ad23,74` &middot; csects `FPMIHIM,FPMGMTIM,FPMIHPC1` &middot; run `R901`
- **#70** *(tape)* OPS 901's death on v39: the CPU STARTS THE MSC AT ADDRESS 0. FIOPDSMU ('VCO/SM COMMON IOP DISPATCHER', output 'PC TABLE POINTER IN R2') in PHASE 8 still had 15 unresolved operands after v38, and they include the addresses it uses to build the MSC program pointer. The MSC then executes the PSA as MSC instructions, wanders into VAASEQUE's procedure code (A5VAASEQ+0x517, CPU code), and stores CPU instruction words into protected FCOS code; each masked DMA store-protect violation sets the documented CC anomaly (CC=10), and FPMGMTIM's closing BR 7 then falls through into FPMIHIM, starting the recursion that ends at address 0.
  - evidence: M901 (v39, YAGPC_PCTRACE): the CPU sets MSC0 PC to 1cb4c / 09838 / 099b2 / 09934 routinely, then 'PCTRACE MSC0 PC<-00000 t=286626569.9 us'; 151 us later 'DMAPROT addr=18d81 val=eaf3 pe=0 pc=2c6c9' and 775 more in 8 ms, all pe=0 (MSC), pc 2c6c9-2c6f8 (A5VAASEQ), targets FCMDSCRM/FCMNINIT code. OPS 201 on v39: ZERO DMAPROT lines. Whole-module diff of phase 8's FIOPDSMU (0x1ebaa-0x1ee25) against DASS G9: 42 halfwords differ = EXACTLY the 42 unresolved relocations the root link reports for FIOPDSMU. 27 were caught by the zero-operand fingerprint; the other 15 hold small NON-ZERO values (0030, 0050, 0014, 0000 beside other zeros) -- UNRELOCATED OFFSETS within FIOCDATS -- which the fingerprint cannot see. Every one of the 15 DASS values occurs in identical context in the THREE correctly linked FIOPDSMU copies our own build placed in other phases on the same tape, and in exactly one broken copy (phase 8). Filled on OI340700-v40boot.mmv (15 halfwords, one load block, checksum restamped).
  - addrs `1ebaa-1ee25,0,2c6c9-2c6f8,18ab0-18d8e` &middot; csects `FIOPDSMU,FIOCDATS,A5VAASEQ,VAASEQUE,FCMDSCRM,FCMNINIT` &middot; procs `MSC` &middot; phases `8` &middot; tape `v39,v40` &middot; env `YAGPC_PCTRACE,YAGPC_DMAPROT,YAGPC_MSCRING` &middot; symptom `POLL FAIL,wait state (masked),DMAPROT,PC<-00000` &middot; run `M901,P901,P201`
- **#71** *(method)* The zero-operand fingerprint (ours==0000, DASS non-zero, matching neighbours) UNDERCOUNTS unresolved relocations: an unresolved external's operand holds the symbol's OFFSET within its section, which is often non-zero (0030, 0050, 0014), and adjacent unresolved cells defeat the neighbour test. For a module whose code otherwise matches, diff the WHOLE module against DASS and cross-check each value against a correctly linked copy of the same module elsewhere in our own build -- the count should equal the link's unresolved-relocation count for that section (42 = 42 for FIOPDSMU).
  - csects `FIOPDSMU` &middot; run `M901`
- **#72** *(tape)* After FIOPDSMU was fixed (v40), OPS 901 still failed -- X901a executing fill at 0x06a8d (bit 15 short of FIOPDHF+0xb at 0x1ea8d), X901b going silent -- and a whole-phase diff of EVERY code section loaded by phases 8 and 18 against DASS G9 leaves exactly THREE differences: FIOG9ADB+1 and +0x78 (the PROGRAM macro's STM and the RETURN macro's LM, both 'c8fb/ccfb 0000' where DASS has a0c6 -- so the G9 MDM A/D BITE processor saves and restores its caller's registers at ADDRESS 0) and FIOPDG9+3 ('22f3 0000' vs a09a). All three are cross-phase references from the phase-8 link into the root's FIOCDATS.
  - evidence: Our own G9 link's symbol table: FI$G9ADB = 0x0a0c6, defined in FIOCDATS.asm:324 'FI$G9ADB DC 8F'0' REGISTER SAVE AREA FOR G9 MDM A/D' -- the PROGRAM macro names the save area &CSECT(1,2).'$'.&CSECT(4,5), so FIOG9ADB's STM/LM MUST address a0c6. 0x0a09a = FIOCF302 in our link. The whole-memory link's relocation list could NOT be used here: a single union link cannot represent overlays that share an address, and it resolves these cells to other modules' compool references (#PCGRRMC, #PCGBIM1, #PCDWDOW); the DASS configs disagree for the same reason, so DASS G9 plus our own symbol table are the authorities. FIOPDG9 begins phase 8's load block 15 (dest 0x1de62, len 250), which is why a symmetric context search missed it. Filled on OI340700-v41boot.mmv (3 cells, phase 8).
  - addrs `1de62-1de6d,1de90-1df0b,1de65,1de91,1df08,6a8d,1ea8d,a0c6,a09a` &middot; csects `FIOG9ADB,FIOPDG9,FIOCDATS,FIOPDHF,FI$G9ADB` &middot; phases `8` &middot; tape `v40,v41` &middot; symptom `POLL FAIL,invalid instruction 0xc6c6 at 0x6a8d` &middot; run `X901a,X901b`
- **#82** *(method)* HAL/S-FC's decimal-to-IBM-float conversion is NOT correctly rounded: the exact decimal expansion of a representable value sometimes compiles one unit low, and the 8-digit literal the dump prints sometimes compiles one unit high. Reconstructing a SCALAR INITIAL from memory therefore needs a literal chosen by COMPILING candidates, not by formula.
  - evidence: TSTCP probe 2026-09-11: 426D9998 from 1.0959998E+02 ok but 1.096000E+02 -> 426D9999; C113A5E3 from 17 digits -> C113A5E2; 3B123456 from 8 digits -> 3B123455. tools/dassrecon/oracle.py batch-compiles candidates (7-17 digits, then values nudged by eighths of an ulp) and keeps one HAL/S-FC turns into the exact bits; thousands per sub-second compile.
  - files `tools/dassrecon/oracle.py` &middot; symptom `scalar literal one ulp off`
- **#83** *(method)* The flight's HAL/S compiler emitted NO TEXT for all-zero data -- a variable initialised to zero, a zero CONSTANT, a repeated n#0 group -- so the dumps show fill (C9FB/C6C6) there, where our HAL/S-FC emits zero text. Fill in a dump at an initialised variable therefore means ZERO (or uninitialised), not 'the source is wrong'; ~158,000 halfwords of the v43-vs-DASS compool and HAL-data difference are this.
  - evidence: CRDCIL's CRDK_ZERO_VECTOR CONSTANT (must have a value) reads C9FB; CSDMDT's INITIAL(115#0) pad reads C9FB; CPAMSP's INITIAL(HEX'0000', 31#HEX'0000') has text for the lone first zero and none for the 31# group. recon_compools.py keeps such sources and reports the class separately.
  - csects `CRDK_ZERO_VECTOR,CSDV_MDT_PAD,CPAV_MSP_CC_SPACELAB_BUFFER` &middot; config `S2,SSW` &middot; symptom `C9FB where we have 0000`
- **#84** *(tape)* GPCIPL's '>>> GPC POWER REFAIL -PROGRAM/MACHINE WERE R' (MSG130) on the IPL display is NOT a power refail: FAILEXEC never runs and SVC 130 is never issued. It is message 132 ('GPCIPL 09.05.00.00.00 LOADED', the annunciation CM4UPDT issues every IPL) shown with message 130's text, because the error-message pointer table ERRMSGS is read two halfwords low. ASM101S/ASM101Sa emitted the RLD for CM4UPDT's 'LA$ B1,ERRMSGS(Z3)' (and nine self-references inside LINES) with R = GPCIPL's ESDID instead of LINES'; phase 10's deck starts LINES as overlay STP2, the LE's inter-block checksum puts it at 3C22 not its assembled 3C20, and the fields naming it were not moved with it.
  - evidence: Headless v43 boot (port 6800, RANGETRACE 235-290, WATCHHW 46e-471, SVCTRACE): 0 instructions executed in FAILEXEC/REFAILED/FAIL#0, FAILSWCH never written by the CPU, no SVC 130 (SVCs seen: X'8084'=132 at 029A4, X'8089'=137, 202, 208, 106). DEU image at 19EE decodes '... >>> GPC POWER REFAIL -PROGRAM/MACHINE WERE R ... 1 132'. v43 PHASE10: GPCIPL+29EA = 3C20, LINES at 3C22 (checksum gap 3C20-3C21). BILDNEW5.obj: 10 RLDs R=1 whose target lies in LINES (29EA in GPCIPL; LINES+107,14E7,1581,1585,1587,1589,158B,158D,158F = Y(MSGEND),Y(MSGCNT),Y($POF057),Y(MSG154A..156B)). The only such RLDs in all 761 assembled objects of the build. pass-ipl-cflm/v18-v26 (another chain) have 3C22 there and never showed the message. Minimal repro: DC Y(own label) in a 2nd CSECT, or LA$ R,SYM(Z3), got R=first CSECT; RS AM=0 fields in a later CSECT were also current-section-relative.
  - addrs `29ea,3c20-3c22,3ca3,3d27,5107,51a1-51af,235-290,46e-471` &middot; csects `GPCIPL,LINES,BILDNEW5,CM4UPDT,ERRMSGS,FAILEXEC,REFAILED,FAIL#0,MSGTABLE` &middot; instrs `LA$,SVC,TSB` &middot; procs `CPU,DEU` &middot; phases `10` &middot; tape `v43,v42,v41,v27,pass-ipl-cflm` &middot; env `YAGPC_RANGETRACE,YAGPC_WATCHHW,YAGPC_SVCTRACE,YAGPC_DEUDUMP` &middot; files `ASM101S/model101.py,ASM101S/ASM101Sa-src/model101.c` &middot; symptom `GPC POWER REFAIL -PROGRAM/MACHINE WERE R,MSG130 on IPL display,message 132 with wrong text,RLD R pointer names first CSECT` &middot; doc `BILDNEW5.lst 6986 (LA$ B1,ERRMSGS(Z3)), TABLGEN 3326, CON80 PHASE10 OVERLAY STP2; lnk101 applyRelocations delta = baseAddress - ESD address`
- **#92** *(flight-sw)* The GRT says which OPS form a multi-GPC set, and it is only ever major function 2: OPS 1, 2, 3, 8 and 9 in MF 2 name more than one GPC, and EVERY single-GPC entry is MF 1 or MF 3. OPS 2 in MF 2 targets exactly GPCs 1 and 2, which is the smallest redundant set the tape can be asked for and the right first experiment for --gpcs 1,2.
  - evidence: Read out of the source, not guessed. CZ2COMMO.hal:434 CZ2B_GRT_GPC_SET ARRAY(10) BIT(16) INITIAL(F000,C000,F000,1400,1400,4400,8400,C000,F000,0000) and CZ2COMMO.hal:527 CZ2V_GRT_TAB (MC,POVL,MFOVL,MF_VALUE,OPS_NBR,MAX_MODE) INITIAL(1,4,3,2,1,6 / 2,5,3,2,2,2 / 3,6,3,2,3,5 / 4,15,14,3,2,2 / 0,16,14,3,4,2 / 6,12,9,1,9,1 / 0,13,9,3,9,1 / 8,7,3,2,8,1 / 9,8,3,2,9,1 / 0,0,0,0,0,0). Only bits 1-5 of the set word are the GPCs: DM6OPS.hal:508 'TARGET_GPC$(12 TO 16) = TSET$(1 TO 5)', so the bit 6 that appears in 1400/4400/8400 is NOT a sixth computer and is dropped. Decoding bits 1-5: index 1 F000={1,2,3,4} MF2 OPS1 maxmode6; 2 C000={1,2} MF2 OPS2 maxmode2; 3 F000={1,2,3,4} MF2 OPS3 maxmode5; 4 1400={4} MF3 OPS2; 5 1400={4} MF3 OPS4; 6 4400={2} MF1 OPS9; 7 8400={1} MF3 OPS9; 8 C000={1,2} MF2 OPS8; 9 F000={1,2,3,4} MF2 OPS9; 10 empty. Rows 5, 7 and 10 can never be selected at all -- DM6OPS.hal:604 requires CZ2V_GRT_MC(GRTS)=GRTS and those three carry MC=0. The single-GPC decode agrees independently with headless-gpcmem.sh's own note that entries 4 and 5 name GPC 4, 6 names GPC 2 and 7 names GPC 1.
  - csects `DM6OPS,CZ2COMMO,ARCGPC` &middot; files `SSSRC/CZ2COMMO.hal,SSSRC/DM6OPS.hal` &middot; symptom `redundant set,target set,GRT,which OPS`
- **#93** *(emulator)* With two computers running, each machine takes a SECOND, spurious IPL that the crew panel never commanded, and the second one reads from MM1 whatever the panel selected. The panel pressed IPL exactly once per GPC; the emulator manufactured the extra edge.
  - evidence: Run mm1, --gpcs 1,2, one discretePanel.py per GPC with GPC1 sourcing MM1 and GPC2 sourcing MM2. Each panel log contains exactly one 'discretePanel: ipl' line. The mode trace shows four IPLs: 'GPC2 MODE: IPL ... from MM2 (BCE 19) (72 blocks)', 'GPC1 ... from MM1 (BCE 18) (72 blocks)', then 'GPC1 ... from MM1 (36 blocks)' and 'GPC2 ... from MM1 (37 blocks)'. The FIRST pair is correct and complete; the second pair is spurious, both machines read MM1, and because they are then on one unit at once they split a 72-block read into 36 and 37 with 72 mid-transfer clashes reported on bus 18 and mmu1 wordsLost=36352. A single-GPC run of the same panel script IPLs exactly once. LEAD, NOT ESTABLISHED: the second IPL's source reverting to MM1 is the local default, i.e. the driven source bit was no longer driven -- so suspect the discrete staleness timeout (DISCRETES_STALE_SEC 1.5) combined with the extra poll load two machines put on each other, letting the panel's bits expire and re-arrive as a fresh edge at mode_switch_held_uncached's iplEdge test (run.c:1139). Do NOT assume; the poll gate was per-process until 6129c4060 and this run is the first with it per machine.
  - procs `BCE18,BCE19` &middot; env `DISCRETES_STALE_SEC,YAGPC_MODETRACE` &middot; files `src/run.c,src/discretes.c` &middot; symptom `double IPL,second IPL,IPL source reverts to MM1,split bootstrap read`
- **#94** *(emulator)* Two computers loading from the two DIFFERENT mass memory units at the same time works exactly as the vehicle should: it is the concurrent case the plan named as the sharper test, and it passes.
  - evidence: Run mm1, --gpcs 1,2 --mmu-model 1:tape --mmu-model 2:tape, GPC1 sourcing MM1 and GPC2 sourcing MM2, both IPLing within the same second. Both read the full bootstrap -- 'GPC2 ... from MM2 (BCE 19) (72 blocks, 36864 halfwords)' and 'GPC1 ... from MM1 (BCE 18) (72 blocks, 36864 halfwords)' -- and mmu2 reports blocksRead=72 wordsOut=36864 wordsTaken=36864 wordsLost=0. No bus-19 contention is reported at all. The contention that IS reported, 72 clashes on bus 18, belongs entirely to the spurious second IPL in the companion entry, where both machines wrongly went to MM1.
  - procs `BCE18,BCE19` &middot; files `src/vehicle.c,src/run.c` &middot; symptom `concurrent mass memory,MM1 and MM2,two GPCs`

## Fixed

- **#14** *(emulator)* MSC repeat state (mscRepeatActive/PC/UntilUs) survived a CPU-forced PC load, so a returning @RAW would not re-arm and used a stale deadline.
  - evidence: Correct as modelling; cleared on the local-store PC write. Did NOT unblock phase 18.
  - addrs `a201-a201` &middot; csects `FIOMDLY` &middot; instrs `@RAW` &middot; procs `MSC` &middot; files `src/iop.c` &middot; run `v55a`
- **#15** *(emulator)* exec_RDL read its count with a HALFWORD access masked to 16 bits and did not ignore the address LSB.
  - evidence: BCE POO: count is bits 14-31 of the main storage FULLWORD, LSB of the address ignored, range 0..262143. Same defect already fixed in its twin #TDL. LATENT: #RDL never executes here (BCE18 uses #RDLI).
  - instrs `#RDL,#RDLI,#TDL` &middot; procs `BCE18` &middot; files `src/iop_bce_instr.c` &middot; doc `BCE PoO IBM-6246556A pt3 (count = bits 14-31 of the fullword)` &middot; run `v84a`
- **#32** *(method)* gpc-buslog.py's KIND table had both enum pairs transposed. Correct order is XMIT_WORD=0, XMIT_CMD=1, RECV_WORD=2, RECV_POLL=3 (yaGpcIntegration.h:177) -- word before command, the opposite of the way one says it.
  - evidence: The wrong table made an ordinary DEU fill (1 command + 100 data words, emitted at a silence boundary) decode as '100 commands issued in zero simulated time', which reads like a bus program spinning. Cross-check that settles it: bus 6 XMIT_WORD must equal the DEU model's own wordsIn -- 111164/111164 for bus 6, 60957 for bus 7, 61477 for bus 8, all exact. Analyses that selected kind==1 for MM commands were unaffected (that IS XMIT_CMD) and are independently corroborated by the MMU model's log.
  - env `YAGPC_BUSLOG` &middot; files `gpc-buslog.py` &middot; doc `yaGpcIntegration.h:177 GpcServiceNumber` &middot; run `v91a`
- **#58** *(tape)* SECOND build defect in the same root load block: 0x1e8, 0x202, 0x218 and 0x22c hold REAL but WRONG Z-CON values, not fill. Tape/runtime have 117c, 13ec, 0be8, 0cc0; all 8 DASS dumps have 88be, 89f6, 85f4, 8660. Every DASS value has bit 0 SET (so a sector is applied) and every one of ours has it CLEAR (so the target is taken as sector 0). Deliberately NOT patched by tools/patch_root_zcons.py -- these want their own diagnosis rather than a hand-applied constant.
  - evidence: DIAGNOSED 2026-09-11 (#75): the four values are 2x the DASS value mod 2^16 (117c = 2*88be, 13ec = 2*89f6, 0be8 = 2*85f4, 0cc0 = 2*8660) -- every relocation to #CDSPSPC/#CDPDSPC/#CDXCCCS/#CDXRDMM was applied twice because each module was linked twice. The 'bit 0 clear' was the carry out of the doubling, not a missing sector. v42: all four equal SSW and G9; root block 0x1a8-0x247 160/160. ORIGINAL EVIDENCE: Found by comparing the whole 160-halfword load block at dest=0x1a8 against DASS G9.fcm: 40 of 160 halfwords differed, 36 of them the 18 fill Z-CONs, leaving exactly these 4. The #ZDCDDS4/ACOS collision was in the whole-memory G9 link only.
  - addrs `1e8,202,218,22c,1e2` &middot; csects `#ZDCDDS4,ACOS` &middot; tape `v36,v41,v42` &middot; symptom `zcon high bit clear,sector 0 target`
- **#64** *(method)* YAGPC_BUSLOG LOST ITS TAIL on every SIGINT-terminated run -- which is how every HEALTHY run ends -- because its 1 MB stdio buffer (~87,000 events) was never written. Worse, when bus traffic collapses (the thing the log exists to catch) the whole collapse fits in that buffer and vanishes, so the file simply stops before the answer. Fixed: fflush once per simulated second.
  - evidence: b901.bus ended at 292.0 s and b201.bus at 273.4 s of 404 s runs, both at ~32 MB. With normal DEU polling at ~1,500 events/s the missing 110-130 s would have forced several flushes, so the truncation itself implied a collapse -- but hid when.
  - env `YAGPC_BUSLOG` &middot; files `src/run.c` &middot; run `b901,b201`
- **#67** *(method)* YAGPC_RINGTRIG could not see FULLWORD stores: cpu_store_fw passed the 32-bit value to a 16-bit comparison, so STM, ST and every PSW save were invisible and the trigger's silence read as 'nothing ever wrote this'. Fixed: a fullword store is tested as two halfword stores at addr and addr+1.
  - evidence: W901 watched FP's R7 slot (932e) for acf8, the value STM would put there, and never fired -- through a run whose ring shows FPMGMTIM's BR 7 landing on 0x1acf8 eight times.
  - env `YAGPC_RINGTRIG` &middot; files `src/cpu.c` &middot; run `W901`
- **#73** *(goal)* POLL FAIL after OPS 201, 301, 801 and 901 is FIXED on OI340700-v41boot.mmv, produced from v36 by tools/patch_unresolved.py (88 halfwords in 7 load blocks, byte-identical to v41, idempotent). The cause throughout is the build's UNRESOLVED CROSS-PHASE RELOCATIONS; the real repair is in the per-phase links (see #59, #60), and the tool is a labelled workaround until then.
  - evidence: Headless, bus-log measured: OPS 201 steady to end of run on V201 (v39) and Z201 (v41), B201/B201b (v38+poison); OPS 301 steady on V301 (v39), Z301 (v41), vs K301 (v37) silent after ~285 s; OPS 901 steady on Y901a and Y901b (v41) with the G9 MDM buses 10/11 carrying traffic for the first time, vs X901b (v40) silent and X901a (v40) crashing at 0x06a8d. INTERACTIVE, by the user with MEDS on v41: OPS 201 (UNIV PTG), 301 (DEORB MNVR COAST, title now shown), 302 (DEORB MNVR EXEC), 801 (FCS/DED DIS C/O), 901 and many GNC 9 displays, 101 (LAUNCH TRAJ 1). Tool check: v36 -> tool -> byte-identical to v41; a second run fills nothing, 0 refused, exit 0. An intermediate version with wildcarded neighbours but no distinctiveness guard wrote a Z-CON value into fill inside an UNRELATED load block (flat 1186050) -- the MIN_ANCHOR guard exists because of that.
  - addrs `1d6-243,1a7ef,1b63e,1bbf7,43c78,43c84,11f0,1ebaa-1ee25,1de62-1df0b` &middot; csects `FIOPDSMU,FIOG9ADB,FIOPDG9,FIOSVCP,FIOCMPLT,FIOPDISP,$0DGRGSE,#DDGRGSE` &middot; tape `v37,v38,v39,v40,v41` &middot; files `tools/patch_unresolved.py,tools/patch_unresolved.fills.json` &middot; symptom `POLL FAIL` &middot; run `D201,B201,B201b,V201,Z201,K301,V301,Z301,X901a,X901b,Y901a,Y901b`
- **#74** *(tape)* The OI340700 v41 volume is now REPRODUCIBLE FROM SOURCE by yaGPC2/tapebuild/build.sh, as documented in HANDOFF-OPS9.md sections 0-3: run from an empty directory on 2026-09-10 it produced OI340700-v41boot.mmv BYTE-IDENTICAL to the tested volume (and its pre-fill OI340700-v36boot.mmv byte-identical to v36).
  - evidence: Before: the handoff described the v27 procedure; v36 had been built by /tmp/claude-1000/buildtape.sh (never committed); a rebuild from ~/pass-build differed in 118,176 halfwords because objects/AIGDEU.obj was left from the v79 WAIT experiment. Found and fixed on the way, each verified by comparison: (1) compilePASS needs --sdl (160 PROGRAM objects otherwise gain START/stack ERs) and --release=OI340700 (CPUSLS/CPTOSV CARDTYPE ACBC; else 8 phase-15 objects cascade away) -- then 1,981/1,981 objects match in ESD/TXT/RLD; (2) dfg was Don's UNCOMMITTED working tree: pinned as upstream 7d90b05 + toolchain-patches/dfg-7d90b05-to-OI340700.patch, verified file-for-file; (3) compilePASS's dfg --release probe never matched because rich styles the two dashes separately -- fixed; without it CS2120 VPD is 00D3 where the S2 DASS dump has 00D5; (4) lib/runtime/RUN is RUNASM with --fill=C6C6 (205/205 byte-identical), ZCON from compilePASS; (5) SYSLIBL1 = one hard link per SD, name order, first definer wins, START excluded (4,275/4,275); (6) sdfpad = 3360-byte SDFs + one zero byte; pchsrc = SSSRC/PCH*.asm without extension; (7) per-phase csect tables regenerate byte-identically from PFS/mafgen; extsyms-02-plus.json (generator lost) committed; (8) DEU load modules = 24 blocks extracted from pass-910.mmv into inputs/deu-loadmodules.mmv; (9) CSPCLB source fixes committed as a patch; (10) toolchain = fork db9d34b + 3 branches, tree a68da6e6 verified; (11) con80build links NOTHING unless build/lib/runtime/{RUN,ZCON} exist in the toolchain checkout -- found only by following the document from an empty directory; now pointed at our own runtime. The build is deterministic (two identical builds byte-identical).
  - csects `AIGDEU,CS2120,CPUSLS,CPTOSV` &middot; tape `v36,v41` &middot; files `tapebuild/build.sh,tapebuild/derive.py,tapebuild/link-and-cut.sh,PASS.REL32V0/compilePASS,HANDOFF-OPS9.md` &middot; symptom `not reproducible`
- **#75** *(tape)* One OBJECT linked TWICE in phase 2 applies every relocation in it twice. DSPSPC.obj (worklist) has #DDSPSPC/#XDSPSPC deferred to a later phase by dropDeferredSections; the reference then pulls the minimal library's #CDSPSPC.obj -- a hard link to the SAME object -- so #CDSPSPC and #ZDSPSPC exist twice, both pinned to the same address, and BOTH RLD sets are applied to the one image. Same for DPDSPC, DXCCCSPE, DXRDMM. lnk101 applied the LE's keep-the-first-definition rule only to MAP imports.
  - evidence: 62 relocation sites recorded twice in PHASE02.sym.json, all 62 wrong against SSW; 12 duplicate sections (#CDSPSPC,#ZDSPSPC,#CDPDSPC,#ZDPDSPC,#CDXCCCS,#ZDXCCCS,#CDXRDMM,#ZDXRDMM,A1-A4DXRDMM). Fix: lnk101 dropDuplicateCsects (tapebuild/toolchain-patches/lnk101-first-definition-and-zcon-pool.patch): 1150 halfwords change in phase 2, every one to the SSW value; no section moves. Includes #58's four root Z-CONs. Our e0a0cd3 had fixed the con80build INSERT path for the same duplicate, but not this library-search path.
  - addrs `1e8,202,218,22c,105f4-1065c,108be-109f5,109f6-10b2d` &middot; csects `#CDSPSPC,#CDPDSPC,#CDXCCCS,#CDXRDMM,DSPSPC,DPDSPC,DXCCCSPE,DXRDMM,dropDuplicateCsects,dropDeferredSections` &middot; phases `2` &middot; config `SSW` &middot; tape `v41,v42` &middot; files `src/lnk101/linker.py` &middot; symptom `relocation applied twice,zcon high bit clear,117c for 88be`
- **#76** *(tape)* The per-phase links left CROSS-PHASE references unresolved because upstream's lnk101.phaseresolve -- the pass that replays each phase's retained RLDs against the phases sharing a memory configuration -- was never run: con80build --phase N does not run it and the v36/v41 procedure linked phase by phase. That, not a regression to bisect, is why v41 needed patch_unresolved.py's 51 resident/phase8 cells.
  - evidence: v41 links: 793 unresolved sites, 165 symbols; 358 of the cells wrong against DASS. phaseresolve on v41's libs alone: 358 -> 42 wrong, phase 8's 45 FIOPDSMU/FIOG9ADB/FIOPDG9 cells correct with no fill. Now tapebuild/link-and-cut.sh stage 4b: 1822 sites resolved, residue exactly #CDCDDS4/#CDKFCM5 (stage 4c). Every halfword v42 changes against v41 in phases 3-18 equals DASS, none broken.
  - csects `phaseresolve,FIOPDSMU,FIOG9ADB,FIOPDG9,FIOSVCP,FIOCMPLT,FIOPDISP,#PCDHMMU,#PCDCSPC` &middot; phases `2,8,9,14` &middot; tape `v41,v42` &middot; files `tapebuild/link-and-cut.sh` &middot; symptom `unresolved relocations,POLL FAIL`
- **#77** *(tape)* The root Z1 pool 0x1D6-0x243 had NO TEXT on every volume before v42 because the phase-2 csect table named the #Z words, so loadExternalSyms satisfied each reference with an ADDRESS-ONLY section and generateZconStubs never built the word. That is the underlying cause of the 0xc6c6 crash at 0x648DA (#74 area), hand-filled as patch_unresolved.py's zcon group.
  - evidence: v41 PHASE02.lib has no extent at 0x1d6-0x243. Fix (toolchain patch): a table ZCON entry for an undefined #Z name is left for generateZconStubs, which pins the stub from the table; stage 4b resolves it. v42 root block 0x1a8-0x247: 160/160 against SSW and G9. #ZDCDDS4/#ZDKFCM5 target SM4, which OI340700 excludes; written from SSW.fcm in stage 4c (inputs/zcon-pool-unlinked.json).
  - addrs `1d6-243` &middot; csects `#ZDCDDG1,#ZDCDDG9,#ZDCDDS4,#ZDKFCM5,generateZconStubs,loadExternalSyms` &middot; phases `2` &middot; config `SSW` &middot; tape `v36,v41,v42` &middot; files `src/lnk101/linker.py` &middot; symptom `invalid instruction 0xc6c6 at 0x48da,zcon unfilled`
- **#78** *(tape)* S2's 26 SM program stacks were ABSENT: the regenerated OI340700 CON80/SM2 keeps its '*STACK * SM2 STACKS' header (seq 009300) but the next card is 011200 -- the whole STACK block was lost. Phase 15's SM programs ran with no stacks.
  - evidence: S2 dump places 53 stacks; v41/v42-early had 29. SM4's block is intact (26 cards, 008300-010002); SM2's = SM4's with $0SM4OPS -> $0SM2OPS, all 26 in csects-S2. Was tapebuild/source-patches in v42; in PFS 24af1848 since v43. 361 of 361 stacks of all 8 configurations at flight address and size. NOT exercised at run time (nothing runs SM OPS 2 yet).
  - csects `SM2,SM4,@0SM2OPS,@0PGDATA,@0SULUPL` &middot; phases `15` &middot; config `S2` &middot; tape `v41,v42` &middot; files `CON80/SM2,tapebuild/source-patches` &middot; symptom `stack missing,store-protect`
- **#79** *(tape)* Thirteen SM compools compile LONGER than the flight's and, pinned at the flight address by the S2 table, OVERLAP their neighbours in phases 14 and 15 (14 and 7 overlapping pairs): #PCSARST 2782 hw vs 352, #PCSDMDT 466/115, #PCPCDIT 336/15, #PCP2GXT 658/126, #PCS2DAR 509/439, ... They are F GEN (generated, mission-dependent) tables from OI340600 sources -- another flight's data. A decision for the user, not a link fix.
  - evidence: FIXED in PFS 24af1848 / v43: 25 SM compools rebuilt from the S2 dump (values from S2.fcm, layout from the listing) by tools/dassrecon/recon_compools.py; each compiled with HAL/S-FC equals #P<stem> halfword for halfword (relocated), bar all-zero variables the flight compiler left without text and CPGPCD's starred FTSBB. v43 phases 14/15: 0 oversized compools, 0 overlapping pairs; compool value diffs only the MMB stamps. ORIGINAL: CSARST.hal CSAS_RMT_NUM_ENTRIES INITIAL(273), 278 entries x 10 hw; flight 35 entries. Unpinning tried in scratch made phase 15 worse (7 -> 20 overlaps).
  - csects `#PCSARST,#PCSDMDT,#PCPCDIT,#PCPCVID,#PCPGPCD,#PCPCTCI,#PCPCANI,#PCPCPCI,#PCP2GXT,#PCS2DAR,#PCS2PAR,#PCPCCST,#PCS2IPT,CSARST` &middot; phases `14,15` &middot; config `S2` &middot; tape `v42,v43` &middot; files `PFS/OI340700/APPLSRC,tools/dassrecon` &middot; symptom `compool overlap,F GEN`
- **#80** *(tape)* The DEU critical-format module (DEUCFLM, 24 blocks from pass-910) cannot be rebuilt from OI340700 sources because OI340700/SSSRC/XD0001.dfg and XD0990.dfg are zero-byte tombstones (PFS 70e65182) -- and those tombstones look wrong: the rule was 'name in none of the 8 dumps', but static formats live in the DEU and mass memory, never GPC memory (no XG deck appears either, none was tombstoned), while CD0001/CD0990 are in all 8 dumps and CFSYSIN lists both backgrounds.
  - evidence: FIXED in PFS 24af1848 / v43: the 17 tombstones (XD0000.hal, XD0001.dfg, XD0990.dfg, 14 XG*.dfg -- exactly CFSYSIN's DEUCFLM members) removed; tapebuild stage 7 builds DEUCFLM with build_deucflm.py + add_sysid_allocs.py (fed pass-910's image the latter reproduces all 24 borrowed blocks). V43 headless: tape-loaded DEU memory differs from v42's in exactly CFIT 30/31 + checksum; displays identical. ORIGINAL: build_deucflm over OI340600 decks = borrowed copy in 3654/3657 hw; pass-910's older deucflm wrote SPARE branches (1DE8) in the exit slots.
  - csects `DEUCFLM,DMACDFT1,XD0001,XD0990,XD0000,CFSYSIN,deucflm` &middot; tape `v42,v43` &middot; files `tools/build_deucflm.py,tapebuild/inputs/deu-loadmodules.mmv` &middot; symptom `DEU load module borrowed`
- **#81** *(tape)* #CDCDDS2 (SM2's display procedure, phase 15) is compiled from OI340600's DCDDS2.hal: 2006 hw against the flight's 2074, 1526 differing. The only code csect in any phase, besides the FCMG3DAT stamp, that differs from DASS in v42.
  - evidence: FIXED in PFS 24af1848 / v43: every member statement decompiled from #CDCDDS2's annotated disassembly and aligned with OI340600 (tools/dassrecon/recon_dcdds2.py); DCD12401 +9 %COPY, 8 CSAS_INB_ENTRY subscripts renumbered (DCD12401, DCD12601), DCDDS2 LDR_SIZE_OF_24 24->28. #CDCDDS2 2074 hw exact; #DDCDDS2 exact but one unreferenced Z-CON literal HAL/S-FC adds for the REMOTE #PCVNMMU include. Every code csect of every phase now equals DASS (FCMG3DAT stamp aside).
  - csects `#CDCDDS2,DCDDS2` &middot; phases `15` &middot; config `S2` &middot; tape `v42,v43` &middot; files `PFS/OI340700/INCL80/DCD12401.hal,PFS/OI340700/INCL80/DCD12601.hal,PFS/OI340700/APPLSRC/DCDDS2.hal` &middot; symptom `code differs from DASS`
- **#85** *(emulator)* With MEDS2.py (or any display unit in another process) the GPCIPL page is hit-and-miss: the clock counts over a blank page. Cause: the DK peer answers LATE IN WALL TIME. MEDS2.py's IDP shares one Python event loop with the MDU's drawing and sometimes cannot read the socket for tens of ms; GPCIPL gives the DK BCE a 5.0 ms message time out (MTO 303) and the MSC gives up sooner, so the BCE error-terminates (ERROR 44 BCE TIME-OUT N RETRY FAIL-DEUIPL, 96 REAL TIME MSC TIME OUT, 97, 99). GPCIPL re-IPLs the unit (a second zero-fill load) and after a second failure gives up on it, never sending the one-shot 509+254-halfword menu fill; time fills still flow, hence the clock. Fixed in yaGPC2: a peer in another process may HOLD the machine (no simulated time passes) while a reply it owes is on its way -- iop.c peerWait, bcenet_framer_peer_wait -- so the reply lands inside the software's own window.
  - evidence: Reproduced in private network namespaces (unshare -rn, Xephyr, Mesa EGL): v44 tape, two MEDS2.py instances, scripted discretePanel. Without the hold 4 of 4 runs failed: DEU zero-filled 2-3 times, menu never sent, 46-60 BCE6 RECV TIMEOUTs (waited=5.00 ms), SVCs 44/96/97/99. With it 7 of 7 showed the menu (screenshot: GPCIPL MENU (1) ... GPCIPL 09.05.00.00.01 LOADED): one load, 0-1 BCE6 time outs, ~150 holds per run, longest 57-65 ms wall. Full run to PASS (ITEM 1 EXEC on KYBD1, RUN): GPC MEMORY drawn by MEDS2.py, 378 holds, 0 time outs. User's logs: 0911-0821 one load then menu; 0911-0904 two loads. Lengthening the time out instead was already refuted (iop.c RECV_TIMEOUT_FLOOR_US: 20 ms floor = load never completes). Headless --deu-model unaffected (no hook installed): DEU image byte-identical; unit tests identical counts to HEAD.
  - addrs `01df6-01df8,020f5-02128,035a2,0359a,019ee,01beb` &middot; csects `GPCIPL,POLLRSP,STMWAIT,CM41` &middot; instrs `@RAW,#RDLI` &middot; procs `BCE6,MSC,DK1,IDP1` &middot; phases `10` &middot; tape `v44` &middot; env `YAGPC_PEER_HOLD_MS,YAGPC_TIMEOUT_TRACE,YAGPC_RECV_FLOOR_US,NSTS_BUS_RCVBUF` &middot; files `src/iop.c,src/iop.h,src/bcenet_framer.c,src/bcenet_framer.h,src/run.c,discretePanel/MEDS2.py` &middot; symptom `clock but no menu,GPCIPL page blank,hit-and-miss GPCIPL menu under MEDS2.py,DEU loaded twice,unheadered fill of 8 halfwords,BCE6 RECV TIMEOUT` &middot; doc `BILDNEW5.lst POLLRSP 4189-4282, MSG044/096/097/099; iop.c RECV_TIMEOUT_FLOOR_US history`
- **#86** *(emulator)* An IPL (the O6 IPL pushbutton, firmware_ipl) did not perform the system reset POO 2.5.3.3 says it 'first causes': CPU pending interrupts, the interval counters, status/diagnose registers and DSE registers carried over from a running machine, and the whole IOP (MSC mid-program, BCEs mid-transfer, MIA latches, DMA queue, interrupt/status registers) was only frozen by HALT and resumed at STBY ahead of FCMBOOT's own MASTER RESET. Also a reply the mass memory still held from the running software would have been read as the bootstrap's first word. Now cpu_system_reset / iop_system_reset / ap101_system_reset run at the IPL press, and stale MM words are drained first. NOT the cause of the user's re-IPL symptom -- see the DEU LOAD entry -- in no headless case tried did the old binary fail.
  - evidence: PoO 2.5.3/2.5.3.2/2.5.3.3 (ASM101S/AP-101S-instruction-set.txt ~3463). Headless re-IPLs with the pre-change binary vs the new one, same scripts: during GPCIPL idle, during PASS RUN (t=300 s), during FCMBOOT's GPCIPL read, during an ITEM 1 EXEC window -- both reboot identically (same MM command sequence, same SVCs, end in STMWAIT). New binary: make test counts identical to HEAD; headless GPC MEMORY display image byte-identical.
  - addrs `00004,00014,0014b,01df6-01df8` &middot; csects `FCMBOOT,GPCIPL,FCMBEX0H` &middot; instrs `PC,LPS` &middot; procs `CPU,MSC,BCE18,MIA` &middot; phases `10` &middot; tape `v44` &middot; files `src/cpu.c,src/cpu.h,src/iop.c,src/iop.h,src/ap101.c,src/ap101.h,src/run.c` &middot; symptom `cold IPL from a running machine fails,re-IPL,HALT then IPL,stale interrupts,IOP not reset` &middot; doc `AP-101S PoO 2.5.3.2 System Reset, 2.5.3.3 IPL; PASS User's Guide OI32 Table 2-2`
- **#87** *(emulator)* Re-IPL from a running PASS appears not to work because the display unit is never reloaded: GPCIPL loads a DEU -- and presents its menu -- only when the unit's poll reply asks for an IPL (POLLRSP, X'0001'), and a unit asks only when freshly powered or after DEU LOAD P/R on panel O6 (PASS User's Guide Table 2-2 step 9, 'Menu IPL only'). Nothing models DEU LOAD: MEDS2.py's IDP and yaGPC2's deumodel start unloaded (so every FIRST IPL works) and can never return to that state. After HALT/IPL/STBY GPCIPL runs again -- its clock restarts from 0 -- but the screen keeps PASS's last page.
  - evidence: Fixed in MEDS2.py 1b8f7f3c0: an IDP pane on each MDU window with DEU LOAD (plus IDP POWER and IDP MAJ FUNC). Namespace run g3 (v44): PASS in RUN, then source MM1 / crt 1 / HALT / DEU LOAD click / IPL / STBY -- IDP1 'DEU LOAD -- memory cleared, requesting IPL', second 'load started/complete', geometry back to gpcipl, MDU menu region hash c9e8d53c = a first boot's; screenshot shows the GPCIPL menu clean. Earlier run g1 without DEU LOAD: no reload, PASS page stays. Also fixed: stale DFG background left drawn when BACKGROUND_TOP's branch goes (Screen_DPS._drawPasses). yaGPC2's in-process deumodel still has no DEU LOAD (headless only).
  - csects `GPCIPL,POLLRSP` &middot; procs `DEU,IDP1,DK1` &middot; tape `v44` &middot; files `discretePanel/MEDS2.py,src/deumodel.c,discretePanel/panelO6.py` &middot; symptom `re-IPL shows old PASS display,GPCIPL menu missing after re-IPL,cold IPL seldom or never works,clock restarts but no menu` &middot; doc `PASS User's Guide OI32 Table 2-2 step 9 DEU LOAD`
- **#88** *(emulator)* The MEDS header clock -- the one on the GPCIPL menu screens -- always started near 000/05:47 and never showed the real time of day. TWO defects in series. (a) The MTU model put its three time halfwords at wire offset 2, behind two zero header words, so FPMLIMCK read 0x0000 as GMT days, failed CHI R2,GE,X'0001' for all three accumulators, and took FPMMTURM's default LHI R5,X'0030' DEFAULT GMT TO 1 DAY = 86400 s. (b) MEDS2.py's IDP-to-MDU MDUMsg.CLOCK carries HALFWORDS, so 86403 wrapped to 20867 s = 000/05:47:47. (c) and separately, the MTU reported ELAPSED simulated time from day 000 rather than a time of day, so even an accepted reading would have been wrong.
  - evidence: The MTU's layout is stated four independent ways, all agreeing that the six halfwords are GMT at 0,1,2 and MET at 3,4,5, from the base: FIOPRMPG's commander does #LBR TFCMMTU1 then #MIN 0,6 (buffer register AT the base); FCMCBLKS' FPMMIBAT is DC Y(0),Y(TFCMMTU1),Y(TFCMMTU2),Y(TFCMMTU3) -- buffer STARTS; DCD14201.hal takes %COPY(DL(19),TFCMMTU1$(1:), 6); and FPMMTURM's FPMLIMCK reads 0(R0) and 1(R0) for GMT, 3(R0) and 4(R0) for MET, with AHI R0,3 POINT TO MET TIME before converting. The LA R3,TFCMMTU1+2 that commit b4a7a6cf6 relied on is in the PCMMU branch (FPMMTURM lines 265-365), where the PMU's 5 Hz voted GMT is read into the MTU buffers and +2 is the MILLISECONDS word, sampled to detect the PCMMU clock ticking. Field packing confirmed independently: FPMLIMCK's seconds limit X'0164' is exactly BCD 0x59 shifted up by the two spare low bits the model already left, and LH on this machine appends 16 low-order zeros (instruction set sec 4.18), which is what makes FPMMTUFX's SLDL 2/4/4/2/4 digit peeling and FPMLIMCK's SRL 6 / NHI X'003F' agree on one layout. MET left zero: FPMLIMCK's MET tests have no lower bound. Measured: PASS commands the MTU at t=223.7 s (the OPS transition, NOT at IPL -- a run must reach it, so a 300 s wall-clock headless run ends just short), and the DEU time fills go from mission=99.5 (000/00:01:40, elapsed since boot) to mission=22010899.040 = 254/18:08:19, the host's own date and local time.
  - csects `FPMMTURM,FPMLIMCK,FPMMTUFX,FIOPRMPG,FPMMIBAT,TFCMMTU1,DCD14201,DMIMCD` &middot; procs `BCE20,BCE21,BCE22,DK6` &middot; tape `pass-ipl-cflm` &middot; env `YAGPC_TIMEFILL,YAGPC_MTUTRACE,--date-time-epoch` &middot; files `src/mtumodel.c,src/run.c,src/deumodel.c,yaShuttle/discretePanel/MEDS2.py` &middot; symptom `menu clock starts 00:05:30,clock never shows time of day,mission=86403,000/05:47:47,GMT defaults to 1 day` &middot; tsec `223.7` &middot; doc `AP-101S instruction set sec 4.18 LOAD HALFWORD; sec 6.3 SHIFT LEFT DOUBLE LOGICAL` &middot; run `mtu-g,mtu-i`
- **#89** *(emulator)* The simulated clock ran at about 2/3 of real time (13 min of MEDS header clock per 20 min of wall), and a mass-memory load that took 45 s on the first ITEM 1 EXEC took 68 s on every re-IPL after a HALT -- the same factor, 1/0.66. NOT a speed problem: the emulator used 15.7% of ONE core of 20 and was in hrtimer_nanosleep in 92% of stack samples. The defect was in rtpacer's REBASE, which moves the wall/sim origin pair to now and forgets whatever the simulation was behind by. It was being applied on every wake from a wait state, on any deficit past STALL_REBASE_MS while executing, and on a slow bus peer -- none of which are host stalls.
  - evidence: Instrumented (YAGPC_PACETRACE now reports rebases by cause and what the wait loop delivers), the arithmetic closed exactly: 'wall= 169.49s sim= 134.32s rate=0.792 | slept 7.17s | rebase 2807 (35.0s written off); by cause: pace 72/18.0s wake 2735/17.0s'. The 35.2 s the clock was missing and the 35.0 s the rebases wrote off are the same seconds. The same run SLEPT 7 s for being ahead while writing off 35 s for being behind -- it cannot be both, and that contradiction is the tell: leads were surrendered by rtpacer_pace's sleep and deficits destroyed by the rebase, so the error could only move one way. ROOT CAUSE in rtpacer_advance_idle: it advanced simulated time to match wall measured from p->idleStartWallSeconds, which rtpacer_enter_idle re-anchors on EVERY entry into the wait state -- so it tracked 1:1 from wherever it happened to be and faithfully PRESERVED the deficit, which the wake rebase then deleted. PASS is in the wait state 65% of its life (wait loop: 108 s wall of 169 s), which is precisely where the time can be won back: the machine is idle, nothing observable happens, the next event is determined. Measuring against the run's own origin lets a wait fast-forward up to the wall clock and never past it; cpu_advance_idle_ns still stops at the next timer expiry and the instant the wait clears, so no interrupt moves. Result 'wall= 483.62s sim= 483.62s rate=1.000 | rebase 0', wall-minus-sim gap 0.00 s sampled every 60 s across 8 minutes, and a run ending 21:30:45 wall showed 254/21:30:38 on the DEU. TWO TRAPS: (a) skipping the wait loop's sleep whenever behind AT ALL busy-spins on the clock -- 747 million passes in a 170 s run at ~15 ns per clock_gettime, measured; RTPACE_CATCHUP_MS is the threshold worth chasing. (b) Waste is not the same as the defect: batchrunner_step disassembled every instruction to a string, snapshotted the register file twice and diffed it with a strcmp per register, and ran a SECOND instr_decode per instruction (30% of all CPU) just to notice an undecodable one -- gating that and letting cpu_exec1 report decodeFailed moved the rate 0.792 -> 0.854 and none of the rest of the way.
  - csects `rtpacer,batchrunner_step,cpu_advance_idle_ns,rtpacer_advance_idle,rtpacer_rebase` &middot; tape `pass-ipl-cflm` &middot; env `YAGPC_PACETRACE,YAGPC_RANGETRACE` &middot; files `src/rtpacer.c,src/rtpacer.h,src/run.c,src/cpu.c,src/cpu.h` &middot; symptom `clock runs at 2/3 speed,13 minutes per 20,re-IPL slower than first IPL,45 s becomes 68 s,MM load takes longer after HALT` &middot; doc `none -- measured` &middot; run `pace-e,pace-k,pace-m`
- **#90** *(emulator)* With --bce-network (real MEDS2/panelO6 processes on the bus, which is what retest-crt2.sh runs) the emulator could not hold 1:1 with real time even after the rebase fix of #89: the user measured ~6 minutes lost in 38, and yaGPC2 pegged at 102% of a core. The headless in-process configuration was fine at 15% CPU, so the cost was thought to be UDP. The dominant cost was NOT UDP: the IOP was being stepped TWICE for every wait state, because cpu_advance_idle_ns() (cpu.c) carries the clock through a wait and services the IOP itself, but AP101.iopNextPassUs is in ap101.c and was never updated, so the first instruction after the wait replayed every 0.5 us pass of the whole wait.
  - evidence: ap101_step_iop() takes one IOP slice per iop_pass_us() (0.5 us -- BCE PoO 3.4.1's 16.5 us MIA sampling over 33 slices) and records the next in AP101.iopNextPassUs. cpu_advance_idle_ns() advances elapsedTimeUs through the wait and calls iop_exec_idle() every IOP_SLICE_NS (500 ns), but cannot see iopNextPassUs. PASS is in the wait state for 95% of its simulated life, so nearly every pass was a duplicate: 462 million in a 250 s run where 24 million is right, and each BCE sampled its MIA twice as often as the hardware does. ap101_iop_resync() re-anchors the pass clock; the skipped passes are exactly the duplicates because iop_exec_channel_control() is an empty stub, so iop_exec_idle() already does everything iop_exec() does. Effect: exec1 2.007 -> 0.861 us per instruction, CPU 14.4% -> 8.8%, with the SAME final machine state to the bit (FP0-FP7, PSW1/PSW2, NIA=01df8, CC, BSR, DSR) and the same mass-memory milestones. Four further per-instruction costs, each measured: (a) the discretes socket was drained on EVERY READ DISCRETE INPUT PCI and the flight software reads those in tight loops -- ~11M recvfrom() in 170 s, now rate-limited to 250 us of wall; (b) DInstr (688 bytes) was memset TWICE per instruction, once in cpu_exec1 and again as instr_decode's first statement; (c) instr_decode scanned all 135 opcode descriptors, now bucketed by hw1's top 8 bits to 3.5 candidates (YAGPC_DECODE_SELFTEST=1 proves 0 mismatches over all 65536 hw1); (d) the bus sockets were drained with a recvfrom() per bus per service, now one poll() for all of them. A full 65536-entry hw1->descriptor table was tried and is WORSE (0.479 -> 0.691 us): 512 KB indexed randomly per instruction misses cache, where the small sorted array stayed in L1. NET, with DOUBLE the display load (4 MEDS): rate 1.000 sustained, exec1 0.756 us per instruction against 1.07 us of simulated time delivered -- execution now runs at 1.4x real time instead of 0.53x -- and process CPU 15.5% -> ~10%. MEASUREMENT TRAP: stray MEDS2.py processes from killed runs stay joined to the bus and rendering (timeout kills the script, not its children); fourteen of them invalidated a series of A/B comparisons and made the bucketed decode look slower than the linear scan.
  - csects `ap101_step_iop,ap101_iop_resync,cpu_advance_idle_ns,instr_decode,iop_discrete_overlay,bcenet_framer_flush_tick,bcenet_transport_poll_ready` &middot; tape `v44` &middot; env `YAGPC_PACETRACE,YAGPC_DECODE_SELFTEST,YAGPC_BUS_SERVICE_US` &middot; files `src/ap101.c,src/ap101.h,src/cpu.c,src/cpu_instr.c,src/discretes.c,src/run.c,src/bcenet_framer.c,src/bcenet_transport.c` &middot; symptom `loses 6 minutes in 38,102% CPU with bce-network,cannot hold real time with MEDS attached,clock still slow after rebase fix` &middot; doc `AP-101S BCE PoO 3.4.1 (16.5 us MIA sampling)` &middot; run `net-n,net-v`

## Latent (real, but unreachable here)

- **#53** *(emulator)* exec_DLY (the MSC's @DLY) is a NO-OP: it ignores its count and just advances the PC. The POO: 'This instruction performs no operation other than delaying the execution of the next instruction. The time period delayed is indicated by the 18-bit EC, with a resolution of 2 microseconds. The maximum delay is roughly .54 sec.' LATENT: the PASS flight software never uses @DLY -- zero occurrences in SSSRC -- so it has never mattered, but the implementation is wrong if anything ever does.
  - evidence: src/iop_msc_instr.c exec_DLY is 'iop_incr_nia(t, 1);' and nothing else. Not to be confused with the BCE's #DLY/#DLYI, which ARE implemented (iop_bce_delay). WHY IT CAME UP, and the answer it produced: asked why the flight software builds a delay out of @RAW when @DLY exists, the answer is not 'the MSC has no delay instruction' -- it has one and never uses it. It is that FIOMDLY and FIOMCKIO must be THE SAME instruction. The disassembly of the linked monitor shows 1cb6c d800 and 1cb70 d800, byte-identical @RAW 0(1); the only difference between 'delay' and 'check for I/O complete' is the accumulator loaded first, -1 versus FIOMBCEM. Because @RAW has two exits -- +2 halfwords when met, +1 on time out -- the pair is a state machine: FIOMDLY times out and falls through into FIOMCKIO, which checks and branches to FIOMTOUT. @DLY has one exit and no condition and cannot do that. So the MSC bit is not a trick to fake a delay, it is the parameter selecting which behaviour of one instruction you get -- which is a further reason the bit must participate (entry 2).
  - addrs `1cb6c-1cb70` &middot; csects `FIOMDLY,FIOMCKIO` &middot; instrs `@DLY,@RAW` &middot; procs `MSC` &middot; files `src/iop_msc_instr.c` &middot; doc `IBM-6246556A @DLY DESCRIPTION`

## Refuted -- DO NOT RETEST

- **#3** *(iop)* The MSC is being clobbered by LOAD MSC BUSY arriving while it is already busy.
  - evidence: By design. TCVTMSC=-1 is 'BUSY BUT INTERRUPTABLE' and FIOSTMSC waits DO UNTIL=(...,NP), which -1 satisfies. The 186 'clobbers' are the intended interrupt path.
  - addrs `1a2-1a2` &middot; csects `FIOSTMSC,FIOPDISP` &middot; instrs `LOAD MSC BUSY` &middot; procs `MSC` &middot; symptom `TCVTMSC latched ffffffff` &middot; run `v51a/v51b`
- **#4** *(iop)* The MSC executes too slowly, so FCOS's next dispatch catches it mid-program.
  - evidence: IOP_PASS_US=0.1 cut clobbers 18x and phase 18 still did not load. Also sio ~= busyset at every second, so when started the MSC does issue its SIO.
  - csects `FIOSTMSC` &middot; procs `MSC` &middot; env `YAGPC_IOP_PASS_US` &middot; run `v51b/v71a`
- **#5** *(iop)* Completions are lost: FIOCMPLT / the @INT path never runs for DK transactions.
  - evidence: MSCINT fires 34-37/s throughout the stall, so completions ARE raised. The failure is dispatch-side, not completion-side.
  - csects `FIOCMPLT,FIOPDISP` &middot; instrs `@INT` &middot; procs `MSC,BCE6,BCE7,BCE8` &middot; symptom `DK buses idle` &middot; run `v64a`
- **#6** *(iop)* TCVTBCEB is stuck because XST double-frees the bus mask.
  - evidence: The mask alternates healthily (FIOPDISP+8 sets, FIOCMPLT+416 clears) and drains to 0000. No double-free.
  - addrs `152-152` &middot; csects `FIOCMPLT,FIOPDISP` &middot; env `YAGPC_WATCHHW` &middot; symptom `TCVTBCEB stuck busy` &middot; run `v66a`
- **#7** *(iop)* DMA contention starves the DK buses while mass memory transfers.
  - evidence: dmaBurst defaults true, so iop_exec_dma_queue drains the whole queue every step. The queue never backs up.
  - procs `BCE6,BCE7,BCE8` &middot; env `dmaBurst` &middot; files `src/iop.c`
- **#8** *(iop)* Mass memory head-blocks ordinary I/O in the active queue.
  - evidence: Separate queues (TCVTIOA vs TCVTMMA), and the ordinary queue is only ever 3 deep. BCE20/BCE24 are serviced normally throughout.
  - addrs `144-144,151-151` &middot; csects `FIOPDISP` &middot; procs `BCE18,BCE20,BCE24` &middot; run `v62a`
- **#9** *(iop)* The IOQE pool leaks entries over time.
  - evidence: Free list is a full 25 from t=150 to t=389, then drains in 5 s and RECOVERS to 24. A spike, not a leak.
  - addrs `14d-14d,90b2-9273` &middot; csects `FIOSVC` &middot; env `YAGPC_IOQEDEPTH` &middot; symptom `pool drains` &middot; run `v62a`
- **#10** *(iop)* DK transfers are ~10x too slow per word (1385 us/word).
  - evidence: Measurement artifact: YAGPC_DKRATE times XMIT_CMD to the NEXT XMIT_CMD and divides by words, i.e. the inter-command GAP, not transfer duration. Figures void.
  - procs `BCE6,BCE7,BCE8` &middot; env `YAGPC_DKRATE` &middot; files `src/run.c` &middot; run `v68a`
- **#11** *(iop)* The DK bus holds are message timeouts.
  - evidence: Zero RECV timeouts on buses 6/7/8 across the window; all 40 timeouts are BCE20, a bus with no model attached.
  - instrs `#RDLI` &middot; procs `BCE6,BCE7,BCE8,BCE20` &middot; env `YAGPC_TIMEOUT_TRACE` &middot; run `v69a`
- **#12** *(iop)* A DK BCE is dispatched but never started (SIO missing).
  - evidence: TCVTBCEB busy-set and SIO match exactly per bus: 42/42, 42/42, 43/43, zero unmatched.
  - addrs `152-152` &middot; csects `FIOPDISP` &middot; instrs `@SIO` &middot; procs `BCE6,BCE7,BCE8` &middot; env `YAGPC_SIOTRACE` &middot; run `v81a`
- **#13** *(iop)* Bus pacing / serial-bus occupancy: modelling the wire as busy per word (YAGPC_BUS_WORD_US, 25 or 33 us/word) will fix the IOQE pool overflow.
  - evidence: Failed three times: global wire, then per-bus, then per-bus without head-of-line blocking. Every attempt broke the boot (keys=0). Left OFF by default.
  - tape `v36` &middot; env `YAGPC_BUS_WORD_US` &middot; files `src/iop.c` &middot; symptom `keys=0,boot broken` &middot; doc `IBM-74-A31-016 (28us word + 5us gap = 33us)` &middot; run `v76-v78`
- **#16** *(emulator)* #DLY is mis-implemented (sole opcode debuting at the transition, t=399.3 on BCE18).
  - evidence: Correct on every check: address (PC+DISP+2xBCE#), the x2 HALFWORD stride, 18-bit count vs the documented 0..4.325 s, LSB-ignored alignment. No transition debut at all on the other build.
  - addrs `1d204-1d204` &middot; instrs `#DLY,#DLYI` &middot; procs `BCE18` &middot; tape `v36,pass-910` &middot; env `YAGPC_FIRSTOP` &middot; tsec `399.3` &middot; doc `BCE PoO IBM-6246556A pt3` &middot; run `v84a/v85ref`
- **#17** *(emulator)* ISPB is being discarded in problem state, so an unprotect vanishes.
  - evidence: exec_ISPB's own comment predicts this symptom, but the measured 'ISPB SKIPPED (problem state)' count is 0.
  - csects `FCMPMOD` &middot; instrs `ISPB` &middot; env `YAGPC_ISPBTRACE` &middot; files `src/cpu_instr.c` &middot; symptom `store protect violation` &middot; run `v86ref`
- **#18** *(tape)* Our tape's #PFCMGPT phase table or load blocks are wrong.
  - evidence: Decoded from PHASE02.lib: phase 3 = 10 blocks/38 tape blocks, phase 8 = 30/243, phase 18 = 8/34, all matching the cut table, descriptors internally consistent.
  - csects `#PFCMGPT` &middot; phases `3,8,18` &middot; tape `v36` &middot; files `PHASE02.lib`
- **#19** *(flight-sw)* AIGDEU's WAIT 0.018 is too short for our I/O; lengthening it will stop the fills stacking.
  - evidence: Built tape v79 with WAIT 0.100. Ran clean (keys=2, transition occurred) and changed nothing: overflow=1, minfree=0, same FIOSVC fault. At the measured rates a fill needs >100 ms. Source restored.
  - csects `AIGDEU,AIG_DEU_LOADER` &middot; phases `8` &middot; tape `v79` &middot; files `SSSRC/AIGDEU.hal` &middot; symptom `pool overflow` &middot; run `v79t`
- **#24** *(iop)* Suppressing AIG_DEU_LOADER by reporting all DEUs already loaded (YAGPC_DEUPRELOADED) will stop the 8-fills-per-DEU that exhaust the IOQE pool.
  - evidence: Valid run (keys=2, latereads=3, simtime 964 s) and the pool still drained: minfree=0, overflow=1, same FIOSVC sentinel fault at t=398.0, no 6/5/0/0. Consistent with v60a (hack on) and v67a (hack off) both showing 22 IOQEs from the same PCT 83a8. The 8 fills are not a retry storm -- an OPS transition legitimately reloads display formats, so they happen whatever ipled says.
  - addrs `80ce-8100,90b2-9273` &middot; csects `AIGDEU,AIG_DEU_LOADER,FIOSVC` &middot; procs `BCE6,BCE7,BCE8` &middot; phases `8` &middot; tape `v36` &middot; env `YAGPC_DEUPRELOADED,YAGPC_DEU_EXTRA_PRELOADED` &middot; files `SSSRC/AIGDEU.hal,src/deumodel.c` &middot; symptom `pool overflow,no 6/5/0/0` &middot; tsec `392-398` &middot; run `v88a`
- **#25** *(iop)* The MSC monitor cycle is longer than AIGDEU's 18 ms fill window (FIOMDLY delaying TCVTMTTG ticks), so every DEU fill misses its window and all eight stack.
  - evidence: Measured TCVTMTTG directly at 0x19b through the transition: dominated by 149 ticks = 4.92 ms (398 of 988 writes), only 39 writes above 18 ms. Well inside the window. The 848-tick/28 ms figure came from t=13.7 s -- the IPL-time mass-memory read, not a fill. Monitoring itself is healthy: TCVTMNTR set to a real IOQE 988 times, ~49/s.
  - addrs `19b-19c,146-146,148-148` &middot; csects `FIOPDISP,FIOMDLY,FIOMNTR,AIGDEU` &middot; instrs `@RAW` &middot; procs `MSC,BCE6,BCE7,BCE8` &middot; phases `8` &middot; tape `v36` &middot; env `YAGPC_WATCHHW,YAGPC_REPEATTRACE` &middot; symptom `pool overflow,fills stack` &middot; tsec `380-400` &middot; run `v89a`
- **#27** *(iop)* RESOLVED, not by the reading it was written for. The question was whether a DK transaction is dispatched inside a bus-6 silence -- i.e. whether FCOS dispatches and the MSC starts the BCE yet nothing reaches the wire (service starvation live), or nothing is dispatched at all (fault upstream in dispatch). NEITHER: the transaction IS dispatched, the BCE DOES run, and it FINISHES in ~0.3 ms. The silence is the wire being legitimately idle after the work is done, while the completion takes ~1053 ms to arrive.
  - evidence: See entry 22. w3's BWTRACE shows #WAT at +0.29 ms into a 1052.64 ms hold, identical to a 0.59 ms hold. Note the earlier v91a analysis of this question was inconclusive because the SIO it found 'inside' each silence sat 0.02-0.03 ms before the silence ENDED -- it was the NEXT transaction's dispatch, not one inside the gap.
  - addrs `152-152` &middot; csects `FIOCMPLT,FIOMNTR` &middot; procs `BCE6,BCE7,BCE8,BCE18,MSC` &middot; phases `8` &middot; tape `v36` &middot; env `YAGPC_BUSLOG` &middot; files `src/mmumodel.c,src/iop.c` &middot; symptom `1053 ms bus hold,pool overflow` &middot; tsec `391-400` &middot; run `v91a,w3`
- **#30** *(method)* Bus 18's multi-second continuous activity during the stall (921,749 events, 292,074 RECV_POLL over 8.54 s, vs 1,833 events / 9 polls for a normal transfer) is a runaway BCE read loop.
  - evidence: NOT an anomaly. In the bus log RECV_POLL/RECV_WORD are emitted once per 16.5 us wheel revolution while a receive is pending, so the counts measure SERVICE TICKS, not words -- 1822 events over 30.13 ms is exactly one per revolution. Each bus-18 burst matches its own MMU READY->0..READY->1 interval exactly (26 blocks: 651 ms ready-gap vs 654 ms burst; 123 blocks: 3109 ms; 115 blocks: 2906 ms; ~25 ms/block throughout). The bus-18 activity is a large read taking the time a large read takes. Do not read the bus-log event counts as word counts.
  - procs `BCE18` &middot; tape `v36` &middot; env `YAGPC_BUSLOG` &middot; files `src/run.c,gpc-buslog.py` &middot; tsec `390-401` &middot; run `v91a`
- **#36** *(method)* RETRACTED (was: 'make while a run is in flight kills it'). It does NOT. v93a and v94a were still running normally after the rebuild -- clang unlinks and recreates the output, so a running process keeps its old inode. They looked dead because gpc.log is EMPTY UNTIL EXIT (the SIMULATED TIME line is written on stop) and because a ps|grep check returned 0 spuriously.
  - evidence: Both were found alive minutes later, at 12:40 and 04:56 elapsed, and on SIGINT wrote SIMULATED TIME 584.5 s and 301.6 s. What IS true and did cost time: (1) a nohup'd headless-gpcmem.sh can be orphaned so its 'sleep SECS; kill -INT' never fires, leaving an emulator running indefinitely and its port base held, which makes the next launch fail with 'a run is already on port base N'; (2) an empty gpc.log means STILL RUNNING, not crashed -- judge progress from deu.log; (3) a scanner using re.search(r't=([0-9.]+)') silently matches the 't=0' inside 'prot=0' and reports 0, so anchor it as \\st=.
  - files `pass-run/headless-gpcmem.sh` &middot; run `v93a,v94a`
- **#39** *(iop)* REFUTED as a mechanism -- the MSC does NOT park in an 8.65 s wait. The 0x3ffff arm is real and correctly identified, but harmless: it is never waited out.
  - evidence: w4, 7932 arms of pc=1cb70 with count=262143. Time from each such arm to the MSC's NEXT repeat arm of any kind: median 33.7 ms, p90 40.0 ms, max 1621 ms, and only 3 of 7932 over 100 ms. So the monitor keeps cycling roughly every 34 ms and the 8.65 s count is not the source of the ~1053 ms completion latency. This is what the 'by design' reading predicts: if FIOMDLY's @RAW is met then every BCE is idle, so FIOMBCEM's buses are idle too and FIOMNTR6's @RAW is satisfied at once through its own +2 exit, whatever count it armed. The met-exit skipping '@LI 0' is therefore NOT a defect, and neither the mask (entry 2, refuted twice) nor this count explains the holds. WHAT THE NUMBERS NOW POINT AT, arithmetic fit only and NOT yet tested: FIOMNTR0 takes its monitor mask from the TOP ACTIVE IOQE (TCVTMIOA, 'GET I/O MONITOR MASK FROM THE TOP ACTIVE IOQE'), so the MSC watches one request at a time; at ~34 ms per monitor cycle a request 31 places down the queue would not be looked at for ~1053 ms, and 1053/34 = 31. That would make the hold a QUEUE DEPTH effect rather than a lost completion, and would move the question back to why so many IOQEs are outstanding -- the DEU fill storm -- rather than to the MSC or the BCE. Test it by recording queue depth (YAGPC_IOQEDEPTH exists) against hold length in the same run.
  - addrs `1cb6a-1cb71,14d-14d` &middot; csects `FIOMNTR,FIOMCNTL,FIOCMPLT` &middot; instrs `@RAW,@LI,@XAX,@L` &middot; procs `MSC` &middot; env `YAGPC_REPEATTRACE,YAGPC_IOQEDEPTH` &middot; files `src/iop_msc_instr.c,SSSRC/FIOMCNTL.asm` &middot; symptom `1053 ms DK holds,completion latency` &middot; tsec `380-400` &middot; doc `FIOMCNTL.asm FIOMDLY/FIOMCKIO comments` &middot; run `w4`
- **#40** *(iop)* WITHDRAWN in part. The claim that FIOMDLY and FIOMCKIO 'must be the same instruction' was over-stated: they sit at different addresses (1cb6c, 1cb70) and nothing requires identical encodings. The fall-through works because @RAW's time-out exit advances +1 into whatever follows. Both disassemble to d800 because both use displacement 0 with indexing -- a consequence, not a requirement. WHAT STANDS: exec_DLY is a no-op, and the POO documents @DLY as a real delay (18-bit EC, 2 us resolution, max ~0.54 s). LATENT: PASS uses @DLY nowhere.
  - evidence: src/iop_msc_instr.c exec_DLY is 'iop_incr_nia(t, 1);' and nothing else; the BCE's #DLY/#DLYI are separate and ARE implemented. Instruction census over SSSRC: @RAW 4 uses, @RNW 0, @RNI 0, @RAI 0, @DLY 0. So PASS uses exactly one repeat instruction for both the delay and the I/O-complete check, and the MSC bit is what distinguishes them. The POO also documents a pure-delay alternative the software does NOT take: 'A zero accumulator will never allow an @RNI or @RNW to reach their repeat conditions. Consequently, each repeat will loop until the maximum repeat count is reached.' Why @RAW was preferred over @RNW-with-zero or @DLY is not recorded in the source and should not be guessed at. NOTE on the two POO format lines: 'I=0 Count / @RAW COUNT' and 'I=1 X + Count / @RAW COUNT (1)' are the non-indexed and indexed forms of the COUNT, not a delay variant -- both still test the condition. FIOMDLY uses the indexed form because its count is computed at run time from TCVTMTTG and moved to the index register by @XAX, which a literal displacement could not carry.
  - addrs `1be4b-1be4b,14d-14d` &middot; csects `FIOMNTR,FIOSVC` &middot; instrs `@DLY,@RAW,@RNW,@RNI,@RAI` &middot; procs `MSC,BCE6` &middot; env `YAGPC_WATCHHW,YAGPC_PGMTRACE` &middot; tsec `390-397` &middot; run `w3,v91a`
- **#43** *(flight-sw)* The store-protect fault comes from an overlay's re-protection landing on memory something else still needs to write -- i.e. FIOMGCV's asynchronous verify-and-reprotect racing another writer.
  - evidence: Testable and tested against #PFCMGPT, negative. NONE of the addresses in the fault is inside any phase's load block: 080ce (the FPMSVCEP sentinel), 080d6 (the lastProt address) and 1be4b (the faulting instruction in FIOSVC) are all outside every overlay region, so their protection is part of the resident image and no overlay re-protected them. The IOQE pool itself (090b2..09273) is likewise covered by NO load block of any phase, so an overlay cannot corrupt or re-protect it. FPMIDLE, FIOCMPLT and FIOMGCV are also outside all overlay regions. The only I/O control structures any overlay covers are TCVTIOFP (0x14d) and TCVTBCEB (0x152), and only through phase 10's LB1 (00000..03c21) -- phase 10 is the IPL phase, loaded long before the transition. THE NON-RACE VERSION OF THE SAME IDEA IS NOT REFUTED AND IS THE LEADING HYPOTHESIS: see the entry on FIOMGCV's PCT.
  - addrs `080ce-08100,090b2-09273,1be4b-1be4b` &middot; csects `FIOMGCV,FIOSVC,FPMSVCEP` &middot; phases `8` &middot; run `w1`
- **#47** *(iop)* The DK completion latency is TCVTMIOA (the active-IOQE head) STALLING -- the head sitting on one entry far longer than that entry's I/O takes, so the defect would be in what advances it (FIOMGCMP/FIOCMPLT dequeuing).
  - evidence: REFUTED by direct measurement. Run z6, YAGPC_WATCHHW=146-152 covering TCVTMIOA (0x146) and TCVTBCEB (0x152) in one run so head changes correlate with bus-6 holds. Inside the 1053 ms hold at 391.833 the head changed 69 TIMES and the longest interval with it unchanged was 35.2 ms; the 2101 ms hold shows 135 changes, same 35 ms ceiling. The head is not stuck and advances briskly throughout. WHAT IS TRUE INSTEAD: the head cycles among a handful of IOQEs and bus 6's entry is almost never selected -- one check of bus 6 against 69 head changes. AND ONE ENTRY DOMINATES: address 9068 is at the head 33 of 69 times in that hold, 33 of 70 in the next, and 65 of 135 in the 2101 ms one -- about 48% consistently -- while the others (905a, 90c4, 90fa, 910c, 9130, 9166, 9178, 918a, 91d2, 922c) rotate. IOQE stride appears to be 18 halfwords. SO THE REMAINING QUESTION IS NARROWER: not why the head is slow, but why one IOQE keeps being re-selected as head while bus 6's waits. Note this is still distinct from the refuted queue-DEPTH idea, which turned on how many entries exist.
  - addrs `146-146,152-152,9068-9068,905a-922c` &middot; csects `FIOMNTR,FIOMGCMP,FIOCMPLT` &middot; procs `MSC,BCE6` &middot; env `YAGPC_WATCHHW` &middot; tsec `390-396` &middot; run `z6`
- **#51** *(emulator)* Porting upstream's coordinated set (zero receive-timeout floor + 33 us MIA receive pacing + @RAW keeping accumulator bit 0) fixes the DK holds. The reasoning was that bit 0 alone had been catastrophic because it was one item of an interdependent set.
  - evidence: REFUTED by run u1, YAGPC_IOP_UPSTREAM=1, full length. The set regresses EXACTLY as the mask alone did: IPL is healthy (all reads through 228 blocks at 128.9 s and 38 at 136.5 s), then the IOQE sentinel fault PGMCHK 0007 at=1be4b lastProt=080d6 fires at t=231.19 and 232.03 -- against x2's 230.9 for the mask alone -- no transition, no overlay landmarks, and the bus-6 hold distribution goes to 15 intervals with a 2166 ms MEDIAN and 13 over 300 ms, against a baseline of 804 intervals at 0.85 ms with 5 over 300 ms. So the pacing and the zero floor do NOT rescue bit 0, and entry 2's refutation of the mask stands on its own rather than being an artifact of isolation. WHAT IS STILL WORTH TESTING, and is now split out behind YAGPC_MIA_PACE (the zero floor needs no flag, YAGPC_RECV_FLOOR_US=0 does it): the pacing and the floor WITHOUT the mask change (run u2). Those two are independently justified -- FCMINIOP programs per-bus MTOs that our 2 ms floor overrides, and a real MIA presents one word per bus word time -- and they may help even though bit 0 does not. IF u2 IS ALSO BAD, the remaining items of 818df88 are load-bearing here and the port must be wider: the sector replacing the high-order bit of a 16-bit address, SRS/SI keeping the low bit of the base, SVC saving the sector in old-PSW bits 40-43, BALR/SCAL taking the link before the branch address, ISPB M1=001/011 pairing (EA, EA+1), @LH sign-extending, and the IOP stepping on elapsed time.
  - instrs `@RAW` &middot; procs `MSC,BCE6` &middot; env `YAGPC_IOP_UPSTREAM,YAGPC_MIA_PACE,YAGPC_RECV_FLOOR_US` &middot; files `src/iop.c,src/iop.h,src/iop_msc_instr.c` &middot; tsec `230-232` &middot; doc `nsts-sim-gpc 818df88` &middot; run `u1,u2,x2`
- **#52** *(emulator)* The ~1053 ms DK holds are caused by our receive timing -- the 2 ms RECV_TIMEOUT_FLOOR_US overriding the per-bus MTOs FCMINIOP programs, and/or the absence of 33 us MIA receive pacing.
  - evidence: REFUTED by run u2: YAGPC_MIA_PACE=1 with YAGPC_RECV_FLOOR_US=0, i.e. both corrections WITHOUT the @RAW mask change. The run is valid (transition at 390.55/392.23/397.87, phase 3's overlay completes at 391.489) and the hold distribution is unchanged from baseline: 804 intervals with 5 over 300 ms and a 4263.80 ms maximum, against baseline's 804 / 5 / 4264. Only the median moves, 0.85 -> 1.36 ms, which is the pacing showing up on short transfers as it should. Phase 8's overlay still never completes, cksum-verify still fires twice (both for phase 3's transaction), and the sentinel fault still lands at 396.91. SO THE TWO CORRECTIONS ARE SAFE AND FAITHFUL BUT ARE NOT THE FIX. They are independently justified -- FCMINIOP programs per-bus MTOs (33 us most buses, 49.5 us bus 24, 5 ms DK, 1.959936 s mass memory) that a 2 ms floor overrides 40-60x, and nsts-sim-gpc 818df88 makes both corrections -- so they are worth keeping on their own merits, but they do not touch the holds. CONTRAST with the @RAW mask, which is ALSO documented as required and which does NOT leave the machine standing (entry 2).
  - procs `BCE6,BCE24,MSC` &middot; env `YAGPC_MIA_PACE,YAGPC_RECV_FLOOR_US` &middot; files `src/iop.c,SSSRC/FCMINIOP.asm` &middot; doc `nsts-sim-gpc 818df88` &middot; run `u1,u2`
- **#66** *(flight-sw)* The zero entries in the SVC entry-point table FPMSVCEP (SVCs 0, 10, 11, 23, 28, 29; the 'unresolved FSVC00nn' relocations of every link) are a link defect.
  - evidence: All eight DASS dumps have 0000 in exactly those six entries too; the other entries match ours exactly. They are unused SVC numbers. A machine that issues one of them branches to 0 through FPMSVC (LA R2,X'80CE' / LH R2,0(R4,R2) / BAL R7,0(R2)), which is what OPS 901 on v38 does at 274.6 s -- so the question is why an unused SVC is issued, not the table.
  - addrs `80ce-80f5,1b13a-1b15a` &middot; csects `FPMSVCEP,FPMSVC` &middot; tape `v38` &middot; run `R901`
- **#69** *(emulator)* exec_BCR is wrong because 'BR 7' (BCR 7,R7) does not branch when CC=2. THE BCR IS CORRECT -- do not 'fix' it.
  - evidence: AP-101S instruction set sec. 5.3: 'bit 5 tests for a code equal 00, bit 6 tests for a code equal 11, and bit 7 tests for a code equal 01... M1 = 111 always branches' -- and the programming-notes table shows every instruction class producing only 00, 11 or 01. CC=10 arises only from the documented CC ANOMALY (Figure 2-20 note '##'): a MASKED DMA STORE PROTECT interrupt 'will set the condition code (CC) to a binary 10... This can result in erroneous GPC operation of an instruction which tries to utilize the CC... before they are set by another instruction'. Our two psw_set_cc(...,2) sites are exactly that note (cc_anomaly and cpu_signal_dma_protect_violation). MEASURED in L901 (v39, OPS 901, landmark stop at FPMIHIM t=285.438): R7 = a0ac0031, the CORRECT return into FIOLGERR, and FP$GMTIM's saved R7 is the same; the Instruction Monitor old PSW at 0x0070 is all zero, so NO interrupt was taken; PSW1 = acf88031, CC=2. So FPMGMTIM's closing c7e7 (BCR 7,R7) fell through to the next halfword, which is FPMIHIM+0 -- as the real machine would. The defect is upstream: something DMAs into protected storage.
  - addrs `1acf7,1acf8,932e,70-77` &middot; csects `FPMGMTIM,FPMIHIM,FIOLGERR` &middot; instrs `BCR,BC` &middot; tape `v39` &middot; doc `AP-101S-instruction-set.txt sec 5.3; Figure 2-20 note ##` &middot; run `L901`

## Superseded

- **#61** *(method)* BUILD PROVENANCE IS NOT KEPT: the tape that a run used cannot be traced back to the tree that made it. Staging is reused in place, so c80boot/ and c80src/ hold only what the LAST build left; 36 numbered volumes were cut across 09-08/09-09 and only the .mmv of each survives. Do not assume a phase*/ or c80boot/ artifact corresponds to any particular tape -- CHECK, by fingerprinting a few halfwords.
  - evidence: WRONG AS STATED: v36's own staging tree survives at /tmp/claude-1000/c80v36 (09-09 14:53), with a .lnk101.repro.json per phase recording every input and its md5; I searched ~/pass-build and c80boot but never c80v*. The real provenance gaps were different: the build script lived only in /tmp/claude-1000/buildtape.sh, several inputs had no recipe, one object (AIGDEU) had drifted, and dfg was an uncommitted working tree. All resolved by yaGPC2/tapebuild/ -- see the entry recording the from-source build.
  - tape `v36,v27,v35` &middot; symptom `cannot reproduce tape from tree`
