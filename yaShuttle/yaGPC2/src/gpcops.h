/* Black-box GpcOps vtable for yaGPC2 -- see yaGpcIntegration.h. Not used
 * anywhere in the standalone `gpc run` CLI path; this is purely the
 * embedding surface for a larger simulator. */
#ifndef YAGPC_GPCOPS_H
#define YAGPC_GPCOPS_H

#include "yaGpcIntegration.h"

extern const GpcOps yaGPC2_ops;

#endif
