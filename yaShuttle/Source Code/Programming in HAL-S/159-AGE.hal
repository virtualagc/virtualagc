C/ From PDF p. 159 of "Programming in HAL/S".

  AGEP:
  PROGRAM;
     DECLARE C CHARACTER(80);
     DECLARE INTEGER,
     		CASE_NUM, AGE, SEX, X;

M    READALL(5) C;

M    CASE_NUM = INTEGER(C      );
S			 1 TO 3

M    AGE = INTEGER(C      );
S		    4 TO 5

M    SEX = INTEGER(C );
S		    6

M    X = INTEGER(C       );
S		  7 TO 10

  CLOSE AGEP;
  