C/ Adapted from PDF p. 107 of "Programming in HAL/S".

  EXAMPLE_4:
  PROGRAM;
     DECLARE A ARRAY(5) SCALAR DOUBLE INITIAL(1, 2, 3, 4, 5);
     DECLARE TEMP SCALAR DOUBLE;
M    TEMP = A  ;
S            1

     DO FOR TEMPORARY T = 1 TO 4;
M       A  = A   ;
S        T    T+1

     END;
M    A  = TEMP;
S     5

     WRITE(6) 'This program rotates a 5-element ARRAY.';
     WRITE(6) 'You should see 2 3 4 5 1, but in SCALAR DOUBLE.';
     WRITE(6) A;

Y %SVCI(0);
  CLOSE EXAMPLE_4;
  