C/ From PDF p. 134 of "Programming in HAL/S."

  TEST_ROLL: PROGRAM;

  ROLL:
  FUNCTION INTEGER;
     RETURN 5 RANDOM + 1;
  CLOSE ROLL;

    DO FOR TEMPORARY I = 1 TO 5;
      WRITE(6) 'ROLL=', TAB(0), ROLL;
    END;

Y %SVCI(0);
  CLOSE TEST_ROLL;
