C/ From PDF p. 194 of "Programming in HAL/S".

  TEST_X:
  PROGRAM;
     REPLACE IO BY "10";
     DECLARE INTEGER INITIAL(0),
                RIGHT, WRONG;
C       .
C       .
C       .               
Y    ON ERROR
Z    ON ERROR$(IO:5)

        DO;
           WRITE(6) 'TEST RESULTS FOLLOW';
           
           WRITE(6) RIGHT, WRONG;
           RETURN;
        END;
     DO WHILE TRUE;
C       .
C       .
C       .
     END;
Y %SVCI(0);
  CLOSE;
  