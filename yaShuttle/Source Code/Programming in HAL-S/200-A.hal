C/ From PDF p. 200 in "Programming in HAL/S".

  A:
  PROGRAM;
Z    ON ERROR$(1:2) IGNORE;

     CALL B;
     CALL C;
  B:
  PROCEDURE;
Z    ON ERROR$(1:2)
Z       GO TO X;
    CALL C;
 X: WRITE(6) 'GOT AN ERROR';
 CLOSE B;
 C:
 PROCEDURE;
 CLOSE C;

Y %SVCI(0);
 CLOSE A;
 