C/ From PDF p. 31 of "Programming in HAL/S".

  DECLARE3:
  PROGRAM;
     DECLARE COUNTER INTEGER;
     DECLARE VECTOR,
                POSITION, VELOCITY, TORQUE;
     DECLARE NEW_CO_ORDS MATRIX,
             SPEED SCALAR,
             N INTEGER,
             WIND_FORCE VECTOR;
Y %SVCI(0);
  CLOSE DECLARE3;
  