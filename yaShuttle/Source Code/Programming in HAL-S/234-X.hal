C/ From PDF p. 234 of "Programming in HAL/S".

  X:
  PROGRAM;
  P:
  TASK;
  CLOSE P;
DEBUG `3 `4 `8
     SCHEDULE P IN 5.4 PRIORITY(49), REPEAT EVERY .03 UNTIL RUNTIME + 100;
DEBUG `3 `4 `8
  CLOSE X;
  