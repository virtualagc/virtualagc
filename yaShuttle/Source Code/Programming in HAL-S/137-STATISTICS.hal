C/ From PDF p. 137 of "Programming in HAL/S".  Surrounding brackets have 
C/ been removed from array variables, since they are not legal HAL/S syntax.

  STATISTICS:
  PROCEDURE(DATA) ASSIGN(LO_VAL, HI_VAL, MEAN);
     DECLARE DATA ARRAY(100) SCALAR;
     DECLARE SCALAR,
     		LO_VAL, HI_VAL, MEAN;
DEBUG `3`4`8
     LO_VAL = MIN(DATA);
DEBUG `3`4`8
     HI_VAL = MAX(DATA);
     MEAN = SUM(DATA) / 100;
  CLOSE STATISTICS;
  