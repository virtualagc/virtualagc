C/ From PDF p. 230 of "Programming in HAL/S".

  STARTUP:
  PROGRAM;
  GUIDANCE:
  TASK;
  
C	...

  CLOSE GUIDANCE;
  NAVIGATION:
  TASK;
  
C	...

  CLOSE NAVIGATION;
  CONTROL:
  TASK;
  
C	...

  CLOSE CONTROL;
     SCHEDULE NAVIGATION PRIORITY(60), REPEAT EVERY 1.0;
     SCHEDULE GUIDANCE PRIORITY(70), REPEAT EVERY 1 / 6;
     SCHEDULE CONTROL PRIORITY(80), REPEAT EVERY 1 / 20;
  CLOSE STARTUP;
  