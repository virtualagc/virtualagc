C/ From PDF p. 222 of "Programming in HAL/S".

  MULTI:
  PROGRAM;
     DECLARE SCALAR,
                A, B, C INITIAL(20);
                
C       .
C       .
C       .

     IF A NOT = 0 THEN
        DO;
           B = C / A;
           
C       .
C       .
C       .

        END;
  T:
  TASK;
     A = 0;
  CLOSE T;
  CLOSE MULTI;
  