C/ From PDF p. 238 in "Programming in HAL/S".

  P:
  PROGRAM;
     DECLARE ORBIT EVENT LATCHED INITIAL(FALSE);
     DECLARE ENGINE_OFF EVENT LATCHED INITIAL(FALSE);
  GUIDANCE:
  TASK;
  CLOSE;
     SCHEDULE GUIDANCE PRIORITY(70), 
        REPEAT EVERY 1 / 6 UNTIL ORBIT AND ENGINE_OFF;
  CLOSE P;
  