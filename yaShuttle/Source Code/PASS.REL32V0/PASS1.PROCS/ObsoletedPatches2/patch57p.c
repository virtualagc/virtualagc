{
  // File:      patch57p.c
  // For:       PREP_LITERAL.c
  // Notes:     1. Page references are from IBM "ESA/390 Principles of
  //               Operation", SA22-7201-08, Ninth Edition, June 2003.
  //            2. Labels are of the form p%d_%d, where the 1st number
  //               indicates the leading patch number of the block, and
  //               the 2nd is the byte offset of the instruction within
  //               within the block.
  //            3. Known-problematic translations are marked with the
  //               string  "* * * F I X M E * * *" (without spaces).
  // History:   2024-06-25 RSB  Auto-generated by XCOM-I --guess-inlines.
  //                            Checked and changed "guess" to "patch".
  //                            The original patch this is replacing seems
  //                            to be unrelated to the actual inlined code.

p57_0: ;
  // (57)          CALL INLINE("58",1,0,ADDR_FIXED_LIMIT); /* L 1,ADDR_FIXED_LIMIT*/      
  address360B = 2192;
  // Type RX, p. 7-7:		L	1,2192(0,0)
  GR[1] = COREWORD(address360B);

p57_4: ;
  // (58)          CALL INLINE("68",6,0,1,0);       /* LD 6,0(0,1)   */                   
  address360B = GR[1] + 0;
  // Type RX, p. 9-10:		LD	6,0(0,1)
  FR[6] = fromFloatIBM(COREWORD(address360B), COREWORD(address360B + 4));

p57_8: ;
}
